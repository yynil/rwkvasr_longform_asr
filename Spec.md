# RWKV Long-Form ASR Spec

## 1. Goal

Build an ASR system for long-form and real-time inference where the encoder replaces self-attention with the latest `RWKV-7 TimeMixer` design from `BlinkDL/RWKV-LM/RWKV-v7/train_temp`, while keeping the feed-forward path standard. The v1 decoding design is `joint CTC + RWKV autoregressive decoder training`.

The primary product goal is not merely a fast causal ASR model. It is a `single-checkpoint dual-mode ASR system`:
- `bidirectional` inference for higher-accuracy offline / non-streaming ASR
- `unidirectional` inference from the same weights for streaming ASR
- optional `alternating` or partially-bidirectional inference for intermediate speed/accuracy trade-offs

Primary target:
- same weights for streaming and non-streaming ASR
- efficient streaming / chunked ASR
- strong long-form robustness
- practical multi-GPU training on `4 x 4090` with `DeepSpeed`

Non-goals for v0:
- RNN-T decoder
- replacing MLP with RWKV channel mixing
- exact reproduction of the paper's WeNet + RNN-T setup

## 2. Paper Analysis

Paper: `2506.19761v1.pdf`

Title:
- `Accurate, fast, cheap: Choose three. Replacing Multi-Head-Attention with Bidirectional Recurrent Attention for Long-Form ASR`

What the paper actually shows:
- The authors keep the overall Conformer encoder structure and only swap the attention computation.
- For RWKV, they use only the time-mixing component as the replacement for MHA. They explicitly do not use RWKV channel mixing because they expect the existing feed-forward and convolution modules to cover local transformations.
- Bidirectional recurrent attention closes the accuracy gap to MHA on short-form ASR.
- On long-form ASR, RWKV-based encoders generalize better than full-context MHA when training utterances are short and decoding chunks are long.
- After long-form fine-tuning, bidirectional RWKV beats a strong limited-context attention baseline in accuracy while delivering about `44%` higher throughput; unidirectional RWKV matches that baseline with about `72%` higher throughput.
- Direction Dropout is the key mechanism that makes a single bidirectional-trained model usable in both bidirectional and unidirectional inference modes with limited accuracy loss.
- Alternating-direction decoding shows that the model can mix future and past information without requiring both directions to be active in every layer.

Numbers directly relevant to this project:
- Short-form table: unidirectional RWKV underperforms MHA, while bidirectional RWKV matches it closely.
- Long-form table: bidirectional RWKV reaches roughly the same or slightly better WER than MHA / LCA at materially higher throughput.
- Throughput table: unidirectional RWKV is fastest; bidirectional RWKV offers a better speed/accuracy point than limited-context MHA.

Implications for our design:
- The mainline model should be `bidirectional RWKV encoder + Direction Dropout training`, not a separate causal baseline.
- Unidirectional inference is a deployment mode of the same model, not a separate model family.
- The training recipe must explicitly optimize for `single-checkpoint dual-mode behavior`.
- Long-form training and chunked evaluation are not optional extras. They are core to model behavior.

## 3. Upstream RWKV-7 Constraints

Source analyzed:
- official `RWKV-LM` repo
- path: `RWKV-v7/train_temp`

Important implementation constraints extracted from upstream:
- The main reusable component is `RWKV_Tmix_x070`.
- It depends on:
  - `head_size`
  - per-layer mix parameters `x_r/x_w/x_k/x_v/x_a/x_g`
  - learned low-rank adapters for decay, value residual, in-context rate, and gate
  - `v_first` value residual passed from the first layer through the whole stack
- The fused CUDA path currently assumes:
  - `HEAD_SIZE == 64`
  - `CHUNK_LEN == 16`
  - bf16 tensors
  - contiguous layout
- The upstream module uses custom initialization and optimizer grouping:
  - `att.w0` has `2x` learning-rate scale
  - weight decay applies only to large matrix weights
  - several output projections are zero-initialized
- Upstream `train_temp` is not a reusable general-purpose layer package. We should port the TimeMixer logic and preserve the optimization rules, not embed the LM trainer unchanged.

## 4. Product Requirements

### Functional
- Train a joint `CTC + RWKV decoder` ASR system with RWKV-7 TimeMixer replacing encoder self-attention.
- Support offline full-utterance training and chunked streaming-style inference from the same checkpoint.
- Support variable-length batches and long-form utterances.
- Support an official RWKV tokenizer that is shared with the decoder checkpoint.
- Support configurable text tokenizers:
  - Whisper multilingual BPE
  - SentencePiece Unigram via external `.model`
  - Qwen3 tokenizer via external `tokenizer.json`
- Save and load model architecture via YAML config files for training and prediction workflows.
- Save and load tokenizer settings via a separate `tokenizer_config.yaml` so prediction defaults stay aligned with the training checkpoint.
- Export inference-friendly `safetensors` weights from `.pt` checkpoints for deployment-oriented prediction workflows.
- Export Rust-consumable `CTC logits + lengths + utt_ids` artifacts from Python model forward passes so the first Candle predictor can be validated before full RWKV Rust forward parity exists.
- Expose inference modes:
  - left-to-right streaming
  - bidirectional offline
  - alternating directions
  - configurable subset of bidirectional layers

### Performance
- Mixed precision `bf16`
- DeepSpeed multi-GPU training on 4 GPUs
- kernel-aware path for RWKV-7 when shapes and dtypes are compatible
- fallback pure PyTorch path for correctness and unit tests
- on CUDA, runtime activations should stay in `bf16` outside numerically sensitive reduction paths
- WebDataset length bucketing for joint CTC+AR training should use a single combined cost, not audio-only frames:
  - `combined_cost = audio_frames + text_cost_weight * estimated_text_tokens`
  - exact text token counts are used when present in the length index
  - otherwise the bucket builder may use text character/byte counts or a bounded `json_size` proxy
  - this intentionally stays one-dimensional to avoid complex per-rank max-audio/max-text balancing while reducing obvious AR decoder stragglers

### Research
- Establish the dual-mode single-checkpoint path first.
- Keep interfaces compatible with:
  - fixed-rate paper-reproduction Direction Dropout
  - optional non-default Direction Dropout schedules for ablations
  - unidirectional / bidirectional / alternating inference from one checkpoint
  - per-layer direction masks at inference

## 5. Proposed Architecture

### 5.1 Encoder

Base shape:
- input: `B x T_audio`
- features: 80-dim log-Mel filterbanks
- frontend default: `global_cmvn + conv2d6` over 80-dim fbank features
- subsampling: WeNet-style `conv2d6` subsampling
- encoder hidden size: initial default `d_model = 512`
- FFN size: initial default `d_ff = 2048`
- layers: initial default `12`
- RWKV head size: `64`
- RWKV attention dim: `512`

Encoder block:
1. macaron FFN
2. bidirectional RWKV-7 TimeMixer in place of self-attention
3. depthwise convolution module
4. post FFN
5. layer norms and residual paths in Conformer style

Rationale:
- This matches the paper's main modeling choice: change the attention computation, keep the rest of the encoder structure.
- It also keeps a strong ASR inductive bias for local acoustic modeling.

### 5.2 RWKV TimeMixer Adaptation

Adaptation rules:
- Input is acoustic hidden states, not token embeddings.
- We port the `RWKV_Tmix_x070` equations into an ASR module.
- We do not port `RWKV_CMix_x070` into the encoder block for v0.
- We preserve `v_first` propagation across encoder layers.
- We define two execution backends:
  - `native`: pure PyTorch reference implementation
  - `cuda`: upstream-compatible fused CUDA extension path using RWKV-7 `wind_backstepping`

CUDA backend rules:
- Source is vendored from `BlinkDL/RWKV-LM/RWKV-v7/train_temp/cuda` under Apache-2.0 with local attribution.
- The first CUDA backend target is training full-sequence `state=None` TimeMixer calls.
- Streaming / chunked inference with an incoming recurrent state remains on the native backend until a stateful fused kernel wrapper is added.
- Runtime requirements:
  - CUDA tensors
  - bf16 contiguous `w/q/k/v/z/a` tensors
  - `head_size = 64`
  - `chunk_len = 16`
- If the CUDA backend is requested but these requirements are not met, training must fail loudly or fall back only for explicitly unsupported stateful inference paths; it must not silently run the slow native path for the main training loop.
- Before large training runs, validate the active CUDA toolchain with `rwkvasr-check-rwkv7-cuda` or `python -m rwkvasr.cli.check_rwkv7_cuda`; this compiles the extension and compares fused output against the native reference on a small tensor.

2026-06-01 upstream kernel update note:
- Upstream `BlinkDL/RWKV-LM` `RWKV-v7/train_temp` commit `f3a67cacc585ba86d11d0e345358405fae550225` advertises faster training kernels.
- On the target `RTX 4090 / sm_89` toolchain, `rwkv7_clampw_v3_for_h100` and `rwkv7_clampw_v3_for_h100_alt` compile and run, but `rwkv7_tmix_mix6_bf16_v5` fails to compile because `atomicAdd(float2*, float2)` is not available for this architecture/toolchain.
- The short-term supported optimization is therefore a `cuda_clampw` backend that uses only `rwkv7_clampw_v3` for the RWKV recurrence and keeps the rest of the TimeMixer graph unchanged.
- `cuda_clampw` takes the raw upstream `w` logits and performs the soft clamp plus `exp(-exp(w))` decay inside the fused kernel. It must be validated against the native reference before replacing the existing `cuda` backend in training configs.
- The broader upstream TimeMix fusion kernels remain an experimental future task until a 4090-compatible atomic implementation or upstream sm89-compatible variant is available.

Directional design:
- Each encoder block contains:
  - an `L2R` TimeMixer branch
  - an `R2L` TimeMixer branch operating on the reversed sequence
- The default merge is elementwise average after restoring the reversed branch to original order.
- Training-time direction masks can disable one branch for a block.
- Inference-time direction masks can select:
  - all bidirectional blocks
  - all unidirectional blocks
  - alternating directions
  - only the last `N` blocks bidirectional

Current implementation status:
- native TimeMixer core is implemented and is the correctness reference
- bidirectional wrapper, Direction Dropout, CTC training, WebDataset bucketing, and prediction are implemented
- fused CUDA TimeMixer is being added as the primary Emilia-scale training backend because native recurrence is too slow for multi-million-hour-step schedules

### 5.3 Direction Dropout

Primary training mechanism:
- During training, each bidirectional RWKV block can drop one direction according to a scheduler.
- For strict paper reproduction, Direction Dropout should use a fixed dropout rate during training rather than a progressive schedule.
- Supported variants:
  - `drop_r2l_only`: optimized for L2R streaming deployment
  - `drop_both`: randomly drop either direction for symmetric dual-mode behavior

Training-time outputs:
- same checkpoint should remain usable under `Bi`, `L2R`, and `Alt` inference
- we will log mode-specific dev WER during training

### 5.4 Positional / Temporal Handling

Decision:
- Do not add Transformer-style relative self-attention position bias, because TimeMixer already injects temporal mixing through shift-based recurrence.
- Keep convolution module for local temporal inductive bias.

Open point:
- We may still need lightweight positional scaling around subsampling or conv modules if convergence is unstable. This remains an ablation item.

### 5.5 CTC Head

Head:
- encoder output projection to vocabulary size plus blank
- CTC loss during training
- greedy decode for lightweight evaluation sanity checks
- standalone `CTC prefix beam search` for prediction and later hotword-bias integration
- standalone `CTC prefix beam search` must support optional decode-time hotword biasing
- beam size must be configurable from the prediction CLI and YAML-driven model reconstruction path
- prediction CLI should expose decode-time insertion / length bonus so CTC deletion bias can be inspected without retraining
- prediction CLI should expose:
  - `hotwords_path`
  - default hotword weight
  - partial-match / prefix hotword bonus scale
- prediction should also support an optional `CTC n-best + RWKV decoder rescoring` branch for joint checkpoints:
  - pure CTC remains the default baseline path
  - rescoring is a decode-time comparison path and must not change training losses or checkpoint format
  - initial combination rule should use token-length-normalized scores:
    - `ctc_avg_score = ctc_beam_score / max(1, token_count)`
    - `decoder_avg_score = autoregressive log-prob per target token`
    - `combined_score = ctc_avg_score + decoder_rescore_weight * decoder_avg_score`
  - rescoring should only reorder candidates inside the `CTC n-best`; it must not invent candidates outside the CTC beam
- prediction outputs must support token-level CTC time alignment, not only token ids / text
- labeled prediction outputs should optionally include decode diagnostics:
  - input feature length
  - encoder / logit length
  - predicted token count
  - reference token count
  - blank-top1 ratio
  - average blank probability
- joint decoder checkpoints must also support a separate `direct RWKV decoder AR` prediction path:
  - `encoder latents -> RWKV decoder -> token generation until EOS=0 or max_len`
  - this path is distinct from CTC decode and must not depend on CTC hypotheses
  - sidecar evaluation for joint decoder training must compare `pure CTC` against `pure RWKV decoder AR`
  - the default sidecar cadence for the active joint run is `every 60 minutes`
  - sidecar checkpoint comments must include aggregate metrics, CTC / AR text predictions, matching ground truth text, and a short per-sample interpretation so failure modes can be inspected without opening multiple preview files
  - decode-time `CTC n-best + decoder rescoring` may remain as an auxiliary experiment, but it is not the mainline evaluation path for joint decoder checkpoints

Why CTC first:
- simpler than RNN-T
- lower latency
- easier streaming path
- easier profiling of encoder gains from RWKV replacement

### 5.6 RWKV Decoder

Primary decoder design:
- use official `RWKV-7` decoder weights from `BlinkDL/rwkv7-g1`
- first target checkpoint: `rwkv7-g1d-0.1b-20260129-ctx8192.pth`
- use official RWKV tokenizer assets from the upstream RWKV-7 path so decoder token ids remain checkpoint-compatible

Decoder role:
- consume text targets autoregressively during training
- consume acoustic encoder outputs through an encoder-to-decoder conditioning path
- provide an auxiliary / joint autoregressive loss that complements CTC
- provide a direct autoregressive inference path at evaluation time using the same conditioning path and EOS stop rule

Compatibility rule:
- the `CTC vocabulary` and the `RWKV decoder vocabulary` must be the same token id space
- we must not reuse Whisper tokenizer for this path because the official RWKV decoder checkpoint would become unusable
- CTC keeps the official tokenizer ids unchanged for text tokens and appends exactly one extra `blank` class outside the tokenizer range
- the decoder path must not invent tokenizer-side BOS / EOS ids that collide with the official vocabulary
- for `rwkv_vocab_v20230424`, upstream generation demos use `token_id == 0` as the stop / EOS condition even though the vocab text file itself does not auto-append EOS
- therefore our ASR joint RWKV path must treat `0` as an explicit training-time EOS token and append it to every target sequence exactly once
- the tokenizer itself remains a raw byte-piece encoder; EOS insertion is a dataset / training policy, not an implicit tokenizer side effect
- direct decoder inference uses:
  - acoustic prefix from encoder latents
  - tokenizer-encoded instruction prompt around the acoustic prefix
  - token generation strategy is configurable:
    - greedy decoding
    - sampling via `do_sample`, `temperature`, `top_k`, `top_p`
  - sidecar AR preview/eval can switch to sampling mode when quality-focused decoding is desired
  - explicit stop when token `0` is emitted
  - configurable `max_new_tokens` guard to avoid runaway decoding
- the joint RWKV decoder must preserve the pretrained LM prompt distribution as much as possible:
  - default prompt template:
    - `User: Transcribe the audio to text in its own language.\n`
    - `<_audio_>` is replaced by projected encoder latents
    - `\nAssistant: `
    - target text followed by `\n` and `EOS=0`
  - AR loss labels are `-100` for all prompt positions, including the audio-latent replacement span
  - AR loss is computed only on the target answer span: `TARGET TEXT + decoder_target_suffix + EOS`
  - direct AR inference must use the same prompt template before generation

Initial scope:
- validate official RWKV code path and tokenizer first
- integrate a minimal ASR decoder-conditioning path second
- keep decoder size fixed to the official `0.1B` checkpoint for the first experiment

Conditioning design for v1:
- encoder produces acoustic states after the ASR frontend and RWKV encoder
- every valid encoder time step is projected into the RWKV decoder hidden interface and inserted at the `<_audio_>` position in the prompt template
- no temporal pooling, fixed-prefix compression, masked mean pooling, or learned resampling is allowed in the mainline AR decoder path because it discards acoustic detail and makes long utterance decoding worse
- decoder training uses teacher forcing on text tokens after the full audio context and assistant prompt
- teacher-forced decoder targets must include the explicit `EOS=0` token so the LM learns when to stop
- logits and AR labels are trained only on the target answer span, not on prompt or audio-context positions
- the implementation may compute decoder hidden states over the full prompt + audio + target sequence, but should apply the large vocabulary LM head only to loss / generation positions to avoid materializing unnecessary `B x audio_T x vocab` logits
- this is preferred over adding cross-attention because:
  - it preserves the official RWKV block structure
  - it keeps checkpoint loading straightforward
  - it keeps the pretrained LM's prompt-continuation interface intact while still conditioning on the complete audio sequence

Autoregressive inference rule:
- the mainline decoder inference path is `encoder latents -> RWKV decoder -> autoregressive generation until EOS or max_len`
- `CTC n-best + decoder rescoring` may exist as an auxiliary experiment, but it is not the primary decoder design and must not be treated as the training-aligned decode path
- AR quality reporting and sidecar preview default to sampling (no greedy path), with a conservative ASR-oriented setting:
  - `temperature=0.8`
  - `top_k=5`
  - `top_p=0.8`

Full-audio AR conditioning rule:
- `decoder_audio_conditioning: full` is the only supported joint-decoder mode
- all valid encoder output frames must be sent to the RWKV decoder in temporal order
- padded encoder frames must be excluded from the per-sample context via `encoded_lengths`
- `decoder_prefix_tokens` is retained only as a backward-compatible config field and must not cause temporal compression in full-audio mode
- direct AR inference uses the same full-audio prompt context before greedy token generation

Hotword bias design for v0:
- apply hotword bias only in decoding, not in training
- hotwords are provided as text entries and tokenized with the checkpoint tokenizer
- bias should affect beam ranking during prefix beam search so rare / difficult terms survive pruning
- support complete-match bonus and lighter partial-prefix bonus
- file format should stay simple:
  - one hotword per line
  - optional per-line weight via tab-separated `text<TAB>weight`
- initial use cases:
  - domain entities
  - numerals / lexicalized number phrases
  - benchmark hard words discovered from English / Chinese error analysis

### 5.7 Dual-Mode Inference

Target behavior:
- `Bi` mode for non-streaming ASR with both directions enabled
- `L2R` mode for streaming ASR using the same checkpoint with only the forward direction enabled
- `Alt` mode for intermediate cost / accuracy trade-off
- optional mixed mode where only selected layers are bidirectional

Design rule:
- Mode switching must not require weight conversion or checkpoint rewriting.

### 5.8 Streaming Inference

Target behavior:
- `L2R` mode consumes audio in chunks
- cache convolution states and RWKV recurrent states across chunks
- output partial CTC hypotheses incrementally

State interfaces needed:
- subsampling cache
- conv cache
- RWKV recurrent state per layer
- optional emitted-token post-processing state for CTC collapse
- token-alignment post-processing state for incremental / full-utterance CTC timestamps

Note:
- Upstream `train_temp` focuses on full-sequence LM training. We will need:
  - an ASR-specific recurrent-state API for chunked `L2R` inference
  - a separate non-streaming execution path for `Bi` / `Alt`

## 6. Training Design

### 6.1 Data

Expected pipeline:
- manifest-based dataset
- WebDataset tar-shard dataset
- audio loading via `torchaudio`
- on-the-fly fbank extraction

### 6.2 Joint Objective

Training objective for the first RWKV-decoder experiment:
- `loss = 0.5 * ctc_loss + 0.5 * rwkv_ar_loss`

Rules:
- start from joint training directly instead of first training CTC-only and then adding the decoder
- keep the encoder architecture unchanged while adding the decoder path
- keep loss weights configurable, but default the first experiment to `0.5 / 0.5`
- keep step checkpointing frequent enough that prediction can be run mid-epoch
- keep the encoder hidden size independent from the decoder hidden size; v1 will use a learned projection from encoder hidden states to decoder hidden states
- decoder text loss uses the official tokenizer vocabulary size, while the CTC head uses `text_vocab_size + 1` because of the extra blank
- when the shared tokenizer is `rwkv_vocab_v20230424`, every training target sequence must be normalized to `text_tokens + [0]`
- EOS appending must be idempotent so pre-tokenized manifests / metadata that already end with `0` are not double-appended

Initial schedule:
- first experiment may run for `1 epoch`
- also save step checkpoints inside the epoch so the decoder path can be inspected before the full epoch ends
- continuing training from any step checkpoint must stay supported

Joint decoder batch-budget rule:
- joint `CTC + RWKV decoder` training must not budget batches with `sum(target_lengths)` only
- decoder-side text loss memory is driven by `batch_size * padded_text_length * vocab_size`
- therefore the batch selector must use padded decoder text positions when decoder loss is enabled
- full-audio conditioning length is represented by the audio-frame side of the budget; the selector should not add a synthetic fixed acoustic prefix term
- joint decoder training must also support an explicit padded-text cap independent of audio, because a batch of many moderate-audio but long-text samples can still OOM even when the combined audio+text budget fits
- in practice the batch selector should enforce both:
  - a combined budget over audio + decoder text
  - a separate decoder text budget over `batch_size * max_text_tokens` plus prompt / suffix / EOS text overhead

Decoder loss memory rule:
- decoder AR loss must not materialize a single full-sequence fp32 cross-entropy tensor when `vocab_size=65536`
- decoder AR loss must also avoid a full `reshape(-1, vocab_size)` or other whole-logit contiguous copy before chunking
- compute decoder cross-entropy in chunks over decoder time positions first, then evaluate CE on each chunk so memory scales with `batch_size * time_chunk * vocab_size`

### 6.3 Official RWKV Hyperparameter Rule

Decoder-side optimization and loading must follow upstream guidance:
- use official RWKV code paths and parameter naming when loading the decoder checkpoint
- reuse official tokenizer assets
- keep official optimizer / LR-scaling conventions wherever decoder parameters are trained
- do not silently swap to a different tokenizer or remap token ids

Validation gate before ASR integration:
- download the official checkpoint locally
- run official `rwkv_v7_demo_fast.py` or an equivalent upstream-accurate script successfully
- verify tokenizer encode/decode is consistent with the checkpoint
- only then start ASR integration work
- optional SpecAugment
- Whisper multilingual tokenization
- optional SentencePiece Unigram tokenization with externally provided `.model`

WeNet `examples/gigaspeech/s0` alignment:
- resample to `16k`
- `fbank` with:
  - `num_mel_bins = 80`
  - `frame_length = 25ms`
  - `frame_shift = 10ms`
  - `dither = 1.0`
- optional `SpecAugment`
- `global_cmvn`
- encoder input layer uses `conv2d6` subsampling before the encoder blocks

Qwen3-ASR / AuRWKV feature alignment:
- `frontend_type: aut_rwkv` should use `feature_extractor_type: qwen3_asr` unless an ablation explicitly says otherwise
- Qwen3-ASR-compatible frontend uses Whisper-style log-mel extraction:
  - `sample_rate = 16000`
  - `feature_size = 128`
  - `n_fft = 400`
  - `hop_length = 160`
  - `dither = 0.0`
  - log-mel dynamic range compression: clamp to `max - 8.0`, then `(log_spec + 4.0) / 4.0`
- Qwen3-ASR feature extraction does not use WeNet global CMVN; `cmvn_file` should remain `null` for this path
- training must save the chosen `feature_extractor_type` in `model_config.yaml` so prediction / evaluation use the same audio frontend as training
- when warming AuRWKV from Qwen3-ASR-0.6B, only matching non-attention encoder tensors are copied; RWKV TimeMixer parameters remain initialized by the RWKV ASR implementation
- AuRWKV / Qwen3-ASR encoder is the required ablation against SenseVoiceRWKV for the OmniASR tokenizer CTC curriculum:
  - keep the same `facebook/omniASR-CTC-7B` SentencePiece CTC tokenizer (`vocab_size: 9812`, `blank_id: 9812`, EOS disabled)
  - keep the same cumulative difficulty stages built from the Omni tokenizer artifact
  - use `frontend_type: aut_rwkv`, `feature_extractor_type: qwen3_asr`, Qwen3-ASR encoder dimensions, and `encoder_init_checkpoint_path: assets/qwen3-asr-0.6b`
  - keep `decoder_enabled: false`, `ctc_loss_weight: 1.0`, and `decoder_loss_weight: 0.0`
  - compare to SenseVoiceRWKV by stage-level normalized WER/CER and preview examples, not only by train/eval loss
- Qwen3-ASR Transformer encoder is the required control run after the AuRWKV Omni CTC ablation fails:
  - keep the exact same clean CTC curriculum root, OmniASR SentencePiece CTC tokenizer, stage manifests, feature extractor, CTC-only loss, and evaluation command as the AuRWKV run
  - use `frontend_type: qwen3_transformer`, Qwen3-ASR encoder dimensions, and `encoder_init_checkpoint_path: assets/qwen3-asr-0.6b`
  - preserve Qwen3-ASR audio encoder structure for this control: 3x stride-2 Conv2D frontend, sinusoidal positions, 18 Transformer encoder layers with Q/K/V/out self-attention, standard FFN, layer norms, and post projection
  - load all matching Qwen3-ASR audio encoder tensors, including attention projection weights; only the new CTC head remains randomly initialized
  - this control run is not a mainline architecture change; it exists to separate BiRWKV replacement problems from tokenizer / data / CTC training recipe problems
- Clean-data BiRWKV diagnostic after both AuRWKV and Transformer controls fail on the CommonVoice-heavy mixed curriculum:
  - keep `frontend_type: aut_rwkv`, `feature_extractor_type: qwen3_asr`, Qwen3-ASR encoder dimensions, and the OmniASR CTC tokenizer
  - train on `/media/usbhd/training_data/asr/curriculum/clean_ctc_voxbox_webdataset/stages/easy_clean_librispeech_aishell3`, which contains LibriSpeech + AISHELL-3 and excludes CommonVoice
  - keep output under `/media/usbhd/rwkvasr_runs` because the repository root filesystem is near capacity and DeepSpeed rank checkpoints are large
  - evaluate on the same stage's stable-hash eval split with normalized WER/CER and preview examples
  - if this clean-data run improves materially, prioritize data mixture, language balance, and target normalization before further architecture changes

SenseVoice / SenseVoiceRWKV alignment:
- `frontend_type: sensevoice_rwkv` is an ASR-first encoder path based on `SenseVoiceEncoderSmall`, with SANM self-attention replaced by bidirectional RWKV-7 TimeMixer.
- Existing checkpoints use `feature_extractor_type: sensevoice_lfr_fbank`, but Nano encoder/logit alignment must use `feature_extractor_type: funasr_wav_frontend` so Kaldi fbank, FunASR left/right LFR padding, waveform scaling, and frame lengths share one implementation:
  - sample rate `16000`
  - 80-bin Kaldi fbank
  - frame length `25ms`
  - frame shift `10ms`
  - hamming window
  - low-frame-rate stacking `lfr_m = 7`, `lfr_n = 6`, yielding 560-dimensional encoder input
- SenseVoice / Fun-ASR-Nano's local config uses `audio_encoder_downsample_rate: 6`, `audio_adaptor_conf.downsample_rate: 1`, and `ctc_decoder_conf.downsample_rate: 1`; its CTC path therefore keeps roughly one encoder step per 60ms LFR frame rather than the Qwen3-ASR / AuT 8x Conv2D frontend's roughly one step per 80ms.
- SenseVoice / Fun-ASR-Nano also sets `ctc_target_normalize: true`; for this project, CTC-only curriculum labels must use a CTC-specific text normalization that removes punctuation / symbol characters and common non-speech annotation tags before tokenizer encoding.
- CTC alignability filtering must use the same frontend time step formula as training: `aut_conv2d8` uses three ceil-halving steps, while `sensevoice_lfr6` uses `ceil(num_10ms_fbank_frames / 6)`.
- The first SenseVoice block consumes 560-dimensional LFR features and projects them to the 512-dimensional encoder stream through the replaced attention path. In Nano-alignment mode this projection is initialized by the joint Q/K/V rank-512 basis described below; outside that mode it remains newly initialized.
- Reusable SenseVoice weights are limited to non-attention tensors whose shapes and semantics still match:
  - encoder feed-forward `w_1 / w_2`
  - block `norm1 / norm2`
  - `after_norm`
  - `tp_encoders` feed-forward and norms when the TP block count is kept
  - `tp_norm`
- FunASR-Nano Q/K/V warm-start for SenseVoiceRWKV is now an explicit alignment mode, superseding the earlier blanket prohibition on copying SANM attention projections:
  - keep RWKV-7 TimeMixer as the temporal operator; this is parameter initialization and teacher-state supervision, not a SANM/self-attention fallback
  - for Nano encoder layers `1..69`, split `linear_q_k_v.weight` into Q/K/V and initialize both BiRWKV directions as `Q -> receptance`, `K -> key`, and `V -> value`; initialize both RWKV output projections from SANM `linear_out.weight`
  - Nano uses QKV/output biases while upstream RWKV-7 projections are bias-free; do not add projection biases or change the upstream TimeMixer parameterization merely to absorb them, and report those tensors as intentionally unmatched
  - the first Nano block maps `560 -> 3x512`, while RWKV first maps `560 -> 512 -> R/K/V`; initialize the shared `560 -> 512` projection with the top rank-512 right-singular basis of concatenated Q/K/V, then project each Nano Q/K/V matrix into that basis. Record Q/K/V reconstruction errors and reject the initialization if they are non-finite or materially larger than the measured ModelScope reference (`4.95% / 4.54% / 2.85%` relative Frobenius error)
  - copy the exactly compatible Nano FFN `w_1/w_2`, `norm1/norm2`, `after_norm`, and `tp_norm` tensors for all 70 mapped blocks. Keep `fsmn_block` unmatched because there is no parameter-equivalent RWKV tensor; its SANM temporal behavior must be learned through hidden-state alignment
  - preserve all RWKV-7-only decay, time-mix, in-context-learning-rate, gate, GroupNorm, and residual parameters plus upstream optimizer handling; QKV warm-start must not silently zero or replace them
- Layerwise Nano-encoder alignment is required before another logits-only continuation:
  - the teacher and student have an exact `50 main + 20 TP = 70` block index mapping, including `after_norm` between blocks 49 and 50 and `tp_norm` after block 69
  - expose and align three tensors for sampled mapped layers: temporal branch output (`SANM attention+FSMN` versus BiRWKV TimeMixer), FFN branch output, and full block output after both residual sub-blocks
  - use deterministic cyclic layer sampling so every layer receives supervision while limiting teacher-record and transfer memory; always report per-layer coverage and keep fixed boundary-layer diagnostics for layers `0, 49, 50, 69`
  - phase 1a trains the BiRWKV temporal path and first-layer projection with copied FFN/Norm frozen, using normalized hidden MSE plus cosine loss on temporal branches and a lower-weight FFN diagnostic; phase 1b adds full-block hidden alignment and may unfreeze FFN/Norm only if copied-FFN error does not fall with temporal alignment
  - all valid frames remain represented, but teacher nonblank neighborhoods may receive extra weight; do not drop blank/background frames entirely because this stage targets the encoder manifold rather than CTC emissions
  - phase 1 gates use fixed-audio per-layer normalized MSE/cosine, boundary discontinuity at main/TP transition, feature/frame-length parity, and gradient/update audits. Do not advance to logits distillation merely because train loss decreases
  - phase 2 starts from the best phase-1 hidden-aligned checkpoint, loads the verified Nano five-layer CTC decoder/head, keeps a decaying layerwise hidden anchor, and adds final CTC logits/sequence/local-emission losses. Promotion remains normalized public English WER and Chinese CER against the original Nano CTC output
  - online hidden/logit alignment should pass the already-collated `funasr_wav_frontend` features directly to Nano's audio encoder and CTC path. Clone them before teacher forward because `SenseVoiceEncoderSmall` scales encoder input in-place. This avoids duplicate tar extraction, duplicate frontend work, independent dither noise, and accidental frame mismatch; audio-path inference remains a diagnostic fallback only
  - a 2026-07-19 real LibriSpeech audit found equal 280-frame lengths but exposed the previous frontend mismatch: legacy `sensevoice_lfr_fbank` versus the Nano encoder input had framewise RMSE `2.7904` and mean cosine `0.98270`, while `funasr_wav_frontend` versus Nano had RMSE `0.1036` and cosine `0.99996` after correcting an audit-hook storage alias. Stage209 therefore did not provide identical teacher/student encoder inputs, and its result cannot be used to reject the distillation objective or BiRWKV structure
- SenseVoiceRWKV CTC targets for the main SenseVoice LFR curriculum should use the SenseVoice/Fun-ASR-Nano multilingual tiktoken dictionary:
  - `tokenizer_type: sensevoice_tiktoken`
  - `tokenizer_model_path: assets/fun-asr-nano-2512/multilingual.tiktoken`
  - `vocab_size: 60515`
  - `blank_id: 60515`
  - `tokenizer_append_eos: false`; CTC targets are acoustic label sequences and must not include an EOS token
  - rationale: on a 2026-06-05 80k-sample smoke over `easy_clean_librispeech_aishell3`, `sensevoice_tiktoken + text_normalization=ctc + sensevoice_lfr6` kept all 80k samples, while `OmniASR SentencePiece + sensevoice_lfr6` kept 75,186 and `OmniASR SentencePiece + aut_conv2d8` kept 64,121; the SenseVoice dictionary has much lower English token density and does not expose an unknown id through the current tiktoken wrapper.
  - the formal 2026-06-05 easy-clean stage at `easy_clean_librispeech_aishell3_ctc_norm_aligned_sensevoice_lfr6_svtok` processed and kept all 285,302 samples: 222,040 English LibriSpeech samples and 63,262 Chinese AISHELL-3 samples, with zero CTC-alignability drops.
- The Meta Omnilingual ASR CTC SentencePiece tokenizer remains the small-head ablation:
  - `tokenizer_type: sentencepiece`
  - `tokenizer_model_path: assets/omnilingual-asr-ctc/omniASR_tokenizer.model`
  - `vocab_size: 9812`
  - `blank_id: 9812`
  - it must be paired with `text_normalization: ctc` and unknown-token filtering; otherwise punctuation / symbol targets can become `<unk>` and destabilize first-stage CTC alignment
  - its smaller output head may help optimization, but its English character-level behavior is token-dense and should not be combined with aggressive 8x downsampling for first CTC alignment runs
- Joint CTC + RWKV AR training needs a dual-target path if CTC keeps SenseVoice tokens while AR keeps the RWKV decoder vocabulary. Do not force a single shared RWKV vocabulary for CTC-only curriculum runs.
- SenseVoiceRWKV random-initialized encoder is a required ablation when partial SenseVoice non-attention warm-start underperforms:
  - keep the same SenseVoice LFR fbank frontend, tokenizer assignment for the current training phase, decoder checkpoint when AR is enabled, CTC/AR loss weights, sidecar sampling settings, and normalized WER/CER metrics
  - set `encoder_init_checkpoint_path: null` so the entire encoder, including FFN/Norm paths, starts from the project initialization rather than Fun-ASR-Nano weights
  - compare against the partial warm-start run by approximate samples seen, not only by optimizer step, because batch size and token budget may differ across runs
  - keep the old warm-start checkpoints and sidecar reports for direct regression analysis
- SenseVoiceRWKV CTC curriculum is the next diagnostic path when random and partial warm-start runs both fail to build robust CTC alignment on noisy long-form data:
  - create a clean, short-utterance WebDataset curriculum root from local voxbox LibriSpeech, AISHELL-3, and CommonVoice Chinese/English sources
  - filter out source metadata splits such as `test`, `dev`, `valid`, `validation`, and `eval` before training; public benchmark/eval samples must not leak into the curriculum train root
  - use two curriculum stages rather than one mixed source distribution:
    - stage 1 `easy_clean`: LibriSpeech + AISHELL-3 only, via symlinked shards and a dedicated `webdataset_index.json`, `webdataset_lengths.jsonl`, and bucket manifest; this is the basic CTC alignment stage
    - stage 2 `clean_plus_commonvoice`: the full filtered root, adding CommonVoice EN/CN after the model shows non-degenerate CTC predictions on stage 1
  - keep `decoder_enabled: false`, `ctc_loss_weight: 1.0`, and `decoder_loss_weight: 0.0` for the curriculum stage so AR language-model loss cannot hide weak acoustic alignment
  - use the SenseVoice/Fun-ASR-Nano multilingual tiktoken dictionary for the main SenseVoice LFR curriculum and keep EOS disabled
  - use `text_normalization: ctc` for CTC-only curriculum targets:
    - convert GigaSpeech-style punctuation markup first, then remove all Unicode punctuation / symbol characters from the target
    - lowercase / casefold text content, collapse whitespace, and remove spaces between adjacent Chinese characters
    - CTC target text should match the normalized WER/CER content metric rather than a punctuated display transcript
  - filter out samples whose tokenizer output contains the tokenizer's unknown id; unknown tokens are not valid acoustic labels for first-stage CTC alignment and can dominate English character-level targets when punctuation is kept
  - build normal project artifacts for the filtered root: `webdataset_index.json`, `webdataset_lengths.jsonl`, `webdataset_lengths.summary.json`, and `webdataset_buckets_audio_text/manifest.json`
  - build a difficulty analysis artifact before creating finer curriculum stages:
    - read each sample's JSON metadata through the existing length-index tar offsets rather than decoding audio
    - compute runtime-normalized text, current CTC tokenizer token count, frames/token ratio, duration, speech ratio, source dataset, and language
    - bucket by an explainable difficulty tier, not only by audio length; short clean high-ratio samples are easiest, long samples, low frame/token ratio, low speech ratio, and high text-token count increase difficulty
    - preserve per-source and per-language summaries so LibriSpeech/AISHELL/CommonVoice imbalances are visible before training
    - write `difficulty_annotated.jsonl`, `difficulty_summary.json`, and per-tier JSONL bucket files that can be converted into future curriculum roots or sampler manifests
  - finer curriculum training should use cumulative difficulty stages generated from the difficulty artifact, not hand-picked shard subsets:
    - stage 1: `very_easy`
    - stage 2: `very_easy + easy`
    - stage 3: `very_easy + easy + medium`
    - ignore `hard` / `extreme` for the first CTC alignment curriculum unless a later spec update adds a cleanup stage
    - each stage writes its own `webdataset_lengths.jsonl`, `webdataset_lengths.summary.json`, and `webdataset_buckets_audio_text/manifest.json`
    - transition between stages by loading the previous stage's exported model checkpoint through `init_checkpoint_path`, not by DeepSpeed resume, because the dataloader schedule and epoch offsets change between stages
    - evaluate each stage on the matching cumulative eval split with normalized WER/CER and preview examples, in addition to training-loop eval loss
    - 2026-06-06 medium-stage follow-up: after the easy-clean SenseVoiceRWKV CTC run reached epoch 8 with full eval loss `1.3289`, start `difficulty_sensevoice/stage3_medium_cumulative` from the easy run's exported `best.pt` via `init_checkpoint_path`; do not carry over `resume_from`, optimizer state, or easy-stage data offsets
    - 2026-06-07 zh-balanced medium follow-up: sampled decoding on the English-heavy `stage3_medium_cumulative` run showed English improving while Chinese degraded by step 14000, so the next continuation stage is `difficulty_sensevoice/stage3_medium_zh_balanced`; for each split, keep all Chinese rows and sample the same number of English rows with seed `20260607`, yielding train `zh=95554/en=95554` and eval `zh=463/en=463`; restart from the better sampled checkpoint `step-6000.pt` through `init_checkpoint_path`, not from the latest drifting checkpoint
    - 2026-06-07 harder balanced follow-up: after `stage3_medium_zh_balanced` restores Chinese sampled WER/CER but still shows CommonVoice weakness, continue with `difficulty_sensevoice/stage4_commonvoice_balanced`; build each split as a 1:1 language mix with roughly 80% CommonVoice and 20% clean anchor per language: all CommonVoice CN plus sampled AISHELL-3 anchor for Chinese, equal-count CommonVoice EN plus equal-count LibriSpeech anchor for English; initialize from the previous balanced run's `best.pt`
    - 2026-06-07 small-LR replay-balanced follow-up: stage4 `step-5000.pt` improves CommonVoice CN by normalized WER/CER, while later epoch-3 degrades, so stage5 starts from `step-5000.pt` with small LR and explicit WER/CER checkpoint selection; build `difficulty_sensevoice/stage5_replay_balanced` as a 1:1 language mix where each language is approximately 50% CommonVoice, 25% clean anchor, and 25% replay sampled from `stage3_medium_zh_balanced`; use a fixed balanced selector eval slice for sidecar WER/CER checkpoint ranking
  - after CTC pred/ref ratio and normalized WER/CER show real alignment on the clean curriculum, continue into joint CTC+AR training via `init_checkpoint_path` from the best curriculum checkpoint
  - 2026-06-08 joint follow-up: stage5 selector chose `step-9000.pt` / copied `wercer_best.pt` by normalized WER on the fixed 128-sample selector slice (`WER=27.19%`, `CER=19.28%`); start the first CTC+AR joint run from `wercer_best.pt`
  - 2026-06-08 stage6 joint curriculum follow-up:
    - stage6a starts from the stage5 WER/CER-best CTC checkpoint on `difficulty_sensevoice/stage5_replay_balanced` so the newly enabled AR chat-template path can learn formatting and punctuation without immediately destabilizing CTC alignment
    - later joint stages should progressively mix harder GigaSpeech/WenetSpeech data while keeping clean replay anchors: stage6b uses approximately `clean=70% / Giga+Wenet=30%`, stage6c uses `50% / 50%`, and stage6d uses `30% / 70%`
    - the hard subset should be balanced between GigaSpeech and WenetSpeech within each stage so English and Chinese both get harder-domain exposure
    - every hard-mix stage should build its own sampled `webdataset_lengths.jsonl` and `webdataset_buckets_audio_text/manifest.json` rather than training on the full GigaSpeech/WenetSpeech root in one pass
    - hard-mix roots that rely on precomputed stage length indexes must still write `webdataset_index.json` split metadata (`split_by`, `eval_ratio`, `hash_seed`, `utt_id_key`) matching the generated training configs, because startup validates the root index before using the stage-specific bucket manifest
    - transition between joint stages by selecting the previous stage checkpoint with the best sidecar normalized WER/CER, preferring the better of CTC and direct RWKV-decoder AR preview metrics for that checkpoint; do not select solely by sampled eval loss
    - the automatic stage runner must stop on abnormal training exit, missing final checkpoint, or missing sidecar metrics instead of silently launching the next stage from a questionable checkpoint
    - the automatic stage runner should be restartable from a later current stage, e.g. after manually restarting stage6b it should monitor stage6b and continue with stage6c/stage6d instead of relaunching stage6b
  - CTC+AR after SenseVoice CTC curriculum must use dual targets:
    - CTC target remains `sensevoice_tiktoken`, `text_normalization=ctc`, `vocab_size=60515`, `blank_id=60515`
    - CTC target text must be normalized before CTC tokenization for every data source, including WebDataset and manifest data; punctuation, markup punctuation tags, symbols, apostrophes, and CTC EOS must not enter the CTC label stream
    - AR target uses the RWKV decoder tokenizer, runtime-normalized transcript text, and the RWKV decoder vocab size inferred from the decoder checkpoint
    - AR target must not reuse CTC-cleaned text; it should preserve transcript punctuation from runtime normalization because punctuation and semantic pauses are part of the decoder task
    - AR conditioning uses a chat-style template around the audio prefix; when `decoder_prompt_before_audio_use_language=true`, `decoder_prompt_before_audio` is a per-sample template and may include `{language}` / `{language_name}` / `{output_language_name}` / `{language_confirmation}`
    - the input prompt must treat metadata language as a label or hint, not an absolute truth; it should explicitly allow the decoder to correct the language if the spoken audio disagrees with the label
    - AR assistant output may also use a per-sample language-confirmation prefix via `decoder_target_prefix_use_language=true`; Chinese samples should be able to start with a confirmation such as `这是中文文字。`, and English samples with a confirmation such as `This is English text.`, before the transcript text
    - the assistant language-confirmation target should be derived from the transcript text when possible, then fall back to metadata language; this lets mislabeled-language training examples teach the AR decoder to correct the language instead of blindly following metadata
    - AR language-hint robustness should use optional wrong-metadata augmentation: `decoder_prompt_language_label_noise_prob` randomly flips the input prompt language label between Chinese and English for that sample, while keeping the assistant target language derived from transcript text
    - decoder prompt / suffix tokenization must use the AR tokenizer, not the CTC tokenizer
    - loading the CTC curriculum checkpoint must preserve the existing CTC head; do not switch the shared model vocab to RWKV just to enable AR

Default frontend contract:
- project default frontend is `WeNet-style fbank -> global_cmvn -> conv2d6 -> RWKV encoder`
- `linear` frontend remains only as an explicit fallback for debugging, unit tests, and ablations
- train / eval CLI defaults must use `conv2d6`

Tokenizer contract:
- tokenizer choice must be explicit in training config through:
  - `tokenizer_type`
  - `tokenizer_model_path`
  - `tokenizer_language`
  - `tokenizer_task`
- `vocab_size` may be inferred from the tokenizer; for SentencePiece, an explicit `vocab_size` must match the model's actual vocab size
- training outputs must save `tokenizer_config.yaml` next to `model_config.yaml`
- prediction CLIs should automatically reuse `tokenizer_config.yaml` when explicit tokenizer flags are not passed
- multilingual SentencePiece candidate preparation should support a `FLORES-200 dev+devtest -> unigram 4k/8k` path for controlled tokenizer comparisons

Global CMVN policy:
- `global_cmvn` statistics are computed on the training split only
- stored in WeNet-compatible JSON with:
  - `mean_stat`
  - `var_stat`
  - `frame_num`
- runtime normalization is:
  - `mean = mean_stat / frame_num`
  - `var = var_stat / frame_num - mean^2`
  - `istd = 1 / sqrt(max(var, 1e-20))`
  - `x_norm = (x - mean) * istd`
- runtime dtype contract is:
  - audio features may be extracted in `fp32` on CPU
  - after transfer to CUDA, batch feature tensors should be cast to `bf16`
  - `global_cmvn` should execute from `fp32` inputs for numerical stability, then cast back to the frontend / encoder compute dtype
  - `conv2d6`, RWKV blocks, and the CTC projection should run in `bf16` on CUDA
  - `log_softmax` and `ctc_loss` should upcast logits to `fp32`
- if `frontend_type == conv2d6` and no `cmvn_file` is supplied, the training entrypoint should materialize a default `global_cmvn.json` under `output_dir` and reuse it for later evaluation

Current implementation status:
- lightweight WeNet-style `fbank + global_cmvn + conv2d6` frontend extracted into this repo
- current train/eval pipeline still accepts precomputed features too, to keep the project usable without full audio manifests
- default training and evaluation path should now assume `conv2d6 + global_cmvn`

WebDataset support:
- training data may live in a directory of `.tar` shards
- inspected dataset root: `/home/yueyulin/data/voxbox/wenetasr`
- current observed structure:
  - `102` shard files
  - total size about `82G`
  - each sample is a pair of:
    - `{key}.wav`
    - `{key}.json`
- observed json schema:
  - `text`
  - `language`
  - `sample_rate`
  - `format`
  - `begin_time`
  - `end_time`
  - `confidence`
  - `sid`
- loader requirements:
  - stream tar shards without depending on the WeNet framework
  - decode audio bytes on the fly during training
  - extract WeNet-style fbank features on the fly
  - tokenize from `json["text"]` unless `token_ids` are already present
  - use `sid` as utterance id when available
  - partition shards across distributed ranks and dataloader workers
  - training entrypoint must accept both manifest and WebDataset roots
- automatic `global_cmvn` computation should work for both data sources
- for long-form multi-GPU training, WebDataset should support an optional offline `length index` built from tar metadata without decoding full audio
- offline dataset-wide preprocessing should live under `tools/` and use `Rust + multi-threading` by default; Python implementations remain only as fallback utilities for tests and debugging
- preferred training path with a length index is:
  - offline scan tar metadata into per-sample `num_frames`
  - build a map-style dataset keyed by `(shard_name, sample_key)`
  - form global batches from length-sorted indices
  - shuffle the batch order, not the within-batch length ordering, so each epoch covers the full length range while consecutive steps still see different length bands
  - compute local batch size dynamically from offline `num_frames`, so long utterances get a smaller `B` and short utterances get a larger `B`
  - split each global batch evenly across ranks so all ranks see a similar length band on the same step
- runtime fallback `B -> B'` token-budget shrinking remains only as a safety valve for residual text-length variance or rare outliers; it must not be the primary batching mechanism
- one epoch must cover the whole train split across all supported lengths; it is invalid to dedicate an epoch to only one length band
- current Python in-memory length-bucketing path is only valid for moderate `webdataset_lengths.jsonl` sizes
- when the length index grows beyond practical in-memory limits, training must:
  - avoid full JSONL loads during startup
  - log an explicit warning
  - fall back to the plain streaming WebDataset loader
  - compute `steps_per_epoch` from split sample counts instead of the offline length index
- exact large-scale length bucketing for Emilia-sized corpora requires a future binary / external-memory sampler; do not pretend the current in-memory JSONL sampler scales to tens of millions of utterances
- large-scale replacement for the current in-memory sampler:
  - Rust preprocessing should build a `bucket manifest` from the large length index
  - bucket id is derived from `num_frames // bucket_width`
  - default `bucket_width = 80` frames
  - output layout should be:
    - one compact manifest JSON
    - many small bucket part files capped by `entries_per_part`
  - each part file should preserve source shard order so samples from the same tar stay contiguous as much as possible
  - training must read only:
    - the small manifest during startup
    - the current bucket part while iterating
  - training must not load all bucket entries into memory at once
  - per-step bucket policy:
    - all ranks use the same bucket on the same step
    - each rank consumes a disjoint slice from that bucket's next global batch
    - local batch size is derived from the bucket's upper frame bound and the configured frame budget
  - each epoch must still cover the full train split across all buckets, with shuffled bucket-step order rather than single-bucket epochs
  - runtime parallelism constraint:
    - bucket-manifest training must preserve strict step ordering across ranks
    - do not let asynchronous worker scheduling reorder bucket steps, or distributed collectives can desynchronize
  - bucket-manifest runtime parallelism should therefore be implemented as:
    - one deterministic per-rank step iterator
    - thread-parallel local sample decode inside the current step
    - a small bounded prefetch queue for upcoming decoded batches
    - decoded prefetch must include tar reads, audio decode, feature extraction, tokenization, and collate, not only JSONL entry lookup
    - optional thread-local tar readers so repeated reads from the same shard reuse file handles safely
    - all file-handle caches must be bounded; tar readers should use per-worker LRU eviction and bucket part readers should close between `take()` calls while preserving file offsets
  - `num_workers` on the bucket-manifest path is interpreted as local decode / prefetch worker count, not as a generic DataLoader worker pool
- inspected Emilia Chinese dataset root: `/media/usbhd/training_data/asr/emilia/Emilia/ZH/Emilia/ZH`
- Emilia current observed structure:
  - flat directory of `.tar` shards, total size about `1.2T`
  - member pairs are `json + mp3`
  - json metadata already includes:
    - `id`
    - `wav`
    - `text`
    - `duration`
    - `speaker`
    - `language`
    - `dnsmos`
- Emilia-specific read policy:
  - do not unpack the dataset
  - do not build the training path around Python `tarfile.extractfile()` random member lookups
  - preferred path is:
    - Rust multi-threaded offline scan of tar headers plus json metadata only
    - infer `num_frames` directly from metadata `duration`
    - record `audio_member`, `audio_format`, `audio_offset`, `audio_size`, `json_offset`, and `json_size`
    - at training time, open each tar shard as a binary file and read members by byte offset rather than by tar-name rescans
  - this preserves the current length-bucketing design while avoiding a full audio decode during indexing and avoiding Python tar random-access overhead during training
- Emilia multilingual mixing policy:
  - if language-specific shard names are already unique, prefer building a virtual mixed root from symlinks rather than adding a bespoke multi-root training path
  - current observed shard naming is safe for this approach:
    - Chinese: `ZH-B*.tar`
    - English: `EN-B*.tar`
  - the mixed root should contain symlinks to both language directories and then reuse the normal single-root WebDataset workflow:
    - `inspect_webdataset`
    - Rust `webdataset_lengths.jsonl`
    - `global_cmvn`
    - standard training / eval configs
  - this keeps split logic, length bucketing, and checkpoint bookkeeping identical to the monolingual path
  - preprocessing and training must use the same `utt_id_key`
  - Emilia datasets use metadata key `id`, so:
    - `inspect_webdataset`
    - `inspect_webdataset_lengths`
    - `compute_cmvn`
    - training / eval configs
    must all set `webdataset_utt_id_key: id`
  - it is invalid to let training fall back to the project default `sid` once an index was built with `id`
- ASR corpus ingestion policy for non-WebDataset sources:
  - new corpora should be normalized into the same WebDataset contract instead of adding dataset-specific training loaders
  - the default large-corpus converter must be a Rust tool under `tools/`; Python conversion is fallback-only for small debugging jobs
  - converter progress must be visible before the first output shard is complete:
    - log total input shards before work starts
    - use an `indicatif` progress bar in the Rust converter for total input-shard progress, elapsed time, ETA, converted / skipped sample counts, and current worker state
    - do not print one line per worker event in interactive runs; reserve lines for command start, final summary, and exceptional warnings
    - update converted / skipped sample counts periodically while a large input shard is still in progress
  - conversion should process input shards concurrently with bounded workers, and then write deterministic output tar shards in a single ordered writer path
  - conversion may use an NVMe staging root for completed tar parts:
    - `--staging-root` writes tar parts to fast local scratch first, then publishes each completed part to the final WebDataset root
    - `--staging-max-bytes` is a safety guard for scratch usage, with scripts defaulting to a 100 GiB limit when staging is enabled
    - staged files must be fully finished before being copied / renamed into the final HDD output root, so resume never sees half-written final tar files
    - even without a separate staging root, active tar writes must use `.part` files and publish to final `.tar` only after `tar::Builder::finish()` succeeds
  - conversion must be resumable at input-shard granularity:
    - successful input shards write `_conversion_state/{prefix}-{input_index}.json` done markers after all output tar parts are finished
    - `--resume` skips marker-validated shards and deletes stale partial parts before retrying unfinished shards
    - `--adopt-existing` may recover pre-marker tar output only when expected tar part count matches the input shard's expected sample count and the final tar part contains the expected number of `.json` entries
    - adoption should avoid scanning all previously written tar payloads; for sequential per-input writers, validating part count plus the final part is sufficient to detect normal interrupted-shard cases
    - corrupt adopted output must not abort the whole conversion; the converter should warn, remove that input shard's old tar parts / marker, and rebuild that input shard
    - preprocessing scripts default to `RESUME=1`; `OVERWRITE=1` requires `RESUME=0` to avoid deleting expensive partial conversions by accident
    - preprocessing scripts should skip already completed downstream artifacts by default, including `webdataset_index.json`, `webdataset_lengths.jsonl` plus summary, bucket manifests, and CMVN; use `OVERWRITE=1` when those derived artifacts must be rebuilt
  - default conversion thread count should be conservative on rotational disks:
    - `4` workers is the safe default for `/media/usbhd`-style HDD storage
    - use higher `CONVERT_THREADS` only when input and output are on SSD/NVMe or separate physical disks
    - running multiple converters concurrently on the same HDD is expected to reduce throughput due to seek contention
  - canonical sample layout remains:
    - `{sample_key}.{wav|flac|mp3}`
    - `{sample_key}.json`
  - canonical metadata keys are:
    - `id`
    - `text`
    - `language`
    - `duration`
    - `sample_rate`
    - `source`
    - optional source-specific provenance such as `source_shard`, `source_id`, and `source_audio_path`
  - generated roots must then run the existing preprocessing chain:
    - `inspect_webdataset --utt-id-key id --split-by shard_name`
    - Rust `rwkvasr-tools` length index
    - Rust `build_bucket_index`
    - `compute_cmvn`
  - mixed roots built from already-normalized corpora should not rescan tar payloads when source artifacts already exist:
    - refresh symlinks into the mixed root
    - merge source `webdataset_index.json` files by summing shard and split counts
    - concatenate source `webdataset_lengths.jsonl` files and merge their summaries
    - rebuild only the mixed bucket manifest from the merged length index
    - this keeps GigaSpeech + WenetSpeech mixing fast enough to run after both source corpora finish preprocessing
  - if true mixed CMVN is intentionally skipped for launch speed, training config must point at an explicit identity/no-op CMVN file instead of silently triggering full-dataset CMVN at train startup
  - default split policy for converted large corpora is shard-name hashing so eval can skip most shard files during normal training/evaluation
  - conversion code should tolerate partially downloaded corpora by skipping incomplete / unpaired input shards when `--skip-missing` is set and reporting exact counts
  - GigaSpeech XL parquet ingestion:
    - read HuggingFace-style parquet shards with Rust `parquet/arrow`
    - accept embedded `audio` dictionaries containing `bytes` and/or `path`
    - infer text from common ASR text columns such as `text`, `sentence`, `normalized_text`, or `transcription`
    - normalize GigaSpeech text before tokenization for ASR runtime compatibility:
      - default training and prediction loaders should use `text_normalization = runtime`
      - runtime normalization should happen after reading sample metadata and before tokenizer encoding so existing WebDataset audio tar shards do not need to be rewritten
      - offline conversion may still write normalized text for archival datasets, but it is not required for training
      - English transcripts should be ASCII-lowercased instead of keeping GigaSpeech all-caps
      - GigaSpeech markup punctuation such as `<COMMA>`, `<PERIOD>`, and `<QUESTIONMARK>` should be converted to real punctuation
      - non-speech / annotation markup such as `<SIL>`, `<NOISE>`, and `<MUSIC>` should be removed from CTC/AR targets because emitting markup tokens is not useful for the current ASR product target
      - for runtime-loader normalization, keep source metadata untouched and expose normalized `text` only inside the decoded sample / eval reference path
      - for optional offline-normalized archival conversion, store the original transcript as `source_text` and record `text_normalization` in metadata
      - length indexes and bucket manifests may remain audio-dominant; if text-token-cost bucketing is used for AR-heavy training, rebuild only the bucket manifest / length text-cost metadata, not the audio tar shards
    - preserve original audio bytes when possible rather than transcoding
    - preserve the input parquet split and shard name in metadata; training scripts should default to `train-*.parquet` and keep official test shards for separate benchmark conversion
  - WenetSpeech ingestion:
    - current downloaded layout is Lhotse-style paired `cuts_*.jsonl.gz` and `cuts_*.tar.gz`
    - each JSONL line is a `MonoCut` with supervision text and recording source path; the paired tar already contains cut-level `.wav` audio
    - conversion should stream one cut shard at a time, pair metadata with tar members by cut id / basename, and write normal WebDataset tar shards
    - do not use Lhotse at training time; it is only an input metadata format for conversion
	- WebDataset length-index schema should no longer assume `.wav` audio:
	  - audio entries must be generic `audio_member`
	  - current minimum supported audio formats are:
	    - `wav`
	    - `mp3`
	    - `flac`
	  - old indexes that only store `wav_member` should remain readable for backward compatibility
	- for mixed roots whose existing bucket manifest was built from concatenated source indexes, do not require expensive bucket rebuilds just to fix early-epoch source order:
	  - training loader must support runtime source interleaving inside each bucket
	  - source groups are inferred from bucket part `first_shard` / `last_shard` prefixes, e.g. `GSXL` and `WSL`
	  - each repeated visit to a bucket should rotate across source groups / source offsets before returning to the same group
	  - rank partitioning must still happen after taking the same global batch on every rank, so all ranks keep synchronized collective shapes
	- on the target `4 x RTX 4090` setup, default DeepSpeed ZeRO-2 configs should not offload optimizer state to CPU unless explicitly requested for an ablation; current model sizes and observed bf16 activation footprints leave enough GPU headroom, and CPU offload reduces utilization
- for 4090 training configs, prefer:
  - larger `max_local_batch` upper bounds
  - offline length-bucket frame budgets as the primary limiter
  - runtime token budget only as a final safety valve
- training outputs must be epoch-centric in addition to step-centric:
  - compute mean `train_loss` for each epoch
  - run one `eval_loss` pass on the eval split at the end of each epoch
  - support `max_eval_samples` so large eval splits can use a capped subset on intermediate epochs
  - when `epochs` is specified and `max_eval_samples` is set, the final epoch must still run full eval
  - save `epoch-N` checkpoints
  - track and export the current `best` checkpoint based on lowest eval loss
  - persist epoch history so checkpoint selection can be done after training without parsing logs
- experiment tracking:
  - support optional `wandb` logging for both single-process and DeepSpeed training
  - DeepSpeed logging must be rank-0 only
  - tracked metrics should include:
    - periodic train loss / data time / step time / rate
    - progress-equivalent fields that mirror the terminal view closely enough for remote monitoring: progress fraction, ETA, effective batch size, token counts, and peak memory
    - epoch train/eval loss
    - step-checkpoint sampled eval loss
    - checkpoint selection metadata
  - `wandb` project name and run name must be configurable from YAML / CLI
  - `wandb` base URL and init timeout must be configurable from YAML / CLI so self-hosted and cloud deployments can both work
  - `wandb` initialization failure must not block training startup; log the failure and continue without experiment tracking unless the user explicitly chooses strict failure behavior in a later spec revision
- step-level checkpoint policy:
  - support a configurable `step_eval_samples` subset for periodic step checkpoints
  - support a configurable `step_eval_every` cadence; if unset, reuse the step checkpoint save cadence
  - 2026-06-06 SenseVoiceRWKV CTC curriculum note: the `sensevoice_rwkv_easy_clean_ctc_norm_lfr6_svtok_bs12_zero1_nockpt_4x4090` run writes multi-GB DeepSpeed checkpoints to `/media/usbhd/rwkvasr_runs`; saving and sampled step eval every 500 steps caused long synchronous USB block-IO stalls, so the active curriculum config uses `save_every=2000`, `step_eval_every=2000`, and `step_eval_samples=128` while preserving epoch eval and latest-checkpoint resume semantics
  - periodic step eval must use a randomized eval subset rather than always the first `N` samples
  - randomized step eval must still preserve offline length bucketing when a bucket manifest or offline length index is available; do not fall back to an unbucketed eval loader just to shuffle sample order
  - eval and step-eval batch sizes should be independently configurable from the train batch size so CTC `log_softmax` does not OOM on long eval batches
  - keep only the best `top_k_step_checkpoints` according to sampled `eval_loss`
  - track step-checkpoint selection metadata in a dedicated YAML so later manual checkpoint inspection does not require parsing logs
  - CTC curriculum runs after stage4 must not rely on sampled `eval_loss` alone for checkpoint selection:
    - run a sidecar checkpoint selector on fixed labeled balanced eval slices
    - compute normalized WER/CER with the project ASR metric normalization
    - write checkpoint-level WER/CER metrics, best checkpoint metadata, and a copied `wercer_best.pt` under the run directory
    - use normalized WER as the primary selection metric and CER as the tie-breaker unless a later spec update changes the target metric
    - tolerate transient per-checkpoint sidecar failures such as GPU OOM by recording a failure entry and continuing to later checkpoints instead of terminating the selector
  - epoch checkpoints and the final epoch-level `best` checkpoint remain separate from step-checkpoint retention
  - in distributed training, step-checkpoint artifact pruning must be single-writer and missing-path tolerant; do not let multiple ranks race on `rmtree`
  - `ds_checkpoints/` root creation must be eager rather than relying on downstream checkpoint writers
  - repository should also support a sidecar checkpoint watcher:
    - watch newly exported `step-*.pt` checkpoints under a run directory
    - polling cadence must be configurable; the default long-running sidecar cadence for background monitoring should be `3600s` so it behaves like an hourly reviewer rather than a tight loop
    - wait until a checkpoint file is stable before evaluation
    - run a small labeled prediction preview on CPU by default so training GPUs stay dedicated to training
    - when CPU preview is used for a run trained with `backend: cuda`, force prediction to a temporary `backend: native` config override instead of trying to load fused CUDA kernels on CPU
    - write preview text, JSONL predictions, watcher state, and a checkpoint-level comment file under the same run directory
    - optionally capture the current `tmux` training pane tail into the watcher output for lightweight progress inspection
    - for mixed-corpus runs, sidecar preview sampling must support per-source quotas, e.g. GigaSpeech and WenetSpeech each contribute a fixed number of examples; implement this by filtering shard globs such as `GSXL-*.tar` and `WSL-*.tar` instead of sequentially scanning the full mixed eval set
    - sidecar comments should combine:
      - sampled step-eval loss trend from `step_checkpoint_metrics.yaml`
      - preview-level blank / length-collapse signals from labeled prediction debug output
      - a short qualitative status such as `reasonable`, `slight regression`, or `needs attention`
    - when the checkpoint has a decoder-enabled model config, the sidecar should also run a rescored branch and compare it against the pure CTC branch on the same preview slice
    - sidecar comparison should report whether decoder rescoring improved or worsened preview-set WER/CER on normalized text
    - sidecar and validation WER/CER are ASR content metrics, not punctuation metrics:
      - runtime text normalization may preserve readable punctuation in previews
      - metric normalization must apply Unicode compatibility normalization and case folding before tokenization
      - metric tokenization must ignore punctuation and symbol characters by default
      - conservative ASR lexical equivalents should be canonicalized when they are unambiguous, e.g. `ok`, `okay`, and `o.k.` all score as the same content
      - project-generated AR language-confirmation prefixes such as `This is English text.` and `这是中文文字。` are output-format metadata and must be stripped before WER/CER tokenization
      - WER should compare English / numeric word tokens plus individual CJK characters
      - CER should compare non-space, non-punctuation content characters
      - predictions that differ only by comma, period, question mark, case, or the above explicit lexical equivalents should have zero WER/CER delta
    - comment generation must be idempotent and support backfilling already-evaluated checkpoints after watcher logic changes
- tokenizer switching rule:
  - changing tokenizer type or vocabulary size changes the CTC target space and output head shape
  - training cannot resume across tokenizer changes; a new run must start from step 0
- large-scale multilingual CTC preference:
  - for Emilia-scale multilingual training, prefer `SentencePiece Unigram 8k` over Whisper multilingual BPE as the default efficiency-oriented tokenizer
  - rationale:
    - much smaller CTC head
    - lower logits / `log_softmax` memory pressure
    - lower eval OOM risk
    - fewer total training steps needed once the larger local batch becomes feasible
  - Whisper multilingual BPE remains a baseline / compatibility tokenizer, not the default efficiency choice
- continue-training support should prioritize epoch checkpoints:
  - single-process training may resume from exported `.pt` checkpoints
  - DeepSpeed training should resume from `ds_checkpoints/` plus `resume_tag`, typically `epoch-N` or `best`
  - checkpoint extra/client_state must persist `step`, `epoch`, epoch history, and current best metric state
  - checkpointing should persist and expose `epoch_batch_offset`, the count of loader batches consumed in the current epoch at save time
  - resume behavior must reuse `epoch_batch_offset` to skip already-read loader batches from the resumed epoch and continue from the exact next batch
  - bucket-manifest WebDataset resume must fast-forward from bucket metadata only and must not decode skipped audio/features before the first resumed training step
  - long resume fast-forward operations must log progress periodically so an apparently silent training session can be distinguished from a hang
  - step-checkpoint top-k pruning must never delete the most recently written checkpoint while `latest_checkpoint.yaml` points at it; latest and best/top-k retention are separate policies
  - default resume target should support `resume_from: latest` by reading `latest_checkpoint.yaml` and resuming from the most recently saved checkpoint path
  - every completed checkpoint export should also refresh `latest_checkpoint.yaml` with:
    - `checkpoint_type` (`pt` or `deepspeed`)
    - `checkpoint_path` (exported `.pt` when available)
    - `step`, `epoch`, `epoch_batch_offset`
    - DeepSpeed-specific `resume_tag` and `deepspeed_checkpoint_dir` when present
  - for stage-2 Direction Dropout post-training, DeepSpeed must also support `init_checkpoint_path`:
    - load model weights from a plain `.pt` or `.safetensors` checkpoint before `deepspeed.initialize`
    - start a fresh optimizer / ZeRO state in a new `output_dir`
    - reset step / epoch counters unless a true DeepSpeed resume is explicitly requested
    - prefer true DeepSpeed resume when `resume_from` is set:
      - if both `init_checkpoint_path` and `resume_from` are present (legacy YAMLs often keep both), skip `init_checkpoint_path` and resume from `resume_from`
      - log the skip so restart behavior is explicit
- `resume_from: latest` must also recover from stale `latest_checkpoint.yaml` metadata:
  - fallback to the latest valid `ds_checkpoints/step-*` when recorded resume target is missing
  - if `client_state` in the DeepSpeed tag is incomplete, fallback to values in `latest_checkpoint.yaml`
  - if `latest_checkpoint.yaml` is stale but resolves to an existing checkpoint tag, prefer the resolved tag's step and infer `epoch_batch_offset` from the matching exported `.pt` when available
  - preserve `epoch_batch_offset` when available so resumed training can skip already-read batches
  - in distributed Direction Dropout training, the sampled per-step direction mask must be identical on every rank; sample once and broadcast, do not let each rank sample independently
- v0 Rust preprocessing scope:
  - `webdataset_lengths.jsonl` generation from tar metadata
  - multi-threaded per-shard scanning
  - record optional tar member offsets and sizes for zero-copy-style random-access reads from plain `.tar` shards
  - summary JSON with split counts and frame bucket histogram
  - future offline CMVN / manifest materialization may also move under `tools/`, but that is not required for the first slice
- v0 Rust prediction scope:
  - standalone `Candle` tool under `tools/`
  - load exported `safetensors` tensors for `logits` and `lengths`
  - run configurable `CTC prefix beam search`
  - write JSONL utterance-level hypotheses with token-level time alignment
  - full RWKV encoder forward parity in Rust is a later slice and must not be implied by the first predictor tool
- prediction/export workflow is explicitly split into two supported paths:
  - Python full-model prediction from `.pt` or `.safetensors` checkpoints
  - Python export of `CTC logits + lengths + utt_ids` followed by Rust/Candle decode + token-level time alignment
- Candle custom-op design constraints for the later RWKV Rust port:
  - user-land custom ops are exposed via `CustomOp1`, `CustomOp2`, and `CustomOp3`
  - inference-only fused kernels should prefer `apply_op*_no_bwd`, because backward support is unnecessary for deployment
  - each custom op backend must be implemented separately:
    - `cpu_fwd`
    - optional `cuda_fwd`
    - optional `metal_fwd`
  - Candle passes arbitrary storage + `Layout`, so CPU fallback code must handle non-contiguous tensors and not assume dense contiguous buffers
  - because custom-op arity stops at 3 tensors, the RWKV fused op should only own the recurrent core; pre/post projections and simple elementwise transforms should remain regular Candle graph ops
  - any fused RWKV op should consume prepacked runtime tensors plus scalar kernel config, rather than trying to embed the whole TimeMixer module as a single opaque op

WebDataset indexing and split policy:
- do not split `train / eval` by shard file because tar packing may not be randomized
- use `sample-level stable hash split` instead
- split key should be:
  - `json["sid"]` if present
  - otherwise tar sample key
- provide an `inspect_webdataset` command that scans shards and writes an index file with at least:
  - total shard count
  - total sample count
  - per-shard sample count
  - per-split sample count under a stable-hash split rule
- train / eval / CMVN must all reuse the same split rule
- when training from WebDataset by `epochs`, compute `steps_per_epoch` from the indexed split sample count, not from shard count

Tokenizer policy:
- default text tokenizer is `Whisper multilingual tokenizer`
- tokenizer source should be fixed upstream assets, not retrained on the current ASR acoustic dataset
- CTC target vocabulary uses only the Whisper `text token` range
- language / task / timestamp / other control tokens are excluded from the default CTC label set
- CTC `blank` is added by this project on top of the Whisper text-token vocabulary
- this gives:
  - no `<unk>` / OOV behavior for normal UTF-8 text
  - a tokenizer that is not tightly coupled to current training-set growth
  - a much smaller CTC head than Qwen / Gemma class LLM tokenizers
- `Qwen3 tokenizer.json` support is allowed as an ablation / comparison path, but it is not the default:
  - it uses a much larger CTC head than Whisper multilingual
  - it tends to merge multiple Chinese characters into one token, which can hurt CTC timestamp granularity
  - 4 x 4090 baseline configs must therefore start from a smaller token/frame budget than Whisper runs

YAML config policy:
- training must emit at least:
  - `model_config.yaml`
  - `train_config.yaml`
- public repository packaging must:
  - keep `README.md` aligned with the current implemented slices and open gaps
  - keep `ROADMAP.md` aligned with the public GitHub task breakdown for remaining milestones
  - document latest public training summary without committing checkpoints or run artifacts
  - exclude `runs/`, DeepSpeed states, exported weights, and local datasets from version control
- training CLI should also accept a YAML config file as the primary launch interface
- when both YAML and CLI args are present, only explicitly provided CLI args may override YAML fields
- DeepSpeed multi-GPU training should use the same YAML-first interface
- training YAML may embed a `deepspeed` mapping directly instead of forcing a separate JSON file
- prediction / evaluation should prefer reconstructing model architecture from YAML rather than repeated CLI shape arguments
- standalone prediction should live under a separate `predict` module / CLI and must not depend on training-loop entrypoints
- repository should also provide a labeled prediction CLI for dev/eval inspection:
  - consume manifest or WebDataset eval samples with references
  - run configurable CTC prefix beam search
  - support decode-time insertion / length bonus ablations
  - save `pred_text`, `ref_text`, token ids, and token-level alignments to JSONL
  - optionally save per-utterance decode diagnostics so short-hypothesis failures can be distinguished from frontend truncation bugs
  - support a small-sample preview workflow before running full WER/CER
- Rust deployment tooling should live under `tools/`; the first `Candle` predictor may target exported `CTC logits + lengths` rather than full RWKV encoder parity
- exported inference checkpoints should support:
  - `.pt` for training resume
  - `.safetensors` for inference loading in Python and future Rust model loading
- decode-export artifacts should support:
  - batched `part-xxxxx.safetensors` files with `logits` and `lengths`
  - sidecar `part-xxxxx.utt_ids.txt`
  - an index JSON summarizing exported parts, mode, blank id, frontend type, and timestamp projection config
- manual CLI shape arguments remain only as a fallback path
- repository should ship minimal example YAML files for training and model architecture so configs can be exchanged directly

Training stages:
1. short/mid-form bidirectional base training
2. fixed-rate Direction Dropout training for dual-mode robustness
3. long-form fine-tuning with concatenated or naturally long utterances
4. streaming/chunk robustness validation in `L2R` mode
5. offline evaluation in `Bi` and `Alt` modes

### 6.2 Optimization

Baseline optimizer rules derived from upstream:
- AdamW or DeepSpeed fused Adam
- `att.w0` uses `2x` LR scale
- weight decay only on large matrix weights
- bf16 training
- gradient checkpointing optional

DeepSpeed mainline policy for target 4090 training:
- optimizer should use `DeepSpeedCPUAdam` in AdamW mode
- default ZeRO mode should be `ZeRO-2`
- optimizer state should use `cpu offload`
- encoder activations should enable gradient checkpointing by default to maximize usable context / batch
- rank0 training output should include live progress, ETA, and per-step timing because audio decode / fbank extraction is online and the first batch can be much slower than steady-state
- variable-length training should use a `batch token budget` policy rather than blindly fixing `batch_size`
- the practical budget metric should be `padded_audio_tokens = B * max_audio_frames`, plus `text_tokens = sum(target_lengths)`
- training should log the observed batch token count, peak memory, and a `22 GiB` target-budget estimate from real runs
- when a candidate loader batch exceeds the configured token budget, the safe default is to shrink the effective local batch from `B` to the largest budget-compliant prefix `B'` for that step
- under `DDP` / `DeepSpeed`, token-budget enforcement must happen before the model step at batch-construction time; per-rank variable counts of forward/backward calls inside one logical step are not safe
- do not execute the dropped tail samples from an oversized candidate batch inside the same logical step
- we should not truncate audio time `T` against a full transcript by default, because that breaks CTC alignment semantics unless transcript segmentation is also available
- pathological single samples larger than the token budget should be isolated or skipped with an explicit warning rather than causing an OOM crash

Initial defaults:
- `lr = 4e-4` to `6e-4` range for base runs
- warmup enabled
- ZeRO-2 first, ZeRO-3 only if memory forces it
- DeepSpeed training YAML should normalize to:
  - `zero_optimization.stage = 2`
  - `zero_optimization.offload_optimizer.device = cpu`
  - `zero_optimization.offload_optimizer.pin_memory = true`

Direction Dropout for paper reproduction:
- The paper describes DirDrop as dropping one recurrent direction with a set probability.
- The experiment section states `For DirDrop, we use a dropout rate of 20%.`
- Historical strict reproduction configs therefore used:
  - `direction_variant = drop_both`
  - `p_start = 0.2`
  - `p_max = 0.2`
  - `warmup_steps = 0`
  - `ramp_steps = 0`
- This is no longer an active default for the current stabilization and Stage11+ ASR training line. Current training must keep full bidirectional encoder masks with `direction_variant = none`, `p_start = 0.0`, `p_max = 0.0`, `warmup_steps = 0`, and `ramp_steps = 0`; old reproduction configs are historical only unless a future ablation explicitly re-enables DirDrop in this spec and uses a separate run name.
- Scheduler-based ramping can remain in code only as a non-default ablation path.

Why not reuse upstream Lightning trainer:
- `train_temp` is tailored to LM training and old Lightning.
- This project needs ASR losses, variable-length audio batching, chunked validation, and simpler maintainability.
- We should reuse the optimization policy, not the full framework.

### 6.3 Hardware Plan

Target environment:
- `4 x RTX 4090`
- `torch 2.10`
- `CUDA 13.0`
- `DeepSpeed`
- managed by `uv`

Execution policy:
- keep all project dependencies in `pyproject.toml`
- use `uv` virtualenv and lockfile when environment sync becomes possible
- compile the fused CUDA extension against the active torch / CUDA toolchain
- default distributed launcher path is `deepspeed --num_gpus ... rwkvasr-train-ctc-deepspeed --config-yaml ...`
- when using `DeepSpeedCPUAdam` under a `torch cu128 + system CUDA 13.0` environment, allow `DS_SKIP_CUDA_CHECK=1` for the CPU optimizer build path
- training artifacts should include:
  - `model_config.yaml`
  - `train_config.yaml`
  - `deepspeed_config.yaml`
  - DeepSpeed checkpoint directories by step tag
  - a plain inference-friendly `step-<N>.pt` checkpoint when the active ZeRO stage allows direct export

## 7. Evaluation Plan

Required validation axes:
- CTC loss decreases on 1-batch overfit
- offline greedy decode sanity in `Bi` mode
- offline prefix beam search sanity with configurable `beam_size`
- Rust/Candle prefix beam search sanity from exported `safetensors logits`
- token-level CTC time alignment sanity in both Python and Rust predictors
- streaming chunked decode sanity in `L2R` mode
- same-checkpoint `Bi / L2R / Alt` decode sanity
- WER/CER on dev set
- latency and throughput under:
  - batch size 1
  - streaming chunk sizes
  - offline long-form chunks

Minimum benchmark outputs:
- tokens or frames per second during training
- audio seconds per second during inference
- peak GPU memory
- WER by chunk size
- WER delta between `Bi` and `L2R` from the same checkpoint
- WER delta between `Bi` and `Alt` from the same checkpoint

## 8. Implementation Plan

### Phase 0: Project Skeleton
- create `uv` project files
- define config layout
- create package skeleton
- add test harness

Status:
- completed in current iteration

### Phase 1: RWKV-7 Core
- port `RWKV_Tmix_x070` into `src/`
- implement reference path without fused CUDA
- add shape and numerical sanity tests

Status:
- in progress
- native recurrent reference path implemented
- basic shape / `v_first` / chunk-consistency checks implemented
- fused CUDA backend not wired into this project yet

### Phase 2: Bidirectional RWKV Wrapper
- implement per-block `L2R` + `R2L` wrapper
- implement reverse / restore sequence utilities
- implement merge strategy and inference mode masks
- add unit tests for `Bi`, `L2R`, and `Alt`

Status:
- in progress
- bidirectional wrapper, reverse/restore, and mode-mask helpers implemented
- module-level inline validation passed

### Phase 3: Direction Dropout
- implement per-layer direction-drop masks
- implement fixed-rate paper-reproduction drop probability
- support `drop_r2l_only` and `drop_both`
- log active-direction statistics during training

Status:
- in progress
- fixed-rate DirDrop and training mask sampling implemented
- scheduler ramp path retained only for non-default ablations
- logging and trainer integration not implemented yet

### Phase 4: ASR Encoder
- implement Conformer-style block with bidirectional RWKV TimeMixer replacing self-attention
- add causal convolution cache support for `L2R`
- add encoder stack state API

Status:
- in progress
- single-block Conformer-style RWKV block implemented
- causal convolution cache and `L2R` chunk-state consistency validated with inline checks
- encoder stack not implemented yet

### Phase 5: CTC Model
- add frontend, tokenizer integration, CTC projection, loss
- add greedy decode
- add standalone prefix beam search prediction path
- add an optional Qwen3-ASR AuT-style encoder variant where:
  - the Qwen3-ASR audio frontend shape is preserved: 128-bin fbank input, 3x stride-2 Conv2D downsampling, sinusoidal positions, post encoder projection
  - Qwen3-ASR audio self-attention is not used; every attention slot is replaced with bidirectional RWKV-7 TimeMixer
  - Qwen3-ASR non-attention module names should be kept close enough to support partial warm-start from the official audio encoder for Conv2D, LayerNorm, FFN, and output projection weights
  - encoder hidden dimension and encoder output dimension may differ, so CTC and AR audio-conditioning heads must consume the encoder output dimension rather than assuming `n_embd`

Status:
- in progress
- minimal feature-to-encoder-to-CTC logits/loss chain implemented
- greedy decode and token-level evaluation implemented
- standalone prefix beam search prediction path not implemented yet

### Phase 6: Training System
- manifest dataset
- collator
- DeepSpeed training entrypoint
- checkpoint save/load

Status:
- in progress
- upstream-style optimizer parameter grouping implemented
- Direction Dropout integrated into a minimal CTC training wrapper
- synthetic one-batch overfit runner implemented and validated
- JSONL manifest dataset, collator, checkpoint save/load, and a minimal train CLI implemented
- DeepSpeed entrypoint not implemented yet
- WebDataset training loaders must tolerate isolated corrupt samples by logging shard/key/member context and skipping the failed sample, while prediction and evaluation loaders remain strict unless explicitly configured otherwise.
- Large SenseVoiceRWKV runs may use ZeRO-1 with optimizer offload disabled and gradient checkpointing disabled when GPU memory leaves enough headroom; this is an optimization-only change and must not alter checkpoint resume semantics or the data-skip counter.
- Clean-data CTC curriculum stages must filter by CTC alignability under the actual training tokenizer and frontend downsampling before bucketed training. For the AuRWKV/Qwen3-ASR + Omni CTC setup, keep only samples where `target_tokens + adjacent_repeated_target_tokens <= aut_conv2d8_out_lengths(num_frames)`; for SenseVoice LFR, use `ceil(num_frames / 6)`. The earlier `easy_clean_librispeech_aishell3` stage was clean but not CTC-aligned; English LibriSpeech samples were often too token-dense after 8x downsampling and produced `zero_infinity=True` loss values of `0.0000`.
- CTC-aligned clean stages should be rebuilt with `text_normalization: ctc`, unknown-token filtering enabled, and a configurable positive `min_logit_required_ratio`. A ratio of exactly `1.0` only proves mathematical CTC feasibility; it may still be too tight for English character-level labels because it leaves almost no blank / repeat alignment slack.
- 2026-06-09: Stage6 CTC-draft AR continuation uses an offline first-pass cache generated from a fixed CTC checkpoint. The cache is a JSONL map keyed by utterance id and stores the normalized CTC hypothesis as `{ctc_draft}` for the decoder prompt. CTC labels remain clean and punctuation-stripped; AR targets keep runtime text normalization and language-confirmation/chat-template behavior. For draft-conditioned AR training, prefer filtering the WebDataset length index to cached utterances so most training samples have a draft instead of silently falling back to an empty prompt field.
- 2026-06-09: Add a Best-RQ probe before committing to a full encoder pre-training stage. The first slice is `SenseVoiceRWKVEncoder + fixed random-projection quantizer + Best-RQ prediction head`, initialized from the current CTC checkpoint and trained on audio-only WebDataset batches. The Best-RQ head and quantizer are discarded after pre-training; the resulting checkpoint is used only as an encoder initialization for a fresh CTC curriculum run. This should be evaluated by early CTC WER/CER and convergence speed before changing the main CTC-draft pipeline.
- 2026-06-09: The accepted Best-RQ continuation pipeline is: wait for a stable latest CTC/joint checkpoint, run a longer Best-RQ encoder pretrain from that checkpoint, initialize a CTC-only curriculum from the Best-RQ checkpoint, then generate an offline CTC draft cache from the CTC curriculum checkpoint. The CTC stage must keep `decoder_enabled: false`, clean CTC text normalization, and SenseVoice multilingual tokenizer labels; AR/draft-conditioned training happens only after this CTC cache is generated.
- 2026-06-09 recovery note: the Best-RQ -> CTC -> draft pipeline must support a `draft-only` resume path from an already completed CTC run. This is needed when the tmux-launched CTC process starts after the monitor's first process check and the outer pipeline exits before draft generation, even though the CTC training later completes successfully. The recovery path should reuse the generated CTC config, `model_config.yaml`, and latest stable `step-*.pt` without rerunning Best-RQ or CTC.
- 2026-06-09 full-draft note: the first offline draft cache used quota sampling and produced about half of the `stage6c_clean50_hard50` training rows. For draft-conditioned AR training on this stage, prefer a full cache built by generating only utterances missing from the sampled base cache, then merging by `utt_id` and filtering the WebDataset length index to the merged cache ids.
- 2026-06-09 full-draft parallelism note: CTC draft expansion can run as multiple independent prediction processes, but each process must own an isolated physical GPU via per-process `CUDA_VISIBLE_DEVICES=<gpu_id>` and use `--device cuda:0` inside that restricted view. Do not launch several predictors with a shared visible CUDA device set and different `--device cuda:N`, because the current RWKV CUDA inference path is not stable under that multi-process configuration.
	- 2026-06-09 stage6d draft-conditioned AR run: start joint CTC+AR training from the Best-RQ-initialized CTC `step-5000.pt`, using the full offline CTC draft cache (`128,116` draft records and `130,357` cached length-index rows) as an AR prompt hint. Keep CTC labels punctuation-stripped with SenseVoice tokenizer; keep AR targets in runtime-normalized RWKV decoder text with language confirmation/chat-template behavior. Sidecar evaluation for this run must use sampling and pass the same draft cache into AR prediction so AR metrics reflect the training-time conditioning path.
	- Draft-conditioned AR must remain audio-conditioned, not draft-only. The decoder input context should include the projected continuous encoder acoustic hiddens together with the CTC-selected draft tokens rendered in the prompt, so the AR decoder can use both the discrete first-pass transcript and the higher-information acoustic representation.
	- 2026-06-09 stage6d eval guard note: the full-draft length index is intentionally filtered to cached training utterances and currently contains no `eval` split rows. Training-loop step/epoch loss eval on that index must therefore skip empty eval loaders instead of recording `nan` or ranking step checkpoints by an empty metric. For this stage, checkpoint selection should use sidecar normalized WER/CER; in-loop step eval can be disabled while the draft-conditioned run is active.
	- 2026-06-09 sidecar note: when a run's training config points to a train-only filtered length index, sidecar prediction must not pass that length index to eval-split prediction commands. It should fall back to raw WebDataset shard iteration so eval split samples can still be selected from the root `webdataset_index.json`; AR sidecar evaluation should still use the just-generated CTC prediction JSONL as the draft cache for the same samples.
	- 2026-06-10 metric note: AR language confirmation remains part of the decoder target format, but it is not speech content. Sidecar and validation WER/CER must strip the generated confirmation prefix before ASR tokenization so short utterances such as `六` or `for example` are not penalized by the fixed metadata phrase.
		- 2026-06-10 large CTC continuation: before returning to AR correction, train a larger CTC-only stage from the best recent CTC checkpoint. The stage must keep `decoder_enabled=false`, `decoder_loss_weight=0`, `ctc_loss_weight=1`, `text_normalization=ctc`, `tokenizer_append_eos=false`, SenseVoice multilingual CTC tokenizer labels, and sidecar/selector WER-CER checkpoint selection. CTC normalization must remove non-pronounced markup such as punctuation tags and bracketed noise/laughter/music/silence annotations so forced CTC alignment is not asked to emit labels for unspoken metadata.
		- 2026-06-11 post-stage7 draft-conditioned AR plan: after the large CTC-only stage finishes, select the initialization checkpoint by normalized WER/CER over an existing-checkpoint candidate set, not by eval loss alone and not from checkpoint files already removed by top-k cleanup. Generate an offline CTC draft cache for the full stage7 train split with one isolated prediction process per physical GPU, merge by utterance id, and filter the length index to cached utterances before CTC+AR training. The joint run must keep CTC labels in `text_normalization=ctc` with no EOS while AR targets use `decoder_text_normalization=runtime`, language-confirmation target prefixes, chat-style language prompts, and the CTC draft prompt together with continuous acoustic encoder hiddens.
		- Full offline draft generation may use a fast CTC greedy-collapse path (`beam_size<=1`) for throughput when producing large training caches. This does not change checkpoint selection or public CTC evaluation, which should continue to use the configured CTC beam settings; generated draft cache metadata/logs must make the decode strategy visible so AR experiments can distinguish greedy-draft and beam-draft inputs.
		- Full offline draft generation must stream JSONL predictions to disk while decoding instead of buffering an entire shard group in memory. Long shard groups can take hours; partial output should be visible for progress inspection and should survive predictor crashes so the run can be restarted or repaired without losing all completed utterances.
		- Parallel draft generation must avoid assigning multiple active predictor processes to the same physical GPU while leaving another configured GPU idle. If the scheduler cannot dynamically track the freed GPU, use one static worker queue per GPU rather than round-robin assignment by launch count.
		- The merged draft cache consumed by training must be compact. Keep raw per-part prediction JSONL files for debugging, but the merged training cache should contain only `utt_id`, normalized `pred_text` / `ctc_draft`, `draft_source_part`, and decode metadata. Do not carry alignments, token ids, or debug payloads into the train-time cache because every training rank needs to load the draft map.
		- Stage7 draft-conditioned CTC+AR restart scripts must prefer `resume_from: latest` when the AR run directory already has a complete `latest_checkpoint.yaml` and DeepSpeed checkpoint. This preserves optimizer state and `epoch_batch_offset`, so a crash/restart does not reread the already-trained prefix of the filtered length index.
		- 2026-06-12 Stage7 OOM recovery: a draft-conditioned AR restart hit CUDA OOM at step 3642 inside decoder cross entropy with batch size 4 and padded decoder text length 1292. The restart script should cap `decoder_text_token_budget` more conservatively by default and set `PYTORCH_ALLOC_CONF=expandable_segments:True` before torch initialization. This is a targeted AR-memory guard; it does not change CTC label normalization, draft conditioning, or checkpoint resume semantics.
		- 2026-06-13 Stage8 data-scale follow-up: Stage7 draft-conditioned CTC+AR consumed only the draft-filtered cache (`~0.8M` train rows), while the local GigaSpeech XL + WenetSpeech L mix has `22.7M` train rows. Before generating another AR draft cache, continue with a CTC-only full-mix stage from the latest Stage7 checkpoint so the acoustic model sees substantially more hard English/Chinese speech. Keep CTC labels punctuation-stripped (`text_normalization=ctc`, no EOS), disable the decoder loss, save frequent checkpoints, and use sidecar WER/CER on balanced GigaSpeech/WenetSpeech samples as the checkpoint-quality signal. After CTC WER/CER improves, generate a larger offline draft cache from the improved CTC checkpoint and return to draft-conditioned CTC+AR.
		- 2026-06-13 CTC label normalization update: CTC targets should represent pronounced content only. The `ctc` text normalization path must strip punctuation/symbols/non-speech annotations, must not append EOS, and must normalize Arabic numerals into language-appropriate spoken text before tokenization. English numeric labels should become word forms such as `twenty one`, `three point one four`, and `ten percent`; Chinese numeric labels should become Chinese spoken forms such as `二零二四年`, `百分之三点五`, and `十二`. AR targets remain `runtime` normalized so punctuation and richer text formatting stay in the decoder objective rather than the CTC alignment objective.
		- 2026-06-13 CTC-only sidecar rule: when a checkpoint/config has `decoder_enabled: false`, sidecar evaluation must not launch RWKV decoder prediction. It should evaluate only the CTC branch, write CTC WER/CER comments, and keep checkpoint selection based on CTC normalized WER/CER. AR comparison is valid only for decoder-enabled checkpoints.
		- 2026-06-13 Stage8 outcome: the full-mix CTC-only run finished normally at 30k steps. On the fixed 128-sample balanced GigaSpeech/WenetSpeech selector, normalized CTC WER/CER improved from `47.92% / 34.81%` at step 2000 to `41.77% / 27.96%` at step 30000, and step 30000 was copied to `wercer_best.pt`. This is real progress but still too noisy for the next large draft-conditioned AR stage; continue CTC from `wercer_best.pt` with a smaller LR before generating the next offline draft cache. Enter draft-conditioned CTC+AR only after balanced hard-set WER/CER drops materially further or a continuation stage stops improving by WER/CER.
		- 2026-06-14 Stage9 outcome: continuing CTC from Stage8 `wercer_best.pt` with LR `1e-5` finished normally at 30k steps. The fixed 128-sample balanced selector chose `step-22000.pt`, improving normalized WER/CER to `39.40% / 25.53%` (`en WER=36.20%`, `zh CER=43.57%`). This confirms large hard-mix CTC continuation is still useful, but the improvement slope is far from paper-level ASR. Add training-time SpecAugment for the next CTC continuation stage while keeping evaluation and prediction unaugmented, CTC labels punctuation-stripped, and CTC EOS disabled.
		- 2026-06-14 Stage10 early-stop decision: continuing from Stage9 `wercer_best.pt` with LR `5e-6` and training-time SpecAugment (`time_masks=2,time_width=20,freq_masks=2,freq_width=27`) improved English on the fixed 128-sample selector but consistently hurt Chinese. The CPU selector measured `step-2000` at `39.78% / 25.86%` (`en WER=34.50%`, `zh CER=47.42%`), `step-4000` at `40.10% / 25.47%` (`en WER=34.73%`, `zh CER=47.27%`), and `step-6000` at `39.85% / 24.95%` (`en WER=34.39%`, `zh CER=47.71%`). Since the bilingual target cannot trade a 1.8-point English WER gain for a 4.1-point Chinese CER regression, stop the strong-SpecAugment branch and restart from Stage9 `wercer_best.pt` as Stage10b with LR `5e-6`, no SpecAugment, the same fullmix data, CTC-only objective, punctuation-stripped CTC labels, no EOS, and WER/CER checkpoint selection.
		- 2026-06-14 Stage10b early signal: Stage10b (`Stage9 wercer_best.pt` + LR `5e-6` + no SpecAugment) reached `36.58% / 23.94%` at `step-2000` on the same fixed 128-sample selector, with `en WER=31.90%` and `zh CER=43.43%`. This beats Stage9 in both languages and reverses the Stage10 Chinese regression, so keep Stage10b as the active CTC continuation branch. Copy the best fixed-selector checkpoint to `wercer_best.pt` as each new evaluated checkpoint improves, and continue evaluating at later save points before returning to large offline CTC-draft generation or CTC+AR training.
		- 2026-06-15 Stage10b follow-up: fixed 128-sample selector results for `step-4000` and `step-6000` were `37.16% / 23.72%` and `36.84% / 23.79%`. Both preserve the lower-CER trend but do not beat `step-2000` on the primary WER metric, so `wercer_best.pt` remains `step-2000.pt`. A persistent `selector_stage10b` tmux session now runs `select_ctc_checkpoint_by_wercer` over the same fixed balanced slice and will update `wercer_best.pt` only when a later stable checkpoint improves normalized WER, with CER as the secondary ranking signal.
		- 2026-06-15 Stage10b `step-8000` improved again on the fixed 128-sample selector to `36.45% / 23.46%` (`en WER=32.24%`, `zh CER=41.95%`, exact `14/128`), so the persistent selector copied it to `wercer_best.pt`. Continue Stage10b beyond 8000 because the no-augmentation low-LR continuation is still producing bilingual WER/CER gains; reconsider stopping only after multiple later selector checkpoints fail to beat the current best.
		- 2026-06-15 Stage10b `step-10000` refreshed the fixed-selector best by WER: `35.62% / 23.55%` overall, exact `15/128`, `en WER=32.01%`, and `zh CER=40.32%`. Overall CER is slightly worse than step8000 because English CER rose, but the primary WER and Chinese CER both improved, so keep Stage10b running and let the persistent selector continue updating `wercer_best.pt`.
		- 2026-06-15 Stage10b `step-12000` did not refresh best: `36.71% / 23.61%`, exact `13/128`, `en WER=32.01%`, and `zh CER=42.84%`. Keep `wercer_best.pt` at step10000. This is only one post-best regression point, so do not stop Stage10b yet; continue to at least step14000/16000 before declaring plateau and switching to balanced2M draft generation.
		- 2026-06-15 Stage10b `step-14000` also did not refresh the WER-selected best: `36.90% / 23.44%`, exact `16/128`. CER improved slightly over step10000, but WER regressed for a second consecutive checkpoint after the step10000 best. Keep `wercer_best.pt` at step10000 and allow one more checkpoint (`step-16000`) before declaring the low-LR no-augmentation CTC continuation plateaued. If step16000 does not beat step10000 by normalized WER, stop Stage10b and use the already prepared balanced2M source-index split to generate the offline CTC draft cache for the next draft-conditioned CTC+AR stage.
		- 2026-06-15 Stage10b `step-16000` confirmed the WER plateau: `36.32% / 23.31%`, exact `16/128`. CER is the best seen in Stage10b, but the primary normalized WER remains worse than the `step-10000` best (`35.62% / 23.55%`). Stop the active CTC continuation, keep `wercer_best.pt` pointing at `step-10000.pt`, and begin balanced2M offline CTC draft generation from that best checkpoint. This draft cache is the next training input for draft-conditioned CTC+AR rather than continuing low-LR CTC-only training.
		- 2026-06-15 Stage10b draft-prep note: the full GigaSpeech XL + WenetSpeech L length index has `22,904,403` rows (`8,282,988` GigaSpeech, `14,621,415` WenetSpeech). A full offline draft cache at current CTC decode throughput would take multiple days and would be Chinese-heavy. Before the next draft-conditioned CTC+AR run, build a fixed, reproducible, compact, source-balanced training subset for draft generation, initially around `2M` rows with a 1:1 GigaSpeech/WenetSpeech quota. The draft generation script should accept this sampled length-index override while still using the original WebDataset root/config and should filter the AR training length index to the generated draft ids.
		- 2026-06-15 Stage10b balanced draft index artifact: `scripts/sample_length_index_by_source.py` produced `/media/usbhd/rwkvasr_runs/sensevoice_rwkv_stage10b_fullmix_ctc_noaug_from_stage9werbest_bs12_lr5e6_zero1_nockpt_4x4090/ctc_draft_cache_stage10b_balanced2m/source_lengths.sampled.jsonl` with exactly `2,000,000` train rows, sampled reproducibly with seed `20260615` as `1,000,000` GigaSpeech + `1,000,000` WenetSpeech. Use this sampled index through `DRAFT_SOURCE_LENGTH_INDEX_PATH` for the next offline CTC draft cache; do not start full 22.9M-row draft generation unless this smaller balanced AR stage shows clear value.
		- 2026-06-15 draft-generation coverage guard: the old Stage7 source-pattern split only covered low-numbered shard ranges and would miss many Stage10b balanced rows such as `GSXL-000232-*` and `WSL-001112-*`. For sampled balanced length indexes, prefer an index-partition mode that writes one mixed length-index part per physical GPU and runs prediction with `--webdataset-shard-pattern "*.tar"`. This keeps coverage exact and avoids brittle shard-range pattern maintenance.
		- 2026-06-15 draft-generation prepare-only verification: `DRAFT_PARTITION_MODE=round_robin_index DRAFT_INDEX_PARTS=4 DRAFT_PREPARE_ONLY=1` split the Stage10b balanced2M sampled index into `length_parts/part_00.jsonl` ... `part_03.jsonl`, exactly `500,000` rows each. Use the same partition mode for the formal four-GPU CTC draft cache generation after the active CTC continuation stops improving.
		- 2026-06-15 Stage10b balanced2M draft generation: after Stage10b plateaued by WER, start a four-GPU offline CTC draft cache from `wercer_best.pt` (`step-10000.pt`) using the prepared balanced2M sampled index. `DRAFT_BATCH_SIZE=8` was stable but only produced about `66` records/sec total; `DRAFT_BATCH_SIZE=16` used roughly `8-14GB` per GPU and produced a similar throughput, indicating CPU feature extraction / decode / WebDataset I/O is the bottleneck rather than GPU memory. Keep the batch16 run active unless it OOMs, then fall back to batch8 or batch12.
		- Long draft-cache generation must be resumable at part granularity. If a part JSONL exists without its `.done` marker, a restart should not delete it by default; instead it should build a temporary length index excluding already emitted `utt_id`s and append only missing predictions to the same part file. This keeps multi-hour draft jobs from losing progress after a process crash or host interruption.
		- Draft-cache length-index partitioning should preserve tar locality. A sampled index in random row order causes each GPU worker to jump across many `.tar` shards, leaving GPUs mostly idle while loader workers wait on random disk reads. For large draft generation, prefer a `shard_round_robin_index` partition mode that groups rows by `shard_name`, sorts each shard by member offset/key, and assigns whole shards to GPU parts with approximately balanced row counts. This should reduce redundant tar opens and random seeks compared with row-level round-robin partitioning.
		- 2026-06-15 planned Stage11 draft-conditioned CTC+AR: when the balanced2M draft cache finishes and `webdataset_lengths.cached.jsonl` is filtered to cached utterances, initialize from Stage10b `wercer_best.pt` and train a joint CTC+AR stage on the balanced draft subset. Keep CTC labels punctuation-stripped (`text_normalization=ctc`, no EOS), keep AR targets/prompt in runtime text with language confirmation/chat-template behavior, and feed both continuous encoder acoustic hiddens and CTC draft prompt text into the AR decoder. Use a conservative first run (`ctc_loss_weight=0.3`, `decoder_loss_weight=0.7`, LR around `1e-5`, micro-batch `4`, grad-accum `4`, decoder text token budget `1024`) and select later checkpoints by normalized sidecar WER/CER rather than training loss alone.
			- 2026-06-15 Stage11 source-balance guard: the balanced2M sampled index is globally `1M` GigaSpeech + `1M` WenetSpeech, but GigaSpeech examples are much longer on average than WenetSpeech. Pure length bucketing can therefore produce long runs of source-skewed batches even when the input JSONL order is shuffled. Extend `bucket_source_interleave` to the length-index sampler as well as bucket-manifest loading: when enabled, build length-bucketed batches within each source label and round-robin source batch groups before the usual per-epoch global batch shuffle. This keeps frame efficiency while reducing English/Chinese source skew during Stage11.
			- 2026-06-15 Stage11 direction policy: this draft-conditioned AR continuation must not use direction dropout. Keep encoder training/eval in full bidirectional mode by forcing `direction_variant=none`, `p_start=0.0`, `p_max=0.0`, `warmup_steps=0`, and `ramp_steps=0` in the generated Stage11 config. `direction_variant=none` must return full forward/backward masks even if a probability field is accidentally changed later. This does not disable ordinary module dropout or draft-prompt dropout.
			- 2026-06-15 direction-dropout default guard: while the long-term dual-mode design can revisit direction dropout as a separate ablation, the current CTC/CTC+AR stabilization stages must not enable it accidentally. Direction-dropout scheduler, synthetic training, and training config dataclass defaults should therefore be `direction_variant=none`, `p_start=0.0`, `p_max=0.0`, `warmup_steps=0`, and `ramp_steps=0`; any experiment that re-enables direction dropout must do so explicitly in `Spec.md` and in the run config.
			- 2026-06-15 direction-dropout hard guard: the active training code must reject non-disabled direction dropout at runtime for the current stabilization line, because stale generated configs still contain historical `direction_variant=drop_both` values. This guard applies only to encoder direction masking. It must not be confused with ordinary model parameter dropout or decoder CTC-draft prompt dropout, which may still be used as regularization/data augmentation.
			- 2026-06-15 Stage11 draft-noise guard: early balanced2M draft inspection shows usable near-transcripts but also a material wrong-language failure mode in GigaSpeech rows. A live 2k-row front-of-cache sanity sample measured about `37.57%` WER / `23.34%` CER on early English rows and included English references decoded as Chinese text. Draft-conditioned AR training should therefore support deterministic CTC draft dropout in the decoder prompt. Start Stage11 with random draft dropout around `0.20`, and drop clearly wrong-language drafts with probability `1.00` so AR does not condition on high-confidence nonsensical cross-language hints. This changes only the prompt hint; CTC labels remain clean, AR targets still include language confirmation/runtime text, and continuous encoder acoustic hiddens remain in the decoder context.
			- 2026-06-15 live Stage11 draft-quality sample: at `1,201,152` generated draft rows, a deterministic 20k-row reservoir sample (`seed=20260615`, metric normalization `ctc`) measured corpus `37.61%` WER / `23.67%` CER overall, `36.55%` / `22.87%` on GigaSpeech English, and `43.62%` / `43.64%` on WenetSpeech Chinese. Empty normalized drafts were rare (`0.07%`), but English wrong-language drafts remained material (`21.61%` of sampled English refs had CJK in the draft). Keep `decoder_ctc_draft_language_mismatch_dropout_prob=1.00`; keep the random draft dropout at `0.20` for the first Stage11 run so the decoder still sees useful near-transcripts while learning to recover from missing/noisy hints.
			- 2026-06-15 Stage11 mixed-script draft guard: wrong-language detection must not rely only on majority script. Some early GigaSpeech drafts are mixed strings such as CJK tokens plus English words; these can be majority-Latin but still impossible for an English reference transcript. For English reference text, any CJK character in the CTC draft should be treated as a language mismatch. For Chinese reference text, a Latin-only draft should be treated as a mismatch. This guard only blanks the draft prompt according to `decoder_ctc_draft_language_mismatch_dropout_prob`; it must not remove the training sample or change CTC / AR targets.
				- 2026-06-15 Stage11 sidecar reproducibility guard: because AR preview uses sampling rather than greedy/beam decode, sidecar checkpoint selection must pass and record a fixed sampling seed. Use `SIDECAR_AR_SEED=20260615` unless an ablation explicitly changes the decode policy; otherwise normalized WER/CER comparisons between checkpoints include decode-time variance.
				- 2026-06-15 Stage11 restart guard: the balanced2M draft generation and draft-conditioned CTC+AR run are long enough that a silent training exit should be handled automatically. Run a separate guard session that waits for the final draft cache and filtered length index, then restarts Stage11 through the existing start script with `resume_from: auto` only when no matching training process is active and the latest exported step checkpoint is below `max_steps`. The Stage11 start script itself must own a run-dir lock and re-check for an active matching training process so the after-draft handoff and restart guard cannot race into duplicate training commands. This preserves DeepSpeed `latest_checkpoint.yaml` / `epoch_batch_offset` resume semantics and must not restart a normally completed run.
				- 2026-06-15 Stage11 process-detection fix: training liveness checks must match only actual training launchers or trainer children for the generated config, not arbitrary shell, grep, sidecar, or monitor commands that merely contain the config path. A false positive here can skip the only training start while sidecar waits forever for `train_config.yaml`. Use command-name and argument filtering in both the handoff start script and the restart guard.
				- 2026-06-15 Stage11 cached-subset loader fix: when a run explicitly provides `webdataset_length_index_path` for a sampled/draft-cached subset and leaves `webdataset_bucket_manifest_path` unset, the loader must prefer that explicit length index and must not auto-discover the root dataset's default bucket manifest. The default bucket manifest was built for the full GigaSpeech/WenetSpeech tar set and bypasses the cached 2M filter, causing repeated `Missing CTC draft` decode skips and inaccurate data-progress semantics. Only use a bucket manifest in this situation when the config explicitly names one that was built for the same cached subset.
		- 2026-06-15 Stage11 prediction guard: prediction / sidecar / public benchmark should not inherit training-time random draft dropout by default because it is a robustness regularizer, not the best inference policy. They should, however, be able to apply the deterministic wrong-language draft guard from the checkpoint tokenizer config so AR is not conditioned on a first-pass transcript in the wrong language. Evaluation commands may still override random draft dropout explicitly for ablations, but the default prediction path keeps random draft dropout at `0.0` and uses the saved mismatch-dropout setting.
		- 2026-06-15 Stage11 metric-normalization guard: sidecar and public benchmark scripts must keep prediction-time text normalization separate from metric-time normalization. Prediction/ref rendering can stay `runtime` so AR punctuation and readable previews are preserved, but WER/CER should default to the stricter ASR-content metric normalization equivalent to the CTC label normalizer: strip non-pronounced punctuation/symbols/non-speech annotations, remove project-generated language-confirmation prefixes, case-fold, and verbalize Arabic numerals into language-appropriate spoken forms before WER/CER tokenization. This value must be wired explicitly as `METRIC_NORMALIZATION=ctc` for Stage11 sidecar rather than relying on helper defaults. This avoids penalizing acceptable pairs such as `21` vs `twenty one` or `2024年` vs `二零二四年` while keeping raw pred/ref JSONL available for later rescoring.
		- 2026-06-15 Stage11 sidecar length-diagnostic fix: preview `pred/ref` length ratios and short/long comments must use the same normalized ASR-content text as WER/CER, not raw decoder token counts. Raw AR token counts include required language-confirmation metadata such as `This is English text.` / `这是中文文字。`, so using them for length-bias diagnosis overstates AR over-generation and can make otherwise correct punctuation/language-format outputs look long.
			- 2026-06-15 Stage11 sampling fix: AR sidecar evaluation must use standard nucleus sampling semantics. Top-p filtering should keep the smallest sorted token set whose cumulative probability reaches or exceeds `top_p`, always keeping at least one token. An implementation that keeps only tokens with cumulative probability `<= top_p` drops the threshold-crossing token and makes conservative ASR sampling narrower than intended, sometimes making the sidecar look closer to greedy than the configured sampling policy.
			- 2026-06-15 Stage11 early stop decision: the first balanced2M draft-conditioned CTC+AR run (`ctc_loss_weight=0.3`, `decoder_loss_weight=0.7`, `decoder_ctc_draft_dropout_prob=0.20`, LR `1e-5`) improved the CTC branch on the fixed 64-sample hard preview (`step-2500` CTC `36.97% / 23.98%`) but the AR branch regressed and showed free paraphrase / repetition failure modes (`step-3000` AR `50.71% / 34.89%`, WenetSpeech AR `67.50% / 67.50%`). Stop this run early rather than letting a decoder-heavy objective degrade the correction behavior.
			- 2026-06-15 Stage11b correction restart: restart from the Stage10b CTC `wercer_best.pt`, not from the unstable Stage11 joint checkpoint, and use a more conservative correction recipe: keep `direction_variant=none`, keep the same balanced2M draft cache and source-interleaved length index, set `ctc_loss_weight=0.8`, `decoder_loss_weight=0.2`, lower LR to about `5e-6`, and set random `decoder_ctc_draft_dropout_prob=0.0` while retaining wrong-language draft dropout at `1.0`. The intent is to make AR learn small draft/audio corrections without overpowering the CTC acoustic objective or learning to hallucinate around missing draft hints.
			- 2026-06-15 Stage11b early-stop decision: the conservative restart still failed as a direct final ASR decoder. On the fixed 64-sample hard sidecar, `step-500` measured CTC `38.81% / 24.70%` vs AR sampling `53.12% / 35.03%`; `step-1000` worsened to CTC `39.24% / 24.51%` vs AR sampling `59.35% / 38.64%`, with WenetSpeech AR `76.25% / 87.50%`. Typical failures are repeated phrases (`不不不...`, `she's in the sleep...`) and fluent semantic rewrites. Stop Stage11b at `step-1000`; more of this objective is not a good use of compute.
			- 2026-06-15 Stage11c decode-safety requirement: draft-conditioned AR sampling must not be allowed to replace a stronger CTC transcript with an unconstrained hallucination. Add an optional prediction-time CTC-draft fallback guard, disabled by default for ablations and enabled for Stage11c sidecar/final-output evaluation. The guard still runs sampling, but if the sampled ASR-content text diverges too far from the same-sample CTC draft by normalized CER, has an abnormal normalized length ratio, or contains obvious repeated spans, the reported final `pred_text` falls back to the CTC draft and records the fallback reason in debug metadata. This is a safety floor, not a claim that AR has improved; a successful Stage11c still needs AR to beat or safely improve CTC on normalized WER/CER.
			- 2026-06-15 Stage11c training restart: restart again from Stage10b `wercer_best.pt`, freeze the encoder and CTC head, and train only the RWKV decoder plus encoder-to-decoder projection on the balanced2M draft-conditioned subset. This keeps the CTC acoustic baseline fixed while testing whether the decoder can learn a correction/scoring role. Use full bidirectional masks (`direction_variant=none`) and keep CTC labels clean even if `ctc_loss_weight` is reduced to zero for this decoder-only stage.
			- 2026-06-15 CTC n-best rescoring fix: when using RWKV decoder sequence scoring to rerank CTC beam candidates, pass the same per-sample `decoder_prompt_before_audio` / CTC-draft prompt used by Stage11 training. Scoring CTC candidates without the draft prompt is off-policy for the draft-conditioned decoder and should not be used to judge whether CTC-constrained AR can help.
			- 2026-06-15 Stage11c sidecar draft-cache fix: sidecar CTC preview must not inherit the train-set `decoder_ctc_draft_cache_path` or `decoder_ctc_draft_missing_policy=error` from the training config, because public/eval preview samples are not guaranteed to exist in the train draft cache. CTC preview should run without a decoder draft cache, then draft-conditioned AR preview should use the just-generated same-checkpoint CTC JSONL as its draft source.
			- 2026-06-15 Stage11c early-stop decision: the decoder-only frozen-encoder run is not improving the ASR objective. On the fixed 64-sample hard sidecar, `step-500` measured CTC/fallback-final AR `38.53% / 25.04%` WER/CER while raw AR was worse at `45.75% / 32.24%`; `step-1000` regressed further to fallback-final AR `38.67% / 25.18%` and raw AR `49.01% / 33.01%`. The fallback guard prevented most hallucinations but also masked the fact that direct sampled AR generation is not yet a reliable correction model. Stop Stage11c at `step-1000` and do not spend the remaining 4k steps on this objective.
			- 2026-06-15 Stage12 CTC recovery plan: return compute to the CTC acoustic path. Start with a short clean/wide replay from Stage10b `wercer_best.pt` on the complete `clean_ctc_voxbox_webdataset` mix (LibriSpeech, AISHELL3, CommonVoice EN/CN) using clean CTC labels, no decoder, no direction dropout, and normalized WER/CER sidecar. The immediate goal is to verify that the current encoder/tokenizer can still improve on clean public-style data before resuming harder GigaSpeech/WenetSpeech fullmix training. Any CTC start script used for Stage12 must explicitly force `direction_variant=none`, `p_start=0.0`, `p_max=0.0`, `warmup_steps=0`, and `ramp_steps=0`.
			- 2026-06-15 Stage12a direction-dropout clarification: the active clean/wide CTC replay uses ordinary model dropout (`dropout=0.1`) plus light SpecAugment, but it does not use direction dropout. The generated config sets `direction_variant=none`, `p_start=0.0`, `p_max=0.0`, `warmup_steps=0`, and `ramp_steps=0`, and the training log confirms full bidirectional encoder masks. Do not describe ordinary parameter dropout or SpecAugment as direction dropout in this stabilization line.
			- 2026-06-15 Stage12a clean64 final: rerun Stage10b `wercer_best.pt` on the same sidecar clean64 slice (`LibriSpeech 16 + AISHELL3 16 + CommonVoice EN 16 + CommonVoice CN 16`) with a CPU-native backend override. The Stage10b baseline is `30.20% / 20.44%` WER/CER, exact `6/64`. Stage12a improved steadily after step3000: `step-4000` `29.74% / 20.19%`, `step-5000` `29.19% / 19.75%`, and final `step-6000` `28.45% / 18.95%`, exact `8/64`. The final `step-6000` checkpoint is recorded as Stage12a `wercer_best.pt`. This confirms that the current encoder/tokenizer still benefits from clean/wide CTC replay with light SpecAugment and no direction dropout, but the absolute error rates remain far from public-paper targets. Next run a full public CTC benchmark from Stage12a `wercer_best.pt` before deciding whether to resume harder GigaSpeech/WenetSpeech CTC, continue clean/public-style replay, or generate a new CTC draft cache for CTC+AR.
			- 2026-06-15 Stage12a public benchmark final: full public CTC eval from Stage12a `wercer_best.pt` improves English but regresses both Chinese public sets. Against the previous public CTC baseline, LibriSpeech test-clean improves from `26.63% / 15.99%` to `23.50% / 13.73%`, LibriSpeech test-other from `41.54% / 27.44%` to `37.07% / 24.07%`, and CommonVoice EN from `67.77% / 46.75%` to `55.79% / 38.31%`; AISHELL-1 regresses from `26.14% / 26.19%` to `32.49% / 32.70%`, and WenetSpeech TEST_NET regresses from `35.55% / 37.72%` to `51.50% / 53.07%`. The cleanwide replay distribution explains the language imbalance: `commonvoice_en` is `77.77%` of samples and `64.88%` of frames, `librispeech` is `15.52%` / `30.63%`, while Chinese is only `aishell3` `4.42%` / `2.59%` plus `commonvoice_cn` `2.29%` / `1.89%`. Also, the current bucket part source-interleave path does not materially fix this for cleanwide because most bucket parts are labeled `mixed`. Do not continue English-heavy cleanwide replay as-is.
			- 2026-06-15 Stage12b plan: build an explicit source-balanced clean/public-anchor CTC stage from the cleanwide length index before resuming hard fullmix. Use all available clean Chinese rows (`aishell3` and `commonvoice_cn`) and a matched English replay budget split across `librispeech` and `commonvoice_en`, rebuild bucket parts from that sampled length index, start from Stage12a `wercer_best.pt`, disable decoder, keep CTC labels normalized with punctuation/non-pronounced symbols removed, keep direction dropout disabled (`direction_variant=none`, `p_start=0.0`, `p_max=0.0`, `warmup_steps=0`, `ramp_steps=0`), and initially avoid stronger SpecAugment until AISHELL-1 recovers. Select checkpoints by public/sidecar normalized WER/CER, not loss alone.
			- 2026-06-15 Stage12b launch: built `/media/usbhd/training_data/asr/curriculum/clean_ctc_voxbox_webdataset/stages/stage12b_source_balanced_clean_public_anchor/webdataset_lengths.jsonl` with `191,106` train rows: `aishell3=62,952`, `commonvoice_cn=32,601`, `librispeech=47,776`, and `commonvoice_en=47,777`; rebuilt `webdataset_buckets_audio_text/manifest.json` with `41` train buckets. Training run `/media/usbhd/rwkvasr_runs/sensevoice_rwkv_stage12b_sourcebalanced_clean_ctc_from_stage12a_werbest_bs12_lr2e6_zero1_nockpt_noaug_nodirdrop_4x4090` starts from Stage12a `wercer_best.pt`, uses LR `2e-6`, `batch_size=12`, grad accum `4`, `max_steps=6000`, `save_every=1000`, `specaugment_enabled=false`, decoder disabled, clean CTC normalization, `tokenizer_append_eos=false`, and direction dropout disabled by guard. Sidecar `sidecar_stage12b_sourcebalanced_ctc` evaluates the same 64-source-balanced clean slice on CPU every `120s`.
			- 2026-06-15 Stage12b sidecar split fix: the first step1000 sidecar produced zero prediction rows because `watch_checkpoint_sidecar` hard-coded WebDataset preview split to `eval`, while the Stage12b sampled length index intentionally contains only `train` rows and the root-level hash eval fallback selected no shards for the source quotas. Add an explicit sidecar WebDataset split option and run Stage12b source-quota preview with `train`; a zero-row prediction preview must not be interpreted as a valid WER/CER result.
			- 2026-06-15 Stage12b early sidecar readout: after the split fix, the train-split clean64 sidecar measured `step-1000` at `28.08% / 18.15%` WER/CER, `step-2000` at `28.26% / 18.01%`, and `step-3000` at `27.90% / 17.79%`. Per-source step3000 metrics were LibriSpeech `23.06% / 13.14%`, AISHELL3 `23.12% / 23.12%`, CommonVoice EN `36.88% / 24.14%`, and CommonVoice CN `35.21% / 35.21%`. This quick slice is not a public benchmark, but it shows the source-balanced replay is no longer worsening Chinese and may be producing a small clean-slice gain by step3000. Sidecar comments for CTC-only runs should include normalized WER/CER directly from the emitted JSONL, not only blank/length diagnostics.
			- 2026-06-16 Stage12b clean64 final: the source-balanced clean replay finished normally at `max_steps=6000` with no direction dropout. The train-split clean64 sidecar measured `step-4000` at `27.99% / 17.56%`, `step-5000` at `27.81% / 17.45%`, and `step-6000` at `27.64% / 17.65%` normalized WER/CER. Step6000 has the best primary WER and the best Chinese CommonVoice CER on this selector, while step5000 has the slightly lower overall CER. Select `step-6000.pt` as Stage12b `wercer_best.pt` for the next full public CTC benchmark because the active objective is bilingual recovery after the English-heavy Stage12a replay. Treat this 64-sample train-split selector only as a checkpoint selector; public AISHELL-1, LibriSpeech, CommonVoice EN, and WenetSpeech results remain the authoritative benchmark.
			- 2026-06-16 Stage12b public benchmark final: full public CTC eval from Stage12b `wercer_best.pt` completed in tmux session `eval_stage12b_public` and left the session open. Compared with Stage12a, Stage12b improved every measured public set: AISHELL-1 `32.49% -> 31.12%` CER, LibriSpeech clean `23.50% -> 22.95%` WER, LibriSpeech other `37.07% -> 36.15%` WER, CommonVoice EN `55.79% -> 54.98%` WER, and WenetSpeech TEST_NET `51.50% -> 49.06%` WER / `53.07% -> 49.50%` CER. This validates source-balanced clean replay as a recovery step, but Chinese public metrics remain much worse than the earlier hard-mix baseline (`AISHELL-1 26.14%`, `WenetSpeech 35.55%`). Do not generate a new AR draft cache from this checkpoint yet; return to hard GigaSpeech/WenetSpeech CTC exposure first.
			- 2026-06-16 Stage13 plan: start a hard-domain fullmix CTC continuation from Stage12b `wercer_best.pt` using the complete `/media/usbhd/training_data/asr/mix/gigaspeech_xl_wenetspeech_l_webdataset` length index and bucket manifest. Keep `decoder_enabled=false`, `ctc_loss_weight=1.0`, `decoder_loss_weight=0.0`, `text_normalization=ctc`, `tokenizer_append_eos=false`, `specaugment_enabled=false`, and no direction dropout (`direction_variant=none`, `p_start=0.0`, `p_max=0.0`, `warmup_steps=0`, `ramp_steps=0`). Use a low LR around `3e-6`, save every `2000` steps, and select later checkpoints by normalized WER/CER on a balanced hard sidecar (`GigaSpeech 64 + WenetSpeech 64`) before rerunning the full public benchmark.
			- 2026-06-16 Stage13 launch: run `/media/usbhd/rwkvasr_runs/sensevoice_rwkv_stage13_fullmix_ctc_from_stage12b_werbest_bs12_lr3e6_zero1_nockpt_noaug_nodirdrop_4x4090` from Stage12b `wercer_best.pt` with full GigaSpeech XL + WenetSpeech L, LR `3e-6`, batch size `12`, grad accumulation `4`, `max_steps=20000`, `save_every=2000`, ZeRO-1, offload disabled, gradient checkpointing disabled, CTC-only objective, and no SpecAugment. The launch guard verified `direction_variant=none`, `p_start=0.0`, `p_max=0.0`, `warmup_steps=0`, and `ramp_steps=0`; the training log also confirms full bidirectional encoder masks. Early startup reached step 200+ at about `1.07 step/s` with roughly `20-21 GiB` reserved per 4090 and no OOM. Sidecar `sidecar_stage13_fullmix_ctc` should evaluate `GigaSpeech 64 + WenetSpeech 64` with normalized CTC WER/CER and a shorter poll interval so step checkpoints are scored promptly.
			- 2026-06-16 Stage13 step2000 sidecar: on the fixed hard eval slice (`GigaSpeech 64 + WenetSpeech 64`), Stage12b initialization measured `38.28% / 24.59%` overall WER/CER, with GigaSpeech `28.82% / 19.71%` and WenetSpeech `49.38% / 50.47%`. Stage13 `step-2000.pt` measured `37.28% / 23.95%` overall, with GigaSpeech `30.41% / 19.91%` and WenetSpeech `45.33% / 45.33%`. This is a net `-1.00` WER and `-0.64` CER improvement over the Stage12b init on the same samples, driven by a large WenetSpeech recovery but with a small GigaSpeech regression. Record `step-2000.pt` as the current Stage13 `wercer_best.pt`, but continue to step4000/6000 before public eval because the next target is to keep WenetSpeech gains while recovering the English hard-set regression.
			- 2026-06-16 Stage13 step4000 sidecar: `step-4000.pt` improved the fixed hard selector to `37.20% / 23.70%` overall WER/CER, with GigaSpeech `29.88% / 19.53%` and WenetSpeech `45.79% / 45.79%`. This is a small but real selector gain over `step-2000.pt` (`37.28% / 23.95%`), mainly by recovering part of the English hard-set regression while keeping most of the WenetSpeech gain over Stage12b init. Copy `step-4000.pt` to Stage13 `wercer_best.pt` and continue training to later checkpoints before public eval. In this run, config `dropout=0.1` is ordinary module/activation dropout; direction dropout remains disabled by `direction_variant=none`, `p_start=0.0`, `p_max=0.0`, `warmup_steps=0`, and `ramp_steps=0`.
			- 2026-06-16 Stage13 fixed-selector artifact: the hard selector set is now materialized as `/media/usbhd/rwkvasr_runs/sensevoice_rwkv_stage13_fullmix_ctc_from_stage12b_werbest_bs12_lr3e6_zero1_nockpt_noaug_nodirdrop_4x4090/wercer_selector_hard128/webdataset_lengths.hard128.jsonl` with exactly `128` eval rows (`64` GigaSpeech English + `64` WenetSpeech Chinese) and explicit `language` / `source_dataset` fields. `wercer_selector_hard128/wercer_checkpoint_metrics.yaml` is preseeded from sidecar outputs for `step-2000.pt` and `step-4000.pt`, so future checkpoint selection can be reproduced without relying on implicit source-quota ordering.
			- 2026-06-16 Stage13 step6000 sidecar: `step-6000.pt` measured `37.13% / 24.02%` overall WER/CER on the same fixed hard selector, with GigaSpeech `30.15% / 20.00%` and WenetSpeech `45.33% / 45.33%`. This is the best primary WER so far (`step-4000` was `37.20%`) and has one more exact sentence (`16/128` vs `15/128`), so copy `step-6000.pt` to Stage13 `wercer_best.pt` under the current WER-first selector. However, the signal is mixed: CER regressed versus `step-4000` (`24.02%` vs `23.70%`) and English hard-set WER/CER moved backward. Do not run the full public benchmark yet; continue to later checkpoints and prefer the next selection only if it improves WER without the same CER / English regression.
			- 2026-06-16 Stage13 step8000 sidecar: `step-8000.pt` regressed to `37.71% / 24.25%` overall WER/CER, with GigaSpeech `30.68% / 20.15%` and WenetSpeech `45.95% / 45.95%`. Even though exact sentence count rose to `17/128`, both sources and both aggregate error rates are worse than the selected `step-6000.pt`; keep Stage13 `wercer_best.pt` at `step-6000.pt`. Continue only long enough to see whether `step-10000.pt` recovers. If `step-10000.pt` also regresses, stop Stage13 and run public CTC eval from the selected best before changing recipe.
				- 2026-06-16 Stage13 step10000 sidecar and stop decision: `step-10000.pt` recovered to `37.13% / 23.70%` overall WER/CER, tying `step-6000.pt` on the primary WER metric while improving secondary CER and English hard-set performance. Per-source metrics are GigaSpeech `29.61% / 19.50%` and WenetSpeech `45.95% / 45.95%`. Select `step-10000.pt` as Stage13 `wercer_best.pt` by the WER-first/CER-second selector. Stop Stage13 here rather than running to 20k because the hard selector has become noisy and the next useful signal should be a full public CTC benchmark from the selected best.
				- 2026-06-16 public-eval throughput note: the first Stage13 public CTC eval launch showed that the existing benchmark driver assigns one dataset per GPU in stage 1 and a single GPU to WenetSpeech TEST_NET in stage 2. This leaves the largest public sets under-parallelized. Add an opt-in CTC manifest-sharding mode to `scripts/run_public_eval_benchmarks.sh` so a public dataset can be split across the available GPUs, merged back into the canonical `<dataset>.ctc.jsonl`, and summarized with the same normalized WER/CER path. Keep the default behavior unchanged; use the sharded mode for Stage13 public eval to reduce wall time without changing metrics.
				- 2026-06-16 Stage13 public eval partial: sharded public CTC eval from Stage13 `step-10000` completed LibriSpeech test-clean, LibriSpeech test-other, AISHELL-1 test, and CommonVoice EN before WenetSpeech finished. The completed public metrics all regressed relative to Stage12b: LibriSpeech clean `22.95 -> 23.56` WER, LibriSpeech other `36.15 -> 39.61` WER, AISHELL-1 `31.12 -> 33.02` CER, and CommonVoice EN `54.98 -> 59.93` WER. This proves that the hard128 selector gain did not transfer to public clean / non-clean English and AISHELL, and Stage13 hard-only continuation causes domain forgetting. Unless WenetSpeech shows an unusually large improvement, do not continue hard-only Stage13.
				- 2026-06-16 Stage13 public eval final: sharded public CTC eval from Stage13 `step-10000` finished normally and left tmux session `eval_stage13_public` open. Compared with Stage12b, Stage13 regressed four public sets: AISHELL-1 `31.12 -> 33.02` CER, LibriSpeech clean `22.95 -> 23.56` WER, LibriSpeech other `36.15 -> 39.61` WER, and CommonVoice EN `54.98 -> 59.93` WER. WenetSpeech TEST_NET improved only modestly from `49.06 / 49.50` to `47.05 / 47.17` WER/CER. This is not a good trade: the hard128 selector gain did not transfer to broad public evaluation, and hard-only continuation caused meaningful public-domain forgetting. Do not use Stage13 as the default next-stage initialization; keep it only as evidence that Wenet can recover with hard exposure.
				- 2026-06-16 Stage14 recovery plan: build a CTC-only public-anchor hard-mix continuation that replays the Stage12b source-balanced clean/public-anchor index while sampling GigaSpeech and WenetSpeech in equal hard quotas. Start from the best available checkpoint after Stage13 public eval comparison, but prefer Stage12b `wercer_best.pt` if Stage13 final public WenetSpeech does not justify the public-regression cost. Keep CTC labels normalized with punctuation/non-pronounced elements removed, `tokenizer_append_eos=false`, `decoder_enabled=false`, `specaugment_enabled=false` initially, and no direction dropout (`direction_variant=none`, `p_start=0.0`, `p_max=0.0`, `warmup_steps=0`, `ramp_steps=0`). The intended first Stage14 dataset is roughly `40-50%` clean/public-anchor replay by sampling with replacement if needed, `25-30%` GigaSpeech, and `25-30%` WenetSpeech, with checkpoint selection by normalized public/sidecar WER-CER rather than the hard128 selector alone.
				- 2026-06-16 Stage14 implementation decision: Stage14 should initialize from Stage12b `wercer_best.pt`, not Stage13, because Stage13's WenetSpeech gain is too small to offset its LibriSpeech/AISHELL/CommonVoice regressions. Use `scripts/start_stage14_public_anchor_hard_ctc_training.sh`, which builds `stage14_public50_hard50` under `/media/usbhd/training_data/asr/curriculum/stage14_public_anchor_hard_mix` with clean replay sampled with replacement, generates a CTC-only config, enforces the no-direction-dropout guard, starts tmux `training`, and starts sidecar `sidecar_stage14_public_anchor_hard_ctc` on a train-split source-quota slice covering LibriSpeech, AISHELL3, CommonVoice EN/CN, GigaSpeech, and WenetSpeech.
				- 2026-06-16 Stage14 launch: built `/media/usbhd/training_data/asr/curriculum/stage14_public_anchor_hard_mix/stages/stage14_public50_hard50/webdataset_lengths.jsonl` with `799,600` rows. The actual train mix is `399,600` clean replay rows (`librispeech=99,920`, `aishell3=131,671`, `commonvoice_en=99,650`, `commonvoice_cn=68,359`) plus `199,800` GigaSpeech and `199,800` WenetSpeech rows; the tiny eval split has `200` GigaSpeech + `200` WenetSpeech rows. Training run `/media/usbhd/rwkvasr_runs/sensevoice_rwkv_stage14_public50_hard50_ctc_from_stage12b_werbest_bs12_lr2e6_zero1_nockpt_noaug_nodirdrop_4x4090` starts from Stage12b `wercer_best.pt`, uses LR `2e-6`, batch size `12`, grad accumulation `4`, `max_steps=12000`, `save_every=2000`, ZeRO-1, CTC-only loss, no SpecAugment, and no direction dropout. Startup loaded Stage12b step6000 with no missing/unexpected keys, passed the direction-dropout guard, and reached early training at about `1.05 step/s` with roughly `20 GiB` used per GPU. Sidecar `sidecar_stage14_public_anchor_hard_ctc` evaluates a 96-sample train-split mixed selector (`12` each LibriSpeech/AISHELL3/CommonVoice EN/CommonVoice CN, `24` each GigaSpeech/WenetSpeech) using normalized CTC WER/CER.
					- 2026-06-16 Stage14 step2000 sidecar: `step-2000.pt` reached the first checkpoint normally and training continued. The mixed 96-sample selector measured `33.99% / 22.88%` normalized WER/CER. A same-selector Stage12b initialization baseline was run for an apples-to-apples comparison and measured `33.92% / 22.70%`, so Stage14 has not produced a net selector gain by 2000 steps. Per-source deltas are mixed: LibriSpeech `24.10 -> 24.93` WER, AISHELL unchanged at `21.85`, CommonVoice EN `49.11 -> 46.43` WER, CommonVoice CN `29.57 -> 30.11`, GigaSpeech `37.28 -> 37.99`, and WenetSpeech `49.34 -> 48.47`. Continue at least to step4000 before changing recipe because the clean-domain regression is small and Wenet/CV-EN moved in the desired direction, but treat step2000 as inconclusive rather than an improvement. Operationally, the sidecar must run on CPU while training uses all four GPUs; a GPU0 sidecar risks OOM because training holds about `21 GiB` on GPU0. A duplicate sidecar briefly truncated the per-source Libri JSONL, but the combined `step-2000.ctc.jsonl` remained complete and was used for source metrics.
					- 2026-06-16 Stage14 step4000 sidecar: `step-4000.pt` again measured `33.99% / 22.88%` normalized WER/CER on the same 96-sample mixed selector, so the run is still not better than the Stage12b same-selector baseline (`33.92% / 22.70%`). Per-source metrics were LibriSpeech `23.55% / 14.83%`, AISHELL3 `21.19% / 21.19%`, CommonVoice EN `48.21% / 31.47%`, CommonVoice CN `30.11% / 30.11%`, GigaSpeech `38.71% / 23.40%`, and WenetSpeech `49.34% / 49.34%`. Clean public-anchor English/AISHELL recovered relative to step2000, but hard GigaSpeech/Wenet did not improve, and CommonVoice CN remains worse than the Stage12b baseline. Continue only to the next checkpoint (`step-6000.pt`) for one more measurement before deciding whether to stop Stage14 and switch to a higher clean-anchor / lower-LR continuation. Direction dropout remains disabled; the active `dropout=0.1` is ordinary module dropout only.
					- 2026-06-16 Stage14 step6000 stop decision: `step-6000.pt` remained flat at `33.99% / 22.90%` on the same 96-sample mixed selector and still did not beat the Stage12b same-selector baseline. Per-source metrics were LibriSpeech `24.10% / 14.96%`, AISHELL3 `21.85% / 21.85%`, CommonVoice EN `47.32% / 31.27%`, CommonVoice CN `29.57% / 29.57%`, GigaSpeech `37.99% / 22.93%`, and WenetSpeech `49.78% / 51.97%`. Since the hard domains did not improve after three checkpoints and WenetSpeech regressed, stop this Stage14 run manually after preserving `step-6000.pt` and its sidecar outputs. Do not promote any Stage14 checkpoint over Stage12b.
					- 2026-06-16 Stage14b plan: restart from Stage12b `wercer_best.pt` with a more conservative CTC-only public-anchor/hard mix: `70%` source-balanced clean/public-anchor replay and `30%` hard data split evenly between GigaSpeech and WenetSpeech. Lower LR to about `1e-6`, keep batch size `12`, grad accumulation `4`, ZeRO-1, no offload, no gradient checkpointing, no SpecAugment initially, clean CTC normalization, `tokenizer_append_eos=false`, and no direction dropout (`direction_variant=none`, `p_start=0.0`, `p_max=0.0`, `warmup_steps=0`, `ramp_steps=0`). Use the same mixed 96-sample sidecar selector for fast checkpoint selection, but require a public benchmark before promoting Stage14b over Stage12b for later CTC+AR work.
					- 2026-06-16 Stage14b launch: built `/media/usbhd/training_data/asr/curriculum/stage14_public_anchor_hard_mix/stages/stage14b_public70_hard30/webdataset_lengths.jsonl` with `799,440` rows total and `799,200` train rows: `559,440` clean replay, `119,880` GigaSpeech, and `119,880` WenetSpeech. Training run `/media/usbhd/rwkvasr_runs/sensevoice_rwkv_stage14b_public70_hard30_ctc_from_stage12b_werbest_bs12_lr1e6_zero1_nockpt_noaug_nodirdrop_4x4090` starts from Stage12b `wercer_best.pt`, uses LR `1e-6`, batch size `12`, grad accumulation `4`, `max_steps=10000`, `save_every=2000`, ZeRO-1, CTC-only loss, no SpecAugment, and CPU sidecar `sidecar_stage14b_public_anchor_hard_ctc`. The generated config and launch guard confirm no direction dropout. Early training reached the first batches at about `0.98 step/s` with roughly `19-20 GiB` used per GPU, and the sidecar is waiting for stable checkpoints with the same 96-sample mixed selector.
					- 2026-06-16 Stage14b step2000 apples-to-apples sidecar: the raw 96-sample sidecar from `step-2000.pt` measured `31.66% / 19.32%`, lower than the earlier Stage14 selector numbers, but Stage14b uses a different sampled length index and therefore required a same-selector Stage12b baseline. Running Stage12b `wercer_best.pt` on the exact Stage14b selector measured `31.90% / 19.39%`, so the true early gain is small but positive: `-0.23` WER and `-0.08` CER. Per-source deltas are LibriSpeech `21.89 -> 21.39` WER, AISHELL3 `24.44 -> 25.19`, CommonVoice EN unchanged in WER with CER `28.45 -> 28.62`, CommonVoice CN unchanged, GigaSpeech `29.46 -> 28.68`, and WenetSpeech unchanged. Continue Stage14b to `step-4000.pt` before making a stop/continue decision; do not promote Stage14b yet because the gain is narrow and Chinese clean did not improve.
					- 2026-06-16 Stage14b step4000 stop decision: `step-4000.pt` regressed on the same Stage14b selector to `32.28% / 19.52%`, worse than both Stage14b `step-2000.pt` (`31.66% / 19.32%`) and the Stage12b same-selector baseline (`31.90% / 19.39%`). Per-source deltas from Stage12b were AISHELL3 `+0.74` WER/CER, CommonVoice CN `+2.10`, CommonVoice EN flat in WER but `+0.16` CER, LibriSpeech flat, GigaSpeech flat in WER with only `-0.09` CER, and WenetSpeech `+0.43`. Stop Stage14b manually after preserving `step-2000.pt` and `step-4000.pt`; only `step-2000.pt` remains a weak candidate for public eval, and it should not replace Stage12b unless full public metrics show a net improvement.
					- 2026-06-16 Stage14b public-eval launch: started tmux `eval_stage14b_public_step2000` to run sharded CTC public benchmark from Stage14b `step-2000.pt`, outputting to `/media/usbhd/rwkvasr_runs/sensevoice_rwkv_stage14b_public70_hard30_ctc_from_stage12b_werbest_bs12_lr1e6_zero1_nockpt_noaug_nodirdrop_4x4090/public_eval_stage14b_step2000_ctc_sharded`. Use `CTC_SHARD_DATASETS=1`, `CTC_SHARD_COUNT=4`, `DEVICES=0,1,2,3`, and `RUN_AR=0` to match the Stage13 sharded public CTC path. Compare against Stage12b public metrics before deciding whether Stage14b has any real value.
					- 2026-06-16 Stage14b public-eval final: Stage14b `step-2000.pt` public CTC eval finished. Relative to Stage12b public metrics, it regressed AISHELL-1 `31.12 -> 31.19` CER, LibriSpeech clean `22.95 -> 22.99` WER / `13.38 -> 13.44` CER, LibriSpeech other `36.15 -> 36.66` WER / `23.50 -> 23.76` CER, and CommonVoice EN `54.98 -> 55.28` WER / `38.30 -> 38.57` CER. It improved only WenetSpeech TEST_NET `49.06 -> 48.23` WER / `49.50 -> 48.62` CER. This confirms that even the best Stage14b checkpoint is a hard-domain tradeoff, not a broad improvement. Do not promote Stage14b; keep Stage12b as the current public-best CTC checkpoint. Next return to a clean/source-balanced low-LR continuation from Stage12b to reduce public clean and CommonVoice error before trying hard-domain replay again.
					- 2026-06-16 Stage15 clean-stability plan: continue from Stage12b `wercer_best.pt` on the existing clean/source-balanced public-anchor index only (`stage12b_source_balanced_clean_public_anchor`, `191,106` train rows: LibriSpeech `47,776`, AISHELL3 `62,952`, CommonVoice EN `47,777`, CommonVoice CN `32,601`). Do not mix GigaSpeech or WenetSpeech in this stabilization stage, because Stage13/14/14b showed hard replay improves Wenet at the cost of broader public clean/CommonVoice regression. Use CTC-only loss, `text_normalization=ctc`, punctuation/non-pronounced cleanup, `tokenizer_append_eos=false`, no SpecAugment initially, ZeRO-1, batch size `12`, grad accumulation `4`, low LR about `1e-6`, and checkpoint every `1000` steps. Direction dropout remains forbidden: `direction_variant=none`, `p_start=0.0`, `p_max=0.0`, `warmup_steps=0`, and `ramp_steps=0`. Here `dropout=0.1` means ordinary module/activation dropout only. Fast sidecar should run on CPU with a fixed 64-sample clean selector (`16` each LibriSpeech/AISHELL3/CommonVoice EN/CommonVoice CN) and normalized CTC WER/CER; a full public benchmark is still required before promoting any Stage15 checkpoint over Stage12b.
					- 2026-06-16 Stage15 launch: added `scripts/start_stage15_clean_stability_ctc_training.sh` and generated `configs/generated/sensevoice_rwkv_stage15_sourcebalanced_clean_ctc_from_stage12b_werbest_4x4090_deepspeed.yaml`. The run directory is `/media/usbhd/rwkvasr_runs/sensevoice_rwkv_stage15_sourcebalanced_clean_ctc_from_stage12b_werbest_bs12_lr1e6_zero1_nockpt_noaug_nodirdrop_4x4090`. It starts from Stage12b `wercer_best.pt` (`step=6000`, `missing=0`, `unexpected=0`), uses LR `1e-6`, `max_steps=6000`, `save_every=1000`, ZeRO-1 without optimizer offload, no gradient checkpointing, no SpecAugment, CTC-only loss, clean CTC normalization, and no direction dropout. Startup logs show full-bidirectional masks and visible tqdm progress at about `0.95 step/s`; GPU memory is roughly `19 GiB` per 4090. CPU sidecar `sidecar_stage15_clean_ctc` watches the same run with the fixed 64-sample clean selector and normalized CTC WER/CER.
					- 2026-06-16 Stage15 early sidecar: a same-selector Stage12b baseline was run on the exact Stage15 clean64 selector before interpreting the Stage15 checkpoints. Baseline measured `27.55% / 17.81%` normalized WER/CER overall. Stage15 `step-1000.pt` measured `27.28% / 17.73%`, and `step-2000.pt` measured `27.19% / 17.73%`. The improvement is real but small: `step-2000` is `-0.35` WER and `-0.08` CER versus Stage12b on the same 64 samples. Per-source `step-2000` deltas versus baseline are LibriSpeech `23.46/13.49 -> 22.86/13.27`, AISHELL unchanged `22.11/22.11`, CommonVoice EN `36.25/23.89 -> 36.25/24.27` (WER flat, CER worse), and CommonVoice CN `34.08/34.08 -> 33.71/33.71`. Continue to later checkpoints before public eval; this early signal is too narrow to promote Stage15 over Stage12b.
					- 2026-06-16 Stage15 final clean64 selector: the run finished normally at `max_steps=6000` and exited with status `0`; GPU memory returned to idle. The fixed clean64 sidecar measured:
					  - Stage12b init baseline: `27.55% / 17.81%`
					  - `step-1000`: `27.28% / 17.73%`
					  - `step-2000`: `27.19% / 17.73%`
					  - `step-3000`: `27.02% / 17.42%`
					  - `step-4000`: `27.02% / 17.59%`
					  - `step-5000`: `26.93% / 17.42%`
					  - `step-6000`: `27.02% / 17.48%`
					  The WER-first/CER-second selected checkpoint is `step-5000.pt`, copied to `wercer_best.pt`, with a same-selector gain of `-0.62` WER and `-0.39` CER versus Stage12b. Per-source gains are mostly LibriSpeech (`23.46/13.49 -> 22.66/12.97`) and CommonVoice EN WER (`36.25 -> 35.00` at step5000); AISHELL is unchanged at the best step, and CommonVoice CN improves only `34.08 -> 33.71`. This is a valid but narrow clean-selector gain, so it still requires a full public CTC benchmark before replacing Stage12b as the project default initialization.
					- 2026-06-16 Stage15 public-eval launch: started tmux `eval_stage15_public` to run sharded public CTC benchmark from Stage15 `wercer_best.pt` (`step-5000.pt`), outputting to `/media/usbhd/rwkvasr_runs/sensevoice_rwkv_stage15_sourcebalanced_clean_ctc_from_stage12b_werbest_bs12_lr1e6_zero1_nockpt_noaug_nodirdrop_4x4090/public_eval_stage15_wercer_best_ctc_sharded`. Use the same Stage13/14b public eval path: `CTC_SHARD_DATASETS=1`, `CTC_SHARD_COUNT=4`, `DEVICES=0,1,2,3`, `RUN_AR=0`, and default public metric normalization `ctc`. Compare against Stage12b public metrics before promoting Stage15.
					- 2026-06-19 Stage15 public-eval final: the sharded public CTC benchmark completed normally on 2026-06-16. Compared with Stage12b public-best, Stage15 `wercer_best.pt` (`step-5000.pt`) improved AISHELL-1 `31.12 -> 30.89` CER, LibriSpeech test-clean `22.95/13.38 -> 22.88/13.32` WER/CER, CommonVoice EN WER `54.98 -> 54.96`, and WenetSpeech TEST_NET `49.06/49.50 -> 48.95/49.37`; it regressed LibriSpeech test-other `36.15/23.50 -> 36.34/23.53` and CommonVoice EN CER `38.30 -> 38.31`. This is a tiny but broader net gain than Stage14b, so Stage15 can be used as the next initialization, but it is still far from public ASR target quality and should not be described as paper-level.
					- 2026-06-19 Stage16 plan: continue from Stage15 `wercer_best.pt` without changing the BiRWKV architecture. Use a conservative CTC-only light-hard replay stage with about `80%` clean/source-balanced public-anchor replay and `20%` hard data split evenly between GigaSpeech and WenetSpeech. Lower LR to `5e-7`, keep batch size `12`, grad accumulation `4`, `max_steps=8000`, `save_every=1000`, ZeRO-1, no optimizer offload, no gradient checkpointing, no SpecAugment initially, `text_normalization=ctc`, `tokenizer_append_eos=false`, and no direction dropout (`direction_variant=none`, `p_start=0.0`, `p_max=0.0`, `warmup_steps=0`, `ramp_steps=0`). The purpose is to recover a small amount of Wenet/GigaSpeech robustness while using a much stronger clean anchor than Stage14/14b to avoid public clean/CommonVoice forgetting. Fast sidecar selection should be treated only as a training-health signal; promotion still requires full public CTC eval against Stage15 and Stage12b.
					- 2026-06-19 Stage16 launch: added `scripts/start_stage16_public_anchor_light_hard_ctc_training.sh` and generated `configs/generated/sensevoice_rwkv_stage16_public80_hard20_ctc_from_stage15_werbest_4x4090_deepspeed.yaml`. The stage data lives under `/media/usbhd/training_data/asr/curriculum/stage16_public_anchor_light_hard_mix/stages/stage16_public80_hard20` with `599,400` train rows: clean replay `479,520`, GigaSpeech `59,940`, and WenetSpeech `59,940`. The run directory is `/media/usbhd/rwkvasr_runs/sensevoice_rwkv_stage16_public80_hard20_ctc_from_stage15_werbest_bs12_lr5e7_zero1_nockpt_noaug_nodirdrop_4x4090`. Startup loaded Stage15 `wercer_best.pt` (`step=5000`, `missing=0`, `unexpected=0`), uses LR `5e-7`, CTC-only loss, no SpecAugment, ZeRO-1 without optimizer offload, no gradient checkpointing, and no direction dropout. The launch log confirmed full bidirectional encoder masks, entered the DeepSpeed training loop, and started CPU sidecar `sidecar_stage16_public_anchor_light_hard_ctc` on an 80-sample mixed selector. A script guard now reuses the existing Stage16 length index and bucket manifest unless `REBUILD_CURRICULUM=1`, so interrupted restarts do not rescan the full hard length index.
					- 2026-06-19 Stage16 early sidecar gate: a same-selector Stage15 `wercer_best.pt` baseline was run by symlinking it as `step-0.pt` into `/media/usbhd/rwkvasr_runs/stage16_same_selector_baselines/stage15_werbest_on_stage16_mixed80` while reusing the Stage16 config and fixed 80-sample mixed selector (`12` each clean LibriSpeech/AISHELL3/CommonVoice EN/CommonVoice CN, `16` each GigaSpeech/WenetSpeech). Baseline measured `28.76% / 17.31%` WER/CER, mean `blank_top1=0.8150`, and pred/ref ratio `1.0079`. Stage16 `step-1000.pt` was identical on this selector (`28.76% / 17.31%`), `step-2000.pt` measured `28.76% / 17.16%`, `step-3000.pt` improved to `28.58% / 17.10%`, `step-4000.pt` measured `28.49% / 17.13%`, `step-5000.pt` reached the current best `28.05% / 16.92%` with mean `blank_top1=0.8142` and pred/ref ratio `1.0129`, and `step-6000.pt` measured `28.14% / 16.92%` with the same blank/length health. This is a stable early signal rather than a promotion result: WER and CER improve versus the Stage15 same-selector baseline with no output collapse, but `step-6000` slightly rebounds on WER versus `step-5000`. Continue Stage16 through later checkpoints and use the sidecar-best candidate for full public CTC eval before promoting over Stage15.
					- 2026-06-19 Stage16 final sidecar: the run completed `max_steps=8000`, training exited, and GPUs returned to idle. The fixed 80-sample mixed selector measured `step-7000.pt` at `28.05% / 16.95%` and `step-8000.pt` at `28.14% / 16.98%`; both kept healthy blank and pred/ref ratios. Sidecar-best remains `step-5000.pt` by WER/CER tie-break (`28.05% / 16.92%`). Use `step-5000.pt` as the Stage16 public-eval candidate; do not promote Stage16 unless full public CTC eval improves over Stage15 public metrics without broader clean/CommonVoice regression.
					- 2026-06-19 Stage16 public-eval final: sharded public CTC eval of `step-5000.pt` completed normally in `/media/usbhd/rwkvasr_runs/sensevoice_rwkv_stage16_public80_hard20_ctc_from_stage15_werbest_bs12_lr5e7_zero1_nockpt_noaug_nodirdrop_4x4090/public_eval_stage16_step5000_ctc_sharded`. Results were AISHELL-1 `30.93` CER, LibriSpeech test-clean `22.88/13.30`, LibriSpeech test-other `36.49/23.59`, CommonVoice EN `55.12/38.34`, and WenetSpeech TEST_NET `48.31/48.71`. Compared with Stage15, Stage16 improves WenetSpeech substantially (`48.95/49.37 -> 48.31/48.71`) and slightly improves LibriSpeech test-clean CER (`13.32 -> 13.30`), but regresses AISHELL (`30.89 -> 30.93`), LibriSpeech test-other (`36.34/23.53 -> 36.49/23.59`), and CommonVoice EN (`54.96/38.31 -> 55.12/38.34`). Stage16 should be treated as a useful hard-robustness intermediate, not a promoted public-best checkpoint.
					- 2026-06-19 Stage17 plan: perform a clean-repair CTC-only stage from Stage16 `step-5000.pt`, still without changing BiRWKV architecture or enabling direction dropout. Use a stronger clean/public anchor than Stage16, approximately `90%` clean source-balanced public replay and `10%` hard replay split GigaSpeech/WenetSpeech, lower LR to about `2.5e-7`, keep batch size `12`, grad accumulation `4`, ZeRO-1, no optimizer offload, no gradient checkpointing, no SpecAugment, `text_normalization=ctc`, `tokenizer_append_eos=false`, and `direction_variant=none`. The goal is to repair CommonVoice/Libri-other/AISHELL regression while preserving part of the Stage16 WenetSpeech gain; promotion requires full public CTC eval against Stage15 and Stage16, not sidecar alone.
					- 2026-06-19 Stage17 launch: added wrapper `scripts/start_stage17_public_repair_ctc_training.sh`, reusing the Stage16 launcher with Stage17-specific overrides. The curriculum lives under `/media/usbhd/training_data/asr/curriculum/stage17_public_repair_light_hard_mix/stages/stage17_public90_hard10` with `599,400` train rows: clean replay `539,460`, GigaSpeech `29,970`, and WenetSpeech `29,970`. Generated config is `configs/generated/sensevoice_rwkv_stage17_public90_hard10_ctc_from_stage16_step5000_4x4090_deepspeed.yaml`; run directory is `/media/usbhd/rwkvasr_runs/sensevoice_rwkv_stage17_public90_hard10_ctc_from_stage16_step5000_bs12_lr2p5e7_zero1_nockpt_noaug_nodirdrop_4x4090`. Startup loaded Stage16 `step-5000.pt` (`missing=0`, `unexpected=0`), uses LR `2.5e-7`, `max_steps=6000`, CTC-only loss, no SpecAugment, no gradient checkpointing, and no direction dropout. The CPU sidecar `sidecar_stage17_public_repair_ctc` uses an 80-sample selector weighted toward clean replay (`16` each clean source, `8` each hard source).
					- 2026-06-19 Stage17 early sidecar: a same-selector Stage16 `step-5000.pt` baseline was run under `/media/usbhd/rwkvasr_runs/stage17_same_selector_baselines/stage16_step5000_on_stage17_cleanheavy80`, measuring `26.79% / 17.00%` WER/CER, mean `blank_top1=0.8278`, and pred/ref ratio `0.9763`. Stage17 `step-1000.pt`, `step-2000.pt`, and `step-3000.pt` are effectively unchanged on the same selector (`26.79% / 17.00%`, no collapse). This suggests the `2.5e-7` repair stage is conservative; continue to later checkpoints, but if public metrics do not move, the next repair attempt should raise LR modestly or increase clean-only replay rather than changing BiRWKV architecture.
					- 2026-06-20 Stage17 step4000 sidecar: `step-4000.pt` measured `26.87% / 17.08%` on the same clean-heavy 80-sample selector, worse than the Stage16 `step-5000.pt` same-selector baseline by `+0.08` WER and `+0.08` CER. Blank and length ratios stayed healthy (`blank_top1=0.8280`, pred/ref ratio `0.9768`), so this is not output collapse; it is a training-direction signal that Stage17's low-LR Stage16 repair is not improving the public-anchor objective. Let the run finish for trace completeness, but do not promote Stage17 unless later sidecar and full public eval reverse this. The next active attempt should return to Stage15 public-best initialization and use source-weighted clean replay for public weak spots rather than continuing from the Stage16 hard-domain branch.
					- 2026-06-20 Stage18 plan: continue CTC-only training from Stage15 `wercer_best.pt` (`step-5000.pt`) without changing the BiRWKV architecture, TimeMixer implementation, CTC label space, or direction-dropout policy. Build a clean-only source-weighted replay stage under the clean curriculum root, sampled with replacement from `stage12b_source_balanced_clean_public_anchor`, with approximately `600k` train rows weighted toward current public weak spots: CommonVoice EN `210k`, LibriSpeech `150k`, AISHELL3 `150k`, and CommonVoice CN `90k`. Use LR `7.5e-7`, `max_steps=8000`, `save_every=1000`, batch size `12`, grad accumulation `4`, ZeRO-1, no optimizer offload, no gradient checkpointing, no SpecAugment initially, `text_normalization=ctc`, `tokenizer_append_eos=false`, and no direction dropout (`direction_variant=none`, `p_start=0.0`, `p_max=0.0`, `warmup_steps=0`, `ramp_steps=0`). The sidecar should use a balanced 96-sample clean selector (`24` per clean source) and a same-selector Stage15 baseline before interpreting checkpoint deltas. Promotion still requires full public CTC eval against Stage15 and Stage16, because a weighted clean selector can overfit the replay distribution.
					- 2026-06-20 Stage18 preparation: added `scripts/start_stage18_publicweak_clean_replay_ctc_training.sh`, which builds `/media/usbhd/training_data/asr/curriculum/clean_ctc_voxbox_webdataset/stages/stage18_publicweak_clean_replay/webdataset_lengths.jsonl` and its bucket manifest, then delegates config generation/training to the Stage15 clean CTC launcher. Dry-run generated exactly `600,000` replay rows with kept counts `commonvoice_en=210,000`, `librispeech=150,000`, `aishell3=150,000`, and `commonvoice_cn=90,000`; generated config `configs/generated/sensevoice_rwkv_stage18_publicweak_cleanreplay_ctc_from_stage15_werbest_4x4090_deepspeed.yaml` points to Stage15 `wercer_best.pt`, uses LR `7.5e-7`, `max_steps=8000`, CTC-only loss, no SpecAugment, and passes the no-direction-dropout guard. The Stage15 same-selector baseline for the Stage18 clean96 sidecar lives under `/media/usbhd/rwkvasr_runs/stage18_same_selector_baselines/stage15_werbest_on_stage18_clean96` and measured `27.12% / 18.68%` normalized WER/CER over 96 samples, with `blank_top1=0.8374` and pred/ref ratio `0.9781`.
					- 2026-06-20 Stage18 launch: after Stage17 completed `step-6000.pt` and exited status `0`, launched `/media/usbhd/rwkvasr_runs/sensevoice_rwkv_stage18_publicweak_cleanreplay_ctc_from_stage15_werbest_bs12_lr7p5e7_zero1_nockpt_noaug_nodirdrop_4x4090` from Stage15 `wercer_best.pt`. Startup loaded the checkpoint with `missing=0` and `unexpected=0`, initialized DeepSpeed ZeRO-1, confirmed full bidirectional masks with direction dropout disabled, and entered training normally. Early training reached at least `step=50` with no OOM or runtime errors, using about `19-20 GiB` per RTX 4090. Sidecar `sidecar_stage18_publicweak_clean_ctc` is active and waiting for stable `step-1000.pt` using the balanced clean96 selector.
					- 2026-06-20 Stage18 step1000 sidecar: `step-1000.pt` measured `27.24% / 18.80%` on the same clean96 selector, which is worse than the Stage15 same-selector baseline by `+0.12` WER and `+0.12` CER. Output health is still acceptable (`blank_top1=0.8375`, pred/ref ratio `0.9775`), so this is not a collapse; it may be early LR/data-distribution perturbation from the source-weighted replay. Continue to `step-2000.pt` for one more measurement. If `step-2000.pt` also remains worse than the Stage15 clean96 baseline, stop Stage18 early and switch recipes rather than running the full `8000` steps.
					- 2026-06-20 Stage18 step2000 sidecar: `step-2000.pt` measured `27.18% / 18.70%`, still worse than the Stage15 clean96 baseline (`27.12% / 18.68%`) but the regression narrowed from step1000 to `+0.06` WER and `+0.02` CER, with stable output health (`blank_top1=0.8374`, pred/ref ratio `0.9778`). Because the run is close to `step-3000.pt` and the trend moved toward baseline rather than away from it, allow exactly one more checkpoint before stopping. If `step-3000.pt` does not at least match the Stage15 clean96 baseline by WER/CER, stop Stage18 and switch to a less aggressive recipe; do not run to `8000` merely because the job is already active.
					- 2026-06-20 Stage18 stop decision: `step-3000.pt` measured `27.18% / 18.70%`, identical to step2000 and still worse than the Stage15 same-selector baseline by `+0.06` WER and `+0.02` CER. Since the allowed extra checkpoint did not at least match baseline, stop Stage18 early after preserving `step-1000.pt`, `step-2000.pt`, `step-3000.pt`, and their sidecar outputs. The result argues that the public-weak source weighting was not immediately beneficial at LR `7.5e-7`; it does not implicate BiRWKV architecture or CTC labels.
					- 2026-06-20 Stage19 plan: retry the same Stage18 public-weak clean replay distribution from Stage15 `wercer_best.pt`, but reduce LR to `3.0e-7` to test whether Stage18's regression was an optimization-rate issue rather than a data-ratio issue. Reuse the exact Stage18 replay length index and clean96 sidecar selector, so the Stage15 baseline `/media/usbhd/rwkvasr_runs/stage18_same_selector_baselines/stage15_werbest_on_stage18_clean96` remains the apples-to-apples comparison (`27.12% / 18.68%`). Keep CTC-only loss, `text_normalization=ctc`, `tokenizer_append_eos=false`, no SpecAugment, no gradient checkpointing, ZeRO-1, and no direction dropout. Use `max_steps=5000`, `save_every=1000`, and stop early if the first two checkpoints remain at or below the Stage18 trend without beating the same-selector Stage15 baseline.
					- 2026-06-20 Stage19 launch: added `scripts/start_stage19_publicweak_clean_replay_low_lr_ctc_training.sh`, a thin wrapper over Stage18 replay setup with LR `3.0e-7`, `max_steps=5000`, and tmux sidecar `sidecar_stage19_publicweak_clean_low_lr_ctc`. Dry-run reused the Stage18 replay index and generated `configs/generated/sensevoice_rwkv_stage19_publicweak_cleanreplay_ctc_from_stage15_werbest_4x4090_deepspeed.yaml`; the no-direction-dropout guard passed. The live run `/media/usbhd/rwkvasr_runs/sensevoice_rwkv_stage19_publicweak_cleanreplay_ctc_from_stage15_werbest_bs12_lr3e7_zero1_nockpt_noaug_nodirdrop_4x4090` loaded Stage15 `wercer_best.pt` (`missing=0`, `unexpected=0`), initialized DeepSpeed ZeRO-1, confirmed full bidirectional masks, and entered training normally.
					- 2026-06-20 Stage19 step1000 sidecar: `step-1000.pt` measured exactly the Stage15 clean96 baseline at `27.12% / 18.68%`, with matching health metrics (`blank_top1=0.8374`, pred/ref ratio `0.9781`). This confirms that lowering LR from Stage18's `7.5e-7` to `3.0e-7` removes the early source-weighted replay regression, but it has not yet produced a gain. Continue to `step-2000.pt`; require an actual WER/CER improvement over the Stage15 same-selector baseline before considering this recipe useful.
					- 2026-06-20 Stage19 step2000 sidecar: `step-2000.pt` improved over the Stage15 clean96 baseline to `27.06% / 18.66%` (`-0.06` WER and `-0.02` CER) with unchanged health metrics (`blank_top1=0.8374`, pred/ref ratio `0.9781`). This supports the hypothesis that Stage18's source-weighted replay regression was mainly LR-related. Continue Stage19 to later checkpoints and keep selecting by WER-first/CER-second on the same clean96 sidecar before any full public CTC eval.
					- 2026-06-20 Stage19 step3000 sidecar: `step-3000.pt` remained at `27.06% / 18.66%`, tying step2000 and preserving the small same-selector gain over Stage15. Because training is already close to `step-4000.pt`, allow one more checkpoint; if step4000 does not improve beyond the step2000/3000 tie, stop Stage19 and use the WER/CER-selected tied candidate for full public CTC eval rather than running to `max_steps=5000` by default.
					- 2026-06-20 Stage19 step4000 stop decision: `step-4000.pt` measured `27.12% / 18.70%` on the same clean96 selector, falling back to the Stage15 baseline WER and worse than the step2000/3000 tie by `+0.06` WER and `+0.04` CER. Output health stayed stable (`blank_top1=0.8374`, pred/ref ratio `0.9785`), so this is a small optimization/data-ratio plateau rather than collapse. Stop Stage19 before `max_steps=5000`, select the earliest WER/CER-tied candidate `step-2000.pt` as Stage19 `wercer_best.pt`, and run the standard sharded public CTC benchmark (`CTC_SHARD_DATASETS=1`, `CTC_SHARD_COUNT=4`, `DEVICES=0,1,2,3`, `RUN_AR=0`, `METRIC_NORMALIZATION=ctc`) before deciding whether Stage19 can replace Stage15. Do not change the BiRWKV architecture, TimeMixer implementation, CTC label space, SpecAugment policy, or direction-dropout policy based on this result.
					- 2026-06-20 Stage19 public-eval launch: copied `step-2000.pt` to Stage19 `wercer_best.pt` and started tmux `eval_stage19_public_step2000` using `scripts/run_public_eval_benchmarks.sh`. Output goes to `/media/usbhd/rwkvasr_runs/sensevoice_rwkv_stage19_publicweak_cleanreplay_ctc_from_stage15_werbest_bs12_lr3e7_zero1_nockpt_noaug_nodirdrop_4x4090/public_eval_stage19_step2000_ctc_sharded`, with `CTC_SHARD_DATASETS=1`, `CTC_SHARD_COUNT=4`, `DEVICES=0,1,2,3`, `RUN_AR=0`, and `METRIC_NORMALIZATION=ctc`. Compare final metrics directly against Stage15 public-best before promoting; the sidecar gain is too small to justify promotion without full-set confirmation.
					- 2026-06-20 Stage19 public-eval final: sharded public CTC eval from Stage19 `wercer_best.pt` (`step-2000.pt`) finished normally. Results were AISHELL-1 `30.89` CER, LibriSpeech test-clean `22.85/13.31`, LibriSpeech test-other `36.33/23.53`, CommonVoice EN `54.94/38.30`, and WenetSpeech TEST_NET `48.98/49.41`. Compared with Stage15 public-best, Stage19 is equal on AISHELL, improves LibriSpeech clean by `-0.03/-0.01`, improves LibriSpeech other WER by `-0.01` with flat CER, improves CommonVoice EN by `-0.02/-0.01`, and regresses WenetSpeech by `+0.03/+0.04`. Treat Stage19 as a useful clean/public-weak stabilization candidate, but not a decisive promoted default because the absolute gains are at rounding-scale and WenetSpeech regressed. The next experiment should keep the same BiRWKV/CTC setup and test a very light hard replay repair from the Stage19 candidate rather than changing architecture or enabling direction dropout.
					- 2026-06-20 Stage20 plan: continue from Stage19 `wercer_best.pt` (`step-2000.pt`) without changing BiRWKV, RWKV-7 TimeMixer, CTC labels, SpecAugment policy, or direction dropout. Build a very light hard-repair mix with `95%` Stage18 public-weak clean replay and `5%` hard replay split equally between GigaSpeech and WenetSpeech, targeting `600k` sampled rows. Use LR `3.0e-7`, `max_steps=5000`, `save_every=1000`, batch size `12`, grad accumulation `4`, ZeRO-1, no optimizer offload, no gradient checkpointing, no SpecAugment, `text_normalization=ctc`, `tokenizer_append_eos=false`, and no direction dropout (`direction_variant=none`, `p_start=0.0`, `p_max=0.0`, `warmup_steps=0`, `ramp_steps=0`). The fast sidecar should use a 96-sample mixed selector (`16` each clean source plus `16` GigaSpeech and `16` WenetSpeech) and first record a same-selector Stage19 baseline before interpreting Stage20 checkpoints. Stop early if the first evaluated checkpoints lose the Stage19 clean/CV public gains without clear hard-set recovery; full public CTC eval is required before promoting Stage20.
					- 2026-06-20 Stage20 preparation: added `scripts/start_stage20_publicweak_light_hard_repair_ctc_training.sh`, reusing the Stage16 clean+hard mix launcher with Stage20 overrides. Dry-run built `/media/usbhd/training_data/asr/curriculum/stage20_publicweak_light_hard_mix/stages/stage20_publicweak95_hard5/webdataset_lengths.jsonl` and bucket manifest, generated `configs/generated/sensevoice_rwkv_stage20_publicweak95_hard5_ctc_from_stage19_werbest_4x4090_deepspeed.yaml`, and passed the no-direction-dropout guard. The stage has `599,400` train rows: `569,430` Stage18 clean replay, `14,985` GigaSpeech, and `14,985` WenetSpeech; the extra eval rows are only `15` GigaSpeech + `15` WenetSpeech because the Stage18 clean replay index has train split only. This is acceptable because Stage20 training uses `webdataset_split=train` and step eval is disabled.
					- 2026-06-20 Stage20 launch: created the same-selector Stage19 baseline directory `/media/usbhd/rwkvasr_runs/stage20_same_selector_baselines/stage19_werbest_on_stage20_mixed96` with `step-0.pt` symlinked to Stage19 `wercer_best.pt`, then started a one-shot CPU sidecar on the Stage20 96-sample mixed selector. Launched Stage20 training run `/media/usbhd/rwkvasr_runs/sensevoice_rwkv_stage20_publicweak95_hard5_ctc_from_stage19_werbest_bs12_lr3e7_zero1_nockpt_noaug_nodirdrop_4x4090`; startup loaded Stage19 `wercer_best.pt` (`step=2000`, `missing=0`, `unexpected=0`), initialized DeepSpeed ZeRO-1, wrote `train_config.yaml`, and entered the training loop with full CTC-only loss. Sidecar `sidecar_stage20_publicweak_light_hard_ctc` is active and waiting for stable checkpoints.
					- 2026-06-20 Stage20 same-selector baseline: Stage19 `wercer_best.pt` on the Stage20 mixed96 selector measured `29.61% / 18.73%` normalized WER/CER, with healthy output ratios (`blank_top1=0.8232`, pred/ref ratio `0.9769`). Per-source baseline metrics were LibriSpeech `20.43/13.32`, AISHELL3 `26.63/26.63`, CommonVoice EN `33.33/17.38`, CommonVoice CN `33.75/33.75`, GigaSpeech `33.79/20.80`, and WenetSpeech `42.25/42.25`. Stage20 checkpoints should be compared against this baseline before any public eval; a useful checkpoint should improve hard-set WER/CER without losing the Stage19 clean/public gains.
					- 2026-06-20 Stage20 step1000 sidecar: `step-1000.pt` exactly matched the Stage19 same-selector baseline at `29.61% / 18.73%`, with identical health metrics (`blank_top1=0.8232`, pred/ref ratio `0.9769`). This means the 5% hard-repair mix has not caused immediate degradation, but it also has not produced measurable hard-set recovery by 1000 steps. Continue to `step-2000.pt`; require an actual WER/CER improvement or at least a per-source hard recovery without clean regression before considering Stage20 useful.
					- 2026-06-20 Stage20 step2000 stop decision: `step-2000.pt` kept overall WER flat at `29.61%` but worsened CER to `18.78%` versus the Stage19 same-selector baseline `18.73%`. Per-source comparison showed no hard recovery: GigaSpeech stayed `33.79/20.80`, WenetSpeech stayed `42.25/42.25`, and the only measurable movement was LibriSpeech CER worsening from `13.32` to `13.42`. Stop Stage20 before `step-3000.pt`; this 95/5 hard-repair mix at LR `3.0e-7` did not repair the Stage19 Wenet public regression and slightly hurt clean English CER. Do not promote Stage20 and do not change the BiRWKV architecture based on this failed repair.
					- 2026-06-20 public decode ablation plan: before starting another training recipe, test whether the current public CTC benchmark is under-reporting due to greedy decoding. Build a fixed 200-sample manifest from each public eval set and compare Stage15 `wercer_best.pt` and Stage19 `wercer_best.pt` with `CTC_BEAM_SIZE=1` versus `CTC_BEAM_SIZE=4`, keeping `TOKEN_PRUNE_TOPK=16`, `RUN_AR=0`, and `METRIC_NORMALIZATION=ctc`. This is an evaluation-only ablation; it must not change the BiRWKV architecture, CTC labels, or checkpoint selection rules. Run full public beam4 only if the sampled comparison shows a consistent WER/CER gain rather than dataset-specific noise.
					- 2026-06-20 public decode ablation result: fixed 200-sample public slices were written under `artifacts/eval_benchmarks/beam_ablation_20260620/manifests_200_seed20260620` and evaluated for Stage15 and Stage19 with beam1 and beam4. For Stage15, beam4 improved AISHELL `30.29 -> 30.22`, LibriSpeech clean `22.52 -> 22.40`, LibriSpeech other `38.01/24.20 -> 37.83/24.09`, and CommonVoice EN `53.82/35.37 -> 53.45/35.18`, while WenetSpeech WER improved slightly `47.50 -> 47.44` but CER worsened `49.44 -> 49.50`. For Stage19, beam4 improved AISHELL `30.26 -> 30.19`, LibriSpeech clean `22.47/13.24 -> 22.40/13.21`, LibriSpeech other `38.10/24.22 -> 37.98/24.12`, and CommonVoice EN `53.71/35.44 -> 53.50/35.29`, while WenetSpeech worsened slightly `47.38/49.29 -> 47.41/49.32`. This supports a full public beam4 eval for both Stage15 and Stage19 before changing training data again; compare beam4-to-beam4 before promoting Stage19.
					- 2026-06-20 full public beam4 result: Stage15 `wercer_best.pt` with `CTC_BEAM_SIZE=4`, `CTC_TOKEN_PRUNE_TOPK=16`, `RUN_AR=0`, and `METRIC_NORMALIZATION=ctc` produced AISHELL-1 `30.93` CER, LibriSpeech test-clean `22.78/13.28`, LibriSpeech test-other `36.27/23.48`, CommonVoice EN `54.85/38.26`, and WenetSpeech TEST_NET `48.91/49.33`. Stage19 `wercer_best.pt` under the same beam4 setting produced AISHELL-1 `30.90`, LibriSpeech test-clean `22.78/13.28`, LibriSpeech test-other `36.25/23.49`, CommonVoice EN `54.84/38.24`, and WenetSpeech TEST_NET `48.94/49.36`. Beam4 improves the public CTC evaluation baseline on LibriSpeech, CommonVoice EN, and WenetSpeech versus greedy, with a small AISHELL regression for Stage15; use beam4 as the preferred public CTC reporting setting going forward. Stage19 remains a useful clean/public-weak candidate but still should not replace Stage15 as the default checkpoint because its beam4 gains are rounding-scale and WenetSpeech regresses by `+0.03/+0.03` versus Stage15 beam4.
					- 2026-06-20 Stage21 plan: before launching another replay-training recipe, test same-architecture checkpoint interpolation / weight soup across existing CTC-only BiRWKV checkpoints. Candidate inputs are Stage15 public-best `wercer_best.pt`, Stage16 hard-robustness `step-5000.pt`, and Stage19 clean/public-weak `wercer_best.pt`; all share the same BiRWKV/TimeMixer architecture, SenseVoice frontend, CTC label space, and disabled direction-dropout policy. Build only inference checkpoints by averaging floating tensors and preserving non-floating checkpoint fields from the Stage15 anchor. Initial soups should stay Stage15-anchored, e.g. `0.75*Stage15 + 0.25*Stage16`, `0.50*Stage15 + 0.50*Stage16`, `0.75*Stage15 + 0.25*Stage19`, and `0.60*Stage15 + 0.20*Stage16 + 0.20*Stage19`. Evaluate them first on the fixed 200-sample public slices with beam4/topk16 and CTC metric normalization. Run full public beam4 only for a soup that beats Stage15 beam4 on the sampled aggregate without a WenetSpeech regression. This is an optimization/evaluation experiment, not an architecture change.
					- 2026-06-20 Stage21 implementation and sample result: added `scripts/create_ctc_checkpoint_soup.py` and generated four inference-only soup checkpoints under `/media/usbhd/rwkvasr_runs/stage21_checkpoint_soups_20260620`, preserving Stage15 `model_config.yaml` and `tokenizer_config.yaml`. Fixed 200-sample public beam4/topk16 results versus Stage15 sample beam4 showed mixed but useful signals. `0.75*Stage15 + 0.25*Stage16` measured AISHELL `30.43`, Libri clean `22.42/13.14`, Libri other `38.07/24.22`, CV EN `53.09/35.34`, and Wenet `47.23/49.26`; `0.50*Stage15 + 0.50*Stage16` improved Wenet further to `46.86/49.02` but worsened clean/Libri-other; `0.75*Stage15 + 0.25*Stage19` measured AISHELL `30.32`, Libri clean `22.27/13.16`, Libri other `37.95/24.08`, CV EN `53.03/35.34`, and Wenet `47.17/49.23`; the three-way `0.60*Stage15 + 0.20*Stage16 + 0.20*Stage19` measured AISHELL `30.36`, Libri clean `22.42/13.19`, Libri other `37.92/24.11`, CV EN `52.98/35.41`, and Wenet `47.17/49.35`. The most balanced candidate is `0.75*Stage15 + 0.25*Stage19`: on the sample it improves macro WER by `-0.12`, macro CER by `-0.02`, improves Wenet by `-0.27/-0.27`, and improves three of five datasets by both WER/CER, though AISHELL and CommonVoice CER still need full-set confirmation. Run full public beam4 for this candidate before promoting or launching more training.
					- 2026-06-20 Stage21 balanced-soup full result: full public beam4 for `0.75*Stage15 + 0.25*Stage19` finished in `/media/usbhd/rwkvasr_runs/stage21_checkpoint_soups_20260620/public_eval_s15_0p75_s19_0p25_ctc_beam4_sharded`. Results were AISHELL-1 `30.91`, LibriSpeech test-clean `22.77/13.29`, LibriSpeech test-other `36.25/23.48`, CommonVoice EN `54.85/38.25`, and WenetSpeech TEST_NET `48.94/49.35`. This candidate does not transfer the sample-level improvement into a clear full-set gain: compared with Stage15 beam4, it improves AISHELL by `-0.02`, Libri clean WER by `-0.01`, Libri other WER by `-0.02`, and CommonVoice CER by `-0.01`, but regresses Libri clean CER by `+0.01` and WenetSpeech by `+0.03/+0.02`. Do not promote it over Stage15. Because the `0.50*Stage15 + 0.50*Stage16` sample candidate met the same aggregate/Wenet gate and showed the strongest Wenet repair, run one full public beam4 evaluation for that hard-robust soup to map the Pareto point before returning to training.
					- 2026-06-20 Stage21 hard-robust soup full result: full public beam4 for `0.50*Stage15 + 0.50*Stage16` finished in `/media/usbhd/rwkvasr_runs/stage21_checkpoint_soups_20260620/public_eval_s15_0p50_s16_0p50_ctc_beam4_sharded`. Results were AISHELL-1 `30.93`, LibriSpeech test-clean `22.77/13.26`, LibriSpeech test-other `36.33/23.51`, CommonVoice EN `54.92/38.27`, and WenetSpeech TEST_NET `48.59/48.99`. Compared with Stage15 beam4, this improves macro WER/CER by `-0.04/-0.06` and repairs Wenet by `-0.32/-0.34`, while holding AISHELL flat and improving Libri clean by `-0.02/-0.02`; the cost is Libri other `+0.06/+0.03` and CommonVoice EN `+0.06/+0.01`. Record this soup as the current Stage21 public-beam4 Pareto checkpoint (`/media/usbhd/rwkvasr_runs/stage21_checkpoint_soups_20260620/wercer_best.pt` symlink), but do not claim it is near mainstream quality. The next training step should start from this soup and try a very small clean-repair continuation to recover Libri-other/CommonVoice without losing the Wenet gain.
					- 2026-06-20 Stage22 plan: continue CTC-only training from Stage21 `wercer_best.pt` without changing BiRWKV, RWKV-7 TimeMixer, frontend, tokenizer/CTC labels, SpecAugment policy, or direction dropout. Build a conservative `90%` clean source-balanced public-anchor + `10%` hard replay mix (`5%` GigaSpeech, `5%` WenetSpeech), target `600k` sampled rows, and use LR `2.0e-7`, `max_steps=4000`, `save_every=1000`, batch size `12`, grad accumulation `4`, ZeRO-1, no optimizer offload, no gradient checkpointing, no SpecAugment, `text_normalization=ctc`, `tokenizer_append_eos=false`, and direction dropout disabled (`direction_variant=none`, `p_start=0.0`, `p_max=0.0`, `warmup_steps=0`, `ramp_steps=0`). Before interpreting checkpoints, run a same-selector Stage21 baseline on a 96-sample clean-heavy mixed selector. Stop early if step1000/2000 fail to improve Libri-other/CommonVoice proxy metrics or erase the Wenet retention signal; full public beam4 is required before promoting any Stage22 checkpoint.
					- 2026-06-20 Stage22 preparation and launch: added `scripts/start_stage22_stage21_soup_clean_repair_ctc_training.sh`, a thin wrapper over the Stage16 clean/hard launcher with Stage22 overrides. Dry-run built `/media/usbhd/training_data/asr/curriculum/stage22_stage21_soup_clean_repair_mix/stages/stage22_public90_hard10_from_stage21_soup/webdataset_lengths.jsonl` and bucket manifest, then generated `configs/generated/sensevoice_rwkv_stage22_public90_hard10_ctc_from_stage21_soup_4x4090_deepspeed.yaml`. The stage has `599,400` train rows: clean replay `539,460`, GigaSpeech `29,970`, and WenetSpeech `29,970`; the tiny eval split has only `30` GigaSpeech + `30` WenetSpeech rows and is not used for checkpoint ranking. The generated config points to Stage21 `wercer_best.pt`, uses LR `2.0e-7`, `max_steps=4000`, CTC-only loss, no SpecAugment, no gradient checkpointing, ZeRO-1, and passed the no-direction-dropout guard. Launched run `/media/usbhd/rwkvasr_runs/sensevoice_rwkv_stage22_public90_hard10_ctc_from_stage21_soup_bs12_lr2e7_zero1_nockpt_noaug_nodirdrop_4x4090`; startup entered the DeepSpeed loop normally with full bidirectional masks and CTC-only loss. A `step-0.pt` symlink to Stage21 `wercer_best.pt` is present in the run dir so the same sidecar selector records the initialization baseline before `step-1000.pt`.
					- 2026-06-20 Stage22 same-selector baseline: sidecar `sidecar_stage22_stage21_soup_clean_repair_ctc` evaluated the Stage21 soup as `step-0.pt` on the clean-heavy mixed96 selector (`18` each LibriSpeech/AISHELL3/CommonVoice EN/CommonVoice CN, `12` each GigaSpeech/WenetSpeech). Token-weighted normalized WER/CER were `26.78% / 17.39%`, exact `7/96`, mean `blank_top1=0.8237`, and mean pred/ref ratio `0.9711`. Per-source baseline metrics were LibriSpeech `20.97/12.59`, AISHELL3 `29.88`, CommonVoice EN `28.26/18.21`, CommonVoice CN `30.22`, GigaSpeech `31.69/22.43`, and WenetSpeech `35.34`. Stage22 checkpoints must beat this same-selector baseline without degrading WenetSpeech before any full public eval.
					- 2026-06-20 Stage22 step1000 sidecar: `step-1000.pt` measured `26.71% / 17.33%` on the same clean-heavy mixed96 selector, a small improvement over the Stage21 soup baseline (`-0.06` WER, `-0.06` CER). The gain came only from LibriSpeech (`20.97/12.59 -> 20.81/12.48`); AISHELL3, CommonVoice EN/CN, GigaSpeech, and WenetSpeech were unchanged. Output health was stable (`blank_top1=0.8238`, pred/ref ratio `0.9707`). This is not enough for promotion, but it does not erase the Stage21 Wenet repair, so continue to `step-2000.pt` before deciding whether to stop early or run a full public beam4 evaluation.
					- 2026-06-20 Stage22 stop decision: `step-2000.pt` and `step-3000.pt` exactly matched the step1000 sidecar result (`26.71% / 17.33%`) with the same health metrics and no additional per-source movement. Stop Stage22 after preserving `step-1000.pt`, `step-2000.pt`, and `step-3000.pt`; symlink Stage22 `wercer_best.pt` to the earliest tied candidate `step-1000.pt`. This stage produced only a tiny LibriSpeech-sidecar clean repair while preserving hard-set metrics, so run a fixed 200-sample public beam4 screen before spending full public eval time. Do not treat Stage22 as promoted unless public beam4 confirms a real gain over Stage21.
					- 2026-06-20 Stage22 public sample result: fixed 200-sample public beam4/topk16 eval of Stage22 `wercer_best.pt` (`step-1000.pt`) measured AISHELL `30.29`, LibriSpeech clean `22.30/13.20`, LibriSpeech other `38.10/24.19`, CommonVoice EN `53.40/35.38`, and WenetSpeech `46.71/48.75`. Compared with the Stage21 hard-robust soup sample, Stage22 improves AISHELL by `-0.03`, Libri clean by `-0.15/-0.03`, Libri other by `-0.03/-0.09`, and Wenet by `-0.15/-0.27`, while CommonVoice EN regresses by `+0.26/+0.12`. Macro sample WER/CER improves from Stage21 `38.18/30.43` to `38.16/30.36`, so run a full public beam4 evaluation before deciding whether Stage22 replaces Stage21. Treat the CommonVoice sample regression as the main promotion risk.
					- 2026-06-20 Stage22 full public result: full public beam4 for Stage22 `wercer_best.pt` (`step-1000.pt`) finished in `/media/usbhd/rwkvasr_runs/sensevoice_rwkv_stage22_public90_hard10_ctc_from_stage21_soup_bs12_lr2e7_zero1_nockpt_noaug_nodirdrop_4x4090/public_eval_stage22_step1000_ctc_beam4_sharded`. Results were AISHELL-1 `30.91`, LibriSpeech test-clean `22.81/13.26`, LibriSpeech test-other `36.29/23.51`, CommonVoice EN `54.93/38.26`, and WenetSpeech TEST_NET `48.59/48.99`. Compared with the Stage21 hard-robust soup full beam4, Stage22 improves AISHELL by `-0.01`, Libri-other WER by `-0.04`, and CommonVoice CER by `-0.01`, but regresses Libri-clean WER by `+0.04` and CommonVoice WER by `+0.02`; Wenet is unchanged. Macro WER/CER are effectively tied (`Stage21 38.706/30.992`, `Stage22 38.708/30.987`). Keep Stage21 hard soup as the current public-beam4 WER-primary Pareto checkpoint and keep Stage22 as a CER-tie candidate, not a promoted replacement. Further progress needs either a stronger data/training change or a decoding-side improvement; do not change BiRWKV architecture.
					- 2026-06-20 Stage23 decode plan: before starting another training recipe, run a small decoding ablation from the current Stage21/Stage22 candidates. Test a wider CTC beam (`CTC_BEAM_SIZE=8`, `CTC_TOKEN_PRUNE_TOPK=32`) on the same fixed 200-sample public slices, first using Stage21 hard soup and Stage22 step1000. Promote a wider beam to full public only if it improves macro WER/CER without worsening WenetSpeech or CommonVoice beyond the beam4/topk16 tradeoff. This is evaluation-only and must not alter BiRWKV, TimeMixer, CTC labels, or training checkpoints.
					- 2026-06-20 Stage23 decode result: fixed 200-sample beam8/topk32 eval did not pass the promotion gate. Stage21 hard soup beam8/topk32 measured AISHELL `30.32`, Libri clean `22.47/13.26`, Libri other `38.07/24.29`, CommonVoice EN `53.29/35.34`, and Wenet `46.83/48.99`, which is not better than its beam4/topk16 sample because Libri clean and CommonVoice regress. Stage22 beam8/topk32 measured AISHELL `30.29`, Libri clean `22.27/13.19`, Libri other `37.98/24.11`, CommonVoice EN `53.45/35.48`, and Wenet `46.80/48.96`; this improves Libri other versus Stage22 beam4 but worsens Wenet (`46.71/48.75 -> 46.80/48.96`) and CommonVoice CER. Keep beam4/topk16 as the preferred public CTC reporting setting.
					- 2026-06-20 Stage24 route diagnosis: existing AuRWKV/Qwen3-ASR + OmniASR CTC runs are not ready to replace the SenseVoice-RWKV mainline. The aligned clean AuRWKV run `/media/usbhd/rwkvasr_runs/aurwkv_qwen3_omni_ctc_aligned_clean_librispeech_aishell3/aurwkv_qwen3_omni_ctc_aligned_clean_librispeech_aishell3_bs12_zero1_nockpt_4x4090` reports `WER 100.00 / CER 99.50` on its stage eval and predicts mostly blanks/spaces plus stray Latin letters on Chinese samples. The Qwen3 Transformer control `/media/usbhd/rwkvasr_runs/qwen3_transformer_omni_ctc/qwen3_transformer_omni_ctc_stage3_medium_cumulative_bs12_zero1_nockpt_4x4090` also fails the stage eval (`WER 115.26 / CER 75.90`) with Latin-letter outputs for Chinese. This is not a public-eval omission; it points to a tokenizer/CTC-alignment/curriculum problem in the Omni path. Do not spend public benchmark time on this branch until it passes a small labeled sanity eval. If the branch is retried, generated configs must use `direction_variant: none`, `p_start: 0.0`, `p_max: 0.0`, `warmup_steps: 0`, and `ramp_steps: 0`; older AuRWKV/Qwen3 configs with `direction_variant: drop_both` and zero probabilities are invalid under the current no-direction-dropout guard.
					- 2026-06-20 Stage24 length-bonus decode result: tested CTC prefix beam length-bonus on the current Stage21 hard-robust soup with fixed public 200-sample manifests, `beam4/topk16`, `mode=bi`, and metric normalization `ctc`. A 50-sample screen showed only a WER/CER tradeoff. The 200-sample confirmation kept the same pattern: existing `length_bonus=0.0` baseline is macro `38.181 / 30.425`; `length_bonus=-0.6` is `38.149 / 30.877`; `length_bonus=-0.3` is `38.176 / 30.599`; `length_bonus=+0.6` is `38.702 / 30.329`. Negative length bonus slightly lowers macro WER by shortening hypotheses, but worsens CER and Wenet; positive length bonus slightly lowers CER but worsens WER and CommonVoice. Do not promote any length-bonus setting to full public evaluation or default reporting. Keep `length_bonus=0.0`, `beam4/topk16` as the preferred public CTC setting and return to data/training improvements without changing the BiRWKV architecture.
					- 2026-06-20 evaluation environment note: direct hand-written prediction commands must export `PATH=/home/yueyulin/github/rwkvasr_longform_asr/.venv/bin:$PATH` as well as `PYTHONPATH=.../src` before running CUDA fused-kernel inference. The uv environment already contains `ninja`, but Torch C++ extension loading discovers the `ninja` executable through `PATH`; missing this export causes `RuntimeError: Ninja is required to load C++ extensions`. `scripts/run_public_eval_benchmarks.sh` already sets this correctly.
					- 2026-06-20 Stage25 plan: since Stage21/22 replay, checkpoint soup, beam-width, and length-bonus changes only move metrics by rounding-scale amounts, run a small SpecAugment training probe before changing architecture or tokenizer. Initialize from Stage21 hard-robust soup (`/media/usbhd/rwkvasr_runs/stage21_checkpoint_soups_20260620/wercer_best.pt`) and reuse the Stage22 `90%` clean source-balanced public-anchor + `10%` hard replay mix (`5%` GigaSpeech, `5%` WenetSpeech) so the same clean-heavy mixed96 sidecar remains comparable. Enable light SpecAugment (`time_masks=2`, `time_width=20`, `freq_masks=2`, `freq_width=27`), use LR `5.0e-7`, `max_steps=4000`, `save_every=1000`, batch size `12`, grad accumulation `4`, ZeRO-1, no optimizer offload, no gradient checkpointing, CTC-only loss, `text_normalization=ctc`, `tokenizer_append_eos=false`, and direction dropout disabled (`direction_variant=none`, `p_start=0.0`, `p_max=0.0`, `warmup_steps=0`, `ramp_steps=0`). Promotion gate: first compare step checkpoints to the Stage21 same-selector baseline (`26.78 / 17.39`) and Stage22 best (`26.71 / 17.33`); only run fixed 200-sample public beam4 if the sidecar improves more than Stage22's tiny Libri-only movement or shows clear CommonVoice/Wenet robustness without clean regression.
					- 2026-06-20 Stage25 launch: reused `scripts/start_stage22_stage21_soup_clean_repair_ctc_training.sh` with env overrides instead of adding a new launcher. Generated config `configs/generated/sensevoice_rwkv_stage25_public90_hard10_specaug_ctc_from_stage21_soup_4x4090_deepspeed.yaml` and run dir `/media/usbhd/rwkvasr_runs/sensevoice_rwkv_stage25_public90_hard10_specaug_ctc_from_stage21_soup_bs12_lr5e7_zero1_nockpt_nodirdrop_4x4090`. The dry-run reused the existing Stage22 curriculum and passed the no-direction-dropout guard. `pytest tests/test_spec_augment.py -q` passed (`3 passed`). Live launch in tmux `training_stage25_specaug_ctc` loaded Stage21 `wercer_best.pt` with `missing=0`, `unexpected=0`, initialized DeepSpeed ZeRO-1, confirmed full-bidirectional masks, and entered the training loop at about `0.99 step/s` with roughly `20-21 GiB` per GPU. Sidecar tmux `sidecar_stage25_specaug_ctc` is watching the run with the same 96-sample clean-heavy selector and beam4/topk16 CTC metrics.
					- 2026-06-20 Stage25 step1000 sidecar: `step-1000.pt` measured `27.29% / 17.62%` on the clean-heavy mixed96 selector, worse than both the Stage21 same-selector baseline (`26.78% / 17.39%`) and Stage22 best (`26.71% / 17.33%`). Per-source comparison versus the Stage21 baseline shows broad clean/public regression: LibriSpeech `20.97/12.59 -> 21.44/12.77`, AISHELL3 `29.88 -> 30.49`, CommonVoice EN `28.26/18.21 -> 30.43/19.17`, CommonVoice CN `30.22 -> 30.84`, and GigaSpeech WER `31.69 -> 32.24` while CER is roughly flat/slightly better (`22.43 -> 22.31`). The only useful signal is WenetSpeech improvement `35.34 -> 33.62`. Because training had already reached about step1780 by the time sidecar completed, continue only to `step-2000.pt` for a second measurement; stop Stage25 if the clean/public regression remains.
					- 2026-06-20 Stage25 stop decision: `step-2000.pt` worsened further to `27.55% / 17.93%` on the same mixed96 selector. Per-source metrics were LibriSpeech `21.44/12.92`, AISHELL3 `29.88`, CommonVoice EN `31.16/19.49`, CommonVoice CN `30.53`, GigaSpeech `34.43/23.47`, and WenetSpeech `34.48`. This loses the step1000 Wenet gain and remains worse than both Stage21 and Stage22 on overall WER/CER. Training was interrupted after preserving `step-1000.pt`, `step-2000.pt`, and DeepSpeed `latest` at step2000; sidecar was stopped and GPUs returned idle. Do not promote Stage25 or run public sample/full eval. Light SpecAugment at LR `5e-7` on the 90/10 mix is a broad regression, not a solution to the Stage21/22 plateau.
					- 2026-06-20 Stage26 plan: before adding a teacher-distillation implementation or another long training recipe, run a finer same-architecture checkpoint-soup screen over the existing CTC-only BiRWKV checkpoints. Candidate components are Stage15 public-best, Stage16 hard-robustness, Stage19 clean/public-weak, and optionally Stage22's tied clean-repair checkpoint; all use the same SenseVoice frontend, CTC tokenizer, BiRWKV/RWKV-7 TimeMixer architecture, CTC label normalization, and disabled direction dropout. This is an inference/evaluation-only optimization: generate weighted-average checkpoints, preserve non-floating metadata from the Stage15 anchor, and evaluate with the fixed 200-sample public manifests using `beam4/topk16`, `length_bonus=0.0`, and `metric_normalization=ctc`. Do not train from, promote, or full-public-evaluate a Stage26 soup unless it beats the current Stage21 hard soup on the sampled macro WER/CER while preserving WenetSpeech and not worsening CommonVoice EN. Teacher distillation remains deferred because the local `funasr`/`modelscope` teacher stack is not installed and the downloaded FunASR-Nano checkpoint does not expose a simple standalone CTC-head state dict under the current project model API.
					- 2026-06-20 Stage26 sample result: generated and screened finer Stage15/Stage16/Stage19 soups under `/media/usbhd/rwkvasr_runs/stage26_checkpoint_soups_20260620`. None passed the promotion gate against the Stage21 hard soup sample baseline (`38.181 / 30.425` macro WER/CER; Wenet `46.86 / 49.02`; CommonVoice EN `53.14 / 35.26`). Low Stage16 weights improved some clean/CER slices but consistently worsened Wenet: `0.55/0.35/0.10` measured macro `38.249 / 30.367`, Wenet `47.20 / 49.14`; `0.60/0.30/0.10` measured `38.269 / 30.414`, Wenet `47.26 / 49.32`. Keeping Stage16 at `50%` preserved Wenet CER better but still lost WER/CommonVoice: `0.45/0.50/0.05` measured `38.210 / 30.367`, Wenet `47.01 / 48.96`, CommonVoice `53.45 / 35.33`; `0.40/0.50/0.10` measured `38.218 / 30.328`, Wenet `46.95 / 48.90`, CommonVoice `53.29 / 35.07`; `0.45/0.45/0.10` measured `38.187 / 30.353`, Wenet `47.10 / 49.05`. Do not promote any Stage26 soup or run full public eval. The remaining cheap soup/beam/length-bonus space is exhausted for now; return to training-side changes that leave the BiRWKV/RWKV-7 TimeMixer architecture intact.
					- 2026-06-20 Stage27 plan: run a short CTC-head-only calibration from the current Stage21 hard soup before attempting larger teacher-distillation work. Freeze the full BiRWKV/SenseVoice encoder (`freeze_encoder=true`) and leave only the existing linear CTC head trainable (`freeze_ctc_head=false`), so the RWKV-7 TimeMixer architecture, bidirectional wrapper, frontend, tokenizer, CTC labels, direction dropout policy, and checkpoint compatibility remain unchanged. Reuse the Stage22 `90%` public-anchor clean + `10%` hard replay mix and the same mixed96 sidecar, but use a higher head-only LR (`1.0e-5` initially), `max_steps=2000`, `save_every=500`, batch size `12`, grad accumulation `4`, ZeRO-1, no optimizer offload, no gradient checkpointing, no SpecAugment, `text_normalization=ctc`, `tokenizer_append_eos=false`, and direction dropout disabled. Gate: compare step checkpoints against the Stage21 same-selector baseline (`26.78 / 17.39`) and Stage22 best (`26.71 / 17.33`); stop if step500/1000 increases WER/CER or changes output length/blank ratios in the same direction as Stage25. Full public eval is allowed only if head-only calibration improves mixed96 overall WER/CER while not degrading WenetSpeech and CommonVoice EN.
					- 2026-06-20 Stage27 result: launched `/media/usbhd/rwkvasr_runs/sensevoice_rwkv_stage27_headonly_calibration_public90_hard10_from_stage21_soup_bs12_lr1e5_zero1_nockpt_noaug_nodirdrop_4x4090` from Stage21 `wercer_best.pt` with `freeze_encoder=true`, `freeze_ctc_head=false`, and `31,044,708` trainable CTC-head parameters. Training was manually stopped after preserving `step-500.pt` and `step-1000.pt`, because the first sidecar gate lagged behind training. GPU sidecar mixed96 results: `step-500.pt` measured overall `26.59 / 17.55`; per-source LibriSpeech `20.50 / 12.59`, AISHELL3 `28.66 / 28.66`, CommonVoice EN `29.71 / 19.33`, CommonVoice CN `30.22 / 30.22`, GigaSpeech `31.69 / 22.77`, WenetSpeech `35.34 / 35.34`. `step-1000.pt` measured overall `26.59 / 17.43`; per-source LibriSpeech `20.66 / 12.48`, AISHELL3 `28.05 / 28.05`, CommonVoice EN `29.71 / 19.49`, CommonVoice CN `30.22 / 30.22`, GigaSpeech `31.69 / 22.43`, WenetSpeech `35.34 / 35.34`. Compared with the Stage21 same-selector baseline (`26.78 / 17.39`; LibriSpeech `20.97 / 12.59`, AISHELL3 `29.88`, CommonVoice EN `28.26 / 18.21`, CommonVoice CN `30.22`, GigaSpeech `31.69 / 22.43`, WenetSpeech `35.34`), head-only calibration improves WER on Libri/AISHELL but worsens CER and CommonVoice EN. Do not run full public eval or promote Stage27.
					- 2026-06-20 Stage28 plan: run an inference-only interpolation screen between the Stage21 hard soup and Stage27 head-only checkpoints. Since Stage27 froze the encoder, this tests whether partial CTC-head movement can keep the Libri/AISHELL WER gains while recovering CER/CommonVoice robustness. Generate weighted-average checkpoints under `/media/usbhd/rwkvasr_runs/stage28_headonly_interpolation_soups_20260620` from Stage21 `wercer_best.pt` plus Stage27 `step-500.pt` / `step-1000.pt`, starting with Stage27 weights `0.25`, `0.50`, and `0.75`. Evaluate only on the same mixed96 sidecar source quotas with `beam4/topk16`, `mode=bi`, and `metric_normalization=ctc`. Full public eval remains disallowed unless a candidate beats Stage21 on both overall WER and CER while not worsening CommonVoice EN or WenetSpeech.
					- 2026-06-20 Stage28 result: screened conservative Stage21/Stage27-step1000 interpolation candidates with Stage27 weights `0.125`, `0.25`, and `0.50` using the same mixed96 sidecar and `beam4/topk16`. `0.125` measured overall `26.91 / 17.35` with LibriSpeech `20.66 / 12.33`, AISHELL3 `31.10 / 31.10`, CommonVoice EN `29.71 / 18.69`, CommonVoice CN `30.53 / 30.53`, GigaSpeech `31.69 / 22.43`, WenetSpeech `34.48 / 34.48`. `0.25` measured overall `26.91 / 17.35` with LibriSpeech `20.81 / 12.37`, AISHELL3 `30.49 / 30.49`, CommonVoice EN `29.71 / 18.69`, CommonVoice CN `30.53 / 30.53`, GigaSpeech `31.69 / 22.43`, WenetSpeech `34.48 / 34.48`. `0.50` measured overall `26.84 / 17.43` with LibriSpeech `20.50 / 12.37`, AISHELL3 `30.49 / 30.49`, CommonVoice EN `30.43 / 19.33`, CommonVoice CN `30.53 / 30.53`, GigaSpeech `31.69 / 22.43`, WenetSpeech `34.48 / 34.48`. The interpolation surface is a WER/CER and CommonVoice/Wenet tradeoff rather than a Pareto improvement; do not promote Stage28 or run full public eval.
					- 2026-06-20 Stage29 plan: run a short full-model ultra-low-LR stabilization probe from the Stage21 hard soup, because head-only movement and interpolation show that the CTC head alone creates WER/CER and CommonVoice tradeoffs. Keep the same BiRWKV/RWKV-7 TimeMixer encoder, SenseVoice frontend, CTC tokenizer, labels, no SpecAugment, no gradient checkpointing, and direction dropout disabled. Use the Stage22 `90%` clean public-anchor + `10%` hard replay mix, but train all model parameters with LR `1.0e-7`, `max_steps=1000`, `save_every=500`, batch size `12`, grad accumulation `4`, ZeRO-1, no optimizer offload, CTC-only loss, `text_normalization=ctc`, and `tokenizer_append_eos=false`. Gate remains strict: compare `step-500.pt` and `step-1000.pt` to Stage21 same-selector (`26.78 / 17.39`) and Stage22 best (`26.71 / 17.33`); do not run full public eval unless a checkpoint improves both overall WER and CER while preserving CommonVoice EN and WenetSpeech.
					- 2026-06-20 Stage29 result: trained `/media/usbhd/rwkvasr_runs/sensevoice_rwkv_stage29_ultralow_full_public90_hard10_from_stage21_soup_bs12_lr1e7_zero1_nockpt_noaug_nodirdrop_4x4090` to `step-1000` without OOM, preserving `step-500.pt` and `step-1000.pt`. GPU sidecar mixed96 results: `step-500.pt` measured overall `26.91 / 17.35`, with LibriSpeech `20.81 / 12.40`, AISHELL3 `31.10 / 31.10`, CommonVoice EN `28.26 / 18.21`, CommonVoice CN `30.53 / 30.53`, GigaSpeech `31.69 / 22.43`, WenetSpeech `35.34 / 35.34`. `step-1000.pt` measured overall `27.10 / 17.37`, with LibriSpeech `20.97 / 12.37`, AISHELL3 `31.71 / 31.71`, CommonVoice EN `28.26 / 18.21`, CommonVoice CN `30.84 / 30.84`, GigaSpeech `31.69 / 22.43`, WenetSpeech `35.34 / 35.34`. Ultra-low-LR full-model movement improves CER slightly but worsens WER and Chinese clean/CV slices; do not promote Stage29 or run full public eval.
					- 2026-06-20 Stage30 plan: before spending more GPU time on another training recipe, run a larger source-balanced selector audit to reduce mixed96 sample noise. This is evaluation-only: keep the exact BiRWKV/RWKV-7 TimeMixer architecture, SenseVoice frontend, tokenizer, CTC labels, `mode=bi`, `beam4/topk16`, `length_bonus=0.0`, and `metric_normalization=ctc`; do not train or edit checkpoints. Compare the current Stage21 hard soup against the most informative non-promoted probes, Stage27 `step-1000.pt` and Stage29 `step-500.pt`, on a 256-sample train-split selector with quotas `clean_librispeech=48`, `clean_aishell=48`, `clean_cv_en=48`, `clean_cv_cn=48`, `gigaspeech=32`, and `wenetspeech=32`. Treat Stage21 as the WER-primary incumbent. Only if a candidate improves both overall WER and CER while not worsening CommonVoice EN or WenetSpeech should it receive fixed 200-sample public eval; otherwise keep Stage21 and switch the next work item to a different same-architecture data/teacher signal rather than another tiny LR or checkpoint-soup probe.
					- 2026-06-20 Stage30 result: the larger 256-sample selector confirmed that Stage27/Stage29 are tradeoffs rather than robust improvements. Stage21 incumbent measured overall `27.56 / 18.08`, with LibriSpeech `20.33 / 12.14`, AISHELL3 `27.38 / 27.38`, CommonVoice EN `38.89 / 27.03`, CommonVoice CN `31.48 / 31.48`, GigaSpeech `29.50 / 18.81`, and WenetSpeech `37.69 / 37.69`. Stage27 head-only step1000 measured overall `27.29 / 18.08`, improving Libri/AISHELL/Giga but worsening CommonVoice EN to `39.13 / 27.64` and WenetSpeech to `38.43 / 38.43`. Stage29 ultra-low-LR step500 measured overall `27.56 / 18.07`, slightly improving CommonVoice EN/Giga CER but worsening CommonVoice CN and WenetSpeech to `38.43 / 38.43`. Do not promote Stage27/Stage29, do not run fixed public eval, and do not spend more time on the same low-LR/head-only/soup surface without a new data or teacher signal.
					- 2026-06-20 Stage31 plan: bootstrap the local FunASR-Nano teacher as a same-architecture training signal source, without changing the BiRWKV/RWKV-7 TimeMixer student architecture or any checkpoint. First install only the missing teacher runtime dependencies into the uv-managed Python environment after a dry-run confirms torch will not be downgraded. Then run the local `assets/fun-asr-nano-2512` checkpoint on its bundled example audio and a tiny source-balanced sample from the Stage22 train mix. This stage is diagnostic only: compare teacher text, dataset labels, and Stage21 student predictions under the existing `ctc` metric normalization; do not train on teacher outputs unless the teacher runner is reproducible, handles both English and Chinese samples, and improves label agreement on hard/noisy sources such as CommonVoice EN and WenetSpeech.
					- 2026-06-20 Stage31 result: installed `funasr==1.3.10`, `modelscope==1.37.1`, `librosa==0.11.0`, and their missing runtime dependencies into the uv-managed environment without changing torch (`torch/torchaudio` remained `2.10.0+cu128`). The local FunASR-Nano checkpoint can run its bundled `zh.mp3` and `en.mp3` examples on GPU, but `trust_remote_code` falls back to the installed FunASR implementation (`No module named 'model'`) and the local checkpoint lacks the advertised `ctc_decoder` / `ctc.ctc_lo` keys, so only the AR-style `text` / `text_tn` output is usable as a teacher; do not use its `ctc_text` or logits for distillation. On a 12-sample hard source-balanced probe selected from Stage30's highest student-WER samples, FunASR text measured `12.16% / 5.83%` WER/CER versus Stage21 student `81.08% / 47.92%`. Per-source teacher/student WER was LibriSpeech `0.00 / 58.33`, AISHELL3 `28.57 / 71.43`, CommonVoice EN `0.00 / 122.22`, CommonVoice CN `22.22 / 72.22`, GigaSpeech `42.86 / 114.29`, and WenetSpeech `0.00 / 100.00`. This is strong enough to justify a teacher-label repair path, but only as text-label supervision with conservative gating.
					- 2026-06-20 Stage32 plan: add a training-only CTC label override cache so teacher text can replace noisy CTC targets for selected utterances without modifying audio, model architecture, tokenizer, frontend, BiRWKV/RWKV-7 TimeMixer blocks, or checkpoint compatibility. The override cache should be keyed by `utt_id` and contain teacher text such as `pred_text` / `teacher_text_tn`; the data loader should apply the override before CTC normalization/tokenization and leave decoder prompt/draft behavior unchanged. Add focused tests proving overridden CTC targets differ from the original label while non-overridden samples remain unchanged. After verification, build a small teacher-labeled hard-source cache and run a short Stage32 repair probe from Stage21 with the existing Stage22 mix, no SpecAugment, no direction dropout, and strict Stage30/Stage21 gate before any public eval.
					- 2026-06-20 Stage32 implementation note: added `ctc_label_override_cache_path` and `ctc_label_override_text_key` to the WebDataset training data path. The override is applied only to the CTC target text before normalization/tokenization; decoder target and prompt logic continue to read original metadata text. Eval WebDataset configs explicitly clear the override cache so validation loss is not scored against teacher labels. Focused tests passed for iterable WebDataset override behavior, MP3/length-index loading, syntax compilation, and train/deepspeed config parsing. A broader pre-existing test subset still has unrelated failures: several tiny training smoke tests configure `vocab_size=8` while the real Whisper tokenizer resolves to `50257`, and one bucket stream test constructs `_BucketEntryStream` with an outdated argument shape.
					- 2026-06-20 Stage32 teacher-cache result: sampled a deterministic source-balanced 512-row Stage22 train subset (`64` LibriSpeech, `64` AISHELL3, `128` CommonVoice EN, `64` CommonVoice CN, `96` GigaSpeech, `96` WenetSpeech). FunASR-Nano does not support batch decoding in the installed implementation, so generation was rerun one sample at a time with progress disabled. After removing one WenetSpeech inference error and one duplicate AISHELL utterance, the clean teacher cache has `510` unique rows at `/media/usbhd/rwkvasr_runs/stage32_teacher_label_repair_probe_20260620/stage32_teacher510.cache.jsonl` with matching length index `stage32_teacher510.lengths.jsonl`. Teacher-vs-original normalized WER/CER is `3.97 / 1.65` overall: LibriSpeech `1.44 / 0.28`, AISHELL3 `1.88 / 1.88`, CommonVoice EN `7.12 / 2.71`, CommonVoice CN `6.33 / 6.33`, GigaSpeech `4.38 / 1.39`, and WenetSpeech `4.48 / 4.48`. A real SenseVoice tokenizer length-index loader smoke batch from this cache succeeded, confirming override is active in the intended training path.
					- 2026-06-20 Stage32 training result: ran a head-only repair probe from Stage21 on the `teacher510` subset with `freeze_encoder=true`, LR `2e-6`, no SpecAugment, no direction dropout, and saved `step-100.pt` before interrupting the run. On the Stage30 256-sample selector, `step-100.pt` measured overall `27.64 / 18.14`, with LibriSpeech `20.52 / 12.20`, AISHELL3 `27.38 / 27.38`, CommonVoice EN `39.13 / 27.24`, CommonVoice CN `31.36 / 31.36`, GigaSpeech `29.50 / 18.81`, and WenetSpeech `37.69 / 37.69`. This is worse than the Stage21 incumbent (`27.56 / 18.08`) and hurts CommonVoice EN, so do not promote Stage32 step100 and do not run public eval. The teacher-label mechanism is useful, but training only on a tiny repeated teacher subset is too concentrated; the next probe should mix teacher-overridden rows gently into the original Stage22 distribution.
					- 2026-06-20 Stage33 plan: build a mixed teacher-repair length index that keeps the original Stage22 distribution dominant and injects the `teacher510` rows as a small oversampled component. Start with roughly `50k` original Stage22 train rows plus `5k` repeated teacher rows (`~9%` teacher), using the same `ctc_label_override_cache_path` so only the teacher rows receive FunASR labels and all other rows keep original labels. Train from Stage21 with the encoder frozen, CTC head trainable, LR `1e-6`, no SpecAugment, no direction dropout, `max_steps=1000`, and save every `500` steps. Gate on the Stage30 256-sample selector first; public eval remains disallowed unless the mixed repair improves both overall WER and CER while not worsening CommonVoice EN or WenetSpeech.
					- 2026-06-20 Stage33 result: trained the mixed teacher-repair head-only run from Stage21 to `step-1000` using `/media/usbhd/rwkvasr_runs/stage33_mixed_teacher_repair_20260620/stage33_original50k_teacher5k.lengths.jsonl` and the `teacher510` CTC label override cache. Training completed normally with encoder frozen, `31,044,708` trainable CTC-head parameters, no SpecAugment, no direction dropout, and final train loss around `1.87`. On the Stage30 256-sample selector, `step-500.pt` measured overall `27.59 / 18.13`, with LibriSpeech `20.40 / 12.15`, AISHELL3 `27.38 / 27.38`, CommonVoice EN `39.13 / 27.29`, CommonVoice CN `31.36 / 31.36`, GigaSpeech `29.50 / 18.81`, and WenetSpeech `37.69 / 37.69`. `step-1000.pt` measured overall `27.51 / 18.05`, with LibriSpeech `20.27 / 12.06`, AISHELL3 `27.77 / 27.77`, CommonVoice EN `38.89 / 27.13`, CommonVoice CN `31.23 / 31.23`, GigaSpeech `29.28 / 18.77`, and WenetSpeech `37.69 / 37.69`. Compared with Stage21 (`27.56 / 18.08`), step1000 gives a tiny overall WER/CER gain but still worsens AISHELL and CommonVoice EN CER, so do not promote it directly or run full public eval. The next step should be an inference-only interpolation screen to test whether partial teacher-repair head movement keeps the small overall gain while recovering the public-anchor slices.
					- 2026-06-20 Stage34 plan: run an inference-only interpolation screen between Stage21 `wercer_best.pt` and Stage33 `step-1000.pt`. Because Stage33 froze the encoder, this changes only the existing CTC-head weights in the checkpoint soup and leaves the BiRWKV/RWKV-7 TimeMixer architecture, SenseVoice frontend, tokenizer, CTC labels, direction policy, and decode path unchanged. Generate candidates with Stage33 weights `0.125`, `0.25`, `0.50`, and `0.75` under `/media/usbhd/rwkvasr_runs/stage34_teacher_repair_interpolation_soups_20260620`, preserving non-floating metadata from Stage21. Evaluate each candidate on the same Stage30 256-sample selector with `beam4/topk16`, `mode=bi`, and `metric_normalization=ctc`. Public eval is allowed only if a candidate beats Stage21 on both overall WER and CER and does not worsen CommonVoice EN or WenetSpeech; otherwise keep Stage21 as the incumbent and move to a larger, better-targeted teacher-cache selection rather than increasing teacher ratio blindly.
					- 2026-06-20 Stage34 result: generated four Stage21/Stage33-step1000 interpolation soups and evaluated them on the Stage30 256-sample selector. Stage33 weights `0.125`, `0.25`, `0.50`, and `0.75` measured overall `27.56 / 18.10`, `27.56 / 18.08`, `27.54 / 18.09`, and `27.54 / 18.11`. The `0.25` soup improved CommonVoice EN to `38.65 / 26.88` and GigaSpeech to `29.28 / 18.77` but worsened CommonVoice CN to `31.60 / 31.60` and did not improve overall WER/CER versus Stage21. The `0.50` and `0.75` soups gave only a `0.02` WER gain with worse CER, and `0.75` worsened CommonVoice EN. Do not promote any Stage34 soup or run full public eval. The teacher-label path needs a larger and more targeted cache, preferably selected by student difficulty and teacher/reference agreement, rather than more checkpoint interpolation.
					- 2026-06-20 Stage35 plan: build a targeted teacher-label repair cache without using the Stage30 selector samples for training, so future selector gates remain meaningful. Sample a fresh source-balanced train subset from the Stage22 mix, exclude any known Stage30 selector ids and existing `teacher510` ids, run the Stage21 incumbent CTC decoder to estimate student difficulty, and teacher-decode only the high-student-error rows with FunASR-Nano. Keep only rows where the teacher text is closer to the dataset reference than the Stage21 student prediction under `ctc` normalization, or where teacher/reference disagreement is small enough to be a plausible label normalization repair. Start with a compact target around `1k-2k` accepted teacher rows, biased toward CommonVoice EN, GigaSpeech, WenetSpeech, and CommonVoice CN, then repeat the Stage33-style gentle mixed-index training with teacher ratio no higher than `5%` and LR no higher than `5e-7`. This remains a data/label optimization only: no BiRWKV/RWKV-7 TimeMixer, frontend, tokenizer, CTC objective, or direction-dropout changes.
					- 2026-06-20 Stage35 result: built a fresh targeted teacher-label cache under `/media/usbhd/rwkvasr_runs/stage35_targeted_teacher_repair_20260620` without using Stage30 selector ids or the previous `teacher510` ids. A 2048-row source-balanced candidate index (`512` each from CommonVoice EN, CommonVoice CN, GigaSpeech, and WenetSpeech) measured Stage21 student difficulty `36.48 / 25.27` overall. Selecting the top `384` hard rows per source produced `1536` teacher targets. Offset-based audio extraction from WebDataset tar files succeeded for all `1536` targets, and four isolated FunASR-Nano teacher processes decoded them with zero inference errors. Conservative filtering accepted `1441` rows: CommonVoice EN `365`, CommonVoice CN `353`, GigaSpeech `360`, and WenetSpeech `363`. On accepted rows, Stage21 student versus teacher normalized errors were: CommonVoice EN student `50.42 / 32.16` vs teacher `6.55 / 2.47`; CommonVoice CN `42.20 / 42.21` vs `5.71 / 5.72`; GigaSpeech `40.56 / 24.64` vs `3.19 / 1.17`; WenetSpeech `55.30 / 55.68` vs `4.41 / 4.45`. This is a much stronger supervision signal than the random `teacher510` cache.
					- 2026-06-20 Stage36 plan: train a gentle head-only targeted teacher-repair probe from Stage21 using the Stage35 accepted cache. Build a mixed length index with `100k` original Stage22 train rows plus `5k` repeated Stage35 accepted teacher rows (`~4.76%` teacher), excluding Stage30 selector ids and accepted teacher ids from the original component. Keep the encoder frozen and only train the existing CTC head, with LR `5e-7`, no SpecAugment, no direction dropout, `max_steps=1000`, `save_every=500`, batch size `12`, ZeRO-1, and no gradient checkpointing. Gate on the Stage30 256-sample selector; run full public eval only if a checkpoint improves both overall WER and CER without worsening CommonVoice EN or WenetSpeech.
					- 2026-06-20 Stage36 result: trained the targeted teacher-repair head-only run from Stage21 to `step-1000` using `/media/usbhd/rwkvasr_runs/stage36_targeted_teacher_repair_20260620/stage36_original100k_teacher5k.lengths.jsonl` and the Stage35 accepted teacher override cache. Training completed normally with encoder frozen, `31,044,708` trainable CTC-head parameters, no SpecAugment, no direction dropout, and final train loss around `1.87`. On the Stage30 256-sample selector, `step-500.pt` measured overall `27.56 / 18.10`, with LibriSpeech `20.40 / 12.18`, AISHELL3 `27.38 / 27.38`, CommonVoice EN `38.89 / 27.03`, CommonVoice CN `31.36 / 31.36`, GigaSpeech `29.50 / 18.81`, and WenetSpeech `37.69 / 37.69`. `step-1000.pt` measured overall `27.64 / 18.18`, with LibriSpeech `20.40 / 12.20`, AISHELL3 `27.38 / 27.38`, CommonVoice EN `39.13 / 27.29`, CommonVoice CN `31.48 / 31.48`, GigaSpeech `29.73 / 18.96`, and WenetSpeech `37.69 / 37.69`. Compared with the Stage21 incumbent (`27.56 / 18.08`), the stronger targeted teacher cache still does not improve the held-out selector under head-only training; do not promote Stage36 or run full public eval. The next step should first audit decode-time CTC beam settings on the unchanged Stage21 checkpoint, because repeated label-repair probes have produced only tiny tradeoffs while leaving the main public gap unchanged.
					- 2026-06-20 Stage37 plan: run an inference-only CTC decode hyperparameter audit on the unchanged Stage21 hard soup before spending more training compute. Keep the same BiRWKV/RWKV-7 TimeMixer architecture, SenseVoice frontend, CTC head, tokenizer, labels, `mode=bi`, and checkpoint weights. Evaluate the Stage21 incumbent on the Stage30 256-sample selector across conservative prefix-beam settings such as `beam_size` and `token_prune_topk`, plus any existing decode-only length/blank controls supported by the prediction CLI. Select decode settings only if they improve both overall WER and CER and do not worsen CommonVoice EN or WenetSpeech; otherwise keep the current `beam4/topk16` decode policy and move to a different data or optimization signal rather than further head-only teacher repair.
					- 2026-06-20 Stage37 result: materialized the Stage30 selector as `/media/usbhd/rwkvasr_runs/stage37_ctc_decode_audit_20260620/stage30_selector256.lengths.jsonl` and confirmed direct `predict_ctc_labeled` reproduces the sidecar Stage21 baseline exactly at `27.56 / 18.08` overall, with LibriSpeech `20.33 / 12.14`, AISHELL3 `27.38 / 27.38`, CommonVoice EN `38.89 / 27.03`, CommonVoice CN `31.48 / 31.48`, GigaSpeech `29.50 / 18.81`, and WenetSpeech `37.69 / 37.69`. The decode audit found the best safe inference-only setting is `beam_size=8`, `token_prune_topk=16`, `length_bonus=0.0`, `insertion_bonus=0.0`: overall `27.51 / 18.04`, with LibriSpeech `20.27 / 12.06`, AISHELL3 `27.38 / 27.38`, CommonVoice EN `38.89 / 27.03`, CommonVoice CN `31.36 / 31.36`, GigaSpeech `29.50 / 18.86`, and WenetSpeech `37.69 / 37.69`. `beam16/topk16` ties WER but has worse CER, `beam8/topk32` worsens CommonVoice EN, and small negative length bonus lowers CER while worsening WenetSpeech. Promote only the decode policy, not a new checkpoint.
					- 2026-06-20 Stage38 plan: run a full public CTC benchmark for the unchanged Stage21 hard soup using the Stage37 decode policy (`mode=bi`, `beam_size=8`, `token_prune_topk=16`, `metric_normalization=ctc`, prediction text normalization `ctc`) and 4-GPU sharded public eval. Compare against the previous Stage21 beam4/topk16 public baseline: AISHELL-1 `30.93`, LibriSpeech clean `22.77 / 13.26`, LibriSpeech other `36.33 / 23.51`, CommonVoice EN `54.92 / 38.27`, WenetSpeech TEST_NET `48.59 / 48.99`, macro public WER/CER `38.706 / 30.992`. Treat Stage38 as an inference-policy validation only; no BiRWKV/RWKV-7 TimeMixer, frontend, tokenizer, labels, CTC objective, or direction/dropout behavior changes.
					- 2026-06-20 Stage38 result: full public CTC eval for the unchanged Stage21 hard soup with `beam8/topk16` completed under `/media/usbhd/rwkvasr_runs/stage21_checkpoint_soups_20260620/public_eval_s15_0p50_s16_0p50_ctc_beam8_topk16_stage38_sharded`. Results were AISHELL-1 `30.93 / 30.93`, LibriSpeech clean `22.77 / 13.26`, LibriSpeech other `36.32 / 23.50`, CommonVoice EN `54.92 / 38.28`, and WenetSpeech TEST_NET `48.58 / 48.98`. The macro public WER/CER is effectively unchanged from the beam4 baseline: WER improves by only about `0.002` absolute and CER is flat/slightly worse depending on rounding. Keep `beam8/topk16` as a harmless decode setting for selector work if useful, but it is not a meaningful paper-level improvement and should not distract from data/model training signal.
					- 2026-06-20 Stage39 plan: build a larger targeted teacher-label cache rather than another head-only or decode-only probe. Sample a fresh source-balanced Stage22 train candidate set with `2048` unique rows each from LibriSpeech, AISHELL3, CommonVoice EN, CommonVoice CN, GigaSpeech, and WenetSpeech (`12288` total), excluding the Stage30/37 selector ids and all previous teacher-cache ids. Run the current Stage21/Stage38 CTC decode policy on the candidate set to estimate student difficulty, select roughly the top `768` hard rows per source for FunASR-Nano teacher decoding, and conservatively accept only rows where teacher text is substantially closer to the dataset reference than the student prediction or is a plausible normalization repair. This remains a label/data optimization only: no BiRWKV/RWKV-7 TimeMixer, SenseVoice frontend, tokenizer, CTC objective, or direction-dropout changes.
					- 2026-06-20 Stage39 result: built `/media/usbhd/rwkvasr_runs/stage39_large_teacher_repair_20260620/stage39_candidate12288.lengths.jsonl` with `2048` rows each from LibriSpeech, AISHELL3, CommonVoice EN, CommonVoice CN, GigaSpeech, and WenetSpeech, excluding `2807` prior selector/teacher ids. Stage21/beam8 student prediction produced `12282` scored rows; missing rows were recorded separately. Student difficulty on the full candidate set averaged: LibriSpeech WER `23.59`, AISHELL3 CER `25.33`, CommonVoice EN WER `42.62`, CommonVoice CN CER `35.38`, GigaSpeech WER `33.30`, and WenetSpeech CER `47.57`. Selecting the top `768` hard rows per source yielded `4608` teacher targets. Offset-based audio extraction succeeded for all targets, and four FunASR-Nano processes decoded all `4608` rows. Conservative filtering accepted `4301` rows into `/media/usbhd/rwkvasr_runs/stage39_large_teacher_repair_20260620/stage39_teacher_accepted.cache.jsonl`: LibriSpeech `765`, AISHELL3 `750`, CommonVoice EN `700`, CommonVoice CN `688`, GigaSpeech `707`, and WenetSpeech `691`. On accepted rows, teacher errors were much lower than student errors, e.g. CommonVoice EN student `67.24` WER vs teacher `8.30`, GigaSpeech `57.79` vs `5.10`, WenetSpeech `74.35` CER vs `4.76`, AISHELL3 `43.71` CER vs `3.16`, CommonVoice CN `54.09` CER vs `6.31`, and LibriSpeech `36.11` WER vs `1.79`.
					- 2026-06-20 Stage40 plan: run a full-model ultra-low-LR teacher-repair probe from the Stage21 hard soup using the Stage39 accepted cache. Because Stage33/36 head-only teacher repair did not generalize, keep the BiRWKV/RWKV-7 TimeMixer architecture unchanged but allow all existing model weights to move with a very small LR (`5e-8`) instead of freezing the encoder. Build a mixed length index with `200k` original Stage22 rows plus `10k` repeated Stage39 accepted teacher rows (`~4.76%` teacher), excluding Stage30/37 selector ids and Stage39 accepted ids from the original component. Keep CTC-only training, no SpecAugment, no direction dropout, `tokenizer_append_eos=false`, `max_steps=1000`, `save_every=500`, batch size `12`, grad accumulation `4`, ZeRO-1, and no optimizer offload. Gate on the Stage30/37 256-sample selector before any public eval; promote only if both overall WER and CER improve without worsening CommonVoice EN or WenetSpeech.
					- 2026-06-20 Stage40 result: trained `/media/usbhd/rwkvasr_runs/sensevoice_rwkv_stage40_fullmodel_teacher_repair_from_stage21_soup_bs12_lr5e8_zero1_nockpt_noaug_nodirdrop_4x4090` to `step-1000` from Stage21 `wercer_best.pt` with all existing model weights trainable, LR `5e-8`, Stage39 teacher overrides via `teacher_text_tn`, no SpecAugment, no direction dropout, CTC-only loss, and ZeRO-1. Loader smoke verified that Stage40 length-index samples matching the teacher cache expose `_ctc_label_override_text` equal to `teacher_text_tn` and collate correctly. Training completed normally at about `1.0 step/s`, saved `step-500.pt` and `step-1000.pt`, and kept the BiRWKV/RWKV-7 TimeMixer architecture unchanged. Selector evaluation used the Stage37 decode policy (`beam8/topk16`) on the same 256-sample Stage30/37 source-balanced selector. Stage21 baseline under this decode is `27.512 / 18.044`. Stage40 `step-500` measured `27.537 / 18.028`: it improved CommonVoice EN `38.89/27.03 -> 38.65/26.98` and GigaSpeech `29.50/18.86 -> 28.83/18.67`, but worsened AISHELL `27.38 -> 27.77` and WenetSpeech `37.69 -> 38.43`. Stage40 `step-1000` measured `27.512 / 18.052`: WER tied the baseline and CommonVoice EN/GigaSpeech improved, but CER worsened and WenetSpeech remained worse at `38.43`. Do not promote Stage40 or run full public eval.
					- 2026-06-20 Stage41 plan: run an inference-only checkpoint interpolation screen between the Stage21 hard soup and Stage40 full-model teacher-repair checkpoints. The goal is to test whether a smaller fraction of the Stage40 movement can preserve its CommonVoice EN and GigaSpeech gains while recovering WenetSpeech and Chinese slices. This does not change the BiRWKV/RWKV-7 TimeMixer architecture, frontend, tokenizer, CTC objective, direction policy, or decode policy. Generate weighted-average candidates under `/media/usbhd/rwkvasr_runs/stage41_stage40_interpolation_soups_20260620` using Stage40 `step-500.pt` and `step-1000.pt` weights `0.125`, `0.25`, and `0.50` against Stage21 `wercer_best.pt`, preserving non-floating metadata from Stage21. Evaluate on the Stage30/37 256-sample selector with `beam8/topk16`, `mode=bi`, and `metric_normalization=ctc`. Promote only if a candidate beats Stage21 on both overall WER and CER while not worsening WenetSpeech and CommonVoice EN; otherwise keep Stage21 as incumbent and switch away from teacher-label repair toward a new same-architecture training signal.
					- 2026-06-20 Stage41 result: generated and evaluated six Stage21/Stage40 interpolation soups under `/media/usbhd/rwkvasr_runs/stage41_stage40_interpolation_soups_20260620`. Summary files are `stage41_selector256_beam8_summary.json` and `.csv`. Only `stage41_s40step500_0p125` passed the selector gate. It measured overall `27.463 / 17.995` versus the Stage21 `beam8/topk16` baseline `27.512 / 18.044`, with CommonVoice EN improving `38.89/27.03 -> 38.41/26.78`, CommonVoice CN improving `31.36 -> 31.23`, GigaSpeech improving `29.50/18.86 -> 28.83/18.67`, and WenetSpeech tying `37.69`. The cost is a small LibriSpeech CER/WER regression (`20.27/12.06 -> 20.40/12.09`) and AISHELL regression (`27.38 -> 27.77`). Stage40-step1000 `0.50` had a larger WER gain (`27.338 / 18.028`) but worsened WenetSpeech to `38.06`, so it failed the gate. Promote `stage41_s40step500_0p125` only to full public evaluation, not yet as the new incumbent checkpoint.
					- 2026-06-20 Stage42 plan: run a full public CTC benchmark for `stage41_s40step500_0p125` using the Stage37 decode policy (`mode=bi`, `beam8/topk16`, prediction text normalization `ctc`, metric normalization `ctc`) and the same sharded public eval harness used in Stage38. Compare against the unchanged Stage21 public baseline with `beam8/topk16`: AISHELL-1 `30.93 / 30.93`, LibriSpeech clean `22.77 / 13.26`, LibriSpeech other `36.32 / 23.50`, CommonVoice EN `54.92 / 38.28`, WenetSpeech TEST_NET `48.58 / 48.98`. Promote the Stage41 interpolation as incumbent only if public macro WER/CER improves without a material WenetSpeech or CommonVoice EN regression; otherwise keep Stage21 and use the Stage41 selector behavior only as a clue for the next same-architecture training signal.
					- 2026-06-20 Stage42 result: full public CTC eval for `stage41_s40step500_0p125` completed under `/media/usbhd/rwkvasr_runs/stage41_stage40_interpolation_soups_20260620/public_eval_s40step500_0p125_ctc_beam8_topk16_stage42_sharded`. Results were AISHELL-1 `30.91 / 30.91`, LibriSpeech test-clean `22.81 / 13.28`, LibriSpeech test-other `36.29 / 23.48`, CommonVoice EN `54.93 / 38.28`, and WenetSpeech TEST_NET `48.58 / 48.99`. This is effectively tied with the Stage21 `beam8/topk16` public baseline: AISHELL and Libri-other move by rounding-scale gains, but Libri-clean, CommonVoice EN WER, and WenetSpeech CER regress by rounding-scale amounts. Do not promote the Stage41 interpolation. Keep Stage21 as the incumbent and treat the Stage40/41 behavior as evidence that teacher-label repair currently creates local selector tradeoffs rather than a transferable public-set improvement.
					- 2026-06-20 Stage43 plan: switch away from teacher-label override and run a same-architecture difficulty-stratified original-label replay probe from Stage21. Reuse the Stage39 Stage21 student-difficulty scores on a fresh 12k source-balanced train candidate set, but train only against the original dataset CTC labels, not FunASR teacher text. Build a compact repeated length index from medium-hard rows per source, with a small easy-anchor component and a cap on extreme-error rows so noisy labels do not dominate. Keep the BiRWKV/RWKV-7 TimeMixer encoder, SenseVoice frontend, tokenizer, CTC head, CTC-only objective, disabled direction dropout, no SpecAugment, and Stage37 `beam8/topk16` eval policy unchanged. Start with a conservative full-model LR around `1e-7`, `max_steps=1000`, `save_every=500`, batch size `12`, grad accumulation `4`, ZeRO-1, and no gradient checkpointing. Gate on the Stage30/37 256-sample selector against the Stage21 baseline (`27.512 / 18.044` with `beam8/topk16`); public eval is allowed only if both overall WER and CER improve without worsening CommonVoice EN or WenetSpeech.
					- 2026-06-20 Stage43 preparation: added `scripts/build_stage43_difficulty_replay.py` and generated `/media/usbhd/rwkvasr_runs/stage43_difficulty_replay_20260620/stage43_difficulty_replay200k.lengths.jsonl` plus summary JSON. The index has exactly `200,000` rows, no missing length records, and balanced source targets around `33,333` rows each. Per source the sampled mix is approximately `15%` easy anchors, `65%` medium difficulty, `20%` hard rows, and `0%` extreme rows, using Stage39 student-error quantiles. Generated config `configs/generated/sensevoice_rwkv_stage43_difficulty_replay_ctc_from_stage21_soup_4x4090_deepspeed.yaml` points to Stage21 `wercer_best.pt`, uses LR `1e-7`, no SpecAugment, no direction dropout, full-model training, and `ctc_label_override_cache_path: null`. DeepSpeed loader smoke with the Stage43 config produced a real SenseVoice LFR batch and direct length-index sample decoding confirmed no `_ctc_label_override_text` is present.
					- 2026-06-20 Stage43 result: trained `/media/usbhd/rwkvasr_runs/sensevoice_rwkv_stage43_difficulty_replay_from_stage21_soup_bs12_lr1e7_zero1_nockpt_noaug_nodirdrop_4x4090` to `step-1000` from Stage21 `wercer_best.pt` with full-model CTC-only training, LR `1e-7`, no SpecAugment, no direction dropout, no teacher-label override, ZeRO-1, and the same BiRWKV/RWKV-7 TimeMixer architecture. Training completed normally at about `1.05 step/s`, saved `step-500.pt` and `step-1000.pt`, and final epoch train loss was about `1.9629`. Selector evaluation reused the Stage37/40 256-sample source-balanced sidecar and `beam8/topk16`. Stage43 `step-500` measured `27.537 / 18.052` versus the Stage21 baseline `27.512 / 18.044`; it improved CommonVoice EN (`38.89/27.03 -> 38.65/26.88`) but worsened WenetSpeech (`37.69 -> 38.43`) and overall metrics. Stage43 `step-1000` measured `27.637 / 18.085`, again improving CommonVoice EN but worsening WenetSpeech and overall metrics. Do not promote Stage43 or run full public eval.
					- 2026-06-20 Stage44 plan: run a checkpoint interpolation screen between Stage21 and the Stage43 difficulty-replay checkpoints to test whether a very small fraction of the Stage43 movement can preserve the CommonVoice EN gain while removing the WenetSpeech regression. This is inference-only checkpoint averaging and does not change the BiRWKV/RWKV-7 TimeMixer encoder, SenseVoice frontend, tokenizer, CTC head, CTC-only objective, direction policy, labels, or Stage37 `beam8/topk16` decode policy. Generate Stage21/Stage43 soups under `/media/usbhd/rwkvasr_runs/stage44_stage43_interpolation_soups_20260620` for `step-500.pt` and `step-1000.pt` using small Stage43 weights such as `0.03125`, `0.0625`, `0.125`, and `0.25`. Gate on the same Stage30/37 256-sample selector; run full public eval only if both overall WER and CER improve versus Stage21 without worsening CommonVoice EN or WenetSpeech.
					- 2026-06-20 Stage44 selector result: generated eight Stage21/Stage43 checkpoint soups and evaluated them on the Stage30/37 256-sample selector with `beam8/topk16`. Summary files are `/media/usbhd/rwkvasr_runs/stage44_stage43_interpolation_soups_20260620/stage44_selector256_beam8_summary.json` and `.csv`. Only `stage44_s43step500_0p03125` passed the selector gate. It measured overall `27.438 / 17.995` versus Stage21 `27.512 / 18.044`, with LibriSpeech `20.20 / 12.05`, AISHELL `27.77 / 27.77`, CommonVoice EN `38.41 / 26.83`, CommonVoice CN tied at `31.36`, GigaSpeech `29.05 / 18.72`, and WenetSpeech tied at `37.69`. The remaining soups either worsened overall metrics or worsened WenetSpeech. Treat this as a candidate for full public eval only, not a promoted incumbent.
					- 2026-06-20 Stage45 plan: run a full public CTC benchmark for `/media/usbhd/rwkvasr_runs/stage44_stage43_interpolation_soups_20260620/stage44_s43step500_0p03125/step-44031.pt` using the Stage37 decode policy (`mode=bi`, `beam8/topk16`, prediction text normalization `ctc`, metric normalization `ctc`) and the same 4-GPU sharded public eval harness used in Stage38 and Stage42. Compare against the unchanged Stage21 public baseline with `beam8/topk16`: AISHELL-1 `30.93 / 30.93`, LibriSpeech clean `22.77 / 13.26`, LibriSpeech other `36.32 / 23.50`, CommonVoice EN `54.92 / 38.28`, and WenetSpeech TEST_NET `48.58 / 48.98`. Promote only if public macro WER/CER improves without material CommonVoice EN or WenetSpeech regression; otherwise keep Stage21 incumbent and continue with a new same-architecture data/optimization signal.
					- 2026-06-20 Stage45 result: full public CTC eval for `stage44_s43step500_0p03125` completed under `/media/usbhd/rwkvasr_runs/stage44_stage43_interpolation_soups_20260620/public_eval_s43step500_0p03125_ctc_beam8_topk16_stage45_sharded`. Results were AISHELL-1 `30.93 / 30.93`, LibriSpeech test-clean `22.78 / 13.27`, LibriSpeech test-other `36.32 / 23.51`, CommonVoice EN `54.93 / 38.29`, and WenetSpeech TEST_NET `48.58 / 48.98`. This is not a public improvement over Stage21: WenetSpeech and AISHELL tie by rounding, but LibriSpeech clean, LibriSpeech other CER, and CommonVoice EN regress by rounding-scale amounts. Do not promote Stage44/45; keep Stage21 as incumbent.
					- 2026-06-20 Stage46 plan: run a multi-signal checkpoint interpolation screen that combines only small fractions of the two same-architecture training directions that showed selector-local value: Stage40 `step-500.pt` teacher-repair and Stage43 `step-500.pt` difficulty-replay. This is checkpoint averaging only; it does not change the BiRWKV/RWKV-7 TimeMixer encoder, SenseVoice frontend, tokenizer, CTC head, CTC-only objective, labels, direction policy, or Stage37 `beam8/topk16` decode policy. Generate a small grid under `/media/usbhd/rwkvasr_runs/stage46_multisignal_interpolation_soups_20260620` with Stage40 weights around `0.03125`, `0.0625`, `0.125` and Stage43 weights around `0.015625`, `0.03125`, `0.0625`, keeping Stage21 as the anchor for the remaining weight. Gate on the same Stage30/37 256-sample selector; run full public eval only for candidates that improve both overall WER and CER without worsening CommonVoice EN or WenetSpeech.
					- 2026-06-20 Stage46 result: generated and evaluated nine multi-signal Stage21/Stage40/Stage43 soups under `/media/usbhd/rwkvasr_runs/stage46_multisignal_interpolation_soups_20260620`; summary files are `stage46_selector256_beam8_summary.json` and `.csv`. No candidate passed the selector gate. The closest overall candidate was `stage46_s40w0p125_s43w0p015625`, which improved overall to `27.488 / 18.020` and improved CommonVoice EN by `-0.725 / -0.358`, but still worsened WenetSpeech from `37.69` to `38.43`. Other candidates either worsened overall metrics or also worsened WenetSpeech. This confirms the current teacher/difficulty directions repeatedly trade WenetSpeech stability for CV/Giga/overall local gains; stop this interpolation line and do not run public eval.
					- 2026-06-20 Stage47 plan: return to training with an explicit WenetSpeech guard rather than more checkpoint interpolation. Build a compact mixed length index from Stage21 that combines source-balanced original Stage22 replay, a small Stage39 teacher-repair component focused on non-Wenet high-error rows, and a repeated WenetSpeech original-label anchor sampled from Stage39 medium/easy Wenet rows. Keep the BiRWKV/RWKV-7 TimeMixer architecture, SenseVoice frontend, tokenizer, CTC head, CTC-only objective, labels, disabled direction dropout, no SpecAugment, and `beam8/topk16` eval policy unchanged. Use very low full-model LR around `2.5e-8` to `5e-8`, `max_steps=1000`, `save_every=500`, batch size `12`, grad accumulation `4`, ZeRO-1, and no gradient checkpointing. Gate on the same 256-sample selector and require no WenetSpeech regression before any public eval.
					- 2026-06-20 Stage47 preparation decision: materialize the Wenet-guard replay as `200k` rows: `180k` source-balanced original Stage22 rows (`30k` each from LibriSpeech, AISHELL3, CommonVoice EN, CommonVoice CN, GigaSpeech, and WenetSpeech), `8k` repeated Stage39 accepted teacher rows restricted to non-Wenet sources, and `12k` repeated WenetSpeech original-label anchors selected from Stage39 Wenet rows at or below the source `q75` student-error cutoff. Exclude the Stage30/37 selector ids and Stage39 accepted teacher ids from the original replay, and write a Stage47-specific teacher override cache containing only the sampled non-Wenet teacher ids so Wenet anchors cannot be silently teacher-overridden. This remains a data/optimization change only; the BiRWKV/RWKV-7 TimeMixer architecture and CTC decode policy are unchanged.
					- 2026-06-20 Stage47 preparation result: added `scripts/build_stage47_wenet_guard_replay.py` and generated `/media/usbhd/rwkvasr_runs/stage47_wenet_guard_repair_20260620/stage47_original180k_teacher8k_wenet12k.lengths.jsonl` plus summary JSON and `/media/usbhd/rwkvasr_runs/stage47_wenet_guard_repair_20260620/stage47_teacher_non_wenet_sampled.cache.jsonl`. The generated length index has exactly `200,000` rows: `180,000` original replay, `8,000` non-Wenet teacher repair, and `12,000` Wenet original-label anchors. Source totals are `31,600` each for LibriSpeech, AISHELL3, CommonVoice EN, CommonVoice CN, and GigaSpeech, and `42,000` for WenetSpeech (`30k` original + `12k` anchor). The teacher cache contains `3,124` unique sampled non-Wenet ids and no Wenet rows; Stage39 accepted Wenet rows are excluded from Wenet anchors. Loader smoke from `configs/generated/sensevoice_rwkv_stage47_wenet_guard_repair_ctc_from_stage21_soup_4x4090_deepspeed.yaml` parsed correctly, produced a real SenseVoice LFR batch, applied `_ctc_label_override_text` on a teacher component sample, and did not apply override on Wenet anchor or original replay samples.
					- 2026-06-20 Stage47 result: trained `/media/usbhd/rwkvasr_runs/sensevoice_rwkv_stage47_wenet_guard_repair_from_stage21_soup_bs12_lr5e8_zero1_nockpt_noaug_nodirdrop_4x4090` to `step-1000` from Stage21 `wercer_best.pt` with LR `5e-8`, CTC-only loss, no SpecAugment, no direction dropout, ZeRO-1, and unchanged BiRWKV/RWKV-7 TimeMixer architecture. Training completed normally at about `1.05 step/s`, saved `step-500.pt` and `step-1000.pt`, and ended with final logged loss around `1.37` on the main rank. Stage47 selector evaluation used a wrapper run dir so the fixed Stage30/37 selector index, not the Stage47 training index, was decoded with `mode=bi`, `beam8/topk16`, and normalized CTC WER/CER. Against the Stage21 selector baseline `27.512 / 18.044`, `step-500` measured `27.587 / 17.938` and `step-1000` measured `27.637 / 17.955`. Both checkpoints improved WenetSpeech (`37.69 -> 36.94`) and GigaSpeech, and both improved overall CER, but both worsened overall WER, AISHELL, CommonVoice EN, and CommonVoice CN. The public-eval gate therefore failed; do not promote Stage47 directly and do not run full public eval from these raw checkpoints.
					- 2026-06-20 Stage48 plan: run an inference-only checkpoint interpolation screen between Stage21 and the Stage47 Wenet-guard checkpoints. The goal is to test whether a small fraction of the Stage47 movement can retain the WenetSpeech and CER gains while recovering overall WER, AISHELL, CommonVoice EN, and CommonVoice CN. This is checkpoint averaging only and does not change the BiRWKV/RWKV-7 TimeMixer encoder, SenseVoice frontend, tokenizer, CTC head, CTC-only objective, labels, direction policy, or Stage37 `beam8/topk16` decode policy. Generate candidates under `/media/usbhd/rwkvasr_runs/stage48_stage47_interpolation_soups_20260620` for Stage47 `step-500.pt` and `step-1000.pt` with small Stage47 weights such as `0.03125`, `0.0625`, `0.125`, `0.25`, and `0.50`, preserving Stage21 metadata. Gate on the same Stage30/37 256-sample selector; full public eval is allowed only if both overall WER and CER improve versus Stage21 without worsening WenetSpeech or CommonVoice EN.
					- 2026-06-20 Stage48 result: generated and evaluated ten Stage21/Stage47 interpolation soups under `/media/usbhd/rwkvasr_runs/stage48_stage47_interpolation_soups_20260620`; summary files are `stage48_selector256_beam8_summary.json` and `.csv`. No candidate passed the selector gate. The best WER candidate was `step-49031` (`Stage47 step-1000` weight `0.03125`), which matched the Stage21 overall WER at `27.512` and improved CER to `17.873`, with WenetSpeech improving to `36.57`, but CommonVoice EN worsened to `39.37 / 27.29`. Other candidates improved WenetSpeech but either worsened overall WER or CommonVoice EN. Do not run public eval from Stage48 and keep Stage21 as incumbent.
					- 2026-06-20 Stage49 plan: run a compensated multi-signal checkpoint interpolation screen that combines a very small Stage43 difficulty-replay component, which previously improved CommonVoice EN/GigaSpeech and passed the selector at low weight, with a small Stage47 Wenet-guard component, which improves WenetSpeech and overall CER but worsens CommonVoice EN alone. This is checkpoint averaging only; it does not change the BiRWKV/RWKV-7 TimeMixer encoder, SenseVoice frontend, tokenizer, CTC head, CTC-only objective, training labels, direction policy, or Stage37 `beam8/topk16` decode policy. Generate a focused grid under `/media/usbhd/rwkvasr_runs/stage49_stage43_stage47_compensated_soups_20260620` with Stage43 `step-500.pt` weights around `0.015625` and `0.03125`, and Stage47 `step-500.pt` / `step-1000.pt` weights around `0.015625`, `0.03125`, `0.0625`, and `0.125`, keeping Stage21 as the remaining anchor. Gate on the same Stage30/37 256-sample selector; full public eval is allowed only if both overall WER and CER improve versus Stage21 without worsening WenetSpeech or CommonVoice EN.
					- 2026-06-20 Stage49 selector result: generated and evaluated sixteen compensated Stage21/Stage43/Stage47 soups under `/media/usbhd/rwkvasr_runs/stage49_stage43_stage47_compensated_soups_20260620`; summary files are `stage49_selector256_beam8_summary.json` and `.csv`. Exactly one candidate passed the selector gate: `step-493187.pt`, with Stage21 anchor `0.90625`, Stage43 `step-500.pt` weight `0.03125`, and Stage47 `step-500.pt` weight `0.0625`. It measured overall `27.413 / 17.784` versus Stage21 `27.512 / 18.044`, CommonVoice EN `38.65 / 26.62` versus `38.89 / 27.03`, and WenetSpeech `36.94 / 36.94` versus `37.69 / 37.69`. This is the first candidate in the Stage40-49 sequence to improve overall WER/CER while also not worsening both CommonVoice EN and WenetSpeech on the fixed selector. Treat it only as a public-eval candidate, not yet an incumbent.
					- 2026-06-20 Stage50 plan: run a full public CTC benchmark for `/media/usbhd/rwkvasr_runs/stage49_stage43_stage47_compensated_soups_20260620/step-493187.pt` using the Stage37 decode policy (`mode=bi`, `beam8/topk16`, prediction text normalization `ctc`, metric normalization `ctc`) and the same 4-GPU sharded public eval harness used in Stage38, Stage42, and Stage45. Compare against the unchanged Stage21 public baseline with `beam8/topk16`: AISHELL-1 `30.93 / 30.93`, LibriSpeech test-clean `22.77 / 13.26`, LibriSpeech test-other `36.32 / 23.50`, CommonVoice EN `54.92 / 38.28`, and WenetSpeech TEST_NET `48.58 / 48.98`. Promote only if public macro WER/CER improves without material CommonVoice EN or WenetSpeech regression; otherwise keep Stage21 incumbent and use the selector behavior to design the next same-architecture data/optimization signal.
					- 2026-06-21 Stage50 result: full public CTC eval for Stage49 `step-493187.pt` completed under `/media/usbhd/rwkvasr_runs/stage49_stage43_stage47_compensated_soups_20260620/public_eval_stage49_step493187_ctc_beam8_topk16_stage50_sharded`. Results were AISHELL-1 `30.92 / 30.92`, LibriSpeech test-clean `22.82 / 13.29`, LibriSpeech test-other `36.26 / 23.46`, CommonVoice EN `54.93 / 38.28`, and WenetSpeech TEST_NET `48.59 / 48.99`. This transfers only partly: AISHELL and LibriSpeech-other improve, but LibriSpeech-clean, CommonVoice EN WER, and WenetSpeech WER/CER regress by rounding-scale amounts. Do not promote Stage49/50; keep Stage21 as incumbent. The selector improvement is still useful evidence that the Stage43/Stage47 directions can compensate locally, but the public-clean and public-Wenet margins are too small.
					- 2026-06-21 Stage51 plan: run a very conservative CTC-head-only public-anchor calibration from the Stage49 `step-493187.pt` candidate. Freeze the full SenseVoice/BiRWKV encoder (`freeze_encoder=true`) and leave only the existing CTC head trainable, so the RWKV-7 TimeMixer architecture and encoder weights do not move. Reuse the Stage22 `90%` public-anchor clean + `10%` hard replay length index, clear all teacher/draft caches, use CTC-only labels, no SpecAugment, no direction dropout, `tokenizer_append_eos=false`, ZeRO-1, batch size `12`, grad accumulation `4`, no optimizer offload, and a lower head-only LR around `2e-7` with `max_steps=500` and `save_every=250`. Gate first on the same Stage30/37 256-sample selector with `beam8/topk16`; public eval is allowed only if overall WER/CER improve versus Stage21 while CommonVoice EN and WenetSpeech do not worsen. If this cannot recover public-clean/CV/Wenet margins, stop the current soup/calibration line and return to a new data signal rather than further tiny checkpoint averaging.
					- 2026-06-21 Stage51 selector result: trained `/media/usbhd/rwkvasr_runs/sensevoice_rwkv_stage51_headonly_public_anchor_from_stage49_step493187_bs12_lr2e7_zero1_nockpt_noaug_nodirdrop_4x4090` from Stage49 `step-493187.pt` with the encoder frozen and only `31,044,708` CTC-head parameters trainable. Training completed normally to `step-500` at about `2.8-2.9 step/s`, saved `step-250.pt` and `step-500.pt`, and kept BiRWKV/RWKV-7 TimeMixer weights frozen. Fixed Stage30/37 selector evaluation with `beam8/topk16` measured `step-250` at `27.413 / 17.792`, CommonVoice EN `38.65 / 26.67`, and WenetSpeech `36.94 / 36.94`; `step-500` measured `27.438 / 17.808`, CommonVoice EN `38.65 / 26.72`, and WenetSpeech `36.94 / 36.94`. Both pass the selector gate versus Stage21 (`27.512 / 18.044`), with `step-250` selected by WER-first/CER-second. Treat `step-250.pt` as a public-eval candidate only, not an incumbent.
					- 2026-06-21 Stage52 plan: run a full public CTC benchmark for `/media/usbhd/rwkvasr_runs/sensevoice_rwkv_stage51_headonly_public_anchor_from_stage49_step493187_bs12_lr2e7_zero1_nockpt_noaug_nodirdrop_4x4090/step-250.pt` using the same Stage37 decode policy (`mode=bi`, `beam8/topk16`, prediction normalization `ctc`, metric normalization `ctc`) and the same 4-GPU sharded public eval harness. Compare against the unchanged Stage21 public baseline with `beam8/topk16`: AISHELL-1 `30.93 / 30.93`, LibriSpeech test-clean `22.77 / 13.26`, LibriSpeech test-other `36.32 / 23.50`, CommonVoice EN `54.92 / 38.28`, and WenetSpeech TEST_NET `48.58 / 48.98`. Promote Stage51 only if public macro WER/CER improves without material CommonVoice EN or WenetSpeech regression; otherwise keep Stage21 incumbent.
					- 2026-06-21 Stage52 result: full public CTC eval for Stage51 `step-250.pt` completed under `/media/usbhd/rwkvasr_runs/sensevoice_rwkv_stage51_headonly_public_anchor_from_stage49_step493187_bs12_lr2e7_zero1_nockpt_noaug_nodirdrop_4x4090/public_eval_stage51_step250_ctc_beam8_topk16_stage52_sharded`. Results were AISHELL-1 `30.92 / 30.92`, LibriSpeech test-clean `22.82 / 13.29`, LibriSpeech test-other `36.27 / 23.47`, CommonVoice EN `54.93 / 38.28`, and WenetSpeech TEST_NET `48.59 / 48.99`. This is not a public improvement over Stage21: AISHELL and LibriSpeech-other improve slightly, but LibriSpeech-clean, CommonVoice EN WER, and WenetSpeech WER/CER still regress at rounding scale. Do not promote Stage51/52; keep Stage21 as incumbent and stop the current Stage43/47 soup plus CTC-head calibration line.
					- 2026-06-21 Stage53 plan: switch to a new data signal rather than further checkpoint averaging. Build an English-noisy teacher repair plus public-clean/Chinese guard replay from Stage21: keep a dominant source-balanced original Stage22 replay component, inject teacher overrides only for the noisy English sources that repeatedly benefit locally (CommonVoice EN and GigaSpeech), and add original-label guard anchors for WenetSpeech plus clean/public Chinese and LibriSpeech slices. Do not use WenetSpeech, AISHELL, CommonVoice CN, or LibriSpeech teacher overrides in this stage, because prior Stage40/47 probes showed teacher labels outside noisy English sources create Chinese/public-clean tradeoffs. Keep the BiRWKV/RWKV-7 TimeMixer architecture, SenseVoice frontend, tokenizer, CTC head, CTC-only objective, no SpecAugment, disabled direction dropout, and `beam8/topk16` eval policy unchanged. Start from Stage21 incumbent, use full-model ultra-low LR around `2.5e-8`, `max_steps=1000`, `save_every=500`, batch size `12`, grad accumulation `4`, ZeRO-1, no gradient checkpointing, and strict Stage30/37 selector gating before any public eval.
					- 2026-06-21 Stage53 preparation result: added `scripts/build_stage53_english_teacher_guard_replay.py` and generated `/media/usbhd/rwkvasr_runs/stage53_english_teacher_guard_20260621/stage53_original180k_engteacher6k_guard14k.lengths.jsonl`, `/media/usbhd/rwkvasr_runs/stage53_english_teacher_guard_20260621/stage53_teacher_cven_giga_sampled.cache.jsonl`, and summary JSON. The index has exactly `200,000` rows: `180,000` source-balanced Stage22 original-label replay, `6,000` English noisy-source teacher rows (`3,000` CommonVoice EN + `3,000` GigaSpeech), and `14,000` original-label guard anchors (`8,000` WenetSpeech, `2,000` each LibriSpeech/AISHELL3/CommonVoice CN). Source totals are LibriSpeech `32k`, AISHELL3 `32k`, CommonVoice CN `32k`, CommonVoice EN `33k`, GigaSpeech `33k`, and WenetSpeech `38k`. The sampled teacher override cache has `1,363` unique rows and only CommonVoice EN/GigaSpeech sources; a string audit found no WenetSpeech, AISHELL, CommonVoice CN, or LibriSpeech rows in the sampled teacher cache. Generated config `configs/generated/sensevoice_rwkv_stage53_english_teacher_guard_ctc_from_stage21_soup_4x4090_deepspeed.yaml` parses with LR `2.5e-8`, `max_steps=1000`, `save_every=500`, no SpecAugment, no direction dropout, and full-model training from Stage21.
					- 2026-06-21 Stage53 selector result: trained `/media/usbhd/rwkvasr_runs/sensevoice_rwkv_stage53_english_teacher_guard_from_stage21_soup_bs12_lr2p5e8_zero1_nockpt_noaug_nodirdrop_4x4090` to `step-1000` from Stage21 `wercer_best.pt` with LR `2.5e-8`, CTC-only loss, no SpecAugment, no direction dropout, ZeRO-1, and unchanged BiRWKV/RWKV-7 TimeMixer architecture. Training completed normally at about `1.03-1.04 step/s`, saved `step-500.pt` and `step-1000.pt`, and ended with epoch train loss `1.9823`. Fixed Stage30/37 selector evaluation used a wrapper run dir with the training teacher cache cleared and measured `step-500` at `27.537 / 17.930`, CommonVoice EN `39.61 / 27.34`, and WenetSpeech `36.57 / 36.57`; `step-1000` measured `27.612 / 17.963`, CommonVoice EN `39.61 / 27.44`, and WenetSpeech `36.94 / 36.94`. Summary files are `/media/usbhd/rwkvasr_runs/stage53_english_teacher_guard_selector_eval_20260621/stage53_selector256_beam8/stage53_selector256_beam8_summary.json` and `.csv`. Both checkpoints improve WenetSpeech and overall CER, but both worsen overall WER and CommonVoice EN, so the public-eval gate failed. Do not run public eval from raw Stage53 and keep Stage21 as incumbent.
					- 2026-06-21 Stage54 plan: run a compensated checkpoint-soup screen that pairs the stronger Stage53 WenetSpeech direction with the Stage43 CommonVoice/GigaSpeech repair direction, using Stage21 as the anchor. This is inference-only checkpoint averaging; it does not change the BiRWKV/RWKV-7 TimeMixer encoder, SenseVoice frontend, tokenizer, CTC head, objective, labels, direction policy, or `beam8/topk16` decode policy. Generate a small grid under `/media/usbhd/rwkvasr_runs/stage54_stage43_stage53_compensated_soups_20260621` with Stage43 `step-500.pt` weights around `0.015625`, `0.03125`, `0.0625`, and Stage53 `step-500.pt` weights around `0.015625`, `0.03125`, `0.0625`, and `0.125`, keeping Stage21 as the remaining anchor. Gate on the same Stage30/37 256-sample selector; full public eval is allowed only if both overall WER and CER improve versus Stage21 without worsening WenetSpeech or CommonVoice EN.
					- 2026-06-21 Stage54 selector result: generated and evaluated twelve Stage21/Stage43/Stage53 compensated soups under `/media/usbhd/rwkvasr_runs/stage54_stage43_stage53_compensated_soups_20260621`. To avoid sidecar state races while using all four GPUs, each soup was evaluated through a one-checkpoint wrapper under `/media/usbhd/rwkvasr_runs/stage54_stage43_stage53_compensated_soups_20260621_wrappers`; the merged selector outputs and summary live in `/media/usbhd/rwkvasr_runs/stage54_stage43_stage53_compensated_soups_20260621/stage54_selector256_beam8/stage54_selector256_beam8_summary.json` and `.csv`. Exactly one candidate passed the strict selector gate: `step-540004.pt`, with Stage21 `0.859375`, Stage43 `step-500.pt` `0.015625`, and Stage53 `step-500.pt` `0.125`. It measured overall `27.488 / 17.865` versus Stage21 `27.512 / 18.044`, CommonVoice EN `38.89 / 26.88` versus `38.89 / 27.03`, and WenetSpeech `36.57 / 36.57` versus `37.69 / 37.69`. Treat it only as a public-eval candidate; do not promote until full public metrics beat Stage21 without material CommonVoice EN or WenetSpeech regression.
					- 2026-06-21 Stage55 plan: run a full public CTC benchmark for `/media/usbhd/rwkvasr_runs/stage54_stage43_stage53_compensated_soups_20260621/step-540004.pt` using the same Stage37 decode policy (`mode=bi`, `beam8/topk16`, prediction normalization `ctc`, metric normalization `ctc`) and the same 4-GPU sharded public eval harness used for Stage50/52. Output to `/media/usbhd/rwkvasr_runs/stage54_stage43_stage53_compensated_soups_20260621/public_eval_stage54_step540004_ctc_beam8_topk16_stage55_sharded`. Compare against the unchanged Stage21 public baseline with `beam8/topk16`: AISHELL-1 `30.93 / 30.93`, LibriSpeech test-clean `22.77 / 13.26`, LibriSpeech test-other `36.32 / 23.50`, CommonVoice EN `54.92 / 38.28`, and WenetSpeech TEST_NET `48.58 / 48.98`. Promote Stage54 only if public macro WER/CER improves without material CommonVoice EN or WenetSpeech regression; otherwise keep Stage21 incumbent and return to another same-architecture data/optimization signal.
					- 2026-06-21 Stage55 result: full public CTC eval for Stage54 `step-540004.pt` completed under `/media/usbhd/rwkvasr_runs/stage54_stage43_stage53_compensated_soups_20260621/public_eval_stage54_step540004_ctc_beam8_topk16_stage55_sharded`. Results were AISHELL-1 `30.91 / 30.91`, LibriSpeech test-clean `22.79 / 13.25`, LibriSpeech test-other `36.31 / 23.51`, CommonVoice EN `54.95 / 38.29`, and WenetSpeech TEST_NET `48.58 / 48.99`. This is not a public promotion over Stage21: AISHELL and LibriSpeech-clean CER improve, LibriSpeech-other WER improves slightly, and WenetSpeech WER ties, but LibriSpeech-clean WER, LibriSpeech-other CER, CommonVoice EN WER/CER, and WenetSpeech CER regress at rounding scale. Do not promote Stage54/55; keep Stage21 as incumbent. The repeated selector-to-public mismatch indicates that future candidates need a stronger public-clean/CommonVoice guard before full public eval, not more compensation soups alone.
					- 2026-06-21 Stage56 plan: add an evaluation-only public-sample guard before any future full public run. Reuse the fixed 200-sample public manifests from `artifacts/eval_benchmarks/beam_ablation_20260620/manifests_200_seed20260620` with the Stage37 decode policy (`mode=bi`, `beam8/topk16`, prediction normalization `ctc`, metric normalization `ctc`). Evaluate Stage21 incumbent plus representative Stage54 candidates: `step-540001.pt` (small Wenet-heavy movement), `step-540004.pt` (only selector-pass candidate, full-public failed), `step-540006.pt` (overall selector improvement but CV CER gate fail), and `step-540012.pt` (strongest selector CV CER but overall WER fail). This stage does not change checkpoints or architecture. Use it to decide whether the remaining Stage54 surface has any public-clean/CommonVoice promise; require future full-public candidates to pass both the train-side selector and this public-sample guard.
					- 2026-06-21 Stage56 result: public-200 guard finished under `/media/usbhd/rwkvasr_runs/stage56_public_sample_guard_seq1_20260621` with summary files `stage56_public200_beam8_topk16_summary.json` and `.csv`. The first attempted 4-shard run was interrupted because repeated 2GB checkpoint loads dominated the 200-sample workload; the completed run used one shard on GPU0, with the same decode policy and metrics. Stage21 public-200 baseline measured macro `38.233 / 30.362`, CommonVoice EN `53.35 / 35.10`, and WenetSpeech `46.95 / 49.02`. Stage54 candidates `step-540004`, `step-540006`, and `step-540012` slightly improved macro WER/CER (`-0.008/-0.055`, `-0.012/-0.039`, and `-0.008/-0.035`), and all improved WenetSpeech, but every Stage54 candidate worsened CommonVoice EN WER/CER; `step-540001` also worsened macro WER. Therefore no Stage54 candidate passes the new public-sample guard, and no further full public eval should be run from the Stage54 surface. Keep Stage21 as incumbent.
					- 2026-06-21 Stage57 plan: before new training, apply the same public-200 `beam8/topk16` guard to the earlier base Pareto surface rather than continuing the failed Stage54 compensation line. Evaluate Stage15 `wercer_best.pt`, Stage16 `step-5000.pt`, Stage19 `wercer_best.pt`, Stage22 `step-1000.pt`, and the most relevant Stage26 base soups (`s15_0p40_s16_0p50_s19_0p10`, `s15_0p40_s16_0p40_s19_0p20`, and `s22_0p75_s19_0p25`) against the Stage21 incumbent on the fixed public-200 manifests with `mode=bi`, prediction normalization `ctc`, and metric normalization `ctc`. This is evaluation-only and does not change the BiRWKV/RWKV-7 TimeMixer architecture, frontend, tokenizer, CTC labels, CTC head, training objective, direction policy, or checkpoints. Promote a candidate to train-selector recheck or full public eval only if it beats Stage21 public-200 macro WER/CER while not worsening both CommonVoice EN and WenetSpeech WER/CER; otherwise start a new same-architecture training stage from Stage21 with an explicit CommonVoice-public guard rather than more checkpoint averaging.
					- 2026-06-21 Stage57 result: public-200 base Pareto guard completed under `/media/usbhd/rwkvasr_runs/stage57_base_pareto_public_sample_guard_20260621`; summary files are `stage57_public200_beam8_topk16_summary.json` and `.csv`. Stage16 and Stage22 improved macro WER/CER but still worsened CommonVoice EN, while Stage15/19 regressed both macro and WenetSpeech. The best macro candidate was `stage26_s22_0p75_s19_0p25` at `38.129 / 30.292`, but it failed the strict guard because CommonVoice EN CER worsened by `+0.033`. Exactly one candidate passed the strict public-sample guard: `stage26_s15_0p40_s16_0p50_s19_0p10.pt`, with macro `38.218 / 30.318` versus Stage21 `38.233 / 30.362`, CommonVoice EN `53.30 / 35.08` versus `53.35 / 35.10`, and WenetSpeech `46.92 / 48.87` versus `46.95 / 49.02`. Treat this as a recheck candidate only; it is still an inference-only soup and has not been validated on the fixed Stage30/37 train-side selector or full public sets.
					- 2026-06-21 Stage58 plan: run a Stage30/37 256-sample train-selector recheck for `/media/usbhd/rwkvasr_runs/stage26_checkpoint_soups_20260620/s15_0p40_s16_0p50_s19_0p10.pt` before spending a full public eval. Use `/media/usbhd/rwkvasr_runs/stage37_ctc_decode_audit_20260620/stage30_selector256.lengths.jsonl`, `mode=bi`, `beam8/topk16`, prediction normalization `ctc`, and metric normalization `ctc`, then compare to the Stage21 selector baseline `27.512 / 18.044` overall with source-level checks for CommonVoice EN and WenetSpeech. This remains evaluation-only and does not change architecture or training. Run full public CTC eval only if the candidate does not worsen overall WER/CER, CommonVoice EN, or WenetSpeech on this selector; otherwise keep Stage21 and move to a new same-architecture training recipe with a stronger CV/public guard.
					- 2026-06-21 Stage58 result: the Stage26 `s15_0p40_s16_0p50_s19_0p10` public-sample-pass soup failed the Stage30/37 selector recheck under `beam8/topk16`. The first direct run failed because the selector is a WebDataset length-index rather than a plain manifest; the completed run used `--webdataset-root /media/usbhd/training_data/asr/curriculum/stage22_stage21_soup_clean_repair_mix` and `--webdataset-length-index-path /media/usbhd/rwkvasr_runs/stage37_ctc_decode_audit_20260620/stage30_selector256.lengths.jsonl`. Summary files are under `/media/usbhd/rwkvasr_runs/stage58_stage26_candidate_selector_recheck_20260621`. The candidate measured overall `27.587 / 17.922` versus Stage21 `27.512 / 18.044`: CER improves by `-0.122`, but WER worsens by `+0.075`. It improves LibriSpeech, GigaSpeech, and WenetSpeech (`37.69 -> 36.94`), but worsens AISHELL (`27.38 -> 28.54`), CommonVoice EN (`38.89/27.03 -> 39.37/27.18`), and CommonVoice CN (`31.36 -> 32.22`). Strict selector gate failed, so do not run full public eval for this soup.
					- 2026-06-21 Stage59 plan: start a new same-architecture short training stage from Stage21 instead of more soup search. Build a compact source-weighted original-label replay under the Stage22 curriculum root with about `200k` train rows: maxed CommonVoice EN clean anchor (`47k`), AISHELL clean anchor (`52k`), LibriSpeech clean anchor (`43k`), CommonVoice CN clean anchor (`28k`), WenetSpeech original-label hard anchor (`25k`), and a small GigaSpeech anchor (`5k`). This directly targets the Stage58 selector weaknesses (CommonVoice EN/AISHELL/CV-CN) while retaining WenetSpeech guard rows. Use full-model CTC-only training from Stage21 `wercer_best.pt`, no teacher labels, no SpecAugment, no gradient checkpointing, ZeRO-1, batch size `12`, grad accumulation `4`, very low LR around `5e-8`, `max_steps=1000`, `save_every=500`, and no direction dropout (`direction_variant=none`, `p_start=0.0`, `p_max=0.0`, `warmup_steps=0`, `ramp_steps=0`). After checkpoints, evaluate both the Stage30/37 selector and the fixed public-200 guard before any full public run.
					- 2026-06-21 Stage59 preparation result: generated `/media/usbhd/training_data/asr/curriculum/stage22_stage21_soup_clean_repair_mix/stages/stage59_cv_public_guard_wenet_anchor/webdataset_lengths.jsonl` plus bucket manifest `webdataset_buckets_audio_text/manifest.json`. The index has exactly `200,000` train rows with only original labels: `170,000` clean public-guard rows and `30,000` hard Wenet/Giga anchor rows. Source totals are AISHELL3 `52k`, CommonVoice EN `47k`, LibriSpeech `43k`, CommonVoice CN `28k`, WenetSpeech `25k`, and GigaSpeech `5k`. Generated config `configs/generated/sensevoice_rwkv_stage59_cv_public_guard_wenet_anchor_ctc_from_stage21_soup_4x4090_deepspeed.yaml` parses with Stage21 `wercer_best.pt` as init, LR `5e-8`, `max_steps=1000`, `save_every=500`, batch size `12`, gradient accumulation `4`, ZeRO-1, bf16, no SpecAugment, no gradient checkpointing, CTC-only loss, and direction dropout disabled. Launch this training before any further Stage59 evaluation; sidecar beam4 is only a health check, while promotion gates remain Stage30/37 selector and public-200 guard with `beam8/topk16`.
					- 2026-06-21 Stage59 result: trained `/media/usbhd/rwkvasr_runs/sensevoice_rwkv_stage59_cv_public_guard_wenet_anchor_from_stage21_soup_bs12_lr5e8_zero1_nockpt_noaug_nodirdrop_4x4090` to `step-1000` from Stage21, saving `step-500.pt` and `step-1000.pt`; training exited normally with final rank-0 loss `1.4801`, epoch train loss `1.9040`, CTC-only loss, and direction dropout disabled. Fixed Stage30/37 selector results are under `/media/usbhd/rwkvasr_runs/stage59_cv_public_guard_eval_20260621/stage59_selector256_b8_k16_summary.json`: `step-500` measured overall `27.687 / 17.946`, and `step-1000` measured `27.662 / 18.012` versus Stage21 `27.512 / 18.044`. Both checkpoints improve LibriSpeech, GigaSpeech, and WenetSpeech, but both worsen overall WER, AISHELL, CommonVoice EN, and CommonVoice CN, so strict selector gate failed. Public-200 guard results are under `/media/usbhd/rwkvasr_runs/stage59_cv_public_guard_eval_20260621/stage59_public200_beam8_topk16_summary.json`: `step-1000` improved macro `38.233 / 30.362 -> 38.182 / 30.303`, AISHELL, LibriSpeech-clean, and WenetSpeech, but worsened CommonVoice EN to `53.400 / 35.165` and LibriSpeech-other WER to `38.165`, so the public-sample gate also failed. Do not promote Stage59 or run full public eval.
					- 2026-06-21 Stage60 plan: run an inference-only Stage21/Stage59 interpolation screen before any more training. Stage59 contains a useful public macro/Wenet direction but overshoots selector WER and CommonVoice guard, so generate small-weight soups under `/media/usbhd/rwkvasr_runs/stage60_stage21_stage59_interpolation_soups_20260621` using Stage21 `wercer_best.pt` as anchor and Stage59 `step-500.pt` / `step-1000.pt` as the movement components. Start with Stage59 weights `0.0625`, `0.125`, `0.25`, and `0.50` for each movement checkpoint; copy anchor configs and keep the exact BiRWKV/RWKV-7 TimeMixer encoder, SenseVoice frontend, tokenizer, CTC head, CTC-only objective, direction policy, and `beam8/topk16` decode policy unchanged. Gate first on the Stage30/37 selector; a soup may receive public-200 guard only if it does not worsen overall WER/CER and does not worsen AISHELL, CommonVoice EN, CommonVoice CN, or WenetSpeech WER/CER versus Stage21. If no soup passes this selector gate, stop the Stage59 data direction and design the next same-architecture training recipe from a different data signal rather than continuing this LR/replay surface.
					- 2026-06-21 Stage60 result: generated eight Stage21/Stage59 interpolation soups under `/media/usbhd/rwkvasr_runs/stage60_stage21_stage59_interpolation_soups_20260621` and evaluated all on the fixed Stage30/37 selector with `beam8/topk16`. The first failed selector attempt used a relative `.venv/bin` PATH, which broke PyTorch CUDA-extension JIT after cwd changed; rerunning with absolute `/home/yueyulin/github/rwkvasr_longform_asr/.venv/bin` completed all predictions. Summary files are `stage60_selector256_b8_k16_summary.json` and `.csv`. No candidate passed the selector gate. The best overall candidate was `stage60_s21_0p5_s59s500_0p5`, measuring `27.562 / 17.865` versus Stage21 `27.512 / 18.044`, with WenetSpeech improving to `36.567 / 36.567` but CommonVoice EN worsening to `39.130 / 27.031`; smaller weights still worsened overall WER and CommonVoice EN. Do not run public-200 or full public eval from Stage60. Stop the Stage59 replay direction.
					- 2026-06-21 Stage61 plan: switch to a new original-label data signal that combines only the parts that have shown local value, without teacher overrides and without changing the BiRWKV/RWKV-7 TimeMixer architecture. Build a reproducible `200k` row difficulty/public/Wenet replay: use Stage43 difficulty-stratified original-label rows for CommonVoice EN (`40k`), GigaSpeech (`20k`), and CommonVoice CN (`10k`) because Stage43 helped selector CV/Giga; use Stage47 Wenet original-anchor rows (`40k`, sampled with replacement from the Wenet easy/medium anchor pool) because Wenet repeatedly needs an explicit guard; use Stage59 clean/public rows for AISHELL (`35k`), LibriSpeech (`35k`), and CommonVoice CN (`20k`) because Stage59 helped public AISHELL/Libri/Wenet but should not drive CV-EN alone. Total source targets are LibriSpeech `35k`, AISHELL3 `35k`, CommonVoice EN `40k`, CommonVoice CN `30k`, GigaSpeech `20k`, and WenetSpeech `40k`. Train from Stage21 `wercer_best.pt` with full-model CTC-only loss, LR `2.5e-8`, `max_steps=500`, `save_every=250`, batch size `12`, gradient accumulation `4`, ZeRO-1, bf16, no SpecAugment, no gradient checkpointing, no teacher cache, and no direction dropout. Gate on the Stage30/37 selector first; public-200 guard is allowed only for a checkpoint that does not worsen overall WER/CER and does not worsen AISHELL, CommonVoice EN, CommonVoice CN, or WenetSpeech versus Stage21.
					- 2026-06-21 Stage61 preparation result: added `scripts/build_stage61_difficulty_public_wenet_replay.py` and generated `/media/usbhd/training_data/asr/curriculum/stage22_stage21_soup_clean_repair_mix/stages/stage61_difficulty_public_wenet_replay/webdataset_lengths.jsonl` plus `webdataset_buckets_audio_text/manifest.json`. The index has exactly `200,000` train rows, `95,471` unique utterance ids, and no teacher override cache. Component counts are Stage43 difficulty CV/Giga `70k`, Stage47 Wenet original anchor `40k`, and Stage59 clean/public guard `90k`. Source counts match the plan: AISHELL3 `35k`, LibriSpeech `35k`, CommonVoice EN `40k`, CommonVoice CN `30k`, GigaSpeech `20k`, and WenetSpeech `40k`; `256` Stage30 selector ids were excluded. Generated config `configs/generated/sensevoice_rwkv_stage61_difficulty_public_wenet_replay_ctc_from_stage21_soup_4x4090_deepspeed.yaml` parses with Stage21 init, LR `2.5e-8`, `max_steps=500`, `save_every=250`, batch size `12`, gradient accumulation `4`, ZeRO-1, bf16, CTC-only loss, no SpecAugment, no gradient checkpointing, and direction dropout disabled.
					- 2026-06-21 Stage61 result: trained `/media/usbhd/rwkvasr_runs/sensevoice_rwkv_stage61_difficulty_public_wenet_replay_from_stage21_soup_bs12_lr2p5e8_zero1_nockpt_noaug_nodirdrop_4x4090` from Stage21 to `step-500`, saving `step-250.pt` and `step-500.pt`; training exited normally with epoch train loss `1.7917`, final rank-0 loss `0.9225`, CTC-only loss, no SpecAugment, no gradient checkpointing, and direction dropout disabled. Fixed Stage30/37 selector summaries are under `/media/usbhd/rwkvasr_runs/stage61_difficulty_public_wenet_eval_20260621/stage61_selector256_b8_k16_summary.json`: `step-250` improved overall `27.512 / 18.044 -> 27.463 / 17.808`, LibriSpeech, GigaSpeech, and WenetSpeech, and kept CommonVoice EN WER flat, but worsened AISHELL (`27.379 -> 28.350`) and CommonVoice CN (`31.358 -> 31.975`). `step-500` measured `27.587 / 17.922` and worsened overall WER plus AISHELL, CommonVoice EN, and CommonVoice CN. Strict selector gate therefore failed for both raw checkpoints, so do not run public-200 or full public eval from Stage61 raw checkpoints.
					- 2026-06-21 Stage62 plan: run an inference-only Stage21/Stage61 checkpoint interpolation screen because Stage61 `step-250` contains useful overall-CER, GigaSpeech, and WenetSpeech movement but overshoots Chinese selector guards. Generate small-weight soups under `/media/usbhd/rwkvasr_runs/stage62_stage21_stage61_interpolation_soups_20260621` using Stage21 `wercer_best.pt` as anchor and Stage61 `step-250.pt` / `step-500.pt` as movement components, starting with Stage61 weights `0.03125`, `0.0625`, `0.125`, `0.25`, and `0.50` for each movement checkpoint. This is checkpoint averaging only; keep the BiRWKV/RWKV-7 TimeMixer encoder, SenseVoice frontend, tokenizer, CTC head, CTC-only objective, training labels, direction policy, and `beam8/topk16` decode policy unchanged. Gate first on the Stage30/37 selector; a Stage62 candidate may receive public-200 guard only if it does not worsen overall WER/CER and does not worsen AISHELL, CommonVoice EN, CommonVoice CN, or WenetSpeech WER/CER versus Stage21.
					- 2026-06-21 Stage62 result: generated ten Stage21/Stage61 interpolation soups under `/media/usbhd/rwkvasr_runs/stage62_stage21_stage61_interpolation_soups_20260621` and evaluated all on the fixed Stage30/37 selector with `beam8/topk16`. The prediction JSONL clean-source ids used WebDataset tar keys with source prefixes, so the summary maps sources by `utt_id` when possible and otherwise by selector row order; this source mapping policy is recorded in `stage62_selector256_b8_k16_summary.json`, with `192` order-fallback rows per candidate. No candidate passed the strict selector gate. The best overall-WER candidate, `stage62_s21_0p9375_s61s250_0p0625`, tied Stage21 WER and improved CER (`27.512 / 18.044 -> 27.512 / 17.889`) with WenetSpeech improving to `36.567`, but AISHELL regressed to `28.350` and CommonVoice CN to `32.222`. Other weights either worsened overall WER or CommonVoice EN/CN. Do not run public-200 or full public eval from Stage62; the Stage61 movement direction itself is incompatible with the Chinese selector guard.
					- 2026-06-21 Stage63 plan: switch away from further Stage61 interpolation and run a CTC-head-only calibration from the Stage21 incumbent, freezing the full SenseVoice/BiRWKV encoder so RWKV-7 TimeMixer and BiRWKV weights do not move. Reuse the Stage59 compact original-label public/CV/Wenet guard index (`200k` rows: AISHELL3 `52k`, CommonVoice EN `47k`, LibriSpeech `43k`, CommonVoice CN `28k`, WenetSpeech `25k`, GigaSpeech `5k`) because Stage59 had useful public-200 macro/Wenet/AISHELL signal but full-model training hurt train-selector guards. Use `freeze_encoder=true`, `freeze_ctc_head=false`, CTC-only labels, no teacher cache, no SpecAugment, no gradient checkpointing, no direction dropout, ZeRO-1, batch size `12`, grad accumulation `4`, a conservative head-only LR around `1e-7`, `max_steps=500`, and `save_every=250`. Gate first on the Stage30/37 selector, then run the fixed public-200 guard only for checkpoints that do not worsen overall WER/CER and do not worsen AISHELL, CommonVoice EN, CommonVoice CN, or WenetSpeech versus Stage21.
					- 2026-06-21 Stage63 result: trained `/media/usbhd/rwkvasr_runs/sensevoice_rwkv_stage63_headonly_cv_public_guard_from_stage21_soup_bs12_lr1e7_zero1_nockpt_noaug_nodirdrop_4x4090` from Stage21 with `freeze_encoder=true` and only `31,044,708` CTC-head parameters trainable. The run completed normally to `step-500`, saved `step-250.pt` and `step-500.pt`, exited status `0`, used CTC-only loss, no SpecAugment, no gradient checkpointing, no direction dropout, and ended with epoch train loss `1.8936`. Fixed Stage30/37 selector summaries are under `/media/usbhd/rwkvasr_runs/stage63_headonly_cv_public_guard_eval_20260621/stage63_selector256_b8_k16_summary.json`: both `step-250` and `step-500` measured `27.587 / 17.889` versus Stage21 `27.512 / 18.044`. They improve LibriSpeech CER, GigaSpeech, and WenetSpeech (`37.687 -> 36.194`) but worsen overall WER, AISHELL (`27.379 -> 28.544`), CommonVoice EN (`38.889/27.031 -> 39.372/27.389`), and CommonVoice CN (`31.358 -> 32.099`). Strict selector gate failed, so do not run public-200 or full public eval from Stage63.
					- 2026-06-21 Stage64 plan: create a clean/public guard counter-direction from Stage21 rather than another Wenet/Giga/CER-biased replay. Reuse the existing Stage18 public-weak clean replay index under `/media/usbhd/training_data/asr/curriculum/clean_ctc_voxbox_webdataset/stages/stage18_publicweak_clean_replay`, which contains only clean/public original-label rows weighted toward current guard weaknesses (CommonVoice EN, AISHELL, CommonVoice CN, plus LibriSpeech) and no hard GigaSpeech/WenetSpeech rows. Train CTC-head-only from Stage21 (`freeze_encoder=true`, `freeze_ctc_head=false`) with a smaller LR around `5e-8`, `max_steps=500`, `save_every=250`, CTC-only loss, no teacher cache, no SpecAugment, no gradient checkpointing, and no direction dropout. Stage64 raw checkpoints are not promotion candidates if Wenet/Giga regress; their purpose is to test whether a clean guard direction can repair AISHELL/CommonVoice slices. Evaluate on the fixed Stage30/37 selector first, and only if it improves AISHELL/CV-EN/CV-CN without catastrophic overall loss should it be combined with the Stage63 Wenet/Giga direction in a later inference-only soup screen.
					- 2026-06-21 Stage64 result: trained `/media/usbhd/rwkvasr_runs/sensevoice_rwkv_stage64_headonly_clean_public_guard_from_stage21_soup_bs12_lr5e8_zero1_nockpt_noaug_nodirdrop_4x4090` from Stage21 on the Stage18 clean/public replay with `freeze_encoder=true`, CTC-only loss, no SpecAugment, no gradient checkpointing, and no direction dropout. Startup confirmed Stage21 weights loaded with `missing=0` / `unexpected=0` and only `31,044,708` CTC-head parameters trainable. The run reached `step=500`, but `/media/usbhd` filled during the final `step-500` DeepSpeed export, leaving only the complete top-level `step-250.pt`; the incomplete `ds_checkpoints/step-500` was removed. To recover working space, DeepSpeed resume-only directories for non-promoted Stage61, Stage63, and Stage64 were removed while preserving top-level `step-*.pt`, configs, logs, and eval summaries. Fixed Stage30/37 selector results are under `/media/usbhd/rwkvasr_runs/stage64_headonly_clean_public_guard_eval_20260621/stage64_selector256_b8_k16_summary.json`: `step-250` measured overall `27.562 / 17.881` versus Stage21 `27.512 / 18.044`. It improves GigaSpeech (`29.505/18.864 -> 27.703/17.778`) and WenetSpeech (`37.687 -> 36.194`) and improves LibriSpeech CER, but it still worsens the clean guard slices AISHELL (`27.379 -> 28.544`), CommonVoice EN (`38.889/27.031 -> 39.372/27.389`), and CommonVoice CN (`31.358 -> 32.099`). Strict selector gate failed, so do not run public-200 or full public eval from Stage64. Since even clean/public head-only calibration moved in the same Wenet/Giga/CER direction as Stage63 rather than repairing AISHELL/CV, do not spend compute reconstructing Stage64 `step-500` unless a later analysis needs it.
					- 2026-06-21 Stage65 plan: run an inference-only small-weight Stage21/Stage64 soup screen before any new training. Stage64 `step-250` has useful GigaSpeech/WenetSpeech/CER movement but still worsens the clean selector guards, so generate soups under `/media/usbhd/rwkvasr_runs/stage65_stage21_stage64_interpolation_soups_20260621` using Stage21 `wercer_best.pt` as anchor and Stage64 `step-250.pt` as the movement component with Stage64 weights `0.03125`, `0.0625`, `0.125`, `0.25`, and `0.50`. This is checkpoint averaging only; keep the BiRWKV/RWKV-7 TimeMixer encoder, SenseVoice frontend, tokenizer, CTC head, CTC-only objective, direction policy, and `beam8/topk16` decode policy unchanged. Gate first on the fixed Stage30/37 selector; only a candidate that does not worsen overall WER/CER and does not worsen AISHELL, CommonVoice EN, CommonVoice CN, or WenetSpeech versus Stage21 may receive public-200 guard. If Stage65 also fails, stop head-only/soup exploration and design a new same-architecture data or objective guard that directly addresses the AISHELL/CommonVoice regressions.
					- 2026-06-21 Stage65 result: generated five Stage21/Stage64 `step-250` soups with Stage64 weights `0.03125`, `0.0625`, `0.125`, `0.25`, and `0.50`, then evaluated all on the fixed Stage30/37 selector with `mode=bi`, `beam8`, `topk16`, and CTC metric normalization. Summary files are `/media/usbhd/rwkvasr_runs/stage65_stage21_stage64_interpolation_soups_20260621/stage65_selector256_b8_k16_summary.json` and `.csv`. No candidate passed. All candidates had the same selector metrics as Stage64 `step-250`: overall `27.562 / 17.881` versus Stage21 `27.512 / 18.044`, with GigaSpeech and WenetSpeech improved but AISHELL, CommonVoice EN, and CommonVoice CN worsened. This means the Stage64 movement is already enough to trigger the same discrete CTC beam outputs at very small soup weights, and it does not provide a clean-guard repair direction. Do not run public-200 or full public eval from Stage65. Remove the failed Stage65 soup `.pt` files after preserving `.soup.json`, prediction JSONL, previews, logs, and summaries to prevent `/media/usbhd` from filling again.
					- 2026-06-21 Stage66 plan: before starting another training run, run a small inference-only compensated soup screen that combines the three historically different same-architecture movement directions: Stage27 `step-1000.pt` for the Libri/AISHELL head-calibration direction, Stage40 `step-500.pt` for the CommonVoice-EN/Giga teacher-repair direction, and Stage64 `step-250.pt` for the Wenet/Giga/CER head-calibration direction. Keep Stage21 `wercer_best.pt` as the anchor and preserve the exact BiRWKV/RWKV-7 TimeMixer architecture, SenseVoice frontend, tokenizer, CTC head, CTC-only objective, direction policy, and Stage37 `beam8/topk16` decode policy. Because `/media/usbhd` is close to full, generate candidates one at a time under `/media/usbhd/rwkvasr_runs/stage66_stage27_stage40_stage64_compensated_soups_20260621`, decode the fixed Stage30/37 selector, then delete the temporary `.pt` while keeping `.soup.json`, prediction JSONL, previews, logs, and summaries. Start with only small weights (`0.03125` and `0.0625` per movement component, total movement no more than `0.1875`) so this tests compensation rather than a large head/teacher shift. Gate remains strict: a candidate may receive public-200 guard only if it improves or ties Stage21 overall WER/CER and does not worsen AISHELL, CommonVoice EN, CommonVoice CN, or WenetSpeech versus the Stage37 baseline.
					- 2026-06-21 Stage66 result: generated and evaluated eight Stage21/Stage27/Stage40/Stage64 compensated soups under `/media/usbhd/rwkvasr_runs/stage66_stage27_stage40_stage64_compensated_soups_20260621`, using the fixed Stage30/37 selector, `mode=bi`, `beam8/topk16`, and CTC normalization. The first attempt used the incomplete clean-only WebDataset root and failed at the first GigaSpeech shard; the completed run used `/media/usbhd/training_data/asr/curriculum/stage22_stage21_soup_clean_repair_mix`. All temporary `.pt` files were deleted after decode; the run directory is only about `6.6M`. Summary files are `stage66_selector256_b8_k16_summary.json` and `.csv`. No candidate passed. Best WER was `stage66_s27w0p0625_s40w0p03125_s64w0p03125` at `27.910 / 18.077`, still `+0.398 / +0.033` worse than Stage21. Best CER was `stage66_s27w0p0625_s40w0p0625_s64w0p03125` at `27.935 / 18.028`, a tiny CER gain but `+0.423` WER worse. All candidates improved WenetSpeech and GigaSpeech slightly, but worsened AISHELL (`+0.777` to `+0.971` WER), CommonVoice EN (`+1.449` to `+1.691` WER), and CommonVoice CN (`+0.988` to `+1.235` WER). Do not run public-200 or full public eval from Stage66, and stop this compensated-soup line.
					- 2026-06-21 Stage67 plan: run an evaluation-only decode audit for the closest trained checkpoint, Stage61 `step-250.pt`, before starting another GPU training job. Stage61 `step-250` already improved fixed-selector overall WER/CER (`27.512 / 18.044 -> 27.463 / 17.808`) and improved LibriSpeech, GigaSpeech, WenetSpeech, and CommonVoice EN CER, while the remaining blockers were AISHELL and CommonVoice CN. Keep the same checkpoint, BiRWKV/RWKV-7 TimeMixer architecture, tokenizer, CTC head, labels, and `mode=bi`; vary only CTC prefix-beam decode knobs around the Stage37 baseline (`beam_size`, `token_prune_topk`, small length bonus, and small insertion bonus) on the fixed Stage30/37 selector. Promote to public-200 guard only if a decode setting improves or ties Stage21 overall WER/CER and does not worsen AISHELL, CommonVoice EN, CommonVoice CN, or WenetSpeech versus the Stage37 baseline. If decode cannot repair the Chinese guard, the next training stage should use a new same-architecture objective such as baseline-logit anchoring / conservative distillation rather than more replay weighting or soup search.
					- 2026-06-21 Stage67 result: direct current-code reevaluation exposed an eval-baseline drift that must be recorded before promotion decisions. The historical Stage30/37 and Stage61 sidecar numbers were produced through an older/sidecar evaluation path; rerunning Stage61 `step-250.pt` directly with the current `cuda_clampw` prediction path produced `27.960 / 18.069` for `beam8/topk16/lb0/ib0`, not the old `27.463 / 17.808`. Trying a `backend: native` override also did not reproduce the old output (`27.985 / 18.077`), so treat older selector numbers as legacy references rather than current-code promotion baselines. Rebuilt the current-code Stage21 fused selector baseline under `/media/usbhd/rwkvasr_runs/stage68_current_code_baseline_audit_20260621`: Stage21 `beam8/topk16/lb0/ib0` now measures `28.010 / 18.109`, and Stage21 with `lb=+0.001` measures `28.060 / 18.117`. Stage61 `step-250.pt` under the same current-code fused path improves Stage21: `lb0` gives `27.960 / 18.069`, and `lb=+0.001` gives `27.960 / 18.044`. Same-decode comparison for `beam8/topk16/lb=+0.001/ib0` is strict-selector-pass: Stage61 improves overall by `-0.100 / -0.073`, improves AISHELL by `-0.388 / -0.388`, ties CommonVoice EN WER while improving CER by `-0.204`, ties CommonVoice CN and WenetSpeech, and slightly improves LibriSpeech/GigaSpeech CER. Use Stage61 `step-250.pt` with `beam8/topk16/lb=+0.001/ib0` as a current-code public-200 candidate, but do not claim it beats the legacy Stage37 baseline without a full legacy/current reconciliation.
					- 2026-06-21 Stage68 plan: run a current-code public-200 guard for Stage61 `step-250.pt` versus Stage21 using the exact same decode policy selected by Stage67: `mode=bi`, `beam8`, `topk16`, `length_bonus=+0.001`, `insertion_bonus=0.0`, prediction normalization `ctc`, and metric normalization `ctc`. Use the fixed public-200 manifests at `artifacts/eval_benchmarks/beam_ablation_20260620/manifests_200_seed20260620` for AISHELL-1 test, LibriSpeech test-clean/test-other, CommonVoice EN test, and WenetSpeech TEST_NET. This is evaluation-only and does not alter the BiRWKV/RWKV-7 TimeMixer architecture, checkpoint weights, tokenizer, CTC head, or labels. Stage61 may become the current-code incumbent only if it improves or ties Stage21 macro WER/CER and does not worsen CommonVoice EN or WenetSpeech WER/CER under the same current-code decode; otherwise keep Stage21 as the incumbent and move to a new same-architecture training objective.
					- 2026-06-21 Stage68 result: current-code public-200 guard completed under `/media/usbhd/rwkvasr_runs/stage68_current_code_public200_stage61_guard_20260621` with summary files `stage68_public200_lbp001_summary.json` and `.csv`. The run used current checkpoint `cuda_clampw` configs, `mode=bi`, `beam8/topk16`, `length_bonus=+0.001`, `insertion_bonus=0.0`, and CTC normalization for both prediction and metrics. Stage21 current-code baseline measured macro `38.435 / 30.438`; per dataset: AISHELL-1 `30.460 / 30.460`, LibriSpeech clean `22.523 / 13.224`, LibriSpeech other `38.347 / 24.267`, CommonVoice EN `54.132 / 35.550`, and WenetSpeech `46.713 / 48.692`. Stage61 `step-250` measured macro `38.466 / 30.521`, worsening macro by `+0.031 / +0.083`; it improved LibriSpeech clean but worsened AISHELL, LibriSpeech other WER, CommonVoice EN (`+0.157 / +0.253`), and WenetSpeech (`+0.030 / +0.150`). Strict public-200 gate failed, so do not promote Stage61 with `lb=+0.001`.
					- 2026-06-21 Stage69 plan: run one more current-code public-200 guard for Stage61 `step-250.pt` using the more conservative Stage67 selector-pass decode `beam8/topk16/length_bonus=0.0/insertion_bonus=0.0`. Stage61 `lb0` also passed the current-code selector gate versus Stage21 `lb0` (`27.960 / 18.069` versus `28.010 / 18.109`) without relying on positive length bias; this may avoid the small CommonVoice/Wenet public regression seen with `lb=+0.001`. Evaluate Stage21 and Stage61 on the same fixed public-200 manifests and same decode. Promote only if macro WER/CER and CommonVoice EN + WenetSpeech WER/CER are no worse than Stage21 under `lb0`; otherwise stop decode tuning and move to a new training objective.
					- 2026-06-21 Stage69 result: current-code public-200 `lb0` guard completed under `/media/usbhd/rwkvasr_runs/stage69_current_code_public200_stage61_lb0_guard_20260621` with summary files `stage69_public200_lb0_summary.json` and `.csv`. Stage21 current-code `lb0` baseline measured macro `38.431 / 30.443`; per dataset: AISHELL-1 `30.460 / 30.460`, LibriSpeech clean `22.497 / 13.218`, LibriSpeech other `38.377 / 24.288`, CommonVoice EN `54.079 / 35.528`, and WenetSpeech `46.743 / 48.722`. Stage61 `step-250` with the same `lb0` decode measured macro `38.472 / 30.526`, worsening macro by `+0.041 / +0.082`; it improved LibriSpeech clean but worsened AISHELL, LibriSpeech other WER, CommonVoice EN (`+0.209 / +0.275`), and WenetSpeech CER (`+0.120`). Strict public-200 gate failed. Stop decode/bonus tuning for Stage61 and do not promote it. The next optimization step should be a same-architecture training objective that explicitly constrains public guard drift, such as current-Stage21 logit/CTC-distribution anchoring on public guard samples while applying a small amount of hard-data learning signal.
					- 2026-06-21 Stage70 plan: add a training-only encoder hidden anchoring objective as the conservative public-drift constraint for the next full-model probe. Keep the BiRWKV/RWKV-7 TimeMixer architecture, bidirectional wrapper, CTC head, frontend, tokenizer, labels, and inference path unchanged; do not add direction dropout, decoder loss, architecture adapters, or channel-mixing replacement. Load the Stage21 hard-soup checkpoint as a frozen same-architecture teacher and add a masked MSE penalty between the student's encoder hidden sequence and the teacher's encoder hidden sequence on the same batch and direction mask. This is intentionally a memory-safe proxy for full CTC-logit KL because the CTC vocabulary is about `60k` and full teacher logits would substantially increase 4x4090 memory pressure. Start with a 4-GPU smoke run (`max_steps=2`) from Stage21 on the Stage61 difficulty/public/Wenet replay index, `lr=1e-8`, `encoder_anchor_loss_weight=0.05`, CTC-only loss, no SpecAugment, no gradient checkpointing, no direction dropout, ZeRO-1, batch size `12`, grad accumulation `4`. If the smoke verifies memory and logging, run the same recipe to `250` steps with saves at `125` and `250`. Gate with current-code `beam8/topk16/lb0/ib0` selector versus the current Stage21 baseline (`28.010 / 18.109`) before any public-200 guard; public-200 may run only if selector WER/CER and AISHELL/CommonVoice/Wenet guards do not regress under the same current-code decode.
					- 2026-06-21 Stage70 implementation/smoke note: added DeepSpeed training config keys `encoder_anchor_checkpoint_path` and `encoder_anchor_loss_weight`; `joint_losses` now exposes the encoder hidden tensor for training-only auxiliary loss; the DeepSpeed loop loads a frozen same-architecture anchor model, runs its encoder under `torch.no_grad()`, computes masked hidden MSE over valid frames, and logs `train/encoder_anchor_loss`. Also added `--local_rank` as an alias for DeepSpeed launcher compatibility. Verification passed: `py_compile` on the touched modules, config parsing for Stage70 smoke/proper YAMLs, a tensor-unit check of masked MSE, and a 4x4090 2-step smoke under `/media/usbhd/rwkvasr_runs/sensevoice_rwkv_stage70_encoder_anchor_smoke_replay_from_stage21_soup_bs12_lr1e8_anchor0p05_zero1_nockpt_noaug_nodirdrop_4x4090`. Smoke loaded Stage21 student and anchor with `missing=0` / `unexpected=0`, logged `anchor=0.8387` then `0.7969`, reached peak reserved memory about `14.26GiB`, and exited successfully at `step=2`. The smoke `.pt` and `ds_checkpoints` were removed afterward to recover disk space; text configs/metrics were kept.
					- 2026-06-21 Stage70 result: trained `/media/usbhd/rwkvasr_runs/sensevoice_rwkv_stage70_encoder_anchor_replay_from_stage21_soup_bs12_lr1e8_anchor0p05_zero1_nockpt_noaug_nodirdrop_4x4090` to `step=250` from the Stage21 hard soup with full-model CTC-only training, `lr=1e-8`, frozen Stage21 encoder-hidden anchor weight `0.05`, no SpecAugment, no gradient checkpointing, and no direction dropout. The run completed normally with final train loss `1.8049`; `step-125.pt` and `step-250.pt` are preserved, and resume-only `ds_checkpoints` were removed after training to recover disk. Current-code selector summary is `/media/usbhd/rwkvasr_runs/stage70_encoder_anchor_selector_eval_20260621/stage70_selector256_b8_k16_lb0_summary.json`: `step-125` measured `27.488 / 18.003` but failed strict selector because WenetSpeech worsened by `+0.373 / +0.373`; `step-250` measured `27.512 / 18.003` versus Stage21 current `28.010 / 18.109` and passed strict selector, improving AISHELL (`-0.971 / -0.971`), CommonVoice EN (`-2.415 / -1.175`), CommonVoice CN (`-0.864 / -0.864`), and tying WenetSpeech. Public-200 `lb0` guard for `step-250` is `/media/usbhd/rwkvasr_runs/stage70_current_code_public200_encoder_anchor_lb0_guard_20260621/stage70_public200_lb0_summary.json`: macro improved from Stage21 `38.431 / 30.443` to `37.678 / 30.359`, with AISHELL, LibriSpeech other, and CommonVoice EN improved, but WenetSpeech worsened from `46.743 / 48.722` to `47.949 / 50.045` (`+1.206 / +1.323`). Strict public-200 gate failed, so do not promote raw Stage70 `step-250` despite the macro gain.
					- 2026-06-21 Stage71 plan: run an inference-only Stage21/Stage70 interpolation screen to test whether a smaller fraction of the Stage70 encoder-anchor movement preserves the public macro/AISHELL/CommonVoice gains while recovering WenetSpeech. Keep the exact BiRWKV/RWKV-7 TimeMixer architecture, SenseVoice frontend, tokenizer, CTC head, labels, direction policy, and `beam8/topk16/lb0/ib0` decode unchanged. Generate soup candidates one at a time under `/media/usbhd/rwkvasr_runs/stage71_stage21_stage70_encoder_anchor_soups_20260621`, using Stage21 `wercer_best.pt` as anchor and Stage70 `step-250.pt` as the movement checkpoint with Stage70 weights `0.125`, `0.25`, `0.50`, and `0.75`; delete each temporary `.pt` after decoding if it fails to save disk. Gate first on the current-code selector versus Stage21 current, then run public-200 only for candidates that pass selector and do not worsen WenetSpeech on the fixed selector. Because Stage70 raw already improves public macro but fails Wenet, prioritize public WenetSpeech and CommonVoice EN guard before considering full public-200 reruns.
					- 2026-06-21 Stage71 result: screened Stage21/Stage70 `step-250` soups with Stage70 weights `0.125`, `0.25`, `0.50`, and `0.75`, generating each `.pt` one at a time and deleting temporary checkpoints after selector decode. Selector summary is `/media/usbhd/rwkvasr_runs/stage71_stage21_stage70_encoder_anchor_soups_20260621/stage71_selector256_b8_k16_lb0_summary.json`. Only `w=0.125` passed strict selector: overall `27.662 / 18.069` versus Stage21 current `28.010 / 18.109`, with AISHELL `-0.971 / -0.971`, CommonVoice EN `-1.449 / -0.766`, CommonVoice CN `-0.864 / -0.864`, and WenetSpeech tied. Larger weights failed due WenetSpeech selector regressions. The `w=0.125` public guard was limited to CommonVoice EN and WenetSpeech under `/media/usbhd/rwkvasr_runs/stage71_stage21_stage70_encoder_anchor_soups_20260621/public200_stage71_s70w0p125_lb0`: CommonVoice EN improved from Stage21 `54.079 / 35.528` to `51.778 / 35.066`, but WenetSpeech worsened from `46.743 / 48.722` to `47.799 / 49.895` (`+1.055 / +1.173`). Do not run full public-200 or promote Stage71 `w=0.125`; the temporary `.pt` was removed, keeping only metadata and predictions.
					- 2026-06-21 Stage72 plan: extend the Stage21/Stage70 interpolation screen to very small Stage70 weights `0.03125` and `0.0625`, because `w=0.125` still carries too much public WenetSpeech drift while improving CommonVoice EN. This remains inference-only checkpoint averaging with no BiRWKV/RWKV-7 TimeMixer, frontend, tokenizer, CTC head, label, or decode-policy change. Gate on the current-code selector first; if a candidate ties WenetSpeech selector and improves overall WER/CER, run only CommonVoice EN and WenetSpeech public guards before any full public-200. Delete temporary `.pt` files after decoding unless a candidate passes both public guards.
					- 2026-06-21 Stage72 result: tiny Stage21/Stage70 soups under `/media/usbhd/rwkvasr_runs/stage72_stage21_stage70_tiny_encoder_anchor_soups_20260621` both passed the current-code selector. `w=0.03125` measured `27.512 / 18.044`, and `w=0.0625` measured `27.512 / 18.036`, both tying WenetSpeech selector and improving AISHELL, CommonVoice EN, and CommonVoice CN versus Stage21 current. Public CommonVoice/Wenet guard still failed for both: `w=0.03125` improved CommonVoice EN to `51.831 / 35.099` but worsened WenetSpeech to `47.919 / 50.135`; `w=0.0625` improved CommonVoice EN to `51.726 / 35.032` but worsened WenetSpeech to `47.859 / 49.955`. Do not run full public-200 or promote Stage72. Since even `3.125%` Stage70 movement hurts public WenetSpeech while the selector stays tied, stop interpolation and add a direct training-time output-distribution constraint rather than further soup shrinking.
					- 2026-06-21 Stage73 plan: add a training-only CTC logit anchoring objective on top of the Stage70 encoder-hidden anchor. Keep the BiRWKV/RWKV-7 TimeMixer architecture, frontend, tokenizer, CTC head, labels, direction policy, and inference path unchanged. Reuse the frozen Stage21 hard-soup checkpoint as the same-architecture teacher; compute the student CTC logits from the existing forward pass and compute teacher CTC logits from the teacher encoded states in small time chunks, applying masked KL over valid frames so full `B x T x 60k` teacher logits do not remain resident. Start from Stage21 again on the Stage61 difficulty/public/Wenet replay index with `encoder_anchor_loss_weight=0.05`, `ctc_logit_anchor_loss_weight=0.2`, `ctc_logit_anchor_chunk_frames=16`, `lr=1e-8`, CTC-only loss, no SpecAugment, no gradient checkpointing, no direction dropout, ZeRO-1, batch size `12`, grad accumulation `4`. First run a 4-GPU `max_steps=2` smoke to verify memory; if it stays under 4090 memory, run `250` steps and gate exactly as Stage70: current-code selector first, then public CommonVoice/Wenet guards, then full public-200 only if both guards pass.
					- 2026-06-21 Stage73 implementation note: added DeepSpeed config/CLI keys `ctc_logit_anchor_loss_weight` and `ctc_logit_anchor_chunk_frames`. The DeepSpeed loop now reuses the frozen anchor teacher for either encoder-hidden MSE or CTC-logit KL; the KL helper computes teacher `ctc_head` outputs chunk-by-chunk under `torch.no_grad()`, applies `softmax`/`log_softmax` in fp32, masks padded frames, and logs `train/ctc_logit_anchor_loss`. Verification passed for `py_compile` on touched modules and a tensor-level KL helper gradient check.
					- 2026-06-21 Stage73 result: 4-GPU smoke passed under `/media/usbhd/rwkvasr_runs/sensevoice_rwkv_stage73_hidden_ctc_anchor_smoke_replay_from_stage21_soup_bs12_lr1e8_hid0p05_kl0p2_zero1_nockpt_noaug_nodirdrop_4x4090`, with peak reserved memory about `15.05GiB` and logged `ctc_anchor` loss. The full run `/media/usbhd/rwkvasr_runs/sensevoice_rwkv_stage73_hidden_ctc_anchor_replay_from_stage21_soup_bs12_lr1e8_hid0p05_kl0p2_zero1_nockpt_noaug_nodirdrop_4x4090` completed to `step=250`; to avoid disk-fill failures it saved only the final step, and resume-only `ds_checkpoints` were removed after preserving `step-250.pt`. Current-code selector summary is `/media/usbhd/rwkvasr_runs/stage73_hidden_ctc_anchor_selector_eval_20260621/stage73_selector256_b8_k16_lb0_summary.json`: Stage73 measured `27.612 / 18.134` versus Stage21 current `28.010 / 18.109`, so WER improved but CER worsened; WenetSpeech selector worsened to `38.060 / 38.060` (`+0.373 / +0.373`). Strict selector failed, so do not run public-200 or promote Stage73. The initial CTC-logit KL anchor implementation is usable, but this weight/recipe over-constrains or redirects the Stage70 movement without solving Wenet; stop this exact training objective.
					- 2026-06-21 Stage74 plan: before another training job, re-audit older same-architecture movement checkpoints under the current-code public Wenet/CommonVoice guard, especially Stage64 `step-250` because it improved Wenet/Giga selector while Stage70 improved public macro/CommonVoice but hurt Wenet. This is evaluation/checkpoint-soup only: keep BiRWKV/RWKV-7 TimeMixer, frontend, tokenizer, CTC head, labels, direction policy, and `beam8/topk16/lb0/ib0` unchanged. First run current-code public CommonVoice EN and WenetSpeech guards for Stage64 `step-250`. If Stage64 improves Wenet public, try a small compensated soup with Stage21 as anchor and tiny Stage70 + Stage64 movement weights; gate on selector and public CommonVoice/Wenet before any full public-200.
					- 2026-06-21 Stage74 result: current-code public CommonVoice/Wenet guard re-audits found no WenetSpeech compensation checkpoint among the likely older directions. Stage64 `step-250` improved CommonVoice EN to `51.831 / 35.099` but worsened WenetSpeech to `47.919 / 50.135`; Stage16 `step-5000` improved CommonVoice EN to `51.621 / 34.845` but worsened WenetSpeech to `47.286 / 49.353`; Stage54 `step-540004` improved CommonVoice EN to `51.883 / 35.132` but worsened WenetSpeech to `47.919 / 49.985`; Stage59 `step-1000` improved CommonVoice EN to `51.621 / 34.900` but worsened WenetSpeech to `47.919 / 50.135`. Baseline for all comparisons was current Stage21 `lb0` public-200 (`CommonVoice EN 54.079 / 35.528`, WenetSpeech 46.743 / 48.722`). Do not build compensated soups from these directions; under current-code decode they share the same CV-improvement/Wenet-regression pattern as Stage70 tiny soups. Next work should construct a Wenet public-proxy guard from non-test WenetSpeech data and optimize against that explicit guard, because the fixed train selector no longer predicts public Wenet drift.
					- 2026-06-21 Stage75 plan: build a Wenet public-proxy selector from non-test WenetSpeech shards, excluding existing public test ids and previous fixed selector ids, and use it as a first-class gate before public-200. Sample enough WenetSpeech train/dev-style rows to match TEST_NET duration/text-length characteristics, decode Stage21 and the strongest CV-improving candidates on this proxy, and keep only proxy rows whose Stage21 errors correlate with public Wenet failure modes. Then run a same-architecture training probe from Stage21 that mixes the Stage70/Stage59 CV-improving data with the new Wenet proxy guard, using either low-LR full-model CTC with encoder hidden anchoring or a source-aware anchor schedule if implemented. No BiRWKV/RWKV-7 TimeMixer architecture changes are allowed.
					- 2026-06-21 Stage75 implementation plan: use the raw WenetSpeech L WebDataset length index at `/media/usbhd/training_data/asr/wenet-e2e/wenetspeech/webdataset_l_train/webdataset_lengths.jsonl`, not the Stage61 replay Wenet subset, because the raw pool has about `14.62M` Wenet rows (`14.51M` train and `115k` eval) while Stage61's Wenet anchor repeats only about `1k` unique ids. Build `/media/usbhd/rwkvasr_runs/stage75_wenet_public_proxy_20260621/stage75_wenet_public_proxy512.lengths.jsonl` with `512` rows, excluding ids from `artifacts/eval_benchmarks/beam_ablation_20260620/manifests_200_seed20260620/wenetspeech_test_net.jsonl` and `/media/usbhd/rwkvasr_runs/stage37_ctc_decode_audit_20260620/stage30_selector256.lengths.jsonl`. Match the fixed public WenetSpeech TEST_NET 200-sample manifest by two axes: approximate duration frames (`duration_sec * 100`, using audio file metadata for public rows and `num_frames` for WebDataset rows) and normalized text-character bins. Because the length index does not store text, first reservoir-sample enough candidates by frame bin from the raw Wenet index, then read candidate JSON payloads via their tar offsets to obtain `text` and select rows by the public frame/text-bin target distribution. This is data selection only; it must not change BiRWKV/RWKV-7 TimeMixer, inference, CTC labels, or decode policy.
					- 2026-06-21 Stage75 first proxy result: generated `/media/usbhd/rwkvasr_runs/stage75_wenet_public_proxy_20260621/stage75_wenet_public_proxy512.lengths.jsonl` from the full raw WenetSpeech L pool with `512` rows, `512` unique ids, and zero fallback selections. However, current-code `beam8/topk16/lb0` decoding showed it is not a valid public-Wenet drift guard: Stage21 measured `44.256 / 44.543`, Stage70 `step-250` measured `44.231 / 44.518` (`-0.025 / -0.025`), and Stage59 `step-1000` measured `44.156 / 44.443` (`-0.100 / -0.100`), while both Stage70 and Stage59 are known to worsen WenetSpeech public-200 under the same decode. Only `3/512` rows were worsened by both Stage70 and Stage59 on CER. Do not use this train+eval duration/text proxy as a training or promotion gate. Next Stage75 substep is to build an eval-split-only Wenet public proxy, still matching TEST_NET duration/text bins and excluding public/selector ids, because the raw train split appears too easy or distribution-shifted relative to TEST_NET.
					- 2026-06-21 Stage75 eval-proxy result: extended `scripts/build_stage75_wenet_public_proxy.py` with `--allowed-split` and generated `/media/usbhd/rwkvasr_runs/stage75_wenet_public_proxy_20260621/stage75_wenet_eval_public_proxy512.lengths.jsonl` from raw WenetSpeech `split=eval` only. The selector has `512` rows, `512` unique ids, zero fallback selections, and the same TEST_NET duration/text-bin target distribution. Current-code `beam8/topk16/lb0` validation under `/media/usbhd/rwkvasr_runs/stage75_wenet_public_proxy_eval_20260621/stage75_proxy_drift_validation_summary.json` showed this eval-only proxy is useful: Stage21 measured `43.857 / 43.993`; Stage70 `step-250` worsened to `43.978 / 44.138` (`+0.121 / +0.145`, CER worse on `23` samples and better on `15`); Stage59 `step-1000` worsened to `43.954 / 44.077` (`+0.097 / +0.085`, CER worse on `19` samples and better on `13`). Use this eval-only proxy as the Wenet guard before public-200, and keep it out of training data for the next probe.
					- 2026-06-21 Stage76 plan: run a same-architecture Wenet-eval-anchor training probe from the Stage21 hard soup. Do not change BiRWKV/RWKV-7 TimeMixer, bidirectional wrapper, SenseVoice frontend, tokenizer, CTC head, direction policy, or decode policy. Build a Stage76 replay length index that reuses the Stage61 difficulty/public/Wenet replay signal but adds repeated raw WenetSpeech `split=eval` original-label anchors sampled from rows excluded from the Stage75 eval proxy and public TEST_NET ids. Keep the Stage75 eval proxy as a validation-only Wenet guard. Train with very low LR (`1e-8` to `2e-8`), CTC-only labels, no SpecAugment, no direction dropout, ZeRO-1, no gradient checkpointing, and frozen Stage21 encoder-hidden anchor weight around `0.05`; avoid the Stage73 CTC-logit KL weight for the first probe because it worsened selector CER. Gate order: fixed Stage30/37 selector, then Stage75 eval-Wenet proxy, then current-code public CommonVoice/Wenet-200, then full public-200 only if all guards pass.
					- 2026-06-21 Stage76A implementation adjustment: the current WebDataset training loader resolves all `shard_name` entries under one `webdataset_root`, so a single length index cannot safely mix Stage61 curriculum shards and raw WenetSpeech eval shards without a broader multi-root data-loader change. To keep the slice narrow and preserve the BiRWKV architecture, run the first probe as a Wenet-only recovery calibration from the CV-improving Stage70 `step-250` checkpoint: build a raw WenetSpeech `split=eval` original-label anchor index excluding Stage75 eval-proxy ids, the failed train+eval proxy ids, public TEST_NET ids, and the fixed selector ids; train on that single root at a very low LR (`5e-9` to `1e-8`) with the frozen Stage21 encoder-hidden anchor still enabled. This tests whether public-Wenet drift can be recovered from Stage70 without changing architecture or adding a multi-root loader. Gate on Stage75 eval proxy first, then Stage30/37 selector and public CommonVoice/Wenet-200.
					- 2026-06-21 Stage76A preparation/smoke: generated `/media/usbhd/training_data/asr/wenet-e2e/wenetspeech/webdataset_l_train/stages/stage76_wenet_eval_anchor20k/webdataset_lengths.jsonl` with `20,000` raw WenetSpeech `split=eval` original-label anchor rows, excluding public TEST_NET, fixed selector, Stage75 eval-proxy, and failed Stage75 train+eval proxy ids. The index has `20,000` unique ids; `693` rows are fallback selections because eval split has insufficient long-duration/long-text candidates for `501_800|041_plus` and `801_plus|041_plus`. Bucket manifest `/media/usbhd/training_data/asr/wenet-e2e/wenetspeech/webdataset_l_train/stages/stage76_wenet_eval_anchor20k/webdataset_buckets_audio_text/manifest.json` has `20,000` samples under split `eval`, so Stage76A training configs explicitly use `webdataset_split=eval`. Loader batch smoke produced SenseVoice LFR features and valid CTC `targets/target_lengths`. 4x4090 DeepSpeed smoke from Stage70 `step-250` completed `2` steps with Stage21 hidden anchor enabled, no CTC-logit KL, no SpecAugment, and no direction dropout; first-step peak reserved memory was about `4.53GiB`. Smoke model/checkpoint artifacts were deleted after verification to recover disk.
					- 2026-06-21 Stage76A result: trained `/media/usbhd/rwkvasr_runs/sensevoice_rwkv_stage76a_wenet_eval_anchor_from_stage70_step250_bs12_lr5e9_anchor0p05_zero1_nockpt_noaug_nodirdrop_4x4090` to `step=250` from Stage70 `step-250` using only raw WenetSpeech eval-anchor original labels, LR `5e-9`, Stage21 encoder-hidden anchor weight `0.05`, no CTC-logit KL, no SpecAugment, no direction dropout, ZeRO-1, and unchanged BiRWKV/RWKV-7 TimeMixer architecture. The run completed normally with epoch train loss about `2.4588`; only `step-250.pt` is retained and DeepSpeed resume/export duplicates were removed. Stage75 eval-Wenet proxy improved substantially versus Stage70: Stage70 was `43.978 / 44.138` (`+0.121 / +0.145` vs Stage21), while Stage76A measured `43.869 / 43.993` (`+0.012 / +0.000` vs Stage21). However, fixed Stage30/37 selector failed because WenetSpeech worsened from Stage21 `37.69 / 37.69` and Stage70 tie to `38.06 / 38.06`, despite overall selector still improving to `27.512 / 18.012`. Do not run public eval for raw Stage76A. Next test only small Stage70→Stage76A interpolation weights to see whether eval-proxy recovery can be kept while restoring selector Wenet tie.
					- 2026-06-21 Stage77 result: screened Stage70/Stage76A interpolation weights `0.50` and `0.75` for the Stage76A component. Both passed the fixed selector Wenet guard: `w=0.50` measured selector overall `27.463 / 18.012` with Wenet `37.69 / 37.69`, and `w=0.75` measured `27.438 / 18.020` with Wenet `37.69 / 37.69`. Eval-Wenet proxy did not fully transfer: `w=0.50` measured `43.881 / 44.041` (`+0.024 / +0.048` vs Stage21), and `w=0.75` measured `43.929 / 44.053` (`+0.073 / +0.060`). Public Wenet-200 guard for `w=0.50` failed, measuring `48.010 / 50.105` versus Stage21 current `46.743 / 48.722` and Stage70 `47.949 / 50.045`. This is worse than Stage70 on public Wenet despite better eval-proxy metrics. Delete temporary Stage77 soup checkpoints and do not run public CommonVoice/full public-200 for this line.
					- 2026-06-21 Stage78 plan: test whether raw WenetSpeech eval anchors can produce a public-Wenet-improving direction when starting from Stage21 rather than trying to repair Stage70. Reuse the Stage76A `20k` raw Wenet eval-anchor index but initialize from Stage21 hard soup, use full-model CTC-only training at very low LR around `5e-9`, no SpecAugment, no direction dropout, no CTC-logit KL, and no architecture changes. For the first probe disable the encoder-hidden anchor so the run can reveal the actual Wenet-eval direction; if it improves public Wenet, use it only as a compensation direction in soups with Stage70. Gate first on fixed selector and public Wenet-200; stop the raw Wenet eval-anchor line if it worsens public Wenet from Stage21.
					- 2026-06-21 Stage78A result: trained `/media/usbhd/rwkvasr_runs/sensevoice_rwkv_stage78a_wenet_eval_anchor_from_stage21_soup_bs12_lr5e9_noanchor_zero1_nockpt_noaug_nodirdrop_4x4090` to `step=250` from Stage21 on the same `20k` raw Wenet eval-anchor index, LR `5e-9`, no hidden anchor, no CTC-logit KL, no SpecAugment, no direction dropout, and unchanged BiRWKV/RWKV-7 TimeMixer architecture. The run completed normally with epoch train loss about `2.4191`; only `step-250.pt` is retained. Public Wenet-200 guard failed: Stage78A measured `47.919 / 50.105` versus Stage21 current `46.743 / 48.722` and Stage70 `47.949 / 50.045`. Therefore raw Wenet eval-anchor training is not a public-Wenet compensation direction, whether started from Stage70 or Stage21. Additional audit confirmed zero canonical-id overlap between public TEST_NET and the Stage75 train+eval proxy, Stage75 eval-only proxy, and Stage76/78 `20k` train anchor after stripping `TEST_NET_` / `wenetspeech_` prefixes.
					- 2026-06-21 Stage79 result: diagnosed the Stage70 public Wenet regression before another training run. Public Wenet-200 edit decomposition shows Stage70 mostly raises deletions rather than substitutions: Stage21 `CER=48.722`, substitutions `38.256%`, insertions `2.376%`, deletions `8.090%`, pred/ref char ratio `0.9429`; Stage70 `CER=50.045`, substitutions `37.805%`, insertions `2.286%`, deletions `9.955%`, pred/ref char ratio `0.9233`. Stage77 and Stage78A keep the same shortened-output pattern. A decode-only Stage70 CTC prefix-beam length-bonus scan on Wenet public-200 (`mode=bi`, `beam8/topk16`, unchanged architecture/weights/tokenizer/CTC labels) did reduce deletions only at very large bonuses, but it could not beat Stage21: the best Stage70 WER was `46.894 / 50.045` at `length_bonus=1.5`, and the best Stage70 CER was `47.045 / 49.654` at `length_bonus=1.0`, versus Stage21 `46.743 / 48.722`. Larger bonuses traded deletions for substitutions/insertions. Stop Stage70 decode-only repair; the Wenet failure needs a training/data objective that preserves content coverage without changing BiRWKV/RWKV-7 TimeMixer.
					- 2026-06-21 Stage80A result: built a Wenet deletion-drift mined anchor instead of another random Wenet eval-anchor replay. Reused the existing Stage76 `20k` raw WenetSpeech `split=eval` anchor pool, which already excludes public TEST_NET, fixed selector, and Stage75 proxy ids. Full-pool mining decoded Stage21 hard soup and Stage70 `step-250` under identical current-code `beam8/topk16/lb0`; the pool itself showed almost no average drift (`Stage21 46.472 / 46.904`, `Stage70 46.462 / 46.903`) but contained sparse Stage70-drift rows: `550/20000` worse by CER, `165/20000` with higher deletion rate, `189/20000` shorter, and `655` positive drift-score rows. Generated `/media/usbhd/training_data/asr/wenet-e2e/wenetspeech/webdataset_l_train/stages/stage80_wenet_deletion_drift_anchor20k/webdataset_lengths.jsonl` by repeating those `655` unique rows to `20k`, built its bucket manifest, and trained Stage80A from Stage70 `step-250` for `250` steps with LR `5e-9`, Stage21 encoder-hidden anchor `0.05`, no CTC-logit KL, no SpecAugment, no direction dropout, and unchanged BiRWKV/RWKV-7 TimeMixer. Smoke passed at about `4.53GiB` peak reserved; full training completed normally with epoch train loss about `3.0085`, retaining only `step-250.pt`. Evaluation failed the pre-public gates: Stage75 eval-Wenet proxy measured `43.869 / 44.005` (near Stage21 CER but slightly worse than Stage76A), while fixed selector measured overall `27.587 / 18.012` and WenetSpeech `38.43 / 38.43`, worse than Stage21/Stage70 selector Wenet `37.69 / 37.69`. Do not run public Wenet/full public-200 for Stage80A. The mined-repeat strategy is still overfitting a sparse raw-eval subset and does not produce a reliable public-Wenet compensation direction.
					- 2026-06-21 Stage81 result: added a decode-only CTC blank-logit bias parameter, defaulting to `0.0`, to test whether the Stage70 public Wenet deletion regression is caused by frame-level blank overconfidence rather than prefix final scoring. This does not change the BiRWKV/RWKV-7 TimeMixer architecture, checkpoint weights, tokenizer, labels, training path, or default decode behavior. The implementation applies the bias before `log_softmax` by adding it only to the blank logit (`negative` values penalize blank), exposes it in labeled and unlabeled CTC prediction CLIs, and passed targeted tests plus `py_compile`. Public Wenet-200 Stage70 sweeps at `blank_logit_bias=-0.10/-0.25/-0.50/-1.00` with `beam8/topk16/lb0` exactly mirrored the Stage79 length-bonus tradeoff: the best setting was `-1.00` at `47.045 / 49.654`, still worse than Stage21 `46.743 / 48.722`, with deletions reduced but substitutions/insertions rising. Stop blank-bias decode repair; the next attempt should combine the CV-improving replay with Wenet anchors in one training stage rather than isolated Wenet-only repair.
					- 2026-06-21 Stage82 result: combined the Stage61 CV/public-difficulty replay with the Stage80 mined Wenet deletion anchors in a single-root training stage, avoiding a multi-root dataloader change by using the existing Stage22 curriculum root symlinks for WenetSpeech shards. Built a `230k` row length index under `/media/usbhd/training_data/asr/curriculum/stage22_stage21_soup_clean_repair_mix/stages/stage82_stage61_plus_wenet_mined_anchor`: all `200k` Stage61 rows plus `30k` Stage80 mined-anchor rows, with Stage80 rows explicitly marked `split=train` for this training stage while preserving `_stage82_original_split`. Source totals were LibriSpeech `35k`, CommonVoice EN `40k`, AISHELL3 `35k`, CommonVoice CN `30k`, GigaSpeech `20k`, and WenetSpeech `70k`. Trained from Stage21 hard soup for `250` steps with LR `1e-8`, Stage21 encoder-hidden anchor `0.05`, no SpecAugment, no direction dropout, no CTC-logit KL, and unchanged BiRWKV/RWKV-7 TimeMixer; smoke peaked around `10.19GiB`, full training completed with epoch train loss about `1.8974`, retaining only `step-250.pt`. Pre-public gates failed: Stage75 eval-Wenet proxy measured `43.917 / 44.041` (better than Stage70 but worse than Stage21/Stage76A), fixed selector measured overall `27.488 / 18.069` but WenetSpeech `38.06 / 38.06`, worse than Stage21/Stage70 Wenet `37.69 / 37.69`. Do not run public guard for raw Stage82. The mixed stage produces useful selector overall movement but still needs interpolation or a stricter Wenet-preserving objective before public evaluation.
					- 2026-06-21 Stage83 result: ran evaluation-only checkpoint soups between Stage21 hard soup and Stage82 `step-250` to shrink the Stage82 movement. Stage82 weights `0.25` and `0.50` both failed fixed-selector Wenet (`38.06` and `38.43` versus Stage21/Stage70 `37.69`). Smaller weights `0.0625` and `0.125` restored selector Wenet to `37.69`; `w=0.125` was the best selector compromise at overall `27.512 / 17.995`, tying Stage70 WER while slightly improving selector CER. However, `w=0.125` failed the Stage75 eval-Wenet proxy at `43.929 / 44.053`, worse than Stage21 `43.857 / 43.993` and Stage76A `43.869 / 43.993`, so do not run public Wenet/CommonVoice for Stage83. Temporary soup checkpoints were deleted after preserving prediction JSONL and summaries.
					- 2026-06-21 Stage84 plan: test whether the remaining public-Wenet deletion/coverage problem is mostly CTC-head calibration by freezing the BiRWKV/SenseVoice encoder and training only the CTC head on the Stage82 mixed replay. This keeps the core BiRWKV/RWKV-7 TimeMixer architecture and checkpoint encoder state unchanged while allowing output-layer calibration. Initialize from Stage21 hard soup, use Stage82 `230k` length index, freeze encoder, keep CTC head trainable, disable encoder hidden anchor because the encoder is frozen, use no SpecAugment/no direction dropout/no CTC-logit KL, and start with LR around `1e-7` for `250` steps. Gate order remains fixed selector, Stage75 eval-Wenet proxy, public Wenet/CommonVoice-200, then full public-200.
					- 2026-06-21 Stage84 result: trained `/media/usbhd/rwkvasr_runs/sensevoice_rwkv_stage84_headonly_stage82_mix_from_stage21_soup_bs12_lr1e7_zero1_nockpt_noaug_nodirdrop_4x4090` to `step=250` from the Stage21 hard soup with `freeze_encoder=true`, CTC head trainable, LR `1e-7`, no encoder-hidden anchor, no CTC-logit KL, no SpecAugment, no direction dropout, and unchanged BiRWKV/RWKV-7 TimeMixer architecture. The run completed normally with epoch train loss about `1.8567`, retaining only `step-250.pt`. Pre-public gates passed: fixed selector measured `27.512 / 18.044` with WenetSpeech tied at `37.69 / 37.69`, and Stage75 eval-Wenet proxy tied Stage21 at `43.857 / 43.993`. Public CommonVoice/Wenet-200 guard then failed under `/media/usbhd/rwkvasr_runs/stage84_headonly_stage82_mix_public200_guard_20260621`: CommonVoice EN improved from Stage21 `54.079 / 35.528` to `51.831 / 35.099`, but WenetSpeech worsened from `46.743 / 48.722` to `47.919 / 50.135`. Do not run full public-200 or promote Stage84. The CTC head is not the sole cause of public-Wenet deletion/coverage drift; the next probe must use a stricter public-Wenet-preserving objective or a better non-test proxy before applying more CV-improving movement.
					- 2026-06-21 Stage85 plan: test an inference-only CTC-head row transplant that keeps the core BiRWKV/RWKV-7 TimeMixer encoder, SenseVoice frontend, tokenizer, CTC labels, and decode policy unchanged. Use Stage21 hard soup as the base checkpoint and copy only Stage84 CTC-head rows whose decoded tokenizer text contains Latin letters or digits and is not a SenseVoice special/timestamp token; keep blank, CJK, special, and all encoder/frontend rows exactly from Stage21. This is a checkpoint-level compatibility probe, not an architecture change. Gate first on fixed selector and Stage75 eval-Wenet proxy, then public CommonVoice/Wenet-200. Promote only if Wenet stays no worse than Stage21 while retaining a meaningful CommonVoice EN gain.
					- 2026-06-21 Stage85 result: generated `/media/usbhd/rwkvasr_runs/stage85_ctc_head_latin_row_transplant_20260621/stage85_latin_rows_from_stage84_on_stage21.pt` by copying `40,668` Latin/digit CTC-head rows from Stage84 onto the Stage21 hard soup while leaving blank, CJK, special-token rows, encoder, frontend, and all non-head parameters from Stage21. Pre-public gates matched Stage84 and passed (`selector 27.512 / 18.044`, Wenet selector `37.69 / 37.69`, eval-Wenet proxy `43.857 / 43.993`), but public CommonVoice/Wenet-200 also matched Stage84 and failed: CommonVoice EN `51.831 / 35.099`, WenetSpeech `47.919 / 50.135`. Stage85 and Stage84 public Wenet predictions were identical, so changing a broad set of Latin rows is enough to alter Chinese CTC beam paths through softmax competition even when Chinese output rows remain Stage21. Do not promote Stage85. Next row-transplant probe should sharply reduce the copied row set, starting with token rows actually observed in the fixed English selector buckets, before abandoning row-level head surgery.
					- 2026-06-21 Stage86 plan: run a narrower checkpoint-level CTC-head transplant using Stage21 as base and Stage84 as row source, but copy only CTC-head rows that appear in the fixed selector's English buckets (`clean_librispeech`, `clean_cv_en`, and `gigaspeech`) as prediction or reference token ids. Keep blank, CJK, special, unobserved Latin rows, encoder/frontend, tokenizer, labels, and decode policy unchanged. Gate with fixed selector and Stage75 eval-Wenet proxy, then public CommonVoice/Wenet-200. The goal is to retain part of the English/CV CTC-head calibration gain while minimizing softmax disturbance to public Wenet.
					- 2026-06-21 Stage86 result: generated `/media/usbhd/rwkvasr_runs/stage86_ctc_head_selector_english_row_transplant_20260621/stage86_selector_english_rows_from_stage84_on_stage21.pt` by copying only `1,477` CTC-head rows observed in the fixed selector English buckets from Stage84 onto Stage21. Pre-public gates again matched Stage84/Stage85 and passed (`selector 27.512 / 18.044`, Wenet selector `37.69 / 37.69`, eval-Wenet proxy `43.857 / 43.993`), but public CommonVoice/Wenet-200 again exactly matched Stage84: CommonVoice EN `51.831 / 35.099`, WenetSpeech `47.919 / 50.135`. Do not promote Stage86. The selector English row set is already sufficient to reproduce the public-Wenet softmax/path disturbance. Next checkpoint-level probe should reduce movement magnitude, not only row count.
					- 2026-06-21 Stage87 plan: screen small-amplitude CTC-head row interpolation between Stage21 and Stage84. Use Stage21 as base and apply `base + w * (Stage84 - base)` only on the Stage86 selector-English row mask, leaving blank, CJK/special/unobserved rows, encoder/frontend, tokenizer, labels, and decode policy unchanged. Try small weights such as `0.125`, `0.25`, and `0.50`, generating one temporary checkpoint at a time and deleting failed candidates. Gate first on public Wenet and CommonVoice-200 for speed because Stage84/85/86 all fooled selector and eval-proxy; if a weight preserves Wenet and keeps CV gain, rerun fixed selector/eval-proxy and then full public-200.
					- 2026-06-21 Stage87 diagnostic result: while screening Stage87, found a public-eval batch-size sensitivity that invalidates the batch-size-1 public conclusions for Stage84/85/86/87. Re-running the Stage21 hard soup on WenetSpeech TEST_NET with current code gave `47.919 / 50.135` at `batch_size=1`, `46.954 / 49.023` at `batch_size=4`, and exactly reproduced the Stage69 baseline `46.743 / 48.722` at `batch_size=8`. Stage84 re-evaluated with `batch_size=8` under `/media/usbhd/rwkvasr_runs/stage84_headonly_stage82_mix_public200_guard_20260621/stage84_b8_lb0` tied Stage21 exactly on both public guard sets: CommonVoice EN `54.079 / 35.528` and WenetSpeech `46.743 / 48.722`. Therefore Stage84 is neutral, not a promotion candidate, and the earlier Stage84/85/86/87 public `batch_size=1` failure numbers must be treated only as evidence of a padding/batch-size invariance bug. Before more optimization claims, fix or explicitly control batch-size sensitivity in prediction/validation. This is an implementation correctness issue and does not change the BiRWKV/RWKV-7 TimeMixer architecture.
					- 2026-06-21 Stage88 plan: debug and fix batch-size/padding sensitivity in CTC prediction. Build a minimal reproducible comparison that runs the same utterances alone and in a padded batch through the same checkpoint, then localize whether drift starts in feature collation, SenseVoice frontend, RWKV encoder masking, CTC head, or prefix-beam decode. The target invariant is that per-utterance logits and decoded CTC output must be independent of unrelated batch companions, aside from expected floating-point noise. Preserve the BiRWKV/RWKV-7 TimeMixer architecture; only adjust padding masks, valid-length slicing, or prediction batching if needed. Until fixed, public benchmark comparisons must use the historical `batch_size=8` setting when comparing to Stage69/70 numbers.
					- 2026-06-21 Stage88 implementation/result: fixed the obvious bidirectional padding leak by adding `reverse_time_by_lengths` and passing valid lengths from the encoders into `BidirectionalRWKVTimeMixer`, so the backward branch reverses only valid frames and leaves padding after the valid sequence instead of moving padding into future context. This keeps the BiRWKV/RWKV-7 TimeMixer architecture and parameters unchanged. Added unit tests for length-aware reversal and padded-batch valid-frame invariance on the native TimeMixer path; `py_compile` passed for touched modules and `pytest tests/test_rwkv7_time_mixer.py -q -k 'reverse_time_by_lengths or batch_padding_invariant or bidirectional_merge or l2r_mask'` passed (`4 passed`). Real Stage21 Wenet public-200 validation under `/media/usbhd/rwkvasr_runs/stage88_batch_invariance_fix_20260621` showed the fix is only partial: `batch_size=1` is deterministic across reruns (`47.919 / 50.135`, `200/200` identical), while `batch_size=8` after length-aware reverse measured `47.889 / 49.985` with `169/200` identical to `batch_size=1`. A temporary eval-only per-sample TimeMixer fallback was tested and removed because it did not achieve full b1/b8 equality and slowed evaluation. Treat the remaining b1/b8 gap as fused/backend numerical sensitivity or another non-TimeMixer batching dependency. Future promotion baselines should be recomputed under the fixed code with an explicit canonical batch policy; use `batch_size=1` for strict single-utterance correctness and keep historical `batch_size=8` numbers only as legacy comparisons.
					- 2026-06-21 Stage89 plan: rebuild the public-200 guard under the fixed length-aware reverse code with an explicit canonical `batch_size=1` policy. Evaluate Stage21 hard soup, Stage70 encoder-anchor `step-250`, and Stage84 head-only `step-250` on the fixed public-200 manifests for AISHELL-1 test, LibriSpeech test-clean/test-other, CommonVoice EN test, and WenetSpeech TEST_NET, using `mode=bi`, `beam8/topk16`, `length_bonus=0`, `insertion_bonus=0`, `blank_logit_bias=0`, prediction normalization `ctc`, and metric normalization `ctc`. This is evaluation-only and does not change architecture, checkpoints, tokenizer, labels, or training objective. Use Stage89 to decide which existing checkpoint remains the fixed-code canonical incumbent before launching another training stage.
					- 2026-06-21 Stage89 result: completed fixed-code canonical `batch_size=1` public-200 reevaluation under `/media/usbhd/rwkvasr_runs/stage89_fixed_code_b1_public200_20260621`, with summary files `stage89_fixed_code_b1_public200_summary.json` and `.csv`. Stage21 hard soup measured macro `37.728 / 30.424`: AISHELL `30.119 / 30.119`, LibriSpeech clean `22.093 / 13.270`, LibriSpeech other `36.676 / 23.499`, CommonVoice EN `51.831 / 35.099`, WenetSpeech `47.919 / 50.135`. Stage84 head-only matched Stage21 exactly on all five public-200 datasets, so it is neutral and not a promotion candidate. Stage70 `step-250` measured macro `37.678 / 30.359`, improving macro by `-0.050 / -0.066`, AISHELL, LibriSpeech clean, CommonVoice EN (`51.726 / 34.944`), and Wenet CER (`50.045`, `-0.090`), but Wenet WER was `47.949` (`+0.030` versus Stage21) and LibriSpeech-other CER worsened at rounding scale. Do not promote Stage70 yet, but keep it as the fixed-code canonical near-pass candidate.
					- 2026-06-21 Stage90 plan: run a small fixed-code `batch_size=1` decode-policy screen for Stage21 versus Stage70 on WenetSpeech TEST_NET and CommonVoice EN public-200 before any new training. Use the same manifests and `beam8/topk16`, varying only `length_bonus` (`0.25`, `0.5`, `1.0`, and possibly `1.5`) while keeping `insertion_bonus=0` and `blank_logit_bias=0`. A decode policy is usable only if Stage70 beats or ties Stage21 under the same policy on Wenet WER/CER and CommonVoice EN WER/CER while preserving Stage70's macro promise. If no decode policy passes, keep Stage21 as incumbent and design the next same-architecture training stage around Stage70's remaining Wenet WER gap.
					- 2026-06-21 Stage90 result: fixed-code `batch_size=1` length-bonus screen completed under `/media/usbhd/rwkvasr_runs/stage90_fixed_code_b1_length_bonus_screen_20260621`, with summary files `stage90_b1_length_bonus_cv_wenet_summary.json` and `.csv`. No tested length bonus (`0.25`, `0.5`, `1.0`, `1.5`) made Stage70 strictly beat or tie Stage21 on both CommonVoice EN and WenetSpeech under the same decode policy. Stage70 consistently improved Wenet CER versus Stage21 but retained a small Wenet WER deficit: at `lb=0.0`, Wenet `+0.030 / -0.090`; at `lb=1.0`, Wenet `+0.030 / -0.211`; at `lb=1.5`, Wenet `+0.090 / -0.030`. CommonVoice EN remained mostly better for Stage70 except at `lb=1.5` WER. Stop length-bonus promotion attempts for Stage70; the remaining blocker is a Wenet WER gap, not a simple length/blank calibration issue.
					- 2026-06-21 Stage91 plan: re-evaluate prior Wenet-recovery and mixed-replay checkpoints under the corrected fixed-code `batch_size=1` public CV/Wenet guard because earlier public conclusions were contaminated by batch policy. Test Stage76A Wenet-eval-anchor from Stage70, Stage78A Wenet-eval-anchor from Stage21, Stage80A deletion-drift anchor from Stage70, and Stage82 mixed Stage61+Wenet-mined anchor from Stage21 on CommonVoice EN and WenetSpeech public-200 with the Stage89 canonical decode (`mode=bi`, `beam8/topk16`, `lb=0`, `ib=0`, `blank_logit_bias=0`). Only a checkpoint that improves or ties Stage21 and Stage70 on Wenet WER/CER without losing Stage70's CommonVoice gain should proceed to full public-200 or checkpoint interpolation.
					- 2026-06-21 Stage91 result: fixed-code `batch_size=1` recovery checkpoint public CV/Wenet guard completed under `/media/usbhd/rwkvasr_runs/stage91_fixed_code_b1_recovery_public_guard_20260621`, with summary files `stage91_fixed_code_b1_recovery_public_guard_summary.json` and `.csv`. Stage76A failed both Stage21 and Stage70 guards. Stage78A, Stage80A, and Stage82 all passed versus Stage21 on the two public guard datasets, but not versus Stage70 because Stage70 has lower Wenet CER. Stage80A is the strongest Stage21-relative guard candidate: CommonVoice EN `51.464 / 34.834` versus Stage21 `51.831 / 35.099` and Stage70 `51.726 / 34.944`; WenetSpeech `47.919 / 50.105` versus Stage21 `47.919 / 50.135` and Stage70 `47.949 / 50.045`. Stage80A therefore retains Stage21 Wenet WER, improves Stage21 Wenet CER, and beats Stage70 on CommonVoice EN, making it the next fixed-code full-public candidate.
					- 2026-06-21 Stage92 plan: complete the fixed-code `batch_size=1` full public-200 evaluation for Stage80A by running the remaining AISHELL-1, LibriSpeech test-clean, and LibriSpeech test-other manifests; reuse Stage91's Stage80A CommonVoice EN and WenetSpeech predictions. Compare full macro and all per-dataset WER/CER against Stage21 and Stage70 from Stage89. Promote Stage80A as the fixed-code b1 incumbent only if macro WER/CER improve versus Stage21 and no key public guard dataset regresses versus Stage21; otherwise use its CV/Wenet behavior as a data signal for the next same-architecture training stage.
					- 2026-06-21 Stage92 result: completed Stage80A fixed-code `batch_size=1` full public-200 under `/media/usbhd/rwkvasr_runs/stage92_stage80a_fixed_code_b1_full_public200_20260621`, with summary files `stage92_stage80a_fixed_code_b1_full_public200_summary.json` and `.csv`. Stage80A measured macro `37.648 / 30.368`, improving Stage21 `37.728 / 30.424` by `-0.080 / -0.056` and passing the key Stage21 public guard: AISHELL `29.983 / 29.983`, LibriSpeech clean `22.017 / 13.241`, CommonVoice EN `51.464 / 34.834`, and WenetSpeech `47.919 / 50.105`. It regressed LibriSpeech-other versus Stage21 (`36.858 / 23.678`, `+0.182 / +0.179`) and is slightly worse than Stage70 macro CER (`30.368` versus `30.359`) because Stage70 has better LibriSpeech-other and Wenet CER. Treat Stage80A as the first fixed-code b1 checkpoint that cleanly beats Stage21 macro and key CV/Wenet guard, but screen Stage70/Stage80A interpolation before declaring it the new incumbent.
					- 2026-06-21 Stage93 plan: run inference-only checkpoint interpolation between Stage70 `step-250` and Stage80A `step-250`, both same architecture and same tokenizer/CTC head shape. Generate small Stage80A weights (`0.25`, `0.50`, `0.75`) over Stage70 as anchor, delete temporary checkpoints after evaluation, and screen first on fixed-code `batch_size=1` public WenetSpeech, CommonVoice EN, and LibriSpeech-other, because these distinguish Stage70 from Stage80A. A candidate may become the next full-public incumbent only if it keeps Stage80A's Stage21 CV/Wenet guard while reducing Stage80A's LibriSpeech-other regression and preserving macro gains.
					- 2026-06-21 Stage93 result: Stage70/Stage80A interpolation screen completed under `/media/usbhd/rwkvasr_runs/stage93_stage70_stage80a_interp_b1_guard_20260621`, with summary files `stage93_stage70_stage80a_interp_b1_guard_summary.json` and `.csv`. No tested interpolation passed the Stage21 public guard. `w0p25` reduced Stage80A's LibriSpeech-other regression (`36.737 / 23.599`, `-0.122 / -0.079` versus Stage80A) but lost CommonVoice CER (`35.154`, `+0.055` versus Stage21) and Wenet WER (`47.949`, `+0.030` versus Stage21). `w0p5` improved CommonVoice versus Stage21 but worsened Wenet (`48.070 / 50.165`), and `w0p75` worsened Wenet further (`48.070 / 50.286`). Do not promote any Stage93 soup. Stage80A remains the strongest fixed-code `batch_size=1` Stage21-relative candidate, with LibriSpeech-other regression as the main blocker.
					- 2026-06-21 Stage94 plan: run a same-architecture low-amplitude LibriSpeech-other repair continuation from Stage80A `step-250.pt`. Keep BiRWKV/RWKV-7 TimeMixer, SenseVoice frontend, tokenizer/CTC labels, bidirectional mode, no direction dropout, no SpecAugment, and CTC-only loss unchanged. Reuse the Stage61 difficulty/public/Wenet replay index because it contains LibriSpeech, CommonVoice EN/CN, AISHELL, GigaSpeech, and WenetSpeech guard rows; initialize from Stage80A, use Stage80A itself as the frozen encoder-hidden anchor (`encoder_anchor_loss_weight=0.05`) to limit drift, start with LR `2.5e-9`, `max_steps=250`, `save_every=125`, ZeRO-1, batch size `12`, grad accumulation `4`, no optimizer offload, and no gradient checkpointing. Gate first on fixed-code `batch_size=1` public-200 LibriSpeech-other, CommonVoice EN, and WenetSpeech using the Stage89 decode policy (`beam8/topk16`, `length_bonus=0`, `blank_logit_bias=0`, CTC prediction/metric normalization). Promote only if LibriSpeech-other improves versus Stage80A while Stage21 CV/Wenet guard stays no worse; run AISHELL and LibriSpeech-clean only after that guard passes.
					- 2026-06-21 Stage94 result: training completed to `step=250` under `/media/usbhd/rwkvasr_runs/sensevoice_rwkv_stage94_stage61_repair_from_stage80a_bs12_lr2p5e9_anchor0p05_zero1_nockpt_noaug_nodirdrop_4x4090`, preserving `step-125.pt` and `step-250.pt`. The run loaded Stage80A cleanly, used Stage80A as hidden anchor, and kept direction dropout/SpecAugment disabled; the process had to be terminated after final checkpoint because the zero-sample epoch eval did not exit promptly, but both `.pt` checkpoints and `latest_checkpoint.yaml` were complete. Public-200 guard/full summary is in `/media/usbhd/rwkvasr_runs/stage94_stage61_repair_b1_guard_20260621/stage94_stage61_repair_b1_guard_summary.json` and `.csv`. `step-125.pt` is useful but not a WER-primary promotion over Stage80A: macro `37.676 / 30.354`, which beats Stage21 by `-0.051 / -0.070`, beats Stage70 by `-0.001 / -0.004`, and trades against Stage80A by `+0.028 / -0.014`. Per dataset: AISHELL `29.949 / 29.949`, Libri-clean `22.042 / 13.270`, Libri-other `36.828 / 23.599`, CommonVoice EN `51.674 / 34.878`, Wenet `47.889 / 50.075`. It repairs Stage80A on Libri-other (`-0.030 / -0.079`) and Wenet (`-0.030 / -0.030`) while worsening Stage80A CommonVoice (`+0.209 / +0.044`) and Libri-clean (`+0.025 / +0.029`). `step-250.pt` over-trained for the guard: Libri-other reached `36.676 / 23.528`, but CommonVoice regressed to `51.935 / 35.154` and Wenet to `47.949 / 50.165`; do not promote step250. Treat Stage94 step125 as a CER/guard-balanced candidate and Stage80A as the WER-primary candidate.
					- 2026-06-21 Stage95 plan: run inference-only checkpoint interpolation between Stage80A `step-250.pt` and Stage94 `step-125.pt`, which share the same BiRWKV/RWKV-7 TimeMixer architecture, SenseVoice frontend, tokenizer, CTC head, and decode policy. Generate Stage94 weights `0.25`, `0.50`, and `0.75` over Stage80A, delete temporary checkpoints after evaluation, and first screen fixed-code `batch_size=1` public-200 LibriSpeech-other, CommonVoice EN, and WenetSpeech. A candidate is interesting only if it keeps Stage80A's WER-primary macro direction while inheriting some Stage94 CER/Wenet repair; if none does, keep Stage80A as WER-primary and Stage94 step125 only as a CER-tie/diagnostic candidate.
					- 2026-06-21 Stage95 result: Stage80A/Stage94-step125 interpolation completed under `/media/usbhd/rwkvasr_runs/stage95_stage80a_stage94_step125_interp_b1_guard_20260621`, with `stage95_stage80a_stage94_step125_interp_b1_guard_summary.json` and `.csv`. Temporary soup checkpoints were deleted after screening except the full-eval candidate `/media/usbhd/rwkvasr_runs/stage95_stage80a_stage94_step125_interp_b1_guard_20260621/stage95_w0p75_candidate.pt`; `wercer_best.pt` should point there if selecting the balanced CER/guard candidate. `w0p25` and `w0p5` failed because WenetSpeech WER stayed worse than Stage21 (`47.949`). `w0p75` passed the guard and was evaluated on all five public-200 sets: macro `37.655 / 30.344`, which improves Stage21 by `-0.072 / -0.080`, improves Stage70 by `-0.022 / -0.015`, trades against Stage80A by `+0.007 / -0.024`, and improves Stage94 step125 by `-0.021 / -0.010`. Per dataset: AISHELL `29.915 / 29.915`, Libri-clean `22.093 / 13.270`, Libri-other `36.797 / 23.650`, CommonVoice EN `51.674 / 34.900`, and Wenet `47.799 / 49.985`. This is the best fixed-code `batch_size=1` CER/guard-balanced checkpoint so far, while Stage80A remains a marginally better WER-primary point by `0.007` macro WER. The main residual blocker is still LibriSpeech-other (`+0.122 / +0.151` versus Stage21), but Wenet and AISHELL are now clearly better than Stage21/Stage80A.
					- 2026-06-21 Stage96 plan: before launching another training run, audit decode-length behavior on Stage95 `w0p75` and Stage80A using existing public-200 predictions. Compute per-dataset pred/ref word or character ratios and deletion/insertion tendencies if available. If ratios indicate a simple CTC blank/deletion bias, run a small decode-only screen on Stage95 `w0p75` for LibriSpeech-other, CommonVoice EN, and WenetSpeech with `blank_logit_bias` values such as `-0.05`, `-0.10`, and `-0.20`, keeping `beam8/topk16`, `length_bonus=0`, `insertion_bonus=0`, `batch_size=1`, and CTC normalization. Promote a decode policy only if it reduces LibriSpeech-other WER/CER or macro WER without losing the Stage95 Wenet/CV guard. If no decode policy passes, the next training stage should be LibriSpeech-other-targeted with stronger Stage95 hidden/logit anchoring and explicit Wenet/CV guard data.
					- 2026-06-21 Stage96 result: length/error audit found no simple blank/deletion calibration issue. Stage95 `w0p75`, Stage80A, and Stage21 have nearly identical LibriSpeech-other word ratios (`0.9736`, `0.9739`, `0.9742`) and similar WER operation splits; for LibriSpeech-other, Stage95 WER `36.797` decomposes into substitution/insertion/deletion `28.654 / 2.917 / 5.226`, Stage80A `28.624 / 2.978 / 5.257`, and Stage21 `28.532 / 2.947 / 5.196`. CommonVoice and Wenet show the same pattern: small differences in substitutions dominate, not output length. Skip the blank-logit-bias screen for now; it is unlikely to fix the residual without trading errors across datasets. The next low-risk optimization should be checkpoint interpolation with Stage21, whose only current advantage is LibriSpeech-other.
					- 2026-06-21 Stage97 plan: run inference-only interpolation between Stage95 `w0p75` balanced candidate and Stage21 hard soup. Both checkpoints share the same BiRWKV/RWKV-7 TimeMixer architecture and CTC label space. Generate small Stage21 weights (`0.125`, `0.25`, `0.375`) over Stage95, delete temporary checkpoints after screening, and evaluate fixed-code `batch_size=1` public-200 LibriSpeech-other, CommonVoice EN, and WenetSpeech. A candidate is useful only if it reduces the Stage95 LibriSpeech-other residual while preserving the Stage95/Stage21 CV and Wenet guard; run AISHELL/Libri-clean only for a passing candidate.
					- 2026-06-21 Stage97 result: Stage95/Stage21 interpolation completed under `/media/usbhd/rwkvasr_runs/stage97_stage95_stage21_interp_b1_guard_20260621`, with `stage97_stage95_stage21_interp_b1_guard_summary.json` and `.csv`. All Stage21 injection points failed the CV/Wenet guard. `w0p125` reduced LibriSpeech-other to `36.706 / 23.521` but regressed CommonVoice to `51.987 / 35.176` and Wenet to `48.040 / 50.105`. `w0p25` and `w0p375` also failed Wenet (`47.979`) and did not deliver a robust LibriSpeech-other repair. Do not promote any Stage97 soup. This confirms that the Stage21 direction contains the desired Libri-other signal but is not compatible with the current Stage95 guard without further training constraints.
					- 2026-06-21 Stage98 plan: return to training from Stage95 `w0p75` with stronger guard constraints, still without changing the BiRWKV/RWKV-7 TimeMixer architecture, frontend, tokenizer, CTC labels, direction policy, or decode policy. Build or reuse a small Libri-heavy repair mix that includes explicit CommonVoice EN and WenetSpeech guard rows under a single WebDataset root; prefer reusing existing Stage22/Stage61 linked roots if they already expose clean LibriSpeech/CommonVoice and Wenet shards. Start from Stage95 `wercer_best.pt`, use Stage95 itself as hidden anchor and add a small CTC-logit KL anchor only if memory allows (`encoder_anchor_loss_weight=0.05`, tentative `ctc_logit_anchor_loss_weight=0.05` with chunked logits), LR around `1e-9` to `2e-9`, `max_steps=125`, save final only or every `125`, ZeRO-1, no SpecAugment, no gradient checkpointing, no direction dropout, batch size `12`, grad accumulation `4`. Gate on LibriSpeech-other/CommonVoice/Wenet public-200 before running full public-200; stop immediately if CV/Wenet moves like Stage97.
					- 2026-06-21 Stage98 implementation detail: use the existing Stage22 unified WebDataset root and create a new linked-length stage `stage98_stage95_libri_guard_repair` without copying tar files. Sample `160k` train rows with explicit quotas: LibriSpeech `70k`, CommonVoice EN `35k`, WenetSpeech `35k`, AISHELL3 `10k`, CommonVoice CN `5k`, and GigaSpeech `5k`. Prefer Stage61 rows for the guard-balanced source pools and Stage22 rows as the larger clean fallback; annotate sampled rows with `_stage98_*` fields for auditability. Generate a fresh bucket manifest from the sampled length index. Training config should initialize and anchor from Stage95 `wercer_best.pt`, use hidden anchor `0.05`, CTC-logit KL anchor `0.05` with `ctc_logit_anchor_chunk_frames=16`, LR `1.0e-9`, `max_steps=125`, `save_every=125`, `top_k_step_checkpoints=1`, ZeRO-1, batch size `12`, grad accumulation `4`, no SpecAugment, no gradient checkpointing, and no direction dropout. Because `/media/usbhd` is nearly full, delete only obsolete `ds_checkpoints/` directories when needed and preserve exported `.pt` checkpoints.
					- 2026-06-21 Stage98 preparation: added `scripts/build_stage98_libri_guard_repair.py`, generated `/media/usbhd/training_data/asr/curriculum/stage22_stage21_soup_clean_repair_mix/stages/stage98_stage95_libri_guard_repair/webdataset_lengths.jsonl`, and built its `webdataset_buckets_audio_text/manifest.json`. The sampled stage has `160,000` rows with source counts exactly matching the quota (`70k` LibriSpeech, `35k` CommonVoice EN, `35k` WenetSpeech, `10k` AISHELL3, `5k` CommonVoice CN, `5k` GigaSpeech). Unique source pools were limited for LibriSpeech and WenetSpeech, so the index includes replacement fills (`22,998` LibriSpeech and `5,000` WenetSpeech); because Stage98 is only `125` steps, this is acceptable as a low-amplitude repair slice but must be noted when interpreting overfit. Generated config `configs/generated/sensevoice_rwkv_stage98_stage95_libri_guard_repair_4x4090_deepspeed.yaml` points to Stage95 `wercer_best.pt` for both initialization and anchor. To make room for the required DeepSpeed checkpoint, deleted only the obsolete failed Omni/Qwen3 control branch recovery directory `/media/usbhd/rwkvasr_runs/qwen3_transformer_omni_ctc/qwen3_transformer_omni_ctc_stage2_easy_cumulative_bs12_zero1_nockpt_4x4090/ds_checkpoints`, preserving that run's exported `.pt` checkpoints; free space increased from about `13G` to `31G`.
					- 2026-06-21 Stage98 result: training finished normally at `step=125` under `/media/usbhd/rwkvasr_runs/sensevoice_rwkv_stage98_stage95_libri_guard_repair_bs12_lr1e9_hid0p05_kl0p05_zero1_nockpt_noaug_nodirdrop_4x4090`, preserving `step-125.pt`. Final epoch train loss was `2.0910`; final logged anchor losses were hidden `0.7902` and CTC-logit KL `0.0463`, with first-step peak memory about `19.90 GiB`, so the dual-anchor recipe is runnable. Public-200 guard metrics under `/media/usbhd/rwkvasr_runs/stage98_stage95_libri_guard_repair_b1_guard_20260621` are LibriSpeech-other `36.58 / 23.52`, CommonVoice EN `51.83 / 35.08`, and WenetSpeech `47.89 / 50.08`. This repairs Stage95 LibriSpeech-other by `-0.21 / -0.13`, but regresses Stage95 CommonVoice by about `+0.16 / +0.18` and Wenet by `+0.09 / +0.09`; it is not a direct promotion, although it still stays around or better than the Stage21 guard. Deleted only Stage98 `ds_checkpoints/` after verifying `step-125.pt` to recover space; keep the exported `.pt` for interpolation and evaluation.
					- 2026-06-21 Stage99 plan: run inference-only checkpoint interpolation between Stage95 `w0p75` and Stage98 `step-125.pt` to keep the Stage98 LibriSpeech-other repair while recovering the Stage95 CommonVoice/Wenet guard. Generate Stage98 weights `0.25`, `0.50`, and `0.75` over Stage95, all same BiRWKV/RWKV-7 TimeMixer architecture and bf16 tensor dtype, and screen fixed-code `batch_size=1` public-200 LibriSpeech-other/CommonVoice EN/WenetSpeech. A candidate is useful only if it improves Stage95 LibriSpeech-other and keeps CommonVoice/Wenet no worse than Stage95 within rounding tolerance; otherwise discard Stage98 as a training signal rather than a checkpoint update.
					- 2026-06-21 Stage98 full-public note and Stage99 result: after completing AISHELL-1 and LibriSpeech-clean for Stage98, full public-200 macro is `37.656 / 30.373`: AISHELL `29.983 / 29.983`, Libri-clean `21.992 / 13.212`, Libri-other `36.585 / 23.521`, CommonVoice EN `51.831 / 35.077`, Wenet `47.889 / 50.075`. Stage98 effectively ties Stage95 macro WER (`+0.0002`) but is worse on macro CER (`+0.0295`), with gains on Libri clean/other offset by AISHELL/CV/Wenet drift. Stage99 `w0p25` (`25%` Stage98 over Stage95) measured Libri-other `36.80 / 23.65`, CommonVoice EN `51.73 / 34.97`, and Wenet `47.80 / 49.98`; it failed because it did not preserve the Stage98 Libri repair and already worsened CommonVoice versus Stage95. Delete the failed `w0p25` soup checkpoint after recording predictions/metrics; do not spend time on higher Stage98 soup weights because Stage98 full already shows the CV/Wenet drift endpoint.
					- 2026-06-21 Stage100 plan: run a lower-amplitude training probe from Stage95 using the same Stage98 Libri-heavy guard index, but with stronger anchors and shorter training: hidden anchor `0.10`, CTC-logit KL anchor `0.10`, `ctc_logit_anchor_chunk_frames=16`, LR `5.0e-10`, `max_steps=64`, `save_every=64`, ZeRO-1, batch size `12`, grad accumulation `4`, no SpecAugment, no gradient checkpointing, and no direction dropout. This is not a new architecture or tokenizer; it tests whether early-stop / stronger-anchor training can retain a smaller LibriSpeech repair without Stage98's CommonVoice/Wenet drift. Gate first on LibriSpeech-other/CommonVoice EN/WenetSpeech public-200; only run full public if the guard is better than Stage98 and not worse than Stage95.
					- 2026-06-21 Stage100 result and Stage101 plan: Stage100 completed `64` steps from Stage95 with stronger anchors, but the public-200 guard did not improve the Pareto point: LibriSpeech-other `36.80 / 23.65`, CommonVoice EN `51.73 / 34.91`, WenetSpeech `47.80 / 49.98`. It behaves like the failed Stage99 low-weight soup: no Libri repair and still a small CommonVoice regression versus Stage95. Delete Stage100 `ds_checkpoints/` after verifying `step-64.pt`; do not promote. Next try the reverse repair direction: initialize from Stage98 `step-125.pt` so the Libri clean/other repair is present, train briefly on the balanced Stage61 difficulty/public/Wenet replay index, and use Stage95 `wercer_best.pt` as hidden+CTC-logit anchor (`0.05 / 0.05`) to pull CommonVoice/Wenet/AISHELL behavior back toward Stage95. Use LR `5.0e-10`, `max_steps=64`, `save_every=64`, no SpecAugment, no gradient checkpointing, no direction dropout, ZeRO-1, batch size `12`, grad accumulation `4`. Gate on LibriSpeech-other/CommonVoice EN/WenetSpeech; the desired outcome is Stage98-level Libri with Stage95-level CV/Wenet.
					- 2026-06-21 Stage101 result and Stage102 plan: Stage101 completed `64` steps from Stage98 on Stage61 with Stage95 hidden/logit anchor, but did not recover the guard: LibriSpeech-other `36.65 / 23.53`, CommonVoice EN `51.83 / 35.08`, WenetSpeech `47.89 / 50.08`. It pulls LibriSpeech-other partly back toward Stage95 while leaving CV/Wenet near Stage98, so it is not useful. Delete Stage101 `ds_checkpoints/` after verifying `step-64.pt`. Next run one stronger guard-recovery probe using existing Stage82 `stage82_stage61_plus_wenet_mined_anchor`, which has a heavier Wenet pool (`70k` Wenet plus `40k` CommonVoice EN and balanced clean guards). Initialize from Stage98 `step-125.pt`, keep Stage95 as hidden+CTC-logit anchor (`0.05 / 0.05`), increase LR to `1.0e-9`, keep `max_steps=64`, `save_every=64`, no SpecAugment, no gradient checkpointing, no direction dropout, ZeRO-1, batch size `12`, grad accumulation `4`. Gate on LibriSpeech-other/CommonVoice EN/WenetSpeech; stop this recovery line if Stage82 still cannot move CV/Wenet back without erasing Libri.
					- 2026-06-21 Stage102 result: Stage102 completed `64` steps from Stage98 on Stage82 with Stage95 hidden/logit anchor and LR `1e-9`, but failed the guard more clearly: LibriSpeech-other `36.74 / 23.56`, CommonVoice EN `51.94 / 35.13`, WenetSpeech `47.95 / 50.14`. Compared with Stage98, this erases much of the Libri repair and worsens CV/Wenet; compared with Stage95, it is worse on all three guard datasets except Libri remains slightly better. Do not promote Stage100/101/102 and stop this Stage98 recovery line. Current fixed-code `batch_size=1` candidates remain: Stage95 as best CER/guard-balanced checkpoint, Stage80A as marginal WER-primary checkpoint, and Stage98 only as a diagnostic Libri-repair/guard-regression point. Further progress should avoid more tiny LR/anchor variants on the same Stage98/Stage61/Stage82 data and instead use a different signal, e.g. targeted teacher correction / confidence-mined Libri-other substitutions with explicit CV/Wenet public guard, or a new decode objective that addresses substitutions without changing the BiRWKV architecture.
					- 2026-06-21 Stage103 plan: before returning to training, run one inference-only interpolation screen between Stage80A `step-250.pt` and Stage98 `step-125.pt`. Stage80A remains the WER-primary point with stronger CommonVoice EN but weaker LibriSpeech-other; Stage98 has the best recent LibriSpeech-other repair and slightly better Wenet than Stage80A, but worse CommonVoice. Both checkpoints share the same BiRWKV/RWKV-7 TimeMixer architecture, SenseVoice frontend, tokenizer, and CTC head dtype/shape. Generate a small Stage98 weight over Stage80A, starting with `w=0.25`, and evaluate fixed-code `batch_size=1` LibriSpeech-other/CommonVoice EN/WenetSpeech public-200. Continue to higher Stage98 weights only if `w=0.25` keeps CommonVoice close to Stage80A/Stage95 while improving LibriSpeech-other. Run full public-200 only for a candidate that beats Stage80A macro direction or Stage95 guard balance without a CV/Wenet regression.
					- 2026-06-21 Stage103 result: Stage80A/Stage98 interpolation `w=0.25` was evaluated under `/media/usbhd/rwkvasr_runs/stage103_stage80a_stage98_interp_b1_guard_20260621` with fixed-code `batch_size=1`, `mode=bi`, `beam8/topk16`, and CTC normalization. Guard metrics were LibriSpeech-other `36.83 / 23.65`, CommonVoice EN `51.52 / 34.88`, and WenetSpeech `48.01 / 50.20`. This does not recover Stage98's LibriSpeech-other repair and substantially worsens Wenet versus both Stage80A and Stage95, so delete the temporary `stage103_w0p25_candidate.pt` soup after keeping predictions/metrics and do not spend time on higher Stage98 weights. Current candidates remain Stage80A as the marginal WER-primary point and Stage95 as the CER/guard-balanced point; further progress needs a different same-architecture signal rather than another Stage98 interpolation or tiny LR/anchor variant.
					- 2026-06-21 Stage104 plan: run a targeted LibriSpeech teacher-label substitution repair from Stage95 without changing the BiRWKV/RWKV-7 TimeMixer architecture, frontend, tokenizer, CTC head, direction policy, or decode policy. Build a new linked length-index stage `stage104_stage95_libri_teacher_guard_repair` under the Stage22 curriculum root with `160k` rows: `8k` repeated LibriSpeech teacher-override rows sampled from the accepted Stage39 FunASR teacher cache by `primary_improvement`, plus original-label guard rows from Stage61/Stage22 with quotas LibriSpeech `45k`, CommonVoice EN `35k`, WenetSpeech `35k`, AISHELL3 `20k`, CommonVoice CN `10k`, and GigaSpeech `7k`. The sampled CTC label override cache must contain only LibriSpeech teacher rows; all CommonVoice/Wenet/AISHELL/Giga rows keep original labels. Train from Stage95 `wercer_best.pt` with Stage95 as hidden and CTC-logit anchor (`0.10 / 0.10`, chunk frames `16`), LR `5e-10`, `max_steps=128`, `save_every=64`, ZeRO-1, batch size `12`, grad accumulation `4`, no SpecAugment, no gradient checkpointing, and no direction dropout. Gate first on fixed-code `batch_size=1` LibriSpeech-other/CommonVoice EN/WenetSpeech public-200; promote or run AISHELL/Libri-clean only if LibriSpeech-other improves versus Stage95 while CommonVoice and Wenet do not regress versus Stage95 within rounding tolerance.
					- 2026-06-21 Stage104 preparation: added `scripts/build_stage104_libri_teacher_guard_repair.py`, generated `/media/usbhd/training_data/asr/curriculum/stage22_stage21_soup_clean_repair_mix/stages/stage104_stage95_libri_teacher_guard_repair/webdataset_lengths.jsonl`, its `webdataset_buckets_audio_text/manifest.json`, and sampled override cache `stage104_libri_teacher_sampled.cache.jsonl`. The generated slice has `160,000` rows: `152,000` original-label guards and `8,000` LibriSpeech teacher-substitution rows. Source totals are LibriSpeech `53k` including teacher rows, CommonVoice EN `35k`, WenetSpeech `35k`, AISHELL3 `20k`, CommonVoice CN `10k`, and GigaSpeech `7k`; WenetSpeech required `5,723` replacement-fill guard rows because the unique pool is limited. The override cache has `765` unique sampled LibriSpeech ids and no non-Libri sources. Generated config `configs/generated/sensevoice_rwkv_stage104_stage95_libri_teacher_guard_repair_4x4090_deepspeed.yaml` parses with Stage95 init/anchor, LR `5e-10`, hidden/logit anchor `0.10 / 0.10`, `max_steps=128`, `save_every=64`, `top_k_step_checkpoints=2`, no SpecAugment, no gradient checkpointing, and no direction dropout.
					- 2026-06-21 Stage104 result: training completed normally to `step=128` under `/media/usbhd/rwkvasr_runs/sensevoice_rwkv_stage104_stage95_libri_teacher_guard_repair_bs12_lr5e10_hid0p10_kl0p10_zero1_nockpt_noaug_nodirdrop_4x4090`, preserving `step-64.pt` and `step-128.pt`; final rank-0 loss was `1.4600`, epoch train loss `2.1418`, and final logged anchor losses were hidden `0.8342` and CTC-logit KL `0.0506`. First-step peak reserved memory was about `14.94 GiB`. Deleted only the run's `ds_checkpoints/` after verifying exported `.pt` files. Fixed-code public-200 guard for `step-64` measured LibriSpeech-other `36.83 / 23.67`, CommonVoice EN `51.62 / 34.90`, and WenetSpeech `47.77 / 49.83`, so it did not repair LibriSpeech-other. `step-128` passed the guard and full public-200 was completed under `/media/usbhd/rwkvasr_runs/stage104_stage95_libri_teacher_guard_b1_guard_20260621`: macro `37.653 / 30.321`, AISHELL `29.983 / 29.983`, Libri-clean `22.093 / 13.253`, Libri-other `36.767 / 23.628`, CommonVoice EN `51.621 / 34.878`, and Wenet `47.799 / 49.865`. Compared with Stage95, this improves macro by about `-0.003 / -0.023`, improves Libri-other and CV slightly, and keeps Wenet WER tied while improving Wenet CER. Compared with Stage80A, it is worse on macro WER by about `+0.004` but better on macro CER by about `-0.047`. Mark `/media/usbhd/rwkvasr_runs/sensevoice_rwkv_stage104_stage95_libri_teacher_guard_repair_bs12_lr5e10_hid0p10_kl0p10_zero1_nockpt_noaug_nodirdrop_4x4090/wercer_best.pt` as a symlink to `step-128.pt` for the new CER/guard-balanced candidate, while Stage80A remains the marginal WER-primary point.
					- 2026-06-21 Stage105 plan: run inference-only interpolation between Stage80A `step-250.pt` and Stage104 `step-128.pt`. Both checkpoints share the same BiRWKV/RWKV-7 TimeMixer architecture, SenseVoice frontend, tokenizer, CTC head, direction policy, and fixed-code decode path. Stage80A has the best current macro WER but weaker CER/Wenet/Libri-other, while Stage104 has the best current CER/guard balance but slightly worse macro WER. Generate Stage104 weights `0.25`, `0.50`, and `0.75` over Stage80A under `/media/usbhd/rwkvasr_runs/stage105_stage80a_stage104_interp_b1_guard_20260621`, evaluate fixed-code `batch_size=1` LibriSpeech-other/CommonVoice EN/WenetSpeech first, and only complete AISHELL/Libri-clean for a candidate that improves or ties Stage80A macro direction while keeping Stage104's Wenet/CER guard. Delete failed temporary soup checkpoints after preserving predictions/metrics.
					- 2026-06-21 Stage105 result: Stage80A/Stage104 interpolation did not produce a better Pareto point. `w0p25` (`25%` Stage104 over Stage80A) measured LibriSpeech-other `36.89 / 23.69`, CommonVoice EN `51.52 / 34.88`, and WenetSpeech `48.01 / 50.20`, failing both Libri and Wenet guard. `w0p75` measured LibriSpeech-other `36.83 / 23.65`, CommonVoice EN `51.62 / 34.89`, and WenetSpeech `47.80 / 49.86`, which is worse than Stage104 on Libri/CV and does not improve Wenet. Because both ends of the tested interpolation surface fail to beat Stage104 and `w0p50` sits between them, skip further Stage105 evaluation and delete the temporary `w0p25`, `w0p50`, and `w0p75` soup checkpoints after preserving predictions/metrics. Keep Stage104 `step-128` as the current CER/guard-balanced candidate and Stage80A as the WER-primary candidate.
					- 2026-06-21 Stage106 plan: run a short Stage61-balanced consolidation from Stage104, still without changing the BiRWKV/RWKV-7 TimeMixer architecture, frontend, tokenizer, CTC head, direction policy, or decode policy. Stage70 showed that Stage61 difficulty/public/Wenet replay can improve the CV/Libri-clean/AISHELL side of the Pareto surface, while Stage104 now supplies the best CER/guard-balanced anchor. Initialize from Stage104 `step-128.pt`, train on the existing Stage61 `stage61_difficulty_public_wenet_replay` length index, use Stage104 itself as hidden+CTC-logit anchor (`0.10 / 0.10`, chunk frames `16`), LR `5e-10`, `max_steps=64`, `save_every=64`, ZeRO-1, batch size `12`, grad accumulation `4`, no SpecAugment, no gradient checkpointing, no teacher override cache, and no direction dropout. Gate first on fixed-code public-200 LibriSpeech-other/CommonVoice EN/WenetSpeech; complete AISHELL/Libri-clean only if the guard is no worse than Stage104 and CV/Libri-clean recovery is plausible.
					- 2026-06-21 Stage106 result: training completed normally to `step=64` under `/media/usbhd/rwkvasr_runs/sensevoice_rwkv_stage106_stage104_stage61_consolidation_bs12_lr5e10_hid0p10_kl0p10_zero1_nockpt_noaug_nodirdrop_4x4090`, preserving `step-64.pt`; final rank-0 loss was `1.8956`, epoch train loss `1.9212`, and final logged anchor losses were hidden `0.7992` and CTC-logit KL `0.0181`. First-step peak reserved memory was about `15.34 GiB`; deleted only `ds_checkpoints/` after verifying exported `.pt`. Fixed-code public-200 guard under `/media/usbhd/rwkvasr_runs/stage106_stage104_stage61_consolidation_b1_guard_20260621` measured LibriSpeech-other `36.80 / 23.65`, CommonVoice EN `51.67 / 34.89`, and WenetSpeech `47.83 / 49.89`, all worse than Stage104 `step-128` within this guard set. Do not run AISHELL/Libri-clean, do not promote Stage106, and stop the Stage61-consolidation-from-Stage104 line unless a future analysis identifies a more source-selective version.
					- 2026-06-21 Stage107 plan: run a very low-weight inference-only interpolation between Stage104 `step-128.pt` and Stage98 `step-125.pt`. Stage98 has the strongest recent Libri-clean/Libri-other repair but worse CommonVoice/Wenet, while Stage104 has the best current CER/guard balance. Because Stage99 showed `25%` Stage98 over Stage95 already overtrades, start with only `6.25%` Stage98 over Stage104 under `/media/usbhd/rwkvasr_runs/stage107_stage104_stage98_tiny_interp_b1_guard_20260621`; evaluate fixed-code `batch_size=1` LibriSpeech-other/CommonVoice EN/WenetSpeech. Continue to `12.5%` or full public only if the low-weight candidate improves LibriSpeech-other without any CV/Wenet regression versus Stage104 within rounding tolerance. This is checkpoint averaging only and does not change the BiRWKV/RWKV-7 TimeMixer architecture or decode policy.
					- 2026-06-21 Stage107 result: `6.25%` Stage98 over Stage104 produced identical fixed-code public-200 guard outputs to Stage104 on all three screened datasets: LibriSpeech-other `36.766940 / 23.628147`, CommonVoice EN `51.621339 / 34.878344`, and WenetSpeech `47.798552 / 49.864662`. Because there is no LibriSpeech-other improvement, do not continue to `12.5%` or full public, and delete the temporary soup checkpoint after preserving predictions/metrics. Stage104 remains the current CER/guard-balanced checkpoint.
					- 2026-06-21 Stage108 plan: expand the LibriSpeech teacher-label signal instead of reusing the saturated Stage39/104 Libri cache. Build a new non-public LibriSpeech-only teacher pipeline without changing the BiRWKV/RWKV-7 TimeMixer architecture, frontend, tokenizer, CTC head, objective, direction policy, or decode policy. Sample a fresh LibriSpeech train candidate set from the Stage22 curriculum root, excluding public/selector ids and all prior accepted teacher ids, initially `4096` unique rows. Decode candidates with the current Stage104 `step-128` checkpoint using fixed `mode=bi`, `beam8/topk16`, and CTC normalization, then select about `1024` high student-error rows for FunASR-Nano teacher decoding. Accept only rows where teacher text is closer to the dataset reference than the Stage104 prediction under normalized WER/CER and teacher error remains low; write both an accepted override cache and matching length index. If the accepted cache is meaningfully larger than the existing `765` Libri teacher rows, build a Stage108 training mix from Stage104 with expanded Libri teacher overrides plus original-label CommonVoice/Wenet/AISHELL/Giga guards, hidden+CTC-logit anchoring to Stage104, very low LR, no SpecAugment, no gradient checkpointing, and no direction dropout. Promotion gate remains fixed-code public-200 LibriSpeech-other/CommonVoice EN/WenetSpeech first, then full public-200 only for a passing checkpoint.
					- 2026-06-21 Stage108 implementation start: added `scripts/build_stage108_libri_teacher_cache.py` with reproducible subcommands for Libri candidate sampling, Stage104 student prediction scoring, audio extraction from WebDataset offsets, FunASR-Nano teacher decoding, and conservative teacher-cache filtering. A smoke run under `/media/usbhd/rwkvasr_runs/stage108_expanded_libri_teacher_smoke_20260621` passed end to end: `32` sampled non-public Libri candidates, Stage104 CTC predictions generated with `mode=bi/beam8/topk16`, top `8` targets extracted from tar offsets, FunASR decoded all `8`, and filtering accepted `6` rows into a teacher override cache. This verifies the Stage108 data path before launching the full `4096`-candidate pass.
					- 2026-06-21 Stage108 formal teacher-cache result and training plan: the full Stage108 cache pipeline under `/media/usbhd/rwkvasr_runs/stage108_expanded_libri_teacher_20260621` decoded `4096` fresh non-public LibriSpeech train candidates with Stage104, selected `1024` high-error rows, extracted audio from WebDataset offsets, and ran FunASR-Nano teacher decoding on four GPUs. Conservative filtering accepted `1002` LibriSpeech rows and rejected only `22` rows (`21` by teacher WER, `1` by teacher CER). Accepted rows averaged Stage104 student `0.4016` WER / `0.2341` CER versus teacher `0.0191` WER / `0.0079` CER against the normalized dataset reference, so the cache is meaningfully larger and cleaner than the previous `765` unique Stage104 sampled teacher ids. For the next same-architecture training slice, generalize the Stage104 data builder only enough to emit Stage108 audit fields, then build `stage108_stage104_expanded_libri_teacher_guard_repair` with `162k` rows: `10k` weighted repeated LibriSpeech teacher rows from this new accepted cache plus the same original-label guard quotas as Stage104 (`45k` LibriSpeech, `35k` CommonVoice EN, `35k` WenetSpeech, `20k` AISHELL3, `10k` CommonVoice CN, `7k` GigaSpeech). Train from Stage104 `step-128.pt` with Stage104 as hidden and CTC-logit anchor (`0.10 / 0.10`, chunk frames `16`), LR `5e-10`, `max_steps=128`, `save_every=64`, ZeRO-1, batch size `12`, grad accumulation `4`, no SpecAugment, no gradient checkpointing, and no direction dropout. This is a data-label repair only; do not change the BiRWKV/RWKV-7 TimeMixer architecture, bidirectional wrapper, frontend, tokenizer, CTC head, or decode policy.
					- 2026-06-21 Stage108 preparation: generalized `scripts/build_stage104_libri_teacher_guard_repair.py` with `--stage-name` and `--audit-prefix` so the same linked-index builder can emit Stage108 audit fields without changing the data-loading or model path. Generated `/media/usbhd/training_data/asr/curriculum/stage22_stage21_soup_clean_repair_mix/stages/stage108_stage104_expanded_libri_teacher_guard_repair/webdataset_lengths.jsonl`, `webdataset_buckets_audio_text/manifest.json`, and `stage108_libri_teacher_sampled.cache.jsonl`. The slice has `162,000` rows: `152,000` original-label guards and `10,000` weighted LibriSpeech teacher-substitution rows. Source totals are LibriSpeech `55k` including teacher rows, CommonVoice EN `35k`, WenetSpeech `35k`, AISHELL3 `20k`, CommonVoice CN `10k`, and GigaSpeech `7k`; WenetSpeech needed `5,032` replacement-fill rows. The sampled override cache contains `948` unique LibriSpeech ids, no non-Libri rows, and no missing length rows. Added `configs/generated/sensevoice_rwkv_stage108_stage104_expanded_libri_teacher_guard_repair_4x4090_deepspeed.yaml`; config parsing verifies Stage104 `step-128.pt` as both init and anchor, hidden/logit anchor `0.10 / 0.10`, LR `5e-10`, `max_steps=128`, `save_every=64`, `top_k_step_checkpoints=2`, no SpecAugment, no gradient checkpointing, and `direction_variant=none`.
					- 2026-06-21 Stage108 result and Stage109 plan: Stage108 training completed normally to `step=128` under `/media/usbhd/rwkvasr_runs/sensevoice_rwkv_stage108_stage104_expanded_libri_teacher_guard_repair_bs12_lr5e10_hid0p10_kl0p10_zero1_nockpt_noaug_nodirdrop_4x4090`, preserving `step-64.pt` and `step-128.pt`; final epoch train loss was `2.1667`, final rank-0 loss `2.3025`, and final hidden/logit anchor losses were `0.7587 / 0.0453`. First-step peak reserved memory was about `11.41 GiB`; deleted only the run's `ds_checkpoints/` after verifying top-level `.pt` exports. Fixed-code public-200 guard under `/media/usbhd/rwkvasr_runs/stage108_stage104_expanded_libri_teacher_guard_b1_guard_20260621` failed promotion. `step-64` measured LibriSpeech-other `36.7669 / 23.6425`, CommonVoice EN `51.7259 / 34.9334`, and WenetSpeech `47.8287 / 49.8947`. `step-128` measured LibriSpeech-other `36.7366 / 23.5995`, CommonVoice EN `51.7782 / 34.9884`, and WenetSpeech `47.8287 / 49.8947`. Compared with Stage104 `step-128`, Stage108 step-128 gives the intended small Libri-other repair but overtrades CommonVoice EN and WenetSpeech, so do not run AISHELL/Libri-clean from raw Stage108. Next run Stage109 as inference-only interpolation between Stage104 `step-128.pt` and Stage108 `step-128.pt`, testing small Stage108 weights first (`0.25`, then `0.50` only if needed) with the same fixed `batch_size=1`, `mode=bi`, `beam8/topk16`, zero length/insertion/blank biases, and CTC normalization. Promote to full public-200 only if a soup keeps Stage104's CommonVoice/Wenet guard while improving LibriSpeech-other; otherwise stop this expanded-Libri-teacher line or reduce teacher sampling pressure in a later data recipe.
					- 2026-06-21 Stage109 result: inference-only Stage104/Stage108 interpolation did not produce a better Pareto point. `25%` Stage108 over Stage104 produced exactly identical fixed-code public-200 guard outputs to Stage104 on LibriSpeech-other, CommonVoice EN, and WenetSpeech. `50%` Stage108 over Stage104 measured LibriSpeech-other `36.7669 / 23.6210`, CommonVoice EN `51.6736 / 34.9334`, and WenetSpeech `47.7986 / 49.8647`: this only gives a tiny Libri CER gain with unchanged Libri WER while worsening CommonVoice EN. Do not continue to `75%`; delete the temporary Stage109 soup `.pt` files after preserving `.soup.json`, predictions, and metrics. The expanded-Libri-teacher direction is useful diagnostically but not sufficient by itself; the next same-architecture data signal should add CommonVoice/Wenet teacher or guard repair instead of pushing more Libri substitutions.
					- 2026-06-21 Stage110 plan: generalize the Stage108 teacher-cache pipeline from LibriSpeech-only to source-aware train-set teacher mining for CommonVoice EN and WenetSpeech, without changing the BiRWKV/RWKV-7 TimeMixer architecture, bidirectional wrapper, frontend, tokenizer, CTC head, objective, direction policy, or decode policy. Extend `scripts/build_stage108_libri_teacher_cache.py` only at the data-pipeline level: add explicit `--source`, source-specific metric language (`en` for CommonVoice EN, `zh` for WenetSpeech), source-specific artifact prefixes, and dynamic audit fields while preserving the existing Stage108 Libri defaults. First run a small smoke for each source, then a formal candidate pass using Stage104 `step-128.pt` predictions with the same fixed `mode=bi`, `beam8/topk16`, zero length/insertion/blank biases, and CTC normalization. Select high student-error train rows, decode them with FunASR-Nano, and accept only teacher texts that improve normalized WER/CER over the Stage104 prediction with low teacher error. Do not start a Stage110 training mix until the accepted CommonVoice/Wenet cache sizes and quality stats are recorded here; the intended next training slice, if caches are useful, is a CV/Wenet teacher-guard repair from Stage104 with Stage104 hidden+CTC-logit anchoring and no architecture change.
					- 2026-06-21 Stage110 implementation/smoke: generalized `scripts/build_stage108_libri_teacher_cache.py` for source-aware candidate sampling, scoring, audio extraction, and filtering while preserving the Stage108 Libri defaults. Syntax checks passed. Smoke under `/media/usbhd/rwkvasr_runs/stage110_cv_wenet_teacher_smoke_20260621` verified end to end for CommonVoice EN and WenetSpeech: each source sampled `8` train candidates, decoded them with Stage104 `step-128.pt`, selected top `4` high-error rows, extracted audio from WebDataset offsets, ran FunASR-Nano teacher decoding, and filtered accepted caches. CommonVoice EN accepted `2/4` rows, rejecting `2` by teacher WER, with accepted average student `0.5000 / 0.3492` WER/CER and teacher `0.0000 / 0.0000`. WenetSpeech accepted `4/4` rows with accepted average student `0.7565 / 0.7792` and teacher `0.0227 / 0.0227`. Proceed to a formal Stage110 cache pass with `2048` fresh candidates per source, `512` teacher targets per source, fixed Stage104 `mode=bi/beam8/topk16` CTC predictions, FunASR-Nano teacher decoding, and the same conservative filters (`min_improvement=0.10`, `max_teacher_wer=0.20`, `max_teacher_cer=0.15`).
					- 2026-06-21 Stage110 formal cache result and training plan: formal source-aware teacher mining under `/media/usbhd/rwkvasr_runs/stage110_cv_wenet_teacher_20260621` sampled and decoded `2048` train candidates each for CommonVoice EN and WenetSpeech with Stage104 `step-128.pt`, then selected `512` high-error teacher targets per source. CommonVoice EN accepted `389/512` teacher rows, rejecting `120` by teacher WER and `3` by teacher CER; accepted rows averaged student `0.7377 / 0.4447` WER/CER versus teacher `0.0454 / 0.0188`. WenetSpeech accepted `380/512`, rejecting `95` by teacher WER and `37` by teacher CER; accepted rows averaged student `0.7862 / 0.8083` versus teacher `0.0207 / 0.0207`. For the next same-architecture training slice, build a multi-source teacher guard mix `stage110_stage104_multi_teacher_guard_repair` from Stage104 with the same original-label guard quotas as Stage104 (`45k` LibriSpeech, `35k` CommonVoice EN, `35k` WenetSpeech, `20k` AISHELL3, `10k` CommonVoice CN, `7k` GigaSpeech) plus repeated teacher rows: `4k` LibriSpeech from the Stage108 accepted cache, `4k` CommonVoice EN from the Stage110 accepted cache, and `4k` WenetSpeech from the Stage110 accepted cache. This keeps teacher pressure balanced across the three currently gated sources instead of pushing Libri alone. Train from Stage104 `step-128.pt` with Stage104 hidden+CTC-logit anchor (`0.10 / 0.10`, chunk frames `16`), LR `5e-10`, `max_steps=128`, `save_every=64`, ZeRO-1, batch size `12`, grad accumulation `4`, no SpecAugment, no gradient checkpointing, and no direction dropout. Gate first on fixed-code public-200 LibriSpeech-other/CommonVoice EN/WenetSpeech.
					- 2026-06-21 Stage110 preparation: extended `scripts/build_stage104_libri_teacher_guard_repair.py` with repeatable `--teacher-spec source:samples:cache:lengths` support so one linked-index stage can combine LibriSpeech, CommonVoice EN, and WenetSpeech teacher substitutions while preserving the legacy single-Libri default. Generated `/media/usbhd/training_data/asr/curriculum/stage22_stage21_soup_clean_repair_mix/stages/stage110_stage104_multi_teacher_guard_repair/webdataset_lengths.jsonl`, `webdataset_buckets_audio_text/manifest.json`, and `stage110_teacher_sampled.cache.jsonl`. The slice has `164,000` rows: `152,000` original-label guards plus `4,000` weighted repeated teacher rows each for LibriSpeech, CommonVoice EN, and WenetSpeech. Source totals are LibriSpeech `49k`, CommonVoice EN `39k`, WenetSpeech `39k`, AISHELL3 `20k`, CommonVoice CN `10k`, and GigaSpeech `7k`; WenetSpeech needed `5,412` replacement-fill guard rows. The sampled override cache contains `1,683` unique teacher ids across LibriSpeech `919`, CommonVoice EN `384`, and WenetSpeech `380`, with no missing length rows and no unexpected sources. Added `configs/generated/sensevoice_rwkv_stage110_stage104_multi_teacher_guard_repair_4x4090_deepspeed.yaml`; config parsing verifies Stage104 `step-128.pt` as both init and anchor, hidden/logit anchor `0.10 / 0.10`, LR `5e-10`, `max_steps=128`, `save_every=64`, `top_k_step_checkpoints=2`, no SpecAugment, no gradient checkpointing, and `direction_variant=none`.
					- 2026-06-21 Stage110 result: training completed normally to `step=128` under `/media/usbhd/rwkvasr_runs/sensevoice_rwkv_stage110_stage104_multi_teacher_guard_repair_bs12_lr5e10_hid0p10_kl0p10_zero1_nockpt_noaug_nodirdrop_4x4090`, preserving `step-64.pt`, `step-128.pt`, and `best.pt`; final rank-0 loss was `1.7291`, epoch train loss `2.2365`, and final logged anchor losses were hidden `0.8238` and CTC-logit KL `0.0734`. First-step peak reserved memory was about `14.50 GiB`; deleted only the run's `ds_checkpoints/` after verifying top-level `.pt` exports. Fixed-code public-200 guard under `/media/usbhd/rwkvasr_runs/stage110_stage104_multi_teacher_guard_b1_guard_20260621` passed. `step-64` measured LibriSpeech-other `36.7669 / 23.6210`, CommonVoice EN `51.6213 / 34.8783`, and WenetSpeech `47.7986 / 49.8647`, effectively matching Stage104. `step-128` measured LibriSpeech-other `36.7669 / 23.5995`, CommonVoice EN `51.6213 / 34.8783`, and WenetSpeech `47.7986 / 49.8647`, so it kept the guard while improving LibriSpeech-other CER. Full fixed-code public-200 for `step-128` measured macro `37.6356 / 30.3030`: AISHELL `29.9489 / 29.9489`, Libri-clean `22.0425 / 13.2238`, Libri-other `36.7669 / 23.5995`, CommonVoice EN `51.6213 / 34.8783`, and Wenet `47.7986 / 49.8647`. This improves Stage104 macro by about `-0.0169 / -0.0183` and also beats the Stage80A WER-primary macro `37.6484 / 30.3684`, making Stage110 the new public-200 WER/CER incumbent. Mark `/media/usbhd/rwkvasr_runs/sensevoice_rwkv_stage110_stage104_multi_teacher_guard_repair_bs12_lr5e10_hid0p10_kl0p10_zero1_nockpt_noaug_nodirdrop_4x4090/wercer_best.pt` as a symlink to `step-128.pt`.
					- 2026-06-21 Stage111 plan: run a no-training interpolation screen between Stage110 `wercer_best.pt` and the Stage80A WER-primary checkpoint to test whether Stage80A's stronger CommonVoice EN behavior can be recovered without losing Stage110's LibriSpeech/WenetSpeech guard. This is an inference-only checkpoint soup and does not change the BiRWKV/RWKV-7 TimeMixer architecture, bidirectional wrapper, frontend, tokenizer, CTC head, objective, direction policy, or fixed public-200 decode policy. Generate candidates with Stage80A weights `0.10` and `0.25` under `/media/usbhd/rwkvasr_runs/stage111_stage110_stage80a_interp_b1_guard_20260621`, preserving Stage110 metadata/config files. Evaluate only fixed-code public-200 LibriSpeech-other/CommonVoice EN/WenetSpeech first; run AISHELL/Libri-clean and consider promotion only if a candidate improves the guard macro or CommonVoice EN while not regressing LibriSpeech-other or WenetSpeech versus Stage110 within rounding tolerance.
					- 2026-06-21 Stage111 result: Stage110/Stage80A interpolation did not improve the fixed public-200 guard. The `10%` Stage80A candidate measured LibriSpeech-other `36.7669 / 23.6140`, CommonVoice EN `51.6213 / 34.8783`, and WenetSpeech `47.7986 / 49.8647`, effectively matching Stage110 but losing the small Stage110 Libri CER gain. The `25%` Stage80A candidate measured LibriSpeech-other `36.8277 / 23.6497`, CommonVoice EN `51.6213 / 34.9004`, and WenetSpeech `47.7986 / 49.8647`, so it regressed Libri/CV and still did not recover Stage80A's CommonVoice WER advantage. Do not run full public-200 for these soups; delete the temporary `.pt` candidates after preserving `.soup.json`, predictions, and metrics. Keep Stage110 `wercer_best.pt` as the incumbent.
					- 2026-06-21 Stage112 plan: continue same-architecture data-label repair from the Stage110 incumbent instead of mixing with older checkpoints. Build `stage112_stage110_multi_teacher_guard6k` with the same original-label guard quotas as Stage110 (`45k` LibriSpeech, `35k` CommonVoice EN, `35k` WenetSpeech, `20k` AISHELL3, `10k` CommonVoice CN, `7k` GigaSpeech), but increase balanced teacher substitution pressure to `6k` repeated rows each for LibriSpeech, CommonVoice EN, and WenetSpeech from the existing Stage108/110 accepted teacher caches (`18k` teacher rows total, `170k` rows overall). Train from Stage110 `wercer_best.pt` with Stage110 as hidden+CTC-logit anchor (`0.10 / 0.10`, chunk frames `16`), LR `5e-10`, `max_steps=128`, `save_every=64`, ZeRO-1, batch size `12`, grad accumulation `4`, no SpecAugment, no gradient checkpointing, and no direction dropout. This still changes only data sampling/labels and training continuation; do not change the BiRWKV/RWKV-7 TimeMixer architecture, frontend, tokenizer, CTC head, objective, direction policy, or fixed public-200 decode policy. Gate first on LibriSpeech-other/CommonVoice EN/WenetSpeech; run full public-200 only if the guard does not regress Stage110.
					- 2026-06-21 Stage112 preparation: generated `/media/usbhd/training_data/asr/curriculum/stage22_stage21_soup_clean_repair_mix/stages/stage112_stage110_multi_teacher_guard6k/webdataset_lengths.jsonl`, `webdataset_buckets_audio_text/manifest.json`, and `stage112_teacher_sampled.cache.jsonl`. The slice has `170,000` rows: `152,000` original-label guards plus `6,000` weighted repeated teacher rows each for LibriSpeech, CommonVoice EN, and WenetSpeech. Source totals are LibriSpeech `51k`, CommonVoice EN `41k`, WenetSpeech `41k`, AISHELL3 `20k`, CommonVoice CN `10k`, and GigaSpeech `7k`; WenetSpeech again needed `5,412` replacement-fill guard rows. The sampled override cache contains `1,704` unique teacher ids across LibriSpeech `940`, CommonVoice EN `384`, and WenetSpeech `380`, with no missing length rows and no unexpected sources. Added `configs/generated/sensevoice_rwkv_stage112_stage110_multi_teacher_guard6k_4x4090_deepspeed.yaml`; config parsing verifies Stage110 `wercer_best.pt` as both init and anchor, hidden/logit anchor `0.10 / 0.10`, LR `5e-10`, `max_steps=128`, `save_every=64`, `top_k_step_checkpoints=2`, no SpecAugment, no gradient checkpointing, and `direction_variant=none`.
					- 2026-06-21 Stage112 result: training completed normally to `step=128` under `/media/usbhd/rwkvasr_runs/sensevoice_rwkv_stage112_stage110_multi_teacher_guard6k_bs12_lr5e10_hid0p10_kl0p10_zero1_nockpt_noaug_nodirdrop_4x4090`, preserving `step-64.pt`, `step-128.pt`, and `best.pt`; final rank-0 loss was `2.1726`, epoch train loss `2.2860`, and final logged anchor losses were hidden `0.8611` and CTC-logit KL `0.0956`. First-step peak reserved memory was about `11.12 GiB`; deleted only the run's `ds_checkpoints/` after verifying top-level `.pt` exports. Fixed-code public-200 guard under `/media/usbhd/rwkvasr_runs/stage112_stage110_multi_teacher_guard6k_b1_guard_20260621` failed promotion. `step-64` measured LibriSpeech-other `36.7669 / 23.6210`, CommonVoice EN `51.6736 / 35.0104`, and WenetSpeech `47.7986 / 49.8647`, showing early CommonVoice regression. `step-128` measured LibriSpeech-other `36.7973 / 23.6497`, CommonVoice EN `51.6736 / 35.0000`, and WenetSpeech `47.7684 / 49.8346`: Wenet improves slightly, but LibriSpeech-other and CommonVoice EN regress versus Stage110. Do not run full public-200 and do not promote Stage112; keep Stage110 `wercer_best.pt` as incumbent. The balanced `6k` teacher pressure is too high for the current anchor/learning-rate recipe.
					- 2026-06-21 Stage113 plan: run a no-training interpolation screen between Stage110 `wercer_best.pt` and Stage112 `step-128.pt` to test whether the small Stage112 Wenet gain can be retained without the Stage112 LibriSpeech/CommonVoice regression. This is an inference-only checkpoint soup and does not change the BiRWKV/RWKV-7 TimeMixer architecture, bidirectional wrapper, frontend, tokenizer, CTC head, objective, direction policy, or fixed public-200 decode policy. Generate candidates with Stage112 weights `0.25` and `0.50` under `/media/usbhd/rwkvasr_runs/stage113_stage110_stage112_interp_b1_guard_20260621`, preserving Stage110 metadata/config files. Evaluate only fixed-code public-200 LibriSpeech-other/CommonVoice EN/WenetSpeech first; run full public-200 only if a candidate keeps Stage110's Libri/CV guard while improving Wenet.
					- 2026-06-21 Stage113 result: Stage110/Stage112 interpolation did not produce a better Pareto point. The `25%` Stage112 candidate measured LibriSpeech-other `36.7669 / 23.5995`, CommonVoice EN `51.6213 / 34.8783`, and WenetSpeech `47.7986 / 49.8647`, identical to Stage110 and therefore not worth full public-200. The `50%` Stage112 candidate measured LibriSpeech-other `36.7669 / 23.6140`, CommonVoice EN `51.6213 / 34.9334`, and WenetSpeech `47.8287 / 49.8947`, losing CV/Wenet without improving Libri. Delete the temporary `.pt` candidates after preserving `.soup.json`, predictions, and metrics. Keep Stage110 `wercer_best.pt` as incumbent; do not continue the Stage112 stronger-teacher line without a different guard/anchor design.
					- 2026-06-21 Stage114 plan: expand CommonVoice EN teacher-label coverage with fresh high-error train samples before attempting another training slice. Stage110 improved the public-200 macro but the current CommonVoice teacher cache has only `389` accepted rows, and Stage112 showed that simply repeating the same CV rows more often causes guard regression. Use the source-aware Stage108/110 teacher mining pipeline with Stage110 `wercer_best.pt` as the student checkpoint, fixed `mode=bi`, `beam8/topk16`, zero length/insertion/blank biases, and CTC normalization. Sample `4096` new CommonVoice EN train candidates from the Stage22 curriculum root, excluding public/selector ids and the prior Stage110 accepted CV teacher cache, decode them with Stage110, select `1024` high normalized WER targets, run FunASR-Nano teacher decoding, and conservatively accept only rows where teacher text improves over the Stage110 prediction with `min_improvement=0.10`, `max_teacher_wer=0.20`, and `max_teacher_cer=0.15`. Do not start training until the accepted count and quality stats are recorded here. This is a data-label mining step only and does not change the BiRWKV/RWKV-7 TimeMixer architecture, bidirectional wrapper, frontend, tokenizer, CTC head, objective, direction policy, or fixed public-200 decode policy.
					- 2026-06-21 Stage114 cache result and training plan: expanded CommonVoice EN teacher mining under `/media/usbhd/rwkvasr_runs/stage114_cv_teacher_expand_20260621/commonvoice_en` completed with Stage110 `wercer_best.pt` as the student. Candidate sampling produced `4096` train rows (`3942` unique utt ids) while excluding `640` ids from selector/prior teacher caches and skipping `1610` excluded candidate rows plus `2` over-length rows. Stage110 decoded all four `1024`-row parts with fixed `mode=bi`, `beam8/topk16`, zero biases, and CTC normalization. Student scoring selected `1024` high-error targets with average primary WER `0.7377` (`min=0.5625`, `max=2.0`), extracted all target audio from WebDataset offsets, and FunASR-Nano teacher decoded all `1024` rows on four GPUs. Conservative filtering accepted `779/1024` rows, rejecting `238` by teacher WER and `7` by teacher CER; accepted rows averaged Stage110 student `0.7213 / 0.4361` WER/CER versus teacher `0.0424 / 0.0160`. This is a meaningful fresh CV expansion over the previous `389` accepted Stage110 CV rows. For the next same-architecture training slice, build a conservative CV-only repair mix `stage114_stage110_cv_teacher_expand_guard` from Stage110 with the same original-label guard quotas as Stage110 (`45k` LibriSpeech, `35k` CommonVoice EN, `35k` WenetSpeech, `20k` AISHELL3, `10k` CommonVoice CN, `7k` GigaSpeech), plus `4k` weighted repeated CommonVoice EN teacher rows from this new Stage114 accepted cache only. Do not add extra Libri/Wenet teacher pressure in this slice because Stage112 showed stronger balanced teacher pressure regresses the guard. Train from Stage110 `wercer_best.pt` with Stage110 hidden+CTC-logit anchor (`0.10 / 0.10`, chunk frames `16`), LR `5e-10`, `max_steps=128`, `save_every=64`, ZeRO-1, batch size `12`, grad accumulation `4`, no SpecAugment, no gradient checkpointing, and no direction dropout. Gate first on fixed-code public-200 LibriSpeech-other/CommonVoice EN/WenetSpeech; run full public-200 only if CommonVoice improves or ties and Libri/Wenet do not regress versus Stage110.
					- 2026-06-21 Stage114 preparation: generated `/media/usbhd/training_data/asr/curriculum/stage22_stage21_soup_clean_repair_mix/stages/stage114_stage110_cv_teacher_expand_guard/webdataset_lengths.jsonl`, `webdataset_buckets_audio_text/manifest.json`, and `stage114_teacher_sampled.cache.jsonl`. The slice has `156,000` rows: `152,000` original-label guards plus `4,000` weighted repeated CommonVoice EN teacher rows from the new Stage114 cache only. Source totals are LibriSpeech `45k`, CommonVoice EN `39k`, WenetSpeech `35k`, AISHELL3 `20k`, CommonVoice CN `10k`, and GigaSpeech `7k`; WenetSpeech needed `5,032` replacement-fill guard rows. The sampled override cache contains `738` unique CommonVoice EN teacher ids, with no missing length rows and no unexpected sources. This keeps Libri/Wenet teacher pressure at zero for the slice while testing whether new CV coverage can repair CommonVoice without harming the guard.
					- 2026-06-21 Stage114 result: training completed normally to `step=128` under `/media/usbhd/rwkvasr_runs/sensevoice_rwkv_stage114_stage110_cv_teacher_expand_guard_bs12_lr5e10_hid0p10_kl0p10_zero1_nockpt_noaug_nodirdrop_4x4090`, preserving `step-64.pt`, `step-128.pt`, and `best.pt`; final rank-0 loss was `1.6403`, epoch train loss `2.1397`, and final logged anchor losses were hidden `0.8294` and CTC-logit KL `0.0661`. First-step peak reserved memory was about `12.73 GiB`; deleted only the run's `ds_checkpoints/` after verifying top-level `.pt` exports. Fixed-code public-200 guard under `/media/usbhd/rwkvasr_runs/stage114_stage110_cv_teacher_expand_guard_b1_guard_20260621` failed promotion. `step-64` measured LibriSpeech-other `36.7669 / 23.6140`, CommonVoice EN `51.6213 / 34.9334`, and WenetSpeech `47.8287 / 49.8947`, already worse than Stage110 on CV CER and Wenet. `step-128` measured LibriSpeech-other `36.7973 / 23.6210`, CommonVoice EN `51.7259 / 35.0104`, and WenetSpeech `47.7684 / 49.8346`: Wenet improves slightly, but LibriSpeech-other and CommonVoice EN regress substantially. Do not run full public-200 and do not promote Stage114; keep Stage110 `wercer_best.pt` as incumbent. Diagnostic conclusion: the fresh CV teacher cache is high quality against train references, but direct CTC label-substitution fine-tuning does not transfer to public CommonVoice and appears to introduce domain/label-distribution shift under this anchor recipe.
					- 2026-06-21 Stage115 plan: run a no-training interpolation screen between Stage110 `wercer_best.pt` and Stage114 `step-128.pt` to test whether Stage114's small Wenet improvement can be isolated without its LibriSpeech/CommonVoice regression. This is an inference-only checkpoint soup and does not change the BiRWKV/RWKV-7 TimeMixer architecture, bidirectional wrapper, frontend, tokenizer, CTC head, objective, direction policy, or fixed public-200 decode policy. Generate candidates with Stage114 weights `0.25` and `0.50` under `/media/usbhd/rwkvasr_runs/stage115_stage110_stage114_interp_b1_guard_20260621`, preserving Stage110 metadata/config files. Evaluate only fixed-code public-200 LibriSpeech-other/CommonVoice EN/WenetSpeech first; run full public-200 only if a candidate keeps Stage110's Libri/CV guard while improving Wenet.
					- 2026-06-21 Stage115 result: Stage110/Stage114 interpolation did not produce a better Pareto point. The `25%` Stage114 candidate measured LibriSpeech-other `36.7669 / 23.5995`, CommonVoice EN `51.6213 / 34.8783`, and WenetSpeech `47.8287 / 49.8947`: Libri/CV match Stage110 but Wenet regresses, so no full public-200. The `50%` Stage114 candidate measured LibriSpeech-other `36.7973 / 23.6425`, CommonVoice EN `51.6213 / 34.8783`, and WenetSpeech `47.8287 / 49.8947`, regressing Libri and Wenet. Delete the temporary `.pt` candidates after preserving `.soup.json`, predictions, and metrics. Keep Stage110 `wercer_best.pt` as incumbent. The Stage114 CV teacher signal should not be used as direct substitution under the current CTC-only recipe; a future attempt should treat it as a diagnostic set or a softer auxiliary/selection signal rather than replacing labels in the main training stream.
					- 2026-06-21 Stage116 plan: test whether the Stage114 CommonVoice signal is useful only as exact hard replay rather than noisy label substitution. A cache diagnosis found `523/779` Stage114 accepted CV rows where teacher WER and teacher CER are both exactly `0` after CTC metric normalization; these rows have high Stage110 student error but teacher text is metric-equivalent to the dataset reference, so they should avoid the label-distribution shift seen in Stage114. Build a stricter Stage116 cache from this exact subset and a conservative `stage116_stage110_cv_exact_hard_replay` mix with the same original-label guard quotas as Stage110 plus only `2k` weighted repeated CommonVoice EN exact-teacher rows. Train from Stage110 `wercer_best.pt` with Stage110 hidden+CTC-logit anchor (`0.10 / 0.10`, chunk frames `16`), LR `5e-10`, `max_steps=128`, `save_every=64`, ZeRO-1, batch size `12`, grad accumulation `4`, no SpecAugment, no gradient checkpointing, and no direction dropout. This remains a data-selection/label-safety test only; do not change the BiRWKV/RWKV-7 TimeMixer architecture, bidirectional wrapper, frontend, tokenizer, CTC head, objective, direction policy, or fixed public-200 decode policy. Gate first on fixed-code public-200 LibriSpeech-other/CommonVoice EN/WenetSpeech; run full public-200 only if CommonVoice improves or ties and Libri/Wenet do not regress versus Stage110.
					- 2026-06-21 Stage116 preparation: filtered `/media/usbhd/rwkvasr_runs/stage114_cv_teacher_expand_20260621/commonvoice_en/stage114_commonvoice_en_teacher_accepted.cache.jsonl` to an exact-teacher subset under `/media/usbhd/rwkvasr_runs/stage116_cv_exact_teacher_20260621/commonvoice_en`, producing `523` cache/length rows (`506` unique utt ids) with average Stage110 student `0.7314 / 0.4369` WER/CER and teacher `0.0000 / 0.0000`. Generated `/media/usbhd/training_data/asr/curriculum/stage22_stage21_soup_clean_repair_mix/stages/stage116_stage110_cv_exact_hard_replay/webdataset_lengths.jsonl`, `webdataset_buckets_audio_text/manifest.json`, and `stage116_teacher_sampled.cache.jsonl`. The slice has `154,000` rows: `152,000` original-label guards plus `2,000` weighted repeated CommonVoice EN exact hard-replay rows. Source totals are LibriSpeech `45k`, CommonVoice EN `37k`, WenetSpeech `35k`, AISHELL3 `20k`, CommonVoice CN `10k`, and GigaSpeech `7k`; WenetSpeech needed `5,032` replacement-fill guard rows. The sampled override cache contains `492` unique CommonVoice EN teacher ids, all with teacher WER/CER exactly `0`, no missing length rows, and no unexpected sources.
					- 2026-06-21 Stage116 result: training completed normally to `step=128` under `/media/usbhd/rwkvasr_runs/sensevoice_rwkv_stage116_stage110_cv_exact_hard_replay_bs12_lr5e10_hid0p10_kl0p10_zero1_nockpt_noaug_nodirdrop_4x4090`, preserving `step-64.pt`, `step-128.pt`, and `best.pt`; final rank-0 loss was `1.6439`, epoch train loss `2.1205`, and final logged anchor losses were hidden `0.8192` and CTC-logit KL `0.0600`. First-step peak reserved memory was about `14.14 GiB`; deleted only the run's `ds_checkpoints/` after verifying top-level `.pt` exports. Fixed-code public-200 guard under `/media/usbhd/rwkvasr_runs/stage116_stage110_cv_exact_hard_replay_b1_guard_20260621` failed promotion. `step-64` measured LibriSpeech-other `36.7669 / 23.6140`, CommonVoice EN `51.6213 / 34.9444`, and WenetSpeech `47.8287 / 49.8947`, already losing CV CER and Wenet. `step-128` measured LibriSpeech-other `36.7973 / 23.6425`, CommonVoice EN `51.6736 / 34.9884`, and WenetSpeech `47.7986 / 49.8647`, regressing Libri/CV while returning Wenet to Stage110. Do not run full public-200 and do not promote Stage116; keep Stage110 `wercer_best.pt` as incumbent. Diagnostic conclusion: even exact CV hard replay selected from train does not improve public CommonVoice under this CTC-only low-LR recipe, indicating the current public CV error is likely not fixed by source-internal hard replay/teacher substitution alone.
					- 2026-06-21 Stage117 plan: switch the next same-architecture repair attempt from CommonVoice EN to AISHELL3 because Stage114/116 ruled out source-internal CV teacher replay under the current low-LR CTC recipe, while public AISHELL-1 remains one of the larger fixed-code public-200 errors. First mine a small AISHELL3 teacher cache using Stage110 `wercer_best.pt` as the student checkpoint, fixed `mode=bi`, `beam8/topk16`, zero length/insertion/blank biases, and CTC normalization. Sample `2048` AISHELL3 train candidates from the Stage22 curriculum root, split into four parts, decode them with Stage110, select up to `512` high-error targets, run FunASR-Nano teacher decoding, and accept only rows where teacher text improves the Stage110 prediction with `min_improvement=0.10`, `max_teacher_wer=0.20`, and `max_teacher_cer=0.15`. If accepted quality is sufficient, build `stage117_stage110_aishell3_teacher_guard` with the same original-label guard quotas as Stage110 (`45k` LibriSpeech, `35k` CommonVoice EN, `35k` WenetSpeech, `20k` AISHELL3, `10k` CommonVoice CN, `7k` GigaSpeech), plus only `2k` weighted repeated AISHELL3 teacher rows. Train from Stage110 `wercer_best.pt` with Stage110 hidden+CTC-logit anchor (`0.10 / 0.10`, chunk frames `16`), LR `5e-10`, `max_steps=128`, `save_every=64`, ZeRO-1, batch size `12`, grad accumulation `4`, no SpecAugment, no gradient checkpointing, and no direction dropout. This changes only source selection and labels; do not change the BiRWKV/RWKV-7 TimeMixer architecture, bidirectional wrapper, frontend, tokenizer, CTC head, objective, direction policy, or the fixed public-200 decode policy. Gate first on fixed-code public-200 AISHELL-1 plus LibriSpeech-other/CommonVoice EN/WenetSpeech; run full public-200 only if AISHELL improves and the guard sets do not regress versus Stage110.
					- 2026-06-21 Stage117 cache result and training decision: AISHELL3 teacher mining under `/media/usbhd/rwkvasr_runs/stage117_aishell3_teacher_20260621/aishell3` produced `2048` candidates (`2025` unique utt ids), all with recovered `text` from the source tar JSON and extracted audio manifests for decoding. Stage110 fixed-policy student decoding scored all candidates with average normalized error `0.2422`; the selected `512` high-error targets averaged `0.4750` error, with selected min/max `0.3333 / 1.0000`. FunASR-Nano teacher decoding completed on all `512` targets and conservative filtering accepted `452`, rejecting `33` by teacher WER and `27` by teacher CER. Accepted rows average Stage110 student `0.4656 / 0.4656` WER/CER versus teacher `0.0170 / 0.0170`, so this cache is high enough quality for the planned conservative Stage117 AISHELL3 repair slice. Proceed with the `2k` weighted repeated AISHELL3 teacher rows plus the original-label guard quotas from the Stage117 plan; keep the same architecture, objective, tokenizer, direction policy, and fixed public-200 decode policy.
					- 2026-06-21 Stage117 result: generated `/media/usbhd/training_data/asr/curriculum/stage22_stage21_soup_clean_repair_mix/stages/stage117_stage110_aishell3_teacher_guard/webdataset_lengths.jsonl`, `webdataset_buckets_audio_text/manifest.json`, and `stage117_teacher_sampled.cache.jsonl`. The slice has `154,000` rows: `152,000` original-label guards plus `2,000` weighted repeated AISHELL3 teacher rows; source totals are AISHELL3 `22k`, LibriSpeech `45k`, CommonVoice EN `35k`, WenetSpeech `35k`, CommonVoice CN `10k`, and GigaSpeech `7k`. The sampled override cache contains `435` unique AISHELL3 ids and no missing length rows. Training completed normally to `step=128` under `/media/usbhd/rwkvasr_runs/sensevoice_rwkv_stage117_stage110_aishell3_teacher_guard_bs12_lr5e10_hid0p10_kl0p10_zero1_nockpt_noaug_nodirdrop_4x4090`, preserving `step-64.pt`, `step-128.pt`, `epoch-1.pt`, and `best.pt`; final rank-0 loss was `2.0786`, epoch train loss `2.1334`, and final logged anchor losses were hidden `0.7906` and CTC-logit KL `0.0235`. First-step peak reserved memory was about `15.21 GiB`; deleted only this run's `ds_checkpoints/` after verifying top-level `.pt` exports. Fixed-code public-200 guard under `/media/usbhd/rwkvasr_runs/stage117_stage110_aishell3_teacher_guard_b1_guard_20260621` failed promotion. `step-64` measured AISHELL `30.02 / 30.02`, LibriSpeech-other `36.80 / 23.62`, CommonVoice EN `51.67 / 34.93`, and WenetSpeech `47.80 / 49.86`. `step-128` measured AISHELL `30.02 / 30.02`, LibriSpeech-other `36.80 / 23.62`, CommonVoice EN `51.73 / 35.05`, and WenetSpeech `47.80 / 49.86`. Against Stage110 (`AISHELL 29.9489`, Libri-other `36.7669 / 23.5995`, CommonVoice `51.6213 / 34.8783`, Wenet `47.7986 / 49.8647`), the AISHELL3 teacher replay regresses the targeted public AISHELL slice and also loses Libri/CV. Do not run full public-200 and do not promote Stage117; keep Stage110 `wercer_best.pt` as incumbent. Diagnostic conclusion: direct source-internal teacher/hard replay has now failed for both CommonVoice EN and AISHELL3 under the current low-LR CTC substitution recipe, so further direct per-source teacher substitution should require a materially different training objective or guard design.
					- 2026-06-21 Stage118 plan: run a no-training interpolation screen between Stage110 `wercer_best.pt` and the Stage117 AISHELL3 repair checkpoints to test whether the Stage117 weight-space direction contains any low-amplitude public-200 improvement despite failing at full strength. This is inference-only and keeps the BiRWKV/RWKV-7 TimeMixer architecture, bidirectional wrapper, frontend, tokenizer, CTC head, objective, direction policy, and fixed public-200 decode policy unchanged. Generate candidates with Stage117 `step-64.pt` and `step-128.pt` weights at `0.125`, `0.25`, and `0.50` under `/media/usbhd/rwkvasr_runs/stage118_stage110_stage117_interp_b1_guard_20260621`, preserving Stage110 metadata/config files. Evaluate only fixed-code public-200 AISHELL-1, LibriSpeech-other, CommonVoice EN, and WenetSpeech first; run full public-200 only if a candidate improves AISHELL and does not regress Libri/CV/Wenet versus Stage110 within rounding tolerance.
					- 2026-06-21 Stage118 result: generated six Stage110/Stage117 interpolation candidates under `/media/usbhd/rwkvasr_runs/stage118_stage110_stage117_interp_b1_guard_20260621`: Stage117 `step-64` and `step-128` weights `0.125`, `0.25`, and `0.50`. Because Stage118's promotion condition required first improving public-200 AISHELL-1, screened AISHELL-1 only before running the full guard. The best candidates (`s64_w0p125`, `s128_w0p125`, and `s128_w0p25`) measured exact AISHELL `0.2994889 / 0.2994889`, identical to Stage110, while the other candidates rounded to `29.98 / 29.98`; none improved AISHELL. Do not run Libri/CV/Wenet or full public-200 for these soups. Delete the temporary `.pt` soup candidates after preserving `.soup.json`, predictions, previews, and metrics. Keep Stage110 `wercer_best.pt` as incumbent.
					- 2026-06-21 Stage119 plan: run a same-architecture original-label guard-only continuation from Stage110 to separate "continued balanced exposure" from teacher-label substitution. Build `stage119_stage110_original_guard_only` by filtering the Stage117 length index down to its `152,000` `original_label_guard` rows, preserving the Stage110/117 original source quotas exactly (`45k` LibriSpeech, `35k` CommonVoice EN, `35k` WenetSpeech, `20k` AISHELL3, `10k` CommonVoice CN, `7k` GigaSpeech) and generating a fresh audio/text bucket manifest. Train from Stage110 `wercer_best.pt` with Stage110 hidden+CTC-logit anchor (`0.10 / 0.10`, chunk frames `16`), LR `5e-10`, `max_steps=128`, `save_every=64`, ZeRO-1, batch size `12`, grad accumulation `4`, no SpecAugment, no gradient checkpointing, no CTC label override cache, and no direction dropout. This changes only data selection by removing teacher rows; do not change the BiRWKV/RWKV-7 TimeMixer architecture, bidirectional wrapper, frontend, tokenizer, CTC head, objective, direction policy, or fixed public-200 decode policy. Gate first on fixed-code public-200 AISHELL-1, LibriSpeech-other, CommonVoice EN, and WenetSpeech; run full public-200 only if the guard macro improves without a dataset regression versus Stage110.
					- 2026-06-21 Stage119 result: filtered the Stage117 length index to `/media/usbhd/training_data/asr/curriculum/stage22_stage21_soup_clean_repair_mix/stages/stage119_stage110_original_guard_only/webdataset_lengths.jsonl` with `152,000` original-label rows and generated `webdataset_buckets_audio_text/manifest.json`. Source counts match the intended guard quotas exactly: LibriSpeech `45k`, CommonVoice EN `35k`, WenetSpeech `35k`, AISHELL3 `20k`, CommonVoice CN `10k`, and GigaSpeech `7k`. Training completed normally to `step=128` under `/media/usbhd/rwkvasr_runs/sensevoice_rwkv_stage119_stage110_original_guard_only_bs12_lr5e10_hid0p10_kl0p10_zero1_nockpt_noaug_nodirdrop_4x4090`, preserving `step-64.pt`, `step-128.pt`, `epoch-1.pt`, and `best.pt`; final rank-0 loss was `2.7132`, epoch train loss `2.1208`, final logged anchor losses were hidden `0.7931` and CTC-logit KL `0.0955`, and first-step peak reserved memory was about `8.17 GiB`. Deleted only this run's `ds_checkpoints/` after verifying top-level `.pt` exports. Fixed-code public-200 guard under `/media/usbhd/rwkvasr_runs/stage119_stage110_original_guard_only_b1_guard_20260621` did not pass strict no-regression promotion. `step-64` measured AISHELL `30.02 / 30.02`, LibriSpeech-other `36.80 / 23.61`, CommonVoice EN `51.62 / 34.88`, and WenetSpeech `47.77 / 49.83`. `step-128` is the useful candidate: AISHELL `29.9148 / 29.9148` improves over Stage110 `29.9489`, LibriSpeech-other ties Stage110 at `36.7669 / 23.5995`, WenetSpeech improves to `47.7684 / 49.8346`, but CommonVoice EN regresses to `51.6736 / 34.8894`. Do not promote Stage119 directly and do not run full public-200 from it yet; the original-label guard-only direction is better than direct teacher substitution but still needs interpolation or guard balancing to remove the CV regression.
					- 2026-06-21 Stage120 plan: run a no-training interpolation screen between Stage110 `wercer_best.pt` and Stage119 `step-128.pt` to test whether Stage119's AISHELL/Wenet gains can be retained while restoring CommonVoice EN. This is inference-only and keeps the BiRWKV/RWKV-7 TimeMixer architecture, bidirectional wrapper, frontend, tokenizer, CTC head, objective, direction policy, and fixed public-200 decode policy unchanged. Generate candidates with Stage119 weights `0.25`, `0.50`, and `0.75` under `/media/usbhd/rwkvasr_runs/stage120_stage110_stage119_interp_b1_guard_20260621`, preserving Stage110 metadata/config files. Evaluate fixed-code public-200 AISHELL-1, LibriSpeech-other, CommonVoice EN, and WenetSpeech first; run full public-200 only if a candidate keeps Stage110's CommonVoice and Libri guard while preserving the Stage119 AISHELL/Wenet gains.
					- 2026-06-21 Stage120 result: screened Stage110/Stage119-step128 interpolation candidates under `/media/usbhd/rwkvasr_runs/stage120_stage110_stage119_interp_b1_guard_20260621` with Stage119 weights `0.25`, `0.50`, and `0.75`. `w0p25` exactly matches Stage110 on AISHELL, Libri-other, CommonVoice, and Wenet, so it is not useful. `w0p50` improves AISHELL to `29.9148 / 29.9148` while exactly matching Stage110 on Libri-other `36.7669 / 23.5995`, CommonVoice `51.6213 / 34.8783`, and Wenet `47.7986 / 49.8647`; this is a no-regression guard candidate but does not preserve Stage119's Wenet gain. `w0p75` preserves Stage119's AISHELL and Wenet gains (`29.9148`, Wenet `47.7684 / 49.8346`) but regresses CommonVoice to `51.6736 / 34.8894`. Do not promote Stage120 yet. Before deciding whether to run full public-200 on `w0p50`, run one finer interpolation point between `0.50` and `0.75` to check whether CV and Wenet have a shared non-regression region.
					- 2026-06-21 Stage121 plan: generate a single Stage110/Stage119-step128 interpolation candidate with Stage119 weight `0.625` under `/media/usbhd/rwkvasr_runs/stage121_stage110_stage119_fine_interp_b1_guard_20260621/w0p625`. Evaluate fixed-code public-200 AISHELL-1, LibriSpeech-other, CommonVoice EN, and WenetSpeech. If `w0p625` preserves the Stage119 AISHELL/Wenet gains while keeping Stage110's Libri/CV guard, complete full public-200 by adding LibriSpeech-clean and consider it for promotion. If `w0p625` fails, keep `w0p50` as the only no-regression guard candidate and complete full public-200 for `w0p50` by evaluating LibriSpeech-clean before deciding whether it can replace Stage110.
					- 2026-06-21 Stage121 interim result: `w0p625` measured AISHELL `29.9148 / 29.9148`, LibriSpeech-other `36.7669 / 23.5995`, CommonVoice `51.6736 / 34.8894`, and WenetSpeech `47.7684 / 49.8346`, matching the `w0p75` behavior rather than finding a shared CV/Wenet non-regression region. This keeps AISHELL/Wenet gains but still regresses CV. Run one final midpoint at Stage119 weight `0.5625`; if it also falls on either the `w0p50` tie side or the `w0p625` CV-regression side, stop fine interpolation and complete full public-200 only for `w0p50`.
					- 2026-06-21 Stage121 final result and Stage120 full-public decision: `w0p5625` measured AISHELL `29.9148 / 29.9148`, LibriSpeech-other `36.7669 / 23.5995`, CommonVoice `51.6736 / 34.8894`, and WenetSpeech `47.7684 / 49.8346`, again matching the CV-regression side. Stop the `0.50`-to-`0.75` fine interpolation search. Completed full fixed-code public-200 for Stage120 `w0p50` by adding LibriSpeech-clean: AISHELL `29.9148 / 29.9148`, LibriSpeech-clean `22.0172 / 13.2296`, LibriSpeech-other `36.7669 / 23.5995`, CommonVoice `51.6213 / 34.8783`, and WenetSpeech `47.7986 / 49.8647`; macro WER/CER are `37.6238 / 30.2974` versus Stage110 `37.6356 / 30.3030`. This improves macro metrics and AISHELL and slightly improves LibriSpeech-clean WER, but LibriSpeech-clean CER regresses by about `0.0058` absolute percentage points versus Stage110 `13.2238`, so do not promote `w0p50` under the strict no-dataset-regression gate.
					- 2026-06-21 Stage122 plan: run a lower-amplitude no-training interpolation screen between Stage110 `wercer_best.pt` and Stage119 `step-128.pt` at Stage119 weights `0.375` and `0.4375` under `/media/usbhd/rwkvasr_runs/stage122_stage110_stage119_low_interp_b1_guard_20260621`. This tests whether the AISHELL threshold flips before the LibriSpeech-clean CER regression appears. Preserve Stage110 metadata/config files and keep the BiRWKV/RWKV-7 TimeMixer architecture, bidirectional wrapper, frontend, tokenizer, CTC head, objective, direction policy, and fixed public-200 decode policy unchanged. First evaluate AISHELL-1 and LibriSpeech-clean public-200 only; if a candidate improves AISHELL and does not regress LibriSpeech-clean WER or CER versus Stage110, complete LibriSpeech-other, CommonVoice EN, and WenetSpeech and consider promotion only if the full five-set gate has no WER/CER regression.
					- 2026-06-21 Stage122 result and Stage123 plan: both low-amplitude candidates (`0.375` and `0.4375` Stage119 weight) measured the same two-set behavior: AISHELL `29.9830 / 29.9830`, worse than Stage110 `29.9489`, while LibriSpeech-clean improved to `22.0172 / 13.2180` versus Stage110 `22.0425 / 13.2238`. Do not complete the remaining guard sets and do not promote Stage122. The useful boundary lies between `0.4375` and `0.50`, where AISHELL flips from worse to better while LibriSpeech-clean CER flips from better to slightly worse. Run Stage123 as an inference-only boundary screen at Stage119 weights `0.46875` and `0.484375` under `/media/usbhd/rwkvasr_runs/stage123_stage110_stage119_boundary_interp_b1_guard_20260621`, evaluating AISHELL-1 and LibriSpeech-clean first with the same fixed public-200 policy. Complete full five-set evaluation only if a candidate improves AISHELL and does not regress LibriSpeech-clean WER or CER.
					- 2026-06-21 Stage123 result and Stage124 plan: `0.46875` and `0.484375` Stage119-weight candidates again measured AISHELL `29.9830 / 29.9830` and LibriSpeech-clean `22.0172 / 13.2180`, identical to Stage122. Do not complete the remaining guard sets and do not promote Stage123. Because Stage120 `0.50` is the first tested point that improves AISHELL but also the first tested point that regresses LibriSpeech-clean CER, run one final near-threshold inference-only screen at Stage119 weights `0.4921875` and `0.49609375` under `/media/usbhd/rwkvasr_runs/stage124_stage110_stage119_threshold_interp_b1_guard_20260621`, still evaluating only AISHELL-1 and LibriSpeech-clean first. If both remain on the Stage122 side or both land on the Stage120 side, stop this interpolation family and keep Stage110 as the strict no-regression incumbent unless a full five-set candidate passes all WER/CER gates.
					- 2026-06-21 Stage124 result: `0.4921875` and `0.49609375` Stage119-weight candidates still measured AISHELL `29.9830 / 29.9830` and LibriSpeech-clean `22.0172 / 13.2180`, identical to Stage122/123 and not useful for promotion. Since the next tested point (`0.50`) improves AISHELL but regresses LibriSpeech-clean CER, the Stage110/Stage119 interpolation family has no observed strict no-regression public-200 candidate. Preserve `.soup.json`, predictions, previews, and metrics, then delete the temporary soup `.pt` files for failed Stage120-Stage124 candidates. Keep Stage110 `wercer_best.pt` as the strict incumbent.
					- 2026-06-21 Stage125 plan: run a same-architecture original-label guard reweighting continuation from Stage110 to test whether Stage119's AISHELL/Wenet signal can be retained while preventing the LibriSpeech-clean and CommonVoice drift. Add a `--no-teacher` mode to the existing Stage104/108/110 linked-index builder so it can emit an original-label-only stage without a dummy teacher cache. Build `stage125_stage110_cv_libri_weighted_original_guard` from the same Stage61/Stage22 linked roots with selector ids excluded, using original-label quotas LibriSpeech `60k`, CommonVoice EN `45k`, WenetSpeech `35k`, AISHELL3 `20k`, CommonVoice CN `10k`, and GigaSpeech `7k` (`177k` rows, replacement fill allowed and recorded). Train from Stage110 `wercer_best.pt` with Stage110 hidden+CTC-logit anchor (`0.10 / 0.10`, chunk frames `16`), LR `5e-10`, `max_steps=128`, `save_every=64`, ZeRO-1, batch size `12`, grad accumulation `4`, no SpecAugment, no gradient checkpointing, no CTC label override cache, and no direction dropout. This changes only data sampling weights and does not change the BiRWKV/RWKV-7 TimeMixer architecture, bidirectional wrapper, frontend, tokenizer, CTC head, objective, direction policy, or fixed public-200 decode policy. Gate first on AISHELL-1, LibriSpeech-clean, CommonVoice EN, and WenetSpeech; only add LibriSpeech-other/full comparison if this four-way guard has no WER/CER regression versus Stage110.
					- 2026-06-21 Stage125 preparation/training result: added `--no-teacher` to `scripts/build_stage104_libri_teacher_guard_repair.py` and verified syntax/config parsing. Generated `/media/usbhd/training_data/asr/curriculum/stage22_stage21_soup_clean_repair_mix/stages/stage125_stage110_cv_libri_weighted_original_guard/webdataset_lengths.jsonl`, an empty `stage125_teacher_sampled.cache.jsonl`, and `webdataset_buckets_audio_text/manifest.json`. The index has exactly `177,000` original-label rows with source counts LibriSpeech `60k`, CommonVoice EN `45k`, WenetSpeech `35k`, AISHELL3 `20k`, CommonVoice CN `10k`, and GigaSpeech `7k`; replacement fill is recorded for LibriSpeech `13,046`, CommonVoice EN `155`, and WenetSpeech `5,032`, with `158,767` unique utterance ids. Generated `configs/generated/sensevoice_rwkv_stage125_stage110_cv_libri_weighted_original_guard_4x4090_deepspeed.yaml`; parsing confirms Stage110 as init and anchor, LR `5e-10`, `max_steps=128`, hidden/logit anchor `0.10 / 0.10`, no CTC override, no SpecAugment, no gradient checkpointing, and no direction dropout. Training completed to `step=128` under `/media/usbhd/rwkvasr_runs/sensevoice_rwkv_stage125_stage110_cv_libri_weighted_original_guard_bs12_lr5e10_hid0p10_kl0p10_zero1_nockpt_noaug_nodirdrop_4x4090`, preserving `step-64.pt`, `step-128.pt`, `epoch-1.pt`, and `best.pt`; final epoch train loss was `2.1741`, final logged losses at step128 were total `2.2812`, CTC `2.1977`, hidden anchor `0.7778`, and CTC-logit KL `0.0576`, with first-step peak reserved memory about `12.83 GiB`. W&B sync hung after checkpoint export, so the residual rank0 sync process was terminated only after verifying the top-level `.pt` files; deleted this run's `ds_checkpoints/` after verification.
					- 2026-06-22 Stage125 public-200 guard result and Stage126 plan: fixed-code `batch_size=1`, `mode=bi`, `beam8/topk16`, zero length/insertion/blank biases, and CTC normalization evaluated `step-64` and `step-128` under `/media/usbhd/rwkvasr_runs/stage125_stage110_cv_libri_weighted_original_guard_b1_guard_20260621`. `step-64` measured AISHELL `29.8807 / 29.8807`, LibriSpeech-clean `22.0425 / 13.2007`, CommonVoice EN `51.6736 / 34.9334`, and WenetSpeech `47.7986 / 49.8647`; it improves AISHELL and Libri-clean CER and ties Wenet, but regresses CommonVoice versus Stage110 `51.6213 / 34.8783`. `step-128` measured AISHELL `29.8807 / 29.8807`, LibriSpeech-clean `22.0172 / 13.2065`, CommonVoice EN `51.8305 / 35.0325`, and WenetSpeech `47.8287 / 49.8947`; it worsens the CV/Wenet guard further. Do not run LibriSpeech-other/full public from raw Stage125 and do not promote it. Run Stage126 as an inference-only interpolation screen between Stage110 `wercer_best.pt` and Stage125 `step-64.pt`, testing Stage125 weights `0.25`, `0.50`, and `0.75` under `/media/usbhd/rwkvasr_runs/stage126_stage110_stage125_step64_interp_b1_guard_20260622`. Evaluate AISHELL-1, LibriSpeech-clean, CommonVoice EN, and WenetSpeech first; complete LibriSpeech-other/full public only if a candidate keeps the Stage110 CV/Wenet guard while retaining at least one AISHELL or Libri-clean gain.
					- 2026-06-22 Stage126 result: Stage110/Stage125-step64 interpolation did not produce a strict public-200 promotion. `w0p25` exactly matched Stage110 on AISHELL, CommonVoice, and Wenet and had only noise-scale Libri-clean movement. `w0p50` kept AISHELL/CommonVoice/Wenet tied and improved LibriSpeech-clean CER to `13.2180`, so it was completed on LibriSpeech-other; full five-set metrics were AISHELL `29.9489 / 29.9489`, Libri-clean `22.0425 / 13.2180`, Libri-other `36.7669 / 23.6066`, CommonVoice `51.6213 / 34.8783`, and Wenet `47.7986 / 49.8647`. This trades Libri-clean CER gain for Libri-other CER regression and macro CER is not better than Stage110, so do not promote. `w0p75` showed the larger desired AISHELL/Libri-clean movement (`29.8807`, Libri-clean `13.2007`) but already regressed CommonVoice to `51.6736 / 34.9334`; do not complete Wenet/full public. Delete the temporary Stage126 `.pt` candidates after preserving `.soup.json`, predictions, previews, and metrics. Keep Stage110 as incumbent.
					- 2026-06-22 Stage126 diagnostic and Stage127 plan: CommonVoice regression for Stage125-step64 and Stage126 `w0p75` is extremely localized: compared with Stage110, only `common_voice_en_591766` changes metric output, replacing the Stage110 hypothesis ending `spoken words` with `working words`, for `+1` WER edit and `+5` CER edits; prediction length is effectively unchanged. Run a final fine interpolation screen between Stage125 weights `0.50` and `0.75`, testing `0.625` and `0.6875` under `/media/usbhd/rwkvasr_runs/stage127_stage110_stage125_step64_fine_interp_b1_guard_20260622`. Evaluate AISHELL-1, LibriSpeech-clean, and CommonVoice EN first. Complete WenetSpeech and LibriSpeech-other only if a candidate keeps CommonVoice tied while improving AISHELL or Libri-clean without introducing a known guard regression.
					- 2026-06-22 Stage127 result and Stage128 plan: completed the interrupted fixed-code public-200 fine interpolation screen under `/media/usbhd/rwkvasr_runs/stage127_stage110_stage125_step64_fine_interp_b1_guard_20260622` after overwriting partial JSONL outputs. `w0p625` measured AISHELL `29.9489 / 29.9489`, LibriSpeech-clean `22.0425 / 13.2180`, and CommonVoice EN `51.6736 / 34.9334`; `w0p6875` CommonVoice EN also measured `51.6736 / 34.9334`. Both candidates fall on the CommonVoice regression side and `w0p625` does not retain the Stage125 AISHELL gain, so do not complete WenetSpeech or LibriSpeech-other and do not promote Stage127. Preserve `.soup.json`, predictions, previews, and metrics, then delete the temporary Stage127 `.pt` candidates. Stage128 should stop simple Stage110/Stage125 interpolation and instead run a same-architecture diagnostic/data step: compare Stage110, Stage125-step64, and interpolation predictions on public-200 to identify whether useful AISHELL/Libri-clean improvements are concentrated in a small set of samples, then choose a non-public-data guard continuation or lower-amplitude retraining plan that targets the observed CommonVoice brittleness without using public test labels for training.
					- 2026-06-22 Stage128 diagnostic result and data plan: generated `/media/usbhd/rwkvasr_runs/stage128_stage125_public200_diff_diagnostic_20260622/diagnostic.json` and `.md`. The Stage125-step64 public-200 movement is not broad: AISHELL improves by only one utterance (`-2` edits), LibriSpeech-clean CER improves on two utterances (`-4` CER edits total), CommonVoice regresses on one utterance (`+1` WER edit / `+5` CER edits), and WenetSpeech only changes text on two utterances with zero metric delta. Stage126/127 interpolations show the same threshold behavior. Stop this original-label reweight/interpolation line. For the next same-architecture data slice, expand non-public WenetSpeech teacher coverage using the existing Stage108/110 source-aware teacher-mining pipeline with Stage110 `wercer_best.pt` as the student: sample `4096` fresh WenetSpeech train candidates excluding selector/prior teacher ids, decode with fixed `mode=bi`, `beam8/topk16`, zero biases, and CTC normalization, select `1024` high-error targets, run FunASR-Nano teacher decoding, and conservatively filter accepted rows. This is only data/label mining; do not change the BiRWKV/RWKV-7 TimeMixer architecture, bidirectional wrapper, frontend, tokenizer, CTC head, objective, direction policy, or fixed public-200 decode policy. If the expanded Wenet cache is meaningfully larger than the current `380` unique Stage110 Wenet teacher ids, build a later Stage129 balanced low-repeat teacher mix from Stage110 that keeps total teacher pressure at or below Stage110 while increasing unique coverage.
					- 2026-06-22 Stage128 pipeline note: the sampled WenetSpeech length rows contain tar JSON offsets but no inline `text`, and the current `predict_ctc_labeled` loader requires `text` or `token_ids` for labeled scoring. Add a narrow `recover-text` subcommand to `scripts/build_stage108_libri_teacher_cache.py` that reads the existing tar JSON metadata and writes text-enriched candidate manifests/parts before student decoding. This is a data-pipeline utility only; it does not alter model architecture, training objective, decode scoring, or benchmark policy.
					- 2026-06-22 Stage128 cache result and Stage129 plan: added and verified `recover-text` in `scripts/build_stage108_libri_teacher_cache.py`, sampled `4,096` fresh non-public WenetSpeech candidates under `/media/usbhd/rwkvasr_runs/stage128_wenet_teacher_expand_20260622/wenetspeech`, recovered inline text for all rows, decoded them with Stage110 `wercer_best.pt` via the WebDataset length-index path, and selected `1,024` high-error teacher targets. The selected targets averaged Stage110 student primary error `0.8069` (`min=0.6154`, `max=1.5`). FunASR-Nano decoded all targets and conservative filtering accepted `764/1,024` Wenet rows, all unique and all source-audited as WenetSpeech, with average Stage110 student `0.7836 / 0.8118` WER/CER versus teacher `0.0204 / 0.0204`; rejects were `203` by teacher WER and `57` by teacher CER. This is meaningfully larger than the current Stage110 Wenet teacher coverage (`380` unique ids). Build Stage129 as a controlled same-architecture training slice from Stage110: keep the original-label guard quotas from Stage110 (`45k` LibriSpeech, `35k` CommonVoice EN, `35k` WenetSpeech, `20k` AISHELL3, `10k` CommonVoice CN, `7k` GigaSpeech), keep total teacher pressure at Stage110's `12k` rows (`4k` LibriSpeech from Stage108, `4k` CommonVoice EN from Stage110, `4k` WenetSpeech from the new Stage128 cache), and train from Stage110 `wercer_best.pt` with Stage110 hidden+CTC-logit anchoring (`0.10 / 0.10`, chunk frames `16`), LR `5e-10`, `max_steps=128`, `save_every=64`, ZeRO-1, batch size `12`, grad accumulation `4`, no SpecAugment, no gradient checkpointing, and no direction dropout. This changes only Wenet teacher unique coverage; it does not change the BiRWKV/RWKV-7 TimeMixer architecture, bidirectional wrapper, frontend, tokenizer, CTC head, objective, direction policy, or fixed public-200 decode policy. Gate first on fixed-code LibriSpeech-other, CommonVoice EN, and WenetSpeech public-200; complete AISHELL/Libri-clean only if those guards do not regress versus Stage110.
					- 2026-06-22 Stage129 preparation/training/public-200 result: built `stage129_stage110_expanded_wenet_teacher_balanced_guard` with `164,000` rows: original-label guard rows `152,000` (LibriSpeech `45k`, CommonVoice EN `35k`, WenetSpeech `35k`, AISHELL3 `20k`, CommonVoice CN `10k`, GigaSpeech `7k`) plus `12,000` teacher rows (`4k` LibriSpeech from Stage108, `4k` CommonVoice EN from Stage110, `4k` WenetSpeech from Stage128). The sampled teacher override cache contains `2,063` unique rows after source linking (`918` LibriSpeech, `384` CommonVoice EN, `761` WenetSpeech), increasing Wenet teacher unique coverage from Stage110's `380` to `761` without increasing total teacher pressure. Training completed from Stage110 `wercer_best.pt` to step `128` under `/media/usbhd/rwkvasr_runs/sensevoice_rwkv_stage129_stage110_expanded_wenet_teacher_balanced_guard_bs12_lr5e10_hid0p10_kl0p10_zero1_nockpt_noaug_nodirdrop_4x4090`; exported `step-64.pt`, `step-128.pt`, `best.pt`, and `epoch-1.pt`, then deleted this run's `ds_checkpoints/` after verifying the top-level checkpoints. Final epoch train loss was `2.2368`; final step128 losses were total `2.0142`, CTC `1.9185`, hidden anchor `0.8457`, and CTC-logit anchor `0.1113`, with first-step peak around `16.12 GiB` reserved. Fixed-code public-200 guard under `/media/usbhd/rwkvasr_runs/stage129_stage110_expanded_wenet_teacher_balanced_guard_b1_guard_20260622` found `step-64` exactly tied Stage110 on all five datasets: AISHELL `29.9489 / 29.9489`, LibriSpeech-clean `22.0425 / 13.2238`, LibriSpeech-other `36.7669 / 23.5995`, CommonVoice EN `51.6213 / 34.8783`, WenetSpeech `47.7986 / 49.8647`, macro `37.6356 / 30.3030`. `step-128` tied LibriSpeech-other and WenetSpeech but regressed CommonVoice EN to `51.7259 / 35.0325`. Do not promote Stage129; Stage110 remains the strict no-regression incumbent.
					- 2026-06-22 Stage130 plan: stop increasing same-pressure teacher coverage for now, because Stage129 shows expanded Wenet teacher uniqueness is neutral at the first checkpoint and drifts CommonVoice by step128. Run a different same-architecture robustness probe from Stage110 on the Stage129 index/cache: enable only very light SpecAugment (`time_masks=1`, `time_width=10`, `freq_masks=1`, `freq_width=8`) while strengthening the Stage110 hidden+CTC-logit anchors to `0.20 / 0.20`, using LR `5e-10`, `max_steps=128`, `save_every=32`, ZeRO-1, batch size `12`, grad accumulation `4`, no gradient checkpointing, and no direction dropout. This tests whether low-amplitude augmentation can improve robustness without changing the BiRWKV/RWKV-7 TimeMixer architecture, bidirectional wrapper, SenseVoice frontend, tokenizer, CTC head/objective, teacher labels, or fixed public-200 decode policy. Gate checkpoints first on fixed-code LibriSpeech-other, CommonVoice EN, and WenetSpeech public-200; only complete AISHELL/LibriSpeech-clean if a checkpoint has no guard regression versus Stage110. If all checkpoints tie or regress, preserve artifacts and move to a non-public train/validation signal instead of using public-test micro-threshold flips for optimization.
					- 2026-06-22 Stage130 training/public-200 guard result and Stage131 plan: generated `configs/generated/sensevoice_rwkv_stage130_stage110_light_specaug_expanded_wenet_teacher_guard_4x4090_deepspeed.yaml` and trained `/media/usbhd/rwkvasr_runs/sensevoice_rwkv_stage130_stage110_light_specaug_expanded_wenet_teacher_guard_bs12_lr5e10_hid0p20_kl0p20_zero1_nockpt_lightaug_nodirdrop_4x4090` from Stage110 `wercer_best.pt`. The config parser verified `specaugment_enabled=true`, light masks `1x10 / 1x8`, anchors `0.20 / 0.20`, LR `5e-10`, `max_steps=128`, `save_every=32`, and no direction dropout. Training completed normally with first-step peak `16.12 GiB` reserved / `13.67 GiB` allocated, exported `step-32.pt`, `step-64.pt`, `step-96.pt`, `step-128.pt`, `best.pt`, and `epoch-1.pt`, synced W&B, then deleted this run's `29 GiB` `ds_checkpoints/` after verifying top-level `.pt` files. Final epoch train loss was `3.0879`; final step128 losses were total `2.2203`, CTC `2.0280`, hidden anchor `0.8577`, and CTC-logit anchor `0.1039`. Fixed-code public-200 guard on LibriSpeech-other / CommonVoice EN / WenetSpeech found no promotable checkpoint: `step32` tied CommonVoice and Wenet but regressed LibriSpeech-other CER to `23.6066` versus Stage110 `23.5995`; `step64` and `step96` measured LibriSpeech-other `36.7669 / 23.6066`, CommonVoice `51.6736 / 34.9004`, and Wenet `47.8287 / 49.8947`; `step128` further regressed LibriSpeech-other to `36.7973 / 23.6353` and CommonVoice to `51.7259 / 34.9444`. Do not complete AISHELL/LibriSpeech-clean and do not promote Stage130. Stage131 should build a fixed non-public private guard before further training selection: sample clean/CV holdout rows from Stage61/Stage22 pools after excluding public selector ids, Stage129 train ids, and accepted teacher ids; sample WenetSpeech/GigaSpeech hard holdout rows from `/media/usbhd/training_data/asr/mix/gigaspeech_xl_wenetspeech_l_webdataset/webdataset_lengths.jsonl` after the same train/teacher exclusions, because the Stage129 Wenet original pool was exhausted by training. Keep outputs split by WebDataset root (`clean_guard.lengths.jsonl` for the Stage22 root and `hard_guard.lengths.jsonl` for the hard-mix root). Score Stage110 and failed recent checkpoints on this private guard first, then use private deltas to choose the next same-architecture data/training direction; public-200 remains only a promotion gate.
					- 2026-06-22 Stage131 private guard build/eval result and Stage132 plan: added `scripts/build_stage131_private_guard.py` and `scripts/summarize_private_guard_results.py`. The first build attempt cached full pools and was interrupted after RSS reached about `28 GiB`; the script was changed to per-source reservoir sampling, then completed with about `44 MiB` RSS. Outputs are under `/media/usbhd/rwkvasr_runs/stage131_private_guard_20260622`: `clean_guard.lengths.jsonl` has `768` rows (LibriSpeech `256`, CommonVoice EN `256`, AISHELL3 `128`, CommonVoice CN `128`) from Stage61/Stage22 after excluding `148,523` selector/train ids; `hard_guard.lengths.jsonl` has `384` rows (WenetSpeech `256`, GigaSpeech `128`) from the non-public hard-mix root, also excluding the same ids. Stage110 private baseline is total `30.0923 / 19.1169` WER/CER. Private comparison shows Stage119 `step-128` is the strongest recent signal: total `30.0656 / 19.0881`, with AISHELL `22.1523 / 22.1523`, CommonVoice EN `40.5832 / 26.8150`, LibriSpeech `23.2092 / 14.0362`, unchanged WenetSpeech `42.4608 / 43.0449`, and only a small GigaSpeech CER regression (`17.9128` vs Stage110 `17.9005`). Stage129 `step-64` is public-stable and improves private CER only (`30.0923 / 19.1057`), mainly on CommonVoice CN/EN and LibriSpeech, but worsens private GigaSpeech. Stage125 and Stage130 variants do not beat Stage119 on the private aggregate, and Stage130's CER gains come with WER/public regressions. Use Stage119 `step-128` as the useful direction, but not as a promotion checkpoint because its public-200 CommonVoice EN regresses. Run Stage132 as an inference-only checkpoint-soup screen combining Stage110, Stage119 `step-128`, and Stage129 `step-64`: test `s119_0p50_s129_0p25_s110_0p25`, `s119_0p50_s129_0p50`, and `s119_0p5625_s129_0p25_s110_0p1875`. First evaluate AISHELL-1, LibriSpeech-clean, and CommonVoice EN public-200 with the fixed decode policy; complete LibriSpeech-other/Wenet/full public only if a candidate preserves Stage120's AISHELL gain while removing the LibriSpeech-clean CER and CommonVoice regressions. This is a checkpoint selection/averaging screen only; it does not change the BiRWKV/RWKV-7 TimeMixer architecture, bidirectional wrapper, frontend, tokenizer, CTC head/objective, or direction policy.
					- 2026-06-22 Stage132 soup result and Stage133 plan: generated the three private-guided soups under `/media/usbhd/rwkvasr_runs/stage132_stage119_stage129_private_guided_soup_20260622`, preserving `.soup.json` metadata and Stage110/Stage119 configs. Public-200 first-pass evaluation on AISHELL-1, LibriSpeech-clean, and CommonVoice EN did not find a strict candidate. `s119_0p50_s129_0p25_s110_0p25` and `s119_0p50_s129_0p50` both measured AISHELL `29.9148 / 29.9148`, LibriSpeech-clean `22.0172 / 13.2296`, and CommonVoice `51.6213 / 34.8783`; they preserve the Stage120 AISHELL/CV behavior but still regress LibriSpeech-clean CER versus Stage110 `13.2238`. `s119_0p5625_s129_0p25_s110_0p1875` measured AISHELL `29.9148 / 29.9148`, LibriSpeech-clean `22.0172 / 13.2296`, and CommonVoice `51.6736 / 34.8894`, so it also regresses CV. Do not complete LibriSpeech-other/Wenet/full public for Stage132 and do not promote. Deleted the three temporary `model.pt` soup files after preserving `.soup.json`, predictions, previews, and metrics. Stage133 should stop public-threshold soup searches and build a larger non-public GigaSpeech hard teacher cache, because private guard WER remains high on GigaSpeech (`29.1487`) and there has not yet been a source-specific Giga teacher substitution stage. Reuse `scripts/build_stage108_libri_teacher_cache.py` with `--source gigaspeech`, `--stage-prefix stage133`, and the hard-mix length index: sample `4,096` GigaSpeech train candidates excluding the public selector, Stage129 train ids, and Stage131 private guard ids; recover text from tar JSON; decode with Stage110 `wercer_best.pt` using fixed `mode=bi`, `beam8/topk16`, zero decode biases, and CTC normalization; select `1,024` high-error targets; extract audio; run FunASR-Nano teacher; and conservatively filter accepted rows. This is data/label mining only and does not change model architecture, TimeMixer behavior, frontend, tokenizer, CTC objective, direction policy, or public decode policy.
					- 2026-06-22 Stage133 Giga teacher cache result and Stage134 plan: sampled `4,096` non-public GigaSpeech train candidates from `/media/usbhd/training_data/asr/mix/gigaspeech_xl_wenetspeech_l_webdataset/webdataset_lengths.jsonl`, excluding Stage129 train ids and Stage131 private guard ids. The sampler saw `8,240,925` eligible Giga rows, skipped `7,160` excluded ids and `34,903` long rows, and wrote four `1,024`-row parts under `/media/usbhd/rwkvasr_runs/stage133_gigaspeech_teacher_expand_20260622/gigaspeech`. Text recovery from tar JSON had `missing_text=0`. Stage110 student decoding completed for all `4,096` candidates; selection chose `1,024` teacher targets with average primary error `0.6914` (`min=0.4474`, `max=3.0`). FunASR-Nano teacher decoding completed all four parts, and conservative filtering accepted `824/1,024` rows with average Stage110 student `0.6694 / 0.3920` WER/CER versus teacher `0.0289 / 0.0118`; rejects were `194` by teacher WER and `6` by teacher CER. Build Stage134 as the first Giga-teacher balanced training slice from Stage110: keep the same original-label guard quotas as Stage110/129 (`45k` LibriSpeech, `35k` CommonVoice EN, `35k` WenetSpeech, `20k` AISHELL3, `10k` CommonVoice CN, `7k` GigaSpeech), keep total teacher pressure at `12k` rows by sampling `3k` each from LibriSpeech Stage108, CommonVoice EN Stage110, WenetSpeech Stage128, and GigaSpeech Stage133 accepted caches, and train from Stage110 `wercer_best.pt` with Stage110 hidden+CTC-logit anchors (`0.10 / 0.10`, chunk frames `16`), LR `5e-10`, `max_steps=128`, `save_every=64`, ZeRO-1, batch size `12`, grad accumulation `4`, no SpecAugment, no gradient checkpointing, and no direction dropout. Gate Stage134 first on the Stage131 private guard; run public-200 promotion gates only if private GigaSpeech improves without broad private clean/CV regression.
					- 2026-06-22 Stage134 preparation/training/private-guard result and public-200 plan: built `stage134_stage110_giga_teacher_balanced_guard` with `164,000` rows: the unchanged Stage110/129 original-label guard quotas (`45k` LibriSpeech, `35k` CommonVoice EN, `35k` WenetSpeech, `20k` AISHELL3, `10k` CommonVoice CN, `7k` GigaSpeech) plus `12,000` teacher rows (`3k` each from LibriSpeech Stage108, CommonVoice EN Stage110, WenetSpeech Stage128, and GigaSpeech Stage133). The sampled teacher cache contains `2,814` source-linked rows before repeat expansion (`886` LibriSpeech, `384` CommonVoice EN, `745` WenetSpeech, `799` GigaSpeech) and no missing lengths. Trained `/media/usbhd/rwkvasr_runs/sensevoice_rwkv_stage134_stage110_giga_teacher_balanced_guard_bs12_lr5e10_hid0p10_kl0p10_zero1_nockpt_noaug_nodirdrop_4x4090` from Stage110 `wercer_best.pt` to step `128`, exported `step-64.pt`, `step-128.pt`, `best.pt`, and `epoch-1.pt`, then deleted this run's `20 GiB` `ds_checkpoints/` after verifying the top-level checkpoints. Final epoch train loss was `2.2432`; final step128 losses were total `2.7895`, CTC `2.7068`, hidden anchor `0.7819`, and CTC-logit anchor `0.0447`. Stage131 private guard qualifies Stage134 for public-200 promotion testing but not promotion: `step-64` measured total `30.0869 / 19.0881` and improved private GigaSpeech CER (`17.7767` vs Stage110 `17.9005`) plus WenetSpeech (`42.4242 / 43.0084` vs `42.4608 / 43.0449`), while `step-128` measured total `30.0763 / 19.0865`, kept GigaSpeech CER improved (`17.8386`), and reduced total CER below both Stage110 and Stage119 step128. Neither checkpoint changes the BiRWKV/RWKV-7 TimeMixer architecture, bidirectional wrapper, SenseVoice frontend, tokenizer, CTC head/objective, direction policy, or fixed decode policy. Run a strict fixed-code public-200 gate next, starting with `step-128` because it has the best private total CER; use `batch_size=1`, `mode=bi`, `beam8/topk16`, zero length/insertion/blank biases, and CTC normalization. Promote only if all five public-200 datasets are no-regression versus Stage110 (`macro 37.6356 / 30.3030` with AISHELL `29.9489 / 29.9489`, LibriSpeech-clean `22.0425 / 13.2238`, LibriSpeech-other `36.7669 / 23.5995`, CommonVoice EN `51.6213 / 34.8783`, and WenetSpeech `47.7986 / 49.8647`); otherwise keep Stage110 as the strict incumbent and use the private/public mismatch to choose the next same-architecture data slice.
					- 2026-06-22 Stage134 step128 public-200 result and step64 recheck plan: fixed-code public-200 evaluation for Stage134 `step-128.pt` completed under `/media/usbhd/rwkvasr_runs/stage134_stage110_giga_teacher_balanced_guard_b1_guard_20260622/step128_public200_b1_beam8_topk16` with exactly `200` predictions for each dataset. Results were AISHELL `29.9830 / 29.9830`, LibriSpeech-clean `22.0172 / 13.2412`, LibriSpeech-other `36.8277 / 23.6497`, CommonVoice EN `51.6736 / 34.9554`, and WenetSpeech `47.8287 / 49.8947`. The checkpoint fails the strict Stage110 no-regression gate despite private-guard CER gains: AISHELL, LibriSpeech-clean CER, LibriSpeech-other, CommonVoice EN, and WenetSpeech all regress at public-200 scale. Do not promote Stage134 step128. Run one final Stage134 public-200 recheck for `step-64.pt`, because private guard showed better source-level GigaSpeech CER and WenetSpeech movement at lower training amplitude; if `step-64` also regresses, stop the Giga-balanced teacher-pressure slice and design the next same-architecture data step without increasing public-test optimization pressure.
					- 2026-06-22 Stage134 step64 public-200 result and Stage135 plan: fixed-code public-200 recheck for Stage134 `step-64.pt` completed under `/media/usbhd/rwkvasr_runs/stage134_stage110_giga_teacher_balanced_guard_b1_guard_20260622/step64_public200_b1_beam8_topk16` with exactly `200` predictions for each dataset. Results were AISHELL `29.9489 / 29.9489`, LibriSpeech-clean `22.0425 / 13.1949`, LibriSpeech-other `36.8885 / 23.6568`, CommonVoice EN `51.7782 / 34.9884`, and WenetSpeech `47.8589 / 49.9248`; macro was about `37.7034 / 30.3428` versus Stage110 `37.6356 / 30.3030`. The lower-amplitude checkpoint improves LibriSpeech-clean CER but regresses LibriSpeech-other, CommonVoice EN, and WenetSpeech, so Stage134 has no promotable checkpoint. Keep Stage110 as the strict public incumbent. Stage135 should not continue public-threshold checkpoint searching; instead build a lower-amplitude non-public Giga micro-teacher slice from Stage110: keep Stage129's public-stable `4k/4k/4k` LibriSpeech/CommonVoice EN/WenetSpeech teacher mix and add only `1k` weighted GigaSpeech teacher rows from Stage133, with the original-label guard quotas unchanged. Train from Stage110 with stronger anchors (`0.20 / 0.20`), lower LR `2.5e-10`, `max_steps=64`, `save_every=32`, ZeRO-1, batch size `12`, grad accumulation `4`, no SpecAugment, no gradient checkpointing, and no direction dropout. Gate Stage135 on the Stage131 private guard first; run public-200 only if private GigaSpeech improves without broad private clean/CV/Wenet regression. This keeps the BiRWKV/RWKV-7 TimeMixer architecture, bidirectional wrapper, SenseVoice frontend, tokenizer, CTC head/objective, direction policy, and fixed decode policy unchanged.
					- 2026-06-23 Stage135 training/private-guard result and Stage136 plan: built `stage135_stage110_giga_micro_teacher_guard` with `165,000` rows: unchanged `152,000` original-label guard rows plus Stage129's public-stable `4k/4k/4k` LibriSpeech/CommonVoice EN/WenetSpeech teacher mix and only `1k` GigaSpeech teacher rows from Stage133. The sampled teacher cache has `2,624` rows before repeat expansion, including `561` sampled GigaSpeech ids, with no missing lengths. Trained `/media/usbhd/rwkvasr_runs/sensevoice_rwkv_stage135_stage110_giga_micro_teacher_guard_bs12_lr2p5e10_hid0p20_kl0p20_zero1_nockpt_noaug_nodirdrop_4x4090` from Stage110 `wercer_best.pt` to step `64` with LR `2.5e-10`, anchors `0.20 / 0.20`, save_every `32`, no SpecAugment, no gradient checkpointing, and no direction dropout. Training completed normally; first-step peak was about `10.95 GiB` reserved / `8.70 GiB` allocated, final epoch train loss `2.3541`, final step64 losses total `2.6211`, CTC `2.4579`, hidden anchor `0.7901`, and CTC-logit anchor `0.0259`. Exported `step-32.pt`, `step-64.pt`, `best.pt`, and `epoch-1.pt`, verified they are readable, then deleted this run's `20 GiB` `ds_checkpoints/`. Stage131 private guard shows Stage135 is not useful: `step-32` exactly ties Stage110 total `30.0923 / 19.1169`, and `step-64` worsens total CER to `19.1281`; GigaSpeech and WenetSpeech remain unchanged, while only CommonVoice CN has a small CER gain and LibriSpeech CER regresses. Do not run public-200 for Stage135. For Stage136, run an inference-only private-guided soup screen to test whether Stage119's private aggregate gains and Stage134's Giga/Wenet private movements are complementary at low amplitude: generate Stage110-anchored soups combining Stage119 `step-128.pt` and Stage134 `step-64.pt` / `step-128.pt`, evaluate only on the Stage131 private guard first, and run public-200 only if a soup beats Stage119 private total without broad clean/CV/Wenet regression. This is checkpoint averaging only; it does not change the BiRWKV/RWKV-7 TimeMixer architecture, bidirectional wrapper, SenseVoice frontend, tokenizer, CTC head/objective, direction policy, or fixed decode policy.
					- 2026-06-23 Stage136 private-soup result and public first-pass plan: generated four Stage110-anchored checkpoint soups under `/media/usbhd/rwkvasr_runs/stage136_stage119_stage134_private_guided_soups_20260623` and evaluated them on the fixed Stage131 private guard. The best private candidate is `s119_0p625_s134s64_0p125_s110_0p25`: total WER ties Stage119 step128 at `30.0656`, total CER improves from Stage119 `19.0881` to `19.0833`, AISHELL and CommonVoice EN preserve the Stage119 gains, LibriSpeech CER improves to `14.0307`, GigaSpeech returns to Stage110 CER `17.9005` rather than Stage119's `17.9128`, and WenetSpeech ties Stage110. Other Stage136 soups either only matched partial Stage119 gains or regressed total/private CER. Treat `s119_0p625_s134s64_0p125_s110_0p25` as a public first-pass candidate, not as an incumbent. Because prior Stage120/121 interpolation showed the Stage119 public CommonVoice boundary is brittle above roughly `0.50` effective weight, first evaluate only AISHELL-1, LibriSpeech-clean, and CommonVoice EN public-200 with the fixed `batch_size=1`, `mode=bi`, `beam8/topk16`, zero decode biases, and CTC normalization policy. Complete LibriSpeech-other/WenetSpeech only if CommonVoice EN and LibriSpeech-clean do not regress versus Stage110. Delete non-selected Stage136 `model.pt` files after preserving their `.soup.json`, private predictions, and metrics.
					- 2026-06-23 Stage136 public first-pass result and Stage137 plan: public first-pass for `s119_0p625_s134s64_0p125_s110_0p25` completed on AISHELL-1, LibriSpeech-clean, and CommonVoice EN. It measured AISHELL `29.9148 / 29.9148`, LibriSpeech-clean `22.0172 / 13.2296`, and CommonVoice EN `51.6736 / 34.8894`. This repeats the known Stage119/Stage120 boundary: AISHELL improves, but LibriSpeech-clean CER and CommonVoice EN WER/CER regress versus Stage110 (`22.0425 / 13.2238`, `51.6213 / 34.8783`). Do not complete LibriSpeech-other/WenetSpeech and do not promote Stage136. The mismatch between Stage131 private CommonVoice EN improvement and public CommonVoice EN regression shows the current private guard is too clean/easy for CV selection. Stage137 should build a non-public CommonVoice EN stress guard from Stage61/Stage22 training rows using existing difficulty metadata (`_stage43_primary_error`, WER/CER, difficulty tier), excluding Stage129/135 train ids, Stage131 private ids, public selector ids, and accepted teacher ids. Sample balanced CV stress rows with separate hard, medium, and easy-anchor buckets under `/media/usbhd/rwkvasr_runs/stage137_cv_stress_guard_20260623`, then evaluate Stage110, Stage119 step128, Stage134 step64/128, and the Stage136 private-best soup on this CV-only guard before any further training. This is evaluation/data-selection infrastructure only and does not change the BiRWKV/RWKV-7 TimeMixer architecture, bidirectional wrapper, SenseVoice frontend, tokenizer, CTC objective, direction policy, or fixed public decode policy.
					- 2026-06-23 Stage137 CV-stress guard result and Stage138 plan: added `scripts/build_stage137_cv_stress_guard.py` and `scripts/summarize_stage137_cv_stress_results.py`. The guard builder strictly excluded public selector ids, Stage129/135 train ids, Stage131 private ids, and CommonVoice EN accepted teacher ids, then sampled all remaining difficulty-tagged CommonVoice EN rows from Stage61: `109` hard, `167` medium, and `70` easy rows (`346` total). Stage137 captures the public CommonVoice failure direction that Stage131 missed. Stage110 baseline on this guard is total `37.2327 / 25.1142`; Stage119 step128 and Stage136 private-best both regress WER to `37.2633` while improving CER to `25.1013`, driven by medium-bucket changes. Stage134 step64 ties Stage110 WER but worsens CER to `25.1270`; Stage134 step128 again regresses WER to `37.2633`. Therefore Stage137 should become a required non-public CV gate before public CV evaluation: reject any future candidate with Stage137 total WER above Stage110, even if private CER improves. Stage138 should run a CV-stress-aware low-amplitude checkpoint screen, not a broad public search: recreate or evaluate candidates with effective Stage119 weight at or below the known public/CV threshold (`<=0.50`), optionally with a small Stage134 component, and require both Stage137 CV-stress WER no-regression and Stage131 private total/CER improvement before any public first-pass. Keep the BiRWKV/RWKV-7 TimeMixer architecture, frontend, tokenizer, CTC objective, direction policy, and fixed public decode policy unchanged.
					- 2026-06-23 Stage138 CV-stress/private result and public first-pass plan: generated three low-amplitude soups under `/media/usbhd/rwkvasr_runs/stage138_cvstress_aware_low_soups_20260623`. Pure `s119_0p50_s110_0p50` failed the new Stage137 CV-stress gate by regressing WER to `37.2633`, matching the public-CV brittleness. `s119_0p4375_s134s64_0p125_s110_0p4375` and `s119_0p50_s134s64_0p125_s110_0p375` both tied Stage110 exactly on Stage137 (`37.2327 / 25.1142`). Stage131 private guard then selected `s119_0p50_s134s64_0p125_s110_0p375`: total `30.0763 / 19.0961` versus Stage110 `30.0923 / 19.1169`, with private CommonVoice EN and LibriSpeech gains, GigaSpeech/WenetSpeech tied to Stage110, and no Stage137 CV-stress WER regression. This candidate is weaker than Stage119 private total but is the first Stage137-compatible private-improving soup. Run public first-pass on AISHELL-1, LibriSpeech-clean, and CommonVoice EN only, using the fixed `batch_size=1`, `mode=bi`, `beam8/topk16`, zero decode biases, and CTC normalization policy. Complete LibriSpeech-other/WenetSpeech only if CommonVoice EN and LibriSpeech-clean do not regress versus Stage110.
					- 2026-06-23 Stage138 public first-pass result and Stage139 plan: the selected low-amplitude soup `s119_0p50_s134s64_0p125_s110_0p375` completed the restricted public first pass under `/media/usbhd/rwkvasr_runs/stage138_cvstress_aware_low_soups_20260623/s119_0p50_s134s64_0p125_s110_0p375/public200_firstpass_b1_beam8_topk16`. It improved AISHELL-1 to `29.9148 / 29.9148`, tied Stage110 on CommonVoice EN at `51.6213 / 34.8783`, and improved LibriSpeech-clean WER to `22.0172`, but LibriSpeech-clean CER worsened from Stage110 `13.2238` to `13.2296`. This fails the strict no-regression gate, so do not complete LibriSpeech-other/WenetSpeech and do not promote Stage138. The useful signal is that Stage137 successfully blocked the previous public-CV regression, but the current private Libri guard did not catch the public Libri-clean CER boundary. Stage139 should add a non-public LibriSpeech-clean CER guard before any further public Libri-clean evaluation: sample a larger held-out LibriSpeech train guard from the Stage22 clean root, strictly excluding Stage110/119/129/134/135 training ids, Stage131 private ids, Stage30 selector ids, public-200 manifest ids, and accepted Libri teacher ids; because these rows do not carry Stage43 difficulty metadata, stratify by text length/frame length into short, medium, and long utterance buckets and evaluate Stage110 plus the known boundary candidates with the fixed `batch_size=1`, `mode=bi`, `beam8/topk16`, zero decode biases, and CTC normalization policy. Make Stage139 Libri CER no-regression a required non-public gate alongside Stage137 CV-stress WER no-regression before touching public AISHELL/Libri/CV again. This is evaluation/data-selection infrastructure only and does not change the BiRWKV/RWKV-7 TimeMixer architecture, bidirectional wrapper, SenseVoice frontend, tokenizer, CTC head/objective, direction policy, or decode policy.
					- 2026-06-23 Stage139 Libri CER guard result and Stage140 plan: added `scripts/build_stage139_libri_cer_guard.py` and `scripts/summarize_stage139_libri_cer_results.py`. The first attempted Stage22/Stage61-only guard was invalid because all unique Stage22/Stage61 Libri ids were already covered by the Stage110/119-era train/exclude set, so the final guard samples from the earlier clean root `/media/usbhd/training_data/asr/curriculum/clean_ctc_voxbox_webdataset/webdataset_lengths.jsonl`, whose shard layout matches the Stage22 root. After excluding `231,628` ids from Stage110/119/125/129/134/135 training, Stage104/108 Libri-teacher training/accepted rows, Stage131 private guard, Stage30 selector, and public-200 manifests, the available Libri pool remained large (`19,919` short, `54,054` medium, `99,914` long). The fixed Stage139 guard contains `1,024` unique Libri rows (`384/384/256` short/medium/long). Under `batch_size=1`, `mode=bi`, `beam8/topk16`, zero decode biases, and CTC normalization, Stage110 measured `23.7824 / 14.9017`. Stage119 step128 measured `23.7659 / 14.9017` overall but regressed short and long CER buckets. The failed Stage138 soup measured `23.7741 / 14.9037`, improving WER by `0.0083` points but adding exactly `2` character errors (`15,515` vs `15,513`) over `104,102` reference chars; this reproduces the public Libri-clean pattern where WER improves while CER slightly worsens. Therefore keep Stage139 as a required Libri guard: reject candidates with total Libri CER above Stage110 or with bucket-level short/long CER regression unless a later Spec entry explicitly relaxes this. Stage140 should run a Libri/CV-aware low-amplitude soup grid around the Stage138 boundary, reducing Stage119 and/or Stage134 weights while keeping Stage110 as anchor. Gate order is Stage137 CV-stress, Stage139 Libri CER, then Stage131 private guard; only candidates passing all three may receive the restricted public AISHELL/Libri-clean/CommonVoice first pass. This is checkpoint averaging only and does not change the BiRWKV/RWKV-7 TimeMixer architecture, bidirectional wrapper, SenseVoice frontend, tokenizer, CTC head/objective, direction policy, or fixed decode policy.
					- 2026-06-23 Stage140 Libri/CV-aware soup screen result and Stage141 plan: generated five Stage110-anchored soups under `/media/usbhd/rwkvasr_runs/stage140_libri_cv_aware_low_soups_20260623`, spanning Stage119 weights `0.375-0.500` and Stage134 step64 weights `0.0625-0.125`. All five tied Stage110 exactly on Stage137 CV-stress (`37.2327 / 25.1142`), so the new CV gate did not reject them. Stage139 Libri CER rejected all five: the strongest WER movements still added Libri character errors (`s119_0p500_s134s64_0p0625_s110_0p4375` measured `23.7741 / 14.9037`, `+2` char errors), while the lower-amplitude variants all measured `23.7824 / 14.9027`, which is still `+1` char error over Stage110 and has a long-bucket CER regression. No Stage140 candidate should receive Stage131 private or public first-pass evaluation. Deleted all five `model.pt` files after preserving `.soup.json`, Stage137 predictions/metrics, and Stage139 predictions/metrics. Stage141 should reduce the Stage119 movement below the observed `0.375` Libri-CER boundary and separately test very-low Stage134-only corrections: screen `s119<=0.3125` with small Stage134 weights plus `s134s64-only` anchors, using the same Stage137 -> Stage139 -> Stage131 gate order. Stop the soup line if Stage141 either fails Stage139 again or passes Stage139 only by becoming indistinguishable from Stage110 on private guard; in that case the next meaningful same-architecture step should be new data/teacher mining rather than further checkpoint interpolation.
					- 2026-06-23 Stage141 ultra-low soup result and Stage142 plan: generated five Stage110-anchored ultra-low soups under `/media/usbhd/rwkvasr_runs/stage141_ultralow_libri_cv_soups_20260623`: three with Stage119 weights `0.25-0.3125` plus Stage134 step64 weights `0.0625-0.125`, and two Stage134-only anchors (`0.0625` and `0.125`). All five passed Stage137 CV-stress exactly at Stage110 (`37.2327 / 25.1142`). Stage139 Libri CER passed all five: the three Stage119-containing soups measured `23.7783 / 14.9008` (`-1` char error vs Stage110, no short/long regression), `s134s64_0p0625_s110_0p9375` tied Stage110, and `s134s64_0p125_s110_0p875` measured `23.7783 / 14.8979` with the best Libri CER. However, Stage131 private guard showed every evaluated Stage141 movement candidate is behaviorally indistinguishable from Stage110 on private aggregate and source metrics: total `30.0923 / 19.1169`, clean `27.8225 / 18.0400`, hard `37.0838 / 24.2674`, exactly matching Stage110 and providing no private improvement. Therefore do not run public first-pass for Stage141 and stop the checkpoint-soup line for now. Deleted all five `model.pt` files after preserving `.soup.json`, Stage137/Stage139/Stage131 predictions, and metrics. Stage142 should switch to new same-architecture data/teacher mining rather than more interpolation: build a fresh non-public Libri/CV residual candidate pool from the clean root, excluding Stage137/139/131 guard ids, all recent train ids, public-200 manifest ids, and accepted teacher ids; decode the pool with Stage110 under the fixed policy; select residual rows where Stage110 has high character error but the rows are not in any guard; then decide whether to run a small teacher-label repair slice. This keeps the BiRWKV/RWKV-7 TimeMixer architecture, bidirectional wrapper, SenseVoice frontend, tokenizer, CTC objective, direction policy, and fixed decode policy unchanged.
					- 2026-06-23 Stage142 Libri CER residual mining partial result and teacher plan: extended `scripts/build_stage108_libri_teacher_cache.py` with a backward-compatible `--primary-metric {wer,cer,max}` scoring option; the default remains `wer`, and Stage142 uses `cer`. Built `/media/usbhd/rwkvasr_runs/stage142_libri_cer_residual_mining_20260623/librispeech/stage142_librispeech_candidate.lengths.jsonl` with `4,096` unique Libri candidates from `/media/usbhd/training_data/asr/curriculum/clean_ctc_voxbox_webdataset/webdataset_lengths.jsonl`, excluding `233,125` ids from Stage37/131/137/139 guards, public-200 manifests, recent Stage104/108/110/119/125/129/134/135 training sets, and prior accepted teacher caches. Stage110 fixed-policy decoding completed all four `1,024`-row parts. The first scoring attempt exposed a key mismatch because WebDataset prediction `utt_id` uses the prefixed `key`; fixed the scorer to map length rows by `utt_id`, `id`, `key`, `audio_member`, and stems. CER-based selection then scored all `4,096` rows with `missing_lengths=0` and selected `1,024` teacher targets with average Stage110 CER `25.2899%` versus candidate-pool average `14.4343%`; selected CER range is `18.4971%` to `100%`, average WER `38.7551%`, average frames `1063`, and average text chars `154.6`. Some top residuals are clear language/confusion failures, e.g. short Libri utterances decoded as Chinese. Next, extract audio for these `1,024` targets and run FunASR-Nano teacher, then filter accepted teacher rows conservatively before deciding on a Stage143 training slice.
					- 2026-06-23 Stage142 teacher result and Stage143 plan: extracted all `1,024` Stage142 Libri teacher-target utterances from the clean root into `/media/usbhd/rwkvasr_runs/stage142_libri_cer_residual_mining_20260623/librispeech/stage142_librispeech_audio_teacher_targets` (`190 MB`) and ran FunASR-Nano over four `256`-row shards. Added a backward-compatible `--improvement-metric {wer,cer,max}` filter option; default remains `wer`, while Stage142 uses `cer`. Conservative filtering with `improvement_metric=cer`, `min_improvement=0.10`, `max_teacher_wer=0.20`, and `max_teacher_cer=0.15` accepted `1,000/1,024` rows. Rejections were `17` by teacher WER, `5` by teacher CER, and `2` by insufficient CER improvement. Accepted rows have average Stage110 student `38.5098 / 25.1940` WER/CER versus teacher `1.8383 / 0.7196`, making this a much stronger Libri-CER repair signal than the previous interpolation surfaces. Stage143 should build a very small same-architecture training slice from Stage110: keep Stage110/129 original-label guard quotas unchanged, add the `1,000` Stage142 Libri-CER teacher rows with repeat expansion no larger than `2k-4k` effective rows, use Stage110 hidden+CTC-logit anchors at least `0.20 / 0.20`, LR no higher than `2.5e-10`, `max_steps=64`, no SpecAugment, no direction dropout, and gate first on Stage137 and Stage139 before Stage131/private/public. Do not alter BiRWKV/RWKV-7 TimeMixer, frontend, tokenizer, CTC head/objective, direction policy, or fixed decode policy.
					- 2026-06-23 Stage143 training/guard result and Stage144 plan: built `stage143_stage110_libri_cer_residual_teacher_guard` with `154,000` rows: `152,000` original-label guard rows plus `2,000` effective Stage142 Libri-CER teacher rows sampled from `865` unique accepted rows. Trained from Stage110 `wercer_best.pt` with Stage110 init/anchor, hidden+CTC-logit anchors `0.20 / 0.20`, LR `2.5e-10`, `max_steps=64`, `save_every=32`, no SpecAugment, no direction dropout, ZeRO-1, batch size `12`, and grad accumulation `4`; exported readable `step-32.pt`, `step-64.pt`, `best.pt`, and `epoch-1.pt`, then deleted the verified DeepSpeed shards. Guard results under the fixed `batch_size=1`, `mode=bi`, `beam8/topk16`, zero-bias, CTC-normalized policy show no promotable checkpoint. `step-32` ties Stage110 on Stage137 CV-stress (`37.2327 / 25.1142`) but regresses Stage139 Libri CER to `23.7907 / 14.9065` versus Stage110 `23.7824 / 14.9017`. `step-64` improves Stage139 Libri CER to `23.7907 / 14.8988` but regresses Stage137 CV-stress WER to `37.2633` versus `37.2327`. Do not run Stage131 private or public-200 for raw Stage143 checkpoints. Stage144 should first run an inference-only Stage110-anchored interpolation screen using Stage143 `step-64.pt` as the residual, because it contains the desired Libri-CER movement but crosses the CV boundary. Test low weights such as `0.125`, `0.25`, `0.375`, and `0.50`, gate in order Stage137 CV-stress -> Stage139 Libri CER -> Stage131 private, and only touch public AISHELL/Libri-clean/CommonVoice if a candidate passes all three with a real private gain. If Stage144 either fails Stage139/CV or passes only by becoming indistinguishable from Stage110, the next training slice should reduce teacher repeat pressure to `<=1,000` effective rows, strengthen anchors to at least `0.30 / 0.30`, and lower LR to `<=1e-10`; keep BiRWKV/RWKV-7 TimeMixer, bidirectional wrapper, SenseVoice frontend, tokenizer, CTC head/objective, direction policy, and fixed decode policy unchanged.
					- 2026-06-23 Stage144 interpolation result and Stage145 plan: generated Stage110-anchored soups from Stage143 `step-64.pt` with residual weights `0.125`, `0.25`, `0.375`, and `0.50` under `/media/usbhd/rwkvasr_runs/stage144_stage110_stage143_step64_interp_20260623`. Stage137 CV-stress rejected `w0.50` (`37.2633 / 25.1077`), while `w0.125`, `w0.25`, and `w0.375` tied Stage110 exactly (`37.2327 / 25.1142`). Stage139 Libri CER then rejected `w0.375` (`23.7865 / 14.9037`, `+2` char errors); `w0.125` and `w0.25` tied Stage110 exactly (`23.7824 / 14.9017`) with no Libri improvement. Stage131 private guard on the larger passing movement `w0.25` also exactly tied Stage110 total/source metrics: total `30.0923 / 19.1169`, clean `27.8225 / 18.0400`, hard `37.0838 / 24.2674`. Therefore do not run public-200 for Stage144; delete the temporary `model.pt` soup weights after preserving `.soup.json`, predictions, previews, and metrics. Stage145 should run one more same-architecture conservative training slice from Stage110 using the Stage142 Libri-CER teacher cache, but halve teacher repeat pressure to `1,000` effective rows, strengthen Stage110 hidden+CTC-logit anchors to `0.30 / 0.30`, reduce LR to `1e-10`, save every `16` steps up to `64`, and keep no SpecAugment/no direction dropout/ZeRO-1/batch size `12`/grad accumulation `4`. Gate every checkpoint first on Stage137 and Stage139; only run Stage131/private if a checkpoint both avoids CV WER regression and improves or exactly preserves Stage139 total/short/long CER. If Stage145 still either ties Stage110 or crosses one of these boundaries, stop Libri-only teacher repair and switch to a broader multi-domain residual-mining slice so that CV preservation and Libri repair are trained together, still without architecture or decode-policy changes.
					- 2026-06-23 Stage145 conservative Libri-only repair result and Stage146 plan: built `stage145_stage110_libri_cer_residual_teacher_guard_conservative` with `153,000` rows: `152,000` original-label guard rows plus `1,000` effective Stage142 Libri-CER teacher rows sampled from `634` unique accepted rows. Trained from Stage110 with LR `1e-10`, anchors `0.30 / 0.30`, `max_steps=64`, `save_every=16`, no SpecAugment, no direction dropout, ZeRO-1, batch size `12`, and grad accumulation `4`; exported readable `step-16/32/48/64`, `best`, and `epoch-1` checkpoints, then deleted verified DeepSpeed shards. Stage137 CV-stress tied Stage110 exactly for all four checkpoints (`37.2327 / 25.1142`). Stage139 Libri CER tied Stage110 exactly for `step-16`, `step-32`, and `step-48` (`23.7824 / 14.9017` with unchanged buckets), while `step-64` crossed the Libri boundary (`23.7865 / 14.9037`). Stage131 private guard on the largest passing checkpoint `step-48` exactly tied Stage110 total/source metrics (`30.0923 / 19.1169`, clean `27.8225 / 18.0400`, hard `37.0838 / 24.2674`). Therefore do not run public-200 for Stage145. The conclusion is stable across Stage143-145: Libri-only residual teacher pressure either collapses back to Stage110 under strong anchoring or improves Libri-CER only after crossing CV/Libri boundary gates. Stage146 should stop Libri-only repair and build a small multi-domain teacher-preservation slice from Stage110: combine the new Stage142 Libri-CER cache with existing CommonVoice EN and Wenet/Giga teacher caches so useful movement is not one-domain-only. Keep total teacher pressure at or below Stage110-era levels, use low repeat per source, keep anchors at least `0.30 / 0.30`, LR no higher than `1e-10`, no SpecAugment, no direction dropout, and retain the same gate order Stage137 -> Stage139 -> Stage131 -> restricted public first-pass. This remains a data/label training-signal change only; BiRWKV/RWKV-7 TimeMixer, bidirectional wrapper, SenseVoice frontend, tokenizer, CTC head/objective, direction policy, and fixed decode policy remain unchanged.
					- 2026-06-23 Stage146 multi-domain low-pressure result and Stage147 plan: built `stage146_stage110_multidomain_residual_teacher_guard` with `156,000` rows: `152,000` original-label guard rows plus `4,000` effective teacher rows, using `1,000` each from Stage142 Libri-CER, Stage114 CommonVoice EN, Stage128 WenetSpeech, and Stage133 GigaSpeech caches (`2,282` unique sampled teacher ids). Trained from Stage110 with LR `1e-10`, anchors `0.30 / 0.30`, `max_steps=64`, `save_every=16`, no SpecAugment, no direction dropout, ZeRO-1, batch size `12`, and grad accumulation `4`; exported readable `step-16/32/48/64`, `best`, and `epoch-1` checkpoints, then deleted verified DeepSpeed shards. Stage137 CV-stress tied Stage110 exactly for all four checkpoints (`37.2327 / 25.1142`). Stage139 Libri CER tied Stage110 for `step-16` and `step-32`, while `step-48` and `step-64` improved total CER to `14.8998` with short unchanged and long improved (`13.9330` vs `13.9418`), so the multi-domain formulation preserves the Libri repair signal without crossing the CV boundary. Stage131 private guard on `step-64` tied Stage110 exactly (`30.0923 / 19.1169`, clean `27.8225 / 18.0400`, hard `37.0838 / 24.2674`). A restricted public first-pass on AISHELL-1, LibriSpeech-clean, and CommonVoice EN also tied Stage110 exactly: AISHELL `29.9489 / 29.9489`, LibriSpeech-clean `22.0425 / 13.2238`, CommonVoice EN `51.6213 / 34.8783`. Do not complete LibriSpeech-other/WenetSpeech and do not promote Stage146, but keep it as evidence that multi-domain teacher pressure is safer than Libri-only pressure. Stage147 should raise the same multi-domain recipe to `8,000` effective teacher rows (`2,000` per source), still below the Stage110-era `12,000` teacher pressure, while keeping LR `1e-10`, anchors `0.30 / 0.30`, `max_steps=64`, no SpecAugment, no direction dropout, and the same Stage137 -> Stage139 -> Stage131 -> restricted-public gate order. This remains a data/label-pressure change only and must not alter the BiRWKV/RWKV-7 TimeMixer architecture, bidirectional wrapper, frontend, tokenizer, CTC head/objective, direction policy, or fixed decode policy.
					- 2026-06-23 Stage147 multi-domain 8k result and Stage148 plan: built `stage147_stage110_multidomain_residual_teacher_guard_8k` with `160,000` rows: `152,000` original-label guard rows plus `8,000` effective teacher rows (`2,000` each from Stage142 Libri-CER, Stage114 CommonVoice EN, Stage128 WenetSpeech, and Stage133 GigaSpeech), sampled from `2,960` unique teacher ids. Trained from Stage110 with LR `1e-10`, anchors `0.30 / 0.30`, `max_steps=64`, `save_every=16`, no SpecAugment, no direction dropout, ZeRO-1, batch size `12`, and grad accumulation `4`; W&B initialization timed out but training completed, all top-level checkpoints were verified, the residual W&B atexit process was interrupted after checkpoint verification, and verified DeepSpeed shards were deleted. Stage137 CV-stress tied Stage110 exactly for all four checkpoints (`37.2327 / 25.1142`). Stage139 Libri CER improved most at `step-32`, measuring `23.7783 / 14.8979` with short unchanged and long improved (`13.9330`), while `step-48/64` measured `23.7824 / 14.8998` and `step-16` tied Stage110. Stage131 private guard on `step-32` again tied Stage110 exactly (`30.0923 / 19.1169`, clean `27.8225 / 18.0400`, hard `37.0838 / 24.2674`). Restricted public first-pass on `step-32` also tied Stage110 exactly on AISHELL-1, LibriSpeech-clean, and CommonVoice EN: `29.9489 / 29.9489`, `22.0425 / 13.2238`, and `51.6213 / 34.8783`. Do not complete LibriSpeech-other/WenetSpeech and do not promote Stage147. The useful trend is monotonic on Stage139 but still below public-200 resolution. Stage148 should test the upper safe teacher-pressure bound for this exact multi-domain recipe: `12,000` effective teacher rows (`3,000` per source), equal to the Stage110-era total teacher pressure but spread across four domains, with the same LR `1e-10`, anchors `0.30 / 0.30`, `max_steps=64`, no SpecAugment, no direction dropout, and the same gate order. If Stage148 still ties public or crosses Stage137/Stage139, stop increasing repeat pressure and switch from repeat amplification to mining fresh unique teacher rows, because repeated-cache pressure will have hit its useful limit.
					- 2026-06-23 Stage148 multi-domain 12k result and Stage149 plan: built `stage148_stage110_multidomain_residual_teacher_guard_12k` with `164,000` rows: `152,000` original-label guard rows plus `12,000` effective teacher rows (`3,000` each from the same Stage142 Libri-CER, Stage114 CommonVoice EN, Stage128 WenetSpeech, and Stage133 GigaSpeech caches), sampled from `3,208` unique teacher ids. Trained from Stage110 with LR `1e-10`, anchors `0.30 / 0.30`, `max_steps=64`, `save_every=16`, no SpecAugment, no direction dropout, ZeRO-1, batch size `12`, and grad accumulation `4`; training completed normally, all top-level checkpoints were verified, and verified DeepSpeed shards were deleted. Stage137 CV-stress tied Stage110 exactly for all four checkpoints (`37.2327 / 25.1142`). Stage139 Libri CER also tied Stage110 exactly for all four checkpoints (`23.7824 / 14.9017` with unchanged buckets). Therefore do not run Stage131 private or public-200 for Stage148. The repeat-pressure curve is now clear: `4k` and `8k` multi-domain pressure can create tiny non-public Libri guard improvements without public movement, while `12k` repeat pressure collapses back to Stage110 under strong anchors. Stage149 should stop reusing the same teacher caches with more replacement sampling and instead mine fresh unique residual teacher rows. Prioritize a balanced fresh pool across LibriSpeech, CommonVoice EN, GigaSpeech, and WenetSpeech, excluding Stage137/139/131 guard ids, public-200 manifests, recent train ids, and all accepted/sample teacher ids from Stage142/114/128/133/146/147/148. Decode fresh candidates with Stage110 under the fixed policy, select high-error rows per source, run FunASR-Nano teachers, filter conservatively, and only then build a new multi-domain training slice. This remains data/label mining only; no architecture, tokenizer, objective, direction policy, or decode-policy change is allowed.
					- 2026-06-23 Stage149 fresh multi-domain teacher mining result and Stage150 plan: sampled `4,096` fresh unique candidates for each of LibriSpeech, CommonVoice EN, GigaSpeech, and WenetSpeech under `/media/usbhd/rwkvasr_runs/stage149_fresh_multidomain_teacher_mining_20260623`, excluding guard/public/recent-train/prior-teacher ids. Stage110 fixed-policy decoding completed all `16,384` candidates with `batch_size=1`, `mode=bi`, `beam8/topk16`, zero decode biases, and CTC normalization. High-error selection picked `1,024` targets per source: LibriSpeech by CER averaged `25.4504%` selected CER, CommonVoice EN by WER averaged `75.8394%` selected WER, GigaSpeech by WER averaged `73.3853%`, and WenetSpeech by CER averaged `83.6713%`. FunASR-Nano teacher decoding and conservative filtering accepted `3,321` fresh rows total: LibriSpeech `1,000/1,024` with student `38.8524 / 25.2445` vs teacher `1.8620 / 0.7720`, CommonVoice EN `765/1,024` with student `74.1820 / 45.3678` vs teacher `4.9804 / 1.9393`, GigaSpeech `815/1,024` with student `71.0729 / 43.1380` vs teacher `2.8127 / 1.2726`, and WenetSpeech `741/1,024` with student `80.0925 / 82.3003` vs teacher `1.9644 / 1.9644`. Stage150 should build a low-pressure same-architecture training slice from Stage110 with the standard `152,000` original-label guard rows plus `4,000` effective fresh teacher rows (`1,000` per source, allowing mild replacement for sources with fewer accepted rows), using the Stage149 accepted caches only. Keep LR `1e-10`, hidden+CTC-logit anchors `0.30 / 0.30`, `max_steps=64`, `save_every=16`, no SpecAugment, no direction dropout, ZeRO-1, batch size `12`, grad accumulation `4`, Stage110 init/anchor, and the same Stage137 -> Stage139 -> Stage131 -> restricted-public gate order. This is a data/label refresh only; BiRWKV/RWKV-7 TimeMixer, bidirectional wrapper, SenseVoice frontend, tokenizer, CTC head/objective, direction policy, and fixed decode policy remain unchanged.
					- 2026-06-23 Stage150 fresh 4k low-pressure result and Stage151 plan: built `stage150_stage110_fresh_multidomain_residual_teacher_guard_4k` with `156,000` rows: the standard `152,000` original-label guard rows plus `4,000` effective fresh teacher rows (`1,000` per source) sampled from `2,270` unique Stage149 teacher ids. Trained from Stage110 with LR `1e-10`, hidden+CTC-logit anchors `0.30 / 0.30`, `max_steps=64`, `save_every=16`, no SpecAugment, no direction dropout, ZeRO-1, batch size `12`, and grad accumulation `4`; W&B synced normally, exported readable `step-16/32/48/64`, `best`, and `epoch-1`, then deleted the verified `29G` DeepSpeed shard directory. Stage137 CV-stress tied Stage110 exactly for all four checkpoints (`37.2327 / 25.1142`). Stage139 Libri CER tied Stage110 exactly for `step-16/32/48` (`23.7824 / 14.9017` with unchanged short/long buckets), while `step-64` crossed the Libri boundary (`23.7865 / 14.9037`, `+1` WER edit and `+2` CER edits). Stage131 private guard on `step-16/32/48` also tied Stage110 exactly on total, clean, hard, and every source bucket (`30.0923 / 19.1169`, clean `27.8225 / 18.0400`, hard `37.0838 / 24.2674`). Therefore do not run public-200 for Stage150. The conclusion is that fresh 4k teacher signal at `LR=1e-10` and `0.30 / 0.30` anchors is too constrained to move the model before the step64 Libri boundary crossing. Stage151 should keep the exact same Stage150 data/cache but allow a controlled larger residual: initialize and anchor to Stage110, set LR `2.5e-10`, anchors `0.20 / 0.20`, `max_steps=64`, `save_every=16`, no SpecAugment, no direction dropout, ZeRO-1, batch size `12`, and grad accumulation `4`. Gate all checkpoints in the same order Stage137 -> Stage139 -> Stage131 -> restricted public. This changes only optimization strength; the BiRWKV/RWKV-7 TimeMixer architecture, bidirectional wrapper, SenseVoice frontend, tokenizer, CTC head/objective, direction policy, and fixed decode policy remain unchanged.
					- 2026-06-23 Stage151 stronger fresh-4k result and Stage152 plan: trained the same Stage150 fresh-4k data from Stage110 with LR `2.5e-10`, hidden+CTC-logit anchors `0.20 / 0.20`, `max_steps=64`, `save_every=16`, no SpecAugment, no direction dropout, ZeRO-1, batch size `12`, and grad accumulation `4`; W&B synced normally, exported readable `step-16/32/48/64`, `best`, and `epoch-1`, then deleted the verified `29G` DeepSpeed shard directory. Stage137 CV-stress tied Stage110 exactly for all four checkpoints (`37.2327 / 25.1142`). Stage139 Libri CER showed the desired movement: `step-16` improved total CER to `14.8998`, `step-48` and `step-64` improved further to `23.7824 / 14.8979` with short unchanged and long CER improved to `13.9330`, while `step-32` kept total CER tied but added one WER edit. Stage131 private guard rejected `step-48` and `step-64` despite the Libri CER gain because total private WER rose by one edit (`30.0976 / 19.1169`, `5643/11958` errors) while clean WER also rose by one edit; hard, GigaSpeech, and WenetSpeech stayed tied. Therefore do not run public-200 for raw Stage151 checkpoints. Stage152 should run an inference-only Stage110-anchored interpolation screen using Stage151 `step-48.pt` as the residual, because it has the best Libri CER gain but only a one-edit private WER regression. Test low-to-mid Stage151 weights (`0.25`, `0.50`, `0.75`) under the fixed decode policy and gate in order Stage137 -> Stage139 -> Stage131; only run restricted public if a soup preserves the Stage139 Libri CER gain without the private WER regression. This is checkpoint averaging only and must not alter BiRWKV/RWKV-7 TimeMixer, bidirectional wrapper, frontend, tokenizer, CTC objective, direction policy, or decode policy.
					- 2026-06-23 Stage152 interpolation partial result and completion plan: generated Stage110-anchored soups from Stage151 `step-48.pt` with residual weights `0.25`, `0.50`, and `0.75` under `/media/usbhd/rwkvasr_runs/stage152_stage110_stage151_step48_interp_20260623`. Stage137 CV-stress tied Stage110 exactly for all three weights (`37.2327 / 25.1142`). Stage139 Libri CER showed no useful movement for `w0.25`, while `w0.50` and `w0.75` preserved the Stage151 Libri gain at `23.7824 / 14.8979` with short unchanged and long CER improved to `13.9330`. Stage131 private guard accepted `w0.50` by tying Stage110 exactly (`30.0923 / 19.1169`, clean `27.8225 / 18.0400`, hard `37.0838 / 24.2674`) and rejected `w0.75` due to the same one-edit total/clean WER regression as raw Stage151. Restricted public first-pass for `w0.50` on AISHELL-1, LibriSpeech test-clean, and CommonVoice EN tied Stage110 exactly (`29.9489 / 29.9489`, `22.0425 / 13.2238`, and `51.6213 / 34.8783`). Because `w0.50` is the first candidate that keeps the non-public Libri-CER gain without private regression, complete the remaining fixed-policy public-200 datasets (LibriSpeech test-other and WenetSpeech TEST_NET) before deciding promotion. Promote only if the full public macro or a public dataset improves without any public guard regression; if full public ties, keep Stage110 as incumbent, delete temporary Stage152 soup weights after preserving metrics/predictions, and move to a new same-architecture training slice rather than further midpoint interpolation.
					- 2026-06-23 Stage152 full public result and Stage153 plan: completed the remaining `w0.50` fixed-policy public-200 evaluation by adding LibriSpeech test-other and WenetSpeech TEST_NET to the already completed AISHELL-1, LibriSpeech test-clean, and CommonVoice EN predictions. The full five-dataset public-200 result exactly tied Stage110 at macro `37.63563826758167 / 30.303032626572374`: AISHELL-1 `29.9489 / 29.9489`, LibriSpeech clean `22.0425 / 13.2238`, LibriSpeech other `36.7669 / 23.5995`, CommonVoice EN `51.6213 / 34.8783`, and WenetSpeech `47.7986 / 49.8647`. Therefore do not promote Stage152 despite its non-public Libri-CER guard gain; delete temporary Stage152 `model.pt` soup weights after preserving `.soup.json`, predictions, and metrics. Stage153 should stop midpoint interpolation and train a new same-architecture fresh-teacher slice from Stage110: use the Stage149 accepted fresh teacher caches with `8,000` effective teacher rows (`2,000` per source, replacement allowed where accepted rows are fewer), the standard `152,000` original-label guard rows, LR `1e-10`, hidden+CTC-logit anchors `0.30 / 0.30`, `max_steps=64`, `save_every=16`, no SpecAugment, no direction dropout, ZeRO-1, batch size `12`, and grad accumulation `4`. Gate in order Stage137 -> Stage139 -> Stage131 -> restricted public first-pass, then full public only if the candidate improves a guard or public metric without regression. This remains a data/label-pressure optimization only; BiRWKV/RWKV-7 TimeMixer, bidirectional wrapper, SenseVoice frontend, tokenizer, CTC head/objective, direction policy, and fixed decode policy remain unchanged.
					- 2026-06-23 Stage153 fresh-8k strong-anchor result and Stage154 plan: built `stage153_stage110_fresh_multidomain_residual_teacher_guard_8k` with `160,000` rows: `152,000` original-label guard rows plus `8,000` effective Stage149 fresh teacher rows (`2,000` each from LibriSpeech, CommonVoice EN, GigaSpeech, and WenetSpeech), sampled from `2,951` unique teacher ids. Trained from Stage110 with LR `1e-10`, hidden+CTC-logit anchors `0.30 / 0.30`, `max_steps=64`, `save_every=16`, no SpecAugment, no direction dropout, ZeRO-1, batch size `12`, and grad accumulation `4`. The training loop completed all `64` steps; W&B `finish()` hung after checkpoint save and was interrupted, but `step-16/32/48/64`, `best`, and `epoch-1` all loaded with `12,613` model tensors and verified DeepSpeed shards were deleted. Stage137 CV-stress tied Stage110 exactly for all checkpoints (`37.2327 / 25.1142`). Stage139 Libri CER tied Stage110 exactly for `step-16` and `step-32` (`23.7824 / 14.9017`), while `step-48` and `step-64` crossed the Libri boundary (`23.7865 / 14.9037`, `+1` WER edit and `+2` CER edits). Stage131 private guard on `step-16` and `step-32` also tied Stage110 exactly (`30.0923 / 19.1169`, clean `27.8225 / 18.0400`, hard `37.0838 / 24.2674`). Therefore do not run public-200 for Stage153. Stage154 should keep the exact Stage153 fresh-8k dataset but allow a slightly larger residual before falling back to interpolation: initialize and anchor to Stage110, set LR `1.5e-10`, hidden+CTC-logit anchors `0.25 / 0.25`, keep `max_steps=64`, `save_every=16`, no SpecAugment, no direction dropout, ZeRO-1, batch size `12`, and grad accumulation `4`. Gate all checkpoints in the same order Stage137 -> Stage139 -> Stage131 -> restricted public. If Stage154 improves Libri/CV but crosses private by only one edit, run a Stage110-anchored interpolation screen; if it only ties or crosses Stage137/Stage139, stop increasing fresh-repeat pressure and mine another unique teacher pool or change source weighting. This is optimization strength only and must not alter the BiRWKV/RWKV-7 TimeMixer architecture, bidirectional wrapper, SenseVoice frontend, tokenizer, CTC head/objective, direction policy, or fixed decode policy.
					- 2026-06-23 Stage154 stronger fresh-8k result and Stage155 plan: trained the exact Stage153 fresh-8k dataset from Stage110 with LR `1.5e-10`, hidden+CTC-logit anchors `0.25 / 0.25`, `max_steps=64`, `save_every=16`, no SpecAugment, no direction dropout, ZeRO-1, batch size `12`, grad accumulation `4`, and W&B disabled to avoid the Stage153 `finish()` hang; all top-level checkpoints loaded with `12,613` model tensors and verified DeepSpeed shards were deleted. Stage137 CV-stress still tied Stage110 exactly for all four checkpoints (`37.2327 / 25.1142`), but Stage139 rejected every checkpoint: `step-16/32` measured `23.7865 / 14.9037`, `step-48` measured `23.7865 / 14.9027`, and `step-64` measured `23.7824 / 14.9027` (`+1` CER edit despite WER tying Stage110). Therefore do not run Stage131/private or public-200 for Stage154. The failure mode is now clear: increasing optimization strength on a Libri-including fresh-repeat slice crosses the Libri medium bucket before any public-facing signal appears. Stage155 should change source weighting instead of increasing repeat pressure: build a same-architecture non-Libri fresh-teacher slice from Stage110 using the standard `152,000` original-label guard rows plus `6,000` effective Stage149 teacher rows (`2,000` each from CommonVoice EN, GigaSpeech, and WenetSpeech, no Libri teacher rows), with LR `1.5e-10`, hidden+CTC-logit anchors `0.25 / 0.25`, `max_steps=64`, `save_every=16`, no SpecAugment, no direction dropout, ZeRO-1, batch size `12`, and grad accumulation `4`. Gate Stage155 first on Stage137 and Stage139; only run Stage131/private if Libri stays tied or better. The intent is to seek CV/Giga/Wenet movement while protecting the Libri guard, with no changes to BiRWKV/RWKV-7 TimeMixer, bidirectional wrapper, SenseVoice frontend, tokenizer, CTC head/objective, direction policy, or fixed decode policy.
					- 2026-06-23 Stage155 non-Libri fresh-6k result and Stage156 plan: built `stage155_stage110_fresh_nonlibri_teacher_guard_6k` with `158,000` rows: the standard `152,000` original-label guard rows plus `6,000` effective Stage149 teacher rows (`2,000` each from CommonVoice EN, GigaSpeech, and WenetSpeech, no Libri teacher rows), sampled from `2,114` unique teacher ids. Trained from Stage110 with LR `1.5e-10`, hidden+CTC-logit anchors `0.25 / 0.25`, `max_steps=64`, `save_every=16`, no SpecAugment, no direction dropout, ZeRO-1, batch size `12`, grad accumulation `4`, and W&B disabled; all top-level checkpoints loaded with `12,613` model tensors and verified DeepSpeed shards were deleted. Stage137 CV-stress tied Stage110 exactly for all four checkpoints (`37.2327 / 25.1142`). Stage139 Libri CER also tied Stage110 exactly for all four checkpoints (`23.7824 / 14.9017`), confirming that removing Libri teacher rows protects the Libri guard. Stage131 private guard on the largest-residual `step-64` tied Stage110 exactly (`30.0923 / 19.1169`, clean `27.8225 / 18.0400`, hard `37.0838 / 24.2674`). Therefore do not run public-200 for Stage155. Repeated pressure on the existing Stage149 accepted teacher rows has now shown three regimes: strong anchors tie, weaker Libri-including pressure crosses Stage139, and non-Libri pressure is safe but still invisible. Stage156 should stop repeat amplification and expand the unique teacher pool: reuse Stage149's already decoded `4,096` candidates per source, exclude all Stage149 accepted/teacher-sampled ids, select the next residual-error band per source, run FunASR-Nano teachers, filter conservatively, and build the next training slice from mostly unique fresh rows rather than higher repeat counts. Keep Stage110 init/anchor, fixed public decode policy, CTC objective, SenseVoice frontend/tokenizer, BiRWKV wrapper, and RWKV-7 TimeMixer architecture unchanged.
					- 2026-06-24 Stage156 data/label policy update: before launching another training recipe, rebuild the CTC-trainable data selection around mainstream ASR practice rather than only repeating accepted teacher rows. The supervised CTC pool should prioritize segmented, transcribed speech used by wav2vec 2.0 / Conformer-style ASR work (LibriSpeech for English clean read speech, AISHELL for Mandarin clean read speech) and add harder large-scale corpora only after filtering (GigaSpeech, WenetSpeech, Common Voice EN/CN). Each row must pass the project tokenizer/frontend CTC feasibility check: non-empty normalized target, no unknown token when requested, `target_tokens + adjacent_repeated_target_tokens <= logit_frames`, and the configured `ctc_logit_required_ratio`. CTC labels are pronounced-content labels only: strip punctuation and symbol markup, bracketed/angle non-speech tags, subtitle/timestamp spans, Whisper/SenseVoice control tokens, project decoder language-confirmation prefixes, speaker/control metadata, and rows that become empty after stripping. Use mainstream ASR teacher correction (current local default: FunASR-Nano/SenseVoice text output; Whisper or another strong ASR model is acceptable when logged) only through `ctc_label_override_cache_path`, with the teacher text normalized by the same CTC rules and conservatively filtered for empty output, tokenizer feasibility, CTC alignability, and excessive divergence from the original label when no trusted reference is available. Keep display/runtime text separate from CTC labels. This is a data/label-generation change only; it must not alter BiRWKV/RWKV-7 TimeMixer, bidirectional mode handling, SenseVoice frontend/tokenizer, the CTC objective/head, or the fixed public decode policy.
					- 2026-06-25 Stage156 execution plan: materialize a reusable FunASR-Nano CTC training-data artifact before more model training. Start from the already decoded Stage149 fresh multi-domain teacher pool (`3,321` accepted rows: LibriSpeech `1000`, CommonVoice EN `765`, GigaSpeech `815`, WenetSpeech `741`) because it has paired Stage110 student prediction, dataset reference, FunASR-Nano text, and conservative teacher filters. Merge accepted length rows and caches into a single Stage156 artifact, run the CTC-pronounced label builder with `teacher_text_tn` as the preferred label source, `sensevoice_tiktoken`, `sensevoice_lfr6`, and `text_normalization=ctc`, and emit both `webdataset_lengths.jsonl` and a training-ready `stage156_funasr_ctc_labels.cache.jsonl` keyed by `utt_id` with `ctc_text`. Compare Stage110 BiRWKV-CTC and FunASR-Nano on exactly the accepted rows using normalized WER/CER before launching any training. If the later Stage156/157 training curve still ties or moves too slowly under hidden/logit anchoring, then add a separate distillation plan that first exposes teacher logits/CTC posteriors from a model with a valid CTC head; the current local FunASR-Nano checkpoint is text-teacher-only because its CTC decoder weights are missing, so do not claim logits/CTC distillation until that teacher path is verified.
					- 2026-06-25 Stage156 FunASR-Nano CTC data result: materialized `/media/usbhd/training_data/asr/curriculum/stage22_stage21_soup_clean_repair_mix/stages/stage156_funasr_nano_ctc_train_data`. The merged accepted teacher cache and lengths each contain `3,321` rows. The CTC-pronounced label build kept all `3,321` rows with zero drops and wrote `webdataset_lengths.jsonl`, `webdataset_lengths.summary.json`, `stage156_funasr_ctc_labels.cache.jsonl`, and `webdataset_buckets_audio_text/manifest.json`; label source is `teacher` for every row. Split counts are train `3,290` / eval `31`; source counts are LibriSpeech `1,000`, CommonVoice EN `765`, GigaSpeech/GSXL `815`, and WenetSpeech/WSL `741`. A bucketed dataloader smoke using SenseVoice LFR 560-dim features and `ctc_label_override_text_key=ctc_text` successfully produced real batches; example GigaSpeech rows with `BUT <COMMA>` and `HA <EXCLAMATIONPOINT>` become CTC labels `but` and `ha`, confirming non-pronounced punctuation tags are excluded. The same-row comparison report is `/media/usbhd/rwkvasr_runs/stage156_funasr_nano_ctc_train_data_20260625/stage156_funasr_vs_stage110_summary.json`: Stage110 sample-average WER/CER is `64.10 / 47.00`, while FunASR-Nano teacher is `2.84 / 1.43`; micro WER/CER improves from `50.75 / 31.40` to `2.83 / 1.22`. By source, teacher sample-average WER/CER is LibriSpeech `1.86 / 0.77`, CommonVoice EN `4.98 / 1.94`, GigaSpeech `2.81 / 1.27`, and WenetSpeech `1.96 / 1.96`. Teacher is better than Stage110 on WER for `3,321/3,321` rows and never worse on CER. This artifact is training-ready via `ctc_label_override_cache_path: .../stage156_funasr_ctc_labels.cache.jsonl` and `ctc_label_override_text_key: ctc_text`. Next training should use it as a small high-confidence teacher slice mixed with original guard rows; do not start logits/CTC distillation unless text-teacher training remains invisible and a valid CTC-logit teacher path is available.
					- 2026-06-25 Stage156 text calibration plan: before any new weight update, derive a text-only CTC calibration pack from `stage156_funasr_ctc_labels.cache.jsonl`. The pack should contain one JSONL row per accepted teacher sample with `utt_id`, source, language, normalized `ctc_text`, SenseVoice tokenizer `token_ids`, token count, character count, and the original train/eval split from the Stage156 length index. It should also write a plain text corpus, token frequency tables, per-source/per-language length distributions, and a smoothed label-prior vector over the SenseVoice CTC vocabulary. This prior is a label-sequence prior, not a CTC frame prior and not a blank calibration by itself; use it to audit CTC-head row coverage, spot over/under-represented token rows, and initialize later calibration experiments only with an explicit blank/nonblank policy. Do not use text-only calibration to change the BiRWKV encoder, frontend, tokenizer, CTC objective, or decode policy.
					- 2026-06-25 Stage156 text calibration result: generated `/media/usbhd/training_data/asr/curriculum/stage22_stage21_soup_clean_repair_mix/stages/stage156_funasr_nano_ctc_text_calibration` from the Stage156 FunASR-Nano CTC label cache. The pack contains `3,321` rows (`3,290` train / `31` eval), source counts LibriSpeech `1,000`, CommonVoice EN `765`, GigaSpeech `815`, and WenetSpeech `741`, with languages EN `2,580` and ZH `741`. SenseVoice tiktoken encoding produced `55,645` non-blank label tokens, `10,869` distinct token ids, and `17.96%` non-blank vocabulary coverage (`vocab_size=60,515`, `blank_id=60,515`). Validation found zero empty labels, zero exact encode/decode mismatches, zero out-of-range token ids, and all rows joined to the Stage156 length index. Files include `ctc_text_calibration.jsonl`, `ctc_text_corpus.txt`, `ctc_token_frequency.json`, `ctc_token_frequency.tsv`, `ctc_label_prior.json`, and `ctc_text_calibration.summary.json`. The prior remains a smoothed non-blank label-sequence prior only (`blank_probability=null`); use it for CTC-head row/frequency audits or later explicit bias calibration experiments, not as a frame-level blank prior or an architecture/decode-policy change.
					- 2026-06-26 Stage157 sufficiency judgment and expansion plan: Stage156 is sufficient for text/token-prior calibration but insufficient as a standalone CTC training/evaluation slice. `3,321` teacher rows are too small to expect a reliable anchored full-model update, and the automatic `31`-row eval split is only a smoke split, not a decision split. Before training, expand the FunASR-Nano teacher pool using already scored Stage149 candidates: take the next residual-error band after the original top-`1,024` targets for LibriSpeech, CommonVoice EN, GigaSpeech, and WenetSpeech, target about `2,048` additional teacher candidates per source, run FunASR-Nano, filter with the same conservative teacher WER/CER and CTC-pronounced label rules, then merge with Stage156. Treat the expanded teacher pool as sufficient for the next anchored experiment only if it reaches roughly `8k+` accepted teacher labels and can reserve about `1k` source-balanced teacher rows for evaluation while preserving a separate large original-label guard training mix. If the expanded pool falls short, continue mining from fresh candidates before training. This remains a data/label and training-schedule change only; do not alter the BiRWKV/RWKV-7 TimeMixer architecture, SenseVoice frontend/tokenizer, CTC objective/head, direction policy, or fixed public decode policy.
					- 2026-06-26 Stage157 expanded teacher data result and Stage158 training plan: selected `2,048` next-band Stage149 scored candidates per source after excluding the original Stage149 teacher targets and ran FunASR-Nano text teacher on `8,192` utterances. Conservative filtering accepted `7,386` new rows: LibriSpeech `1,738`, CommonVoice EN `1,877`, GigaSpeech `1,905`, and WenetSpeech `1,866`. Merging with Stage156 gives `10,707` accepted teacher rows; reserve a source-balanced heldout teacher eval split of `1,000` rows (`250` each source) and keep `9,707` train teacher rows. The CTC feasibility rebuild kept `10,707/10,707` rows with zero drops, wrote `/media/usbhd/training_data/asr/curriculum/stage22_stage21_soup_clean_repair_mix/stages/stage157_funasr_nano_ctc_expanded_pool`, `stage157_funasr_ctc_labels.cache.jsonl`, and a bucket manifest; a bucketed loader smoke successfully read both train and eval splits with SenseVoice LFR features and CTC label overrides. Build Stage158 as `152k` original-label guard rows plus `8k` source-balanced teacher rows (`2k` effective per source) from the Stage157 train pool, starting from Stage110 with LR `1e-10`, hidden+CTC-logit anchors `0.30 / 0.30`, `max_steps=64`, `save_every=16`, ZeRO-1, batch size `12`, grad accumulation `4`, no SpecAugment, no gradient checkpointing, and no direction dropout. Stage158's historical teacher sampler produced `4,544` unique sampled teacher ids for `8,000` effective teacher rows; this is acceptable for the immediate conservative run and should only be changed by a separate unique-sampling script if the run remains invisible. Validate first on the `1,000`-row Stage157 heldout teacher split and then on Stage137, Stage139, and Stage131 before any public-200 evaluation. The local FunASR-Nano path remains text-teacher-only because CTC decoder/logit weights are missing; do not claim logits/CTC distillation for this run.
					- 2026-06-26 Stage158 conservative training result and Stage159 plan: Stage158 trained successfully from Stage110 for `64` steps with LR `1e-10`, anchors `0.30 / 0.30`, no W&B, no SpecAugment, and no direction dropout. Top-level `step-16/32/48/64`, `best.pt`, and `epoch-1.pt` load with `12,613` model tensors. Final epoch train loss was `2.3574`; final rank0 step64 losses were total `2.7546`, CTC `2.5035`, hidden anchor `0.7960`, and CTC-logit anchor `0.0410`. Fixed heldout teacher evaluation on the `1,000`-row Stage157 eval manifest used `batch_size=1`, `mode=bi`, `beam8/topk16`, zero length/insertion/blank biases, and CTC normalization for both Stage110 and Stage158 `step-64`. Stage158 tied Stage110 exactly on every heldout row: overall sample-average `43.3299 / 32.1350` WER/CER and micro `36.4180 / 22.1217`, with `0/1000` rows improved or worsened. Per-source sample-average WER/CER also tied exactly: LibriSpeech `29.2489 / 17.9579`, CommonVoice EN `50.0297 / 31.5228`, GigaSpeech `39.7602 / 24.7500`, and WenetSpeech `54.2808 / 54.3093`. Do not run Stage137/139/131 or public gates for Stage158 because the checkpoint is prediction-identical to Stage110 on the new teacher-heldout eval. Stage159 should reuse the same Stage158 data artifact but increase update visibility conservatively: initialize from Stage110, set LR `2.5e-10`, hidden+CTC-logit anchors `0.20 / 0.20`, `max_steps=64`, `save_every=16`, ZeRO-1, batch size `12`, grad accumulation `4`, no SpecAugment, no gradient checkpointing, and no direction dropout. Validate Stage159 first on the same heldout teacher eval; only run Stage137/139/131 if it changes heldout predictions without a broad heldout regression.
					- 2026-06-26 Stage159 expanded FunASR-Nano teacher run result: Stage159 trained from Stage110 on the Stage158 data artifact for `64` steps with LR `2.5e-10`, hidden+CTC-logit anchors `0.20 / 0.20`, no W&B, no SpecAugment, and no direction dropout. Final epoch train loss was `2.2629`; final rank0 step64 losses were total `2.5952`, CTC `2.4382`, hidden anchor `0.7657`, and CTC-logit anchor `0.0194`. On the `1,000`-row Stage157 heldout FunASR-Nano teacher eval, fixed `batch_size=1`, `mode=bi`, `beam8/topk16`, zero-bias CTC decode improved Stage110 slightly: sample-average WER/CER `43.3098 / 32.1160` versus `43.3299 / 32.1350`, micro WER/CER `36.4002 / 22.1023` versus `36.4180 / 22.1217`. Only `6/1000` rows changed; WER was better/tie/worse `3/997/0`, CER `6/994/0`. Stage137 CV stress guard tied Stage110 exactly at `37.2327 / 25.1142` WER/CER with `0/346` changed rows. Stage139 Libri CER guard improved aggregate WER/CER from `23.7824 / 14.9017` to `23.7783 / 14.8950` (`-1` WER edit, `-7` CER edits), but changed `5/1024` rows and had paired WER better/tie/worse `2/1021/1`, CER `3/1019/2`; the short bucket CER regressed by one character while medium/long improved. Stage131 private guard tied Stage110 exactly across `1,152/1,152` rows: total `30.0923 / 19.1169`, clean `27.8225 / 18.0400`, hard `37.0838 / 24.2674`, and every per-source metric unchanged. Therefore Stage159 is a valid proof that the expanded teacher data is trainable and can move predictions without destabilizing broad guards, but it is not a promotion checkpoint because the gains are tiny and Stage139 has a few row-level regressions. Next same-architecture direction should either increase unique teacher exposure and/or add verified logits/CTC distillation from a teacher with an actual CTC head; keep BiRWKV/RWKV-7 TimeMixer, SenseVoice frontend/tokenizer, CTC head/objective, and the fixed decode policy unchanged.
					- 2026-06-26 Stage160/161 larger FunASR-Nano CTC data plan: treat the Stage157 `10,707` accepted teacher rows as still insufficient for meaningful CTC model improvement. First build Stage160 from the remaining already scored Stage149 residual band: after Stage149's original `1,024/source` teacher targets and Stage157's next `2,048/source`, select the final `1,024/source` from the `4,096/source` scored candidates for LibriSpeech, CommonVoice EN, GigaSpeech, and WenetSpeech; run FunASR-Nano text teacher, repair audit fields if needed, filter with the same conservative WER/CER gates, rebuild CTC-pronounced labels, and merge with Stage157. This is a fast incremental expansion but likely still below the required scale. If the merged pool remains below roughly `30k` accepted teacher labels, immediately start Stage161 fresh candidate mining from the large train pools with new exclusions for Stage149/157/160 accepted and target ids, using Stage110 fixed CTC predictions for scoring and FunASR-Nano for teacher text. Stage161 should target at least `8,192` fresh candidates per source and prefer scaling to `16,384/source` if throughput is acceptable, then reserve a source-balanced heldout teacher eval of at least `2k` rows before training. Also verify the FunASR-Nano CTC-logit path separately: upstream `fun_asr_nano/model.py` exposes `ctc_decoder -> self.ctc.log_softmax`, but the current local `assets/fun-asr-nano-2512/model.pt` load under FunASR `1.3.10` reports missing `ctc_decoder.*` and `ctc.ctc_lo.*` keys after remote-code fallback (`No module named 'model'`), so its CTC logits must not be used for distillation until a complete remote-code/weight pair is validated. Continue using FunASR-Nano text output for labels; do not alter BiRWKV/RWKV-7 TimeMixer, SenseVoice frontend/tokenizer, CTC head/objective, direction policy, or fixed decode policy.
					- 2026-06-26 Stage160 execution result and ModelScope CTC checkpoint correction: the ModelScope file API for `FunAudioLLM/Fun-ASR-Nano-2512` reports current `model.pt` size `2,127,426,538` bytes and sha256 `81fec8616083c69377f3ceef36aba3655660ee0ca69a5d4a1e9810cd340ca499`, while the existing local `assets/fun-asr-nano-2512/model.pt` is `1,971,149,431` bytes with sha256 `55ae0d2...`; this explains the missing `ctc_decoder.*` and `ctc.ctc_lo.*` load warnings. Download the current ModelScope weight into `/media/usbhd/models/Fun-ASR-Nano-2512-modelscope/model.pt`, verify the sha256, and only then consider FunASR-Nano CTC logits usable. Stage160 selected the remaining nonzero-error Stage149 residual candidates after excluding Stage149 and Stage157 teacher targets, but the final band was small: LibriSpeech `954`, CommonVoice EN `778`, GigaSpeech `505`, WenetSpeech `849` teacher candidates (`3,086` total). FunASR-Nano text generation completed for all `3,086`; conservative filtering accepted only `1,639`: LibriSpeech `0`, CommonVoice EN `595`, GigaSpeech `290`, WenetSpeech `754`. This confirms the Stage149 scored pool is exhausted and cannot supply enough new CTC supervision. Stage161 fresh mining has therefore started: sampled `8,192` new candidates each for LibriSpeech, CommonVoice EN, GigaSpeech, and WenetSpeech (`32,768` total), excluding the Stage149 candidate pool, Stage157 accepted pool, and Stage160 targets; GigaSpeech/WenetSpeech text recovery from tar metadata completed with `0` missing text. Next run Stage110 fixed CTC decode on these candidates, score residuals, select high-error teacher targets, then run FunASR-Nano text teacher and conservative CTC-label filtering.
					- 2026-06-26 FunASR-Nano ModelScope CTC weight verification: downloaded `/media/usbhd/models/Fun-ASR-Nano-2512-modelscope/model.pt` from the current ModelScope repo and verified sha256 `81fec8616083c69377f3ceef36aba3655660ee0ca69a5d4a1e9810cd340ca499`. Direct `torch.load` finds `1,347` tensors with `86` CTC-related keys, including `ctc_decoder.linear1.weight` and `ctc.ctc_lo.weight`; `AutoModel(..., model=/media/usbhd/models/Fun-ASR-Nano-2512-modelscope, device=cpu)` loads `FunASRNano` with `has_ctc_decoder=True`, `has_ctc=True`, and `has_ctc_tokenizer=True` without the old missing-CTC-key warnings. Therefore the ModelScope checkpoint is a valid candidate for later FunASR-Nano CTC argmax/logit export and distillation experiments. The older local `assets/fun-asr-nano-2512/model.pt` remains text-teacher-only because it still lacks those CTC weights. This verification changes only the teacher-data/logit-distillation route; it does not change BiRWKV/RWKV-7 TimeMixer, SenseVoice frontend/tokenizer, our CTC head/objective, direction policy, or fixed decode policy.
					- 2026-06-26 Stage161 fresh FunASR-Nano teacher result and Stage162 continuation plan: Stage161 decoded all `8,192/source` fresh candidates with the fixed Stage110 CTC policy, selected `4,096/source` high-residual teacher targets, ran FunASR-Nano text teacher, and conservatively filtered accepted labels. Accepted rows are LibriSpeech `4,033`, CommonVoice EN `3,359`, GigaSpeech `3,615`, and WenetSpeech `3,315` (`14,322` total). The summary is `/media/usbhd/rwkvasr_runs/stage161_funasr_nano_teacher_fresh_20260626/stage161_teacher_filter_all.summary.json`; average teacher errors on accepted rows are low (LibriSpeech WER/CER `0.0142/0.0055`, CommonVoice EN `0.0452/0.0172`, GigaSpeech `0.0278/0.0113`, WenetSpeech `0.0206/0.0206`) and the selected student errors remain high, so the rows are useful CTC correction data. However, Stage157 `10,707` + Stage160 `1,639` + Stage161 `14,322` gives only `26,668` accepted teacher labels, below the planned `30k+` threshold and before reserving a larger heldout teacher eval. Do not start training yet. Continue with Stage162 fresh mining from the same large pools, excluding Stage149/157/160/161 candidates and accepted targets, with at least `4,096` new candidates per source (or more if throughput remains acceptable), then repeat fixed Stage110 residual scoring, FunASR-Nano teacher labeling, conservative filtering, and CTC-pronounced label rebuild. Keep all architecture, tokenizer, CTC objective/head, direction policy, and decode-policy settings unchanged.
					- 2026-06-26 Stage162 sufficient-data result and Stage163 training plan: Stage162 sampled another `4,096/source` fresh candidates after excluding Stage149/157/160/161 data. Because the running accepted total crossed the `30k+` threshold after CommonVoice EN, GigaSpeech, and WenetSpeech completed, the incomplete Stage162 LibriSpeech teacher run was stopped and excluded from the pool. Stage162 accepted CommonVoice EN `1,674`, GigaSpeech `1,784`, and WenetSpeech `1,658` (`5,116` total), bringing the merged Stage157+160+161+162 accepted pool to `31,784` rows. The merged pool lives at `/media/usbhd/training_data/asr/curriculum/stage22_stage21_soup_clean_repair_mix/stages/stage162_funasr_nano_ctc_31k_pool`, with source counts LibriSpeech `6,771`, CommonVoice EN `8,270`, GigaSpeech `8,409`, and WenetSpeech `8,334`; it reserves `500/source` heldout rows (`2,000` eval total) and keeps `29,784` train rows. CTC-pronounced label rebuild kept `31,784/31,784` rows with zero drops and wrote `stage162_funasr_ctc_labels.cache.jsonl`, `webdataset_lengths.jsonl`, and `webdataset_buckets_audio_text/manifest.json`; a bucketed dataloader smoke produced SenseVoice LFR features with valid CTC targets. Build Stage163 as the immediate training verification: `152k` original-label guard rows plus `24k` teacher effective rows (`6k/source`) sampled from the Stage162 train pool, start from Stage110 `wercer_best.pt`, use LR `2.5e-10`, hidden+CTC-logit anchors `0.20 / 0.20`, `max_steps=128`, `save_every=32`, ZeRO-1, batch size `12`, grad accumulation `4`, no SpecAugment, no gradient checkpointing, and no direction dropout. Validate first on the Stage162 heldout teacher eval with the fixed CTC decode policy, then run Stage137/139/131 only if heldout predictions move without broad regression. This remains a data and training-schedule change only; do not change BiRWKV/RWKV-7 TimeMixer, SenseVoice frontend/tokenizer, CTC head/objective, direction policy, or fixed decode policy.
					- 2026-06-26 Stage163 result and Stage164 CTC-teacher plan: Stage163 trained from Stage110 on `152k` original-label guard plus `24k` effective FunASR-Nano text-teacher rows for `128` steps with LR `2.5e-10`, hidden+CTC-logit anchors `0.20 / 0.20`, no SpecAugment, no gradient checkpointing, and no direction dropout. Fixed Stage162 heldout teacher eval (`2,000` rows, `batch_size=1`, `mode=bi`, `beam8/topk16`, zero length/insertion/blank biases, CTC normalization) showed that the training signal is real but too weak for promotion. Versus Stage110 micro `0.389964 / 0.241611` WER/CER (`12691 / 30875` edits), Stage163 `step-32` is the only checkpoint that improves both metrics, but only to `0.389934 / 0.241595` (`12690 / 30873`, `1/2000` changed prediction). `step-96` gives the best WER edit count (`12688`, delta `-3`) but worsens CER to `30886` (delta `+11`), and `step-128` worsens CER further to `30889`. Do not run broad public or private gates for Stage163 and do not promote it. Because the current ModelScope `FunAudioLLM/Fun-ASR-Nano-2512` checkpoint has verified `ctc_decoder.*` and `ctc.ctc_lo.*` weights, Stage164 should add a reproducible FunASR-Nano CTC export path before more anchored text-label training. The first gate is a CTC-only export on the Stage162 heldout split: run FunASR-Nano `inference_prepare`, then `ctc_decoder` and `ctc.log_softmax` without LLM generation; record CTC argmax text, token ids, optional top-k/logit summaries, and compare against both FunASR-Nano LLM teacher text and original labels. Note that FunASR-Nano CTC uses `ctc_lo` shape `(60515, 512)` and `blank_id=60514`, while the current project CTC head uses `blank_id=60515`; therefore use CTC argmax text to rebuild project labels immediately, but do not do raw logits/KL distillation until vocabulary and blank alignment are explicitly mapped and verified. This Stage164 work is a teacher-data and distillation-tooling change only; keep BiRWKV/RWKV-7 TimeMixer, SenseVoice frontend/tokenizer, our CTC head/objective, direction policy, and fixed decode policy unchanged.
					- 2026-06-26 Stage164 FunASR-Nano CTC export result and Stage165 teacher-heavy plan: added `scripts/export_funasr_nano_ctc.py`, a CTC-only exporter that loads the current ModelScope FunASR-Nano checkpoint, runs `inference_prepare`, `ctc_decoder`, and `ctc.log_softmax`, and writes CTC argmax text plus lightweight blank/top1 statistics without invoking LLM generation or storing full logits. Heldout export on the Stage162 `2,000`-row eval split completed `2000/2000` with zero errors. FunASR-Nano LLM teacher remains much better than Nano CTC argmax against original labels: LLM teacher `2.37 / 1.01` WER/CER versus Nano CTC `10.16 / 6.23`; Nano CTC versus LLM teacher is `9.42 / 5.77`. Therefore do not replace LLM teacher labels with Nano CTC argmax labels. Use Nano CTC first as a confidence and distillation signal. The Stage162 train-pool Nano CTC export completed `29,784/29,784` with zero errors under `/media/usbhd/rwkvasr_runs/stage164_funasr_nano_ctc_export_20260626`. Train-pool CTC-vs-LLM consistency is micro `9.42 / 5.77` WER/CER, median sample `7.69 / 3.90`; `10,713` rows are exact CTC/LLM matches, `15,114` satisfy `WER<=0.10 and CER<=0.05`, and `21,875` satisfy `WER<=0.20 and CER<=0.10` with source counts CommonVoice EN `4,576`, GigaSpeech `5,897`, LibriSpeech `5,420`, and WenetSpeech `5,982`. Build Stage165 from the `WER<=0.20/CER<=0.10` CTC-consistent subset but still train on the LLM `teacher_text_tn` labels: sample `15k/source` effective teacher rows (`60k` total, with replacement) plus `60k` original-label guard rows (`20k` LibriSpeech, `15k` CommonVoice EN, `15k` WenetSpeech, `5k` AISHELL3, `2.5k` CommonVoice CN, `2.5k` GigaSpeech). Start from Stage110, anchor to Stage110, raise teacher exposure rather than changing architecture: LR `5e-10`, hidden+CTC-logit anchors `0.15 / 0.15`, `max_steps=256`, `save_every=32`, ZeRO-1, batch size `12`, grad accumulation `4`, no SpecAugment, no gradient checkpointing, and no direction dropout. Validate first on the same Stage162 heldout teacher eval and only run Stage137/139/131 if the heldout improvement is larger than Stage163 without CER regression. Raw logits/KL distillation remains a separate follow-up after mapping FunASR blank `60514` to project blank `60515` and masking the project-only id `60514`.
					- 2026-06-26 Stage165 teacher-heavy result and next direction: built `/media/usbhd/training_data/asr/curriculum/stage22_stage21_soup_clean_repair_mix/stages/stage165_stage110_funasr_nano_ctc_consistent_teacher_heavy_60k_guard_60k` with `120,000` rows: `60k` CTC-consistent FunASR-Nano LLM teacher effective samples (`15k/source`, `18,134` unique sampled teacher ids) plus `60k` original-label guard rows. A bucketed dataloader smoke read valid SenseVoice LFR features and CTC override targets. Training completed normally under `/media/usbhd/rwkvasr_runs/sensevoice_rwkv_stage165_stage110_funasr_nano_ctc_consistent_teacher_heavy_60k_guard_60k_bs12_lr5e10_hid0p15_kl0p15_zero1_nockpt_noaug_nodirdrop_4x4090` for `256` steps; epoch train loss was `2.5438`, and checkpoints `step-32` through `step-256` plus `best.pt` were exported. On the Stage162 heldout teacher eval, `step-256` is the best Stage165 point: micro WER/CER `0.389749 / 0.241595`, `12684 / 30873` edits, improving Stage110 by `-7 / -2` edits with `98/2000` changed predictions. Per-source WER deltas are non-positive, but CommonVoice CER regresses by `+2` chars; paired counts are WER better/tie/worse `25/1955/20` and CER `38/1925/37`. Stage137 CV stress guard does not pass cleanly: `step-256` is `+1` WER edit and CER tie versus Stage110, while `step-224` is also `+1` WER edit but `-3` CER chars. Stage139 Libri CER guard fails for both candidates: `step-256` is `+2` WER edits and `+22` CER chars versus Stage110, and `step-224` is `+1` WER edit and `+12` CER chars, mostly in the long bucket. Do not run Stage131 or promote Stage165 as-is. Stage165 proves that higher teacher exposure moves the model more than Stage163, but text-label continuation still trades against Libri/CV guards. Next low-cost screen should try Stage110/Stage165 checkpoint interpolation (small Stage165 weights over Stage110, first on Stage162 heldout plus Stage139/137) to see if the heldout gain can be retained without guard drift. If interpolation fails, stop relying on stronger text-label substitution alone and implement mapped FunASR-Nano CTC top-k/logit distillation, with FunASR blank `60514 -> project blank 60515` and project id `60514` masked, while preserving the BiRWKV/RWKV-7 TimeMixer architecture and fixed decode policy.
					- 2026-06-26 Stage166 implementation plan: prioritize direct CTC logits distillation over more text-only teacher continuation. Implement a thin top-k FunASR-Nano CTC distillation path: export per-utterance teacher frame top-k log-prob ids/values plus explicit blank probability from the verified ModelScope CTC head; map FunASR ids `0..60513` to the same project ids, map FunASR blank `60514` to project blank `60515`, and mask project id `60514`; train with a configurable top-k KL/CE loss keyed by `utt_id`, with time-axis nearest/linear mapping when teacher and student frame counts differ. Keep full logits off disk unless a later profiling pass proves it is affordable. This is a training-loss/data-cache change only, not an architecture, tokenizer, CTC head, direction policy, or decode-policy change.
					- 2026-06-26 Stage166 implementation smoke: added `scripts/export_funasr_nano_ctc_topk.py` and DeepSpeed training config keys `ctc_teacher_topk_cache_path`, `ctc_teacher_topk_loss_weight`, `ctc_teacher_topk_time_map`, and `ctc_teacher_topk_missing_policy`. The training loop now adds a sparse top-k CTC teacher CE term after the normal project CTC loss, keyed by `batch.utt_ids`, with Nano blank `60514` mapped to project blank `60515` and project-only id `60514` ignored before gather. The loss supports nearest and linear time mapping without changing model forward outputs. Syntax checks passed for the exporter, DeepSpeed training loop, and CLI; synthetic loss checks returned finite values for nearest/linear mapping and missing-cache skip mode. A real 4-row Stage162 eval export using `/media/usbhd/models/Fun-ASR-Nano-2512-modelscope` completed with zero errors under `/media/usbhd/rwkvasr_runs/stage166_funasr_nano_ctc_topk_probe_20260626`: average frames `106.0`, max frames `146`, tensor sidecars have shape `[frames, 16]`, contain project blank `60515`, contain no `60514`, and all log-probs are finite. The next step is to export the Stage162 train split in tensor-sidecar mode and run a short Stage166 distillation training slice from Stage110; keep the BiRWKV/RWKV-7 TimeMixer architecture, frontend, tokenizer, project CTC head, direction policy, and fixed decode policy unchanged.
					- 2026-06-26 Stage166 result and Stage167 adjustment: exported full Stage162 train Nano CTC top-k cache in 4 GPU shards under `/media/usbhd/rwkvasr_runs/stage166_funasr_nano_ctc_topk_train_20260626`; merged cache has `29,784` unique rows, `0` export errors, average `94.95` CTC frames, max `300`, source counts CommonVoice EN `7,770`, GigaSpeech `7,909`, LibriSpeech `6,271`, and WenetSpeech `7,834`. Tensor-sidecar validation sampled `61` records with no missing tensors, no project-id conflict `60514`, project blank `60515` present, and finite log-probs. Training with top-k CE weight `0.03` from Stage110 completed `256` steps under `/media/usbhd/rwkvasr_runs/sensevoice_rwkv_stage166_stage110_funasr_nano_ctc_topk_distill_bs12_lr5e10_hid0p15_kl0p15_topk0p03_zero1_nockpt_noaug_nodirdrop_4x4090`; final epoch train loss was `2.6396`, checkpoints `step-32` through `step-256` plus `best.pt` exported. On the `2,000`-row Stage162 teacher-heldout eval, Stage166 `step-64` is WER-best at `12,688 / 30,876` WER/CER edits versus Stage110 `12,691 / 30,875`, i.e. `-3 / +1` edits with only `7/2000` changed predictions; Stage166 `step-256` is CER-best at `12,693 / 30,862`, i.e. `+2 / -13` edits with `24/2000` changed predictions. FunASR-Nano's own CTC argmax remains far better against the same LLM teacher target (`3,065 / 7,373` edits, `9.42 / 5.77` WER/CER), so the gap is still optimization / student capacity under this recipe, not teacher availability. Do not run guard gates for Stage166 because it is weaker than Stage165's heldout best (`12,684 / 30,873`) and too small to promote. Stage167 should keep the same architecture, data, decode policy, Stage110 initialization, and Stage110 hidden/logit anchors, but raise `ctc_teacher_topk_loss_weight` from `0.03` to `0.10` to make the direct CTC distillation signal visible.
					- 2026-06-27 Stage167 result and guard decision: Stage167 reused the Stage166 data/cache and raised `ctc_teacher_topk_loss_weight` to `0.10` while keeping Stage110 initialization, Stage110 hidden/CTC-logit anchors `0.15 / 0.15`, no SpecAugment, no gradient checkpointing, no direction dropout, and the same BiRWKV/RWKV-7 TimeMixer architecture. Training completed `256` steps under `/media/usbhd/rwkvasr_runs/sensevoice_rwkv_stage167_stage110_funasr_nano_ctc_topk_distill_bs12_lr5e10_hid0p15_kl0p15_topk0p10_zero1_nockpt_noaug_nodirdrop_4x4090`; final epoch train loss was `2.8606`, with checkpoints `step-32` through `step-256` plus `best.pt`. On Stage162 teacher-heldout, `step-256` was best by WER then CER: `12,685 / 30,858` WER/CER edits versus Stage110 `12,691 / 30,875`, i.e. `-6 / -17` edits with `23/2000` changed predictions. `step-96` tied the WER gain with fewer changed rows (`12,685 / 30,866`, `-6 / -9`, `5/2000` changed). Broad guards do not justify promotion. `step-256` improved Stage139 Libri CER guard versus Stage110 (`5,759 / 15,501` vs `5,762 / 15,513`) and kept Stage137 CV WER tied but CER `+2` chars; however Stage131 private guard regressed to `5,646 / 11,971` edits versus Stage110 `5,642 / 11,958`. `step-96` was safer on private WER (`5,641`, `-1`) but still regressed private CER to `11,965` (`+7`), CV CER to `3,908` (`+3`), and Libri to `5,763 / 15,516` (`+1 / +3`). Therefore Stage167 proves direct Nano CTC top-k distillation gives a larger, real heldout signal than text-only continuation, but the current recipe is still blocked by broad-guard drift and by conflict with the Stage110 CTC-logit anchor. Do not promote Stage167; keep the architecture, frontend, tokenizer, project CTC head, direction policy, and fixed decode policy unchanged.
					- 2026-06-27 Stage168 plan: run one more narrow top-k CTC distillation slice that reduces the contradictory Stage110 CTC-logit anchor while retaining a hidden-state anchor as the stability guard. Start from Stage110 on the Stage166/167 data and top-k cache, keep `ctc_teacher_topk_loss_weight=0.10`, change `ctc_logit_anchor_loss_weight` from `0.15` to `0.05`, keep `encoder_anchor_loss_weight=0.15`, LR `5e-10`, `max_steps=256`, `save_every=32`, ZeRO-1, batch size `12`, grad accumulation `4`, no SpecAugment, no gradient checkpointing, and no direction dropout. Gate order is Stage162 teacher-heldout first, then Stage137/139, then Stage131 only for checkpoints that improve heldout without CV/Libri regression. This is still a loss-weight experiment only; do not change BiRWKV/RWKV-7 TimeMixer, SenseVoice frontend/tokenizer, project CTC head/objective, streaming/offline decode mode, or fixed decode policy.
					- 2026-06-27 Stage168 result: Stage168 trained to `256` steps with the planned reduced Stage110 CTC-logit anchor (`0.05`) and unchanged Nano CTC top-k weight (`0.10`). The run directory is `/media/usbhd/rwkvasr_runs/sensevoice_rwkv_stage168_stage110_funasr_nano_ctc_topk_distill_bs12_lr5e10_hid0p15_kl0p05_topk0p10_zero1_nockpt_noaug_nodirdrop_4x4090`; epoch train loss was `2.8545`, and checkpoints `step-32` through `step-256` plus `best.pt` were exported. On the Stage162 teacher-heldout eval, the selected checkpoints were: `step-128` `12,684 / 30,853` WER/CER edits (`-7 / -22` vs Stage110), `step-96` `12,686 / 30,866` (`-5 / -9`), and `step-256` `12,687 / 30,855` (`-4 / -20`). `step-128` is the best Stage168 point and the best direct CTC-distillation heldout result so far; reducing the Stage110 CTC-logit anchor was beneficial. Broad guards are mixed but much better than Stage167: Stage137 CV stress ties WER and improves CER by `-1` char versus Stage110 (`1,219 / 3,904` edits), Stage131 private improves by `-3 / -11` edits (`5,639 / 11,947`), but Stage139 Libri CER guard regresses by `+2 / +1` edits (`5,764 / 15,514`). `step-256` is worse on broad guards (`+1 / +7` CV, `+6 / +25` Libri, `+3 / -8` private), so do not use it. Stage168 `step-128` is therefore the current best non-promotion candidate: it shows direct Nano CTC top-k distillation can improve teacher-heldout and private/CV guards without changing architecture, but the remaining Libri micro-regression blocks strict promotion. Next Stage169 should be an evaluation-only checkpoint soup between Stage110 and Stage168 `step-128` to see whether a smaller movement preserves most heldout/private/CV gains while removing the `+2/+1` Libri guard drift. Do not change BiRWKV/RWKV-7 TimeMixer, SenseVoice frontend/tokenizer, project CTC head/objective, direction policy, or fixed decode policy.
					- 2026-06-27 Stage169 revised logits-distillation plan: user priority is to continue direct FunASR-Nano CTC logits distillation rather than run an interpolation-only soup. The current Stage166-168 top-k cache covers only `18,210/78,134` unique samples in the Stage165 training mix; the missing `59,924` unique samples are almost entirely original-label guard rows. Before the next training run, extend the FunASR-Nano CTC top-k export over the full Stage165 train length index in 4 GPU shards, dedupe by `utt_id`, store tensor sidecars, and validate that mapped ids contain project blank `60515` and never use project-only id `60514`. This is a disk-backed online distillation route: the teacher CTC forward is generated from the same training rows and consumed by `utt_id` during training, but kept outside the DeepSpeed step to avoid adding a second large model and raw-audio path dependency inside each training rank. After cache coverage is complete, train from Stage110 with the same BiRWKV/RWKV-7 TimeMixer architecture, SenseVoice frontend/tokenizer, project CTC head/objective, disabled direction dropout, and fixed decode policy. Use Stage168's more stable anchor balance (`encoder_anchor_loss_weight=0.15`, `ctc_logit_anchor_loss_weight=0.05`) and `ctc_teacher_topk_loss_weight=0.10`, switch `ctc_teacher_topk_missing_policy` to `error` if full coverage validates, and evaluate Stage162 teacher-heldout first, then Stage137 CV, Stage139 Libri, and Stage131 private. True in-step FunASR online distillation remains a later slice only after raw audio paths are carried through `CTCBatch` and a per-rank VRAM/throughput smoke proves it is worth the extra coupling.
					- 2026-06-27 Stage169 export implementation note: the full Stage165 unique index was written under `/media/usbhd/rwkvasr_runs/stage169_funasr_nano_ctc_topk_stage165_full_20260627` with `78,134` unique rows (`41,866` repeated training rows skipped). Because original-label guard rows usually have tar offsets but no standalone `audio_path`, extend `scripts/export_funasr_nano_ctc_topk.py` to accept `--webdataset-index-path` and `--audio-cache-dir`, resolve `shard_name` to tar path, and materialize audio by `audio_offset/audio_size` when needed. A 4-row probe including CommonVoice/Wenet guard rows exported with zero errors; tensor sidecars contained blank `60515`, no project-only `60514`, and finite shapes. This is still only teacher-cache tooling; it does not change the BiRWKV/RWKV-7 TimeMixer student, tokenizer, project CTC head, loss interface, or decode policy.
					- 2026-06-27 Stage169 full-cache result and training start gate: the 4-GPU FunASR-Nano CTC top-k export over the deduped Stage165 index completed successfully. The merged cache is `/media/usbhd/rwkvasr_runs/stage169_funasr_nano_ctc_topk_stage165_full_20260627/funasr_nano_ctc_topk_stage165_unique_merged.jsonl` with `78,118` train records, `0` row errors, `0` duplicate records, `0` missing train ids, and `0` missing tensor sidecars; the remaining `16` unique rows in the deduped index are `eval` split and are not consumed by the Stage165 train loader. Average teacher CTC frames are `108.39`, max `333`, and source counts are AISHELL3 `5,000`, CommonVoice CN `2,500`, CommonVoice EN `18,952`, GigaSpeech `7,306`, LibriSpeech `24,444`, and WenetSpeech `19,916`. A deterministic `543`-tensor validation sample found no project-only id `60514`, no missing project blank `60515`, and no non-finite top-k or blank log-probs. Delete the temporary audio materialization cache after export; retain only the merged JSONL, shard JSONLs, summaries, and tensor sidecars. Stage169 training is now allowed to start from Stage110 with `ctc_teacher_topk_missing_policy=error` using `configs/generated/sensevoice_rwkv_stage169_stage110_funasr_nano_ctc_topk_fullstage165_distill_4x4090_deepspeed.yaml`.
					- 2026-06-27 Stage169 training result: trained from Stage110 for `256` steps with the full Stage165 FunASR-Nano CTC top-k cache, `ctc_teacher_topk_loss_weight=0.10`, hidden anchor `0.15`, CTC-logit anchor `0.05`, LR `5e-10`, ZeRO-1, batch size `12`, grad accumulation `4`, no SpecAugment, no gradient checkpointing, and no direction dropout. Training completed under `/media/usbhd/rwkvasr_runs/sensevoice_rwkv_stage169_stage110_funasr_nano_ctc_topk_fullstage165_distill_bs12_lr5e10_hid0p15_kl0p05_topk0p10_zero1_nockpt_noaug_nodirdrop_4x4090`; epoch train loss was `2.8592`. Every logged step had `teacher_match=batch_size` and `teacher_missing=0`, confirming full logits-cache coverage in the DeepSpeed loop. Exported `step-32` through `step-256`, `best.pt`, and `epoch-1.pt`; after verifying top-level `.pt` files, delete this run's `ds_checkpoints/`, leaving the run directory at about `6.9G`. Next evaluate Stage162 teacher-heldout for `step-96`, `step-128`, `step-160`, and `step-256`; run Stage137/139/131 broad guards only for checkpoints that beat or match Stage168 heldout without obvious CER drift.
					- 2026-06-27 Stage169 teacher-heldout result: evaluated `step-96`, `step-128`, `step-160`, and `step-256` on the fixed 2,000-row Stage162 FunASR teacher-heldout set with `batch_size=1`, `mode=bi`, `beam8/topk16`, zero length/insertion/blank biases, and CTC metric normalization. Summary is `/media/usbhd/rwkvasr_runs/stage169_teacher_heldout_eval_20260627/stage169_selected_ckpts_vs_stage110_stage168_stage162_teacher_eval_summary.v2.json`. `step-160` is best at `12,679 / 30,842` WER/CER edits, improving Stage110 by `-12 / -33` edits and Stage168-step128 by `-5 / -11`; `step-128` is `12,684 / 30,857`, tying Stage168 WER but `+4` CER; `step-256` is `12,682 / 30,861`, `-2 / +8` versus Stage168. Use `step-160` as the first broad-guard candidate; evaluate Stage137 CV, Stage139 Libri, and Stage131 private before considering promotion. Keep architecture, tokenizer, project CTC head/objective, direction policy, and fixed decode policy unchanged.
					- 2026-06-27 Stage169 broad-guard result and Stage170 plan: Stage169 full-cache logits distillation fixed the Stage168 Libri guard drift but introduced small private/CV drift. Stage169-step160 broad guards are CV `1,220 / 3,902` (`+1 / -3` vs Stage110), Libri `5,760 / 15,507` (`-2 / -6`), and private `5,644 / 11,961` (`+2 / +3`). Stage169-step128 gives CV `1,219 / 3,905` (`0 / 0`), Libri `5,758 / 15,499` (`-4 / -14`), and private `5,646 / 11,964` (`+4 / +6`). Stage169-step96 gives CV `1,219 / 3,905` (`0 / 0`), Libri `5,757 / 15,492` (`-5 / -21`), and private `5,645 / 11,961` (`+3 / +3`). Therefore no raw Stage169 checkpoint passes strict no-regression, but the complementarity is useful: Stage168-step128 passes private/CV and fails Libri by only `+2/+1`, while Stage169-step96 passes CV/Libri and fails private by only `+3/+3`. Run Stage170 as evaluation-only checkpoint interpolation between Stage168-step128 and Stage169-step96 at Stage169 weights `0.25`, `0.50`, and `0.75`. First evaluate Stage162 teacher-heldout, then run Stage137/139/131 only for candidates retaining heldout gain; this changes no architecture, tokenizer, CTC head/objective, direction policy, data, or decode policy.
					- 2026-06-27 Stage170 interpolation result: created Stage168-step128 / Stage169-step96 soups at Stage169 weights `0.25`, `0.50`, `0.75`, and a fine boundary point `0.375` under `/media/usbhd/rwkvasr_runs/stage170_stage168_step128_stage169_step96_interp_20260627`. Heldout screening showed `w0p25` and `w0p375` both at `12,682 / 30,859` (`-9 / -16` vs Stage110), while `w0p50` regressed heldout CER (`12,689 / 30,885`) and `w0p75` was weaker (`12,686 / 30,872`). Broad guards for `w0p375` pass the strict Stage110 no-regression gate: Stage137 CV `1,219 / 3,904` (`0 / -1`), Stage139 Libri `5,762 / 15,510` (`0 / -3`), and Stage131 private `5,640 / 11,955` (`-2 / -3`). This is the first candidate in the FunASR-Nano CTC logits-distillation line that improves Stage162 teacher-heldout and does not regress the measured CV/Libri/private guards, without changing the BiRWKV/RWKV-7 TimeMixer architecture, SenseVoice frontend/tokenizer, project CTC head/objective, direction policy, or fixed decode policy. Preserve `w0p375` as the current promotion candidate; delete failed interpolation `.pt` files after preserving `.soup.json`, predictions, previews, and metrics.
					- 2026-06-27 Stage171 online CTC distillation plan: implement an in-training FunASR-Nano teacher path from the current Stage170 `w0p375` checkpoint. Keep the BiRWKV/RWKV-7 TimeMixer student architecture, SenseVoice frontend/tokenizer, project CTC head/objective, disabled direction dropout, and fixed decode policy unchanged. Add DeepSpeed config keys for `ctc_teacher_online_model_path`, `ctc_teacher_online_loss_weight`, `ctc_teacher_online_top_k`, `ctc_teacher_online_audio_cache_dir`, and optional index/device overrides. The online teacher runs under `torch.no_grad()` on each rank, resolves the current batch's `utt_id`s back to the WebDataset length index, materializes tar-backed audio into a disk cache when no standalone `audio_path` exists, forwards FunASR-Nano's CTC decoder, maps Nano blank `60514` to project blank `60515`, ignores project-only id `60514`, and feeds the resulting sparse top-k frame log-probs into the existing time-mapped top-k CE loss. The first Stage171 training config should initialize and anchor from `/media/usbhd/rwkvasr_runs/stage170_stage168_step128_stage169_step96_interp_20260627/w0p375/stage170_w0p375.pt`, use Stage169's full Stage165 train mix, use online top-k weight `0.10`, hidden anchor `0.15`, CTC-logit anchor `0.05`, LR `5e-10`, ZeRO-1, batch size `12`, grad accumulation `4`, no SpecAugment, no gradient checkpointing, and no direction dropout. Run a one-batch or very short smoke first to validate VRAM and teacher throughput before committing a full 128-256 step run.
					- 2026-06-27 Stage171 implementation/training result: added the online FunASR-Nano CTC top-k teacher helper and DeepSpeed integration without changing the BiRWKV/RWKV-7 TimeMixer student architecture, frontend, tokenizer, CTC head/objective, or direction policy. A direct 1-row teacher smoke produced top-k tensors of shape `[264, 16]`, mapped blank `60515`, no project-only `60514`, and finite log-probs. A 4-GPU 1-step DeepSpeed smoke from Stage170 `w0p375` succeeded with online teacher `12/12` matched, `0` missing, teacher forward about `0.97s`, and peak reserved memory about `16.75 GiB`. The full Stage171 run `/media/usbhd/rwkvasr_runs/sensevoice_rwkv_stage171_stage170_online_funasr_nano_ctc_distill_bs12_lr5e10_hid0p15_kl0p05_online0p10_zero1_nockpt_noaug_nodirdrop_4x4090` trained for `256` steps from Stage170 `w0p375` with online teacher weight `0.10`, hidden anchor `0.15`, CTC-logit anchor `0.05`, LR `5e-10`, ZeRO-1, batch size `12`, grad accumulation `4`, no SpecAugment, no gradient checkpointing, and no direction dropout. Every logged batch had `online_teacher_missing=0`; final epoch train loss was `2.8582`, and `step-32` through `step-256`, `best.pt`, and `epoch-1.pt` were exported. DeepSpeed state was deleted after top-level checkpoints were verified, leaving the run directory about `7.5G`.
					- 2026-06-27 Stage171 evaluation result: heldout predictions for `step-64`, `step-128`, `step-192`, and `step-256` were generated on the Stage162 2,000-row FunASR teacher-heldout set. Using the current `rwkvasr.eval.text_metrics` implementation consistently over Stage168/Stage170/Stage171 prediction JSONLs, `step-256` is best: `12,782 / 31,119` WER/CER edits versus Stage170 `w0p375` `12,789 / 31,145`, i.e. `-7 / -26` edits. The absolute heldout edit counts are not directly mixed with the older Stage170 legacy summary because that summary used an earlier metric/ref-normalization path; the internally consistent current-metric comparison is saved at `/media/usbhd/rwkvasr_runs/stage171_teacher_heldout_eval_20260627/stage171_vs_stage170_stage162_teacher_eval_summary.current_metrics.json`. Stage171 `step-256` also passes the measured broad guards relative to Stage170 `w0p375`: Stage137 CV `1,216 / 3,903` (`-3 / -1`), Stage139 Libri `5,760 / 15,510` (`-2 / 0`), and Stage131 private `5,640 / 11,945` (`0 / -10`). Relative to Stage110, the same guards are CV `-3 / -2`, Libri `-2 / -3`, and private `-2 / -13`. The guard/heldout comparison is `/media/usbhd/rwkvasr_runs/stage171_guard_eval_20260627/stage171_step256_guard_and_heldout_comparison.json`. Stage171 `step-256` supersedes Stage170 `w0p375` as the current measured promotion candidate, pending any larger public/full-set benchmark the project requires before claiming broader ASR progress.
					- 2026-06-27 Stage172 audio-only online CTC distillation plan: user direction is to use online FunASR-Nano CTC logits distillation and not depend on text labels; only resolvable audio plus `utt_id` is required. Implement an explicit `allow_missing_targets` data/training flag for this path. When enabled, WebDataset samples without `token_ids` or text may still produce features and empty packed CTC targets, and `RWKVCTCModel.joint_losses` must skip supervised CTC/decoder losses when their configured weights are zero while still returning encoded states, logits, and logit lengths for online teacher CE and optional anchor losses. Keep this flag defaulting to `false` so normal labeled CTC training and evaluation still fail fast on missing labels. Build the curriculum from Stage165 audio rows using difficulty metadata only for ordering/source balance: easiest rows first by source-aware score using audio length, existing Stage165 student-residual fields where present, and FunASR CTC confidence if available, but the Stage172 loss itself must be `ctc_loss_weight=0.0`, `decoder_loss_weight=0.0`, `ctc_teacher_topk_loss_weight=0.0`, and `ctc_teacher_online_loss_weight>0`. Start Stage172a from Stage171 `step-256`, use the same online teacher model path, map Nano blank `60514 -> 60515`, mask project-only `60514`, keep no SpecAugment/no direction dropout/no gradient checkpointing, and gate after each curriculum stage on Stage162 teacher-heldout plus Stage137/139/131 guards before proceeding to the next harder stage. This remains a data/optimization-signal change only; do not alter BiRWKV/RWKV-7 TimeMixer, bidirectional wrapper, SenseVoice frontend, tokenizer vocabulary, project CTC head, streaming/offline mode semantics, or fixed decode policy.
					- 2026-06-27 Stage172a implementation and result: implemented audio-only online CTC distillation support without changing the BiRWKV/RWKV-7 TimeMixer student architecture, frontend, tokenizer, CTC head, direction policy, or decode policy. `allow_missing_targets=true` now lets WebDataset training rows provide only audio/ids while forcing empty CTC targets, and supervised `ctc_loss_weight=0.0` plus `decoder_loss_weight=0.0` skips labeled losses while preserving logits/encoded outputs for FunASR-Nano online CTC top-k CE and anchors. Built `/media/usbhd/training_data/asr/curriculum/stage22_stage21_soup_clean_repair_mix/stages/stage172_audio_only_online_ctc_curriculum` with three 120k-row audio-only curriculum stages: easy `q=0.65`, medium `q=0.85`, and full-hard `q=1.0`; source-balanced targets are AISHELL3 `7,681`, CommonVoice CN `3,840`, CommonVoice EN `29,113`, GigaSpeech `11,223`, LibriSpeech `37,549`, and WenetSpeech `30,594` per stage. Stage172a trained from Stage171 `step-256` on the easy stage with online teacher weight `1.0`, hidden anchor `0.10`, CTC-logit anchor `0.02`, LR `2.5e-10`, batch size `12`, grad accumulation `4`, ZeRO-1, no SpecAugment, no gradient checkpointing, and no direction dropout; every logged batch had `online_teacher_missing=0`. Raw Stage172a `step-32` improved Stage162 heldout by `-2 / -2` WER/CER edits versus Stage171 but failed strict guard due private CER `+1`; a conservative soup `0.50 * Stage171 step-256 + 0.50 * Stage172a step-32` passed the gate: Stage162 heldout `12,779 / 31,112` (`-3 / -7` vs Stage171), private `5,640 / 11,945` (`0 / 0`), CV `1,216 / 3,903` (`0 / 0`), and Libri `5,759 / 15,507` (`-1 / -3`). Preserve `/media/usbhd/rwkvasr_runs/stage172a_stage171_step256_stage172a_step32_interp_20260627/w0p50/stage172a_w0p50.pt` as the current Stage172a promotion candidate; comparison summary is `/media/usbhd/rwkvasr_runs/stage172a_guard_eval_20260627/stage172a_interp_w0p50_guard_and_heldout_comparison.json`.
					- 2026-06-27 Stage172b medium curriculum plan: continue audio-only online FunASR-Nano CTC logits distillation from the gated Stage172a `w0p50` candidate on `stage172b_medium_online_ctc`, which contains `120,000` sampled training rows from a larger/easier-to-medium source-aware pool (`q=0.85`, `66,403` unique candidates) while still not using text labels for the loss. Initialize and anchor from `/media/usbhd/rwkvasr_runs/stage172a_stage171_step256_stage172a_step32_interp_20260627/w0p50/stage172a_w0p50.pt`, keep `allow_missing_targets=true`, `ctc_loss_weight=0.0`, `decoder_loss_weight=0.0`, `ctc_teacher_topk_loss_weight=0.0`, online teacher weight `1.0`, hidden anchor `0.10`, CTC-logit anchor `0.02`, LR `2.5e-10`, ZeRO-1, batch size `12`, grad accumulation `4`, no SpecAugment, no gradient checkpointing, and no direction dropout. Save every `16` steps for early-stopping resolution and train at most `128` steps before the first gate. Evaluate Stage162 teacher-heldout checkpoints first; run Stage137 CV, Stage139 Libri, and Stage131 private only for candidates that improve or at least do not regress heldout WER/CER versus Stage172a `w0p50`. Do not proceed to Stage172c until Stage172b has a candidate with strict non-regression on all measured guards.
					- 2026-06-27 Stage172b raw result and interpolation screen plan: Stage172b trained normally for `128` steps under `/media/usbhd/rwkvasr_runs/sensevoice_rwkv_stage172b_stage172a_w0p50_audio_only_online_funasr_nano_ctc_distill_bs12_lr2p5e10_hid0p10_kl0p02_online1p0_zero1_nockpt_noaug_nodirdrop_4x4090`; every logged step had supervised `ctc=-0.0000`, `decoder=-0.0000`, and `online_teacher_missing=0`, confirming the run used audio-only online distillation rather than labels. Heldout Stage162 current-metric screening over `step-16` through `step-128` found no raw checkpoint that passes the Stage172a `w0p50` gate: best WER is `step-112`/`step-128` at `12,778` edits (`-1` versus Stage172a), but CER regresses to `31,127`/`31,130` (`+15`/`+18`); earlier checkpoints also regress CER by at least `+7`. Therefore do not run broad guards on raw Stage172b and do not proceed to Stage172c. Run an evaluation-only interpolation screen between Stage172a `w0p50` and Stage172b `step-112` at Stage172b weights `0.25`, `0.50`, and `0.75`; promote only if a soup keeps Stage172a's heldout CER non-regression while improving or tying WER, then run the same Stage137/139/131 strict guard before any harder curriculum. This changes no architecture, tokenizer, CTC head/objective semantics, direction policy, or decode policy.
					- 2026-06-27 Stage172b retry plan: the Stage172b `step-112` interpolation screen also failed the Stage172a heldout gate: `w0p25` reached `12,781 / 31,119` (`+2 / +7` versus Stage172a), while `w0p50` and `w0p75` kept or worsened the CER regression. Delete the failed interpolation `.pt` files after preserving `.soup.json` and heldout JSONL/metrics. For the next medium retry, avoid moving the acoustic encoder: initialize from Stage172a `w0p50`, set `freeze_encoder=true`, train only the CTC head, reduce online teacher pressure to `0.30`, strengthen CTC-logit anchor to `0.10`, set hidden anchor to `0.0`, use LR `5e-9`, `max_steps=64`, `save_every=16`, and keep `allow_missing_targets=true`, supervised CTC/decoder losses at zero, no SpecAugment, no gradient checkpointing, and no direction dropout. Gate on Stage162 heldout first; if no checkpoint ties or improves both WER and CER versus Stage172a `w0p50`, stop the medium curriculum and keep Stage172a `w0p50` as the current best.
					- 2026-06-27 Stage172b2 head-only result and Stage172b3 plan: Stage172b2 trained as planned with encoder frozen, only the CTC head trainable (`31,044,708` trainable params), online teacher weight `0.30`, CTC-logit anchor `0.10`, LR `5e-9`, and zero supervised label loss. Heldout Stage162 metrics for `step-16`, `step-32`, `step-48`, and `step-64` all exactly matched Stage172a `w0p50` at `12,779 / 31,112`; direct prediction comparison confirmed `0/2000` decoded token/text changes, so this retry was too conservative and produced no useful medium-stage movement. Run one final medium head-only calibration attempt before stopping: keep encoder frozen and labels disabled, but raise LR to `5e-8`, online teacher weight to `0.50`, keep CTC-logit anchor `0.10`, `max_steps=64`, `save_every=16`, and gate on Stage162 heldout. If Stage172b3 does not improve or at least keep Stage172a `w0p50` on both WER and CER with changed predictions, stop Stage172 medium work and retain Stage172a `w0p50` as the current candidate rather than proceeding to Stage172c.
					- 2026-06-27 Stage172b3 final medium result: Stage172b3 trained normally with encoder frozen, online teacher weight `0.50`, CTC-logit anchor `0.10`, LR `5e-8`, and zero supervised labels. Heldout Stage162 results for `step-16`, `step-32`, `step-48`, and `step-64` again exactly matched Stage172a `w0p50` at `12,779 / 31,112`, and direct comparison found `0/2000` decoded token/text changes, although scores changed on up to `704/2000` rows by `step-64`. Therefore the medium audio-only head-only retries are insufficient to move CTC decode, while the full-model Stage172b run moved decode but caused unacceptable CER drift. Stop Stage172b/Stage172c progression for now; keep Stage172a `w0p50` as the current best audio-only online-distilled candidate. Any future medium retry should change the optimization signal more materially, e.g. controlled full-model update with stronger CTC-logit anchor and per-source guard replay, or a teacher/student logit temperature/blank-bias study before another training run.
					- 2026-06-27 Stage173 blank-aware CTC logits distillation plan: user direction is to push CTC logits distillation closer to FunASR-Nano CTC while preserving the BiRWKV/RWKV-7 TimeMixer architecture. Current online teacher top-k CE renormalizes the selected top-k probabilities, so it matches the teacher's relative preference among selected tokens but does not directly preserve the teacher's absolute blank probability or the remaining non-blank mass. This can move decode under full-model training but also drift CER. Add an explicit CTC blank-probability alignment term for both cached and online top-k teacher records: the FunASR online teacher should export per-frame `blank_log_probs`, the DeepSpeed loop should map those frames to student time with the existing `nearest/linear` policy, and the student should minimize binary cross-entropy between teacher blank probability and student softmax blank probability using the project blank id. Add config keys `ctc_teacher_topk_blank_loss_weight` and `ctc_teacher_online_blank_loss_weight`, defaulting to `0.0`, and log their losses separately. First verify with unit/smoke tests, then run a short Stage173a from Stage172a `w0p50` on Stage172b medium audio-only data with labels disabled, low full-model LR, strong Stage172a CTC-logit anchor, online top-k CE plus blank loss, and the same Stage162 heldout-first gate before any guard or harder curriculum.
					- 2026-06-27 Stage173a result and Stage174 plan: implemented blank-aware online CTC distillation and verified a 4-GPU smoke with `online_teacher_match=5/5`, `online_blank_match=5/5`, and finite `online_ctc_blank`. Stage173a trained for `96` steps from Stage172a `w0p50` on medium audio-only curriculum data with supervised CTC/decoder losses disabled, online top-k CE `0.50`, blank BCE `0.50`, hidden anchor `0.20`, CTC-logit anchor `0.10`, and LR `1e-10`. All logged batches had `online_teacher_missing=0` and `online_blank_missing=0`. The first Stage162 heldout decode was generated from a rebuilt manifest whose ref denominator differed from the established gate, so final scoring realigned candidate `ref_text`/`ref_token_ids` by `utt_id` to the existing Stage172a `w0p50` Stage162 eval JSONL. Under the aligned-ref gate, Stage173a did not beat the current candidate: `step-16` is exactly unchanged at `12,779 / 31,112` with `0/2000` changed predictions, `step-32` and `step-48` are `+1 / +2`, and `step-64/80/96` are `+2 / +8`. Summary is `/media/usbhd/rwkvasr_runs/stage173a_teacher_heldout_eval_20260627/stage173a_vs_stage172a_w0p50_stage162_eval_summary.aligned_ref.json`. Therefore keep Stage172a `w0p50` as current best. The next online CTC distillation slice should preserve more of the teacher distribution than top-k-conditional CE plus blank BCE: add a compact categorical distribution loss over `blank`, each selected nonblank top-k token, and an `other` bucket for the remaining probability mass. The teacher side uses exported top-k full log-probs plus explicit blank log-prob; the student side computes log-probs for the same categories with project blank `60515`, masks project-only id `60514`, and derives `other` by logsumexp over the vocabulary excluding blank and selected ids. Add config keys `ctc_teacher_topk_mass_loss_weight` and `ctc_teacher_online_mass_loss_weight`, defaulting to `0.0`, and log these losses separately. Stage174a should start from Stage172a `w0p50`, keep labels disabled, use online mass CE as the main signal with strong Stage172a CTC-logit anchor, run a short heldout-gated train first, and avoid proceeding to harder curriculum unless Stage162 aligned-ref WER/CER improves or at least ties with changed predictions.
					- 2026-06-27 Stage174a result and Stage174b plan: implemented compact `blank/top-k/other` CTC teacher mass CE for cached and online top-k records with config keys `ctc_teacher_topk_mass_loss_weight` and `ctc_teacher_online_mass_loss_weight`, defaulting to zero. Syntax checks, CLI exposure, a synthetic numerical check, and a 4-GPU smoke passed; the smoke showed `online_ctc_mass=4.2649`, `online_mass_match=5/5`, and `online_mass_missing=0` with old top-k/blank losses disabled. Stage174a trained from Stage172a `w0p50` on the medium audio-only curriculum for `96` steps with online mass CE `1.0`, hidden anchor `0.20`, CTC-logit anchor `0.10`, and LR `1.5e-10`; all logged steps had `online_mass_missing=0`. Aligned-ref Stage162 heldout screening failed the Stage172a gate: `step-16` is `12,780 / 31,114` (`+1 / +2`), `step-32` is `+3 / +10`, `step-48/64` are `+3 / +11`, and `step-80/96` are `+2 / +14`; summary is `/media/usbhd/rwkvasr_runs/stage174a_teacher_heldout_eval_20260627/stage174a_vs_stage172a_w0p50_stage162_eval_summary.aligned_ref.json`. Do not run broad guards and keep Stage172a `w0p50` as current best. Since mass CE moves predictions but medium data is negative, stay faithful to curriculum and retry the new loss on the easy audio-only split before any further medium/full-hard work: Stage174b should initialize and anchor from Stage172a `w0p50`, use `stage172a_easy_online_ctc`, labels disabled, online mass CE `1.0`, hidden anchor `0.20`, CTC-logit anchor `0.10`, LR `1e-10`, `max_steps=64`, `save_every=16`, no SpecAugment, no direction dropout, and heldout aligned-ref gate first. Promote or guard only if it improves or ties Stage172a `w0p50` with actual changed predictions; otherwise stop mass-CE continuation and revisit the teacher signal/row selection rather than pushing harder data.
					- 2026-06-27 Stage174b result: Stage174b trained the compact mass CE on the easy audio-only split for `64` steps from Stage172a `w0p50` with labels disabled, online mass CE `1.0`, hidden anchor `0.20`, CTC-logit anchor `0.10`, and LR `1e-10`; all logged batches had `online_mass_missing=0`. Aligned-ref Stage162 heldout screening again did not beat Stage172a: `step-16` is exactly unchanged at `12,779 / 31,112` with `0/2000` changed predictions, while `step-32/48/64` are `12,780 / 31,117` (`+1 / +5`) with only `1-2/2000` changed predictions. Summary is `/media/usbhd/rwkvasr_runs/stage174b_teacher_heldout_eval_20260627/stage174b_vs_stage172a_w0p50_stage162_eval_summary.aligned_ref.json`. Therefore stop the current mass-CE continuation and do not proceed to medium/full-hard with it. The evidence so far is that direct FunASR-Nano CTC logits can help only under carefully constrained earlier settings, while stronger online audio-only CTC-only objectives either do not move decode or move it against the LLM-teacher heldout gate. Keep Stage172a `w0p50` as current best. The next change should be teacher-signal selection rather than more force: e.g. mine rows/frames where Nano CTC is high-confidence or agrees with the stronger FunASR text/LLM teacher, then apply online/cached logits only on that subset, or add a small amount of text-teacher/CTC-label supervision for calibration while preserving the BiRWKV/RWKV-7 TimeMixer architecture.
					- 2026-06-28 Stage175 objective correction: the CTC distillation target is now explicitly `student CTC -> FunASR-Nano CTC`; FunASR LLM/text output and human-label WER/CER are not the primary gate for this phase. The purpose is to make the BiRWKV CTC head a strong Nano-CTC imitation foundation before a later LLM tuning stage. Keep the BiRWKV/RWKV-7 TimeMixer architecture, frontend, tokenizer vocabulary, CTC head shape, and streaming/offline mode semantics unchanged. Add a direct Nano-CTC imitation evaluator that aligns student CTC prediction JSONL and FunASR-Nano CTC export JSONL by `utt_id`, reports greedy text/token edit distance to Nano CTC, exact text/token agreement, length ratio, source breakdown, and available blank diagnostics. Re-score existing Stage172/173/174 candidate outputs against Nano CTC first; then select the next curriculum move by Nano-CTC imitation improvement rather than LLM-teacher heldout WER/CER. If the current output-level metrics show progress despite text-label regressions, continue with online logits/mass distillation; if they do not, adjust the distillation weight/temperature/frame objective while still using Nano CTC as the only teacher signal.
					- 2026-06-28 Stage175a plan: implement and smoke-test online full-vocabulary FunASR-Nano CTC KL distillation because top-k CE, blank BCE, and compact mass CE are only approximations of the Nano CTC frame distribution. The online teacher should optionally export mapped full-frame log probabilities with Nano blank `60514` moved to the project blank `60515` and project-only ignored id `60514` zeroed out. The training loss is framewise `KL(Nano CTC || student CTC)` after the existing nearest/linear frame-time mapping. Start from Stage172a `w0p50` on the easy audio-only curriculum with supervised CTC/decoder losses disabled, online full KL weight `1.0`, no top-k/mass/blank teacher losses, light Stage172a hidden/logit anchors, no SpecAugment, no direction dropout, no gradient checkpointing, and a 4-GPU `max_steps=2` smoke before any longer run. Gate with greedy Nano-CTC imitation metrics first: Nano WER/CER, token edit rate, exact text/token match, and blank probability difference.
					- 2026-06-28 Stage175a result and Stage175b plan: full-vocabulary online KL implementation passed syntax/CLI checks, a synthetic KL check, and a 4-GPU smoke. Smoke logged `online_ctc_full=4.6742` then `4.0512`, `online_full_missing=0`, and peak reserved memory about `6.75GiB`, so full-vocab online distillation is feasible on the target hardware. Stage175a then trained `64` steps on the easy audio-only curriculum from Stage172a `w0p50` with online full KL `1.0`, hidden anchor `0.05`, CTC-logit anchor `0.02`, and LR `2.5e-10`; every logged batch had `online_full_missing=0`. Greedy Nano-CTC imitation did not improve: step16 exactly matched Stage172a `w0p50`, while steps 32/48/64 slightly worsened Nano WER/CER or token ER. This means the conservative anchored full-model update is too weak for the new objective. Stage175b should keep the same online full KL teacher and easy audio-only data, but freeze the encoder, train only the CTC head, remove hidden/logit anchors, raise LR substantially, and gate again on greedy Nano-CTC imitation. If the head-only update still cannot improve token/text agreement with Nano CTC, the next retry should unfreeze the full model with a higher LR and no same-checkpoint anchor, because the objective for this phase is Nano-CTC alignment rather than LLM/text-label preservation.
					- 2026-06-28 Stage175b result and Stage175c plan: Stage175b trained only the CTC head from Stage172a `w0p50` for `64` steps with online full KL `1.0`, LR `5e-7`, no anchors, no labels, and frozen encoder. Training was stable, peak reserved memory was about `4.24GiB`, and every logged batch had `online_full_missing=0`. Greedy Nano-CTC imitation showed a small but real token-alignment gain at step64: Nano WER `38.4575 -> 38.4545`, Nano token ER `39.1210 -> 39.1012`, and exact text/token match `0.25% -> 0.30%`; Nano CER slightly worsened `24.7025 -> 24.7033`. This suggests the head-only direction can move toward Nano CTC but full KL alone is diluted by blank-heavy frames. Stage175c should continue from Stage175b `step-64`, keep the encoder frozen and labels disabled, and combine online full-vocabulary KL with online Nano top-k CE so nonblank/token ranking receives a stronger direct signal while full KL still preserves blank and tail mass. Keep no anchors, no SpecAugment, no direction dropout, save every `16` steps, and gate by greedy Nano-CTC imitation.
					- 2026-06-28 Stage175c result and Stage175d plan: Stage175c continued from Stage175b `step-64` with frozen encoder, online top-k CE `1.0`, online full KL `1.0`, LR `5e-7`, no anchors, and no labels for `128` steps. Training was stable and both online teacher losses had `missing=0`. Greedy Nano-CTC screening found no better token-alignment checkpoint than Stage175b `step-64`: Stage175c `step-112/128` had the lowest Nano WER (`38.4514`) but worse Nano CER (`24.7049`) and token ER (`39.1040`) than Stage175b `step-64` (`38.4545 / 24.7033 / 39.1012`). Keep Stage175b `step-64` as the current Nano-CTC imitation candidate by token ER. Since head-only updates are plateauing, Stage175d should initialize from Stage175b `step-64`, unfreeze the full BiRWKV model, keep labels/anchors disabled, and train with online top-k CE plus full KL at a conservative full-model LR. Gate again on greedy Nano-CTC imitation before moving to medium curriculum data.
					- 2026-06-28 Stage175d result and Stage175e plan: Stage175d initialized from Stage175b `step-64`, unfroze the full BiRWKV model, disabled labels and anchors, and trained on easy audio-only data for `64` steps with online top-k CE `1.0`, online full KL `1.0`, and LR `1e-9`. Training was stable with `online_teacher_missing=0` and `online_full_missing=0`; first-step peak reserved memory was about `5.86GiB`. Greedy Nano-CTC screening found the best balanced candidate at `step-48`: Nano WER/CER/token ER `38.4483 / 24.6976 / 39.1012`, improving Stage172a `w0p50` (`38.4575 / 24.7025 / 39.1210`) and improving or tying Stage175b token alignment. Stage175d `step-16` had better token ER (`39.0870`) but worse text/CER, so keep Stage175d `step-48` as the current balanced Nano-CTC imitation candidate. Stage175e should continue the curriculum from Stage175d `step-48` onto the medium audio-only split with the same Nano-only objective, labels/anchors disabled, conservative LR, and greedy Nano-CTC imitation as the gate before considering harder data.
					- 2026-06-28 Stage175e result and current CTC-phase gate: Stage175e continued from Stage175d `step-48` onto `stage172b_medium_online_ctc` with the same Nano-only objective, labels disabled, anchors disabled, online top-k CE `1.0`, online full KL `1.0`, and full-model LR `1e-9` for `64` steps. The medium split reported `66,403` unique online-teacher rows, first-step peak reserved memory was about `12.32GiB`, and all logged batches had `online_teacher_missing=0` plus `online_full_missing=0`. Greedy Nano-CTC imitation on the fixed `2,000`-row Stage162 heldout shows `step-16` as the current balanced Nano-CTC candidate: Stage172a baseline `38.4575 / 24.7025 / 39.1210`, Stage175d `step-48` `38.4483 / 24.6976 / 39.1012`, and Stage175e `step-16` `38.4483 / 24.6928 / 39.1012` for Nano WER/CER/token ER. Stage175e `step-48` has the best Nano CER (`24.6872`) but its Nano WER returns to the Stage172a baseline and token ER is worse than `step-16`, so do not promote it as the balanced checkpoint. The active phase objective remains only `student CTC -> FunASR-Nano CTC`; do not use FunASR LLM/text output or human-label gates to decide this phase. Before moving to harder data, either keep Stage175e `step-16` as the current checkpoint or run a small evaluation-only interpolation between Stage175e `step-16` and `step-48` and require it to improve or tie all three Nano-CTC imitation metrics without changing the BiRWKV/RWKV-7 TimeMixer architecture, SenseVoice frontend/tokenizer, CTC head, or streaming/offline semantics.
					- 2026-06-28 Stage175f interpolation result and Stage175g plan: created evaluation-only Stage175e `step-16` / `step-48` soups with `step-48` weights `0.25`, `0.50`, and `0.75`, then decoded the fixed Stage162 heldout with greedy CTC and compared only against FunASR-Nano CTC. After normalizing unlabeled prediction fields, the interpolation summary is `/media/usbhd/rwkvasr_runs/stage175_nano_ctc_imitation_eval_20260628/greedy_stage175ef_interp_vs_nano_ctc_summary.md`: `w25` gives the lowest Nano WER (`38.4330`) but worsens Nano CER/token ER to `24.7017 / 39.1266`; `w50` is `38.4360 / 24.6936 / 39.1323`; `w75` is `38.4545 / 24.6968 / 39.1436`. Therefore no interpolation point beats Stage175e `step-16` on the balanced Nano-CTC gate (`38.4483 / 24.6928 / 39.1012`), and the active checkpoint remains Stage175e `step-16`. The next training slice should stay on the medium curriculum instead of moving to hard data, initialize from Stage175e `step-16`, keep labels and anchors disabled, and increase the nonblank/top-k teacher pressure relative to full KL because the best WER interpolation points traded away token alignment. Stage175g should use online Nano top-k CE as the dominant term with full-vocabulary KL retained at lower weight to preserve blank/tail distribution, still with the same BiRWKV/RWKV-7 TimeMixer architecture and Nano-CTC-only gate.
					- 2026-06-28 Stage175g result and Stage175h plan: Stage175g trained from Stage175e `step-16` on the medium audio-only curriculum for `128` steps with labels/anchors disabled, online Nano top-k CE weight `3.0`, online full-vocabulary KL weight `0.5`, LR `7.5e-10`, ZeRO-1, no SpecAugment, no direction dropout, and no gradient checkpointing. The run completed normally; first-step peak reserved memory was about `12.32GiB`, final epoch train loss was `14.0518`, and all logged online teacher/full losses had `missing=0`. Greedy Nano-CTC screening summary is `/media/usbhd/rwkvasr_runs/stage175_nano_ctc_imitation_eval_20260628/greedy_stage175g_vs_nano_ctc_summary.md`. Stage175g `step-16` is the best point from this slice by Nano WER/token ER: `38.4422 / 24.6960 / 39.0983`, improving Stage175e `step-16` on WER and token ER (`38.4483 / 24.6928 / 39.1012`) but slightly regressing CER. Later Stage175g checkpoints mostly worsen CER and token ER, and `step-128` regresses all three relative to Stage175e `step-16`; do not continue this exact high-top-k recipe deeper. Keep Stage175g `step-16` as the current token/WER candidate and Stage175e `step-16` as the current CER-balanced candidate. Stage175h should be evaluation-only interpolation between those two early checkpoints to see whether a smaller high-top-k update can retain the Stage175g WER/token improvement while recovering Stage175e CER. Continue to gate only by direct Nano-CTC imitation.
					- 2026-06-28 Stage175h result and current Nano-CTC candidate: created Stage175e `step-16` / Stage175g `step-16` evaluation-only soups with Stage175g weights `0.25`, `0.50`, and `0.75`, decoded the fixed Stage162 heldout with greedy CTC, and scored only against FunASR-Nano CTC. Summary is `/media/usbhd/rwkvasr_runs/stage175_nano_ctc_imitation_eval_20260628/greedy_stage175h_vs_nano_ctc_summary.md`. Stage175h `w25` is the new current Nano-CTC imitation candidate: Nano WER/CER/token ER `38.4391 / 24.6912 / 39.0955`, improving Stage172a `w0p50` baseline (`38.4575 / 24.7025 / 39.1210`), Stage175e `step-16` (`38.4483 / 24.6928 / 39.1012`), and Stage175g `step-16` (`38.4422 / 24.6960 / 39.0983`). Stage175h `w50` has the best WER (`38.4360`) and same CER (`24.6912`) but worse token ER (`39.0983`), so prefer `w25` because the phase objective is CTC-output/token alignment to Nano CTC. Retained checkpoint: `/media/usbhd/rwkvasr_runs/stage175h_stage175e16_stage175g16_interp_20260628/w25/stage175h_w25.pt`; deleted failed `w50/w75` `.pt` files after preserving soup metadata and prediction summaries. Also deleted Stage175g `ds_checkpoints/` and `funasr_online_audio_cache/` after verifying top-level checkpoints, reducing that run directory to about `6.9G`. Do not move to harder data until the next slice has a clear Nano-CTC imitation plan; a plausible next step is to continue from Stage175h `w25` with a shorter, lower-LR medium run using moderate top-k pressure rather than the exact Stage175g high-top-k continuation, and still no labels or LLM/text gate.
					- 2026-06-28 Stage176 online Nano-CTC sequence distillation plan: frame-level full KL/top-k CE has produced only small output-level gains, so add an optional online teacher CTC-sequence loss that still requires only audio. The FunASR-Nano online teacher should return its collapsed CTC argmax token sequence from `ctc.log_softmax`, with Nano blank `60514` removed and nonblank ids kept in project id space; the DeepSpeed loop should add config key `ctc_teacher_online_sequence_loss_weight`, defaulting to `0.0`, and when enabled compute standard student CTC loss against those teacher token sequences using the student's `logits`/`logit_lengths`. This is pseudo-label sequence distillation from Nano CTC, not FunASR LLM/text or human labels. Keep supervised `ctc_loss_weight=0.0`, keep labels optional/missing, and retain the existing frame top-k/full KL terms as stabilizers. First verify syntax and a synthetic loss check, then run a short Stage176a from Stage175h `w25` on the medium audio-only split with a small teacher-sequence weight plus moderate top-k/full KL. Gate only by direct Nano-CTC imitation.
					- 2026-06-28 Stage176a implementation/result: implemented online Nano-CTC sequence distillation as planned. `FunASRNanoCTCTopKOnlineTeacher` now returns `argmax_token_ids`; `DeepSpeedTrainConfig` and `rwkvasr.cli.train_ctc_deepspeed` expose `ctc_teacher_online_sequence_loss_weight`; the DeepSpeed loop computes `F.ctc_loss` against teacher argmax sequences and logs `online_ctc_sequence`, `online_sequence_match`, and `online_sequence_missing`. Syntax checks, CLI exposure, and a synthetic sequence-loss check passed. A 4-GPU smoke from Stage175h `w25` with sequence weight `0.05`, top-k `1.0`, full KL `0.5` showed `online_sequence_match=batch`, `online_sequence_missing=0`, and peak reserved memory about `13.07GiB`. Full Stage176a then trained `64` steps on medium audio-only data with no supervised labels and no LLM/text gate; all logged sequence/top-k/full losses had `missing=0`, final epoch train loss was `6.0282`, and top-level checkpoints were exported. Greedy Nano-CTC screening summary is `/media/usbhd/rwkvasr_runs/stage175_nano_ctc_imitation_eval_20260628/greedy_stage176a_vs_nano_ctc_summary.md`. Stage176a did not beat Stage175h `w25`: `step-16` was `38.4452 / 24.6952 / 39.0955`, `step-32` `38.4391 / 24.6984 / 39.0983`, `step-48` `38.4422 / 24.7137 / 39.1097`, and `step-64` `38.4452 / 24.7105 / 39.1040` for Nano WER/CER/token ER, versus Stage175h `w25` `38.4391 / 24.6912 / 39.0955`. Therefore keep Stage175h `w25` as the current Nano-CTC candidate. The sequence-loss path is technically valid but this weight/recipe should not be continued; if revisiting it, use either much smaller sequence weight or isolated head-only calibration, and require direct Nano-CTC improvement before any longer run. Deleted Stage176a smoke and the full run's `ds_checkpoints/` plus `funasr_online_audio_cache/` after verifying top-level checkpoints.
					- 2026-06-28 Stage177 large-data online Nano-CTC alignment plan: the current online alignment pool is too small for the stated CTC objective. Stage172 medium has only `66,403` unique online-teacher rows, while the disk already has a merged WebDataset index covering `24,335,204` samples: clean `1,430,801` samples from `/media/usbhd/training_data/asr/curriculum/clean_ctc_voxbox_webdataset` and hard `22,904,403` samples from `/media/usbhd/training_data/asr/mix/gigaspeech_xl_wenetspeech_l_webdataset`. Build a new Stage177 audio-only online CTC alignment pool from the existing full length indexes, without copying audio and without using text labels or FunASR LLM output. The first pool should target `1,000,000` train rows with deterministic source-balanced reservoir sampling and a conservative `num_frames <= 2000` cap so online FunASR-Nano teacher throughput remains practical: AISHELL3 up to all available train rows (`~62k`), CommonVoice CN up to all available train rows (`~32k`), CommonVoice EN `180k`, LibriSpeech `180k`, GigaSpeech `270k`, and WenetSpeech the remaining quota. Annotate rows with Stage177 provenance fields, `source_dataset`, `language`, `ctc_online_only=true`, and `uses_text_labels=false`; then build bucket manifests with the existing Rust bucket indexer. Start Stage177a from `/media/usbhd/rwkvasr_runs/stage175h_stage175e16_stage175g16_interp_20260628/w25/stage175h_w25.pt`, keep the BiRWKV/RWKV-7 TimeMixer architecture, SenseVoice frontend/tokenizer, CTC head, blank mapping, and decode policy unchanged, keep supervised CTC/decoder losses at zero, and train only with online FunASR-Nano CTC logits. Use top-k CE as the main nonblank pressure and full-vocabulary KL as a stabilizer; do not continue the failed Stage176 sequence loss unless a later screen justifies it. Gate checkpoints only by direct Nano-CTC imitation against the fixed Stage162 heldout teacher export, and require enough steps to consume meaningfully more unique audio than Stage172 before concluding this path has plateaued.
					- 2026-06-28 Stage177a data build and smoke result: added `scripts/build_stage177_large_online_ctc_alignment.py` and built `/media/usbhd/training_data/asr/curriculum/stage22_stage21_soup_clean_repair_mix/stages/stage177_large_audio_only_online_ctc_alignment/stage177a_large1m_online_ctc`. The stage has exactly `1,000,000` rows and `1,000,000` unique `utt_id`s selected from `24,118,779` frame-filtered train candidates, with source counts AISHELL3 `62,952`, CommonVoice CN `32,601`, CommonVoice EN `180,000`, LibriSpeech `180,000`, GigaSpeech `270,000`, and WenetSpeech `274,447`; only `50` train rows were skipped for `num_frames > 2000`. Frame distribution is min/p50/p90/p99/max `26/388/1354/1614/2000`, and the bucket manifest has `1,000,000` train samples over `18` frame buckets. The config `configs/generated/sensevoice_rwkv_stage177a_stage175h_w25_large1m_audio_only_online_funasr_nano_ctc_topk2_fullkl0p5_4x4090_deepspeed.yaml` initializes from Stage175h `w25`, disables labels/anchors/sequence loss, and uses online Nano top-k CE weight `2.0` plus full-vocabulary KL weight `0.5`. A 4-GPU 2-step smoke passed with `online_teacher_missing=0`, `online_full_missing=0`, first-step peak reserved memory about `12.16GiB`, and online teacher forward about `0.65-0.72s`; no BiRWKV/RWKV-7 TimeMixer architecture, frontend/tokenizer, CTC head, blank mapping, or decode policy changes were made. Proceed with the full Stage177a run and screen saved checkpoints by direct Nano-CTC imitation.
					- 2026-06-28 Stage178 CV22 correction plan: the large Stage177a pool did not directly consume `/media/usbhd/common_voice_22/webdataset`; its CommonVoice rows came from the older clean curriculum index. The user pointed out that CV22 is a much larger and more appropriate disk-resident audio source for the active CTC-output alignment phase. Build Stage178 as a data-source correction, not an architecture change: inspect `/media/usbhd/common_voice_22/webdataset` in-place, keep shard names relative to that root (`en/shard_*.tar`, `zh-CN/shard_*.tar`, etc.), compute `num_frames` from MP3 audio because CV22 JSON metadata lacks duration, and record `audio_offset/audio_size` plus `json_offset/json_size` so training and the online FunASR-Nano teacher can read tar members without unpacking the 99G corpus. The first Stage178 pool should be audio-only, label-free, and source/language annotated, with supervised CTC/decoder losses at zero and online FunASR-Nano CTC logits as the only target. Start from Stage175h `w25` unless Stage177 evaluation-only interpolation produces a clearly better Nano-CTC imitation checkpoint. Gate again only by direct greedy Nano-CTC imitation on the fixed Stage162 teacher-heldout set; do not use CV22 text, FunASR LLM output, human labels, or public WER gates for this phase.
					- 2026-06-28 Stage178b mixed-data correction plan: direct CV22 inspection showed the converted `/media/usbhd/common_voice_22/webdataset` has `120,000` paired rows and only `112,603` frame-filtered selected rows under `num_frames <= 2000`, so CV22 alone is not enough for the requested large-data online CTC alignment. Use CV22 as the complete CommonVoice replacement source, then build a mixed-root Stage178b pool by writing absolute tar paths in `shard_name`/`tar_path` and setting the training `webdataset_root` to `/`; this avoids copying audio and allows CV22, clean AISHELL/Libri, and Giga/Wenet shards to coexist in one bucket manifest. Do not include the old CommonVoice curriculum rows in Stage178b. Target `1,000,000` unique audio-only rows: all available CV22 rows first, AISHELL3 up to all available rows, LibriSpeech around `180k`, GigaSpeech around `300k`, and WenetSpeech as the remaining quota. Keep labels disabled and use only online FunASR-Nano CTC top-k/full-KL distillation; no BiRWKV/RWKV-7 TimeMixer, frontend/tokenizer, CTC head, blank mapping, or decode policy changes are permitted.
					- 2026-06-28 Stage179 literature/data-scale correction plan: Stage178b is a useful plumbing check but not enough data for the stated goal of matching FunASR-Nano CTC. Its exact selected audio is only `1,000,000` utterances / `1424.26h`: AISHELL3 `62.86h`, CV22 en `95.84h`, CV22 zh `63.09h`, GigaSpeech `362.54h`, LibriSpeech `604.64h`, and WenetSpeech `235.29h`. Current mainstream ASR reports use far larger coverage: Fun-ASR reports models trained on `>10M` hours, SenseVoice reports `>400k` hours, Whisper reports `680k` hours, XLS-R reports `436k` hours, wav2vec 2.0 uses `53k` hours of Libri-Light pretraining, and SpeechStew shows a supervised multi-corpus recipe around `5k` hours. Therefore, do not interpret Stage178b failure or small heldout movement as a BiRWKV architecture limit. Build Stage179 from the full disk-resident audio pool, not another `1M`-row slice: all CV22 rows, all useful clean public rows except old CommonVoice when CV22 is serving as the CommonVoice source, plus much larger GigaSpeech/WenetSpeech and Emilia EN/ZH samples. The local summaries estimate Giga/Wenet at roughly `22k-28k` hours, Emilia EN/ZH at roughly `116k-126k` hours, and clean public at roughly `2.8k-3.2k` hours, so a `>=10k` hour online CTC distillation pool is feasible without copying audio. Also update the distillation recipe to be CTC blank-aware: CTC-KD papers warn that blank frames dominate frame-level KL/CE, so add optional nonblank/neighbor frame filtering for online top-k/full-logit teacher losses. Keep this as a training-signal/data-scale change only; do not alter BiRWKV/RWKV-7 TimeMixer, the bidirectional wrapper, SenseVoice frontend/tokenizer, CTC head shape, blank id mapping, or streaming/offline same-checkpoint semantics. Gate Stage179 only by direct Nano-CTC imitation against the fixed Stage162 teacher-heldout export, with Stage178b checkpoints serving as an intermediate data-plumbing comparison rather than the endpoint.
					- 2026-06-29 Stage179 USBHD dedupe/curriculum build result: added `scripts/build_stage179_usbhd_dedup_ctc_curriculum.py` and built `/media/usbhd/training_data/asr/curriculum/stage22_stage21_soup_clean_repair_mix/stages/stage179_usbhd_dedup_online_ctc_alignment` from the full disk pool without copying audio. The dedupe key is `blake2b16(root_or_tar_path + audio_member + audio_offset + audio_size)` and the selected base indexes produced `62,072,225` unique train audio rows / `118,465.161h`, with `0` physical duplicates across the selected base pools. Source hours are CV22 `158.931h`, clean public `2422.829h`, GigaSpeech/WenetSpeech `19809.868h`, and Emilia EN/ZH `96073.532h`. Difficulty splits and bucket manifests are complete: `stage179a_easy_dedup_audio_only_online_ctc` has `1,174,987` rows / `1490.119h`, `stage179b_medium_dedup_audio_only_online_ctc` has `42,247,508` rows / `47771.242h`, `stage179c_hard_dedup_audio_only_online_ctc` has `18,649,326` rows / `69200.368h`, and `stage179d_long_dedup_audio_only_online_ctc` has `404` rows / `3.432h`. Stage179a starts from `/media/usbhd/rwkvasr_runs/stage175h_stage175e16_stage175g16_interp_20260628/w25/stage175h_w25.pt` and trains only from online FunASR-Nano CTC logits with labels/decoder/anchors disabled, nonblank-neighbor frame filtering, top-k CE weight `2.0`, blank BCE weight `0.2`, full-vocabulary KL weight `0.5`, no sequence loss, no SpecAugment, no direction dropout, and no gradient checkpointing. This is still only a data/signal/curriculum change; the BiRWKV/RWKV-7 TimeMixer architecture, frontend/tokenizer, CTC head, blank mapping, and same-checkpoint streaming/offline design are unchanged. Promote to the medium split only after direct Nano-CTC imitation evaluation shows the easy split has improved or at least not regressed the current Stage175h `w25` CTC candidate.
					- 2026-06-29 Stage179a easy result and Stage179b plan: Stage179a trained for `4096` steps on the `1490.119h` easy split with online FunASR-Nano CTC logits only; all logged online teacher/top-k, blank, and full-KL paths had `missing=0`. Direct greedy Nano-CTC imitation on the fixed Stage162 heldout found a small but valid easy-stage improvement at `step-768`: Stage175h `w25` baseline is `38.439093 / 24.691219 / 39.095511` Nano WER/CER/token ER, while Stage179a `step-768` is `38.432950 / 24.680779 / 39.095511`, improving WER and CER while tying token ER. Later checkpoints trade metrics and should not be used as the next initialization. Proceed to Stage179b medium from `/media/usbhd/rwkvasr_runs/sensevoice_rwkv_stage179a_stage175h_w25_usbhd_easy1490h_audio_only_online_funasr_nano_ctc_nonblanknbr_topk2_blank0p2_fullkl0p5_bs12_lr5e10_zero1_nockpt_noaug_nodirdrop_4x4090/step-768.pt` on `stage179b_medium_dedup_audio_only_online_ctc` (`42,247,508` rows / `47771.242h`). Keep the same architecture and teacher objective, keep labels/decoder/anchors disabled, retain nonblank-neighbor filtering plus blank BCE/full KL, and run enough medium steps before judging the stage; use the fixed Stage162 Nano-CTC heldout as the promotion gate.
					- 2026-06-29 Stage179b online-teacher IO fix plan: the first medium smoke exposed a data-pipeline issue, not a model/architecture issue: every rank tried to load the full `42,247,508`-row medium length index into the FunASR-Nano online teacher before step 1. For Stage179b and harder splits, the WebDataset bucket loader must carry each decoded sample's audio locator (`tar_path`/`shard_name`, `audio_member`, byte offset/size, source/language fields, and `utt_id`) into `ASRBatch`, and the online teacher must prefer these per-batch rows over any global audio index. Keep the old audio-index fallback for smaller manifest-based runs, but allow bucketed Stage179 configs to set `ctc_teacher_online_audio_index_path: null` so medium/hard training scales without per-rank full-index loads. This is an IO plumbing change only; it must not alter the BiRWKV/RWKV-7 TimeMixer architecture, SenseVoice frontend/tokenizer, CTC head, blank mapping, online CTC logits objective, or decode policy.
					- 2026-06-29 Stage179b online-teacher IO fix result: implemented per-batch audio-row propagation through `WebDatasetLengthEntry.raw`, `BucketedWebDatasetBatchLoader`, `ASRBatch`, `CTCBatch`, and the token-budget rebatching helpers, and updated the FunASR-Nano online teacher to accept batch-inline rows while retaining the old audio-index fallback. Stage179b config now sets `ctc_teacher_online_audio_index_path: null`. A direct medium bucket sample verified `tar_path`, `audio_member`, `audio_offset/audio_size`, `source`, and `language` are present and usable. A 4-GPU 2-step smoke under `/media/usbhd/rwkvasr_runs/smoke_stage179b_medium_online_ctc_batch_inline_v2` initialized with `audio_index=batch_inline`, avoided loading the `36.4GiB` medium length index, and completed with `online_teacher_match=12/12`, `online_teacher_missing=0`, `online_blank_missing=0`, and `online_full_missing=0` on both logged steps. Proceed to the full Stage179b medium run from Stage179a `step-768` with the same Nano-only CTC logits objective and unchanged BiRWKV/RWKV-7 TimeMixer architecture.
					- 2026-06-29 Stage179b medium result and optimization correction: the full Stage179b run completed `8192` steps on the `47,771.242h` medium split from Stage179a `step-768`; online FunASR-Nano CTC top-k, blank, and full-KL targets had `missing=0`, the final checkpoint `step-8192.pt` was written, and 4-rank DeepSpeed exited normally. Direct Nano-CTC heldout imitation on the fixed Stage162 teacher set did not improve: Stage175h `w25` baseline was `38.4391 / 24.6912 / 39.0955` Nano WER/CER/token ER, Stage179a `step-768` was `38.4330 / 24.6808 / 39.0955`, and Stage179b `step-6656/7168/7680/8192` were `38.4422 / 24.7065 / 39.1012`, `38.4483 / 24.6752 / 39.1097`, `38.4729 / 24.7185 / 39.1295`, and `38.4330 / 24.6808 / 39.1040`. The root cause is not data scale or teacher IO: the run used actual `lr=5.0e-10`, which is effectively frozen for an unanchored Nano-CTC logits alignment objective over 70 encoder layers plus the CTC head. Do not advance to hard/long on that LR. Stage179b2 should rerun the medium split as an optimization calibration from Stage179a `step-768`, still using only audio and online FunASR-Nano CTC logits, with labels/decoder/anchors disabled, nonblank-neighbor filtering, top-k CE `2.0`, blank BCE `0.2`, full KL `0.5`, no sequence loss, no SpecAugment, no direction dropout, `lr=1.0e-6`, `warmup_steps=0` because the current config key is reserved by direction-dropout validation rather than optimizer warmup, `max_steps=2048`, and `save_every=512`. Gate `step-512/1024/1536/2048` by the same Stage162 Nano-CTC heldout. Only if Stage179b2 shows real heldout movement should the recipe be extended on medium and then promoted to hard/long; otherwise adjust optimization or objective before consuming the harder `69,200h` split. This is an optimizer/config correction only and must not alter the BiRWKV/RWKV-7 TimeMixer architecture, SenseVoice frontend/tokenizer, CTC head shape, blank mapping, or same-checkpoint streaming/offline design.
					- 2026-06-29 Stage179b2 lr sweep result and Stage179b3 plan: Stage179b2 (`lr=1.0e-6`, blank BCE `0.2`, full KL `0.5`) completed `2048` medium steps from Stage179a `step-768` with no teacher misses and a large train-objective drop (`step=2048` online top-k/full losses around `2.15/2.02`), but direct heldout greedy imitation regressed badly. Stage179b2 `step-512/1024/1536/2048` measured `38.9121 / 25.7424 / 39.6159`, `43.6636 / 32.8954 / 44.8879`, `59.1283 / 54.6249 / 60.1295`, and `68.8894 / 68.0225 / 69.6524` Nano WER/CER/token ER, with len ratio collapsing from the baseline `0.914` to `0.893`, `0.791`, `0.546`, and `0.425`. This shows the frame-level objective can be optimized but the LR/blank pressure is too strong and makes greedy CTC outputs shorter/more blank. Do not promote any Stage179b2 checkpoint and do not enter hard/long yet. Stage179b3 should keep the same medium data and Stage179a `step-768` init but reduce optimization and remove explicit blank pressure: `lr=1.0e-7`, top-k CE `2.0`, blank BCE `0.0`, full KL `0.1`, nonblank-neighbor frame filtering, no sequence loss, labels/decoder/anchors disabled, no SpecAugment, no direction dropout, `max_steps=1024`, and `save_every=256`. Gate `step-256/512/768/1024` by the same Stage162 Nano-CTC heldout; require WER/CER/token ER and len ratio to improve or at least not regress before extending medium or moving to hard/long. This remains an optimizer/objective calibration only; BiRWKV/RWKV-7 TimeMixer, frontend/tokenizer, CTC head shape, blank mapping, and same-checkpoint streaming/offline semantics stay unchanged.
					- 2026-06-29 Stage179b3 result and Stage179b4 plan: Stage179b3 completed `1024` medium steps from Stage179a `step-768` with `lr=1.0e-7`, no explicit blank BCE, nonblank-neighbor top-k CE `2.0`, and full KL `0.1`. Online FunASR-Nano teacher matching stayed healthy through the final step (`online_teacher_match=12/12`, `online_teacher_missing=0`) and checkpoints `step-256/512/768/1024` were written. Direct Nano-CTC heldout imitation showed that the objective is stable but too weak to materially improve greedy CTC: Stage179b3 `step-256/512/768/1024` measured `38.4237 / 24.6944 / 39.0927`, `38.4637 / 24.7258 / 39.1351`, `38.4391 / 24.7129 / 39.1153`, and `38.4207 / 24.7217 / 39.1238` Nano WER/CER/token ER, with len ratio staying near baseline (`0.9145` to `0.9136`) and no collapse. Do not promote Stage179b3 to hard/long: the large `47,771h` medium data path is working, but frame-level logits alone either under-move at `1e-7` or collapse with the stronger Stage179b2 blank-heavy setup. Stage179b4 should run a short medium calibration from the same Stage179a `step-768` init, still using audio-only online FunASR-Nano CTC targets, with labels/decoder/anchors disabled, `lr=3.0e-7`, top-k CE `2.0`, blank BCE `0.0`, full KL `0.05`, nonblank-neighbor frame filtering, and a small online teacher greedy sequence CTC weight `0.05`. The sequence term uses the existing Nano CTC argmax tokens after removing blank and ignored project-only token ids, and is intended only to keep the student greedy CTC path aligned while logits distillation supplies frame-level distribution pressure. Gate `step-128/256/384/512` by the same Stage162 Nano-CTC heldout before any longer medium run or hard/long promotion. This remains a training-objective calibration only; do not alter the BiRWKV/RWKV-7 TimeMixer architecture, bidirectional wrapper, frontend/tokenizer, CTC head shape, blank mapping, or streaming/offline same-checkpoint semantics.
					- 2026-06-29 Stage179b4 result and Stage179b5 plan: Stage179b4 completed `512` medium steps with `lr=3.0e-7`, no explicit blank BCE, nonblank-neighbor top-k CE `2.0`, full KL `0.05`, and online teacher greedy sequence CTC `0.05`. Online teacher and sequence matching were healthy through `step=512` (`online_teacher_match=12/12`, `online_sequence_match=12/12`, no missing records), and checkpoints `step-128/256/384/512` were written. Direct Nano-CTC heldout imitation did not improve: Stage179b4 `step-128/256/384/512` measured `38.4360 / 24.7073 / 39.1012`, `38.4545 / 24.7354 / 39.1266`, `38.4575 / 24.7322 / 39.1153`, and `38.4422 / 24.7948 / 39.1238`, with len ratio drifting from `0.9146` to `0.9116`. Do not extend Stage179b4 and do not enter hard/long. The next calibration should isolate the CTC head: keep the Stage179a `step-768` initialization and the same `47,771h` medium audio-only pool, freeze the full SenseVoice/BiRWKV encoder (`freeze_encoder=true`), train only the existing CTC head, use nonblank-neighbor online Nano top-k CE `2.0` plus nonblank-neighbor full KL `0.5`, no blank BCE, no sequence loss, labels/decoder/anchors disabled, no SpecAugment, no direction dropout, `lr=5.0e-7`, `max_steps=512`, and `save_every=128`. Gate `step-128/256/384/512` by the same Stage162 Nano-CTC heldout. If head-only medium does not improve Nano WER/CER/token ER or exact/token match, treat the current frame/greedy-target formulation as the bottleneck before consuming the hard `69,200h` split. This remains a training/optimization isolation change only; BiRWKV/RWKV-7 TimeMixer, bidirectional wrapper, frontend/tokenizer, CTC head shape, blank id mapping, and streaming/offline same-checkpoint behavior stay unchanged.
					- 2026-06-29 Stage179b5 result and Stage179b6 plan: Stage179b5 completed `512` medium steps with the encoder frozen and only `31,044,708` CTC-head parameters trainable. Online Nano top-k/full-KL matching stayed healthy (`online_teacher_match=12/12`, no missing records), checkpoints `step-128/256/384/512` were written, and train loss remained finite. Direct Nano-CTC heldout imitation was only a tiny CER-only movement and not a promotion: Stage179b5 `step-128/256/384/512` measured `38.4360 / 24.6824 / 39.0983`, `38.4422 / 24.6792 / 39.0983`, `38.4422 / 24.6792 / 39.0983`, and `38.4452 / 24.6800 / 39.0955`; Stage179a `step-768` remains better on WER and ties token ER (`38.4330 / 24.6808 / 39.0955`). Do not extend Stage179b5 and do not enter hard/long. Since frame-level logits have now shown either under-movement, regression, or collapse across full-model and head-only probes, run a short Stage179b6 diagnostic that isolates the teacher CTC sequence signal: same Stage179a `step-768` init, same medium audio-only pool, `freeze_encoder=true`, train only the CTC head, online Nano teacher greedy CTC sequence loss `1.0`, online top-k/full/blank losses `0.0`, labels/decoder/anchors disabled, no SpecAugment, no direction dropout, `lr=5.0e-7`, `max_steps=256`, and `save_every=64`. This still uses only audio plus online FunASR-Nano CTC outputs, but tests whether pseudo-label CTC alignment is a better bridge than frame-level logits for the current student. Gate `step-64/128/192/256` by the same Stage162 Nano-CTC heldout before any longer run.
					- 2026-06-29 Stage179b6 result: Stage179b6 completed `256` medium steps with the encoder frozen, only the CTC head trainable, and online Nano greedy CTC sequence loss `1.0` as the only training objective. Online sequence matching stayed healthy through the final step (`online_sequence_match=12/12`, no missing records), checkpoints `step-64/128/192/256` were written, and final rank0 sequence loss was `1.3957`. Direct Nano-CTC heldout imitation was still only noise-scale movement: Stage179b6 `step-64/128/192/256` measured `38.4330 / 24.6808 / 39.0955`, `38.4299 / 24.6824 / 39.0955`, `38.4360 / 24.6832 / 39.0983`, and `38.4391 / 24.6848 / 39.1012`; the small WER gain at `step-128` is paired with CER regression and no token-ER gain. Do not promote Stage179b6, do not extend it, and do not enter hard/long. The full deduped `/media/usbhd` pool is large enough (`118,465h` total; `47,771h` medium), but Stage179b2-b6 show that the current top-k/full/frame-filtered logits and greedy-sequence CTC formulations are not yet an effective bridge from the current BiRWKV student to Nano-CTC. The next change should be objective-level and diagnostic before more compute: directly evaluate heldout frame/logit KL and blank/nonblank mass on a small fixed subset, then consider time-alignment alternatives or teacher-token projection checks. Architecture, frontend/tokenizer, CTC head shape, blank id mapping, and streaming/offline behavior remain unchanged.
					- 2026-06-29 Stage180 architecture-alignment plan: Stage179b2-b6 indicate that direct `BiRWKV encoder -> linear CTC head` imitation of FunASR-Nano final CTC logits is an ineffective bridge even with the `47,771h` medium split. FunASR-Nano's actual CTC branch is `SenseVoiceEncoderSmall -> adaptor.Transformer ctc_decoder -> CTC linear`, where `ctc_decoder_conf` is `downsample_rate=1`, `encoder_dim=512`, `llm_dim=512`, `ffn_dim=2048`, `n_layer=5`. The installed FunASR `adaptor.Transformer` first applies `Linear(512,2048) + ReLU + Linear(2048,512)`, then five standard Transformer `EncoderLayer`s; each layer uses multi-head self-attention with the default `8` heads, dropout `0.0`, and a small `PositionwiseFeedForward(512,128)`. Stage180 should keep the project encoder as BiRWKV/RWKV-7 TimeMixer and add an optional Nano-style CTC decoder before the CTC projection. The first implementation should support loading Nano `ctc_decoder.*` weights directly and loading Nano `ctc.ctc_lo.*` into the project CTC head with explicit blank remapping (`teacher blank 60514 -> project blank 60515`, project-only `60514` kept ignored or initialized safely). This is a CTC output-path alignment change, not a replacement of the BiRWKV encoder; any later attempt to replace the 5-layer Transformer CTC decoder with RWKV must be a separate Spec update after the Nano-style decoder has been verified.
					- 2026-06-29 Stage180 streaming caveat: the Nano-style 5-layer CTC decoder is a full-context Transformer adaptor and therefore is not a streaming-safe production replacement for the current linear CTC head. Use it first as an alignment diagnostic and non-streaming CTC bridge. If it materially improves Nano-CTC imitation, the follow-up must either distill its behavior back into the BiRWKV encoder / linear head, replace the CTC decoder with an RWKV or causal/chunked module, or keep separate streaming and offline CTC heads with explicit evaluation. Do not treat the full-context Transformer CTC decoder as satisfying the single-checkpoint streaming target until a streaming-compatible variant is verified.
					- 2026-06-29 Stage180 hard-to-easy online distillation plan: follow the requested reverse curriculum using the existing deduped `/media/usbhd` pool in order `stage179c_hard` (`69,200h`) -> `stage179b_medium` (`47,771h`) -> `stage179a_easy` (`1,490h`). Stage180a starts from Stage179a `step-768`, enables the Nano-style CTC decoder path, loads Nano `ctc_decoder.*` and `ctc.ctc_lo.*` from `/media/usbhd/models/Fun-ASR-Nano-2512-modelscope/model.pt`, freezes the Nano CTC decoder and CTC head, and trains only the BiRWKV/SenseVoiceRWKV encoder against online FunASR-Nano CTC logits. Use audio-only online teacher targets with labels/decoder/anchors disabled, project blank `60515`, ignored project-only id `60514`, nonblank-neighbor frame filtering, top-k CE plus a small full-vocabulary KL, and no explicit blank BCE until a hard-split smoke proves the new decoder path does not collapse length ratio. Because the fixed Nano CTC decoder adds full-context self-attention memory on top of the 1B BiRWKV encoder and online teacher, begin with a short 4-GPU hard-split smoke at smaller local batch / frame budget, then only launch the full hard stage if `online_teacher_match`, `online_full_missing`, loss finiteness, and GPU memory are healthy. Later medium/easy stages should initialize from the best hard checkpoint rather than resuming DeepSpeed state, and each transition must be gated by direct Nano-CTC imitation on the fixed Stage162 heldout.
					- 2026-06-29 Stage180a hard launch status: launched the hard-first online Nano-CTC logits distillation run with config `configs/generated/sensevoice_rwkv_stage180a_stage179a768_usbhd_hard69200h_encoderonly_online_funasr_nano_ctc_decoderinit_topk1_fullkl0p05_lr5e8_4x4090_deepspeed.yaml` and output `/media/usbhd/rwkvasr_runs/sensevoice_rwkv_stage180a_stage179a768_usbhd_hard69200h_encoderonly_online_funasr_nano_ctc_decoderinit_topk1_fullkl0p05_bs4_fb1200_lr5e8_zero1_nockpt_noaug_nodirdrop_4x4090`. The full run uses local batch `4`, gradient accumulation `8`, frame budget `1200`, Nano CTC decoder/head frozen, and only the BiRWKV/SenseVoiceRWKV encoder trainable. Startup passed through `step=110` with `online_teacher_missing=0`, `online_full_missing=0`, finite losses, and approximately `10-11GiB` GPU memory per rank; save checkpoints every `256` steps and gate hard-stage promotion by fixed Stage162 Nano-CTC imitation before moving to medium/easy.
					- 2026-06-29 Stage180a hard result: the run completed `2048` steps normally with checkpoints `step-256` through `step-2048`, `online_teacher_missing=0`, `online_full_missing=0`, and final rank0 loss `6.8124`, but fixed Stage162 Nano-CTC heldout imitation failed catastrophically. Greedy CTC summaries are `/media/usbhd/rwkvasr_runs/stage180a_teacher_heldout_eval_20260629/stage180a_greedy_vs_nano_ctc_summary.fast.md` and `.json`; all eight checkpoints have Nano WER/CER/token ER around `352.8-353.2 / 438.3-438.8 / 475.9-476.2`, token length ratio about `5.05`, zero text/token exact matches, student average blank probability about `0.000008`, and Nano teacher average blank probability `0.792891` (`blank_top1` about `0.00002` vs teacher `0.80158`). Beam8 decoding is also invalid for promotion, with WER/CER/token ER around `352 / 434 / 475` and the same over-generation. Do not promote any Stage180a checkpoint and do not continue to medium/easy under this recipe. The training process and data path are healthy; the failure is objective/calibration: the frozen Nano CTC decoder/head receives off-manifold BiRWKV encoder states, while the active objective uses nonblank-neighbor top-k plus small full KL and no explicit blank loss, so it never restores Nano's high blank prior. Next work must first run a short diagnostic/repair that measures initial `step-0` blank/logit distribution and adds full-frame blank/mass alignment or an encoder-feature/adaptor alignment term before consuming more hard data; the BiRWKV/RWKV-7 TimeMixer encoder architecture remains unchanged.
					- 2026-06-30 Stage180 repair implementation plan: keep the BiRWKV/RWKV-7 encoder and Nano-style CTC decoder/head architecture unchanged, but fix the distillation plumbing before any new hard run. First, cached full-logit teacher tensors must remap FunASR-Nano blank `60514` to project blank `60515` and mask project-only ignored ids exactly like the online teacher already does; a raw pad-only vocabulary expansion is wrong. Second, full-vocabulary KL must have an independent frame-filter knob so it can run on all frames while top-k CE remains limited to nonblank-neighbor frames; otherwise the blank-dominant CTC prior is removed from the only dense distribution loss. The Stage180 repair config should use full-frame blank/full supervision (`ctc_teacher_online_blank_loss_weight > 0`, `ctc_teacher_online_full_frame_filter=all`, and/or mass loss) and gate after a short run by student blank probability/top1 ratio before full heldout WER/CER. This is a loss/config repair only, not an encoder architecture change.
					- 2026-06-30 Stage180b repair result and Stage180c plan: implemented cached full-logit blank remapping and independent online full-KL frame filtering. Stage180b used the hard split, Stage179a `step-768` init, frozen Nano CTC decoder/head, online top-k CE `1.0`, blank BCE `1.0`, full KL `1.0`, and `ctc_teacher_online_full_frame_filter=all`; it ran `128` steps successfully once the launch PATH included `.venv/bin` for the fused CUDA `ninja` build. The loss plumbing is now active (`online_blank_match=1/1`, `online_full_match=1/1`, no teacher misses), but the `lr=5.0e-8` diagnostic did not move the heldout CTC blank prior: on a 200-sample Stage162 Nano-CTC check, student average blank probability stayed `6.6e-6`, blank-top1 stayed `0.0`, token length ratio stayed `5.09`, and Nano WER/CER remained invalid. Do not extend this LR. Stage180c should rerun the same frozen Nano-decoder/head architecture from Stage179a `step-768` with the same full-frame blank/full supervision but raise `lr` to `1.0e-6`, run only a short `128`-step hard diagnostic with checkpoints every `32` steps, and gate by student blank probability/top1 before considering any longer hard run. This is an optimizer calibration only; BiRWKV/RWKV-7 TimeMixer, SenseVoice frontend, Nano-style CTC decoder/head shapes, and blank mapping stay unchanged.
					- 2026-06-30 Stage180d hidden-alignment diagnostic plan: Stage180c with `lr=1.0e-6` still left the 200-sample heldout blank probability at about `6.65e-6`, blank-top1 `0.0`, and token length ratio about `5.09`; parameter diffs confirm the encoder updates are real but extremely small while the frozen Nano CTC decoder/head remain unchanged. This indicates the frozen Nano CTC branch is seeing off-manifold BiRWKV encoder states and logits-only blank/full losses are not pulling the encoder into Nano's hidden space fast enough. Add an optional online teacher encoder-hidden MSE loss: FunASR-Nano teacher records may return `encoder_out`, the training loop aligns it to student `encoded` with the existing nearest/linear time map, and a new `ctc_teacher_online_encoder_loss_weight` gates the term. This is a diagnostic objective to align `BiRWKV encoder -> frozen Nano CTC decoder/head`; it must not change the BiRWKV/RWKV-7 architecture, SenseVoice frontend, CTC decoder/head structure, tokenizer, or blank mapping.
					- 2026-06-30 Stage180d result and Stage180e plan: Stage180d implemented online Nano encoder-hidden MSE and completed a 64-step hard diagnostic with `ctc_teacher_online_encoder_loss_weight=1.0`; `online_encoder_match=1/1` and no misses confirmed the new path works, but hidden MSE stayed around `9.0` and the 200-sample heldout output remained unchanged (`avg_blank ~= 6.62e-6`, blank-top1 `0.0`, len ratio `5.09`). Frozen Nano CTC decoder/head is therefore too rigid for a short encoder-only bridge from the current BiRWKV hidden distribution. Stage180e should keep the Nano-style 5-layer CTC decoder architecture and Nano initialization, keep the CTC head frozen to preserve vocabulary/blank mapping, but unfreeze the CTC decoder so it can adapt to BiRWKV encoder states under the same online Nano CTC output losses. This changes only the trainability of the CTC adaptor, not the BiRWKV/RWKV-7 encoder architecture; gate by heldout blank probability before any longer run.
					- 2026-06-30 Stage180e result and Stage180f repair plan: Stage180e completed the 128-step hard diagnostic with Nano decoder trainable and Nano CTC head frozen. Online teacher matching stayed healthy and the Nano decoder did update (`ctc_decoder` relative delta about `1.88e-3` versus the loaded Nano init), but the fixed 200-sample Stage162 Nano-CTC heldout remained collapsed: Nano WER/CER/token ER `302.99 / 659.29 / 415.33`, length ratio `5.08`, student average blank probability `6.64e-6`, and blank-top1 `0.0`. This rules out a missing-teacher or frozen-decoder bug, but shows the frozen Nano head still forces an off-manifold hidden-state bridge that does not repair the CTC blank prior in a short hard run. Stage180f should keep the BiRWKV/RWKV-7 encoder and Nano-style 5-layer CTC decoder architecture unchanged, but temporarily unfreeze the CTC head as a calibration probe, load the Nano decoder/head initialization, train encoder + CTC decoder + CTC head with online Nano top-k CE, blank BCE, and full-frame full-vocabulary KL, and gate first on heldout blank probability / length ratio before WER. If Stage180f quickly restores blank mass, later work can distill the calibrated output path back into a streaming-safe head; if it also fails, the next blocker is time/feature alignment rather than head trainability.
					- 2026-06-30 Stage180 prediction-path bugfix and Stage180g plan: the Stage180a-f heldout results above were invalid because `predict_ctc` / `predict_ctc_labeled` bypassed the optional Nano-style CTC decoder and called `model.ctc_head(encoded)` directly. The fixed path now calls `model.ctc_logits_from_encoded(encoded, encoded_lengths)` and targeted tests cover both unlabeled and labeled prediction. Re-evaluation with the decoder path changes the diagnosis completely: Stage180a `step-2048` and Stage180e `step-128` are all-blank (`avg_blank 0.989` and `0.981`, blank-top1 `1.0`, length ratio `0.0`), while Stage180f moves blank down only partially (`step-128 avg_blank 0.826`, blank-top1 `0.989`, length ratio `0.061`; `step-256 avg_blank 0.850`, blank-top1 `0.993`, length ratio `0.029`). The CTC decoder branch is therefore active but over-blanked, not under-blanked. Stage180g should keep the BiRWKV/RWKV-7 encoder and Nano 5-layer CTC decoder/head architecture unchanged, load the same Nano initialization, leave decoder and head trainable, remove explicit blank BCE and dense full-frame KL for the short repair, and use stronger nonblank-neighbor top-k CE plus online Nano greedy sequence CTC to force emissions. Gate first on nonzero heldout length ratio and blank-top1 moving below `0.95`; only then resume full-frame distribution matching.
					- 2026-06-30 Stage180g result: Stage180g completed `256` hard steps with decoder/head trainable, online Nano top-k CE `5.0`, sequence CTC `2.0`, and no explicit blank/full loss. Teacher and sequence matching were healthy and train losses dropped (`online_ctc_sequence` commonly moved from the mid/high teens toward about `9-12`), but fixed Stage162 decoder-path heldout still did not recover usable emissions. `step-64` remained all blank (`avg_blank 0.974`, blank-top1 `1.0`, length ratio `0.0`); `step-128` was the best short checkpoint but still mostly blank (`avg_blank 0.798`, blank-top1 `0.983`, length ratio `0.083`, Nano WER/CER/token ER `100.00 / 97.73 / 100.00`); `step-256` regressed toward blank (`avg_blank 0.860`, blank-top1 `0.993`, length ratio `0.024`). Do not promote Stage180g or continue it as-is. The prediction-path fix is valid and required, but the CTC alignment recipe now needs a stronger nonblank-emission bridge, such as head/decoder-only warmup on a small easy subset with higher sequence weight and frequent early gating, or an explicit frame prior / bias calibration that lowers blank before reintroducing full KL. The BiRWKV encoder architecture and Nano decoder structure remain unchanged.
					- 2026-06-30 Stage180h plan: add a CTC-head initialization calibration knob for the Nano blank row, `funasr_nano_ctc_init_blank_bias_delta`, applied immediately after loading Nano `ctc.ctc_lo.*` and remapping teacher blank `60514 -> 60515`. This is not an architecture change; it is equivalent to starting from a different trainable CTC-head bias so the over-blanked Nano decoder path can produce nonblank gradients. Stage180h should run an easy-split output-branch warmup from Stage179a `step-768`: freeze the BiRWKV/SenseVoiceRWKV encoder, load Nano decoder/head, set blank bias delta around `-3.0`, train only Nano decoder + CTC head with strong online Nano sequence CTC and nonblank-neighbor top-k CE, keep blank/full losses off, and gate early by decoder-path heldout length ratio and blank-top1. If this pulls blank-top1 below `0.95` with nonzero token emissions, follow with a second short stage that reintroduces modest full-frame KL for distribution matching before returning to hard/medium data.
					- 2026-06-30 Stage180h result and Stage180i plan: Stage180h completed `512` easy-split steps with encoder frozen, Nano CTC decoder/head trainable, blank bias delta `-3.0`, online top-k CE `8.0`, sequence CTC `8.0`, and no blank/full/mass loss. Decoder-path heldout evaluation with the fixed `ctc_logits_from_encoded` path shows this did not repair emissions: Stage180h `step-64/128/256/512` measured Nano WER/CER/token ER `100/98.84/100`, `100/99.52/100`, `100/100/100`, and `100/100/100`; average blank probabilities were `0.870/0.890/0.843/0.850`, blank-top1 was `0.993/0.998/1.000/1.000`, and length ratios fell from `0.030` to `0.0`. The failure is not a time-length mismatch: matching by `utt_id` on the 200-sample heldout gives identical Nano/student CTC frame counts (`20.655` average), while Nano teacher blank-top1 is only `0.771` and the student remains `0.993+`. Do not continue the frozen-encoder blank-bias warmup. Stage180i should continue from the best decoder-path emission checkpoint, Stage180g `step-128`, without reloading Nano decoder/head over it; unfreeze encoder, Nano CTC decoder, and CTC head; switch top-k frame filtering to `nonblank` only; reintroduce moderate full-frame blank/mass/full-KL calibration (`blank`/`mass`/`full` weights below the nonblank and sequence losses) so blank probability is matched rather than merely suppressed; and capture stdout/stderr logs for per-step match diagnostics. This is a distillation-objective/configuration change only. The BiRWKV/RWKV-7 encoder architecture, SenseVoice frontend, Nano 5-layer CTC decoder structure, tokenizer, blank remapping (`60514 -> 60515`), and streaming caveat remain unchanged.
					- 2026-06-30 Stage180i result and Stage180j plan: Stage180i ran `256` hard-split steps from Stage180g `step-128` without reloading Nano CTC weights, with encoder/decoder/head trainable, `nonblank` top-k CE `8.0`, sequence CTC `6.0`, blank/mass/full weights `0.5/0.5/0.25`, top-k `32`, and logs captured at `train_launch.log`. The plumbing is healthy: online top-k, blank, mass, full, and sequence all matched `1/1` on rank0 with no misses. Heldout decoder-path metrics improved only marginally and remain unusable: best Stage180i is `step-192` with Nano WER/CER/token ER `100.00 / 97.25 / 100.00`, length ratio `0.0834`, average blank `0.786`, and blank-top1 `0.981`; Stage180g step128 was `100.00 / 97.73 / 100.00`, length ratio `0.0834`, average blank `0.798`, blank-top1 `0.983`. Output inspection shows sparse high-frequency English tokens, including on Chinese utterances, rather than correct teacher tokens. The main blocker is now insufficient teacher-greedy sequence dominance and too few optimizer updates: the hard bucket produced local batch `1`, and grad accumulation `8` means `256` logged steps provide only about `32` optimizer updates. Stage180j should continue from Stage180i `step-192`, reduce DeepSpeed gradient accumulation to `1`, keep full encoder/decoder/head trainable, increase online Nano sequence CTC weight substantially, keep nonblank top-k CE as auxiliary, and reduce blank/mass/full calibration to small stabilizers. Gate by heldout `pred/ref` and token correctness before any longer large-data run. This remains online audio-only CTC distillation and does not change the BiRWKV/RWKV-7 architecture.
					- 2026-06-30 Stage180j result and Stage180k plan: Stage180j continued from Stage180i `step-192` with gradient accumulation `1`, online Nano sequence CTC weight `30.0`, nonblank top-k CE `4.0`, and small blank/mass/full stabilizers. Training losses were finite and teacher matching stayed healthy, but the run failed while saving a later DeepSpeed sharded checkpoint because `/media/usbhd` filled; exported top-level checkpoints through `step-320.pt` were preserved and the failed run's `ds_checkpoints` directory was removed to recover disk space. Heldout decoder-path imitation shows the method regressed: `step-64` predicted only `3/200` nonempty samples (`avg_blank 0.935`, blank-top1 `0.9987`, pred/ref `0.0034`), while `step-128/256/320` were all blank (`blank_top1 1.0`, pred/ref `0.0`, Nano WER/CER/token ER `100/100/100`). This proves that simply raising sequence CTC weight gives the optimizer a blank-collapse path instead of forcing the correct Nano emissions. Stage180k should add a direct online nonblank-emission objective derived from the teacher frame top-1: on frames where FunASR-Nano top-1 is nonblank and above the configured confidence threshold, apply a hard CE to the mapped teacher token and optionally a margin term requiring that token's logit to beat the project blank logit. Keep sequence CTC only as a lower-weight ordering auxiliary, keep full-frame blank/mass/full calibration small, continue from the best pre-collapse checkpoint Stage180i `step-192`, and gate after `64/128/192` optimizer steps by heldout nonempty count, pred/ref, blank-top1, and token correctness. To keep short experiments from failing on disk, add an explicit config switch that can skip DeepSpeed sharded checkpoint saves while still exporting rank-zero top-level `.pt` checkpoints for evaluation when ZeRO stage is below 3. These are training-objective and checkpointing changes only; the BiRWKV/RWKV-7 encoder architecture, SenseVoice frontend, Nano 5-layer CTC decoder structure, tokenizer, blank remapping (`60514 -> 60515`), and online audio-only teacher setup remain unchanged.
					- 2026-06-30 Stage180k result and Stage180l plan: implemented online nonblank hard CE plus blank-margin loss and the `save_deepspeed_sharded_checkpoints=false` export-only checkpoint mode. Targeted tests and py_compile passed. Stage180k trained `192` hard-split optimizer steps from Stage180i `step-192` with online top-k CE `2.0`, sequence CTC `4.0`, nonblank hard/margin `20.0/10.0`, small blank/mass/full `0.05/0.05/0.02`, LR `1.0e-6`, and no DS sharded checkpoint files; `step-64/128/192.pt` and `best.pt` exported normally while `ds_checkpoints` stayed empty. The new bridge is effective for emissions but not yet for token identity: versus Stage180i `step-192` (`len ratio 0.0834`, pred/ref `0.0692`, blank-top1 `0.981`, Nano CER `97.25`), Stage180k `step-192` reached `len ratio 0.1826`, pred/ref `0.1635`, blank-top1 `0.956`, and Nano CER `95.14`. Earlier Stage180k `step-64` over-emitted (`len ratio 0.593`, Nano WER/CER `113.57/111.77`), and all Stage180k checkpoints still have zero exact token matches and wrong frequent-token outputs, including English tokens on Chinese samples. Therefore do not increase margin/hard further. Stage180l should continue from Stage180k `step-192` with a lower LR (`5.0e-7`), reduce hard/margin weights, and shift objective mass toward token-distribution and sequence identity: top-k CE around `8.0`, sequence CTC around `8.0`, nonblank hard/margin around `8.0/2.0`, modest blank/mass/full calibration, top-k at least `16`, and the same export-only checkpointing. Gate by whether Nano CER/token ER improves while pred/ref remains nonzero and blank-top1 does not return to `1.0`. This remains an objective/curriculum adjustment only; do not change BiRWKV/RWKV-7 architecture, frontend, Nano CTC decoder structure, tokenizer, blank mapping, or online audio-only teacher path.
					- 2026-06-30 Stage180l result and Stage180m plan: Stage180l trained `256` hard-split optimizer steps from Stage180k `step-192` with lower LR (`5.0e-7`), online top-k/sequence weights `8.0/8.0`, reduced nonblank hard/margin `8.0/2.0`, and modest blank/mass/full `0.1/0.1/0.1`. Training was healthy and all online teacher paths matched with no misses, but fixed 200-sample decoder-path heldout collapsed back to blank: `step-64/128/192/256` had length ratios only `0.0132/0.0138/0.0121/0.0121`, average blank about `0.802-0.808`, blank-top1 about `0.996`, zero nonempty usable predictions, and Nano token ER remained `100`. This shows the single-frame nonblank hard CE is too brittle: reducing it lets full/sequence losses pick a blank-dominant CTC path, while increasing it earlier created emissions with wrong token identity. Stage180m should add a local CTC emission-window objective derived from Nano teacher top-1 peaks. For each high-confidence teacher nonblank frame, map it to the student timeline and require the teacher token to win at least one frame in a configurable local window by minimizing the per-window hard CE with a softmin/logsumexp aggregation plus an optional blank-vs-token margin. This handles small Nano/student time-boundary mismatches and gives token identity pressure without forcing every mapped frame to emit. Keep the BiRWKV/RWKV-7 encoder, SenseVoice frontend, Nano 5-layer CTC decoder/head structure, tokenizer, blank remapping (`60514 -> 60515`), and online audio-only teacher path unchanged. Verify the new loss with synthetic tests, then start Stage180m from Stage180k `step-192` or the better Stage180k emission checkpoint with sequence/full calibration reduced and gate early by heldout nonempty count, pred/ref length, blank-top1, and Nano CER/token ER.
					- 2026-07-01 Stage180m result and Stage180n plan: implemented online nonblank emission-window CE plus window blank-margin loss. The smoke verified nonzero teacher peak events and finite loss, and Stage180m trained `192` hard-split steps from Stage180k `step-192` with window/margin weights `30.0/8.0`, sequence `2.0`, top-k `1.0`, blank/mass/full `0.01/0.02/0.0`, confidence threshold `0.25`, LR `8.0e-7`, and export-only checkpoints. The window loss did exactly what it was asked to do but too strongly: heldout blank-top1 fell from Stage180k `0.956` to Stage180m `0.859/0.742/0.449` at `step-64/128/192`, nonempty samples rose to `110/129/183`, and length ratio rose to `0.603/1.052/1.820`, but Nano WER/CER/token ER regressed to `116.19/109.56/114.40`, `143.09/144.46/140.88`, and `183.56/256.76/181.65`. This is over-emission with wrong token identity, not progress. Do not continue Stage180m as-is. Stage180n should return to Stage180k `step-192`, keep the window loss but make it a high-confidence correction rather than the dominant prior: raise `ctc_teacher_frame_filter_min_nonblank_prob` to about `0.60`, lower window/margin weights to about `10-12/2-3`, reintroduce stronger blank/mass/full calibration (`~0.2/0.2/0.1`) on all frames, use sequence/top-k as moderate auxiliaries, and run only a short early-gated diagnostic. Gate by preserving nonempty emissions without exceeding length ratio `~0.3-0.6` and by improving Nano CER/token ER over Stage180k `step-192`. This remains a loss-weight/curriculum adjustment only; BiRWKV/RWKV-7 architecture, frontend, Nano CTC decoder structure, tokenizer, blank mapping, and online audio-only teacher path remain unchanged.
					- 2026-07-01 Stage180n result and Stage180o plan: Stage180n trained `128` hard-split optimizer steps from Stage180k `step-192` with high-confidence window/margin weights `12.0/3.0`, online top-k/sequence `4.0/4.0`, blank/mass/full `0.2/0.2/0.1`, confidence threshold `0.60`, LR `5.0e-7`, and export-only checkpoints. It avoided Stage180m's over-emission, but moved back toward blank-dominant output instead of improving token identity. Heldout decoder-path metrics were `step-32` Nano WER/CER/token ER `100.50/95.51/100.58` with length ratio `0.128`, then `step-64/96/128` regressed to near-blank length ratios `0.052/0.041/0.040`; Stage180k `step-192` remains better on CER/token identity (`100.996/95.143/100.929`, length ratio `0.183`). This shows the current objective family has a two-sided failure: top1 hard/window pressure can open emissions but makes frequent wrong tokens win, while blank/mass/full calibration quickly gives CTC a blank-collapse path. Stage180o should add a local top-k distribution emission-window loss, not just another weight sweep. For each high-confidence Nano nonblank peak, map the peak to the student timeline, take Nano's valid nonblank top-k distribution at that peak, and minimize a softmin/logsumexp window reduction of the top-k cross-entropy over nearby student frames. Keep optional top1 window margin only as a small auxiliary. This preserves local timing freedom like Stage180m, but token identity gradients come from Nano's local distribution instead of a single argmax. Verify with synthetic tests, then start an easy-split diagnostic from Stage180k `step-192` or Stage180n `step-32`, using output under `/tmp` because `/media/usbhd` has only about `8.3G` free. Gate early by Nano CER/token ER improvement over Stage180k, nonzero but bounded length ratio, and no return to blank-top1 `1.0`. This is a distillation-objective and curriculum change only; do not alter the BiRWKV/RWKV-7 encoder, SenseVoice frontend, Nano 5-layer CTC decoder/head structure, tokenizer, blank remapping, or online audio-only teacher path.
					- 2026-07-01 Stage180o result and Stage180p plan: implemented local top-k distribution emission-window distillation (`ctc_teacher_online_nonblank_window_topk_loss_weight`) and verified it with synthetic tests and a 4-GPU online-teacher smoke. Stage180o trained `128` easy-split steps from Stage180k `step-192` under `/tmp` with window-top-k `16.0`, small top1 window margin `1.0`, online top-k/sequence `2.0/2.0`, blank/mass/full `0.1/0.1/0.05`, threshold `0.50`, and LR `5.0e-7`. Training was stable and online teacher matching had no misses. On the same 200-sample Stage162 Nano-CTC heldout, Stage180o did not materially improve token identity: the best checkpoint was `step-64`, with Nano WER/CER/token ER `100.747/95.090/100.813`, length ratio `0.173`, `59/200` nonempty samples, average blank `0.586`, and blank-top1 `0.958`. This gives only a tiny CER change versus Stage180k `step-192` (`100.996/95.143/100.929`, length ratio `0.183`) while WER/token ER remain worse and exact token/text matches stay zero. Nonempty predictions still concentrate on the same high-frequency wrong tokens (`the`, `are`, `of`, `a`) plus occasional Chinese tokens on English samples, so local emission losses are correcting blank/emission strength more than utterance-level token identity. Stage180p should add an audio-only Nano sequence token-presence loss from the teacher CTC argmax sequence: for each valid nonblank teacher token, maximize the probability that the student assigns that token to at least one frame in the utterance, using a stable `-log(1 - prod_t(1 - p_t(token)))` objective. This is weaker than full CTC ordering but directly tells the CTC head which Nano tokens must be reachable somewhere, and should be combined with modest local window-top-k, sequence CTC, and blank/mass/full calibration. Verify with synthetic tests first; then run a short easy-split diagnostic from Stage180k `step-192` with lower window-top-k weight than Stage180o and presence as the main token-identity bridge. This is still online audio-only CTC distillation and does not alter BiRWKV/RWKV-7 architecture, frontend, Nano CTC decoder structure, tokenizer, blank mapping, or downstream LLM plans.
					- 2026-07-01 Stage180p result and Stage180q plan: implemented audio-only Nano sequence token-presence loss (`ctc_teacher_online_sequence_presence_loss_weight`) and verified it with synthetic tests plus a 4-GPU online-teacher smoke. Stage180p trained `128` easy-split steps from Stage180k `step-192` under `/tmp` with presence `8.0`, window-top-k `6.0`, top-k/sequence `2.0/2.0`, blank/mass/full `0.1/0.1/0.05`, threshold `0.50`, LR `5.0e-7`, and export-only checkpoints. Training was numerically healthy and all online teacher branches matched. The heldout result shows presence did not fix identity: Stage180p `step-32/64/96/128` measured Nano WER/CER/token ER `100.996/95.407/100.929`, `100.374/95.301/100.581`, `100.374/95.512/100.697`, and `100.125/95.723/100.581`, with length ratio falling from `0.183` to `0.122`, nonempty samples falling from `61/200` to `49/200`, blank-top1 rising from `0.956` to `0.970`, and teacher-token overlap recall staying near zero (`0.001-0.005`). This means the presence term mainly reinforced shorter blank-dominant outputs instead of making the correct Nano tokens win; the persistent failure mode remains sparse emissions of a few frequent wrong tokens. Stage180q should not add another weak auxiliary. Instead run a sequence-dominant online CTC stage from Stage180k `step-192`: make teacher collapsed argmax token sequence CTC the primary loss with substantially higher weight, keep local window-top-k only as a small emission helper, remove presence, and keep blank/mass/full as small calibration terms. Use the easy split, export-only checkpoints under `/tmp`, LR around `5.0e-7`, and short early gating. Promote only if teacher-token overlap recall and Nano CER/token ER improve without collapsing to all-blank or over-emitting. This remains an online audio-only CTC distillation/curriculum change; do not alter the BiRWKV/RWKV-7 encoder, SenseVoice frontend, Nano 5-layer CTC decoder/head structure, tokenizer, blank remapping, or downstream LLM plan.
					- 2026-07-01 Stage180q result and Stage180r plan: Stage180q trained `128` easy-split steps from Stage180k `step-192` with sequence CTC `24.0`, window-top-k `4.0`, online top-k `0.5`, blank/mass/full `0.02/0.02/0.01`, threshold `0.50`, LR `5.0e-7`, and export-only checkpoints. A single-process smoke exposed and fixed a debug-path device bug where online Nano teacher used `cuda:-1` when `local_rank=-1`; online teacher device now resolves bare CUDA to `cuda:0` while torchrun ranks still use their rank-local devices. Stage180q itself was stable but failed by blank-collapse: on the same 200-sample heldout, `step-32/64/96/128` measured Nano WER/CER/token ER `100.125/95.723/100.232`, `99.751/98.311/99.884`, `100.000/99.578/100.000`, and `99.876/99.419/100.000`; length ratio fell from `0.117` to `0.034/0.006/0.010`, nonempty samples fell from `49/200` to `21/4/6`, blank-top1 rose to `0.991-0.999`, and teacher-token overlap recall fell to zero by `step-96`. Therefore sequence CTC alone is still too indirect from this initialization: it first suppresses wrong emissions before learning correct token identities. Stage180r should add a direct ordered token-identity bridge from the Nano collapsed sequence, `ctc_teacher_online_sequence_window_loss_weight`: map each teacher token position monotonically to the student CTC timeline, apply a local softmin CE over a configurable window so each Nano token must become likely near its expected relative position, and combine it with modest sequence CTC plus small window-top-k/blank calibration. This is a distillation objective addition only; keep the BiRWKV/RWKV-7 encoder, SenseVoice frontend, Nano 5-layer CTC decoder/head, tokenizer, blank remapping, and online audio-only teacher path unchanged.
					- 2026-07-01 Stage180r result and Stage180s plan: implemented ordered Nano sequence-window CE (`ctc_teacher_online_sequence_window_loss_weight`, radius, temperature), fixed CLI coverage, and verified it with synthetic tests, py_compile, and a single-GPU online-teacher smoke. Stage180r trained `128` easy-split steps from Stage180k `step-192` with sequence-window `16.0`, sequence CTC `4.0`, window-top-k `2.0`, blank/mass/full `0.05/0.05/0.02`, LR `5.0e-7`, and export-only checkpoints. Training was stable and sequence-window events were present on batches with nonempty Nano sequences. Heldout imitation still did not improve identity: Stage180r `step-32/64/96/128` measured Nano WER/CER/token ER `101.121/95.776/100.929`, `100.000/95.671/100.232`, `100.000/96.885/99.884`, and `99.876/97.307/99.884`. `step-32` preserved Stage180k-like length ratio (`0.181` vs `0.183`) but kept the same high-frequency wrong tokens and teacher-token overlap recall stayed `0.0046`; later checkpoints shortened toward blank (`length ratio 0.116/0.069/0.054`, blank-top1 `0.970-0.986`). Therefore the new local ordered identity loss can hold emission volume briefly but still cannot make correct Nano token identities win during full-model updates. Stage180s should diagnose whether the existing BiRWKV encoder representation is currently readable by the Nano 5-layer CTC decoder/head: freeze the encoder, train only the Nano CTC decoder + CTC head from Stage180k `step-192`, raise sequence-window substantially, keep sequence CTC as a smaller ordering auxiliary, and use little or no blank/full calibration. If fixed-encoder output-branch training still fails to increase teacher-token overlap, the next direction should be encoder representation alignment or deeper CTC-decoder adaptation rather than more full-model loss-weight sweeps. This remains an objective/curriculum diagnostic only and does not change BiRWKV/RWKV-7 architecture, frontend, Nano CTC decoder/head structure, tokenizer, blank mapping, or online audio-only teacher path.
					- 2026-07-01 Stage180s result and Stage180t plan: Stage180s froze the BiRWKV encoder and trained only the Nano 5-layer CTC decoder plus CTC head for `128` easy-split steps from Stage180k `step-192`, using sequence-window `64.0`, sequence CTC `4.0`, window-top-k `4.0`, blank/mass `0.01/0.01`, LR `1.0e-6`, and export-only checkpoints. The output branch can reduce blank and emit more tokens, but not the right tokens: `step-32/64/96/128` measured Nano WER/CER/token ER `103.238/95.090/103.368`, `106.725/96.304/107.898`, `104.110/94.667/104.065`, and `105.106/96.568/104.762`; length ratio rose to `0.303/0.439/0.316/0.364`, nonempty samples to `77/95/80/89`, and blank-top1 fell to `0.928/0.891/0.923/0.911`. However teacher-token overlap recall only reached `0.0093` at best and predictions mostly amplified the same wrong frequent tokens (`264`, `366`, `295`, `257`, etc.), so CER movement is not a meaningful identity win. This fixed-encoder diagnostic indicates the current BiRWKV representation is not sufficiently Nano-CTC-readable by decoder/head adaptation alone. Stage180t should turn on online Nano encoder hidden-state alignment while keeping the architecture unchanged: train from Stage180k `step-192` with encoder/decoder/head unfrozen, set `ctc_teacher_online_encoder_loss_weight` to a modest value, return Nano `encoder_out` from the online teacher, use linear/nearest time mapping for MSE, and keep sequence-window/sequence/window-top-k as secondary CTC identity constraints with light blank/mass calibration. Gate by whether teacher-token overlap recall improves beyond Stage180s without large over-emission. This is representation distillation, not an architecture change: BiRWKV/RWKV-7 encoder, SenseVoice frontend, Nano CTC decoder/head structure, tokenizer, blank remapping, and audio-only online teacher path remain unchanged.
					- 2026-07-01 Stage180t result and Stage180u plan: Stage180t trained `128` easy-split steps from Stage180k `step-192` with the full model unfrozen, online Nano encoder hidden MSE `0.2`, sequence-window `16.0`, sequence CTC `2.0`, window-top-k `2.0`, blank/mass/full `0.02/0.02/0.01`, linear time mapping, LR `5.0e-7`, and export-only checkpoints. Online encoder records matched every logged batch and raw encoder MSE stayed around `8-9`, so the plumbing is valid. Heldout imitation still did not improve token identity: `step-32/64/96/128` measured Nano WER/CER/token ER `100.996/95.459/100.929`, `101.245/95.776/101.161`, `100.747/95.301/100.813`, and `100.872/95.407/101.045`; length ratio stayed Stage180k-like then shrank (`0.187/0.181/0.162/0.157`), and teacher-token overlap recall peaked at only `0.0058` before falling. The predicted token distribution remains dominated by the same wrong high-frequency ids, which means raw hidden MSE at this strength is not sufficient to rotate BiRWKV states into a Nano-CTC-readable space. Do not treat Stage180t as progress. Stage180u should change the representation-alignment formulation before more training: add a normalized encoder alignment option that compares per-frame layer-normalized or L2-normalized hidden states, optionally with a small learned/projected adapter used only for the loss, so scale/offset mismatch does not dominate MSE. Gate it with a short smoke plus the same 200-sample Nano imitation metrics and token-overlap recall. This is still a training-loss/diagnostic addition only; the deployed BiRWKV/RWKV-7 encoder architecture, frontend, Nano 5-layer CTC decoder/head, tokenizer, blank remapping, and audio-only online teacher path remain unchanged.
					- 2026-07-01 Stage181 Nano-encoder control plan: before more BiRWKV loss shaping, add a diagnostic control that replaces the BiRWKV/SenseVoice-RWKV encoder with the FunASR-Nano native `audio_encoder` while keeping the same project CTC pipeline and the Nano 5-layer CTC decoder/head. The upstream Nano encoder is `SenseVoiceEncoderSmall` with SANM attention/FSMN blocks, not the Qwen3 Transformer baseline already present as `frontend_type=qwen3_transformer`; record it as `frontend_type=funasr_nano_encoder` so the comparison is explicit. Load `audio_encoder.*`, `ctc_decoder.*`, and `ctc.ctc_lo.*` from `/media/usbhd/models/Fun-ASR-Nano-2512-modelscope/model.pt`, preserve the project blank remap `60514 -> 60515`, and run a short same-data control under `/tmp` because `/media/usbhd` is nearly full. If this control immediately matches Nano CTC, the failure is in BiRWKV representation readability; if it does not, the mismatch is in feature extraction, tokenizer/blank projection, or our CTC decoder/head wrapper. This is a comparison experiment only and does not change the mainline requirement that deployed models use BiRWKV/RWKV-7 TimeMixer with direction dropout.
					- 2026-07-01 Stage181 feature-extraction correction: the first Nano-encoder control checkpoint loaded all Nano weights correctly (`audio_encoder` 914 tensors, CTC decoder 84 tensors, CTC head 60,515 rows), and a direct one-sample parity check using FunASR's own extracted `speech` tensor matched native Nano CTC greedy output. The same checkpoint reached only `80%` token/text exact on the 200-sample heldout when driven by the project `sensevoice_lfr_fbank` feature extractor, so the remaining mismatch is frontend, not model weights. FunASR `WavFrontend` uses 16 kHz hamming fbank, `dither=1.0`, sample upscale, and an LFR implementation that left-pads by repeating the first frame before the `m=7,n=6` stride; the existing project LFR stacks from frame 0 and only right-pads. Add a separate `funasr_wav_frontend` extractor for the Stage181 control instead of changing historical `sensevoice_lfr_fbank` behavior.
					- 2026-07-01 Stage181 Nano-encoder control result: added `frontend_type=funasr_nano_encoder`, `feature_extractor_type=funasr_wav_frontend`, and `funasr_nano_ctc_init_load_encoder` so the project model can run Nano `audio_encoder -> ctc_decoder -> ctc_head` through the same prediction/summarization path as BiRWKV checkpoints. The control checkpoint `/tmp/rwkvasr_runs/stage181_funasr_nano_encoder_control_20260701/funasr_nano_encoder_ctc_init.pt` loaded all Nano encoder/decoder/head weights with project blank remap `60514 -> 60515`. On the same 200-sample Stage180 Nano-CTC heldout, BiRWKV decoder-path checkpoints remained near total mismatch (`stage180k_step192` Nano WER/CER/token ER `100.996/95.459/100.929`; `stage180t_step64` `101.245/95.776/101.161`, both `0%` token/text exact). The Nano encoder control with the old project LFR reached `6.102/4.594/6.039` and `80.0%` exact, proving the wrapper was mostly correct but the frontend differed. The Nano encoder control with FunASR `WavFrontend` reached Nano WER/CER/token ER `0.374/0.317/0.348`, `98.5%` token/text exact, length ratio `0.998`, and blank-prob diff `0.00053`; the remaining 3 mismatches are consistent with FunASR `dither=1.0` / audio-load nondeterminism. Conclusion: Nano encoder + Nano CTC is readable and the project CTC wrapper is valid; the Stage180 failure is primarily that the current BiRWKV/SenseVoiceRWKV representation is not Nano-CTC-readable, with an additional historical mismatch between `sensevoice_lfr_fbank` and FunASR's true LFR frontend. Do not treat this control as a mainline architecture change.
					- 2026-07-01 Stage182 FunASR-frontend BiRWKV diagnostic plan: before adding more representation losses, run a narrow BiRWKV diagnostic that keeps `frontend_type=sensevoice_rwkv`, the same Stage180k `step-192` checkpoint, Nano 5-layer CTC decoder/head, tokenizer, blank remap, and online audio-only teacher, but changes only student feature extraction from historical `sensevoice_lfr_fbank` to FunASR-compatible `funasr_wav_frontend`. First evaluate Stage180k with this frontend on the same 200-sample Nano-CTC heldout to measure whether frontend alignment alone changes blank/top-token behavior. Then run a short easy-split online distillation under `/tmp` (`32-64` steps, export-only checkpoints) with the Stage180t loss family but FunASR frontend. Gate by Nano CER/token ER, teacher-token overlap, nonempty count, and length ratio relative to Stage180k/Stage180t; if it does not improve, the remaining issue is deeper BiRWKV representation geometry rather than frontend mismatch. This diagnostic does not change the mainline BiRWKV architecture.
					- 2026-07-01 Stage183 self-attention encoder ablation plan: add a strict control for the user's proposed replacement test by using `frontend_type=funasr_nano_encoder` as a SenseVoice/Nano SANM self-attention student, the same FunASR `WavFrontend`, the same Nano 5-layer CTC decoder/head, the same online Nano CTC teacher, and the same Stage179a easy online-alignment data budget as the current BiRWKV distillation controls. To avoid the trivial full Nano-copy result already measured in Stage181, introduce a loader switch that copies only Nano non-attention encoder weights (FFN/norm/after-norm/tp-norm) while leaving `self_attn.*` randomly initialized; train this random-attention student with the Stage180t loss family for a short equal-data run, then evaluate against the same 200-sample Nano-CTC heldout. If the random-attention SANM student rapidly improves Nano token ER/overlap while BiRWKV does not, the blocker is likely RWKV representation geometry. If it also fails, the issue is more about data volume/loss curriculum or the Nano CTC decoder/head being hard to align from non-teacher encoder states. This is an ablation only; deployed/mainline architecture remains BiRWKV/RWKV-7 TimeMixer with direction-dropout support.
					- 2026-07-01 Stage183 self-attention encoder ablation result: implemented `funasr_nano_ctc_init_load_encoder_attention` so the Nano encoder loader can copy non-attention tensors while skipping SANM `self_attn.*`. The random-attention student loaded Nano encoder `564` tensors, skipped `350` attention tensors, loaded the Nano CTC decoder `84` tensors, and loaded/remapped all `60,515` CTC head rows. FunASR's FSMN conv path cannot run under DeepSpeed bf16 autocast with fp32 weights, so DeepSpeed normalization now respects an explicit YAML `bf16.enabled: false`; this ablation ran fp32 input/model compute while leaving the normal CUDA default bf16 behavior unchanged for configs that do not opt out. Stage183 trained on the same Stage179a easy online-alignment data and Stage180t loss family for `64` steps, then continued another `64` steps from the exported `step-64.pt` without reloading Nano init, giving a strict `128`-step equal-data control. On the 200-sample Nano-CTC heldout, this did not distill Nano CTC logits: random-attention `step32/step64/total128` measured Nano WER/CER/token ER `100.000/100.000/100.116`, `100.000/100.053/100.232`, and `100.000/99.947/100.000`, with `0%` exact match and token length ratios `0.034/0.023/0.019`. The model is more blank-collapsed than Stage180t BiRWKV (`step64` length ratio `0.181`, Nano WER/CER/token ER `101.245/95.776/101.161`) and remains far from the full Nano encoder control (`0.374/0.317/0.348`, `98.5%` exact). Conclusion: simply replacing BiRWKV with randomly initialized self-attention/SANM under the same small online distillation budget does not make Nano CTC logits align; full Nano attention weights are the key upper-bound signal, so the next useful controls are either full/partial attention weight seeding, stronger hidden/logit distillation with more data, or an architecture-to-architecture encoder state adapter rather than more short random-attention training.
					- 2026-07-01 Stage184 full self-attention initialization stability-control plan: before changing BiRWKV losses again, run the same online Nano CTC distillation recipe as Stage183 but load the full FunASR-Nano `audio_encoder.*`, including SANM `self_attn.*`, plus the Nano CTC decoder/head. This checks whether the current Stage180t/183 loss mixture and FunASR `WavFrontend` preserve a student that already lives on the Nano CTC manifold. Use the same Stage179a easy data, same tokenizer/blank remap, same heldout 200 Nano-CTC evaluation, and fp32 DeepSpeed config required by FunASR FSMN. If full-init remains near the Stage181 full Nano control after 64 steps, the training/evaluation path is stable and the failures are representation-initialization/architecture-transfer problems. If full-init degrades badly, fix the distillation loss weights or optimizer before spending more compute on BiRWKV alignment. This control is not a mainline architecture change; BiRWKV/RWKV-7 remains the target encoder.
					- 2026-07-01 Stage184 full self-attention initialization stability-control result: full Nano initialization loaded all encoder tensors (`914`, skipped attention `0`), the Nano CTC decoder (`84`), and the remapped Nano CTC head (`60,515` rows), then trained `64` steps on the same Stage179a easy online-alignment data and Stage180t/183 loss mixture. This control stayed far better than random-attention Stage183, but it degraded a model that was already on the Nano CTC manifold: on the fixed 200-sample Nano-CTC heldout, Stage184 `step32` measured WER/CER/token ER `2.366/1.954/2.323`, `90.5%` exact, length ratio `1.028`; Stage184 `step64` measured `4.981/4.382/4.762`, `82.0%` exact, length ratio `1.058`. The untrained full Nano control remains `0.374/0.317/0.348`, `98.5%` exact, length ratio `0.998`. Since the student begins from the teacher weights, this regression points to the loss recipe rather than the model wrapper: the uniform ordered `sequence_window` / sequence losses do not use Nano's true emission frames and can push even a correct Nano encoder off its original CTC timing. Therefore do not spend more BiRWKV compute with the Stage180t/183 loss mixture until a safer full-init control can preserve Nano CTC behavior.
					- 2026-07-01 Stage185 safe-loss full-init control plan: rerun the full Nano self-attention initialization control with the same FunASR `WavFrontend`, same Stage179a easy data, same online Nano teacher, same optimizer scale, and same 200-sample heldout, but remove the harmful uniform sequence constraints by setting `ctc_teacher_online_sequence_loss_weight=0.0` and `ctc_teacher_online_sequence_window_loss_weight=0.0`. Keep only frame-local Nano-logit calibration (`nonblank_window_topk=2.0`, blank/mass/full `0.02/0.02/0.01`) plus encoder hidden alignment `0.2` for this stability check. Gate success by keeping full-init close to Stage181 after `64` steps, ideally heldout exact match above `95%` and WER/CER near or below `1%`. If Stage185 is stable, transfer this safer loss recipe back to BiRWKV/RWKV-7 alignment; if it still degrades full-init, reduce or disable the remaining non-frame losses before any further architecture comparison.
					- 2026-07-01 Stage185 safe-loss full-init control result and Stage186 plan: Stage185 trained `64` steps with sequence and sequence-window disabled. It improved over Stage184 at the same `64`-step point but still did not meet the full-init stability gate: heldout Nano WER/CER/token ER was `2.740/2.376/2.671`, `89.0%` exact at `step32`, and `3.736/3.326/3.717`, `85.5%` exact at `step64`; length ratio stayed high (`1.040 -> 1.053`) and blank-prob diff rose to `0.028`. Compared with Stage184 `step64` (`4.981/4.382/4.762`, `82.0%` exact), removing sequence-window reduced damage but the remaining `nonblank_window_topk=2.0` plus calibration losses still pulls even a full Nano-initialized model away from Nano CTC. Stage186 should isolate pure CTC-logit distillation by using full Nano initialization again, same data and LR, but disabling all sequence, nonblank-window, blank/mass, and encoder-hidden objectives, leaving only full-vocabulary online KL over CTC logits (`ctc_teacher_online_full_loss_weight=1.0`, all frames). If this pure logits KL preserves the Stage181 control, it becomes the safe base objective for BiRWKV/RWKV-7 alignment; if it still degrades, the online teacher/student feature stochasticity or optimizer update scale must be fixed before more architecture comparisons.
					- 2026-07-01 Stage186 full-KL-only full-init result and Stage187 BiRWKV plan: Stage186 trained the full Nano self-attention initialization for `64` steps with only full-vocabulary online CTC KL enabled (`full=1.0`; sequence, sequence-window, nonblank-window, blank/mass, and encoder-hidden losses all `0`). This preserves the Nano CTC manifold: on the fixed 200-sample heldout, `step32` measured Nano WER/CER/token ER `0.498/0.422/0.465`, `98.0%` exact, length ratio `0.993`, and `step64` measured `0.374/0.317/0.348`, `98.5%` exact, length ratio `0.999`, matching the Stage181 untrained full Nano control. Therefore the online teacher path and optimizer can be stable; the damaging components were the sequence/window/top-k calibration losses. Stage187 should migrate this objective back to the mainline BiRWKV/RWKV-7 student under the same Stage179a easy data budget and Nano CTC decoder/head path: initialize from the best existing BiRWKV decoder-path checkpoint, use FunASR-compatible frontend only if the checkpoint/config path supports it without changing architecture, train with full-vocab online CTC KL as the primary loss, and evaluate against the same Nano-CTC heldout. This is still a distillation objective change, not a replacement of BiRWKV by self-attention.
					- 2026-07-01 Stage187 BiRWKV full-KL-only result: migrated the Stage186 pure full-vocabulary online CTC KL objective back to the mainline `sensevoice_rwkv` BiRWKV student, initialized from Stage180k `step-192`, with the same Stage179a easy data and Nano CTC decoder/head path. The first attempt with `cuda_clampw` failed before training because the environment currently lacks `ninja` for fused RWKV CUDA extension JIT; the rerun used the existing `native` RWKV backend, which changes kernel implementation only and keeps the BiRWKV architecture unchanged. Training was numerically stable for `64` steps (`online_full_missing=0`, final rank0 loss `2.516`, average train loss `1.902`), but it still did not distill Nano CTC outputs: heldout Nano WER/CER/token ER was `100.374/95.090/100.581`, `0%` exact, length ratio `0.151` at `step32`, and `99.876/97.202/99.884`, `0%` exact, length ratio `0.060` at `step64`. Compared with Stage180t BiRWKV sequence-window (`101.245/95.776/101.161`, length ratio `0.182`) this is only a small token-ER reduction and worsens emission length by `step64`; it remains far from Stage186 full self-attention initialization (`0.374/0.317/0.348`, `98.5%` exact). Conclusion: the self-attention replacement test does not show that randomly initialized attention can learn Nano CTC under equal data, and the stable full-KL objective alone is insufficient to make the current BiRWKV encoder representation readable by Nano's 5-layer CTC decoder/head. The next useful direction is an explicit encoder-state bridge or teacher-initialized adapter before the Nano CTC decoder, while preserving the BiRWKV/RWKV-7 encoder as the mainline architecture.
					- 2026-07-01 Stage188 CTC bridge plan: add an optional CTC-path bridge between the BiRWKV encoder output and the Nano 5-layer CTC decoder/head. The default must be identity so existing checkpoints and inference are unchanged; non-identity bridge variants should be initialized as near-identity residual adapters so loading Stage180/187 checkpoints with `strict=False` remains safe. The first diagnostic should freeze the BiRWKV encoder, Nano CTC decoder, and CTC head, train only the bridge on Stage179a easy audio with online Nano teacher, and use Nano encoder hidden alignment plus stable full-vocabulary CTC KL (`encoder=1.0`, `full=1.0`, all sequence/window/top-k/blank/mass losses disabled). The hidden loss should consume the bridge output, not the raw BiRWKV encoder output, because the goal is to make Nano's CTC decoder see Nano-like encoder states without changing the BiRWKV core. Gate by the fixed 200-sample Nano-CTC heldout: any meaningful bridge should increase token length ratio and reduce token ER from Stage187; if it cannot, unfreeze the bridge plus the Nano CTC decoder before unfreezing BiRWKV.
					- 2026-07-01 Stage188 CTC bridge result and Stage189 plan: implemented `ctc_bridge_type` (`none`/`linear`/`residual_mlp`) as a CTC-path-only adapter before the Nano CTC decoder, with identity defaults and zero-initialized residual output for safe old-checkpoint loading. `joint_losses` now returns both raw `encoded` and bridge output `ctc_encoded`, and online Nano encoder hidden loss uses `ctc_encoded`. Stage188 froze BiRWKV encoder, Nano CTC decoder, and CTC head, trained only the `2.1M` residual bridge parameters for `64` steps with encoder hidden MSE `1.0` plus full CTC KL `1.0`. Training was fast with `cuda_clampw` after making `ninja` available in PATH and numerically stable, but heldout imitation worsened: `step32` measured Nano WER/CER/token ER `100.000/100.000/100.000`, length ratio `0.004`; `step64` measured `100.000/99.947/100.000`, length ratio `0.008`, both `0%` exact. Hidden MSE dominated the objective and reduced some batch losses, but it did not create Nano-readable CTC emission timing; it pushed the frozen Nano decoder/head toward near-empty output. Stage189 should isolate the bridge itself by using the same frozen encoder/decoder/head and same residual bridge, but disabling encoder hidden MSE and training only with the stable full-vocabulary CTC KL (`full=1.0`). If bridge-only full-KL also fails, the next bridge diagnostic should unfreeze the Nano CTC decoder together with the bridge while keeping BiRWKV frozen.
					- 2026-07-01 Stage189 bridge-only full-KL result and Stage190 plan: Stage189 disabled encoder hidden MSE and trained only the residual bridge with all-frame full-vocabulary CTC KL. It was stable but worse than Stage187: heldout `step32` measured Nano WER/CER/token ER `100.000/100.000/100.000`, length ratio `0.0008`; `step64` produced empty output on the 200-sample heldout (`100.000/100.000/100.000`, length ratio `0.0000`). This shows that all-frame full KL is safe only when the student already sits on the Nano manifold; from the BiRWKV representation it is dominated by blank frames and drives the bridge toward blank-collapse. Stage190 should keep the BiRWKV encoder frozen and the CTC head frozen, but train the bridge plus Nano CTC decoder with full-vocabulary KL only on teacher nonblank-neighbor frames (`full_frame_filter=nonblank_neighbors`, radius `2`) plus at most a tiny blank calibration term. This tests whether the CTC decoder can adapt to BiRWKV states when the objective emphasizes actual Nano emission frames instead of all-frame blank mass.
					- 2026-07-01 Stage190 bridge+decoder nonblank-KL result: Stage190 froze the BiRWKV encoder and Nano CTC head, trained the residual bridge plus Nano 5-layer CTC decoder for `64` steps with full-vocabulary KL restricted to teacher nonblank-neighbor frames (`radius=2`, `min_nonblank_prob=0.25`). Training completed normally, but the selected-frame signal was too sparse and still collapsed to empty greedy CTC: heldout `step32` and `step64` both measured Nano WER/CER/token ER `100.000/100.000/100.000`, text/token exact `0%`, and length ratio `0.0000`. Relative to Stage187, unfreezing the CTC decoder under nonblank-only full KL did not rescue the BiRWKV representation; it removed emissions entirely. Do not extend this recipe. The next requested architecture comparison should be a fair self-attention replacement control using the stable Stage186/187 all-frame full-KL-only objective, because Stage183 random self-attention used the older sequence/window losses that were later shown to degrade even a full Nano-initialized encoder.
					- 2026-07-01 Stage191 fair self-attention replacement plan: rerun the random SANM self-attention student control with `frontend_type=funasr_nano_encoder`, FunASR `WavFrontend`, Nano CTC decoder/head loaded and remapped, Nano non-attention encoder weights loaded, and Nano encoder `self_attn.*` weights left randomly initialized. Use the same Stage179a easy data budget, `64` steps, batch/frame budget, optimizer scale, heldout set, tokenizer, blank mapping, and all-frame full-vocabulary online CTC KL-only objective as Stage186/187. This directly tests the user's question: whether replacing BiRWKV with self-attention can distill Nano CTC logits under equal data when the loss itself is stable. Compare Stage191 against Stage187 BiRWKV full-KL-only and Stage186 full Nano self-attention initialization. If Stage191 still collapses, the equal-data answer is no for random self-attention; the successful path requires teacher attention initialization or a better representation-alignment curriculum, not merely an attention operator swap.
					- 2026-07-01 Stage191 fair self-attention replacement result: trained the random SANM self-attention student for `64` steps under the same full-KL-only objective, same Stage179a easy data, same Nano CTC decoder/head, and same heldout as Stage187. Startup confirmed the intended architecture split: loaded `564` Nano non-attention encoder tensors, skipped `350` Nano `self_attn.*` tensors, and loaded/remapped Nano CTC decoder/head. Training was healthy (`online_full_match` present, no missing teacher rows, final rank0 loss `2.5390`, epoch train loss `1.8373`), but heldout Nano-CTC imitation still failed. Stage191 `step32` measured Nano WER/CER/token ER `100.000/99.842/100.000`, exact `0%`, length ratio `0.0237`; `step64` measured `100.000/100.000/100.000`, exact `0%`, length ratio `0.00146`, with previews showing blank-top1 `1.0` and empty predictions. The fair comparison is therefore: full Nano self-attention initialization (Stage186) preserves Nano CTC at `0.374/0.317/0.348` and `98.5%` exact; BiRWKV full-KL-only (Stage187) fails at `99.876/97.202/99.884`, length ratio `0.0598`; random self-attention full-KL-only (Stage191) also fails and is even more blank-collapsed at `100/100/100`, length ratio `0.00146`. Conclusion: under equal small-data online distillation, simply replacing BiRWKV with self-attention does not distill Nano ASR CTC logits. The decisive factor is whether the encoder starts on the Nano representation manifold, not the mere presence of self-attention. Further alignment work should target representation initialization/adaptation or a curriculum that first creates nonblank emissions before all-frame KL, while keeping BiRWKV/RWKV-7 as the mainline architecture.
					- 2026-07-02 Stage204 non-attention Conformer control plan: add a diagnostic `frontend_type=sensevoice_conformer_conv` that keeps the SenseVoice 70-layer shape, sinusoidal positions, layer norms, FFN path, and CTC interface, but replaces each BiRWKV TimeMixer slot with a depthwise-separable Conformer convolution module rather than MHSA/SANM/self-attention. This is a control experiment only and must not change the BiRWKV/RWKV-7 mainline. Initialize whatever SenseVoice non-attention weights match, leave the new convolution mixers random, load/remap the verified ModelScope Nano 5-layer CTC decoder/head, and run the same Stage179a easy audio-only online Nano CTC full-KL-only objective used by Stage187/191 for an equal-data `64`-step check. If this non-attention Conformer control also fails, the result supports the interpretation that small off-manifold distillation is under-trained/under-covered rather than that BiRWKV alone is the bottleneck.
					- 2026-07-02 Stage205 all-USBHD Nano CTC distillation data plan: use the already materialized Stage179 deduplicated `/media/usbhd` audio-only curriculum as the primary online Nano CTC logits source before creating more labels. Its manifest covers `62,072,225` unique audio rows and `118,465.161` hours after dedupe from CommonVoice22, clean public ASR, GigaSpeech/WenetSpeech, and Emilia: Stage179a easy `1,174,987` rows / `1,490.119h`, Stage179b medium `42,247,508` rows / `47,771.242h`, Stage179c hard `18,649,326` rows / `69,200.368h`, and Stage179d long `404` rows / `3.432h`. The next mainline BiRWKV run should stop using tiny equal-data diagnostics as evidence of data sufficiency and instead train online CTC logits over the Stage179 curriculum with enough steps per difficulty bucket, promoting checkpoints only by fixed Nano-CTC heldout imitation plus public normalized WER/CER gates.
					- 2026-07-02 Stage204 non-attention Conformer control result: implemented `frontend_type=sensevoice_conformer_conv`, a SenseVoice-shaped non-attention Conformer-conv encoder that uses depthwise-separable convolution in the temporal slot and contains no `self_attn` or `time_mixer` modules. Unit tests and a 4-GPU 2-step smoke passed. The full `64`-step equal-data run loaded `564` SenseVoice non-attention tensors, skipped `421` new conv/projection tensors, loaded Nano CTC decoder/head, and trained with online full-KL `missing=0`, final rank0 loss `2.9150`, epoch train loss `1.8311`. Fixed 200-row Nano-CTC heldout imitation still fully collapsed: Stage204 `step32` and `step64` both measured Nano WER/CER/token ER `100.000/100.000/100.000`, text/token exact `0%`, length ratio `0.0000`, with previews showing all blank predictions. This matches the random self-attention failure and is worse than the BiRWKV Stage187 length ratio. The equal-data control therefore supports the training-data/training-duration hypothesis: off-manifold encoders do not learn Nano CTC behavior from a tiny `64`-step easy-split KL check.
					- 2026-07-02 Stage205a launch plan: return to the mainline `sensevoice_rwkv` BiRWKV encoder and use the Stage179 all-USBHD pool rather than small structure controls. Start from the previously best stable Stage179a `step-768` checkpoint, train on Stage179b medium (`47,771.242h`) for `8192` steps with the stable non-collapse recipe from Stage179b3 (`lr=1e-7`, nonblank-neighbor online top-k CE `2.0`, full KL `0.1`, no blank BCE, no sequence loss, labels/decoder/anchors disabled, no SpecAugment, no direction dropout), save every `1024`, and gate every checkpoint on the fixed Stage162 Nano-CTC heldout. If length ratio stays stable and WER/CER/token ER finally move, extend medium and then promote to Stage179c hard; if it remains flat after enough medium steps, the bottleneck is objective/alignment rather than raw disk data availability.
					- 2026-07-02 Stage205a config/smoke result: generated `configs/generated/sensevoice_rwkv_stage205a_stage179a768_usbhd_medium47771h_audio_only_online_funasr_nano_ctc_nonblanknbr_topk2_blank0_fullkl0p1_lr1e7_8192_4x4090_deepspeed.yaml` with export-only step checkpoints (`save_deepspeed_sharded_checkpoints=false`) to reduce disk pressure during long online distillation. A first smoke revealed an environment issue rather than a model/data issue: `cuda_clampw` JIT needs the `ninja` executable on `PATH`; the Python package and `.venv/bin/ninja` exist, so launch commands must prepend `PATH="$PWD/.venv/bin:$PATH"` when not activating the venv. With that path fix, the 4-GPU Stage179b medium 2-step smoke completed: batch-inline audio rows worked, the 36.4GiB length index correctly fell back to streaming bucket loading, `online_teacher_match=12/12`, `online_teacher_missing=0`, `online_full_match=12/12`, `online_full_missing=0`, first-step teacher time was `1.18s`, and peak reserved memory was `11.79GiB`. Proceed with the full `8192`-step Stage205a run using the same launch environment.
					- 2026-07-02 Stage205a full run started: launched the `8192`-step Stage179b medium online Nano-CTC distillation under `/media/usbhd/rwkvasr_runs/sensevoice_rwkv_stage205a_stage179a768_usbhd_medium47771h_audio_only_online_funasr_nano_ctc_nonblanknbr_topk2_blank0_fullkl0p1_bs12_lr1e7_8192_zero1_exportonly_noaug_nodirdrop_4x4090` with `setsid -f` so it survives the agent shell, PID `1918803`, log `train.log`. Startup reached the training loop, loaded Stage179a `step-768` with `missing=0 unexpected=0`, used batch-inline audio rows, and at step `20` still had `online_teacher_missing=0` and `online_full_missing=0`; first-step timing was `data=0.37s compute=3.11s online_teacher=1.14s total=3.48s`, peak reserved memory `11.79GiB`. The first promotion gate is the exported `step-1024.pt`; evaluate it against the fixed Stage162 Nano-CTC heldout before deciding whether to continue medium, alter objective, or promote to hard.
					- 2026-07-02 Stage205a interruption and IO fix plan: the full run trained through the first gate and exported `step-1024.pt`, then continued to at least logged `step=1540` with `online_teacher_missing=0` and `online_full_missing=0`, but failed at `09:54` with `OSError: [Errno 28] No space left on device` while writing extracted tar audio into `funasr_online_audio_cache/rank*`. `/media/usbhd` is fully occupied (`100%`, about `972K` free), and persistent extracted wav caching is not appropriate for the Stage179b 47,771h online-distillation pool because utterance reuse is rare. Add a small online-teacher option to not keep tar-backed extracted audio after each FunASR forward, set it for Stage205a resume, delete only the Stage205a generated cache files, and resume from exported `step-1024.pt` with the same objective and BiRWKV architecture.
					- 2026-07-02 Stage205a resume result: added `ctc_teacher_online_keep_audio_cache` to the online FunASR-Nano teacher and DeepSpeed CLI/config. When set to `false`, tar-backed audio is extracted just long enough for the FunASR forward and then deleted; existing direct `audio_path` rows are unchanged. Syntax checks and a tar-backed unit smoke passed. Deleted only the Stage205a generated `funasr_online_audio_cache`, freeing `/media/usbhd` from about `972K` to `7.5G` available. A 4-GPU 2-step resume smoke from `step-1024.pt` with `keep_audio_cache=False` passed with `online_teacher_match=12/12`, `online_teacher_missing=0`, `online_full_match=12/12`, `online_full_missing=0`, and peak reserved memory `11.79GiB`. The resumed continuation is running from `step-1024.pt` for the remaining `7168` relative steps under `/tmp/rwkvasr_runs/sensevoice_rwkv_stage205a_cont1024_usbhd_medium47771h_audio_only_online_funasr_nano_ctc_nonblanknbr_topk2_blank0_fullkl0p1_bs12_lr1e7_7168_zero1_exportonly_noaug_nodirdrop_4x4090_nocache`, PID `1921682`, log `train.log`. Output moved to the root filesystem (`/tmp`, about `966G` free) while training audio still reads from `/media/usbhd`; at continuation step `30`, online teacher/full matches remain healthy and the no-cache directory is only `20K`.
					- 2026-07-03 Stage205a completion and data-coverage diagnosis: the continuation completed to relative `step=7168`, producing `/tmp/rwkvasr_runs/sensevoice_rwkv_stage205a_cont1024_usbhd_medium47771h_audio_only_online_funasr_nano_ctc_nonblanknbr_topk2_blank0_fullkl0p1_bs12_lr1e7_7168_zero1_exportonly_noaug_nodirdrop_4x4090_nocache/step-7168.pt` plus `epoch-1.pt`; final epoch train loss was `10.4246`, and online teacher/full losses remained `missing=0`. This run still did not use enough of Stage179b medium: because `max_steps` counts dataloader batches and DeepSpeed `gradient_accumulation_steps=4`, the effective Stage205a weights saw about `8192` data batches / `2048` optimizer updates / roughly `393k` sample exposures, at most about `0.93%` of the `42,247,508`-row medium split (`~445h` out of `47,771h` by average row duration), with likely duplicated early batches in the continuation because it restarted from the exported model checkpoint rather than a DeepSpeed dataloader/optimizer state. Fixed Stage162 Nano-CTC heldout evaluation of the final point regressed: Stage179a `step768` was `38.4330 / 24.6808 / 39.0955` Nano WER/CER/token ER, Stage179b3 `step256` was `38.4237 / 24.6944 / 39.0927`, while Stage205a final effective `8192` was `40.2512 / 27.6513 / 41.1064`, length ratio `0.868250`. Do not promote the final Stage205a checkpoint. The result confirms the medium pool was not actually consumed at meaningful scale and also suggests the current nonblank-neighbor top-k/full-KL recipe can drift/shorten outputs when extended without a heldout gate. Next: evaluate all intermediate Stage205a checkpoints, keep only any point that beats Stage179b3 `step256`, and for the next large-data run use true DeepSpeed resume or explicit epoch-offset resume plus much larger data-batch budgets (tens to hundreds of thousands of batches) and periodic heldout gates.
					- 2026-07-03 Stage206 full-USBHD online CTC logits distillation plan and smoke: added `step_eval_split` so step evaluation can explicitly sample the train split when no heldout split is carved out, and changed DeepSpeed step/epoch evaluation to compute the same online FunASR-Nano CTC teacher objective used for training instead of the zero supervised CTC loss on audio-only batches. Also fixed epoch-boundary resume bookkeeping so `epoch_batch_offset` is saved as `0` after a completed epoch, and made `resume_from=latest` recognize `epoch-N` DeepSpeed tags; this is required for split-by-split full-data training without skipping the next split. Added `scripts/run_stage206_full_online_ctc_distill.py`, which runs Stage179c hard -> Stage179b medium -> Stage179a easy -> Stage179d long for three full passes, using one output directory and DeepSpeed optimizer resume across segments. Estimated coverage with local batch `12`, world size `4`, and frame budget `12000` is hard `673,272`, medium `891,655`, easy `24,734`, and long `42` data batches per pass, for `1,589,703` batches/epoch and `4,769,109` total data-batch steps across three epochs. The run starts from Stage179b3 `step-256.pt`, keeps the mainline `sensevoice_rwkv` BiRWKV/RWKV-7 architecture unchanged, disables labels/decoder/anchors/full-vocab KL for scale, and uses online Nano top-k logits CE (`top_k=32`, weight `2.0`) plus blank/mass calibration (`0.05/0.05`), LR `5e-8`, `ctc_teacher_online_keep_audio_cache=false`, `save_every=10000`, and `step_eval_every=10000` with `256` online-teacher eval samples. A 4-GPU smoke (`2` easy steps, eval every step) passed: online top-k/blank/mass matches were `12/12` with `missing=0`, first-step timing was `data=0.37s compute=3.21s online_teacher=1.04s`, peak reserved memory `8.66GiB`, step eval losses were `18.8915` and `8.2861`, and the smoke latest checkpoint recorded `epoch_batch_offset: 0`.
					- 2026-07-03 Stage206 full run launch status: launched the full three-epoch Stage206 orchestrator in the background, PID `1926492`, output `/tmp/rwkvasr_runs/sensevoice_rwkv_stage206_stage179b3s256_usbhd_all118465h_3ep_online_topk32_blankmass_lr5e8_eval10k_dsresume_4x4090`, with segment log `logs/epoch1_hard.log`. The active segment is `epoch1_hard`, target global step `673,272`. Startup loaded Stage179b3 `step-256.pt` with `missing=0 unexpected=0`, used batch-inline audio rows and no persistent audio cache, and reached step `10` without teacher misses. Step `1` on the hard split had `online_teacher_match=7/7`, blank/mass matches `7/7`, first-step timing `data=0.26s compute=2.63s online_teacher=0.72s`, and peak reserved memory `17.11GiB`; by step `10`, online top-k/blank/mass matches remained complete with `missing=0`. `/tmp` had about `957G` free and `/media/usbhd` remained read-only for training data with about `7.6G` free at launch.
					- 2026-07-11 Stage206 fixed-loss evaluation plan: the live Stage206 hard-split run is healthy, but its built-in step checkpoint ranking uses only `256` train-split online-teacher samples, so late checkpoints can look worse simply from sample noise while bucketed training loss continues to fall. Add an eval-only CLI that loads an exported `.pt` checkpoint plus the run's `train_config.yaml` / `model_config.yaml`, builds the same WebDataset eval loader, runs the same online FunASR-Nano CTC top-k/blank/mass objective as the DeepSpeed training loop, and writes a JSON summary with weighted loss and component losses over a fixed, user-selected sample count and split seed. This is a diagnostic/evaluation slice only: do not change the BiRWKV/RWKV-7 architecture, frontend, tokenizer, CTC head, Stage206 training objective, or the currently running process. Use it after `400k` and later checkpoints to compare against `320k/330k/360k/390k` with a larger fixed sample count before making any training-recipe decision.
					- 2026-07-16 Stage207 real CTC benchmark comparison plan: evaluate Stage206 exported checkpoints and the original FunASR-Nano-2512 CTC branch on exactly the same public labeled audio. The primary clean sets are full LibriSpeech `test-clean` and `test-other` for English WER and full AISHELL-1 `test` for Chinese CER; Common Voice 22 English test and WenetSpeech TEST_NET remain secondary robustness sets after the clean gate. Both systems must use raw greedy CTC collapse with no beam search, language model, hotwords, insertion bonus, or blank bias. Decode with the shared SenseVoice CTC tokenizer, apply the repository `ctc` text normalization to reference and hypothesis, verify identical `utt_id` coverage, and report corpus-level normalized WER/CER plus prediction length and blank/emission diagnostics. The FunASR-Nano score is the direct `encoder + 5-layer CTC decoder + CTC head` output, not its LLM output. Add a manifest-based Nano-CTC evaluator and make CPU checkpoint reconstruction switch fused RWKV backends to the native reference so smoke/subset evaluation can run while the four GPUs remain occupied. This is evaluation plumbing only and must not alter the BiRWKV/RWKV-7 architecture or the live Stage206 objective. Use the measured student-to-teacher gap, not Stage206 distillation loss alone, to decide the next recipe: a large public WER/CER gap or severe emission/length mismatch requires stronger sequence/full-logit and possibly representation distillation; a small gap should favor finishing the current three-epoch schedule before changing the loss.
					- 2026-07-16 Stage207 real CTC benchmark comparison result: completed a decision-grade fixed random `public-200` comparison on real LibriSpeech test-clean/test-other and AISHELL-1 test audio, with identical utterance coverage, raw greedy CTC, no decode bonuses, the byte-identical Nano/learner tokenizer asset (`sha256 747979631e813193436aabcff7c1c235d37de8097b71c563ec8b63b7a515c718`), and shared `ctc` metric normalization. Direct Nano-CTC versus Stage206 `step-690000` measured LibriSpeech clean WER `7.457 / 36.678`, LibriSpeech other WER `13.127 / 52.294`, and AISHELL CER `3.850 / 48.620`. Nano prediction/reference unit ratios were `1.002/1.003/1.000`; student ratios were only `0.727/0.624/0.659`. Error decomposition confirms deletion is the dominant student failure: clean student substitution/deletion/insertion `8.696/27.629/0.354`, other `13.886/38.013/0.395`, and AISHELL `14.310/34.208/0.102`, while Nano deletion stayed below `0.8` on every set. On the same first `64` rows, Stage206 `step-500000` versus `step-690000` was essentially flat: clean WER `37.576 -> 36.212`, other `55.733 -> 54.237`, and AISHELL CER `53.347 -> 53.450`; this rules out a late-checkpoint collapse and shows that the current nonblank-neighbor top-k32 CE plus weak blank/mass objective has plateaued. The current output contains no meaningful non-pronunciation-token pollution, and student mean blank probability can be lower than Nano even while blank-top1 and deletion are higher, so further blank-bias reduction or simply repeating more data under the same objective is not supported. Stronger online audio-only distillation is required. The next recipe should preserve BiRWKV/RWKV-7 and use the current/latest checkpoint as initialization, keep sparse top-k logits, add a ramped teacher-greedy sequence CTC term plus local ordered/nonblank-window top-k peak supervision, retain only small blank/mass calibration, and gate first on fixed public-200 deletion rate and output/reference unit ratio before resuming the full-data schedule. Do not enable dominant all-frame full-vocabulary KL at the start because blank/background frames would dominate; consider low-weight nonblank-neighbor filtered full KL only after sequence emissions recover. Detailed artifacts are `artifacts/eval_benchmarks/stage207_ctc_teacher_compare_20260716.{md,json}` and prediction/report files under `/tmp/rwkvasr_eval/stage207_step690k_public200` and `/tmp/rwkvasr_eval/stage207_step500k_public64`.
					- 2026-07-17 Stage208 stronger online CTC distillation plan: stop Stage206 only after its next complete `10k` checkpoint boundary and initialize a separate run from that exported model, preserving the mainline `sensevoice_rwkv` BiRWKV/RWKV-7 encoder, frontend, Nano-compatible 5-layer CTC decoder/head path, tokenizer, blank mapping, and audio-only online teacher. The first admission phase uses the Stage179b medium split for `2,000` data batches (`~96k` four-GPU sample exposures and `500` optimizer updates at gradient accumulation `4`), with checkpoints and online-teacher loss evaluation every `500` batches. Keep nonblank-neighbor top-k32 CE as the base objective at weight `2.0`, reduce blank/mass calibration to `0.02/0.02`, and add bounded emission/order supervision at final weights teacher-greedy sequence CTC `0.25`, local teacher nonblank-window top-1 CE `0.25`, local nonblank-window top-k CE `0.50`, and blank-vs-token margin `0.05`, radius `2`, soft-min temperature `0.2`, margin `0.5`; do not enable presence, uniform sequence-window, encoder-hidden, or full-vocabulary KL in this phase. The low auxiliary weights are deliberate because the earlier Stage180q/r diagnostics used sequence/window weights `24/16` on only `128` easy batches and collapsed toward blank, whereas Stage206 now provides a much stronger emitted-token initialization. Run a two-step four-GPU smoke before launch, then gate the `500/1000/1500/2000` checkpoints against the fixed real public benchmark, using normalized English WER, Chinese CER, deletion rate, and hypothesis/reference unit ratio. Promotion requires no material WER/CER regression and a consistent deletion/length-ratio improvement on at least two checkpoints; blank collapse, output/reference ratio below the Stage207 baseline, or insertion-led over-emission stops promotion. Only after this gate may the auxiliary weights be increased gradually and the remaining medium/easy/long then later hard data schedule resume. Keep all-frame full KL disabled until emission length has recovered; any later KL must be low-weight and filtered to teacher nonblank neighborhoods.
					- 2026-07-01 Stage192 nonblank-emission recovery plan: since all-frame KL collapses off-manifold BiRWKV states to blank and Stage190 nonblank-neighbor full KL was too sparse, run a short decoder/head calibration that explicitly creates Nano-like nonblank emissions before returning to logits KL. Initialize from Stage180k `step-192`, freeze the BiRWKV/SenseVoiceRWKV encoder, keep the Nano 5-layer CTC decoder and CTC head trainable, and do not add the bridge. Use the same Stage179a easy audio-only online teacher data and disable all-frame full KL, blank BCE, mass CE, uniform sequence CTC, and uniform sequence-window losses. Enable only teacher-token presence plus teacher nonblank peak window losses: `sequence_presence=8.0`, `nonblank_window CE=8.0`, `nonblank_window_margin=4.0`, `nonblank_window_topk=2.0`, radius `4`, temperature `0.2`, `min_nonblank_prob=0.0`, LR `1e-6`, `64` steps. Gate by the fixed 200-sample Nano-CTC heldout: success is not final WER yet, but a clear increase in nonempty predictions / length ratio and lower token ER than Stage180k/Stage187 without the Stage180s over-emission drift. If Stage192 still emits empty or wrong tokens, the next step must move from loss-only calibration to a representation adapter or partial teacher-initialized projection before unfreezing BiRWKV.
					- 2026-07-01 Stage192 nonblank-emission recovery result and Stage193 plan: Stage192 confirmed that the Nano CTC decoder/head can be pushed to emit from frozen Stage180k BiRWKV states, but the objective is too unconstrained. Training completed with active sequence-presence and nonblank-window events and no missing teacher rows; window/margin losses fell during the run. Heldout results: `step32` increased nonempty predictions to `96/200`, length ratio `0.461`, but worsened Nano WER/CER/token ER to `108.966/99.419/108.711`; `step64` increased nonempty predictions to `165/200` and length ratio `1.478`, but collapsed into wrong-token over-emission with `155.666/156.864/166.086`. This is useful because it separates blank-collapse from representation readability: emissions can be created, but teacher token identity/order is not controlled enough. Do not promote Stage192. Stage193 should start from Stage192 `step32`, keep the encoder frozen and decoder/head trainable, reduce LR to `3e-7`, lower nonblank-window weights, and add Nano greedy-token CTC sequence loss plus tiny blank/mass calibration: `sequence=2.0`, `sequence_presence=1.0`, `nonblank_window=2.0`, `nonblank_window_margin=0.5`, `nonblank_window_topk=0.5`, `blank=0.02`, `mass=0.02`, full KL `0.0`, no uniform sequence-window. Run only `32` steps with checkpoints every `16`; gate by whether length ratio stays near `0.4-0.8` while token ER improves from Stage192 step32 rather than drifting toward Stage192 step64.
					- 2026-07-01 Stage193 sequence-stabilization result and Stage194 plan: Stage193 stabilized emission length but still did not make the decoder/head read the correct Nano tokens. From Stage192 `step32`, the reduced-LR sequence/blank/mass/window recipe completed `32` steps with active online sequence, blank, mass, and nonblank-window matches. Heldout `step16` measured `108.717/99.366/108.246`, length ratio `0.455`; `step32` improved slightly to `107.223/98.099/107.201`, length ratio `0.397`, nonempty `88/200`. This prevents Stage192 step64-style over-emission, but it remains worse than the Stage180o/187 baselines and gives `0%` exact. A post-hoc text-vocab-only special-token filter removed only `2` predicted non-text special tokens and barely changed token ER (`107.201 -> 107.085`), so special-token pollution is not the main failure mode. The next diagnostic should add representation capacity, not more loss-only tuning: start from Stage193 `step32`, enable the identity-initialized residual CTC bridge, freeze BiRWKV, train bridge plus Nano CTC decoder/head, keep the Stage193 sequence/window/blank/mass recipe, and add only a small Nano encoder-hidden MSE (`0.1`) on bridge output. Run `32` steps, save every `16`, and gate by whether token ER improves below Stage193 while length ratio remains near `0.3-0.6`. If this still cannot improve token identity, loss-only and shallow adapter calibration are not enough; move to a teacher-initialized representation projection or partial encoder-state distillation before more medium/hard data.
					- 2026-07-01 Stage194 bridge+sequence result and Stage195 plan: Stage194 started from Stage193 `step32`, added the identity-initialized residual CTC bridge, froze BiRWKV, and trained bridge+Nano CTC decoder/head for `32` steps with the same sequence/window/blank/mass recipe plus small Nano encoder-hidden MSE (`0.1`). Checkpoint loading had only the expected `6` missing bridge parameters. Heldout improved consistently but modestly: Stage194 `step16` measured `106.849/97.730/106.853`, length ratio `0.390`; `step32` measured `105.604/97.043/105.691`, length ratio `0.359`, versus Stage193 `step32` `107.223/98.099/107.201`, length ratio `0.397`. This indicates the bridge helps slightly, but the improvement may partly be moving back toward blank because the length ratio is falling and exact is still `0%`; it remains worse than the blank-heavy Stage187 token ER (`99.884`). Run one controlled Stage195 extension from Stage194 `step32` with the same recipe for `64` more steps, saving every `32`. Gate by requiring token ER to improve while length ratio does not collapse below roughly `0.2`; if token ER only improves by deleting emissions, stop this line and switch to stronger representation distillation rather than further loss tuning.
					- 2026-07-01 Stage195 bridge extension result and Stage196 plan: Stage195 continued Stage194 `step32` for `64` more frozen-BiRWKV steps with the residual CTC bridge, Nano CTC decoder/head trainable, and the same sequence/window/blank/mass plus small encoder-hidden recipe. The trend was monotonic but still not a real Nano-CTC alignment: Stage195 `step32` measured heldout Nano WER/CER/token ER `103.985/95.618/104.297`, length ratio `0.325`; Stage195 `step64` measured `101.993/95.248/102.323`, length ratio `0.236`, exact `0%`. This improves over Stage193/194, but most of the gain is accompanied by shorter output and it is still worse than the blank-heavy Stage187 token ER (`99.884`, length ratio `0.060`) and far from full Nano initialization. Do not blindly extend the frozen-encoder bridge line. Stage196 should probe whether the frozen BiRWKV representation is the bottleneck by starting from Stage195 `step64`, keeping the residual CTC bridge and Nano CTC decoder/head, unfreezing the full BiRWKV encoder at a very low LR (`1e-7`), and running only `32` easy-split online Nano CTC steps with the same bounded emission/sequence objective. Gate by whether token ER improves without length ratio falling below Stage195's `0.236`; if it improves mainly by blank deletion, switch to stronger representation-level curriculum or partial teacher-state initialization rather than more decoder/head calibration.
					- 2026-07-01 Stage196 low-LR full-encoder result: Stage196 started from Stage195 `step64`, unfroze the full BiRWKV encoder, residual bridge, Nano CTC decoder, and CTC head, and ran `32` easy-split online Nano CTC steps at LR `1e-7` with the same Stage195 objective. Training was stable (`missing=0 unexpected=0`, no online-teacher misses, peak reserved memory about `6.1GiB`), but it did not materially improve Nano CTC alignment. Heldout Stage196 `step16` measured Nano WER/CER/token ER `101.868/95.565/102.207`, length ratio `0.231`; `step32` measured `101.744/95.407/102.091`, length ratio `0.232`, exact `0%`. Versus Stage195 `step64` (`101.993/95.248/102.323`, length ratio `0.236`), only `7-12/200` predictions changed, nonempty predictions stayed `66 -> 65`, and predicted tokens fell `181 -> 177`, so the small token-ER gain is not a real representation breakthrough. Stop this low-LR extension line. The next alignment experiment should change the representation curriculum rather than continue decoder/head calibration: either train a stronger teacher-state adapter on Nano encoder hidden features before CTC loss, or use a partial teacher-initialized projection/teacher-assistant stage while keeping the BiRWKV/RWKV-7 encoder as the target architecture.
					- 2026-07-01 Stage197 representation-adapter plan: add a stronger optional CTC-path adapter, `ctc_bridge_type=context_residual_mlp`, that remains identity-initialized but adds local temporal context through a depthwise 1-D convolution plus residual FFN before the Nano 5-layer CTC decoder. This is not an encoder architecture change: the BiRWKV/RWKV-7 encoder output and streaming/non-streaming contract remain unchanged, and the adapter is only used on the CTC distillation path when explicitly configured. The first Stage197 run should start from the pre-collapse Stage180k `step192` student, load/remap the verified ModelScope Nano CTC decoder/head, freeze BiRWKV, CTC decoder, and CTC head, and train only the new context bridge with Nano encoder hidden MSE (`ctc_teacher_online_encoder_loss_weight=1.0`) for a short easy-split diagnostic. Disable CTC full/sequence/window/blank/mass losses in this first phase so the adapter is evaluated as a representation aligner, not as another blank-collapse recipe. Gate by the fixed 200-sample Nano-CTC heldout and by hidden-loss trend: success requires length ratio/nonempty outputs to improve relative to Stage187/188 without over-emission; if hidden-only context alignment cannot improve CTC readability, move to a teacher-assistant state objective or partial Nano state initialization rather than tuning CTC losses further.
					- 2026-07-01 Stage197 context-bridge hidden-only result and Stage198 plan: implemented `ctc_bridge_type=context_residual_mlp` with identity initialization, length masking, depthwise temporal context, and residual FFN. Targeted tests passed. Stage197 started from Stage180k `step192`, reloaded the verified ModelScope Nano CTC decoder/head, froze BiRWKV/decoder/head, and trained only the `2.10M` context bridge parameters with all-frame Nano encoder hidden MSE. Hidden loss fell quickly (`~8.69` at step1 to `~1.87` at step128 on rank0), proving the adapter has optimization capacity, but CTC readability worsened badly because all-frame hidden MSE is dominated by blank/background frames. Heldout Stage197 `step64` measured Nano WER/CER/token ER `100.000/99.947/100.000`, length ratio `0.00267`, only `2/200` nonempty predictions, avg blank `0.971`; `step128` measured `100.000/99.947/100.000`, length ratio `0.00167`, `1/200` nonempty predictions, avg blank `0.979`. Do not continue all-frame hidden-only alignment. Stage198 should extend the hidden-loss implementation to honor `ctc_teacher_frame_filter=nonblank_neighbors` for encoder hidden MSE: derive teacher nonblank/top-k confidence masks from Nano CTC records, map only those selected teacher frames and neighbors to the student timeline, and average MSE over selected student frames. Then train the same context bridge with frozen BiRWKV and frozen Nano decoder/head using nonblank-neighbor hidden MSE, optionally with a very small nonblank-window top-k/logit helper only after the hidden-filtered diagnostic shows nonempty outputs.
					- 2026-07-01 Stage198 filtered-hidden result and Stage199 plan: extended online encoder hidden MSE so it honors `ctc_teacher_frame_filter=nonblank/nonblank_neighbors`; tests verify that blank-frame hidden errors are ignored when filtering is enabled. Stage198 started from Stage180k `step192`, reloaded the verified Nano CTC decoder/head, froze BiRWKV/decoder/head, and trained the context bridge with `nonblank_neighbors` radius `2` and min nonblank probability `0.25`. The filtered objective ran correctly and nonzero filtered losses decreased (`~8.68` early to `~3.4` by step128), but CTC output remained essentially all blank: Stage198 `step64` measured Nano WER/CER/token ER `100.000/99.842/100.000`, length ratio `0.00167`, `1/200` nonempty, avg blank `0.971`; Stage198 `step128` measured `100.000/99.947/100.000`, length ratio `0.00167`, `1/200` nonempty, avg blank `0.969`. Filtered hidden alignment alone is therefore still insufficient. Stage199 should start from Stage198 `step128`, keep BiRWKV frozen, unfreeze the context bridge plus Nano CTC decoder/head, and add bounded CTC emission pressure: filtered encoder hidden MSE as a representation anchor, small Nano sequence/presence losses, and local nonblank-window top-k / top1 losses around teacher emission frames, with tiny blank/mass calibration. Gate by recovering nonempty predictions and length ratio above Stage198 while improving token ER versus Stage195/196; if it only returns to wrong-token over-emission, the representation adapter line is not enough and the next move should be a teacher-assistant encoder-state initialization rather than more loss tuning.
					- 2026-07-01 Stage199 context-bridge CTC recovery result: Stage199 started from Stage198 `step128`, kept BiRWKV frozen, unfroze the context bridge plus Nano CTC decoder/head (`41.17M` trainable parameters), and trained `64` steps with filtered encoder hidden MSE `0.5`, sequence/presence `1.0/1.0`, nonblank-window/top-k/margin `4.0/2.0/1.0`, and tiny blank/mass `0.02/0.02`. Online nonblank-window events were active and the average blank probability moved in the right direction, but greedy CTC did not recover usable emissions. Heldout Stage199 `step32` measured Nano WER/CER/token ER `100.000/99.947/100.000`, length ratio `0.00167`, `1/200` nonempty, avg blank `0.964`; Stage199 `step64` measured `100.000/99.947/100.000`, length ratio `0.00250`, `2/200` nonempty, avg blank `0.949`. This is a marginal blank-probability change, not alignment, and it remains far worse than Stage195's wrong-token-but-nonempty output (`66/200` nonempty, length ratio `0.236`, token ER `102.323`) and Stage187's blank-heavy full-KL baseline (`99.884` token ER). Stop the shallow context-bridge calibration line. The next material experiment should introduce a teacher-assistant or partial Nano encoder-state initialization before the CTC decoder: train an intermediate Nano-structured projection/assistant to map BiRWKV states into Nano encoder-state space, or distill selected Nano encoder layers/states into a larger adapter with stronger supervision, then only afterward reintroduce CTC logits. Continuing to tune local CTC losses on the current shallow adapter is not supported by the evidence.
					- 2026-07-01 Stage200 Nano-tail teacher-assistant plan: implement an optional `ctc_bridge_type=nano_encoder_tailN` CTC-path bridge that wraps the last `N` 512-dim FunASR-Nano `tp_encoders` plus `tp_norm` between BiRWKV and the Nano 5-layer CTC decoder. This is a teacher-assistant bridge, not an encoder replacement: the BiRWKV/RWKV-7 acoustic encoder remains the upstream representation, and the Nano-tail blocks are only a CTC-path adapter for distillation diagnostics. The bridge must load matching `audio_encoder.tp_encoders.{20-N..19}.*` and `audio_encoder.tp_norm.*` tensors from the verified ModelScope Nano checkpoint, while preserving old bridge types and checkpoint compatibility. First run `N=5`, start from Stage180k `step192`, reload Nano-tail bridge plus Nano CTC decoder/head from ModelScope, freeze BiRWKV, and train Nano-tail bridge + CTC decoder/head on Stage179a easy online teacher data with bounded nonblank/window/sequence objectives and small hidden alignment. Gate by whether this teacher-initialized assistant recovers nonempty Nano-like CTC emissions beyond Stage195/199 without over-emission; if it still collapses to blank, the bridge needs a deeper teacher-state initialization or a larger multi-layer state distillation phase before CTC logits.
					- 2026-07-01 Stage200 Nano-tail teacher-assistant result and Stage201 plan: implemented `ctc_bridge_type=nano_encoder_tailN` with direct loading of the last Nano `tp_encoders` plus `tp_norm`; tests and a smoke load confirmed `67/67` bridge tensors loaded, alongside the Nano CTC decoder (`84`) and remapped head (`60,515` rows). Stage200 `N=5` started from Stage180k `step192`, froze BiRWKV, and trained the Nano-tail bridge plus Nano CTC decoder/head (`54.86M` trainable parameters) for `64` easy-split online steps. The assistant reduced selected hidden MSE, but the CTC path became fully blank: heldout Stage200 `step32` and `step64` both measured Nano WER/CER/token ER `100.000/100.000/100.000`, exact `0%`, length ratio `0.000000`, with average blank probability still around `0.995`. This means the teacher-tail blocks are preserving the Nano blank/background manifold but are not creating emission peaks from the off-manifold BiRWKV states. Stage201 should restart from the clean Stage180k `step192` + ModelScope Nano-tail/decoder/head initialization, not from the blank-collapsed Stage200 checkpoint, and run a short nonblank-emission warmup: apply a negative blank-bias delta at init, disable hidden/all-frame/full-KL/blank/mass losses, freeze the BiRWKV encoder, and train only Nano-tail bridge + CTC decoder/head with high teacher-token presence plus teacher nonblank-window top-k/margin objectives. Gate success by nonempty prediction count and length ratio recovering above Stage187/199 while keeping token ER from exploding beyond Stage195; only after emissions exist should full-vocabulary KL be reintroduced.
					- 2026-07-01 Stage201 Nano-tail5 nonblank warmup result and Stage202 plan: Stage201 restarted from clean Stage180k `step192`, loaded Nano-tail5/decoder/head from ModelScope, applied CTC head blank-bias delta `-3.0`, froze BiRWKV, and trained Nano-tail5 + CTC decoder/head for `64` easy-split online steps with only sequence-presence and nonblank-window/top-k/margin objectives. Training was healthy and reduced average blank probability, but the raw greedy CTC remained almost all blank: heldout `step16/32/48` had `0/200` nonempty predictions and length ratio `0.000`; `step64` had only `3/200` nonempty predictions, `5` predicted tokens total, length ratio `0.0085`, and Nano WER/CER/token ER `100.000/100.106/100.000`. A decode-time blank-bias sweep on `step64` showed that additional blank penalties can force emissions but do not solve token identity: `blank -2/-4/-6/-8` gave length ratios `0.103/0.402/0.494/0.499` with token ER `100.348/101.974/104.530/104.646`. Therefore the problem is not just a blank threshold; Nano-tail5 is still not mapping BiRWKV states into the correct Nano token manifold. Stage202 should test a shallower teacher-assistant bridge, `ctc_bridge_type=nano_encoder_tail1`, starting again from Stage180k `step192` with ModelScope Nano-tail1/decoder/head, blank-bias delta `-4.0`, frozen BiRWKV, and a sequence-stabilized objective from the start: small Nano greedy-token CTC sequence loss, sequence-presence, local nonblank-window/top-k/margin, and tiny blank/mass calibration. Gate by length ratio entering the `0.15-0.6` band and token ER improving below Stage195/187 rather than merely deleting or over-inserting emissions.
					- 2026-07-01 Stage202 Nano-tail1 sequence result and Stage203 plan: Stage202 loaded Nano-tail1/decoder/head from ModelScope (`15` bridge tensors), applied blank-bias delta `-4.0`, froze BiRWKV, and trained `64` easy-split online steps with sequence CTC, presence, nonblank-window/top-k/margin, and tiny blank/mass calibration. Tail1 is easier to emit than tail5, but still not aligned: heldout `step16/32/48/64` measured length ratios `0.0135/0.0268/0.0674/0.1136`, nonempty counts `6/9/25/36` out of `200`, and token ER `100.000/100.116/100.348/101.045`. The predictions contain many non-pronunciation or invalid units such as `<|nospeech|>`, timestamp/special ids, pure whitespace, replacement-character byte fragments, and punctuation-only tokens; these are not valid CTC acoustic labels and directly compete with teacher text tokens. Stage203 should add a tokenizer-derived CTC suppression mask before any further loss tuning: when using `sensevoice_tiktoken`, build a project-token mask that suppresses special/timestamp/control ids, pure whitespace tokens, single-token decodes that normalize to empty under CTC normalization, and single-token decodes containing Unicode replacement characters, while preserving the project blank id. Apply this mask at the CTC-logit level for training losses and prediction, not as a text-only postprocess. This is an output-space constraint aligned with CTC label cleanup; it does not alter the BiRWKV/RWKV-7 encoder architecture. After tests, first run an inference-only diagnostic on Stage202 `step64` with suppression enabled, then train Stage203 from Stage202 `step64` or clean Stage180k depending on whether suppression reveals useful hidden token probabilities.
					- 2026-07-01 Stage203 suppression diagnostic and training plan: implemented tokenizer-derived `ctc_suppressed_token_ids` and model-level CTC logit masking; tests verify SenseVoice `<|nospeech|>`, timestamp/special ids, pure whitespace, replacement-character byte fragments, and invalid single-token decodes are suppressed while normal text tokens and blank are preserved. The real FunASR-Nano tokenizer yields `3,629` suppressed token ids out of `60,515`. An inference-only diagnostic on Stage202 `step64` with suppression enabled reduced invalid-token competition but also exposed that valid text-token logits are still weak: token ER improved from `101.045` to `100.116`, while length ratio fell from `0.1136` to `0.0248`. Therefore Stage203 should continue from Stage202 `step64` rather than restart from clean Nano init, enable `ctc_suppress_non_pronunciation_tokens: true` during training, keep BiRWKV frozen, keep Nano-tail1/decoder/head trainable, and increase valid nonblank pressure modestly (`sequence=2`, `presence=2`, `nonblank_window=4`, `margin=1`, `topk=1`, blank/mass `0.02`) without full-KL. Gate by increasing suppressed-decoded length ratio above `0.05` while keeping token ER at or below the Stage202 suppressed diagnostic; if it still collapses to blank, the next step should add a valid-token class prior or train a smaller bridge/head without the Nano CTC head bias rather than continuing tail calibration.
					- 2026-06-15 public benchmark target context: current open ASR reports are far below the active hard-set CTC error rates. Fun-ASR Technical Report Table 1 reports Fun-ASR-Nano around `AIShell1 CER 1.22`, `LibriSpeech clean/other WER 1.51/3.03`, and `WenetSpeech Net WER 5.46`; Qwen3-ASR reports Qwen3-ASR-0.6B around `LibriSpeech clean/other WER 2.11/4.55`, `GigaSpeech WER 8.88`, and `CV-en WER 9.92`, with Qwen3-ASR-1.7B lower on most of those. Treat these as public-reference targets for later full-set benchmark reporting, while recognizing that the project still needs large improvements before paper-level claims are meaningful.

### Phase 7: Dual-Mode Validation
- chunked `L2R` streaming runner
- `Bi` and `Alt` offline runners
  - benchmark and WER/CER scripts
- public full-set benchmark manifests for clean and non-clean ASR reporting:
  - `zh_clean`: AISHELL-1 test
  - `en_clean`: LibriSpeech test-clean and test-other
  - `zh_non_clean`: WenetSpeech TEST_NET
  - `en_non_clean`: Mozilla Common Voice English test
  - all benchmark reports must use the normalized ASR WER/CER metric described in the sidecar section
  - reports should retain raw pred/ref JSONL so CTC and RWKV decoder AR can be rescored after metric changes
  - benchmark prediction normalization and metric normalization must be separate knobs; default prediction normalization is `runtime`, while default metric normalization is `ctc` so punctuation, language-confirmation metadata, non-speech annotations, and Arabic numerals are normalized before WER/CER
  - long full-set prediction runs must emit periodic sample-count progress so tmux sessions can be distinguished from hangs while JSONL output is still buffered
  - AR sampling benchmark runs must record and fix the sampling seed because WER/CER otherwise has decode-time variance
  - draft-conditioned AR benchmark runs must generate CTC predictions on the same public eval split first and pass that eval prediction JSONL as the AR `{ctc_draft}` cache; do not reuse a train-set offline draft cache for public eval samples
- same-checkpoint mode-comparison reports
- standalone prediction CLI with JSONL outputs

Status:
- in progress
- greedy CTC decoding and same-checkpoint `Bi/L2R/Alt` token-level evaluation implemented
  - current validation still had token-error metrics on manifest token ids; replace with normalized-text WER/CER computation as the primary sidecar and validation report metric
- standalone prefix-beam prediction CLI and exported utterance-level hypotheses are the next slice

## 9. Immediate Next Steps

1. Finish `uv sync` so `pytest`, `ruff`, and runtime dependencies are available in `.venv`.
2. Implement the bidirectional TimeMixer wrapper and mode masks.
3. Implement fixed-rate Direction Dropout first; keep scheduler logic only for ablations.
4. Add optimizer parameter-group helpers that preserve upstream `att.w0` lr scaling and matrix-only weight decay.
5. Extend verification with runnable unit tests for:
   - forward shape
   - `v_first` propagation
   - direction masking
   - `Bi` vs chunked `L2R` mode semantics
   - chunk padding to fused-kernel constraints
   - causal chunk-by-chunk consistency in `L2R`

## 10. Open Questions

- Which dataset is first target: AISHELL, GigaSpeech, LibriSpeech, internal manifests, or mixed?
- Should we keep a byte-level tokenizer as an ablation against Whisper multilingual text-only CTC targets?
- What is the target `Bi -> L2R` WER delta that is acceptable for the first milestone?
- Should the fused CUDA kernel be mandatory for training, or optional behind a feature flag until the pure PyTorch path is verified?

## 11. Active Training

- 2026-07-19 Stage209 postmortem and Stage210 admission rules: normalized public-200 evaluation improved monotonically from Stage208, but remains far from the native Nano CTC target. Stage209 checkpoints at `5k/10k/15k/20k` reached LibriSpeech test-clean WER `32.230/30.915/30.106/29.702`, test-other WER `48.557/46.490/45.640/45.731`, and AISHELL-1 CER `44.395/41.431/40.545/39.966`; the corresponding Nano CTC references are `7.457/13.127/3.850`. Two implementation defects make the old logits-only data count misleading. First, Stage209 used the historical `sensevoice_lfr_fbank` frontend while Nano uses `WavFrontend`; a real LibriSpeech audit measured encoder-input RMSE `2.7904` and mean cosine `0.98270`, whereas the new `funasr_wav_frontend` path measured RMSE `0.1036` and cosine `0.999956`. Second, the DeepSpeed loop called `engine.zero_grad()` before every microbatch even with `gradient_accumulation_steps=4`, erasing the earlier microbatch gradients before each accumulation boundary. Remove that call and let `engine.step()` manage accumulation; Stage210 initially uses accumulation `1` so its data-batch and optimizer-step accounting are unambiguous.
- 2026-07-19 Stage210a raw-scale admission diagnostic: the first formal attempt was intentionally stopped before any checkpoint after 20 updates. Its 256-sample step-0 objective was `152.5995`, and the cyclic 40-47 layer group produced loss `1998.6116` while neighboring groups were mostly `3-21`. Because normalized MSE plus cosine is bounded near this scale, the outlier came from the provisional `raw_mse_weight=0.01` acting on not-yet-calibrated deep RWKV activations. Stage210a must therefore begin with normalized MSE plus cosine only (`raw_mse_weight=0`) to align representation direction/shape. Raw scale alignment may be introduced only after the fixed hidden objective stabilizes, with a separately admitted low weight; the stopped run is not a training result and consumed no checkpointed phase data.
- 2026-07-19 Stage210a stacked shape diagnostic: the run under `/tmp/rwkvasr_runs/sensevoice_rwkv_stage210_stage209_nanoqkv_exactfrontend_easy1490h_1ep_hidden_subblock_shape_lr1e5_4x4090`, W&B run `sxe1v4xc`, was stopped deliberately near step `370` before its first checkpoint after validating the stacked path. Its unshuffled 256-sample step-0 objective was `2.653313`; the first complete 70-layer cycle stayed within `2.5491-2.7505`, including layers 40-47 at `2.7366`, with all teacher records matched and maximum frame delta `0`. Peak reserved memory was `6.39 GiB`. This run is a diagnostic only and must not be treated as the admitted Stage210a result because accumulated upstream student error prevents it from isolating the temporal operator during initialization.
- 2026-07-19 Stage210a operator-local formal launch: the admitted teacher-forced run is active under `/tmp/rwkvasr_runs/sensevoice_rwkv_stage210_stage209_nanoqkv_exactfrontend_easy1490h_1ep_hidden_localshape_lr1e5_4x4090`, W&B run `12b5ex2r`. The bucket manifest resolves to exactly `30,064` data batches for one complete easy epoch with four GPUs, local batch `12`, frame budget `8,000`, and gradient accumulation `1`. Its unshuffled fixed 256-utterance step-0 objective is `2.377073`. The first 600 updates have complete teacher matching, zero missing records, zero frame delta, and stable finite layer losses; for the repeated layer 32-39 group, total loss changed from `2.2669` on its first sampled batch to `2.0935` at step 600, while temporal-mixer loss changed from `2.2309` to `2.0612`. This early training-batch comparison is directional evidence only; the fixed 256-utterance evaluations at `10k/20k/30k` remain the admission metric. Peak reserved memory is `4.35 GiB`, and a detached ten-minute monitor records process, checkpoint, disk, GPU, and loss status in `monitor_10min.log`.
- Stage210 hidden alignment is split into operator-local and stacked stages. The operator-local stage captures the exact Nano input to each encoder layer and applies the corresponding student `norm1 -> input_proj (layer 0 only) -> BiRWKV TimeMixer` to that teacher input. It must preserve BiRWKV forward/backward execution and RWKV-7 `v_first`: layer 0 is always evaluated as the shared `v_first` source even when it is not one of the loss layers. The local post-temporal residual and copied `norm2/feed_forward` then produce the student MLP target. This removes accumulated upstream student error from the temporal/MLP operator target and is the intended meaning of sub-block alignment. After local fixed metrics converge, the existing full student stack becomes the next hidden stage and aligns temporal, MLP, and complete block outputs under student-propagated inputs. Both modes use the same exact features, layer IDs, masks, and teacher tensors so their fixed losses are directly auditable; neither mode changes the BiRWKV architecture.
- Stage210 hidden objectives must report temporal/FFN/block student RMS, teacher RMS, their ratio, cosine, and a finite per-layer loss on the fixed evaluation set. Shape-only LayerNorm-MSE plus cosine is allowed for the initial operator-local stage, but it is not sufficient evidence for promotion because it is nearly invariant to activation magnitude. Unbounded raw MSE is prohibited for uncalibrated deep layers. Before stacked block or logits alignment, add and admit a bounded energy-normalized MSE term, `2 * mean((student-teacher)^2) / (mean(student^2) + mean(teacher^2) + eps)`, which preserves scale sensitivity without allowing a high-energy layer to dominate the batch. Promotion requires improving fixed-set shape and energy metrics without a pathological RMS ratio; do not infer scale convergence from training loss alone.
- 2026-07-19 Stage210 scale audit and objective correction: an independent nine-step four-GPU smoke on the exact formal initialization covered all 70 layers with finite losses/gradients, complete teacher matching, and zero frame delta. Mixer student/teacher RMS ratios by cyclic layer group were `2.49, 2.67, 1.89, 1.17, 1.24, 41.21, 0.38, 5.43, 1.95`; layers 40-47 had student/teacher RMS `459.12/11.14`. Mixer cosine remained near zero (`-0.021` to `0.042`) and bounded energy-normalized MSE remained near `2`. The Q/K/V transfer is therefore wired correctly but does not by itself calibrate the very different RWKV temporal operator. Shape-only metrics are invariant to common rescaling, while symmetric energy MSE has weak radial gradient when vectors are nearly orthogonal. Add a per-frame Smooth-L1 loss on `log(RMS(student)+eps) - log(RMS(teacher)+eps)` so angular shape and radial scale receive separate gradients; use provisional weights shape `1.0`, cosine `0.25`, energy `0.25`, and log-RMS `0.10`. Admit them only after another nine-step all-layer smoke remains finite, then stop the no-checkpoint shape-only run and restart Stage210a from the same Stage209 plus Nano initialization with a new fixed step-0 baseline and the original one-epoch/10k evaluation contract.
- Stage210 metric collection must not introduce a device synchronization per sample/layer/component event. Accumulate detached RMS, cosine, energy, and log-RMS sufficient statistics on the training device and transfer a stacked summary once per batch. The first scale-aware formal admission attempt established fixed step-0 loss `3.0252` and W&B run `j0k8141e`, but it may be restarted before any checkpoint solely to remove per-event `.item()` synchronization; the optimized replacement must reproduce the same fixed baseline within deterministic tolerance before it is admitted.
- 2026-07-19 Stage210a scale-aware pre-admission: the statistics-batched run under `/tmp/rwkvasr_runs/sensevoice_rwkv_stage210_stage209_nanoqkv_exactfrontend_easy1490h_1ep_hidden_localenergy_statsbatch_lr1e5_4x4090`, W&B run `hcw0y4yj`, reproduced fixed step-0 loss `3.0256` and reached step `820` with complete matching and zero frame delta before being stopped without a checkpoint to correct fixed-eval layer weighting. It also established that the hard layer 42-49 group was learning: mixer cosine reached `0.170`, RMS ratio fell from `18.9` to `14.5`, and group loss reached `2.706`. These are diagnostic training-batch results, not the promotion baseline.
- Stage210 fixed evaluation must use the same uniform cyclic layer sampler as training. Forcing boundary layers `0/49/50/69` into every one of the 64 fixed-eval batches gives those four layers half of all hidden events while each remaining layer is seen only about four times; such a loss is not a valid 70-layer promotion metric. Use eight uniformly cycled layers per eval batch so 256 utterances at eval batch size four cover every layer about seven to eight times. The `hcw0y4yj` run may be stopped before its first checkpoint solely to correct this evaluation bias; the replacement baseline supersedes `3.0256`, while the training objective, initialization, data order, and one-epoch contract stay unchanged.
- 2026-07-19 Stage210a exact-scale uniform-eval control: `/tmp/rwkvasr_runs/sensevoice_rwkv_stage210_stage209_nanoqkv_exactfrontend_easy1490h_1ep_hidden_localenergy_uniformeval_lr1e5_4x4090`, W&B run `a830xkj7`, established fixed unshuffled 256-utterance step-0 loss `3.0308`, complete teacher matching, zero frame delta, and finite all-layer execution. It was deliberately stopped without an admission checkpoint after step `4260` only after the paired same-data 350-step fixed evaluation proved that `rwkv_norm` converges substantially faster. This run is retained as an exact-scale control and must not be promoted; the replacement keeps the same data, objective, LR, checkpoint source, and `10k/20k/30k` gates while changing only QKV projection scale compatibility.
- 2026-07-19 Stage210a norm-matched formal admission: the replacement is active under `/tmp/rwkvasr_runs/sensevoice_rwkv_stage210_stage209_nanoqkv_rwkvnorm_exactfrontend_easy1490h_1ep_hidden_localenergy_uniformeval_lr1e5_4x4090`, W&B run `pvzk8t9u`. Its fixed unshuffled 256-utterance step-0 loss is `2.9998`, reproducing the independent ablation baseline `2.9997`; the first full 70-layer cycle has complete teacher matching, zero missing records, zero frame delta, and finite gradients. Device-resident FP16 teacher hidden targets are active, and first-step total/teacher time is `1.00/0.20 s`. This is the admitted Stage210a run: consume all `30,064` data batches, save and evaluate at `10k/20k/30k`, and do not stop early. The detached ten-minute monitor is `rwkvasr_stage210a_rwkvnorm_monitor` and writes `monitor_10min.log` under the run directory.
- Stage210 online hidden-state distillation must not round-trip same-rank teacher activations through host memory. Add an explicit device-resident layer-hidden record path for online hidden-only training: detach the requested Nano layer tensors, cast them to the same FP16 storage precision used by the existing CPU record path, and keep them on the teacher/student rank device until the batch loss is complete. The default record behavior stays CPU-backed for exporters and caches. The active uniform-eval run may reach its first `10k` checkpoint before adopting this optimization; resume from that exact DeepSpeed checkpoint so no samples or optimizer state are discarded, and compare first-step timing before/after. Device residency is a throughput change, not a promotion-metric change, so fixed loss must remain within normal FP16 numerical tolerance and all matching/frame-delta gates remain mandatory.
- The phase-2 full-logit path must provide the same explicit device-resident option for Nano's full CTC log-probability records. Keep FP16 storage and the existing blank/ignored-token projection unchanged, preserve CPU-backed records by default for export/cache use, and enable device residency only when teacher and student share the rank device. This removes a roughly `frames x 60,516` GPU-to-host-to-GPU transfer per batch without changing KL semantics.
- Device-resident full-logit distillation must keep time-map indices on the device of the tensor being indexed. The historical full-KL implementation forced nearest and linear-map indices to CPU because all cached targets were host-backed; applying that path to GPU-resident `full_log_probs` would reintroduce transfers and may fail with mixed-device `index_select`. Select full logits, top-k ids, and top-k probabilities with indices moved independently to each source tensor's device, and keep linear interpolation weights beside the full-logit tensor. Cover both nearest and linear maps with a CUDA-resident regression test before Stage210c is admitted.
- Stage210 fixed evaluation must also expose all-layer diagnostics instead of only a scalar objective. Aggregate each sampled layer's loss, energy-normalized MSE, log-RMS error, cosine, student RMS, teacher RMS, and RMS ratio over valid frames/elements and all four ranks; save the complete mapping beside each fixed-eval checkpoint and report per-layer loss/cosine/RMS ratio to W&B. Use these fixed-audio metrics, not variable training batches, to identify non-converging depth regions and to choose the hidden checkpoint promoted to stacked-block alignment.
- 2026-07-19 Stage210 distributed fixed-layer sampling correction: the first implementation selected layers from each rank's local `eval_batch_index`, so four ranks repeated the same 16 layer groups instead of using the intended 64 global groups. The scalar step-0/step-10k objective remains internally comparable because both evaluations use the same deterministic schedule, but its per-layer sample coverage is only one or two rank-synchronized groups and differs by up to `2x`; it is not the intended all-layer diagnostic. Derive the selector cursor from `eval_batch_index * world_size + rank`, which gives every layer seven or eight distinct global batch groups across the fixed 256-utterance evaluation. The Stage210 runner provides a zero-update `--eval-only` path and an explicit `--skip-nano-init` switch so reconstructed initialization and trained checkpoints can be scored without changing parameters. The corrected reconstructed baseline is `2.987220` with all 70 layers present and per-layer frame weights spanning `800-930`, versus `480-1000` under the repeated-rank schedule. Preserve the admitted process through its first 10k checkpoint, then score that checkpoint with the corrected schedule before making a Stage210b promotion decision; do not compare a corrected-schedule scalar directly with the historical local-schedule scalar.
- 2026-07-19 Stage210 real-audio hidden-hook semantic audit: a short Common Voice zh-CN utterance was run through the CPU Nano teacher while capturing layers `0/10/40/42/49/50/69`. Reconstructing Nano's residual formula from the captured `input/mixer/ffn` tensors matched captured block outputs with relative RMSE `0.000206-0.000657`. Loading all `564` matching Nano non-attention tensors into the student and recomputing its copied MLP on the exact teacher post-mixer inputs matched Nano FFN outputs with relative RMSE `0.000198-0.000521` and mean cosine `0.99999988-1.0`. This covers the main-to-TP normalization boundary and rules out wrong hook positions or FFN/Norm key mapping as the source of the remaining gap; the active error is in temporal-operator direction/scale alignment.
- 2026-07-19 Stage210 encoder-entry and fixed-input variance audit: the project and FunASR sinusoidal position encoders match exactly in FP32 (`max_abs=0`, relative error `0`); the actual BF16 student path versus the BF16-rounded FP32 teacher reference has relative error `0.00318` and cosine `0.999981`, which is expected precision loss rather than a different position formula. `funasr_wav_frontend` retains Nano's default `dither=1.0`, but two independent corrected-schedule reconstructions of the same 256-utterance step-0 model scored `2.987220` and `2.987181`, a delta of only `3.9e-5` (`0.0013%`). Keep the shared train-time frontend unchanged and treat this measured variance as fixed-eval numerical tolerance; it is far below a promotion-scale improvement and is not a candidate explanation for the distillation gap.
- Stage210 QKV warm-start must distinguish direction transfer from operator scale. A 70-layer ModelScope audit measured Nano Q/K/V weight-RMS ratios against the corresponding upstream RWKV-7 receptance/key/value initialization RMS: median `19.479/196.312/22.510`, with K as high as `288.085`. Exact transfer therefore drives RWKV's unnormalized `r * k * r_k * v` residual far outside its upstream operating scale even though the copied directions are useful. The paired four-GPU, 350-step, fixed-256 ablation used the same Stage209 checkpoint, data order, layer schedule, optimizer, and loss: exact QKV changed `3.0303 -> 2.9178` (`3.7%`), while `rwkv_norm` changed `2.9997 -> 2.2964` (`23.4%`) and finished `21.3%` lower. Stage210 therefore uses `rwkv_norm`: preserve every Nano Q/K/V matrix direction and relative structure, rescale only its global RMS to RWKV-7's upstream projection RMS (`0.5/sqrt(3C)` for receptance/value and `0.05/sqrt(3C)` for key), keep Nano output projection exact, and preserve all RWKV-only parameters. This is a scale-compatible Nano initialization, not random initialization or an architecture change.
- 2026-07-19 Stage210 formal-kernel parity audit: the active run uses upstream `rwkv7_clampw_v3` semantics, with decay `exp(-exp(-0.5) * sigmoid(raw_w))`, rather than the older wind-backstepping decay transform. A deterministic BF16 CUDA comparison against an independent FP32 recurrent reference measured forward relative L2 error `2.41e-6`; all six input-gradient cosines were `1.0`, the largest gradient relative L2 error was `1.55e-4`, and every gradient was finite. Preserve `cuda_clampw` for the admitted run and add a CUDA-gated forward/backward regression test. The kernel is not a candidate explanation for the remaining Nano hidden-state gap unless that regression fails on the target runtime.
- Stage210 layer sampling is an optimization-coverage control, not a model change. Keep the admitted Stage210a process at eight uniformly cycled loss layers per data batch through all `30,064` batches so its `0/10k/20k/30k` contract stays comparable. Expose a guarded `1-70` layer-sample-count runner option and admit a 24-layer smoke only after it proves complete teacher matching, zero frame delta, finite gradients, and acceptable four-card memory. At the corrected `10k` gate, increase layer coverage only in a separate continuation or the next phase if fixed per-layer metrics show that the eight-layer schedule is update-limited; do not overwrite or silently mutate the active run.
- 2026-07-19 Stage210 24-layer coverage admission: a fresh three-step, four-GPU smoke under `/tmp/rwkvasr_runs/stage210a_layer24_coverage_probe_v2_20260719_smoke` covered all 70 layers (`0-23`, `24-47`, then `48-69` plus wraparound), matched every teacher row, kept missing records and frame delta at zero, and completed without non-finite gradients or OOM. Peak reserved memory on the first local rank was `4.69 GiB`; the representative training step took about `1.72 s`, roughly `2.3x` the eight-layer path for `3x` layer-update density. The 24-layer setting is therefore technically admitted but not yet selected. Smoke exports intentionally omit DeepSpeed optimizer shards, so the runner must reject attempts to auto-resume such an output with a clear error instead of constructing an invalid `resume_from=latest` config.
- Stage210 must isolate whether the Stage209 RWKV-only state is a useful prior. Build a zero-update control from the exact upstream TimeMixer initialization, then apply the same admitted Nano `rwkv_norm` Q/K/V/output transfer, first-layer SVD projection, copied Norm/MLP, and copied CTC decoder/head used by the active run. Compare it on the corrected fixed 256-utterance evaluation before spending data batches. This is not a fully random student: only parameters with no valid Nano counterpart use fresh upstream initialization. Train a paired control only if that fixed baseline materially beats the Stage209-inherited baseline or reveals a clearly healthier all-layer scale/cosine profile.
- 2026-07-19 Stage210 fresh-RWKV control: the zero-update corrected fixed loss was `2.6671`, `10.7%` below the Stage209-inherited `2.987220`, and a same-data 350-step probe reached `1.9770` (`25.9%` below its own baseline and lower than the inherited 350-step control). However, training-batch mixer RMS ratios stayed around `1e-4-1e-2` while cosine improved, so the lower scalar partly rewards normalized shape on near-zero temporal outputs. The current fixed per-layer YAML combines mixer and copied-FFN statistics; FFN bias energy masks this collapse and makes its RMS ratio unsuitable for initialization selection. Extend fixed evaluation to emit nested per-layer metrics separately for every active `mixer`/`ffn`/`block` component using the already-computed teacher/student tensors. Keep the combined metric for backward comparison, but select fresh, inherited, or hybrid initialization only from component-specific mixer scale plus cosine and external CTC evidence.
- 2026-07-19 Stage210 component-metric verification: the corrected evaluator now records all 70 layers separately for `mixer` and `ffn` without another teacher/student forward. On the fresh step350 control, mixer mean loss/cosine/RMS ratio were `1.9352/0.5776/0.00381` while FFN values were `0.1150/0.9630/0.9750`, proving that the historical combined RMS hid temporal-output collapse. The Stage209-inherited step-0 mixer values were `2.9551/0.0118/0.1194`; despite worse initial shape, its non-collapsed temporal scale is a materially better optimization prior, and the active run's sampled mixer RMS ratios have subsequently approached `0.9-1.0`. Do not restart from fresh RWKV-only parameters. Use the component-specific inherited step-0 artifact under `/tmp/rwkvasr_runs/stage210a_inherited_rwkv_step0_component_eval_eval` as the comparison baseline for the corrected step10k sidecar.
- 2026-07-19 Stage210a step10k fixed gate: the uninterrupted formal run exported the complete `step-10000.pt` and continued training. Its original-schedule fixed loss fell from `2.9998` to `0.9141`. The corrected global-rank sidecar under `/tmp/rwkvasr_runs/stage210a_rwkvnorm_global_component_eval_step10000_eval` measured `0.889592` versus the matching inherited step-0 baseline `2.987020`, a `70.22%` reduction. Mixer mean loss/cosine/RMS ratio improved from `2.9551/0.0118/0.1194` to `0.8448/0.7170/0.7010`; mixer loss, cosine, and distance-to-unit RMS ratio improved independently in all `70/70` layers. The copied-FFN functional loss/cosine improved from `0.1212/0.9620` to `0.0674/0.9778`, with loss and cosine improving in all `70/70` layers. There were no missing teacher rows, frame mismatch is impossible under the admitted zero-tolerance path without a hard failure, and no non-finite or CUDA error occurred. Layers `10-29` remain the weakest depth band (`10-19`: loss/cosine/RMS ratio `1.3494/0.5437/0.6097`; `20-29`: `1.1111/0.6342/0.6027`), but their same-depth online metrics continue improving and do not show a plateau. Keep the admitted eight-layer cyclic schedule unchanged through all `30,064` data batches so the `0/10k/20k/30k` experiment remains comparable and consumes the full `1,490.119 h` easy epoch. Corrected component sidecars are armed for step20k and step30k. Select the admitted 24-layer schedule only for a separate continuation or the next hidden phase if the corrected step10k-to-step20k mixer loss improves by less than `10%` while the `10-29` band remains below cosine `0.65` or outside RMS ratio `0.75-1.25`; do not mutate or restart the active run.
- 2026-07-20 Stage210a completion and Stage210b admission: Stage210a consumed all `30,064/30,064` batches from the complete `1,490.119 h` easy split and exported `10k/20k/30k/30064/epoch-1` checkpoints without early stopping, missing teacher rows, frame mismatch, non-finite loss, OOM, or CUDA failure. Corrected fixed losses at `0/10k/20k/30k` were `2.987020/0.889592/0.696960/0.602417`; the post-10k intervals still improved by `21.65%` and `13.57%`. Mixer mean loss/cosine/RMS ratio at 30k were `0.5690/0.8115/0.7569`, with all 70 layers better than step0. The remaining weakest bands were `10-19` (`0.9038/0.6982/0.6842`) and `20-29` (`0.7424/0.7628/0.6492`), but neither plateaued. A matching evaluation of the 64-update epoch-final export measured total loss `0.601992` and mixer `0.568420/0.811696/0.757378`, slightly better than step30k, so select `step-30064.pt` as the Stage210b initialization. Stage210a passes the operator-local gate; do not add a separate MLP stage because the copied-FFN functional loss/cosine also improved across all 70 layers. Before formal Stage210b, record a stacked whole-block step0 baseline and require a short four-GPU smoke with complete online-teacher matching, zero frame delta, finite gradients, and memory below the 24 GiB device limit.
- 2026-07-20 Stage210b stacked admission: the corrected 256-utterance step0 baseline from Stage210a `step-30064.pt` is `1.590025` for the weighted stacked objective. Separate mean loss/cosine/RMS ratio values are mixer `1.3043/0.5563/0.5952`, copied-FFN functional output `1.6966/0.4033/0.6997`, and residual block `0.5135/0.8353/0.6721`. This expected gap relative to teacher-forced Stage210a measures accumulated student-state error rather than a regression in the selected checkpoint. A three-step four-GPU backward smoke loaded the checkpoint with `missing=0 unexpected=0`, trained all `336,503,904` encoder parameters while keeping the Nano five-layer CTC decoder/head frozen, matched every online-teacher row, kept frame delta at zero, and completed with finite losses and no OOM. The first batch reserved `6.84 GiB` and allocated `6.17 GiB`; retain the adaptive `8,000` frame budget and do not enable gradient checkpointing unless a representative formal batch proves it necessary. Admit the existing Stage210b weights (`mixer=0.5`, `ffn=0.25`, `block=1.0`), eight cyclic layers per update, LR `3e-6`, one complete `1,490.119 h` easy epoch, and fixed evaluation/checkpoints every `10,000` batches. Continue to use the copied/frozen Nano CTC output path; final-logit KL remains disabled until the stacked hidden gate passes.
- 2026-07-20 Stage210b formal launch: started `/tmp/rwkvasr_runs/sensevoice_rwkv_stage210b_stage210a_step30064_stacked_fullblock_easy1490h_1ep_lr3e6_4x4090` on four RTX 4090s with W&B run `w32pizg4`. The formal loader resolves to exactly `30,064` batches, and the active pre-update fixed baseline is `1.5921` over 256 utterances, consistent with the independent `1.590025` admission baseline. At logged step 50, every online-teacher row matched, missing count and frame delta remained zero, and the run had no non-finite or CUDA errors. Runtime memory was about `11.7 GiB` per card on the sampled active batch, while the first-batch measured peak remained `6.84 GiB`; leave the admitted frame budget and checkpointing policy unchanged. A detached ten-minute monitor records process, loss, checkpoints, disks, and all four GPUs. Do not stop before all data are consumed; use the corrected component-aware `10k/20k/30k` fixed reports to select the Stage210b checkpoint for final-logit alignment.
- Stage210 is an adaptive three-stage alignment, while the model architecture remains fixed. Stage210a is operator-local teacher-forced alignment of Nano SANM temporal outputs and the resulting copied-MLP branch outputs; Nano FFN/Norm parameters are copied exactly and frozen, so the MLP objective is a functional constraint on the TimeMixer result rather than a separate parameter-training stage. Stage210b starts only from a passing Stage210a checkpoint, propagates student states through the complete encoder, and aligns temporal, MLP, and full residual-block hidden states. Stage210c aligns the final full-vocabulary Nano CTC log probabilities through the copied five-layer Nano CTC decoder/head while retaining a decaying hidden-state anchor. A separate MLP-only stage is introduced only if the exact-copy semantic audit fails; the current real-audio audit passed. Promotion and loss reweighting are decided from fixed-set per-layer loss/cosine/RMS metrics and normalized WER/CER, never from variable training batches alone.
- Stage210c is split into two logits curricula, with launch weights treated as provisional until the Stage210b fixed gate. The `nonblank` admission phase uses exact Nano features, device-resident full logits, full-vocabulary KL on teacher nonblank frames plus their immediate neighbors, explicit blank/mass calibration, and a stacked temporal/MLP/block hidden anchor on one complete easy epoch. The copied Nano five-layer CTC decoder and CTC head remain frozen so this phase must move the encoder onto the Nano output manifold instead of destroying an already correct output path. Only after normalized English WER, Chinese CER, decoded-length ratio, fixed full-KL, and hidden metrics improve without blank collapse may the `full` phase use all-frame KL over the complete deduplicated easy/medium/hard/long pool for three epochs. With local batch `12`, four ranks, and an `8,000` frame budget, the manifests resolve to `30,064` nonblank-admission steps and `6,257,160` cumulative full-curriculum steps. The full phase reduces but does not initially remove the hidden anchor. Sequence, presence, margin, and window pseudo-label objectives remain disabled in both phases; if full-KL plateaus, change one loss family at a time based on the fixed gates.
- Stage210c fixed evaluation must decompose the logits objective rather than report only the weighted training sum. On the same unshuffled fixed audio, aggregate across all ranks: selected-frame full-vocabulary KL, selected and all-frame top-1 agreement, blank-probability MAE, teacher/student nonblank-frame rates, CTC-collapsed token edit rate, and student/teacher collapsed-length ratio. Save these metrics beside the layer report and send them to W&B at every 10k gate. Use blank/nonblank and collapsed-sequence diagnostics to distinguish distribution calibration from peak-timing failure; normalized public English WER and Chinese CER remain the external promotion criteria.
- 2026-07-19 Nano CTC output-path parity audit: load the same `84` `ctc_decoder.*` tensors from ModelScope `model.pt` into FunASR's registered `Transformer` adaptor and the project's `NanoCTCTransformerDecoder`, then evaluate a deterministic two-utterance random batch with unequal lengths. The five-layer decoder outputs and output lengths match exactly (`max_abs=0`, relative error `0`). Applying `ctc.ctc_lo` and remapping teacher blank `60514` to project blank `60515` also gives exact equality over every active logit, while the now-unused project row `60514` remains suppressed at `-10000`. A separate full-vocabulary storage audit found all FP16 teacher log probabilities finite (`min=-62.90625`) and post-quantization probability mass in `0.9999416-1.0002326`, with maximum log-normalizer error `2.33e-4`; device-resident FP16 storage is therefore adequate for Stage210c. The copied decoder/head implementation and vocabulary/blank projection are not candidate explanations for a later Stage210c gap; investigate encoder hidden geometry, optimization, and frame weighting first.
- Stage210 phase 1 must use exact batched FunASR features for both teacher and student, no SpecAugment or dropout, frame tolerance `0`, deterministic cyclic layer sampling, and an unshuffled fixed step-eval subset. Record the fixed 256-utterance hidden objective at step `0`, then reuse the same data order and layer schedule at `10k/20k/30k`; comparisons without this pre-update baseline are not admission evidence. Stage210a maps Nano Q/K/V/output into both RWKV directions with the admitted `rwkv_norm` projection scaling, copies Nano FFN/Norm plus the five-layer CTC decoder/head, retains current-checkpoint RWKV-only parameters, freezes copied FFN/Norm and the CTC path, and optimizes the first-block projection plus TimeMixer against temporal-mixer and MLP-branch hidden targets. It must consume one complete `stage179a_easy` epoch (`1,174,987` unique utterances, `1,490.119 h`) with checkpoints and fixed evaluation every `10,000` data batches; no early stopping is allowed after admission. Promotion to full-block alignment requires zero missing records, zero frame mismatch, all 70 layers covered, finite gradients, and a material fixed-set hidden-loss reduction rather than a training-loss-only improvement. Stage210b starts from the selected Stage210a checkpoint without reapplying Nano initialization, adds full-block hidden loss, and unfreezes the encoder only after the copied-subblock path has demonstrated convergence. Phase 2 full-logit KL may start only after the hidden-stage gate and must retain a decaying layerwise anchor; the previously harmful sequence/window objectives remain disabled until full-logit and normalized WER/CER evidence justify them.
- 2026-07-19 Stage210 hierarchical encoder-alignment plan: Stage209 completed all `20,000` data batches and exported checkpoints at `5k/10k/15k/20k`; its phase-weighted 512-sample online losses are not directly comparable (`7.9466/8.2592/8.8856/9.6325` because sequence/margin weights increase), so fixed public-200 WER/CER evaluation is running for all four checkpoints before selecting a baseline. The next experiment must not continue logits-only tuning. Implement an optional audited Nano-QKV-to-BiRWKV initialization on top of the current strongest checkpoint: exact Q/K/V/output transfer for layers 1-69 to both directions, joint rank-512 SVD initialization for the first 560-dimensional block, exact FFN/Norm recopy, and explicit reports for skipped biases/FSMN tensors. The measured first-block SVD reconstruction errors on ModelScope `model.pt` are Q `0.04954`, K `0.04536`, V `0.02850`, versus K/V `0.11239/0.19388` when Q alone is used as the bottleneck, so only the joint-SVD mapping is admitted. Extend the online Nano teacher to return requested per-layer temporal/FFN/block tensors and train with deterministic cyclic layer sampling. Stage210a is a short fixed-data initialization and feature-parity audit with copied FFN/Norm and CTC decoder/head frozen; Stage210b aligns temporal sub-block outputs, and Stage210c adds complete block outputs only after fixed-audio layer metrics improve. Phase 2 logits alignment may start only from a passing hidden checkpoint and must retain a decaying layerwise hidden anchor. The BiRWKV/RWKV-7 architecture, 70-layer mapping, direction behavior, tokenizer, and Nano five-layer CTC output path remain unchanged.
- 2026-07-18 Stage209 implementation and launch: added `scripts/run_stage209_emission_margin_online_ctc_distill.py` plus direct-entrypoint/config tests. The runner uses four cumulative `5k/10k/15k/20k` data-batch phases, keeps the validated top-k/window objectives at `2.0/0.25/0.50`, keeps blank/mass at `0.02/0.02`, sets the local teacher-token-versus-blank target margin to `1.0`, and ramps sequence/margin weights from `0.50/0.25` to `1.00/0.50`; it does not change the 70-layer BiRWKV encoder, frontend, CTC vocabulary/head, or radius-2 alignment. Runner tests pass (`5 passed`) and Ruff, Python compilation, and diff checks pass. A two-step four-GPU smoke loaded the Stage208 final model with `missing=0 unexpected=0`; all top-k, sequence, local top1/margin, and local top-k branches matched every teacher row with zero missing, and peak reserved memory was `10.99GiB`. The Stage208 `epoch-1.pt` selected by latest metadata and the evaluated `step-2000.pt` contain 12,613 identical model tensors (`changed=0`, max absolute difference `0`). The formal run is active under `/tmp/rwkvasr_runs/sensevoice_rwkv_stage209_stage208_medium_emission_margin1_seqctc_ramp_20k_lr5e8_4x4090`; orchestrator PID `2166757` reached logged step `50` with all online-teacher branches complete and no missing rows. Training GPUs use about `21.65GiB` each with roughly `2.4GiB` free. Ten-minute process/loss/checkpoint/disk/GPU monitoring is active as PID `2167319` and writes `monitor_10min.log`.
- 2026-07-18 Stage208 final diagnosis and Stage209 plan: Stage208 completed all four admission phases to `step=2000` without missing online-teacher rows after the 8k frame-budget recovery. Fixed normalized public-200 greedy CTC metrics improved monotonically from the exact Stage206 `step-710000` initialization to Stage208 `step-2000`: LibriSpeech clean WER `36.198 -> 35.288`, LibriSpeech other WER `52.203 -> 51.534`, and AISHELL-1 CER `48.484 -> 47.462`; decoded length ratios increased from `0.7374/0.6326/0.6641` to `0.7530/0.6484/0.6797`. The gain is real but small because 2,000 data batches with gradient accumulation 4 produced only 500 optimizer updates: relative parameter L2 movement at step 2000 is only `2.63e-5` overall, `2.21e-5` in the encoder, and `1.16e-4` in the CTC head. A direct Nano/student spike diagnostic on 96 fixed public samples found that `98.27%` of 984 common-token peaks are already within `+-2` student frames, so a wider alignment window or delay model is not the first intervention. The residual is primarily missing emissions: on fixed 64-sample subsets, decode-only blank bias `-1.0` changes normalized clean/other WER to `30.606/48.554` and AISHELL CER to `44.078`, with length ratios `0.9386/0.8584/0.8991`; blank bias `-1.5` pushes English ratios to `1.0144/0.9950` and worsens English WER to `32.273/50.249`, establishing that a global bias is useful only as a diagnostic and would over-emit. Stage209 must therefore preserve the BiRWKV/RWKV-7 encoder, Nano-compatible CTC output space, top-k teacher loss, radius-2 windows, and small blank/mass calibration, while replacing the weak `0.05` local margin contribution with a ramped teacher-token-versus-blank objective using margin `1.0`. Start from Stage208 `step-2000.pt`, train the medium split for 20,000 additional data batches in four 5,000-batch DeepSpeed-resumed phases (5,000 optimizer updates total), keep LR `5e-8`, 8k frame budgets, no full-vocabulary KL, and ramp sequence/window/top-k/margin weights conservatively. Save and run online-teacher checkpoint loss every 5,000 batches, with no early stopping; public normalized WER/CER and length-ratio gates at phase checkpoints determine whether the stronger emission objective should be kept for a later full-data continuation.
- 2026-07-17 Stage208 smoke and launch status: Stage206 reached and fully exported `step-710000.pt` (`737,400,197` bytes) before shutdown; its 256-sample online-teacher checkpoint eval was `3.0945`. Added `scripts/run_stage208_stronger_online_ctc_distill.py` with four monotonic 500-batch DeepSpeed-resumed ramp phases and config tests. Targeted sequence/nonblank-window plus runner tests passed (`6 passed`) and Ruff passed. A two-step four-GPU smoke loaded the 710k checkpoint with `missing=0 unexpected=0`; online top-k, sequence, nonblank-window top-1/margin, and nonblank-window top-k all matched `12/12` teacher rows with zero missing, produced `128/146` local peak events on the first two batches, and used `12.84GiB` peak reserved memory. The formal Stage208 admission run is active under `/tmp/rwkvasr_runs/sensevoice_rwkv_stage208_stage206_latest_medium_stronger_seqctc_nonblankwin_ramp_2k_lr5e8_4x4090`, orchestrator PID `2149422`, initialized explicitly from Stage206 `step-710000.pt`; at logged step `20` all enabled teacher branches remained complete with no missing rows. A detached monitor writes process, latest loss/eval, checkpoint, disk, and GPU state every ten minutes to `monitor_10min.log` (PID file `monitor_10min.pid`). Do not promote beyond the 2k admission ramp until the fixed real public WER/CER/deletion/length gate is run.
- 2026-07-17 Stage208 1k gate and OOM recovery plan: ramp1 and ramp2 completed and exported `step-500.pt`/`step-1000.pt`; ramp3 resumed optimizer/data offset correctly and remained numerically healthy through logged step `1310`, with all teacher branches matched and no missing rows, but rank 1 OOMed during backward on the next unusually large variable-length batch (`23.31GiB` in use, only `201.62MiB` free, `224MiB` allocation requested). The unsaved ramp3 suffix is discarded and the recovery point remains the complete step 1000 DeepSpeed checkpoint. Fixed real public-200 greedy CTC evaluation against the exact Stage206 710k initialization shows a small but consistent early signal at Stage208 step 1000: LibriSpeech clean WER `36.198 -> 35.920` with deletion `26.618 -> 26.289` and unit ratio `0.7374 -> 0.7406`; LibriSpeech other WER `52.203 -> 52.173` with deletion `37.223 -> 36.737` and ratio `0.6326 -> 0.6375`; AISHELL-1 CER `48.484 -> 48.211` with deletion `33.697 -> 33.560` and ratio `0.6641 -> 0.6654`. Insertion is unchanged, so the auxiliary objective is reducing under-emission rather than causing over-emission, but the gain is not yet large enough for promotion. Resume ramp3/ramp4 from step 1000 with both batch token and length-bucket frame budgets reduced from `12000` to `8000` and expandable CUDA allocator segments enabled; preserve all loss weights, LR, optimizer state, and data offset. Re-run the fixed real public gate at step 1500/2000 before any full-data promotion. Evaluation artifacts are under `/tmp/rwkvasr_eval/stage208_public200`.
- 2026-07-17 Stage208 OOM recovery result: the runner now forces `batch_token_budget=8000`, `length_bucket_frame_budget=8000`, and `PYTORCH_ALLOC_CONF=expandable_segments:True`; targeted tests passed (`6 passed`), Ruff and `git diff --check` passed. The formal run resumed from the complete DeepSpeed step 1000 state with `epoch_batch_offset=1000`, and the loader confirmed `frame_budget=8000`. It reached logged step `1330`, safely passing the previous post-1310 OOM point; long batches were reduced as far as three samples while all enabled online teacher branches retained complete matching and zero missing rows. Across the 33 resumed ten-step log samples through step 1330, mean total loss was `5.1441`, with early/late logged-window means `5.4960/4.9350`; the last three losses were `3.8935/4.2784/4.4657`. GPU use was about `21.1-21.4GiB`, leaving `2.7-2.9GiB` free per 24GiB card. Ramp3 remains active under orchestrator PID `2157109`; the independent ten-minute monitor is PID `2157658` and writes `monitor_10min_resume.log`. Continue uninterrupted to step 1500 and ramp4 step 2000, then run the fixed public-200 Nano/baseline/Stage208 WER/CER gate before promotion.
- 2026-07-21 Stage210b completion: the stacked full-block alignment run under `/tmp/rwkvasr_runs/sensevoice_rwkv_stage210b_stage210a_step30064_stacked_fullblock_easy1490h_1ep_lr3e6_4x4090` consumed all `30,064/30,064` easy-split batches (`1,174,987` unique utterances, `1,490.119 h`) and exported `step-10000/20000/30000/30064` plus `epoch-1` checkpoints without early stopping, missing teacher rows, frame mismatch, OOM, non-finite loss, or CUDA/NCCL failure. Corrected fixed stacked losses at step `0/10k/20k/30k` were `1.592088/0.715573/0.666671/0.625598`; the final step-30064 sidecar measured `0.621446`. From step 0 to step 30064, mixer loss/cosine/RMS ratio changed from `1.304965/0.556084/0.595117` to `0.661753/0.764643/0.906133`, copied-FFN functional output from `1.698210/0.402877/0.702354` to `0.603721/0.787853/0.948773`, and residual block from `0.514655/0.834889/0.667511` to `0.121549/0.959043/0.996511`; loss and cosine improved in all `70/70` layers for all three components. Select `step-30064.pt` for logits alignment.
- 2026-07-21 Stage210b real public-200 gate and Stage210c admission: fixed-manifest normalized greedy CTC evaluation at Stage210b step 30064 measured AISHELL-1 `7.29% WER / 7.36% CER`, LibriSpeech test-clean `9.93% / 5.78%`, test-other `17.65% / 10.12%`, Common Voice 22 EN `27.88% / 16.89%`, and WenetSpeech TEST_NET `24.76% / 28.36%`. On the exact AISHELL/LibriSpeech samples with cached native Nano CTC predictions, Stage209-to-Stage210b-to-Nano primary error changed from AISHELL CER `39.97 -> 7.36 -> 3.85`, LibriSpeech-clean WER `29.70 -> 9.93 -> 7.46`, and LibriSpeech-other WER `45.73 -> 17.65 -> 13.13`. Stage210b restored collapsed-length ratios from `0.85/0.89/0.82` to approximately `1.00` and reduced deletion errors to `0.44/0.76/1.61` percentage points, so the remaining Nano gap is dominated by token substitution/distribution mismatch rather than blank collapse or temporal under-emission. Admit Stage210c `nonblank` from `step-30064.pt`: run one complete easy epoch with full-vocabulary online KL on teacher nonblank frames and immediate neighbors, small blank/mass calibration, frozen copied Nano five-layer decoder/head, and decayed stacked hidden anchors. Require a four-GPU smoke with exact checkpoint loading, zero teacher/frame mismatch, finite full-logit loss, and safe 24 GiB memory before the formal `30,064`-batch run; evaluate and save every `10,000` batches with no early stopping.
- 2026-07-21 Stage210c nonblank launch: the two-step four-GPU smoke loaded Stage210b step 30064 with `missing=0 unexpected=0`, exercised nonzero full-vocabulary KL, blank/mass calibration, and mixer/FFN/block anchors with complete teacher matching and zero frame delta, remained finite, exported its final checkpoint, and used `5.18 GiB` peak reserved memory on the short admission batch. The formal run is active under `/tmp/rwkvasr_runs/sensevoice_rwkv_stage210c_stage210b_step30064_nonblank_easy1490h_1ep_fullkl_hiddenanchor_4x4090`, W&B run `r0v2egfo`, with the exact `30,064`-batch easy-epoch contract. Its fixed 256-utterance step-0 diagnostics are total loss `1.608812`, selected-frame full KL `1.282118`, selected/all-frame top-1 agreement `0.777778/0.893967`, blank-probability MAE `0.099973`, teacher/student nonblank rates `0.179634/0.151571`, CTC token error `0.299611`, collapsed-length ratio `0.851167`, and mean frame delta `0`. The first formal batch used local batch `12`, full-KL matched `12/12`, reserved `6.89 GiB`, and completed in `2.18 s`; subsequent allocator residency was about `12 GiB` per card. Continue all data without early stopping and use only the fixed 10k sidecars plus normalized real WER/CER for curriculum decisions.
- 2026-07-23 Stage210c nonblank completion and rejection: the run consumed all `30,064/30,064` easy-split batches and exported `step-10000/20000/30000/30064` plus `epoch-1` without missing teacher rows, frame mismatch, OOM, non-finite loss, or CUDA/NCCL failure. Fixed total loss at step `0/10k/20k/30k` was `1.608812/0.860926/0.907324/0.907818`; selected-frame full KL was `1.282118/0.601291/0.617806/0.622049`, and CTC token error was `0.299611/0.272287/0.284333/0.285437`. Although student/teacher nonblank-frame ratio initially improved from `0.8438` to `0.8721`, collapsed token-length ratio worsened from `0.8512` to `0.8227` at 10k and `0.7990` at 30k, showing that the neighbor-only target broadened/merged emissions instead of preserving the complete CTC path. Real fixed-manifest public-200 evaluation confirmed failure to promote: Stage210b versus Stage210c 10k/30k measured AISHELL CER `7.36/9.64/7.73`, LibriSpeech-clean WER `9.93/10.26/9.96`, LibriSpeech-other WER `17.65/18.51/18.02`, Common Voice EN WER `27.88/28.19/28.29`, and WenetSpeech CER `28.36/24.63/24.78`. The Wenet gain is real but does not offset broad regressions. Keep Stage210b step 30064 as the incumbent and do not start the configured three-epoch Stage210c full continuation.
- 2026-07-23 Stage210d objective correction: restart from Stage210b step 30064 and replace the hard `nonblank_neighbors` frame mask with all-frame full-vocabulary KL plus an explicit configurable nonblank-frame importance weight. All teacher frames must remain in the denominator so blank timing and short inter-token gaps cannot drift; use a conservative initial nonblank multiplier of `4.0`, close to but below the fixed-set blank/nonblank balancing ratio, and retain complete full-logit projection and zero-tolerance frame alignment. Strengthen the stacked hidden anchors relative to Stage210c and lower LR before admitting a full epoch. First run a short fixed-data admission with sequence/presence/window/margin losses still disabled so the effect of weighted all-frame KL is isolated. Admit a low-weight online CTC sequence constraint only if fixed and real metrics show that all-frame weighting still lowers KL while collapsed length falls. Promotion requires no regression on normalized AISHELL, both LibriSpeech sets, and Common Voice while retaining the Wenet gain; only then may the complete easy/medium/hard/long curriculum begin.
- 2026-07-23 Stage210d implementation and smoke: add `ctc_teacher_online_full_nonblank_weight` with backward-compatible default `1.0`; weighted KL keeps every frame in the numerator and denominator, multiplies only teacher-top1 nonblank frames, and validates that the weight is finite and positive. The dedicated runner uses all-frame KL `1.0`, nonblank multiplier `4.0`, blank/mass calibration `0.02/0.02`, stacked mixer/FFN/block anchors `0.25/0.125/0.50`, LR `3e-7`, frozen Nano five-layer decoder/head, and no sequence/presence/window/margin objectives. Related runner/DeepSpeed and full-logit tests passed (`31 + 8`), Ruff and diff checks passed. A two-step four-GPU smoke loaded Stage210b step 30064 with `missing=0 unexpected=0`, logged `full_frame_filter=all` and `full_nonblank_weight=4`, matched every teacher branch with zero frame delta, remained finite, exported the final checkpoint, and reserved `5.17 GiB` at the first step. Admit a `2,000`-batch run on the easy split with fixed evaluation and checkpoints at `1,000/2,000`; do not continue to a complete epoch until normalized public metrics pass the stated no-regression gate.
- 2026-07-23 Stage210 residual-gap audit and Stage210e design: keep the active Stage210d admission unchanged through its `1k/2k` gates, but prepare a separate path-state objective rather than another masked-logit continuation. The online Nano teacher is already forced to `eval()`, every batch has exact feature/frame parity, the vocabulary and blank projection are correct, and the copied five-layer CTC decoder/head have previously matched FunASR output exactly. On the fixed Stage210b initialization, encoder layer 69 block cosine/RMS ratio are already `0.98979/1.03757`, while full-logit KL, CTC token error, and collapsed-length ratio remain `0.68030/0.29738/0.85520`; small post-encoder geometry errors are therefore being amplified by the shared five-layer CTC path. Current layer sampling supervises raw encoder branches but does not explicitly supervise the post-`tp_norm` encoder output or the hidden state after each frozen CTC decoder block. CTC KD literature also establishes that independent CTC models can disagree in spike timing, that blank-only masking discards useful alignment information, and that representation plus intermediate/output KD is stronger than frame KL alone. Stage210e must preserve all frames and the complete vocabulary while adding: (1) fixed-eval active-frame agreement, blank false-negative/false-positive rates, and token insertion/deletion/substitution counts; (2) a backward-compatible posterior-nonblank frame-weight mode, `1 + (max_weight - 1) * p_teacher(nonblank)`, which never masks blank frames; and (3) optional final encoder-output plus frozen Nano CTC-decoder input/block hidden-state alignment. Decoder-state targets and student states must be captured from the same copied modules and exact frame lengths; decoder/head parameters remain frozen so gradients update only the BiRWKV encoder. Use zero weight decay for this narrow alignment continuation because Nano's CTC training config uses no weight decay and copied FFN/Norm parameters should not drift merely from AdamW shrinkage. Do not add sequence CTC yet: admit it only if Stage210d/210e edit decomposition still shows deletion or collapsed-length regression after all-frame/path-state alignment.
- Stage210e is an ablation-gated training change, not an encoder architecture change. Start its short admission from whichever of Stage210b, Stage210d-1k, or Stage210d-2k wins the exact fixed and normalized public gates; never initialize from a checkpoint selected only by training-batch loss. Compare the all-frame hard-weight Stage210d control against posterior-weighted all-frame KL plus final/path hidden alignment on the same fixed 256 utterances and public-200 manifests. Promotion requires lower CTC token error and full KL, collapsed-length ratio moving toward `1.0`, no active-frame agreement loss, and no normalized regression on AISHELL-1, LibriSpeech clean/other, or Common Voice EN. Only a passing objective may be scaled from easy to medium/hard/long audio.
- 2026-07-23 Stage210d 1k gate: fixed-256 full KL improved from `0.68030` to `0.51912`, all-frame top-1 agreement from `0.89330` to `0.89901`, blank-probability MAE from `0.10018` to `0.09559`, student/teacher nonblank ratio from `0.85047` to `0.92430`, token error from `0.29738` to `0.25948`, and collapsed-length ratio from `0.85520` to `0.89504`. The normalized public-200 gate was mixed: AISHELL CER `7.36 -> 7.16`, LibriSpeech-clean WER `9.93 -> 9.83`, LibriSpeech-other WER `17.65 -> 17.65`, and WenetSpeech CER `28.36 -> 24.12` improved or held, but Common Voice EN WER regressed `27.88 -> 28.82`. Error decomposition identifies a hard-weight emission overshoot rather than renewed deletion collapse: on Common Voice, insertion/substitution/deletion changed from `2.72/21.97/3.19` to `3.50/22.33/2.98` percentage points; similar insertion-for-deletion exchange appears on both LibriSpeech sets. Stage210d therefore remains an uninterrupted `2k` control but fails the strict 1k promotion gate.
- 2026-07-23 direct Nano-CTC public baselines: native FunASR-Nano CTC on the exact normalized 200-utterance manifests is AISHELL CER `3.85`, LibriSpeech-clean WER `7.46`, LibriSpeech-other WER `13.13`, Common Voice EN WER `22.23`, and WenetSpeech CER `9.26`; its predicted/reference unit ratios are approximately `1.000/1.002/1.003/1.000/0.998`. The Stage210d Common Voice regression therefore moves away from both reference text and the teacher, despite lower aggregate fixed-set KL. Stage210e must use posterior rather than hard top-1 nonblank weighting and explicitly align the post-encoder and five-layer CTC-decoder path to reduce token substitutions without forcing extra spikes. Keep Stage210b as the default Stage210e initialization unless Stage210d-2k passes every public no-regression gate.
- 2026-07-23 Stage210d 2k completion and rejection: the admission consumed all `2,000/2,000` data batches, exported complete `step-1000/2000` checkpoints, and exited normally with no teacher miss, frame mismatch, OOM, or early stopping. From Stage210b to step 1k/2k, fixed full KL was `0.68030 -> 0.51912 -> 0.46571`, token error `0.29738 -> 0.25948 -> 0.25121`, and exact sequence rate `0.49219 -> 0.50391 -> 0.53125`; however, all-frame top-1 agreement peaked at 1k and returned to `0.89346`, blank MAE worsened to `0.10880`, and the nonblank-frame ratio overshot to `1.00748` while collapsed token length remained only `0.89719`. The extra active frames are therefore duplicated or broadened spikes that CTC later merges, not correct new tokens. The normalized public-200 step-2k metrics were AISHELL CER `7.26`, LibriSpeech clean/other WER `9.61/17.93`, Common Voice EN WER `28.24`, and WenetSpeech CER `22.83`; gains on clean and Wenet do not pass the no-regression gate because other and Common Voice remain worse than Stage210b. Direct student-to-Nano errors likewise remain mixed (`5.15/6.13/12.67/20.97/19.16%` on AISHELL, clean, other, Common Voice, and Wenet). Reject both Stage210d checkpoints as Stage210e initializations, retain Stage210b step 30064, and treat hard top-1 nonblank weighting as a completed control rather than a scalable objective.
- 2026-07-23 Stage210e formal launch: the path-state admission is active under `/tmp/rwkvasr_runs/sensevoice_rwkv_stage210e_stage210b_step30064_pathstate_posteriornb4_finalenc0p25_decoder0p25_admission2k_lr3e7_wd0_4x4090`, W&B run `j20cy8dd`, from the admitted Stage210b step 30064 checkpoint. Startup loaded `missing=0 unexpected=0`, froze all 86 Nano CTC-decoder/head tensors, left `336,503,904` encoder parameters trainable, and initialized four-rank DeepSpeed ZeRO-1 normally. The fixed 256-utterance step-0 baseline is total loss `1.548465`, decoder-path hidden loss `0.333550`, full KL `0.678039`, active/all-frame top-1 agreement `0.579143/0.893967`, teacher-nonblank-to-student-blank rate `0.322160`, blank-to-nonblank rate `0.036703`, token insertion/deletion/substitution rates `0.008738/0.153398/0.136893`, collapsed-length ratio `0.855340`, and exact sequence rate `0.492188`. Initial train batches capture all six decoder path states per utterance, match every teacher row with zero frame delta, and remain finite. Continue without early stopping to the fixed `1k/2k` gates; an independent ten-minute monitor records process, disk, checkpoint, GPU, and loss state.
- 2026-07-23 Stage210e 1k gate and residual diagnosis: the uninterrupted run's fixed-256 total/decoder-path/full-KL losses changed from `1.548466/0.333550/0.678039` to `1.136378/0.273809/0.519265`; active/all-frame agreement improved from `0.579143/0.893967` to `0.631285/0.898841`, token error from `0.299029` to `0.255814`, collapsed-length ratio from `0.855340` to `0.896318`, and exact sequence rate from `0.492188` to `0.511719`. The exact normalized public-200 gate measured AISHELL CER `7.02`, LibriSpeech clean/other WER `9.78/17.68`, Common Voice EN WER `28.71`, and WenetSpeech CER `21.53`, versus the Stage210b incumbent `7.36/9.93/17.65/27.88/28.36`. Direct student-to-Nano primary errors were `4.91/6.00/12.39/20.71/20.31%`. Path-state alignment is therefore real, but the `4x` posterior-nonblank weighting still exchanges deletions for insertions: relative to Stage210b, direct-to-Nano insertion/deletion changed from `1.12/1.52` to `1.73/0.88` points on LibriSpeech-other and from `1.88/2.35` to `2.51/2.20` on Common Voice. Continue Stage210e unchanged through 2k, but do not scale this weighted objective unless the 2k public gate reverses both regressions.
- Stage210f factorized-CTC plan: preserve the BiRWKV/RWKV-7 encoder and frozen copied Nano five-layer decoder/head, but stop using a scalar nonblank frame multiplier as the main balancing mechanism. CTC KD studies identify sparse/arbitrary spike timing and blank dominance as separate failure modes; factorized progressive KD reports that blank/nonblank discrimination, balanced hidden transfer, and separately normalized posterior transfer outperform conventional CTC KD, while symmetric blank selection can support label-free KD. The project has already completed the representation stages and now observes substitution-dominated residuals plus insertion/deletion exchange. Add a backward-compatible online loss that (1) matches the binary teacher blank-versus-nonblank posterior on all valid frames, and (2) on teacher-top1 nonblank frames only, removes blank and ignored ids, renormalizes teacher and student over the valid nonblank vocabulary, and minimizes full-vocabulary conditional KL. Normalize the binary and token terms independently so `82%` blank frames cannot suppress token identity and the token term cannot change the student's blank logit. Retain a low uniform all-frame KL only as a distribution stabilizer, retain final-encoder/decoder-path hidden anchors, use zero weight decay, and keep all sequence/window/margin losses off in the first isolated admission. This follows the factorized/progressive result (`https://doi.org/10.1016/j.specom.2024.103071`) and the CTC spike-alignment diagnosis (`https://arxiv.org/abs/1904.08311`) without changing inference architecture. First run a short 4-GPU smoke and a fixed/public admission from the best Stage210e path checkpoint; promote to complete easy/medium/hard/long data only if direct Nano substitution falls without increasing insertion, blank MAE/nonblank ratio remain calibrated, and normalized AISHELL/LibriSpeech/Common Voice do not regress.
- 2026-07-23 Stage210e 2k completion and gate: the uninterrupted admission consumed `2,000/2,000` batches, exported complete 1k/2k checkpoints, synchronized W&B run `j20cy8dd`, and exited normally. From 1k to 2k, fixed full KL improved `0.519265 -> 0.467458`, active top-1 agreement `0.631285 -> 0.658263`, token error `0.255814 -> 0.253152`, and exact sequence rate `0.511719 -> 0.523438`; however blank MAE worsened `0.093959 -> 0.105072` and the nonblank ratio moved from `0.917132` to a slight `1.008403` overshoot. Normalized public-200 2k metrics are AISHELL CER `6.88`, LibriSpeech clean/other WER `9.58/17.62`, Common Voice EN WER `28.45`, and WenetSpeech CER `22.65`. These improve four sets versus 1k but Wenet regresses and Common Voice remains worse than the Stage210b incumbent `27.88`, so reject the scalar-`4x` posterior weighting as a scalable objective. Direct normalized student-to-Nano errors at 2k are `4.74/6.03/12.30/20.76/18.80%`; four of five substitution rates improve versus 1k, so use Stage210e step 2000 as the Stage210f initialization while correcting its deletion/blank calibration explicitly.
- 2026-07-23 Stage210f implementation: add `ctc_teacher_online_conditional_nonblank_loss_weight` without changing the model or inference path. Its loss selects teacher-top1 pronunciation frames, excludes project blank `60515` and ignored remapped teacher blank `60514`, independently normalizes both distributions over ids `0..60513`, and therefore has exactly zero gradient on blank/ignored logits. The all-frame blank BCE now also excludes ignored ids from its nonblank log-sum-exp, fixing a prior objective leak. Fixed evaluation separately reports conditional-nonblank KL and binary blank KL. The admission recipe uses blank/full/conditional weights `0.25/0.25/1.0`, full nonblank multiplier `1.0`, encoder/decoder-path anchors `0.25/0.25`, LR `2e-7`, weight decay `0`, frozen copied Nano decoder/head, and no sequence/window/margin loss. Unit, gradient, runner, and existing CTC tests pass `56/56`; run a real four-GPU smoke before the 2k admission and keep 500-step fixed checkpoints for diagnosis.
- 2026-07-23 Stage210f four-GPU smoke: initialized from Stage210e step 2000 with `missing=0 unexpected=0`, kept all 86 copied Nano decoder/head tensors frozen, and trained the `336,503,904` encoder parameters for `2/2` batches. Every full/conditional/blank/encoder/decoder/layer teacher lookup matched, decoder and layer frame deltas stayed zero, both losses were finite, peak memory was about `7.4 GiB/GPU`, and the exported step-2 checkpoint plus fixed metrics were complete. On the deliberately tiny four-sample smoke subset, conditional-nonblank KL was `2.1143 -> 2.0691` and total eval loss `2.0782 -> 1.9924`; this verifies execution and diagnostics only, not recognition quality. Proceed to the uninterrupted 2k admission from the original Stage210e step 2000 checkpoint, not the two-step smoke checkpoint.
- 2026-07-23 Stage210f formal launch and 500-step gate: the admission is active under `/tmp/rwkvasr_runs/sensevoice_rwkv_stage210f_stage210e_step2000_factorized_blank0p25_conditional1_full0p25_pathstate_admission2k_lr2e7_wd0_4x4090`, W&B run `hybosmvk`. It loaded Stage210e step 2000 exactly, froze all 86 decoder/head tensors, and has complete online-teacher matching with zero frame delta and finite four-GPU execution. From fixed step 0 to step 500, total loss improved `1.296800 -> 1.254657`, conditional-nonblank KL `0.703658 -> 0.691553`, all-frame KL `0.462996 -> 0.458760`, blank binary KL `0.298890 -> 0.296988`, and decoder-hidden loss `0.281064 -> 0.274479`. The discrete gate has not improved yet: active-frame top-1 agreement changed `0.663246 -> 0.655462`, token error `0.244898 -> 0.254138`, insertion/deletion/substitution `0.011662/0.107872/0.125364 -> 0.015579/0.108082/0.130477`, and sequence exact rate stayed `0.523438`. Keep the process uninterrupted through `1k/1.5k/2k` because 500 updates are not enough to reject a soft posterior objective, retain every checkpoint, and run exact public/direct-Nano gates after training. The fixed loader re-extracts `funasr_wav_frontend` features with Nano's `dither=1.0`, so teacher token counts can differ by one or two frames between evaluations even though the teacher is in `eval()` and `torch.inference_mode()`; this is known input noise, not teacher dropout, and must be removed from the next admission's checkpoint comparison.
- Stage210g conditional-hard contingency: if no Stage210f checkpoint improves direct Nano substitutions and public metrics, keep the architecture, copied decoder/head, and factorized blank/token separation unchanged, but add a low-weight teacher-top1 hard token objective inside the legal nonblank vocabulary. Select only teacher-top1 pronunciation frames, remove project blank and ignored ids before the student softmax, and apply cross entropy to the teacher top-1 token in that conditional vocabulary. The loss must have exactly zero gradient on blank/ignored logits; do not reuse the historical nonblank hard loss whose full-vocabulary denominator couples token identity back to blank timing. Retain soft conditional KL for teacher uncertainty and use the hard term only to turn lower posterior KL into better active-frame argmax/substitution. Before any Stage210g run, materialize the per-rank 256-sample step-eval batches once on CPU and replay the exact same feature tensors at step 0 and every checkpoint, preserving train-time dither while making promotion metrics bitwise input-stable. Add gradient-isolation and fixed-batch replay tests, run a four-GPU smoke, and use a short admission from the best public Stage210f checkpoint; do not scale it to the three-epoch curriculum without real normalized public and direct-Nano gains.
- 2026-07-23 Stage210g implementation: add the backward-compatible `ctc_teacher_online_conditional_nonblank_hard_loss_weight` and `step_eval_cache_batches` controls. The new CE uses the projected full Nano distribution only to select teacher-active top-1 targets, normalizes student logits over legal nonblank ids, and has verified zero gradient on project blank `60515`, ignored id `60514`, and teacher-blank frames. Fixed logit diagnostics now report this conditional hard CE independently. Cached step evaluation decodes each rank's assigned subset exactly once, keeps the CPU `ASRBatch` tensors, and replays those same tensors for baseline and every checkpoint; train-time random dither remains unchanged. The Stage210g runner retains Stage210f's binary blank `0.25`, soft conditional token `1.0`, encoder/decoder/layer objectives, adds conditional hard token `0.25`, enables exact fixed-feature replay, keeps decoder/head frozen, and retains the 500-step checkpoint cadence. Set uniform all-frame full KL to `0`: the FKL decomposition identifies nonblank-token distributions on teacher blank frames as the irregular BFKL term and removes it, while NBFKL plus binary BKL retain the useful token and blank information (`https://doi.org/10.1016/j.specom.2024.103071`). This also keeps the run label-free, consistent with symmetric blank-selection KD showing that CTC supervision can be omitted (`https://arxiv.org/abs/2506.01503`). Relevant loss, gradient, replay, DeepSpeed, and runner tests pass `79/79`; Ruff and bytecode compilation pass. Do not launch it until Stage210f's 500/1k/1.5k/2k real public gates identify the initialization checkpoint.
- 2026-07-23 Stage210f completion and Stage210g initialization gate: the uninterrupted four-GPU run consumed all `2,000/2,000` batches with no teacher misses, frame mismatch, OOM, or non-finite loss. Fixed-input diagnostics improved soft losses through step 2000, but active-frame top-1 fell `0.663246 -> 0.645581`, the student/teacher nonblank ratio fell `1.001866 -> 0.926512`, and token deletion rose `0.107872 -> 0.114341`; this is objective overfitting toward blank, not evidence that the corpus is exhausted. Normalized public-200 AISHELL CER / LibriSpeech clean WER / LibriSpeech other WER / Common Voice EN WER / WenetSpeech CER are `7.05/9.48/17.84/27.82/22.62` at 500, `6.88/9.48/17.59/27.72/22.65` at 1000, `6.88/9.45/17.50/27.56/22.53` at 1500, and `7.09/9.68/17.38/28.24/22.77` at 2000. Direct normalized student-to-Nano errors at step 1500 are `4.84/5.83/12.24/19.98/18.50%`; step 2000 only improves LibriSpeech-other slightly while deletion rises on all five sets and output length shrinks, reaching Common Voice deletion `4.97%` and WenetSpeech deletion `7.98%`. Select Stage210f step 1500 as the balanced Stage210g initialization, remove the all-frame KL that rewards teacher-blank token distributions, and require the Stage210g fixed gate to lower active-token hard CE and substitution without further deletion/nonblank-ratio drift before scaling.
- 2026-07-23 Stage210g completion and source-exposure diagnosis: the factorized conditional-hard admission consumed `2,000/2,000` online-teacher batches without misses, frame mismatch, OOM, non-finite loss, or early stopping; W&B run `nm9tcyp6` synchronized normally. Exact cached-256 metrics from initialization to step 2000 changed as follows: total loss `1.395457 -> 1.337683`, conditional soft KL `0.681100 -> 0.655263`, conditional hard CE `0.983198 -> 0.946717`, active top-1 `0.648372 -> 0.653953`, token error `0.246363 -> 0.235693`, insertion/deletion/substitution `0.012609/0.112512/0.121242 -> 0.010669/0.099903/0.125121`, collapsed-length ratio `0.900097 -> 0.910766`, decoder hidden loss `0.267951 -> 0.257825`, and weighted layer loss `0.326051 -> 0.325307`. The new factorization fixes the Stage210f blank/deletion drift, but substitution remains the limiting component. Public-200 AISHELL CER / LibriSpeech clean WER / LibriSpeech other WER / Common Voice EN WER / WenetSpeech CER are `6.81/9.50/17.59/27.67/22.50` at 500, `6.64/9.45/17.50/27.67/22.47` at 1000, `6.64/9.33/17.38/27.88/22.35` at 1500, and `6.85/9.43/17.38/27.93/21.95` at 2000. Direct normalized student-to-Nano errors are `4.81/5.85/12.15/19.98/18.50`, `4.53/5.83/11.88/19.72/18.59`, `4.57/5.78/12.00/19.82/18.41`, and `4.77/5.80/12.09/19.87/17.93%`; step 1000 is the balanced initialization for the next isolated admission, while later checkpoints increasingly specialize toward Chinese/Wenet data.
- Stage210h source-balanced external-bucket plan: do not attribute the Stage210g domain tradeoff to model capacity or add another loss family before fixing observed data exposure. Replaying the exact external bucket schedule shows that 500/1000/1500/2000 steps consumed only `21.7/46.5/73.6/101.7h`, and the 2000-step sample mix was `63.21% cv22_en`, `28.03% cv22_zh`, `6.89% AISHELL3`, `0.77% LibriSpeech`, `0.66% CommonVoice CN`, and `0.43% commonvoice_en`. This is not representative of the complete easy pool, which contains `944,383` commonvoice_en rows: only `340` such rows were exposed. The external bucket builder currently writes source-contiguous rows into mixed 100k parts, while `_SourceInterleavedBucketEntryStream` infers a part label from only its first/last shard; `bucket_source_interleave` therefore cannot interleave sources inside those parts. Extend the bucket manifest backward-compatibly with an explicit per-part `source_label`, add an optional builder source field that writes source-specific parts within each length bucket, and make the loader prefer the explicit label. Build the corrected easy manifest under `/tmp` because USBHD is nearly full, verify the first 500/1000/2000 batches are source/language balanced with no duplicate or omitted rows, then run a four-GPU smoke and an isolated 2k Stage210h continuation from Stage210g step 1000 with the Stage210g losses unchanged. This isolates data order from loss changes. Only a real public/direct-Nano improvement may promote the source-balanced pipeline to complete easy and then medium/hard/long data; a later token-ranking loss change remains contingent on substitution after this data fix.
- 2026-07-23 Stage210h source-bucket implementation and replay gate: the Rust external bucket builder now accepts `--source-field`, captures dynamic JSON fields, creates collision-free source-specific part paths inside each length bucket, writes explicit per-part `source_label`, and merges source writers back into one logical bucket. The Python manifest reader prefers that explicit label while retaining legacy first/last-shard inference for old manifests. The rebuilt `/tmp/rwkvasr_data/stage210h_easy_source_grouped_buckets/manifest.json` contains exactly `1,174,987` rows across 28 buckets and six sources, matching every original source count. A training-equivalent epoch-1 schedule replay (`seed=42`, global four-GPU batches, frame budget `8000`) exposes `78,148` unique rows / `98.664h` in 2000 steps with zero duplicates: AISHELL3 / CommonVoice CN / commonvoice_en / CV22 EN / CV22 ZH / LibriSpeech contribute `17.47/17.30/17.51/17.52/11.95/18.25%` of samples. Full-epoch replay consumes `1,174,820/1,174,987` unique rows and `1,489.878h`; the 167 unconsumed rows are only the expected per-bucket `drop_last` tails. Stage210h must initialize from Stage210g step 1000 and retain the exact Stage210g factorized losses, LR, frozen Nano decoder/head, and fixed-eval protocol; only the corrected manifest and `bucket_source_interleave=true` differ.
- 2026-07-23 Stage210h launch gate: Rust tests pass `3/3`, WebDataset and Stage210g/210h runner tests pass `30/30`, Ruff and bytecode compilation pass, and source replay reports zero duplicate identities. The four-GPU two-step smoke loaded Stage210g step 1000 with `missing=0 / unexpected=0`, froze the same 86 Nano decoder/head tensors, completed every enabled online teacher objective with `missing=0`, kept decoder/layer frame delta at zero, reserved at most `5.55 GiB` per GPU, and changed its fixed four-sample loss from `1.7898` to `1.7528`. The formal 2000-step run is active under `/tmp/rwkvasr_runs/sensevoice_rwkv_stage210h_stage210g1000_sourcebalanced_easy_factorized_hard_admission2k_lr2e7_wd0_4x4090`, W&B run `cuu7es0d`, with fixed 256-sample baseline loss `0.9773`, checkpoints/evaluation every 500 steps, no early stopping, and a ten-minute process/GPU/disk/loss monitor.
- 2026-07-23 Stage210h completion and evaluation audit: the source-balanced admission consumed all `2,000/2,000` online-teacher batches and exited normally, with no teacher miss, frame mismatch, OOM, non-finite loss, or early stop. Fixed-input total loss improved `0.9773 -> 0.9631`, conditional soft KL `0.5832 -> 0.5752`, binary blank loss `0.1197 -> 0.1175`, and decoder-hidden loss `0.1694 -> 0.1659`; active-frame agreement and collapsed-token error remained noisy (`0.6673 -> 0.6613` and `0.2548 -> 0.2590`). On the full public manifests' first 200 rows, step 500 already improved Stage210g step 1000 from AISHELL CER / LibriSpeech clean WER / LibriSpeech other WER / Common Voice EN WER / WenetSpeech CER `6.64/9.45/17.50/27.67/22.47` to `4.64/9.37/17.32/26.99/19.12`; later checkpoints did not improve every set simultaneously. This confirms that corrected source exposure matters, but the soft objective still does not reliably convert lower representation loss into better discrete CTC paths. A first direct-Nano summary was invalid: the student predictions used the first 200 rows from the full manifests while the cached Nano predictions used the fixed seeded `manifests_200_seed20260620`, leaving only four matching AISHELL ids. Re-evaluate all candidates on that exact seeded sample set and explicitly read the Nano cache's `pred_text/pred_token_ids` fields before selecting the initialization checkpoint.
- Stage210i blank/nonblank-balanced representation plan: preserve the BiRWKV/RWKV-7 architecture, copied and frozen Nano five-layer CTC decoder/head, source-balanced corpus, optimizer, and factorized conditional token objectives. The sampled 70-layer mixer/FFN/block losses, decoder-path hidden losses, and binary blank loss currently average uniformly over time; the final encoder anchor is restricted to teacher nonblank frames plus radius-1 neighbors but does not balance those groups. Because Nano predicts blank on roughly `92%` of all frames, the all-frame terms can decrease while pronunciation-frame representations and CTC boundaries receive little gradient. Add one backward-compatible `all | teacher_top1_balanced` frame reduction mode. In balanced mode, derive the pronunciation mask from the projected Nano CTC top-1 path after excluding project blank and ignored/non-pronunciation ids, compute blank and pronunciation means independently over the selected frames, and average the two non-empty groups equally. Apply the mode consistently to final encoder hidden, sampled block components, all copied CTC decoder states, and binary blank calibration; leave conditional soft/hard token losses unchanged because they already normalize only over teacher-active frames. Verify exact legacy behavior by default, equal group contribution under severe blank imbalance, ignored-id handling, gradients on both groups, and online records with exact frame alignment. Run an isolated four-GPU smoke and 2k admission from the best same-sample Stage210h checkpoint at unchanged LR `2e-7`; promote only if direct normalized Nano token error/substitution and public CER/WER improve without insertion/deletion or nonblank-ratio drift.
- 2026-07-23 Stage210h same-sample selection: all four checkpoints were re-decoded on the exact fixed-seed 200-utterance manifests used by the native Nano CTC cache, and the comparison loader was hardened to accept the native evaluator's `pred_text/pred_token_ids` schema. Step 2000 has the best combined public/direct tradeoff. Its normalized public AISHELL CER / LibriSpeech clean WER / LibriSpeech other WER / Common Voice EN WER / WenetSpeech CER are `6.68/9.38/17.14/27.62/22.29`, versus native Nano `3.85/7.46/13.13/22.23/9.26`. Direct normalized student-to-Nano CER/WER is `4.67/5.67/12.03/19.87/18.32%` in the same order. The direct token-length ratio is near one on the first four sets but only `0.8498` on Wenet, making deletion/blank calibration the largest remaining domain failure. Select Stage210h step 2000 as the Stage210i initialization; do not treat the earlier first-200-row metrics as comparable evidence.
- 2026-07-23 Stage210i implementation and smoke: add the backward-compatible `ctc_teacher_online_frame_balance_mode`, defaulting to `all`. `teacher_top1_balanced` derives the active mask from Nano's projected top-1 CTC path, excludes blank and ignored id `60514`, accumulates active and blank frame means independently without per-layer host synchronization, and averages the non-empty groups equally. The mode is wired into online binary blank BCE, final encoder hidden loss, sampled mixer/FFN/block hidden loss, and all copied Nano CTC-decoder states; conditional token losses remain active-only and unchanged. The direct Nano summarizer now falls back to native evaluator `pred_text/pred_token_ids`, and public evaluation accepts an explicit unsharded `CTC_LIMIT` for diagnostic subsets. Relevant legacy and new tests pass (`90 passed, 2 skipped` plus focused balanced-loss and evaluator tests), Ruff, bytecode compilation, shell syntax, and diff checks pass. The four-GPU two-step smoke loaded Stage210h step 2000 with `missing=0/unexpected=0`, froze all 86 decoder/head tensors, matched every online objective with zero encoder/decoder/layer frame delta, remained finite, and reserved at most `5.55 GiB/GPU`; fixed four-sample loss was `1.8572 -> 1.8650` and token error stayed `0.50`, which is execution evidence only. Proceed to the uninterrupted 2k admission at unchanged LR `2e-7`, save/evaluate every 500 steps, and retain every gate for exact same-sample public/direct comparison.
- 2026-07-23 Stage210i completion and rejection: the uninterrupted four-GPU admission consumed all `2,000/2,000` source-balanced easy batches and exited normally with no teacher miss, frame mismatch, OOM, non-finite loss, or early stop. On the exact cached 256-utterance input, token error improved `25.316 -> 24.895%`, deletion `9.705 -> 8.228%`, collapsed-length ratio `0.9135 -> 0.9325`, active-frame agreement `0.670 -> 0.694`, and exact sequence rate `67.97 -> 69.53%`; however insertion rose `1.055 -> 1.477%`, substitution rose `14.557 -> 15.190%`, student/teacher nonblank ratio overshot `0.954 -> 1.018`, and blank MAE worsened `0.03925 -> 0.04141`. The sampled mixer/FFN/block means and decoder-hidden loss were effectively flat, so the rare-frame representation intent did not receive enough useful optimization at eight sampled layers and LR `2e-7`. Exact seeded public AISHELL CER / LibriSpeech clean WER / LibriSpeech other WER / Common Voice EN WER / WenetSpeech CER changed from Stage210h `6.68/9.38/17.14/27.62/22.29` to `6.24/9.38/17.84/28.29/22.23`. Direct normalized student-to-Nano primary errors changed `4.67/5.67/12.03/19.87/18.32` to `4.23/5.67/12.39/19.77/18.26%`. Equal blank/nonblank reduction improves under-emission and clean Chinese but shifts the true frame prior toward excess nonblank emissions and fails the broad no-regression gate. Do not continue Stage210i or use it as the next initialization; retain Stage210h step 2000.
- Stage210j decoupled hidden-alignment plan: preserve the BiRWKV/RWKV-7 encoder, Nano-initialized/copied FFN and norms, frozen Nano five-layer CTC decoder/head, exact shared `funasr_wav_frontend` features, tokenizer/blank projection, source-balanced easy manifest, and factorized conditional token objectives. Split the historical shared frame-balance setting into backward-compatible blank-loss and hidden-loss controls. Stage210j uses `teacher_top1_balanced` only for final encoder, sampled mixer/FFN/block, and CTC-decoder hidden states; binary blank calibration must return to the true all-frame mean so rare-frame representation emphasis cannot alter the teacher blank prior. The configured boundary layer ids are currently only logged: the training selector calls `_select_layer_hidden_ids` without `include_boundaries=True`, while fixed diagnostics identify layers `49/50` as the worst block boundary. Add an explicit default-off boundary-inclusion control, test exact selection/coverage, and enable it for Stage210j. Increase sampled hidden coverage from `8` to the already memory-admitted `24` layers per batch; with four required boundaries this gives 20 cyclic layers plus `0/49/50/69`, rather than only about 228 updates per ordinary layer in a 2k admission. Double final encoder and decoder-hidden objective weights from `0.25` to `0.50`, retain the existing layer component weights and LR `2e-7`, and make no sequence/window or vocabulary change in this isolation. Run a four-GPU two-step smoke and an uninterrupted 2k admission from Stage210h step 2000 with fixed evaluation/checkpoints every 500 steps. Promotion requires lower direct Nano token error and substitution on all English and Chinese gates without the Stage210i insertion/nonblank overshoot; only a passing checkpoint may move to the source-balanced medium pool, which contains `9,722.895h` WenetSpeech and `47,771.242h` total audio.
- 2026-07-23 Stage210j implementation and smoke: add optional `ctc_teacher_online_blank_frame_balance_mode` and `ctc_teacher_online_hidden_frame_balance_mode`; when absent they resolve to the legacy shared mode, preserving every existing config. Blank BCE now uses the resolved blank mode while final encoder, all sampled encoder components, and all six CTC-decoder path states use the resolved hidden mode in both train and fixed-eval paths. Add the default-off `ctc_teacher_online_layer_include_boundaries` and pass it into the training selector; fixed evaluation remains uniformly sampled. CLI/YAML resolution, fallback/override behavior, 24-layer cyclic coverage, runner configuration, and legacy Stage210i behavior are covered by tests. Relevant DeepSpeed/CTC/Stage210 runner tests pass `91/91`; focused new/legacy tests pass, and Ruff, bytecode compilation, dry-run config generation, and diff checks pass. The four-GPU two-step smoke loaded Stage210h step 2000 with `missing=0/unexpected=0`, froze the same 86 decoder/head tensors, logged blank/hidden modes `all/teacher_top1_balanced`, selected exactly 24 layers per step with `0/49/50/69` present, matched every teacher branch with zero frame delta, stayed finite, reserved at most `5.60 GiB/GPU`, exported step 2, and exited normally. Proceed to the complete 2k admission without early stopping.
- 2026-07-23 Stage210j step-500 gate and parameter-motion audit: the uninterrupted four-GPU admission reached the first fixed 256-utterance gate with zero teacher misses/frame deltas and no non-finite or memory error. Relative to its exact cached step-0 baseline, token error changed `26.216 -> 25.793%`, insertion/deletion `1.268/9.302 -> 1.057/9.091%`, and decoder-hidden loss `0.16327 -> 0.16164`; substitution and collapsed-length ratio were unchanged at `15.645%` and `0.91966`. This is not yet representation convergence: active-frame agreement fell `0.66004 -> 0.65606`, full/blank KL worsened `0.18397/0.11892 -> 0.18529/0.12036`, and mean mixer/FFN/block losses were effectively unchanged (`0.50143/0.38022/0.07214 -> 0.50151/0.38022/0.07218`), including boundary layers `49/50`. A direct checkpoint audit against Stage210h step 2000 found only `2.76e-5` relative movement in TimeMixer projection weights and `1.04e-5` across the full encoder after 500 updates; copied Nano FFN/Norm tensors remain healthy at about `1.10%` relative L2 drift from the original Nano values and did not materially move in Stage210j. Therefore the present hidden plateau is primarily an effective-step-size/optimization issue, not failed FFN copying or evidence that more low-LR data alone will align hidden states. Keep Stage210j uninterrupted through all 2,000 steps as the required control. If later fixed gates confirm flat hidden metrics, the next isolated admission must increase TimeMixer-effective LR and separate a hidden-only update phase from factorized logits alignment; do not scale the unchanged `2e-7` joint recipe to the medium pool.
- 2026-07-23 Stage210j layer-sampling correction: `_ctc_teacher_layer_hidden_loss` reduces each component over all selected layer/frame events before applying its component weight. Raising the sampled layer count from `8` to `24` therefore raises an ordinary layer's selection probability by `3x` while reducing its contribution on a selected step by approximately `3x`; over a complete cyclic schedule its expected raw per-layer gradient mass is essentially unchanged. AdamW still gives each layer more non-empty update events, so the denser sampler can reduce variance and improve optimizer-state coverage, while explicitly included boundaries receive extra exposure. It does not provide the assumed `3x` raw gradient strength, however, and increases measured step time to roughly `3.5 s`. For the next scalable hidden phase, return to a smaller cyclic sample, deliberately oversample the empirically weak `10-29` band plus structural boundaries, and increase actual TimeMixer step size rather than paying for dense layer capture. Any change from mean to summed layer reduction must be explicit and tested because it would also change objective scale with sample count.
- 2026-07-23 Stage210j step-1000 gate: on the same cached 256 utterances, total loss reached `1.02075`, but token error remained exactly at the step-500 value `25.793%` and substitution remained exactly at the step-0 value `15.645%`. Deletion improved `9.302 -> 8.668%` while insertion worsened `1.268 -> 1.480%`, collapsed-length ratio moved `0.91966 -> 0.92812`, and the nonblank-rate ratio returned to its baseline `0.95229`; this is another insertion/deletion exchange rather than token-identity learning. Conditional soft/hard token losses decreased `0.58522/0.92769 -> 0.58183/0.92134`, but active-frame agreement stayed below baseline (`0.66004 -> 0.65606`) and full/blank KL worsened. Representation metrics confirm no useful hidden update: mixer loss/cosine changed `0.501431/0.829662 -> 0.501608/0.829615`, FFN loss `0.380222 -> 0.380121`, block loss `0.072138 -> 0.072176`, and boundary losses `L49/L50` were effectively unchanged. Complete the 2k control, but require the next hidden-stage admission to show a material fixed-set mixer/block reduction before any public checkpoint promotion or medium-data expansion.
- 2026-07-23 Stage210j step-1500 gate: fixed total loss rebounded from `1.02075` at step 1000 to `1.02158`. Relative to step 0, token error changed `26.216 -> 25.581%`, but substitution moved by only one token (`15.645 -> 15.433%` over 473 teacher tokens), insertion stayed elevated at `1.480%`, and collapsed length stayed `0.92812`. This is not a representation gain: mean mixer loss worsened monotonically `0.501431 -> 0.501766`, mixer cosine fell, FFN/block and layers `49/50` remained flat-to-worse, while full/blank KL worsened `0.18397/0.11892 -> 0.18707/0.12231`. Finish the required 2k control without interruption, reject the unchanged low-LR joint recipe for larger data, and use Stage210h step 2000 as the default next initialization unless the final real public gate provides contrary evidence.
- 2026-07-23 Stage210j completion: the control consumed all `2,000/2,000` source-balanced batches and exited normally with complete teacher matching, zero frame delta, finite execution, and complete 500-step checkpoints. Final fixed token error returned to `25.793%`, substitution returned to the step-0 value `15.645%`, and the best one-token substitution fluctuation remains step 1500 rather than a converged trend. Decoder-hidden and conditional token losses decreased, but mixer/FFN/block loss worsened `0.501431/0.380222/0.072138 -> 0.502022/0.380548/0.072357`; layers `49/50` also failed to improve. Relative to Stage210h step 2000, the final encoder moved only `3.70e-5`, TimeMixer projections `9.25e-5`, and copied FFN `9.53e-6`, while the frozen CTC decoder/head remained bit-identical. Reject Stage210j for scaling. Run the exact seeded public/direct-Nano gate only on its fixed-best step 1500 as a final sanity check, retain Stage210h step 2000 as the default Stage210k initialization, and do not spend public-eval time on the inferior step 2000.
- 2026-07-23 Stage210j real gate and rejection: on the exact seeded public-200 sets, step 1500 measured AISHELL CER / LibriSpeech clean WER / LibriSpeech other WER / Common Voice EN WER / WenetSpeech CER `6.47/9.40/17.59/27.93/22.17`, versus Stage210h `6.68/9.38/17.14/27.62/22.29`. Direct normalized student-to-Nano primary errors were `4.46/5.70/12.12/19.98/18.14%`, versus Stage210h `4.67/5.67/12.03/19.87/18.32%`. Token-path decomposition shows the mechanism: AISHELL and Wenet deletion/substitution improve slightly, but all three English sets trade deletion for more insertion and substitution, including Common Voice `I/D/S=1.90/4.12/13.13%` versus Stage210h `1.37/5.21/12.51%`. This fails the bilingual no-regression gate and confirms the tiny fixed-set gain was not general representation alignment. Select Stage210h step 2000 for Stage210k and do not retain any Stage210j checkpoint as an initialization candidate.
- 2026-07-23 direct token-path residual audit: on the exact seeded public-200 predictions, Stage210h step 2000 versus native Nano has token-id insertion/deletion/substitution rates of `0.03/0.17/4.40%` on AISHELL, `0.79/2.19/2.70%` on LibriSpeech clean, `1.17/2.64/7.49%` on LibriSpeech other, `1.37/5.21/12.51%` on Common Voice EN, and `0.25/7.60/9.71%` on WenetSpeech. The residual is substitution-dominant on every set except clean, with a second deletion problem on Common Voice and Wenet. `summarize_nano_ctc_imitation.py` version 2 now emits these token I/D/S counts and normalized rates overall and by source; it reuses the same optimal-path backtrace as fixed training evaluation, and targeted evaluator/DeepSpeed tests pass `33/33`. This supports repairing encoder/token geometry before another output phase. After a hidden-stage checkpoint passes, the output-stage gate should ablate a low-weight CTC loss against Nano's collapsed greedy token sequence: unlike exact-frame KL it is invariant to harmless CTC spike shifts, while factorized blank and conditional-token terms retain frame calibration. Do not mix this sequence objective into the TimeMixer-only isolation stage.
- 2026-07-23 local-versus-stacked hidden audit: evaluate Stage210b step 30064 and Stage210h step 2000 with the same current code, exact Nano features, 256-utterance fixed split, eight uniformly sampled layers, and no parameter update. In teacher-forced operator-local mode, Stage210h total/mixer loss is `0.59646/0.56091`, slightly worse than Stage210b `0.59222/0.55692`; copied-FFN functional loss remains only `0.05151`, cosine `0.98254`, and mean RMS ratio `0.99917`, ruling out FFN weights as the bottleneck. Under the complete student stack, Stage210h mixer/FFN/block loss is `0.64357/0.54500/0.11042`, better than Stage210b `0.66132/0.60322/0.12129`, so later logits stages improved the propagated trajectory while slightly degrading individual TimeMixer operators. The `10-19` local mixer band remains worst near `0.918`. This establishes real room for a local TimeMixer repair, but its public/stacked gate is mandatory because a local-only update can disturb the globally improved trajectory. Stage210k must be followed by a separately gated stacked phase rather than promoted directly to logits from local loss alone.
- Stage210k TimeMixer-local realignment plan: prepare this admission while Stage210j completes, but do not select its initialization until the complete Stage210j fixed/public gate. Return to the already validated operator-local phase instead of adding another joint loss: use source-balanced easy audio, exact shared Nano features, teacher-forced per-layer inputs, mixer/FFN-functional weights `1.0/0.25`, no block/logit/blank/final-encoder/decoder-hidden/sequence objective, and freeze every encoder parameter except BiRWKV TimeMixer plus the first-layer input projection. Keep copied Nano FFN/Norm and the five-layer CTC decoder/head frozen, use uniform eight-layer cyclic sampling without always-on boundaries, all-frame hidden reduction, zero weight decay, and LR `3e-6` (`15x` Stage210j but below the original successful local phase's `1e-5`). Run a two-step four-GPU smoke, then an uninterrupted 1,000-step admission with exact cached 256-utterance fixed evaluation/checkpoints every 250 steps. This is an optimization-isolation stage, not a model change. Promotion requires zero misses/frame deltas, finite execution, all 70 layers covered, at least a material fixed mixer-loss/cosine improvement by 500/1000 steps, and no catastrophic same-sample public/direct-Nano regression. If the local operator is already saturated and does not move at `3e-6`, skip further local repetition and proceed to a separately gated stacked-hidden continuation; do not compensate by increasing sampled-layer count.
- 2026-07-23 Stage210k implementation and smoke: add a dedicated runner that requires an explicitly selected initialization checkpoint, inherits the corrected source-balanced easy manifest, freezes the full encoder except TimeMixer and the first input projection, freezes all Nano decoder/head tensors, and zeros every non-layer objective. Formal gates are `250/500/750/1000` with cached fixed features. Runner, evaluator, and DeepSpeed tests pass, Ruff/bytecode/diff checks pass, and the real four-GPU two-step smoke loaded Stage210h step 2000 with `missing=0/unexpected=0`. It froze `650` tensors, left exactly `189,378,560` TimeMixer/input-projection parameters trainable, selected eight cyclic layers per step, matched every teacher row with zero layer frame delta, kept all logits/blank/sequence/final-path losses exactly zero, reserved at most `4.26 GiB/GPU`, exported step 2, and exited normally. Launch the isolated 1k admission from the original Stage210h checkpoint, not the smoke export.
- 2026-07-23 Stage210k completion and distribution-shift rejection: the four-GPU admission consumed all `1,000/1,000` batches and exited normally with complete teacher matching, zero frame delta, finite execution, and complete `250/500/750/1000` checkpoints. On the exact cached 256-utterance teacher-forced audit, total hidden loss improved `0.445313 -> 0.411128` (`-7.68%`), TimeMixer loss/energy mismatch/cosine/RMS ratio improved `0.420443/0.339477/0.855781/0.855023 -> 0.388145/0.306276/0.865007/0.908179`, and every ten-layer band improved. This local gain did not survive the real student state distribution. Complete-stack total loss for Stage210h versus Stage210k `250/500/1000` is `0.586571/0.590250/0.597056/0.610033`; stacked FFN loss worsens `0.545588 -> 0.562517 -> 0.574890 -> 0.596040` and block loss worsens `0.109847 -> 0.111858 -> 0.114711 -> 0.119658`. The best available Stage210k checkpoint at step 250 also regresses every seeded public set: AISHELL CER / LibriSpeech clean WER / LibriSpeech other WER / Common Voice EN WER / WenetSpeech CER become `7.33/9.73/17.41/27.82/23.04`, versus Stage210h `6.68/9.38/17.14/27.62/22.29`. Direct Nano token error likewise worsens on all five sets to `5.39/6.15/11.41/19.29/18.44%`. Reject every Stage210k checkpoint and retain Stage210h step 2000. The failure is not insufficient optimizer movement: teacher-forced optimization improves each isolated operator while moving it away from the inputs produced by preceding student blocks; the frozen copied FFN then amplifies that state-distribution shift through the 70-layer stack. Shared RWKV `v_first` also gives layer 0 gradient exposure on every local update, with step-250 relative parameter movement `1.30e-3` versus roughly `0.4-0.8e-3` for most later layers. Keep teacher-forced metrics as diagnostics only and do not run another local-only continuation.
- Stage210l stacked-hidden repair plan: restart from the admitted Stage210h step 2000 checkpoint, never from Stage210k. Preserve the BiRWKV/RWKV-7 architecture, source-balanced easy audio, exact online Nano features, copied Nano five-layer CTC decoder/head, tokenizer, blank mapping, and all-frame hidden reduction. Run the complete student encoder stack and train only BiRWKV TimeMixer plus the first-layer input projection; freeze copied FFN/Norm and the complete CTC decoder/head so the already compatible non-temporal path cannot drift. Use stacked mixer/FFN-functional/block weights `0.5/0.25/1.0`, final post-`tp_norm` encoder hidden weight `0.5`, and frozen CTC-decoder path hidden weight `0.5`; keep every final-logit, blank, conditional-token, sequence, and window objective at zero. Retain uniform eight-layer cyclic sampling, zero weight decay, LR `3e-6`, exact fixed 256-utterance cached evaluation, and checkpoints every `250` steps for a `1,000`-step admission. This directly optimizes the distribution consumed at inference while retaining intermediate and task-path supervision. Promotion requires stacked total, block, final-encoder, and decoder-hidden losses to improve without teacher misses/frame mismatch, and the exact seeded public/direct-Nano gate must not regress. Only a passing stacked checkpoint may enter final-logit alignment; otherwise diagnose gradient allocation and add difficulty-aware layer sampling before increasing data volume.
- 2026-07-23 Stage210l implementation and smoke: add a dedicated stacked-hidden runner that inherits the corrected source-balanced schedule but zeros every final-logit/blank/conditional/sequence/window objective. Runner/config tests pass `6/6`, Ruff and bytecode compilation pass. The real four-GPU two-step smoke loaded Stage210h step 2000 with `missing=0/unexpected=0`, froze `650` tensors, and left exactly `189,378,560` TimeMixer/input-projection parameters trainable. Online final-encoder hidden, all six frozen CTC-decoder path states, and sampled mixer/FFN/block states matched every row with zero frame delta; all output-distribution losses remained exactly zero. Peak reserved/allocated memory was `4.96/4.56 GiB` per GPU, step 2 exported successfully, and the process exited normally. Launch the uninterrupted 1k admission from Stage210h, not the smoke checkpoint, and retain all four 250-step gates.
- 2026-07-23 Stage210l step-250 fixed gate: on the exact cached 256-utterance split, complete objective loss improves `0.531341 -> 0.502796` (`-5.37%`). The weighted stacked-layer term improves `0.413692 -> 0.402438`; its mixer/FFN/block components improve `0.491861/0.381096/0.072487 -> 0.482256/0.366842/0.069600` (`-1.95%/-3.74%/-3.98%`). Because the fixed objective is the sum of the weighted layer term plus `0.5` times each terminal term, its decomposition gives final encoder hidden `0.070186 -> 0.065653` (`-6.46%`) and frozen six-state CTC-decoder hidden `0.165112 -> 0.135063` (`-18.20%`). Every ten-layer band improves for mixer, FFN, and block; the hardest mixer band remains layers `10-19` at `0.827847`, while layers `60-69` show the largest mixer gain at `-5.86%`. The trainable TimeMixer/input-projection tensors moved by global relative L2 `4.93e-4`, so this is not another ineffective-step-size result. Movement is depth-skewed: bands `0-49` are approximately `3.5e-4` to `4.4e-4`, versus `6.64e-4/8.14e-4` for `50-59/60-69`, consistent with stronger terminal-path gradients and the larger late-layer gains. If later gates retain this pattern, a subsequent hidden continuation should oversample the difficult `10-29` band rather than globally raising LR. Teacher matches are complete, frame delta remains zero, and no OOM/non-finite event occurred. This passes the first stacked-hidden admission gate; continue the same uninterrupted run through steps `500/750/1000` before selecting a checkpoint or enabling logits losses.
- 2026-07-23 Stage210l step-500 fixed gate: complete fixed loss reaches `0.498094`, or `-6.26%` from baseline and another `-0.94%` from step 250. Weighted stacked-layer loss is `0.399615`; mixer/FFN/block are `0.480149/0.362818/0.068836`, corresponding to baseline gains of `-2.38%/-4.80%/-5.04%`. Frozen CTC-decoder hidden continues improving to `0.131395` (`-20.42%` from baseline, `-2.72%` since step 250), while derived final encoder hidden is nearly flat after the first gate at `0.065563` (`-6.59%` from baseline but only `-0.14%` since step 250). Every ten-layer band still improves from both baseline and step 250, so the process is healthy and must continue through 1000; however, the slowing terminal-encoder gain means a same-recipe long continuation is not yet justified. After the complete fixed/public gate, use the passing hidden checkpoint for a separately isolated output phase that retains hidden anchors and adds a low-weight CTC loss against Nano's collapsed greedy sequence plus factorized blank/conditional-token losses. The sequence term is invariant to harmless CTC spike shifts; do not return to the rejected neighbor-only or uniform all-frame KL recipes.
- 2026-07-23 Stage210l step-750 fixed gate: hidden-only optimization has reached its useful plateau. Relative to step 500, total loss changes `0.498094 -> 0.498168` (`+0.015%`), derived final encoder hidden `0.065563 -> 0.065764` (`+0.31%`), mixer `0.480149 -> 0.480451` (`+0.063%`), frozen decoder hidden `0.131395 -> 0.131378` (`-0.013%`), FFN `0.362818 -> 0.362458` (`-0.10%`), and block `0.068836 -> 0.068757` (`-0.12%`). Several late-layer mixer/block bands now regress slightly while the difficult `10-29` bands still make small gains, so the aggregate is trading errors across depth rather than converging further. Complete the promised 1000 steps without interruption, but retain step 500 as the provisional fixed-best checkpoint. Unless step 1000 produces a new material all-component gain, evaluate step 500 first on the exact seeded public/direct-Nano gates and do not scale this unchanged hidden-only objective to more data.
- 2026-07-23 Stage210l completion and fixed selection: the formal run consumed all `1,000/1,000` batches and exited normally after exporting all four checkpoints and fixed sidecars; W&B run `9hbljrd1` synchronized successfully. Step 1000 breaks the transient step-750 plateau and is the fixed-best checkpoint: total loss is `0.493612` (`-7.10%` from baseline), weighted layer loss `0.397204` (`-3.99%`), derived final encoder hidden `0.065105` (`-7.24%`), and frozen decoder hidden `0.127710` (`-22.65%`). Mixer/FFN/block losses reach `0.478182/0.359732/0.068181`, gains of `-2.78%/-5.61%/-5.94%`; mixer cosine improves `0.824580 -> 0.829357`. All seven mixer depth bands improve from baseline, including the difficult `10-19` band `0.834750 -> 0.821245`. There were no teacher misses, frame deltas, OOMs, non-finite losses, or CUDA/NCCL failures. Select step 1000 for the exact seeded public/direct-Nano gate, retaining step 500 only as a lower-movement fallback if the real gate exposes late overfit. Do not infer output-token improvement from hidden metrics alone.
- 2026-07-23 Stage210l real gate and objective-gap diagnosis: neither hidden checkpoint passes the strict bilingual public no-regression gate. Stage210h versus Stage210l step 1000 normalized AISHELL CER / LibriSpeech clean WER / LibriSpeech other WER / Common Voice EN WER / WenetSpeech CER is `6.68/9.38/17.14/27.62/22.29 -> 6.37/9.68/17.29/27.41/22.20`; step 500 is worse at `6.41/9.83/17.38/27.88/22.02`. Current-version direct Nano token error at step 1000 improves on AISHELL, LibriSpeech-other, Common Voice, and WenetSpeech (`4.60/11.30/19.10/17.56 -> 4.23/11.19/18.39/17.28%`) but regresses LibriSpeech-clean `5.68 -> 5.82%`. The edit decomposition is consistent across domains: hidden alignment reduces deletion and improves collapsed length, while substitution rises on clean/other/CV/Wenet and insertion rises on clean/CV. This is not a data-volume or execution failure; the hidden objective has no direct token-identity constraint. A configuration audit also clarifies that sampled layer and decoder-hidden losses were all-frame, while the final encoder anchor intentionally inherited `ctc_teacher_frame_filter=nonblank_neighbors`; future runners must set that choice explicitly instead of relying on inheritance. Keep Stage210h as the production incumbent. Stage210l step 1000 may initialize only a short output-repair admission, not a scaled curriculum, because its stronger hidden and four-of-five direct-token gains are useful but not sufficient promotion evidence.
- Stage210m sequence-CTC output-repair plan: initialize the isolated admission from Stage210l step 1000 while retaining Stage210h as the fallback incumbent. Preserve BiRWKV/RWKV-7, copied FFN/Norm, the frozen Nano five-layer CTC decoder/head, exact online Nano features, tokenizer/blank projection, and source-balanced easy schedule. Train only TimeMixer plus the first input projection at LR `5e-7`, zero weight decay, for an uninterrupted `1,000` batches with exact cached fixed evaluation/checkpoints every `250`. Add a low-weight (`0.10`) CTC loss against Nano's collapsed greedy token sequence so equivalent CTC spike shifts remain legal; retain factorized all-frame binary blank `0.25`, conditional nonblank soft KL `1.0`, and legal-nonblank hard CE `0.25`, while keeping uniform full KL, BFKL, mass, top-k, presence, window, and margin objectives at zero. Reduce but retain stacked mixer/FFN/block anchors at `0.25/0.125/0.50`, final active-plus-neighbor encoder anchor `0.25`, and all-frame frozen decoder-hidden anchor `0.25`. Sample 12 layers with hard/structural ids `0/11/12/17/20/49/50/69` always present and four cyclic slots, because Stage210l identifies those mixer/block locations as the remaining bottlenecks; fixed evaluation remains uniformly sampled. Run a two-step four-GPU smoke before the formal admission. Promotion requires lower fixed conditional hard CE, substitution, and total token error without renewed deletion/nonblank-length drift, followed by exact seeded English WER/Chinese CER and direct-Nano improvement; only then build source-grouped medium/hard/long buckets and launch the full three-epoch online curriculum.
- 2026-07-23 Stage210m implementation and smoke: add a dedicated runner that explicitly sets every output/hidden objective, final encoder frame filter, difficult-layer set, freeze policy, and fixed-eval cadence instead of relying on an inherited logits recipe. Stage210k/l/m runner tests pass `6/6`; Ruff, bytecode compilation, and diff checks pass. The real four-GPU two-step smoke loaded Stage210l step 1000 with `missing=0/unexpected=0`, froze `650` tensors, and left exactly `189,378,560` TimeMixer/input-projection parameters trainable. Binary blank, conditional soft/hard token, final encoder, all six decoder hidden states, 12 stacked layer targets, and teacher-sequence CTC were all finite and nonzero; sequence matching was complete, teacher misses and all frame deltas were zero. Selected layers were `0/1/2/3/4/11/12/17/20/49/50/69` then `0/5/6/7/8/11/12/17/20/49/50/69`, proving the eight fixed plus four cyclic policy. Peak reserved/allocated memory was `4.97/4.57 GiB` per GPU, step 2 exported, and the process exited normally. Launch the uninterrupted 1k admission from Stage210l step 1000, never from the smoke export.
- 2026-07-23 Stage210m completion and fixed gate: the uninterrupted four-GPU run consumed all `1,000/1,000` online-teacher batches, exported the complete `250/500/750/1000` checkpoint set, synchronized W&B run `jbheifus`, and exited with status zero. Every teacher lookup matched, every encoder/decoder/layer frame delta was zero, and there was no OOM, non-finite value, CUDA, or NCCL failure. On the exact cached 256-utterance split, fixed total loss fell `1.210707 -> 1.126171`, full/conditional-soft/conditional-hard/blank losses fell `0.207052/0.637142/0.984022/0.141681 -> 0.185281/0.571128/0.919596/0.125274`, and active-frame top-1 improved `0.652610 -> 0.672691`. Greedy token error improved `25.793 -> 24.947%`; insertion/deletion/substitution changed `2.114/8.879/14.799 -> 1.480/9.302/14.165%`, exact sequence rate `69.141 -> 70.703%`, and collapsed length `0.93235 -> 0.92178`. Step 750 reaches the same token error with `1.480/8.879/14.588%` I/D/S and is the lower-deletion fallback. This proves that sequence CTC plus factorized token losses can lower substitutions, but also exposes a residual deletion-versus-substitution exchange; select checkpoints by I/D/S, not aggregate differentiable loss.
- 2026-07-23 Stage210m real gate and exposure audit: normalized public AISHELL CER / LibriSpeech clean WER / LibriSpeech other WER / Common Voice EN WER / WenetSpeech CER is `6.13/9.45/17.65/27.62/21.65` at step 750 and `6.13/9.38/17.47/27.56/21.47` at step 1000, versus Stage210h `6.68/9.38/17.14/27.62/22.29`. Current-version direct Nano token error for Stage210h versus step 1000 is `4.60 -> 3.85%` AISHELL, `5.68 -> 5.66%` LibriSpeech clean, `11.30 -> 11.47%` LibriSpeech other, `19.10 -> 18.20%` Common Voice, and `17.56 -> 16.67%` WenetSpeech. The only broad failure is LibriSpeech-other, where deletion improves `2.64 -> 2.34%` but insertion/substitution worsen `1.17/7.49 -> 1.34/7.80%`. A deterministic replay of the exact first 1,000 four-GPU batches confirms the corrected source interleave is working: `39,308` utterances and only `48.141 h` were exposed, including `8.629 h` LibriSpeech; batch exposure is approximately balanced across six sources except the smaller CV22-ZH stream. This admission consumed only `3.23%` of the `1,490.119 h` easy split. The remaining gap is therefore not evidence against BiRWKV, failed Nano parameter transfer, tokenizer/blank mismatch, teacher loss, or a loader bug. It is a combination of insufficient output-stage exposure and an exact-frame token objective that does not tolerate the small CTC spike shifts already measured in this project.
- Stage210n full-easy spike-tolerant output alignment plan: preserve the 70-layer BiRWKV/RWKV-7 encoder, copied Nano FFN/Norm, frozen Nano five-layer CTC decoder/head, exact online Nano frontend, vocabulary/blank projection, and corrected source-grouped easy manifest. Add a backward-compatible conditional-nonblank window-hard objective: identify each collapsed teacher nonblank spike, search the corresponding student `+-2` frame window, compute hard CE only over legal nonblank ids, and use a temperature-`0.20` normalized soft minimum. It must have exactly zero gradient on project blank and ignored ids, count one event per collapsed teacher spike, and retain exact frame-length checks. This directly targets token identity while allowing harmless spike displacement. Start from Stage210m step 1000; retain binary blank `0.25`, conditional soft token `1.0`, reduce exact-frame conditional hard from `0.25` to `0.125`, add window-hard `0.25`, increase sequence CTC from `0.10` to `0.20`, and retain the Stage210m stacked/final/decoder hidden anchors. Train only TimeMixer plus the first input projection at LR `3e-7`, zero weight decay. After unit/gradient/config tests and a two-step four-GPU smoke, run the complete `1,490 h` easy epoch without early stopping, with exact cached fixed evaluation/checkpoints at step `0/10000/20000/30000/final` and W&B logging. Promotion to the source-balanced medium pool requires lower direct Nano substitution/token error on both LibriSpeech sets plus no public AISHELL/Common Voice/Wenet regression; do not infer saturation or change architecture from another sub-epoch sample.
- 2026-07-23 Stage210n implementation and smoke: extend the existing nonblank-window objective with a backward-compatible `full | conditional_nonblank_hard` mode. The conditional mode excludes project blank and every ignored id before its log-sum-exp, so its gradient on those logits is exactly zero; it reuses collapsed teacher peaks, zero-tolerance time mapping, radius-2 windows, and normalized soft-min reduction. The DeepSpeed loop now also runs fixed step evaluation when the final step is not divisible by the normal evaluation interval, ensuring step `30064` receives a sidecar. New gradient/window and Stage210m/n runner tests plus the relevant CTC suite pass `62/62`; Ruff, bytecode compilation, and the 30,064-step dry-run config pass. The real four-GPU two-step smoke loaded Stage210m step 1000 with `missing=0/unexpected=0`, froze `650` tensors, left `189,378,560` TimeMixer/input-projection parameters trainable, and produced `7/1` conditional-window teacher-spike events on its two batches. Every enabled output/encoder/decoder/layer branch matched all rows with zero frame delta; peak reserved/allocated memory was `4.97/4.57 GiB`, the final step-2 fixed evaluation ran, the checkpoint exported, and the process exited normally. Launch the formal run from the original Stage210m step 1000 checkpoint, never from the smoke export.
- 2026-07-23 Stage210n formal launch: the full `30,064`-batch easy-epoch run is active under `/tmp/rwkvasr_runs/sensevoice_rwkv_stage210n_stage210m1000_sourcebalanced_easy1490h_1ep_sequence0p2_condwindow0p25_stackedhard12_lr3e7_wd0_4x4090`, W&B run `tdpd7ba7`, in persistent tmux session `rwkvasr_stage210n_full_easy`. It loaded Stage210m step 1000 exactly, retained the audited `650` frozen tensors and `189,378,560` trainable parameters, and measured fixed step-0 loss `1.3671` on 256 cached utterances. Initial full-size batches have complete teacher/window/sequence/hidden matching and zero frame delta; the first batch reserved/allocated `9.66/9.37 GiB`, while later variable batches use approximately `19-20 GiB/GPU`. A separate `rwkvasr_stage210n_monitor` session records process, latest step/loss, disk, and GPU memory every ten minutes. The additional DeepSpeed regression suite passes `29/29`. Continue all `30,064` batches without early stopping and use only the `10k/20k/30k/final` fixed plus public/direct-Nano gates for promotion.
- 2026-07-23 Stage210m real-audio Nano-gap audit: recomputing both systems against the same references, rather than treating student-to-Nano token error as WER/CER, confirms that Stage210m step 1000 is not yet close enough to native Nano CTC. On the exact seeded 200-utterance manifests with greedy CTC and `ctc` text normalization, Nano versus Stage210m is AISHELL CER `3.850/6.133`, LibriSpeech clean WER `7.457/9.378`, LibriSpeech other WER `13.127/17.472`, Common Voice EN WER `22.228/27.563`, and WenetSpeech CER `9.263/21.474`. Absolute student gaps are therefore `+2.283/+1.921/+4.345/+5.335/+12.211` points, with exact `200/200` utterance-id coverage on every set. The WenetSpeech gap in particular cannot be described as a small residual even though direct student-to-Nano token metrics improved. All later reports must keep reference WER/CER and teacher-imitation error as separate fields.
- Stage210n real-audio promotion gate: evaluate checkpoints `10k/20k/30k/final` on the same real public audio after the uninterrupted training process releases all four GPUs, so evaluation cannot contend with or destabilize DeepSpeed. The report must verify identical utterance-id coverage and normalized reference text before comparing systems. A 200-sample checkpoint passes the provisional "close to Nano" gate only when every dataset has both relative error ratio `student / Nano <= 1.20` and absolute WER/CER gap `<= 3.0` percentage points. This seeded gate is only for checkpoint selection; the selected candidate must then run the complete AISHELL-1 test, LibriSpeech test-clean/test-other, Common Voice EN test, and WenetSpeech TEST_NET manifests before promotion. Fixed loss or student-to-Nano token error alone is never sufficient for promotion.
- 2026-07-23 Stage210n real-audio gate implementation: add `scripts/compare_public_ctc_with_nano.py`, which requires exact Nano/student utterance coverage and normalized-reference equality before reporting both systems' reference WER/CER, absolute and relative gaps, paired changed/improved/worsened counts, and per-dataset/overall gates. Its Stage210m replay is stored under `/tmp/rwkvasr_eval/stage210m_step1000_seeded_public200/nano_real_audio_comparison.{json,md}` and exactly reproduces the audited metrics. Add `scripts/run_stage210n_real_audio_gates.sh` to wait until the uninterrupted DeepSpeed session releases all GPUs, then decode and compare checkpoints `10k/20k/30k/30064` on the exact seed-`20260620` 200-utterance manifests. Bash syntax/dry-run, Ruff, and five related tests pass. The persistent queue is active in tmux session `rwkvasr_stage210n_real_gates` and logs to the Stage210n run's `real_audio_gates.log`; it must not be moved ahead of training or run concurrently with the four-GPU job.
- Stage211 strict chained attention-replacement curriculum: retain the fixed 70-layer BiRWKV/RWKV-7 architecture and treat Nano SANM self-attention, complete Nano encoder block, and Nano CTC output as three distinct targets. In every phase, train only BiRWKV TimeMixer parameters plus the unavoidable first-layer `560 -> 512` input projection; keep all Nano-copied FFN/MLP, `norm1/norm2`, `after_norm/tp_norm`, five-layer CTC decoder, and CTC head parameters frozen. Phase A uses exact teacher layer inputs and aligns only each Nano `self_attn` output against the corresponding BiRWKV TimeMixer output; FFN, block, terminal-path, and logit losses are zero. Phase B must initialize from the selected Phase-A checkpoint, run the complete student stack, and align complete residual block outputs under student-propagated inputs; it must not restart from a pre-Phase-A baseline. Phase C must initialize from the selected Phase-B checkpoint and only then enable factorized blank/nonblank, spike-tolerant sequence/window, and final-logit objectives, with low hidden anchors retained to prevent forgetting. No phase advances automatically from training-batch loss.
- 2026-07-23 Stage211 initialization and historical-gap audit: direct checkpoint comparison against ModelScope Nano confirms that Stage210a step 30064 has all `564/564` copied encoder non-attention tensors bit-identical after BF16 conversion: `280` FFN/MLP tensors, `280` per-layer Norm tensors, and `4` terminal Norm tensors all have relative L2 and maximum absolute error `0`. Stage210h and Stage210m instead have FFN relative L2 drift `0.010986`, so they do not satisfy the strict frozen-Nano-MLP initialization contract. Use Stage210a step 30064 as the default clean Phase-A initialization. Its TimeMixer was already Nano-QKV warm-started with the admitted `rwkv_norm` scaling and trained over one complete `1,490.119 h` easy epoch, avoiding random initialization. The earlier Stage210k local continuation improved its teacher-forced mixer loss by `7.68%` but was rejected before a chained stacked repair; Stage210l then restarted from Stage210h. Stage211 specifically tests the missing chained path instead of repeating that reset.
- Stage211 phase gates and data contract: each formal phase uses the corrected source-balanced `1,490.119 h` easy manifest for all `30,064` batches, with fixed cached evaluation/checkpoints at `0/10k/20k/30k/final` and no early stopping. Phase A is selected by all-layer local mixer loss/cosine/RMS, requiring no missing rows or frame deltas and material improvement in the weak `10-29` bands; temporary public WER regression is diagnostic rather than a Phase-A rejection because the full student distribution has not yet been repaired. Phase B is selected by stacked block and terminal-path hidden metrics and must recover or improve normalized public AISHELL, both LibriSpeech sets, Common Voice EN, and WenetSpeech relative to its clean initialization before logits are admitted. Phase C uses the same real-audio Nano comparison gate as Stage210n: every seeded dataset requires `student / Nano <= 1.20` and absolute WER/CER gap `<= 3.0` points, followed by complete public test-set evaluation. Keep phase outputs separate and require an explicit selected checkpoint for each transition.
- 2026-07-23 Stage211 implementation and queued smoke: add `scripts/run_stage211_strict_chained_alignment.py` with explicit `mixer | block | logits` phases and no automatic transition. Every phase enforces `freeze_encoder_except_time_mixer=true`, frozen CTC decoder/head, zero FFN loss, zero weight decay, one complete `30,064`-batch easy epoch, and `10k/20k/30k/final` fixed gates. Phase A is teacher-forced mixer-only; Phase B uses stacked block `1.0`, a mixer retention anchor `0.25`, and terminal encoder/decoder-hidden anchors `0.5/0.5` with all logits losses zero; Phase C enables the admitted factorized/spike-tolerant output losses with reduced mixer/block anchors. Fresh Phase-B/C runs require an explicit preceding checkpoint. The runner performs a default bit-exact 564-tensor Nano non-attention audit before launch; a real dry-run against Stage210a passed `564/564` and generated the expected 30,064-step Phase-A config. New and inherited runner tests pass `18/18`, and Ruff plus bytecode compilation pass. The two-step Phase-A GPU smoke is queued in tmux `rwkvasr_stage211a_smoke_queue`; it waits for `rwkvasr_stage210n_real_gates` to finish before using the GPUs and must not be promoted automatically to a formal run.
- Stage211 strict transition contract: the only legal order is `mixer (A) -> block (B) -> logits (C) -> supervised CTC SFT (D)`. A fresh B/C/D run must provide both the selected predecessor checkpoint and a machine-readable promotion receipt. The receipt must name the exact source and target phases, bind the checkpoint and independent gate report by absolute path and SHA-256, and record a passing decision. The runner must reject a missing, stale, phase-mismatched, path-mismatched, checkpoint-mismatched, or gate-report-mismatched receipt. Training completion or lower batch loss never creates a receipt and never launches the next phase. Resuming an already admitted output directory remains legal, but changing its initialization or phase in place is not.
- Stage211d labeled CTC SFT plan: only after a Phase-C checkpoint passes the real reference WER/CER Nano gate, initialize a separate supervised run from that exact checkpoint. Use ground-truth CTC as the primary objective (`ctc_loss_weight=1.0`), `text_normalization=ctc`, pronunciation-only CTC suppression, and `allow_missing_targets=false`; retain low online Nano regularizers only to limit catastrophic drift (`blank=0.05`, conditional nonblank KL `0.10`, mixer/block/final-encoder/decoder-hidden anchors `0.05/0.10/0.10/0.10`). Continue training only TimeMixer plus the unavoidable input projection: Nano-copied FFN/MLP and Norm, the five-layer CTC decoder, and the CTC head remain frozen in this first supervised stage. This preserves the attribution to BiRWKV and uses label CTC's path marginalization to repair teacher spike-placement errors without changing the architecture or output vocabulary.
- Stage211d labeled-data contract: require an explicitly supplied labeled WebDataset root, length index, and bucket manifest rather than inheriting the Stage211 A/B/C audio-only pool. Before launch, audit every length-index row for a JSON label member, non-empty normalized text/tokens, zero CTC unknown tokens, and `ctc_required_frames <= ctc_logit_frames`; require non-empty disjoint `train` and `eval` splits. The first admitted dataset is `/media/usbhd/training_data/asr/curriculum/clean_ctc_voxbox_webdataset/stages/easy_clean_librispeech_aishell3_ctc_norm_aligned_sensevoice_lfr6_svtok`: `285,302` total utterances, `809.128 h`, `283,868` train plus `1,434` eval, `8,792,460` CTC tokens, and zero infeasible rows. At batch `12`, four GPUs, frame budget `8,000`, and exact no-drop distributed tails, one complete train epoch is `12,045` batches. Run all batches without early stopping, evaluate/save every `2,000` steps plus final, and select by normalized ground-truth English WER/Chinese CER on the five public gates. SFT promotion requires improvement over Phase C without regression on any guarded set; lower supervised loss alone is insufficient.
- 2026-07-24 Stage211 strict-chain and SFT implementation: extend the runner to explicit `mixer | block | logits | sft` phases while preserving one-phase-per-invocation behavior. Fresh B/C/D runs now require a promotion receipt whose source/target phase, passing decision, selected checkpoint path/SHA-256, and independent gate-report path/SHA-256 all validate; immutable phase/data/receipt provenance is recorded before a real launch and checked on resume. Add `scripts/create_stage211_promotion_receipt.py` as the only supported receipt writer, with an explicit gate-passed confirmation. Stage211d config uses supervised CTC `1.0`, low Nano retention anchors, frozen Nano MLP/Norm/decoder/head, pronunciation-only suppression, and an explicitly audited labeled dataset. A real full-index preflight confirmed the stated `285,302` rows, `809.128 h`, zero unknown/infeasible CTC rows, and the later exact-tail correction raises the complete no-drop epoch from the original floor estimate to `12,045` train steps. The generated SFT config parses successfully with eval/checkpoints every `2,000`; the Stage211a real dry-run still passes the `564/564` exact Nano non-attention audit. Stage210k/l/m/n plus Stage211 runner tests pass `23/23`, Ruff, bytecode compilation, and diff checks pass. No Stage211 phase is auto-promoted, and no Stage211d GPU run is allowed before the formal C gate.
- 2026-07-24 Stage210n completion and rejection: the uninterrupted job consumed all `30,064/30,064` source-balanced easy batches and exported fixed/checkpoint pairs at `10k/20k/30k/30064` without teacher misses, frame deltas, non-finite loss, OOM, CUDA, or NCCL failure. Fixed total loss at step `0/10k/20k/30k/final` was `1.367082/1.317205/1.320116/1.333906/1.334867`; token error was `24.841/24.841/25.265/25.265/25.265%`, so the differentiable objective reached its best fixed value near 10k and did not convert the remaining epoch into discrete-token gains. Native Nano versus the broad-best Stage210n step 10k normalized AISHELL CER, LibriSpeech clean/other WER, Common Voice EN WER, and WenetSpeech CER is `3.850/5.860`, `7.457/9.353`, `13.127/16.925`, `22.228/27.040`, and `9.263/20.692`. All checkpoints fail the every-dataset `<=1.20x` and `<=3`-point gate; step 10k is retained only as the Stage210n broad-best diagnostic, not promoted. This full-epoch result confirms that more batches under the same mixed hidden/output objective are insufficient and motivates the strict Stage211 decomposition.
- 2026-07-24 Stage211a mixer-only smoke: after Stage210n real gates released the GPUs, the queued four-GPU run loaded the clean Stage210a step 30064 initialization with `missing=0/unexpected=0`, passed the `564/564` exact Nano non-attention audit, froze `650` tensors, and left `189,378,560` TimeMixer/input-projection parameters trainable. The only active target was teacher-forced local mixer alignment (`mixer=1`, FFN/block/path/logits/CTC all zero); every row matched with zero frame delta. Fixed four-sample loss was `0.602942 -> 0.602858` over two steps, peak reserved/allocated memory was `4.26/4.12 GiB`, all checkpoints/provenance were written, and the process exited normally. This admits the Phase-A implementation smoke only; the formal 30,064-batch Phase A must start from the original Stage210a checkpoint, never the smoke checkpoint.
- 2026-07-24 Stage211a formal launch: start the isolated `30,064`-batch teacher-forced mixer phase from the original clean Stage210a step 30064 checkpoint in tmux `rwkvasr_stage211a_formal`, with a separate ten-minute `rwkvasr_stage211a_monitor` and W&B run `3cur58ry`. Startup again passed the `564/564` exact audit, loaded `missing=0/unexpected=0`, froze `650` tensors, and exposed only the `189,378,560` TimeMixer/input-projection parameters. The exact cached 256-utterance step-0 baseline is total/mixer loss `0.432616`, mean all-layer mixer loss `0.407606`, and mean mixer cosine `0.859488`. Initial batches cover cyclic eight-layer bands, have complete online-teacher matches and zero frame delta, and keep every non-mixer objective exactly zero. Run all `30,064` batches with fixed gates at `10k/20k/30k/final`; do not create a Phase-A promotion receipt or launch Phase B until the all-layer/depth-band gate is independently evaluated.
- 2026-07-25 Stage211a completion and hidden gate: the uninterrupted four-GPU run completed all `30,064/30,064` batches in one `1,490.119 h` easy epoch, exported fixed 256-utterance evaluations at `0/10k/20k/30k/final`, synced W&B run `3cur58ry`, and exited without missing teacher rows, frame deltas, non-finite loss, OOM, CUDA, or NCCL errors. The best fixed checkpoint is `step-30000.pt`: total fixed loss improved `0.432616 -> 0.358507`; the mean 70-layer local mixer loss improved `0.407606 -> 0.336233` (`-17.51%`) and cosine improved `0.859488 -> 0.884103`. The prescribed weak bands improved materially: layers `10-19` loss/cosine changed `0.700945/0.755197 -> 0.596192/0.792835` (`-14.94%`, `+0.037638`), and layers `20-29` changed `0.542880/0.814734 -> 0.432674/0.851187` (`-20.30%`, `+0.036453`). Every one of the 70 local mixer losses improved versus step 0. Final step 30064 is statistically tied but has slightly worse total and mean mixer loss than step 30000, so use only step 30000 for the independent Phase-A gate and SHA-256-bound `mixer -> block` receipt. Phase B must first pass a two-step four-GPU smoke with the receipt and exact Nano non-attention audit, then run its own isolated `30,064`-batch stacked block phase; Phase C remains blocked.
- 2026-07-25 Stage211b admission smoke: the independent Phase-A report passed and the `mixer -> block` receipt binds `step-30000.pt` SHA-256 `bc887781aba331205a22a6dd65065820fd8c648f484c57f7884d9484812dbd0c` to that report. The fresh two-step four-GPU stacked smoke revalidated `564/564` bit-exact Nano non-attention tensors, loaded with `missing=0/unexpected=0`, retained `650` frozen tensors and `189,378,560` trainable TimeMixer/input-projection parameters, and activated exactly mixer `0.25`, block `1.0`, terminal encoder `0.5`, and five-layer decoder-hidden `0.5`; all logit and CTC losses remained zero. Online teacher matches were complete with zero layer/decoder frame delta, fixed four-utterance loss moved `1.3047 -> 1.2909`, peak reserved memory was `4.95 GiB`, and the process exited normally. This admits only a fresh formal Phase B initialized from the SHA-bound Phase-A step 30000 checkpoint; Phase C remains blocked pending Phase-B hidden and real-audio gates.
- 2026-07-25 Stage211b formal launch: start the isolated stacked-block phase in tmux `rwkvasr_stage211b_formal` with a separate ten-minute `rwkvasr_stage211b_monitor`, W&B run `9rur7wu0`, and the immutable A-to-B receipt. Startup revalidated `564/564` exact Nano non-attention tensors, loaded the selected Phase-A step 30000 checkpoint with `missing=0/unexpected=0`, and preserved the intended `650` frozen / `189,378,560` trainable split. The cached 256-utterance step-0 Phase-B loss is `1.1649`; initial full-size batches match every online teacher row and all sampled layer plus decoder-hidden frame lengths, with peak reserved/allocated memory `9.26/8.68 GiB`. Run all `30,064` batches with no early stop and fixed hidden gates at `10k/20k/30k/final`. Do not create a `block -> logits` receipt or start Phase C until stacked block, terminal path, and normalized five-domain real-audio comparisons are independently evaluated.
- 2026-07-25 Stage211 storage/crash incident and recoverable evidence: the host later rebooted at `20:05 CST`, clearing `/tmp/rwkvasr_runs`, all local Stage211 checkpoints/receipts/eval YAMLs, and the tmux logs. The 20 TB USB/XFS source disk also failed to enumerate after boot (`/dev/sda` absent, not merely unmounted); the previous boot journal records repeated `DID_TIME_OUT`/I/O errors on that disk, and the Stage211b W&B heartbeat stopped at `15:43 CST`, consistent with a source-disk stall before the reboot. W&B run `9rur7wu0` proves the interrupted formal B reached step `19,020/30,064` (`63.27%`) but uploaded only metadata and requirements, not a checkpoint, so no cloud weight recovery is possible. Its exact fixed 256-utterance `0 -> 10k` metrics nevertheless validate the method: total Phase-B loss `1.164927 -> 0.334383`, mean 70-layer block loss `0.340650 -> 0.092788` (`-72.76%`), block cosine `0.896037 -> 0.968966`, block RMS ratio `0.734190 -> 0.978865`, decoder-hidden loss `0.934184 -> 0.157018` (`-83.19%`), and mixer-retention loss `0.955390 -> 0.494825`. Do not treat this as a completed Phase-B gate because the exact checkpoint and real-audio evaluation are missing. Before rerunning, move every formal Stage211 output, promotion receipt, fixed-eval sidecar, and resume checkpoint off `/tmp` to persistent NVMe storage; use `/tmp` only for disposable smoke artifacts. Formal A/B/C must save persistent resume checkpoints every `2,000` steps while retaining the prescribed fixed hidden/logit evaluations only at `10,000` steps and final, limiting a machine or source-disk failure to at most 2k replayed batches without changing selection criteria. After the USB disk is re-enumerated, first audit/copy the exact required curriculum shards to stable local storage where capacity permits, regenerate persistent bucket manifests, then rerun strict Phase A and chained Phase B from their original admissible initialization. No Phase C promotion is legal from W&B metrics alone.
- 2026-07-25 Stage211 persistent-recovery implementation: change the strict runner default from `/tmp/rwkvasr_runs` to `${RWKVASR_STAGE211_OUTPUT_ROOT:-~/rwkvasr_runs}` and reject formal output under `/tmp`, `/var/tmp`, or `/dev/shm`; smoke and dry-run artifacts remain allowed there. Remove the volatile implicit Phase-A checkpoint and bucket-manifest defaults: every fresh phase now requires an explicit persistent initialization checkpoint, every invocation requires an explicit bucket manifest, and the default Nano teacher is the repository-local persistent `assets/fun-asr-nano-2512/model.pt`. Formal A/B/C save resume checkpoints every `2,000` steps while fixed evaluation remains at `10k/20k/30k/final`; SFT remains at its existing 2k cadence. Before a real audio-only launch, audit that the manifest root, source length index, and every bucket-part file exist, override the inherited runtime root/index with that audited contract, and bind the audit into immutable Stage211 provenance. This specifically prevents a copied manifest from silently retaining the old failed-disk index path. Ruff and the Stage210k/l/m/n, Stage211, and public-Nano comparison suites pass (`29 passed`).
- 2026-07-25 Stage211 post-reboot reconstruction contract: after the USB enclosure was powered back on, `/media/usbhd` reappeared as read-only XFS and all required easy-split audio shards were readable, but the Stage210a/Stage211 checkpoints formerly under `/tmp` remained irrecoverable and neither W&B run contained model artifacts. Rebuild the source-grouped easy manifest on persistent NVMe from the canonical Stage179a length index using bucket width `200`, automatic text cost `4`, and `_stage179_source`; the resulting manifest must contain exactly `1,174,987` rows, six sources, 70 part files, and `30,064` four-GPU batches. Because strict Phase A requires the lost clean Stage210a initialization, first reconstruct Stage210a from the last persistent architecture-compatible Stage179b3 step-256 checkpoint by reapplying the admitted Nano initialization: exact copied FFN/Norm/terminal Norm, `rwkv_norm` Q/K/V transfer plus exact output projection and first-layer SVD projection, and copied five-layer CTC decoder/head. This is a new recovery run, not a claim that the missing Stage209 RWKV-only state was recovered. Its fixed 256-utterance step-0 component report must be compared with the historical inherited baseline before promotion, then it must consume all `30,064` source-balanced easy batches with fixed evaluation at `0/10k/20k/30k/final`, persistent DeepSpeed/export checkpoints every `2,000` steps, and no early stopping. Only its independently selected final checkpoint may restart strict Stage211 A; all recovery outputs, manifests, logs, and monitoring state must stay on NVMe.
- 2026-07-25 Stage210a persistent reconstruction launch: rebuild `/home/yueyulin/rwkvasr_data/stage211_easy_source_grouped_buckets/manifest.json` with the required `1,174,987` rows, six sources, 70 parts, and `30,064` batches; the manifest SHA-256 is `af8cc91b3225abfba7a9f8211a958f2b33cfdfa113b4f9adfdc1363742ff4c7b`. Copy Stage179b3 step 256 and the complete ModelScope Nano directory to NVMe and verify their `model.pt` SHA-256 values against USB as `bfe5c0f5b989c5dfbe66d7355396f0b8919dd6200a5ff7ccf31eb7d11b5b1a2c` and `81fec8616083c69377f3ceef36aba3655660ee0ca69a5d4a1e9810cd340ca499`. A two-step four-GPU preflight loaded the compatible checkpoint with only the expected missing 84 not-yet-created decoder tensors, then Nano initialization loaded `1,125` encoder mappings, all `84` decoder tensors, and all `60,515` projected CTC rows; it preserved the admitted first-layer reconstruction errors, froze `650` tensors, exposed only `189,378,560` TimeMixer/input-projection parameters, matched every hidden target with zero frame delta, and peaked at `4.56 GiB`. Start the formal run under persistent `/home/yueyulin/rwkvasr_runs/sensevoice_rwkv_stage210a_recovery_stage179b3_nanoqkv_rwkvnorm_sourcebalanced_easy1490h_1ep_hidden_localenergy_lr1e5_4x4090`, tmux `rwkvasr_stage210a_recovery_formal`, monitor `rwkvasr_stage210a_recovery_monitor`, and W&B `gckd0m9o`. Its fixed 256-utterance step-0 loss is `2.9821`, only `0.16%` below the historical inherited `2.987020` reference; initial updates have complete teacher matching, zero frame delta, finite loss, and all non-hidden objectives disabled. Continue all `30,064` batches without early stopping, save resumable state every `2,000` steps, and do not relabel this recovery run as the lost original checkpoint.
- 2026-07-26 Stage210a persistent reconstruction completion and gate: the four-GPU run consumed all `30,064/30,064` source-balanced easy batches, wrote every prescribed 2k persistent export and DeepSpeed state plus fixed `0/10k/20k/30k/final` reports, synced W&B `gckd0m9o`, and exited normally without teacher misses, frame deltas, non-finite loss, OOM, CUDA, NCCL, or source-disk errors. Fixed total loss was `2.982137/0.703522/0.516148/0.443658/0.443776`; at step 30000, mean 70-layer mixer loss/cosine/RMS ratio reached `0.408580/0.860142/0.852224` from `2.952735/0.012411/0.121015`, copied-FFN functional loss/cosine reached `0.034014/0.988508` from `0.118678/0.963140`, and mixer loss plus cosine improved in all `70/70` layers. The weak `10-19` and `20-29` bands reached `0.721132/0.751008/0.777708` and `0.532465/0.819311/0.792903`; both are materially stronger than the lost original Stage210a final bands. Select persistent `step-30000.pt`, not the statistically tied step 30064, because it has the lowest total, mixer, and FFN fixed losses and a slightly better mean RMS ratio. It also materially beats the original Stage210a final mixer `0.568420/0.811696/0.757378`, so it passes the operator-local gate and is admitted as the clean Stage211A recovery initialization.
- 2026-07-26 Stage211A recovery smoke: the selected Stage210a recovery step 30000 passed the strict `564/564` bit-exact Nano non-attention audit. A fresh two-step four-GPU mixer-only smoke loaded it with `missing=0/unexpected=0`, retained the required `650` frozen tensors and `189,378,560` trainable TimeMixer/input-projection parameters, kept every FFN/block/path/logit/CTC objective at zero, matched every online-teacher row with zero frame delta, and moved the fixed four-utterance objective `0.6081 -> 0.6080` with peak reserved memory `4.26 GiB`. This admits a new persistent formal Stage211A run over all `30,064` source-balanced easy batches with resume checkpoints every 2k and fixed gates at `0/10k/20k/30k/final`; it does not admit Phase B or recreate any lost Phase-A receipt.
- 2026-07-26 Stage211A persistent recovery launch: start the formal mixer-only phase under `/home/yueyulin/rwkvasr_runs/sensevoice_rwkv_stage211a_recovery_stage210a30000_nanomlpfrozen_teacherforced_mixeronly_easy1490h_1ep_lr3e6_wd0_4x4090`, tmux `rwkvasr_stage211a_recovery_formal`, monitor `rwkvasr_stage211a_recovery_monitor`, and W&B run `0txqc64x`. Startup revalidated `564/564` exact frozen Nano non-attention tensors, audited all 70 persistent source-grouped part files, bound the selected Stage210a step 30000 SHA-256 into immutable provenance, and loaded with `missing=0/unexpected=0`. The cached fixed 256-utterance step-0 loss is `0.4343`, consistent with the lost original Phase-A baseline `0.4326`; first-step peak reserved/allocated memory is `4.62/4.47 GiB`, teacher matches and layer frame lengths are complete, and every non-mixer objective remains zero. Run all `30,064` batches without early stopping, persist resume state every 2k, and block Phase B until the independent all-layer/depth-band gate selects and SHA-binds a Phase-A checkpoint.
- 2026-07-26 Stage211 full-data and per-phase public-benchmark correction: the strict order remains `RWKV layer/mixer (A) -> stacked block (B) -> CTC logits (C) -> labeled CTC SFT (D)`, but a `1,490.119 h` easy-only run is only the first curriculum segment of A/B/C and must not be reported or promoted as a completed full-data phase. Each of A, B, and C must consume the complete deduplicated audio-only pool in ordered `easy -> medium -> hard -> long` segments: `1,174,987 / 42,247,508 / 18,649,326 / 404` rows and `1,490.119 / 47,771.242 / 69,200.368 / 3.432 h`, for an exact total of `62,072,225` unique rows and `118,465.161 h` per epoch. The already-running A-easy job predates the standardized full-data profile and remains a valid calibration warmup and initialization source, but it cannot issue an A-easy coverage receipt or skip the standardized A-easy segment. Every completed A/B/C/D phase must run direct greedy CTC on the full fixed public suite with shared `ctc` normalization and identical utterance coverage: LibriSpeech test-clean `2,620`, LibriSpeech test-other `2,939`, Common Voice 22 English test `16,396`, AISHELL-1 test `7,176`, and WenetSpeech TEST_NET `24,774`. Each machine-readable phase report must bind the phase name, selected checkpoint absolute path and SHA-256, benchmark-manifest paths and SHA-256 values, exact sample counts, cached direct Nano-CTC prediction hashes, student prediction hashes, normalized English WER and Chinese CER, gap/ratio versus Nano, and full-data curriculum coverage receipts. A promotion receipt is invalid if this complete benchmark contract is absent or inconsistent. A and B may use hidden/path improvement plus non-catastrophic public-metric progress as their phase-specific admission gate because their output objective is intentionally incomplete; C must pass the existing every-dataset `<=1.20x` and `<=3`-point Nano gate before D; D must improve the selected C checkpoint without regression on any guarded public set. The labeled SFT phase must consume at least the complete audited `809.128 h` labeled epoch and continue only by independently measured public WER/CER, never by supervised loss alone.
- 2026-07-26 Stage211 three-epoch throughput correction: the `batch=12`, raw-frame-budget `8,000` profile underuses each RTX 4090 at about `6.7 GiB`; simply raising batch size does little because the raw-frame budget is the active cap. Before any full-data segment, require a two-step four-GPU smoke of `micro-batch=36`, raw-frame-budget `24,000`, and the unchanged objective/optimizer, rejecting it on OOM, non-finite loss, teacher mismatch, frame mismatch, or unsafe peak memory. If admitted, standardize every full-data A/B/C difficulty segment to three complete epochs with this profile. The estimated per-epoch easy/medium/hard/long counts are `9,954 / 334,553 / 322,054 / 14`, so each full phase consumes `186,216,675` row exposures and `355,395.483 h` in `29,862 / 1,003,659 / 966,162 / 42` segment steps (`1,999,725` total), approximately the same optimizer-step order as one pass under the old profile while using three times the audio. Treat the already-running A-easy `30,064`-step job as an additional calibration warmup and initialization source, not as a substitute for the standardized three-epoch A-easy coverage receipt. Keep fixed hidden evaluation on one immutable 256-row easy subset across all non-easy segments, retain only the newest two 2k recovery checkpoints plus at most eight independently ranked 10k candidates, and preserve every metric sidecar.
- 2026-07-26 Stage211A recovery 10k hidden/public diagnostic: the fixed 256-row gate improved total loss `0.434316 -> 0.379054` (`-12.72%`), mean 70-layer mixer loss `0.408430 -> 0.355732` (`-12.90%`), and cosine `0.860179 -> 0.876595`; mixer loss and cosine improved in all `70/70` layers, while weak bands `10-19` and `20-29` improved `0.720392 -> 0.636874` and `0.532876 -> 0.451511`. A matched real-audio public-200 comparison shows that the still-large absolute CTC gap is inherited rather than caused by Phase A: Stage210a initialization versus A step 10k normalized AISHELL CER changed `99.93 -> 90.32`, LibriSpeech clean/other WER `75.96/88.76 -> 49.85/70.80`, Common Voice EN WER `86.45 -> 69.30`, and WenetSpeech CER stayed `100.00 -> 100.00`. Output/reference unit ratios improved `0.049 -> 0.228`, `0.363 -> 0.700`, `0.210 -> 0.490`, `0.259 -> 0.529`, and `0.005 -> 0.010`, confirming that local mixer alignment is reducing deletion collapse but has not solved stacked propagation, especially on difficult Chinese. Continue A-easy to all `30,064` steps without changing its objective; do not promote it, and use the standardized full-data A plus stacked block B to address the remaining gap.
- 2026-07-26 Stage211 restart-safe curriculum execution: run every formal A/B/C phase through one re-entrant controller that owns exactly four persistent segment directories in `easy -> medium -> hard -> long` order. The controller must run the admitted `batch=36`, frame-budget `24,000` four-GPU smoke before a fresh phase, invoke the strict single-segment runner, verify the exact completion step, create and revalidate an immutable segment receipt, and use that receipt plus the exact completion-checkpoint SHA-256 to admit the next segment. A resumed segment must use only its recorded provenance and local DeepSpeed state; it cannot accept a replacement initialization or receipt. After `long`, the controller must emit a machine-readable curriculum summary but must not start the next phase. Phase promotion remains a separate operation gated by the fixed hidden-path report and full normalized English WER/Chinese CER comparison against direct Nano CTC. Bind fixed step-eval reports to the exact eval split/part hashes so hidden gates cannot compare different samples accidentally.
- 2026-07-26 Stage211 persistent full-data manifests: after the external disk recovered, rate-limited copies of the medium, hard, and long curriculum metadata completed on NVMe with source/destination manifest hashes verified. Materialize one shared 256-row fixed hidden-eval part at `/home/yueyulin/rwkvasr_data/stage211_full_curriculum/fixed_hidden_eval/part_000000.jsonl`, SHA-256 `9f4bf09cdbbf5bc963d6282a640e45fcd8da75f2e23a41e4515b6766288c84f1`, and attach it to all four immutable training manifests. Reloading the generated manifests proves exact train/eval counts `1,174,987/256`, `42,247,508/256`, `18,649,326/256`, and `404/256`, and the admitted `batch=36`, frame-budget `24,000`, world-size `4` step estimates `9,954`, `334,553`, `322,054`, and `14` per epoch. The generated manifest SHA-256 values are easy `3e421da90f5145a9a63db7908a7bc737fb0a5da1fe725aa198c3979785d14924`, medium `8fe5828da6f5d949a4febd7ab64abe5a73e1baef5beaed4861dfcf74b2a4d0b9`, hard `754d73f4b0bc4d1da9a8d10423a86960bc46f9bb796a25a8d9d910d0dc950977`, and long `38f0b73ea8fc87da7a8882059cf2bca558875328ae054b81350966a882bca1c5`.
- 2026-07-26 Stage211D labeled-SFT completion contract: after Stage211C passes its every-dataset direct Nano-CTC gate, run one restart-safe labeled CTC SFT epoch from the exact promoted C checkpoint. The immutable pronunciation-only labeled corpus must audit to `283,868` train rows, `1,434` eval rows, `285,302` total rows, `809.12755 h`, `8,792,460` CTC tokens, and `12,045` optimizer steps under the existing `batch=12`, frame-budget `8,000`, world-size `4`, exact-tail no-drop profile. Require a two-step four-GPU smoke before the formal run, bind the final checkpoint, manifest, length index, provenance, and logits-promotion receipt by SHA-256, then run the same complete five-dataset normalized direct-greedy-CTC suite. The D gate passes only when no guarded dataset regresses versus the selected C checkpoint, macro normalized error strictly improves, and at least one dataset strictly improves; a lower labeled eval loss alone is insufficient. The persistent A/B/C supervisor must continue through D and emit a final machine-readable completion report rather than stopping after logits.
- 2026-07-26 Stage211 exact-tail coverage correction: the first full-data profile used floor-divided per-bucket step estimates and the bucket loader default `length_bucket_drop_last=true`, while its coverage receipt claimed every corpus row was exposed in every epoch. This can silently omit each length bucket's incomplete four-rank tail and is not an admissible proof of the requested complete-data contract. Formal A/B/C/D must instead set `length_bucket_drop_last=false` and `skip_oversized_samples=false`; the distributed bucket loader must preserve every original tail row and deterministically pad only the missing rank slots by cycling already-present tail rows so all ranks execute the same number of collectives. Under this exact-coverage combination, the training loop must fail immediately if token-budget selection would skip a sample, drop a candidate-batch tail, or produce no executable sample. This is memory-safe under the admitted budgets: audited easy/medium/hard/long maximum raw frames are `900 / 1,800 / 3,000 / 9,644`, all below `24,000`, and the labeled-D maximum `num_frames + CTC tokens` is `2,066`, below `8,000`. Coverage receipts must bind both no-drop settings and report deterministic padding separately from unique-row exposures. With `batch=36`, frame budget `24,000`, and world size `4`, the exact ceil-divided A/B/C easy/medium/hard/long counts are `9,982 / 334,571 / 322,105 / 35` steps per epoch, with `633 / 744 / 986 / 296` padded rank-slot samples per epoch. Each A/B/C phase therefore runs `29,946 / 1,003,713 / 966,315 / 105` segment steps, `2,000,079` steps total, exposes every one of the `62,072,225` unique rows once per epoch for three epochs, and records `7,977` deterministic padding samples separately. Stage D at `batch=12`, frame budget `8,000`, and world size `4` must run `12,045` ceil-divided steps for its one labeled epoch, expose all `283,868` train rows, and record `368` deterministic padding samples. The already-running 30,064-step calibration predates this correction and remains calibration only; it cannot create a full-data coverage receipt.
- 2026-07-26 Stage211A recovery 20k hidden diagnostic: on the same immutable 256-row fixed split, step `0 -> 10k -> 20k` total mixer objective is `0.434316 -> 0.379054 -> 0.364505`; mean 70-layer mixer loss is `0.408430 -> 0.355732 -> 0.341902`, and mean cosine is `0.860179 -> 0.876595 -> 0.881577`. Step 20k improves loss and cosine in all `70/70` layers relative to both step 0 and step 10k. The weak `10-19` band improves loss/cosine `0.720392/0.751160 -> 0.612329/0.785954`, and `20-29` improves `0.532876/0.819174 -> 0.434040/0.849429`. This is a continued hidden-alignment signal, not a WER/CER promotion result; finish all `30,064` calibration steps, select only from complete fixed reports, and run the full normalized five-dataset public benchmark before using the checkpoint to initialize exact-tail full Stage A.
- 2026-07-26 Stage211 exact-decode coverage correction: exact-tail batching and no token-budget truncation are insufficient if the WebDataset training loader still silently replaces or omits rows whose tar member, metadata, audio decode, or feature extraction raises an exception. Formal Stage211 A/B/C/D and their full-profile smoke runs must therefore set an explicit `webdataset_skip_decode_errors=false`; the DeepSpeed loader must honor that setting instead of forcing permissive skipping. Any decode failure must terminate the segment before a coverage receipt can be issued, so the offending row can be repaired or explicitly removed by rebuilding and re-auditing the immutable corpus rather than being hidden inside a nominally complete run. Curriculum and SFT provenance/coverage receipts must bind this fail-fast setting, and phase gates must reject any receipt that allows decode skipping. The already-running 30,064-step calibration predates this correction and remains calibration-only; its log contains zero reported corrupt-sample skips through the current progress, but it still cannot prove formal full-data coverage.
- 2026-07-26 Stage211 public-evaluation utilization correction: keep the first four public datasets in one-dataset-per-GPU mode, but shard the second-stage `24,774`-utterance WenetSpeech TEST_NET evaluation deterministically across all four GPUs. Merge every shard without filtering or deduplication and retain the existing exact utt-id coverage, shared `ctc` normalization, greedy decode, and Nano-comparison gates. Formal calibration/A/B/C/D evaluation launchers must set `CTC_SHARD_STAGE2=1`; this changes only evaluation wall time and cannot change the model, references, metric contract, or required sample count.
- 2026-07-26 Stage211A calibration completion and full public baseline: the uninterrupted calibration consumed all `30,064/30,064` easy batches. On the immutable 256-row hidden split, step 30k is the selected checkpoint because all `70/70` mixer layers improve both loss and cosine versus step 0 and it has the lowest complete fixed loss: total loss `0.434316 -> 0.356260`, mean 70-layer mixer loss `0.408430 -> 0.334212`, and mean cosine `0.860179 -> 0.884576`. The final step 30,064 is slightly worse on total loss (`0.356350`) and is not selected. Complete normalized greedy-CTC evaluation covers exactly AISHELL-1 `7,176`, LibriSpeech clean `2,620`, LibriSpeech other `2,939`, Common Voice 22 EN `16,396`, and WenetSpeech TEST_NET `24,774` utterances, with zero normalized-reference mismatches against the direct Nano caches. Native Nano versus the selected BiRWKV checkpoint CER/WER is respectively `4.183/57.256`, `7.903/28.036`, `11.962/50.835`, `21.513/57.558`, and `8.631/98.394%`; every dataset fails the `<=1.20x` and `<=3`-point gate. The student deletion rates are `39.630/11.345/28.789/34.152/96.166%`, so the present public failure is dominated by missing emissions, especially on WenetSpeech, despite genuine hidden-state improvement. This checkpoint is a calibrated initialization for strict full-data Stage A, not a usable CTC endpoint and not a reason to alter the BiRWKV architecture.
- 2026-07-26 Stage211 restart-safe supervisor recovery: the first post-calibration supervisor exited after writing all five prediction files and `metrics.json` because its long-running Bash source was edited while Bash was still reading the file, producing a mixed old/new tail and an unmatched-quote EOF. Current checked-in scripts pass syntax validation; do not rerun the completed `53,905` audio inferences. A recovery may reuse those predictions only after reselecting and hashing the exact step-30k checkpoint, verifying all five exact line counts, recomputing the Nano comparison to prove identical utterance coverage and normalized references, and checking the comparison's checkpoint SHA. Long-running A/B/C/D orchestration must execute an immutable runtime snapshot so later source updates cannot mutate an active process. This is an orchestration/reproducibility correction only and does not change the model, losses, curriculum, phase gates, or required data exposure.
- 2026-07-26 Stage211 full-profile smoke provenance repair: the recovered Stage A four-GPU smoke passed the `564/564` exact Nano non-attention audit and loaded the selected step-30k checkpoint with `missing=0/unexpected=0`, then failed before its first training batch while binding fixed-eval provenance. `_resolve_bucket_manifest_path` intentionally returns a configuration string, but `_build_step_eval_provenance` required a `Path` and called `.resolve()` directly. Normalize the accepted `str | Path` at the provenance boundary and cover both forms in unit tests before relaunching from the original checkpoint. The failed smoke produced no optimizer step or checkpoint and does not count as data exposure; this is a provenance type-normalization bug only, not a model, data, teacher, or memory failure.
- 2026-07-26 Stage211 smoke retry-log isolation: after the provenance repair, the repeated smoke completed both optimizer steps with online layer match `24/24` and `30/30`, zero teacher misses/frame deltas, peak reserved memory `6.02 GiB`, and a valid step-2 checkpoint. Promotion was nevertheless rejected because the shared append-only smoke log still contained the previous attempt's traceback. Keep append-only logs for diagnosis, but define the auditable attempt as the suffix beginning at the last distributed-initialization marker; every success requirement and bad-pattern scan must apply to that same latest attempt. A current-attempt error still fails closed, while an older preserved failure cannot invalidate a later clean rerun.
- 2026-07-26 Stage211A exact-coverage formal launch: the recovered immutable supervisor admitted the four-GPU full-profile smoke and started the standardized `easy` mixer segment from the selected calibration step 30000 checkpoint. The formal config keeps the Nano MLP/Norm, five-layer CTC decoder, and CTC head frozen, enables only teacher-forced mixer hidden alignment, uses local batch `36`, frame budget `24,000`, `length_bucket_drop_last=false`, `skip_oversized_samples=false`, and `webdataset_skip_decode_errors=false`, and targets exactly `29,946 = 9,982 * 3` optimizer steps. Initial fixed 256-row loss is `0.3258`; through the observed step 660 all online layer rows matched with zero missing rows/frame delta and the log contained no non-finite loss, OOM, CUDA/NCCL failure, decode error, token-budget truncation, or skipped sample. A direct two-rank regression now executes the distributed padded-tail loader for three consecutive epochs under source-interleave mode and proves that every original row remains present in each epoch while only deterministic rank-slot padding is repeated; the exact-tail, token-budget fail-fast, and decode fail-fast checks pass together. This launch is only Stage A/easy progress. It cannot admit Stage B until all four A curricula finish and the complete normalized `53,905`-utterance English-WER/Chinese-CER plus hidden-alignment gate passes.
- 2026-07-26 Stage211 stepwise final-results contract: the final labeled-SFT gate alone is not a sufficient user-facing proof of the requested ordered alignment because it directly presents only the selected C baseline and D result. After D passes, require one immutable JSON report plus a Markdown table that directly bind the calibration reuse receipt, A/mixer phase gate, B/block phase gate, C/logits phase gate, and D/SFT final report by absolute path and SHA-256. Revalidate every embedded complete public benchmark, enforce the exact checkpoint chain `calibration -> mixer -> block -> logits -> sft`, and render normalized AISHELL/WenetSpeech CER plus LibriSpeech/Common Voice English WER for Nano and every student stage. The report may be emitted only when all A/B/C/D gates pass and must fail closed on a missing stage, changed hash, incomplete benchmark, wrong stage order, or checkpoint-chain mismatch. This reporting addition does not change model architecture, data order, losses, optimizer state, or promotion criteria.
- 2026-07-26 Stage211A first persistent recovery point: the exact-coverage easy run wrote `step-2000.pt` SHA-256 `422cf018d6500fc81d10c1ce7b91cd79ef0492b46fb61a87a0cbd7cf6706f5c4` plus a complete `2.9 GiB` ZeRO-1 directory with one model state and four optimizer-state shards. Both `latest_checkpoint.yaml` and DeepSpeed `latest` select `step-2000`; the exported state and DeepSpeed module contain the same `12,697` tensor keys with zero missing or changed tensors. DeepSpeed client state and the latest pointer independently record `step=2000`, `epoch=1`, and `epoch_batch_offset=2000`, so a restart will replay exactly the remaining first-epoch schedule instead of beginning a new epoch or skipping the prefix twice. Training continued beyond step 2000 with complete teacher/layer matching and no frame delta, coverage truncation, decode error, non-finite loss, OOM, CUDA, or NCCL failure.
- 2026-07-26 Stage211 post-remount full-curriculum storage preflight: with `/media/usbhd` restored read-only, reloading all four fixed-eval manifests found `749` readable bucket-part files and zero missing files. Recomputed `rows / steps-per-epoch / deterministic-tail-padding` are exactly `1,174,987 / 9,982 / 633` for easy, `42,247,508 / 334,571 / 744` for medium, `18,649,326 / 322,105 / 986` for hard, and `404 / 35 / 296` for long, and every manifest still binds the same 256-row hidden-eval split. The four source length indexes are readable, and all `702` unique first/last shard boundary references recorded across the bucket parts exist. This preflight proves the restored metadata and recorded shard boundaries are available; it does not weaken the formal per-row fail-closed decode policy, which must continue to reject any later missing or corrupt audio instead of silently skipping it.
- 2026-07-26 Stage211 layer-sampling coverage audit: sparse training logs appeared to overrepresent seven layer bands because the logger emits the first ten steps and then every tenth step, while the eight-of-70 cyclic layer sampler repeats every 35 optimizer steps. Replaying the actual per-step schedule through step 2460 gives all 70 layer IDs `281` or `282` selections, with spread one; over every complete 35-step cycle each layer is selected exactly four times. Keep this exact uniform-cycle invariant under regression test. Do not infer layer starvation from the aliased ten-step telemetry or change a healthy formal run on that basis.
- 2026-07-26 Stage211 frozen-path checkpoint-delta gate: a direct comparison of the selected calibration checkpoint with formal A/easy step 2000 confirms all `564/564` Nano MLP/Norm tensors, all `84/84` five-layer CTC-decoder tensors, both CTC-head tensors, and every other non-operator tensor remain bit-identical. Changes are confined to BiRWKV TimeMixer parameters and the three state-dict aliases of layer 0 `input_proj`; this projection is part of the documented `norm1 -> input_proj -> BiRWKV TimeMixer` operator-local path and performs the necessary `560 -> 512` adaptation, so it is not frozen-path drift. Every completed A/B/C curriculum-segment receipt must now recompute the complete start/end checkpoint delta, require identical tensor keys/shapes/dtypes, require at least one allowed operator tensor to change, and fail closed if any tensor outside `.time_mixer.` or `.input_proj.` changes. The receipt must also bind `freeze_encoder_except_time_mixer=true`, `freeze_ctc_decoder=true`, `freeze_ctc_head=true`, and zero weight decay. This is a promotion-proof invariant and does not modify the active optimizer or model architecture.
- 2026-07-26 Stage211A second persistent recovery point: `step-4000.pt` SHA-256 `7fbd53df0d094073b0cdf6bd72c2f93a5d0cd7254bbd0423656febae03dcded1` and its complete ZeRO-1 directory were written before training continued past step 4150. The latest pointer, exported checkpoint, and DeepSpeed client state all record `step=4000`, `epoch=1`, and `epoch_batch_offset=4000`; exported and sharded model states contain the same `12,697` tensor keys with zero missing, unexpected, or changed tensors. All four optimizer shards report partition counts `[4,4]`, optimizer step 4000, two FP32 groups, and no overflow. Relative to the phase initialization, `10,449` allowed TimeMixer/input-projection tensor aliases have changed while all `1,774` frozen tensor aliases remain bit-identical and no forbidden path changed; the independent step-2000 to step-4000 delta gives the same zero-drift result. Logged 500-step means improve from loss/cosine `0.365086/0.871066` at steps 1-500 to `0.302988/0.891504` at steps 3501-4000 despite expected batch-level variation.
- 2026-07-26 Stage211 public-metric regression contract: the phase-level Nano comparison must prove the configured primary metric through the actual comparison entrypoint, not only through isolated tokenizer tests. AISHELL-1 and WenetSpeech must select corpus-level normalized Chinese CER; LibriSpeech and Common Voice English must select corpus-level normalized WER. CTC normalization must remove non-pronounced noise/music annotations, subtitle/control tokens, language-confirmation prefixes, and speaker metadata consistently from both references and predictions before scoring. The comparison must fail closed on duplicate utterance IDs before metric aggregation. Keep explicit regression cases for a one-character Chinese deletion, normalization-equivalent Chinese records, and duplicate IDs so every later A/B/C/D public report has a tested metric and coverage contract. This audit changes no model, loss, optimizer, active training process, or benchmark threshold.
- 2026-07-26 Stage211 public-metric regression result: the direct comparison tests now exercise both Chinese dataset routes, prove that one deletion from `语音识别` scores as `25%` CER and deletion rate, prove normalization-equivalent Chinese control/noise/speaker records score as zero error, and reject duplicate utterance IDs. The comparison plus shared text-metric suite passes `24/24`. Re-running the current entrypoint over all `53,905` cached real-audio predictions produced `/home/yueyulin/rwkvasr_eval/stage211_metric_contract_audit_20260726/nano_comparison.json`, byte-identical to the selected calibration report with SHA-256 `f581dbe041bf550168dbaccb12c7670bb36bfe42a4d6662192f0bb9428640c62`; all five exact sample counts and zero normalized-reference mismatches remain unchanged. This validates the metric path only and does not promote the still-failing calibration checkpoint.
- 2026-07-26 Stage211 supervisor chain-binding correction: the persistent shell controller already executes `mixer -> block -> logits -> labeled SFT`, compares A against calibration, B against A, and D against C, while the C finalizer enforces the direct Nano gate. The SFT finalizer must additionally receive the controller's exact `CALIBRATION_REUSE_RECEIPT` path rather than rediscovering it through a default, so an intentional `CALIBRATION_EVAL_DIR` or receipt override cannot make the final stepwise report bind a different calibration artifact. Keep a source-level regression assertion for the ordered phase calls, all three baseline links, and explicit calibration-receipt forwarding. The active formal run executes an immutable older shell snapshot with the same current default receipt path and SHA-256 `33c76474988c3403f262ea8a6af1e3757ce25af3ffd9dfdae99624cba9449b67`, so this future/recovery robustness correction must not mutate or restart it.
- 2026-07-26 Stage211 local-teacher path binding correction: the strict runner accepts and audits an explicit Nano `model.pt`, but its generated config still inherited the historical Stage206 `ctc_teacher_online_model_path` under `/media/usbhd`. Derive the online teacher directory from the explicit `--nano-checkpoint` parent for every newly generated A/B/C/D config so model loading uses the complete persistent NVMe copy instead of adding an avoidable USB-enclosure dependency. Keep a config-level regression assertion that a custom checkpoint path overrides the inherited legacy directory. The currently running A/easy process has already loaded the same Nano teacher and must not be restarted or have its live config edited; the correction takes effect for subsequent fresh curriculum segments and any future recovery-generated config.
- 2026-07-26 Stage211A third persistent recovery point and monitor cadence: `step-6000.pt` SHA-256 `8fec93a7333ce7de34575f0c1c728599f35c903ad655c2580ac5c159d1b794fe` is bound by a latest pointer recording epoch `1`, offset `6000`, and the complete `step-6000` ZeRO-1 directory. Export and DeepSpeed module contain the same `12,697` tensors with zero missing, unexpected, metadata-changed, or value-changed tensors; all four optimizer shards report optimizer step `6000`, partition counts `[4,4]`, two FP32 groups, and no overflow. Relative to the formal phase initialization, `10,788` allowed TimeMixer/input-projection aliases changed (`567,876,096` numel), `135` allowed aliases remained equal, all `1,774` frozen aliases remained bit-identical, and no forbidden path changed. Rolling retention correctly removed step 2000 and keeps only complete step 4000/6000 export plus ZeRO states; training continued past step 6060. Mean online loss/cosine at steps `5501-6000` is `0.280576/0.899330` versus `0.365086/0.871066` at steps `1-500`. Per user direction, replace the ten-minute observer with tmux `rwkvasr_stage211_abcd_hourly_monitor`, logging once every `3600` seconds; checkpoint and fixed-eval gates remain unchanged.
- 2026-07-26 Stage211 online-teacher identity receipt contract: binding only `train_config.yaml` does not prove which Nano weights supplied the online hidden/logit targets because the config records a model directory rather than the loaded file identity. Every completed A/B/C curriculum-segment receipt and the D labeled-SFT completion report must resolve `ctc_teacher_online_model_path/model.pt`, require a non-empty regular file, and bind its absolute path plus SHA-256. Each A/B/C phase gate must rehash every bound teacher checkpoint and require one identical teacher SHA across easy, medium, hard, and long, while allowing the path to move between the verified USB and NVMe copies. A missing, changed, or mixed-version teacher must fail promotion. Derive this evidence from the persisted train config so the active immutable controller remains compatible and no running process or optimizer state is changed.
- 2026-07-26 Stage211 phase-objective receipt contract: a train-config hash proves immutability but does not by itself prove that a segment used the objective assigned to its named stage. Define one executable objective contract for `mixer`, `block`, `logits`, and `sft`, covering the project vocabulary/tokenizer and `60514 -> 60515` teacher/project blank mapping, learning rate, label availability and CTC weight, teacher-forced versus stacked input, every active hidden/output loss weight, sampled/boundary-layer policy, full-logit residency, pronunciation-only suppression, zero FFN/channel-mixing substitution, frozen Nano non-attention/decoder/head paths, and zero weight decay. Receipt creation and every A/B/C phase gate must reload and validate the persisted config against the named phase; D must perform the same validation before writing and whenever reading its completion report. Any missing, mistyped, stale, or cross-phase objective setting fails closed. This is an evidence/gating correction and must not alter the active run's already-correct objective or optimizer state.
- 2026-07-26 Stage211 phase-objective receipt verification: the executable contract binds `63` typed settings per phase, including project `vocab_size=60515`, project blank `60515`, Nano blank/ignored id `60514`, SenseVoice tokenizer identity, and CTC normalization. The persisted active A/easy config passes the complete mixer contract, and generated mixer/block/logits/SFT configs all pass their respective contracts; a block config changed back to teacher-forced input is rejected. Receipt creation, A/B/C phase-gate revalidation, and D completion write/read validation now share this contract. The teacher-identity, strict-chain, receipt, and SFT regression subset passes `47/47`; Ruff, bytecode compilation, and diff checks pass. No training config, live process, loss, or optimizer state was changed.
- 2026-07-26 Stage211 cross-phase teacher continuity contract: requiring one Nano SHA within each phase does not prevent a different teacher version from being introduced at `A -> B`, `B -> C`, or `C -> D`. Every promotion receipt must extract and bind the unique Nano teacher checkpoint SHA from its fully validated source-phase coverage. Receipt validation must rederive that SHA from the immutable gate report, and every fresh target-phase launch must hash its explicit `--nano-checkpoint` and require equality before training. Curriculum continuations must likewise compare the predecessor segment's bound teacher SHA with the explicit checkpoint used for the next difficulty. Paths may change between the verified USB and NVMe copies, but bytes may not. This fail-early continuity check changes no model, loss, data, or active process.
- 2026-07-26 Stage211 cross-phase teacher continuity verification: promotion-receipt construction now carries the source gate's unique Nano SHA, receipt reads rederive it from the fully validated coverage, and fresh target-phase plus curriculum-continuation entrypoints compare their explicit teacher checkpoint before launch. Regression tests admit the same teacher, reject different bytes for both phase and difficulty transitions, and retain all existing checkpoint/gate tamper checks; the focused Stage211 suite passes `48/48`, with Ruff, bytecode compilation, and diff checks clean. The active A/easy process and persisted config were not changed.
- 2026-07-26 Stage211 final teacher-chain evidence contract: runtime admission checks are necessary but the final stepwise WER/CER artifact must independently prove the same teacher continuity. The D finalizer must validate the C-to-D promotion receipt against the teacher checkpoint bound by `sft_complete.json`, bind that teacher path/SHA into `stage211_complete.json`, and revalidate both on read. The final calibration/A/B/C/D report must extract the unique teacher SHA from every validated A/B/C coverage plus D, require one value across all trained phases, expose it at top level and in each trained-stage record, and fail on any mismatch. Calibration remains a separately bound baseline and is not counted as a completed full-data phase.
- 2026-07-26 Stage211 final teacher-chain evidence verification: the SFT finalizer now rechecks the actual D teacher against the C-to-D promotion receipt and binds it into the final completion report. The stepwise JSON/Markdown revalidates A/B/C coverage plus D, records `nano_teacher_chain_passed=true` and the unique teacher SHA, and rejects a synthetic block-stage teacher substitution while retaining the strict checkpoint-chain and public-metric checks. The focused Stage211 suite remains `48/48`; Ruff, bytecode compilation, and diff checks pass. This affects only future receipts/reports and does not alter the running A/easy job.
- 2026-07-26 Stage211 runtime epoch-coverage contract: manifest cardinality plus `max_steps` is insufficient direct evidence that every planned epoch reached its natural loader boundary. The DeepSpeed loop already emits `epoch-N.pt` only after loader exhaustion and records `step`, `extra.epoch`, zeroed `extra.epoch_batch_offset`, and `extra.completed_epoch_batch_count`. Every A/B/C segment receipt must bind and validate all three epoch checkpoints, requiring cumulative steps `steps_per_epoch * N` and exactly `steps_per_epoch` completed batches for each epoch. D must bind the analogous single labeled epoch checkpoint with `12,045` completed batches. Phase and SFT gates must rehash every bound epoch artifact and reject missing, partial, overrun, stale-offset, or count-mismatched evidence. Keep manifest/no-drop/decode-fail-fast proofs alongside this runtime evidence; neither substitutes for the other.
- 2026-07-26 Stage211 runtime epoch-coverage verification: add a shared epoch-checkpoint auditor and gate validator, bind its records into every A/B/C segment receipt and D completion, require curriculum admission to validate the preceding runtime record, and require phase reports to preserve the original receipt record exactly. Synthetic complete `3 x 4` and D-style one-epoch checkpoints pass; a `3/4` partial epoch, altered phase count, or altered SFT count fails. The Stage211 receipt/runner/SFT suite passes `51/51`; Ruff, bytecode compilation, and diff checks pass. The current A/easy process already emits the required epoch artifacts naturally, so no restart or live configuration change is needed.
- 2026-07-26 Stage211 Nano public-baseline provenance contract: unchanged Nano prediction hashes across phase reports prove benchmark consistency but do not identify the `model.pt` that generated the cached predictions. Require one immutable provenance receipt for the complete five-dataset Nano direct-CTC baseline. The receipt must bind the non-empty Nano `model.pt` by absolute path and SHA-256; bind every original per-dataset inference report, prediction JSONL, and public manifest by absolute path and SHA-256; validate the report's Nano model directory, direct `greedy_ctc` decode, CTC normalization, language, exact sample count, manifest path, and prediction path; and require complete utterance coverage with normalized references matching the manifest. Every A/B/C phase gate and the D/final stepwise gate must revalidate this receipt, require its prediction and manifest bindings to equal the benchmark bindings, and require its Nano checkpoint SHA-256 to equal the unique online-teacher SHA-256. Existing cached predictions may receive a legacy attestation only from their original reports plus the still-resolved model path; all newly generated Nano reports must record the checkpoint path and SHA-256 at inference time. Use a default receipt path so the immutable active supervisor remains compatible. This is an evaluation-provenance correction only and must not restart or alter training.
- 2026-07-26 Stage211 complete-Nano default correction: the provenance audit reconfirmed that the historical repository asset is the incomplete `1,971,149,431`-byte text checkpoint (`55ae0d2f...`) without the Nano CTC decoder/head, while the cached public CTC baseline and admitted online teacher use the complete `2,127,426,538`-byte ModelScope checkpoint (`81fec861...`). The full-phase controller, labeled-SFT controller, immutable supervisor, and active A/easy config already select the complete checkpoint, but the single-segment strict runner still exposes the incomplete repository asset as its fallback. Change that fallback to `${RWKVASR_NANO_CHECKPOINT:-~/models/Fun-ASR-Nano-2512-modelscope/model.pt}` and retain explicit per-launch checkpoint hashing. Reject the incomplete asset through the new public-baseline/online-teacher hash gate. This correction applies only to future direct launches; the active A/easy process remains on the verified complete USB copy and must not restart.
- 2026-07-26 Stage211 Nano public-baseline provenance verification: deep validation of all five original Nano inference reports, public manifests, and cached predictions passed for exactly `53,905` utterances with identical ID coverage and zero normalized-reference mismatch. The immutable legacy-attestation receipt is `/home/yueyulin/rwkvasr_eval/stage211_public_full/nano_2512/provenance_receipt.json`, SHA-256 `1309c7a42bb8f38c5a471acea5a54f16d1446a629358abf25594ddc7f553305b`; it binds the complete ModelScope `model.pt` SHA-256 `81fec8616083c69377f3ceef36aba3655660ee0ca69a5d4a1e9810cd340ca499`. New Nano inference reports embed the checkpoint path/SHA directly, and the public-Nano runner emits the provenance receipt automatically. A/B/C phase gates, D finalization, and the final stepwise report now require one matching receipt and reject prediction substitution, inference-report mutation, a different public-baseline teacher, or a cross-stage provenance substitution. The focused Stage211/public-CTC suite passes `66/66`; Ruff, shell syntax, bytecode compilation, and diff checks pass. The active process, config, optimizer, and data order were not changed.
- 2026-07-26 Stage211C independent logits-alignment gate contract: a passing five-dataset WER/CER result is necessary but does not by itself prove that Phase C performed the requested final-logit alignment. Phase C must therefore emit an immutable `logits_alignment_gate` from its exact easy-segment step-0 fixed report and long-segment final fixed report over the same SHA-bound 256 utterances. Require finite full-vocabulary KL, conditional-nonblank KL, blank binary KL, blank-probability MAE, all-frame and active-frame top-1 agreement, CTC token error/deletion rates, nonblank-rate ratio, collapsed-length ratio, sequence exact rate, and coverage counters. Promotion requires at least 5% reduction in both full KL and conditional-nonblank KL, non-increasing blank KL/MAE and deletion rate, strictly improved all/active top-1 and token ER, non-decreasing sequence exact rate, nonblank and collapsed-length ratios within `0.90-1.10` and no farther from one, identical teacher/frame/token totals, exactly 256 matched utterances, zero missing utterances, and zero mean frame delta. The C phase gate must pass both this independent fixed-logit gate and the complete normalized public Nano WER/CER gate. A/B/C gate validation must also rehash and revalidate the bound alignment report rather than trusting copied top-level booleans. This evidence change applies to future Phase C finalization and does not alter the active Phase A process.
- 2026-07-26 Stage211 fixed-feature evaluation determinism: the FunASR-compatible frontend retains Nano's training-time `dither=1.0`, so identical audio materialized in separate phase processes is not necessarily represented by identical features unless the DataLoader worker seed is fixed. Add an evaluation-only `step_eval_feature_seed` (default `0`) and create the cached fixed-evaluation iterator inside a CPU RNG fork seeded by `feature_seed + distributed_rank`; restore the caller's RNG state after materialization. This must make the SHA-bound 256-utterance baseline/candidate reports comparable across processes while leaving train-loader randomness, model stochasticity, fixed-audio membership, and the active already-loaded Stage A process unchanged. Do not add this backward-compatible default to the immutable Stage211 phase-config key contract.
- 2026-07-26 Stage211C logits-gate/determinism verification: all four curriculum manifests resolve their `eval` split to the same 256-row part (`9f4bf09c...`). A direct FunASR WavFrontend check produced bit-identical LFR features at the same seed (`max_delta=0`) and different features at a different seed (`max_delta=0.938919067`). The independent logits gate, phase-gate source revalidation, fixed-feature seed handling, Nano provenance, public comparison, curriculum receipts, SFT controls, runner, and relevant DeepSpeed helpers pass the focused suite with `106 passed, 1 skipped, 1 deselected`; the skipped test is the optional environment-dependent single-process DeepSpeed smoke. Ruff, Python compilation, shell syntax, and diff checks must pass before publishing. The running Stage A process and its immutable config remain untouched.
- 2026-07-26 Stage211 phase-final alignment re-evaluation contract: reports emitted by different training processes are insufficient evidence for a strict phase-wide hidden/logits comparison, and the active Stage A easy baseline predates the fixed-feature seed. Before every A/B/C phase gate, independently evaluate the exact phase-initialization checkpoint and exact final long checkpoint in one process over one materialized copy of the shared SHA-bound 256-row eval part with `step_eval_feature_seed=0`. Reuse the same cached host batches, phase objective, model config, and online Nano teacher for both evaluations; run with dropout/specaugment disabled and no parameter update. Each report must bind its role, logical gate step, actual checkpoint step/path/SHA-256, train/model config path/SHA-256, Nano `model.pt` path/SHA-256, fixed-eval manifest/part hashes, feature seed, complete hidden/decoder/logits metrics, and exact 256-sample coverage. Hidden/logits gates must require the supplied baseline checkpoint explicitly, verify both report bindings, and reject reports copied from in-training cross-process evaluation. This post-phase evidence pass may run only after training releases the GPU and must not restart or alter the active Stage A process.
- 2026-07-26 Stage211 phase-final paired-evaluation verification: the finalizer now materializes the fixed 256-utterance feature batches exactly once at seed `0`, reuses the same host-batch object and one Nano teacher for phase-initial and final checkpoints, and emits immutable checkpoint/config/teacher/data-hash-bound source reports before hidden/logits and public gates. The offline evaluator requests all teacher layer/decoder hiddens and full log probabilities required by the active phase. Gate reconstruction rejects cross-process legacy reports, changed checkpoints/configs/teacher/data, mismatched pair IDs/provenance, nonzero feature seeds, incomplete 70-layer hidden coverage, incomplete logits coverage, and a baseline other than the easy-segment initialization. The focused public-CTC, receipt, A/B/C/D runner, pair-evaluation, gate, and DeepSpeed suite passes `111 passed, 1 skipped, 1 deselected`; Ruff, bytecode compilation, and diff checks pass. The active Stage A process, config, optimizer, and data order remain unchanged.
- 2026-07-26 Stage211A easy step-10000 fixed gate and 23:23 hourly health check: the uninterrupted four-GPU mixer run completed epoch 1 at the natural `9,982`-batch boundary and reached monitored step `13,090/29,946` (`43.71%`) with no CUDA/NCCL/OOM/non-finite error. The process-created cached 256-utterance host feature set was reused from its step-0 baseline through the step-10000 gate. Fixed total loss improved `0.325849 -> 0.302217` (`-7.25%`), mean 70-layer mixer loss improved `0.317921 -> 0.294805` (`-7.27%`), and mean cosine improved `0.888589 -> 0.896328`; all `70/70` layers improved in both loss and cosine, including loss reductions of `6.68%` and `7.57%` in the difficult `10-19` and `20-29` bands. Teacher missing counts and frame deltas remain zero. The complete step-12000 export plus four optimizer shards is the latest recovery point. All four GPUs remain active at approximately `8.6 GiB` each, root has `883 GiB` free, the read-only USB dataset mount has `365 GiB` free, and W&B run `miqj7iik` continues returning HTTP `200`. This is an intermediate same-process gate, not the formal phase promotion: after easy/medium/hard/long all finish, the new seed-0 paired evaluator must independently re-evaluate the exact phase initialization and final checkpoint before public WER/CER promotion.
- 2026-07-26 Stage211 downstream-chain and data preflight: the active immutable supervisor snapshot invokes the current repository Python controllers and preserves the fail-closed order `A mixer -> A paired/public gate -> B block -> B paired/public gate -> C logits -> C paired/Nano gate -> D labeled SFT -> final stepwise report`. All `745` A/B/C training part files, four shared-eval references, and `702` manifest first/last shard endpoint checks resolve on the mounted data; the common fixed-eval part is identical across all four difficulties. Recomputing the loader schedule from each manifest with `batch=36`, world size `4`, frame budget `24,000`, and no tail dropping exactly reproduces per-epoch steps `9,982/334,571/322,105/35` and deterministic padding `633/744/986/296`. The labeled-D root also resolves all `51` part files, `19` shard endpoints, and the complete `283,868`-row train index. `finalize_stage211_labeled_sft.py` automatically revalidates calibration/A/B/C/D checkpoint, teacher, Nano-baseline, data-coverage, and public-metric chains before writing immutable `stage211_stepwise_results.json` and Markdown. No training process, config, checkpoint, data order, or mounted dataset was modified by this read-only preflight.
- 2026-07-26 Stage211A step-12000 parameter-boundary audit: direct mmap comparison against the immutable phase initialization finds the same `12,697` state tensor keys and metadata, `10,899` changed TimeMixer/input-projection aliases (`567,932,928` aliased numel), `1,774` bit-identical frozen aliases, and zero forbidden-path changes. On the unique `encoder.sensevoice_encoder.layers` path, `3,633/3,641` TimeMixer/input-projection tensors cross a BF16 export value across all seven ten-layer bands; the aggregate relative L2 movement over the canonical `189,378,560` trainable parameters is `0.0125043`, with band values `0.014867/0.013643/0.012511/0.010408/0.010952/0.014724/0.012458`. Six of the eight byte-identical physical export tensors are layer-0 `v0/v1/v2`, which the upstream `layer_id == 0` v-first rule structurally bypasses. The other two are forward layer-69 `k_a/k_k`; their DeepSpeed FP32 masters prove that all `512/512` elements did update, at relative L2 `3.99e-4/5.73e-4`, but had not crossed a BF16 export quantization interval. This rules out a layer-69 gradient bug and proves the current gain comes from broad BiRWKV operator learning while copied Nano MLP/Norm/CTC decoder/head state remains frozen.
- 2026-07-27 Stage211A easy step-20000 fixed gate and epoch-2 completion: the natural second-epoch boundary completed at step `19,964 = 9,982 * 2`, with full-epoch train loss improving from `0.295399` in epoch 1 to `0.278318` in epoch 2. The same process reused the identical SHA-bound 256-utterance cached feature set for step `0/10k/20k`. Fixed total mixer objective improved `0.325849 -> 0.302217 -> 0.283386`; step 20k is `13.03%` below initialization and `6.23%` below step 10k. Mean 70-layer mixer loss improved `0.317921 -> 0.294805 -> 0.276354`, while mean cosine improved `0.888589 -> 0.896328 -> 0.902614`. Every one of the `70/70` layers improves both loss and cosine relative to initialization and independently relative to step 10k; from 10k to 20k, all ten-layer bands reduce loss by `5.71-7.03%`, including `5.71%` at layers `10-19` and `6.70%` at layers `20-29`. The complete step-20000 recovery point contains the exported model, one model-state shard, and all four optimizer-state shards; the latest pointer records epoch 3 offset 36, proving the final easy epoch began after the natural boundary. Online rows continue with zero teacher missing count and zero layer frame delta. This is healthy continued convergence, but it remains an intermediate easy-segment signal rather than the Stage A promotion result; finish step `29,946`, then medium/hard/long, followed by the independent paired hidden gate and complete normalized public WER/CER evaluation.
- 2026-07-27 Stage211A easy exact-coverage completion and medium launch: the uninterrupted easy segment completed exactly `29,946 = 9,982 * 3` optimizer steps and emitted all three natural epoch checkpoints at steps `9,982/19,964/29,946`, each recording `9,982` completed loader batches and zero residual epoch offset. It consumed all `1,174,987` unique rows three times (`3,524,961` row exposures, `4,470.357 h`) plus only the declared deterministic distributed-tail padding (`633` samples per epoch, `1,899` total); full-epoch train loss improved monotonically `0.295399 -> 0.278318 -> 0.263754`. On the same process-cached SHA-bound 256 utterances, fixed total loss at step `0/10k/20k/29,946` is `0.325849/0.302217/0.283386/0.270618`, mean 70-layer mixer loss is `0.317921/0.294805/0.276354/0.263852`, and mean cosine is `0.888589/0.896328/0.902614/0.907003`; final versus initialization improves loss/cosine in all `70/70` layers, with mean mixer loss down `17.01%`. The receipt parameter audit finds `10,905` allowed changed TimeMixer/input-projection aliases (`567,936,000` aliased numel), all `1,774` frozen aliases bit-identical, and zero forbidden changes. Final checkpoint SHA-256 is `2383cdfbe856e4c75e65d737fdd39182fa7508a34b72e9e461532209b60df781`; immutable receipt SHA-256 is `128176ae8938607d9de769c837f53a51e6c7e9487572ac2556d90f7457d534d1`, bound to Nano teacher SHA-256 `81fec8616083c69377f3ceef36aba3655660ee0ca69a5d4a1e9810cd340ca499`. The strict controller accepted that receipt and launched medium from the exact easy final checkpoint over `42,247,508` rows, targeting `1,003,713 = 334,571 * 3` steps; its independently seeded fixed baseline is `0.270969`, and initial online rows have zero missing teachers/frame delta. The easy in-process diagnostic remains non-promotional because its host features predate the cross-process seed contract; the formal Stage A decision still requires medium, hard, and long completion followed by the one-process seed-0 paired hidden evaluation and complete normalized English WER/Chinese CER gate.
- 2026-07-27 Stage211A medium step-10000 cross-domain diagnostic: the deterministic seed-0 256-utterance audit is a real early retention regression rather than frontend noise. Fixed total loss changes `0.270969 -> 0.276318` (`+1.97%`), mean 70-layer mixer loss `0.264180 -> 0.269865` (`+2.15%`), and mean cosine `0.906886 -> 0.905928`; only `20/70` layers improve loss and `18/70` improve cosine, with the largest band loss increases at layers `30-39` (`+7.09%`) and `50-59` (`+5.79%`). Training itself is functioning: every one of the seven cyclic eight-layer groups improves its online loss/cosine from the first 2k to the latest 2k medium steps, teacher missing count and frame delta remain zero, and an independent checkpoint delta audit finds `10,896` changed allowed aliases, all `1,774` frozen aliases bit-identical, and zero forbidden-path changes. The likely cause is the intended but disjoint domain transition: easy is dominated by Common Voice English (`944,383/1,174,987` rows) and contains no Emilia/GigaSpeech/WenetSpeech, while medium is dominated by WenetSpeech (`14,448,982`), Emilia ZH/EN (`11,022,836/8,974,415`), and GigaSpeech (`7,440,139`). Continue the required exact three-epoch medium run without early termination and use the immutable 10k cadence to determine whether cross-domain retention recovers with coverage. This checkpoint is neither a phase failure nor a promotion candidate. The final one-process paired hidden gate and full bilingual public gate remain authoritative; if the completed A phase fails them, do not promote to block alignment and add a separately receipted cumulative easy-replay correction while preserving complete medium/hard/long coverage and the BiRWKV architecture.
- 2026-07-27 Stage211A medium step-20000 partial retention recovery: on the identical seed-0 fixed features, total loss at step `0/10k/20k` is `0.270969/0.276318/0.270548`, mean 70-layer mixer loss is `0.264180/0.269865/0.264430`, and mean cosine is `0.906886/0.905928/0.907515`. From 10k to 20k, total loss falls `2.09%`, mean mixer loss falls `2.01%`, cosine rises `0.00159`, and loss/cosine improve in `59/70` and `58/70` layers. Relative to the medium initialization, total loss is now marginally better (`-0.16%`) and cosine is higher (`+0.00063`), while mean mixer loss remains effectively flat (`+0.09%`); `30/70` layers improve loss and `39/70` improve cosine. The residual is depth-localized: layers `30-39`, `50-59`, and `60-69` remain above their baseline loss by `5.27%/5.50%/2.85%`, despite improvement in nearly every band from 10k. Online medium loss around 10k versus 20k improves `0.34347 -> 0.31550` and cosine `0.87837 -> 0.88780`, with zero teacher misses/frame delta and finite execution. This reverses most of the initial cross-domain regression and argues against immediate catastrophic collapse, but does not yet prove broad retention. Continue the exact full-data run unchanged and require subsequent 10k gates plus the final paired/public evaluation to establish promotion.
- 2026-07-27 Stage211A medium step-30000 positive retention crossover: fixed total loss at step `0/10k/20k/30k` is `0.270969/0.276318/0.270548/0.261495`, mean 70-layer mixer loss is `0.264180/0.269865/0.264430/0.255596`, and mean cosine is `0.906886/0.905928/0.907515/0.910090`. From 20k to 30k, total and mean mixer loss fall `3.35%/3.34%`, cosine rises `0.00257`, and loss/cosine improve in `69/70` and `68/70` layers. Relative to the medium initialization, total loss is now `3.50%` lower, mean mixer loss `3.25%` lower, cosine `0.00320` higher, and `51/70` layers improve loss. The prior deep-band residual has substantially contracted: layers `30-39`, `50-59`, and `60-69` are now only `+0.68%/+2.03%/+0.02%` above baseline loss, versus `+5.27%/+5.50%/+2.85%` at 20k. Online medium loss at 10k/20k/30k is `0.34347/0.31550/0.30480`, with cosine `0.87837/0.88780/0.89153`, zero teacher misses/frame delta, and finite execution. This converts the 10k domain-transition warning into measurable broad recovery, while leaving layer band `50-59` as the main retention residual. Continue the unchanged exact-coverage three-epoch run; only the final paired hidden and bilingual public gates can prove phase promotion.
- 2026-07-27 Stage211A medium step-40000 all-band recovery: fixed total loss improves from the medium initialization `0.270969 -> 0.251331` (`-7.25%`), mean 70-layer mixer loss `0.264180 -> 0.245699` (`-7.00%`), and mean cosine `0.906886 -> 0.913118`. From 30k to 40k, loss and cosine improve in all `70/70` layers; every ten-layer band is now better than the medium initialization. The former residual bands `30-39/50-59/60-69` have crossed to loss changes of `-3.25%/-1.42%/-3.43%`, and the remaining individual-layer count improves to `58/70` by loss and `61/70` by cosine versus initialization. Online loss/cosine around 30k versus 40k is `0.30480/0.89153 -> 0.28617/0.89780`, with zero teacher misses/frame delta and finite execution. The early disjoint-domain regression is therefore recovered at both aggregate and depth-band scope without replay or a recipe change. Continue the full three-epoch contract; do not infer final WER/CER promotion from this hidden-only intermediate result.
- 2026-07-27 Stage211A medium step-50000 continued fixed-set convergence: on the same SHA-bound seed-0 256-utterance features, fixed total loss improves `0.270969 -> 0.251331 -> 0.243768` at initialization/40k/50k, so step 50k is `10.04%` below initialization and `3.01%` below step 40k. Mean 70-layer mixer loss improves `0.264180 -> 0.245699 -> 0.238325`, mean cosine improves `0.906886 -> 0.913118 -> 0.915436`, and every `70/70` layer improves both loss and cosine from 40k to 50k. All seven ten-layer bands continue downward from 40k and are `4.29-13.64%` below initialization; relative to initialization, `67/70` individual layers improve loss and `66/70` improve cosine, with the remaining loss residuals confined to layers `51/55/69`. Evaluation provenance is bit-identical across the baseline and checkpoints, all metrics are finite, teacher missing count and frame delta remain zero, and the complete step-50000 export plus four optimizer shards is recoverable. This strengthens the hidden-alignment evidence but remains only `5.11%` of the prescribed medium segment and cannot substitute for finishing all three epochs or for the full post-Stage-A normalized English-WER/Chinese-CER gate.
- 2026-07-27 Stage211A medium step-60000 continued all-layer convergence: the identical 256-utterance fixed gate improves total loss `0.270969 -> 0.243768 -> 0.235772` at initialization/50k/60k, making step 60k `12.99%` better than initialization and `3.28%` better than step 50k. Mean 70-layer mixer loss improves `0.264180 -> 0.238325 -> 0.230510`, mean cosine improves `0.906886 -> 0.915436 -> 0.918066`, and all `70/70` layers improve both metrics from 50k to 60k. Every ten-layer band is now `7.18-16.80%` below initialization and improves another `3.02-3.67%` from 50k; `68/70` individual layers now beat initialization in both loss and cosine, leaving only layers `55/69` as local residuals. Evaluation provenance remains identical and all metrics are finite; online matching still has zero missing rows/frame delta, and the complete step-62000 recovery point was written after the gate. This is continued Phase-A hidden alignment, not a WER/CER result: preserve the recipe and exact three-epoch medium coverage before any promotion decision.
- 2026-07-27 Stage211A medium step-70000 residual-layer contraction: on the unchanged fixed gate, total loss at initialization/60k/70k is `0.270969/0.235772/0.230674`, making step 70k `14.87%` better than initialization and `2.16%` better than step 60k. Mean 70-layer mixer loss improves `0.264180 -> 0.230510 -> 0.225538`, mean cosine improves `0.906886 -> 0.918066 -> 0.919719`, and all `70/70` layers improve both metrics from 60k to 70k. All depth bands improve another `1.74-2.64%` and are now `9.63-18.90%` below initialization. Individual loss alignment reaches `69/70` layers versus initialization, leaving only layer 69 slightly above its own initial loss; cosine reaches `68/70`, with residuals at layers `55/69`. Provenance remains identical, metrics are finite, online missing/frame-delta counts remain zero, and step 72000 is the complete latest recovery point. Continue the exact medium coverage unchanged; this hidden-only gate still does not authorize Stage B or replace the post-Stage-A bilingual public evaluation.
- 2026-07-28 Stage211A medium step-80000 all-layer initial-baseline crossover: fixed total loss improves `0.270969 -> 0.230674 -> 0.224191` at initialization/70k/80k, so step 80k is `17.26%` below initialization and `2.81%` below step 70k. Mean 70-layer mixer loss improves `0.264180 -> 0.225538 -> 0.219173`, and mean cosine improves `0.906886 -> 0.919719 -> 0.921950`. All `70/70` layers improve both loss and cosine from 70k to 80k, and for the first time all `70/70` layers also beat their medium initialization in both metrics. Every ten-layer band improves another `2.30-3.40%` from 70k and is now `12.70-21.46%` below initialization. Evaluation provenance is unchanged, all metrics are finite, online missing/frame-delta counts remain zero, and the step-80000 model plus all four optimizer shards form a complete recovery point. This removes the last local fixed-hidden residual but is only `7.97%` of the contracted medium steps; complete all three epochs and retain the final paired/public gates.
- 2026-07-28 Stage211A medium step-90000 diminishing but positive fixed gain: total fixed loss improves `0.224191 -> 0.221666` from 80k to 90k (`-1.13%`) and is `18.19%` below initialization; mean 70-layer mixer loss improves `0.219173 -> 0.216777`, while mean cosine improves `0.921950 -> 0.922852`. The gain is no longer uniformly layer-local: `57/70` layers improve loss and `59/70` improve cosine from 80k, and band `50-59` is effectively flat at `+0.005%` loss, while the other six bands improve `0.30-1.93%`. Nevertheless, every `70/70` layer remains better than medium initialization in both loss and cosine, all depth bands remain `12.70-22.97%` below initial loss, provenance is unchanged, metrics are finite, and online matching has zero missing rows/frame delta. The complete step-90000 recovery point is valid. At only `9.05%` of the medium contract, this modest fixed-set plateau is not evidence for early stopping or a recipe change; continue unchanged and use subsequent gates to distinguish normal local variance from sustained saturation.
- 2026-07-28 Stage211A medium step-100000 resumed broad fixed gain: the 90k slowdown does not persist. Total fixed loss improves `0.221666 -> 0.216595` from 90k to 100k (`-2.29%`) and is `20.07%` below initialization; mean 70-layer mixer loss improves `0.216777 -> 0.211792`, and mean cosine improves `0.922852 -> 0.924562`. From 90k, loss improves in `69/70` layers, cosine in `70/70`, and all seven depth bands improve by `2.00-3.24%`; only layer 69 loss is statistically flat/slightly higher (`+0.118%`). All `70/70` layers remain better than medium initialization in both metrics, with band losses `14.85-24.64%` lower. Provenance remains fixed, metrics are finite, matching/frame deltas remain exact, and the step-100000 model plus four optimizer shards is a complete recovery point. This confirms continued learning through `10.12%` of the medium segment and supports retaining the prescribed full-data, three-epoch recipe.
- 2026-07-28 Stage211A medium step-110000 broad fixed gain: total fixed loss improves `0.216595 -> 0.212485` from 100k to 110k (`-1.90%`) and is `21.58%` below initialization; mean 70-layer mixer loss improves `0.211792 -> 0.207772`, while mean cosine improves `0.924562 -> 0.925977`. Loss improves in all `70/70` layers and cosine in `69/70` from 100k; all seven depth bands improve by `1.43-2.80%` and stand `16.98-26.75%` below initialization. Every layer remains better than medium initialization in both metrics, evaluation provenance remains fixed, all values are finite, and online matching/frame deltas remain exact. Training continued to step 112340 with a complete step-112000 recovery point. Continue unchanged: the fixed-hidden signal remains healthy, but Phase A still requires complete medium/hard/long coverage and the final paired hidden plus bilingual WER/CER gates.
- 2026-07-28 Stage211A medium step-120000 renewed all-layer fixed gain: total fixed loss improves `0.212485 -> 0.206758` from 110k to 120k (`-2.70%`) and is `23.70%` below initialization; mean 70-layer mixer loss improves `0.207772 -> 0.202161`, while mean cosine improves `0.925977 -> 0.927988`. All `70/70` layers improve both loss and cosine from 110k, all seven depth bands improve by `2.46-3.21%`, and band losses are now `19.03-28.81%` below initialization. Evaluation provenance remains identical, all values are finite, online matching has zero missing rows/frame delta, and training continued to step 123050 with a complete step-122000 recovery point. The medium hidden-alignment trajectory remains broad rather than depth-local; continue the exact three-epoch contract without early stopping.
- 2026-07-28 Stage211A medium step-130000 positive aggregate with local slowdown: total fixed loss improves `0.206758 -> 0.205655` from 120k to 130k (`-0.53%`) and is `24.10%` below initialization; mean 70-layer mixer loss improves `0.202161 -> 0.201133`, and mean cosine improves `0.927988 -> 0.928381`. The local gain narrows to `53/70` layers by loss and `55/70` by cosine, while every depth band still improves (`0.15-1.68%`) and every `70/70` layer remains better than medium initialization in both metrics. Provenance is unchanged, values are finite, online matching/frame deltas remain exact, and step 130000 has a complete model plus four optimizer shards. As with the temporary 90k slowdown, one modest interval at `12.97%` completion is not a reason to alter the recipe; continue unchanged and use 140k onward to test whether broad gains resume.
- 2026-07-28 Stage211A medium step-140000 broad-gain resumption: the 130k local slowdown is temporary. Total fixed loss improves `0.205655 -> 0.200801` from 130k to 140k (`-2.36%`) and is `25.90%` below initialization; mean 70-layer mixer loss improves `0.201133 -> 0.196345`, and mean cosine improves `0.928381 -> 0.930071`. Loss improves in `69/70` layers and cosine in `70/70`; every depth band improves by `1.90-3.32%` and stands `21.87-31.68%` below initialization. Only layer 69 loss is effectively flat/slightly higher (`+0.211%`) versus 130k, while all layers remain better than initialization. Provenance remains fixed, all metrics are finite, online matching/frame deltas remain exact, and step 140000 is a complete recovery point. Continue the unchanged exact-coverage recipe.
- 2026-07-28 Stage211A medium step-150000 first small fixed-set reversal: total fixed loss changes `0.200801 -> 0.200925` from 140k to 150k (`+0.062%`), mean 70-layer loss changes `0.196345 -> 0.196536`, and mean cosine changes `0.930071 -> 0.930045`. Only `33/70` layers improve loss and cosine; shallow bands `0-39` still improve `0.07-0.79%`, while bands `40-49/50-59/60-69` regress `0.38/1.92/1.08%`, led by layers 55 and 69. This is not yet sustained degradation: step 150k remains better than 130k, all `70/70` layers remain better than medium initialization, total loss remains `25.85%` below initialization, provenance is unchanged, all values are finite, and online matching/frame deltas remain exact. The complete step-150000 recovery point is valid, but step 140000 remains the better fixed candidate. Continue unchanged through 160k; do not react to one `0.062%` interval while exact three-epoch coverage is mandatory.
- 2026-07-29 Stage211A medium step-160000 recovery and new fixed best: total fixed loss improves `0.200925 -> 0.198528` from 150k to 160k (`-1.19%`), beats the former 140k best by `1.13%`, and is `26.73%` below initialization. Mean 70-layer loss improves `0.196536 -> 0.194202`, and mean cosine improves `0.930045 -> 0.930915`; `65/70` layers improve loss and `66/70` improve cosine from 150k, while every depth band improves by `0.88-1.68%`. All `70/70` layers remain better than medium initialization, with band losses `21.32-33.17%` lower. Provenance remains fixed, all metrics are finite, online matching/frame deltas remain exact, and training continued to step 162260 with a complete step-162000 recovery point. This confirms the 150k reversal was a one-gate oscillation rather than sustained degradation; continue unchanged.
- 2026-07-29 Stage211A medium step-170000 continued new-best convergence: total fixed loss improves `0.198528 -> 0.195914` from 160k to 170k (`-1.32%`) and is `27.70%` below initialization; mean 70-layer loss improves `0.194202 -> 0.191655`, and mean cosine improves `0.930915 -> 0.931795`. Loss and cosine each improve in `68/70` layers; all seven depth bands improve by `0.91-1.76%` and are `22.57-34.35%` below initialization. Only layers 20 and 67 show tiny local loss increases (`+0.259/+0.277%`), while all `70/70` layers remain better than initialization. Provenance remains fixed, metrics are finite, online matching/frame deltas remain exact, and training continued to step 172820 with a complete step-172000 recovery point. Preserve the unchanged full-data recipe.
- 2026-07-29 Stage211A medium step-180000 strongest all-layer fixed gate: total fixed loss improves `0.195914 -> 0.189993` from 170k to 180k (`-3.02%`) and is `29.88%` below initialization; mean 70-layer loss improves `0.191655 -> 0.185785`, and mean cosine improves `0.931795 -> 0.933870`. Every `70/70` layer improves both loss and cosine from 170k. All seven depth bands improve by `2.33-4.56%`, with the formerly oscillatory `60-69` band producing the largest gain, and bands are now `25.34-35.88%` below initialization. Provenance remains fixed, metrics are finite, online matching/frame deltas remain exact, and training continued to step 183350 with a complete step-182000 recovery point. This is broad depth-wise learning rather than a shallow-layer-only gain; continue unchanged.
- 2026-07-29 Stage211A medium step-190000 continued broad new-best gain: total fixed loss improves `0.189993 -> 0.186182` from 180k to 190k (`-2.01%`) and is `31.29%` below initialization; mean 70-layer loss improves `0.185785 -> 0.182062`, and mean cosine improves `0.933870 -> 0.935161`. Loss and cosine improve in `68/70` layers; all seven depth bands improve by `1.11-2.84%` and are `27.02-37.41%` below initialization. Only layers 40 and 41 regress locally by `1.11/1.61%` loss while every layer remains better than initialization. Provenance remains fixed, all metrics are finite, online matching/frame deltas remain exact, and step 190000 is a complete recovery point. Continue unchanged.
- 2026-07-29 Stage211A medium step-200000 continued fixed-best convergence: total fixed loss improves `0.186182 -> 0.182926` from 190k to 200k (`-1.75%`) and is `32.49%` below initialization; mean 70-layer loss improves `0.182062 -> 0.178839`, and mean cosine improves `0.935161 -> 0.936320`. Loss and cosine improve in `68/70` layers; all seven depth bands improve by `1.29-2.22%`. Only layers 3 and 69 show small local loss regressions (`+0.35/+1.79%`), while every `70/70` layer remains better than initialization in both loss and cosine. Evaluation provenance is identical, all metrics are finite, online matching has zero missing rows/frame delta, and the step-200000 model plus all four optimizer shards is a complete recovery point. Training continued through step 200820 (`20.01%` of the medium contract and `60.02%` of epoch 1); preserve the unchanged full-data three-epoch recipe.
- 2026-07-29 Stage211A medium step-210000 continued broad fixed-best gain: total fixed loss improves `0.182926 -> 0.179752` from 200k to 210k (`-1.74%`) and is `33.66%` below initialization; mean 70-layer loss improves `0.178839 -> 0.175721`, and mean cosine improves `0.936320 -> 0.937422`. Loss and cosine improve in `69/70` layers; all seven depth bands improve by `1.41-2.54%`, with the largest reductions in bands `50-59` and `60-69`. Only layer 0 shows a small local loss increase (`+0.79%`) and cosine decrease (`-0.00010`), while every `70/70` layer remains better than initialization in both metrics. Evaluation provenance is identical, all metrics are finite, online matching has zero missing rows/frame delta, and the step-210000 model plus all four optimizer shards is a complete recovery point. Training continued through step 211170 (`21.04%` of the medium contract and `63.12%` of epoch 1); continue unchanged.
- 2026-07-29 Stage211A medium step-220000 all-layer fixed-best gain: total fixed loss improves `0.179752 -> 0.176134` from 210k to 220k (`-2.01%`) and is `35.00%` below initialization; mean 70-layer loss improves `0.175721 -> 0.172165`, and mean cosine improves `0.937422 -> 0.938702`. Every `70/70` layer improves both loss and cosine from 210k and remains better than initialization; all seven depth bands improve by `1.57-2.69%`, with the largest reduction in band `60-69`. Evaluation provenance is identical, all metrics are finite, online matching has zero missing rows/frame delta, and the step-220000 model plus all four optimizer shards is a complete recovery point. Training continued through step 221640 (`22.08%` of the medium contract and `66.25%` of epoch 1); retain the unchanged full-data three-epoch recipe.
- 2026-07-29 Stage211A medium step-230000 continued broad fixed-best gain: total fixed loss improves `0.176134 -> 0.172748` from 220k to 230k (`-1.92%`) and is `36.25%` below initialization; mean 70-layer loss improves `0.172165 -> 0.168859`, and mean cosine improves `0.938702 -> 0.939881`. Loss and cosine improve in `69/70` layers; all seven depth bands improve by `1.30-2.36%`. Only layer 55 shows a negligible local loss increase (`+0.10%`) and cosine decrease (`-0.00012`), while every `70/70` layer remains better than initialization in both metrics. Evaluation provenance is identical, all metrics are finite, online matching has zero missing rows/frame delta, and the step-230000 model plus all four optimizer shards is a complete recovery point. Training continued through step 232230 (`23.14%` of the medium contract and `69.41%` of epoch 1); continue unchanged.
- 2026-07-30 Stage211A medium step-240000 second fixed-set reversal: total fixed loss changes `0.172748 -> 0.174374` from 230k to 240k (`+0.94%`), mean 70-layer loss changes `0.168859 -> 0.170487`, and mean cosine changes `0.939881 -> 0.939327`. Only `6/70` layers improve loss and `10/70` improve cosine; all seven depth bands regress by `0.54-2.11%`, led by band `60-69`. This is a broad local reversal but not yet sustained degradation: step 240k remains about `1.00%` better than step 220k and `35.65%` better than initialization, and every `70/70` layer remains better than initialization in both loss and cosine. Evaluation provenance is identical, all metrics are finite, online matching has zero missing rows/frame delta, and the step-240000 model plus all four optimizer shards is a complete recovery point. Post-gate online training through step 242850 remains healthy, with last-1000 loss/cosine `0.169551/0.939301`. Retain step 230k as the fixed-best candidate, continue the mandatory exact-coverage run unchanged, and use step 250k to distinguish oscillation from sustained reversal.
- 2026-07-30 Stage211A medium step-250000 full recovery and strongest fixed-best gain: total fixed loss improves `0.174374 -> 0.167458` from 240k to 250k (`-3.97%`), beats the prior step-230k best by `3.06%`, and is `38.20%` below initialization. Mean 70-layer loss improves `0.170487 -> 0.163709`, and mean cosine improves `0.939327 -> 0.941700`. Every `70/70` layer improves both loss and cosine relative to both 240k and 230k; all seven depth bands improve by `3.36-5.90%`, with the largest gain in band `60-69`. Evaluation provenance is identical, all metrics are finite, online matching has zero missing rows/frame delta, and the step-250000 model plus all four optimizer shards is a complete recovery point. Training resumed through step 250110 (`24.92%` of the medium contract and `74.76%` of epoch 1). This proves the 240k reversal was a one-gate oscillation rather than sustained degradation; continue unchanged.
- 2026-07-30 Stage211A medium step-260000 continued all-layer fixed-best gain: total fixed loss improves `0.167458 -> 0.162566` from 250k to 260k (`-2.92%`) and is `40.01%` below initialization; mean 70-layer loss improves `0.163709 -> 0.158927`, and mean cosine improves `0.941700 -> 0.943386`. Every `70/70` layer improves both loss and cosine from 250k and remains better than initialization; all seven depth bands improve by `2.47-3.69%`, with the largest reductions in bands `50-59` and `60-69`. Evaluation provenance is identical, all metrics are finite, online matching has zero missing rows/frame delta, and the step-260000 model plus all four optimizer shards is a complete recovery point. Training continued through step 260980 (`26.00%` of the medium contract and `78.00%` of epoch 1). The fixed gate remains materially responsive at the current LR and coverage, so continue unchanged.
- 2026-07-30 Stage211A medium step-270000 positive fixed-best with depth-local slowdown: total fixed loss improves `0.162566 -> 0.161462` from 260k to 270k (`-0.68%`) and is `40.41%` below initialization; mean 70-layer loss improves `0.158927 -> 0.157894`, and mean cosine improves `0.943386 -> 0.943746`. Loss/cosine improve in `50/70` and `53/70` layers. Bands `0-39` improve by `0.25-1.51%`, while bands `40-49`, `50-59`, and `60-69` are nearly flat with small loss changes of `+0.07/+0.04/+0.26%`; layer 55 has the largest local loss increase at `+1.88%`. Every `70/70` layer remains better than initialization in both loss and cosine, evaluation provenance is identical, all metrics are finite, online matching has zero missing rows/frame delta, and the step-270000 model plus all four optimizer shards is a complete recovery point. Training continued through step 271860 (`27.09%` of the medium contract and `81.26%` of epoch 1). Treat this as a local slowdown after the strong 250k-260k gain, retain the unchanged exact-coverage recipe, and use 280k to test whether deep-band gains resume.
- 2026-07-30 Stage211A medium step-280000 small fixed-set reversal and local plateau: total fixed loss changes `0.161462 -> 0.162055` from 270k to 280k (`+0.37%`), while remaining `0.31%` better than step 260k and `40.19%` better than initialization. Mean 70-layer loss changes `0.157894 -> 0.158487`, and mean cosine changes `0.943746 -> 0.943561`; only `22/70` layers improve loss and `24/70` improve cosine from 270k. Band `0-9` is effectively flat/slightly better (`-0.03%`), while bands `10-69` change by `+0.01%` to `+0.89%`, with the largest local reversal in bands `40-49` and `50-59`; layers 0, 69, and 55 have the largest relative local loss increases. Every `70/70` layer remains better than initialization, evaluation provenance is identical, all metrics are finite, online matching has zero missing rows/frame delta, and the step-280000 model plus all four optimizer shards is a complete recovery point. Training continued through step 282740 (`28.17%` of the medium contract and `84.51%` of epoch 1), with healthy last-1000 online loss/cosine `0.160644/0.942495`. Treat 260k-280k as a local plateau/oscillation rather than a failure, preserve exact three-epoch coverage, and use 290k to test whether broad gains resume.
- 2026-07-30 Stage211A medium step-290000 broad recovery beyond the local plateau: total fixed loss improves `0.162055 -> 0.160161` from 280k to 290k (`-1.17%`), beats the prior step-270k best by `0.81%`, and is `40.89%` below initialization. Mean 70-layer loss improves `0.158487 -> 0.156624`, and mean cosine improves `0.943561 -> 0.944205`. Loss and cosine improve in `65/70` layers from 280k, and all seven depth bands improve by `0.91-1.82%`; the previously slow bands `40-49`, `50-59`, and `60-69` improve by `1.33/1.82/1.53%`. Relative to 270k, `62/70` layers improve loss and `61/70` improve cosine, while every `70/70` layer remains better than initialization. Evaluation provenance is identical, all metrics are finite, online matching has zero missing rows/frame delta, and the step-290000 model plus all four optimizer shards is a complete recovery point. This resolves the 270k-280k local plateau as an oscillation with a new fixed best; continue unchanged.
- 2026-07-30 Stage211A medium step-300000 near-flat fixed gate with depth rotation: total fixed loss changes `0.160161 -> 0.160232` from 290k to 300k (`+0.044%`) and remains `40.87%` below initialization; mean 70-layer loss changes `0.156624 -> 0.156771`, and mean cosine changes `0.944205 -> 0.944155`. Only `24/70` layers improve loss and `22/70` improve cosine. Bands `0-9`, `10-19`, and `20-29` continue to improve by `0.83/0.76/0.22%`, while bands `30-39`, `40-49`, `50-59`, and `60-69` regress by `0.99/1.06/1.77/1.00%`, led locally by layer 55. Every `70/70` layer remains better than initialization, evaluation provenance is identical, all metrics are finite, online matching has zero missing rows/frame delta, and the step-300000 model plus all four optimizer shards is a complete recovery point. Training continued through step 301020 (`29.99%` of the medium contract and `89.97%` of epoch 1), with healthy last-1000 online loss/cosine `0.158764/0.943171`. Treat this as continued fixed-set oscillation near the 290k best rather than a training fault; do not stop the exact three-epoch contract, and use 310k plus the first-epoch boundary to judge whether the plateau persists.
- 2026-07-30 Stage211A medium step-310000 strong all-layer break below the plateau: total fixed loss improves `0.160232 -> 0.153907` from 300k to 310k (`-3.95%`), beats the prior step-290k best by `3.90%`, and is `43.20%` below initialization. Mean 70-layer loss improves `0.156771 -> 0.150539`, and mean cosine improves `0.944155 -> 0.946358`. Every `70/70` layer improves both loss and cosine from 300k; relative to 290k, `69/70` layers improve loss and all `70/70` improve cosine, with only layer 69 retaining a small relative loss residual. All seven depth bands improve by `3.25-5.47%`, and the largest gains occur in bands `50-59` and `60-69`, resolving the prior deep-band slowdown. Evaluation provenance is identical, all metrics are finite, online matching has zero missing rows/frame delta, and the step-310000 model plus all four optimizer shards is a complete recovery point. Training continued through step 312030 (`31.09%` of the medium contract and `93.26%` of epoch 1), with last-500 online loss/cosine `0.153105/0.945180`. The 270k-300k plateau is decisively broken; continue unchanged.
- 2026-07-30 Stage211A medium step-320000 near-flat gate after the strong breakthrough: total fixed loss changes `0.153907 -> 0.154017` from 310k to 320k (`+0.071%`) and remains `43.16%` below initialization; mean 70-layer loss changes `0.150539 -> 0.150674`, and mean cosine changes `0.946358 -> 0.946324`. Loss/cosine improve in `27/70` and `26/70` layers. Band `0-9` continues to improve by `1.13%`, while the remaining bands change by only `+0.04%` to `+0.56%`; layers 34 and 55 have the largest local loss increases at about `1.95%`. Every `70/70` layer remains better than initialization, evaluation provenance is identical, all metrics are finite, online matching has zero missing rows/frame delta, and the step-320000 model plus all four optimizer shards is a complete recovery point. Training continued through step 322960 (`32.18%` of the medium contract and `96.53%` of epoch 1), with healthy last-1000 online loss/cosine `0.154920/0.944541`. Treat this as a normal local oscillation after the 310k step change; continue unchanged through the first-epoch boundary and the exact three-epoch contract.
- 2026-07-31 Stage211A medium step-330000 broad fixed-best gain near the first-epoch boundary: total fixed loss improves `0.154017 -> 0.152219` from 320k to 330k (`-1.17%`), beats the prior step-310k best by `1.10%`, and is `43.82%` below initialization. Mean 70-layer loss improves `0.150674 -> 0.148914`, and mean cosine improves `0.946324 -> 0.946954`. Loss and cosine improve in `69/70` layers from 320k; all seven depth bands improve by `1.03-1.50%`, with only layer 67 loss effectively flat/slightly higher (`+0.033%`). Relative to 310k, `66/70` layers improve both loss and cosine, while every `70/70` layer remains better than initialization. Evaluation provenance is identical, all metrics are finite, online matching has zero missing rows/frame delta, and the step-330000 model plus all four optimizer shards is a complete recovery point. Training continued through step 330230 (`32.90%` of the medium contract and `98.70%` of epoch 1), leaving 4,341 steps to the exact first-epoch boundary; continue unchanged.
- 2026-07-31 Stage211A medium exact epoch-1 completion and coverage audit: the bucketed dataloader naturally exhausted at step `334571`, emitted `Epoch 1 complete` with aggregate train loss `0.2043`, saved `epoch-1.pt`, and saved a complete DeepSpeed recovery tag containing the model state plus all four optimizer shards. This is a real manifest traversal rather than an exposure-only step estimate: the loader constructs `334571` bucket-aware global batches, consumes all `42,247,508` train rows sequentially across ranks, and adds only the declared `744` repeated tail samples needed to make each bucket divisible by its dynamic global batch. Exact-coverage guards remain active (`length_bucket_drop_last=false`, `skip_oversized_samples=false`, `webdataset_skip_decode_errors=false`). The startup log reports `steps_per_epoch=None` only because the strict runner supplies the three-epoch contract as explicit `max_steps=1003713`; epoch boundaries are still determined by loader exhaustion and the epoch checkpoint records `step=334571`, `epoch=1`, and `epoch_batch_offset=0`. Training entered epoch 2 without restart and continued through step 334710 with zero teacher-missing events and zero hidden-frame delta. Preserve the unchanged Stage211A mixer recipe.
- 2026-07-31 Stage211A medium step-340000 starts epoch 2 with a broad fixed-best gain: total fixed loss improves `0.152219 -> 0.148329` from 330k to 340k (`-2.56%`) and is `45.26%` below initialization; mean 70-layer loss improves `0.148914 -> 0.145085`, and mean cosine improves `0.946954 -> 0.948294`. Loss and cosine improve in `66/70` layers, and all seven depth bands improve by `1.24-3.52%`. The only local loss increases are layers 0, 1, 2, and 69 (`+0.89/+2.47/+1.74/+2.53%`), with layer 69 still having a very small absolute loss (`0.008531`); every `70/70` layer remains better than initialization in both loss and cosine. Evaluation provenance is identical, all metrics are finite, online matching has zero missing rows/frame delta, and step 340000 has a complete export plus all four optimizer shards. Training continued through step 341110 (`33.98%` of the medium contract and `1.95%` of epoch 2), with last-1000 online loss/cosine `0.149698/0.946390`. This confirms that entering the second exact traversal did not reset or stall convergence; continue unchanged.
- 2026-07-31 Stage211 A/B/C/D transition and evaluation-gate audit: the active immutable supervisor runs the required order `mixer -> block -> logits -> labeled SFT` and cannot enter a later phase without the preceding checkpoint-bound promotion receipt. Each A/B/C phase first validates all easy/medium/hard/long three-epoch coverage receipts, then evaluates the same five complete real-audio benchmarks with CTC normalization: AISHELL-1 and WenetSpeech TEST_NET CER plus LibriSpeech test-clean/test-other and Common Voice 22 English WER. Mixer and block promotion additionally require an independently recomputed hidden-alignment gate plus improved macro public error/deletion relative to the bound preceding checkpoint, with per-dataset regression limits. Logits promotion requires an independent logits-alignment gate and every public dataset to satisfy both `student/Nano <= 1.20` and absolute gap `<= 3.0` points. Labeled SFT must retain that every-dataset Nano gate, have no public-dataset regression relative to Stage211C, improve the macro error, and emit the final immutable stepwise A/B/C/D report. The active supervisor's implicit Nano provenance and calibration-reuse paths resolve to existing SHA-bound receipts. Verification `uv run pytest -q tests/test_stage211_runner.py tests/test_stage211_sft.py` passes `51/51` tests (only 14 upstream Torch JIT deprecation warnings). No transition or evaluation-path change is required while Stage211A continues.
- 2026-07-31 Stage211 future-phase gradient and labeled-coverage audit: all four phases intentionally preserve the Nano-copied non-attention path by freezing the MLP/norm tensors, five-layer CTC decoder, and CTC head while optimizing only BiRWKV `TimeMixer` plus `input_proj` (`189,378,560` trainable parameters in the active model). This does not detach later losses: frozen decoder/head weights still participate in autograd with respect to their inputs, so Stage211B stacked block, encoder, and decoder-hidden losses, Stage211C blank/conditional/sequence/window logits losses, and Stage211D ground-truth CTC loss all update the BiRWKV path. A/B/C each require all `62,072,225` unique audio rows totaling `118,465.161` hours and three exact traversals before evaluation. Stage211D separately validates and consumes the complete pronunciation-only labeled manifest: `283,868` train plus `1,434` eval rows (`285,302` total), `809.12755` hours, `8,792,460` CTC tokens, and one exact `12,045`-step labeled epoch with only `368` declared bucket-tail repetitions. Decode-error skipping, oversized-sample skipping, and bucket dropping remain disabled. The phase-specific smoke preflight still guards finite nonzero losses, zero missing/frame-delta events, memory safety, and actual optimizer steps before each future formal phase.
- 2026-07-31 Stage211A medium step-350000 continued fixed-best convergence at a slower interval: total fixed loss improves `0.148329 -> 0.147528` from 340k to 350k (`-0.54%`) and is `45.56%` below initialization; mean 70-layer loss improves `0.145085 -> 0.144300`, and mean cosine improves `0.948294 -> 0.948593`. Loss improves in `54/70` layers and cosine in `56/70`; every depth band remains non-regressing and improves by `0.02-0.89%`. Layer 0 has the largest local loss increase (`+7.11%`) but remains at a small absolute loss of `0.025737` and far below its initialization, while every `70/70` layer remains better than initialization in both metrics. Evaluation provenance is identical, all metrics are finite, online matching has zero missing rows/frame delta, and step 350000 is the top fixed-eval candidate with a complete export, four optimizer shards, and an exact epoch-2 resume offset. Training continued through step 350020 (`34.87%` of the medium contract and `4.62%` of epoch 2). This is a positive broad gate after the stronger 340k gain, not a sustained plateau or reversal; retain the unchanged learning rate and loss recipe.
- 2026-07-31 Stage211A medium step-360000 broad fixed-set reversal after the 350k best: total fixed loss changes `0.147528 -> 0.149456` (`+1.31%`), mean 70-layer loss changes `0.144300 -> 0.146225`, and mean cosine changes `0.948593 -> 0.947923`. Only `3/70` layers improve loss and cosine, and all seven depth bands regress by `0.65-2.49%`, with the largest increases in bands `50-59` and `60-69`; layers 67 and 55 increase by `5.23%` and `4.52%`. This is not yet evidence of sustained degradation: total loss remains `44.84%` below initialization, every `70/70` layer remains better than initialization in both metrics, provenance is unchanged, metrics are finite, online matching/frame deltas remain exact, and the complete step-360000 model plus four optimizer shards is a valid recovery point. Post-gate training continued through step 360020 (`35.87%` of the medium contract and `7.61%` of epoch 2), with last-1000 online loss/cosine `0.144249/0.948353`. Retain step 350000 as the fixed-best candidate, continue the mandatory exact-coverage run unchanged, and use step 370000 to distinguish another one-gate oscillation from a persistent reversal.
- 2026-07-31 Stage211A medium step-370000 recovery and marginal new fixed best: total fixed loss improves `0.149456 -> 0.147356` from 360k (`-1.41%`), edges below the prior step-350k best by `0.116%`, and is `45.62%` below initialization. Mean 70-layer loss/cosine recover from `0.146225/0.947923` to `0.144174/0.948654`; relative to 360k, loss and cosine improve in `68/70` layers and all seven depth bands improve by `0.80-2.48%`. Relative to 350k, `38/70` losses and `39/70` cosines improve: bands `0-29` and `60-69` improve, while bands `30-59` rotate slightly upward by only `0.45-0.59%`. Energy MSE reaches a new low `0.109194`, while mean log-RMS loss is essentially tied/slightly above 350k (`0.014262` versus `0.014067`), so this is a real but marginal aggregate gain within an oscillatory slow-convergence regime rather than a broad step change. Every `70/70` layer remains better than initialization in loss and cosine, evaluation provenance is identical, all metrics are finite, online teacher matching has zero missing rows and zero frame delta, and the step-370000 model plus all four optimizer shards is a complete recovery point. The ranked checkpoint index now selects step 370000 first and step 350000 second. Continue the mandatory three-epoch exact-coverage run unchanged and use later 10k gates to determine whether the new best extends.
- 2026-07-31 Stage211A medium step-380000 broad extension of the fixed best: total fixed loss improves `0.147356 -> 0.146549` from 370k (`-0.55%`), is `0.66%` below the earlier 350k candidate, and is `45.92%` below initialization. Mean 70-layer loss/cosine improve `0.144174/0.948654 -> 0.143403/0.948916`; loss improves in `57/70` layers and cosine in `58/70`, while all seven depth bands reduce loss by `0.03-0.89%`. Unlike the marginal 370k result, component scale diagnostics also establish new fixed bests: mean energy MSE `0.108560`, log-RMS loss `0.014024`, and absolute RMS-ratio error `0.030504`. Every `70/70` layer remains better than initialization in loss and cosine, provenance is unchanged, all values are finite, teacher matching/frame lengths are exact, and the step-380000 export plus model state and all four optimizer shards is complete. The ranked checkpoint index selects 380k first, 370k second, and 350k third. Training continued through step 380810 (`37.94%` of the medium contract and `13.82%` of epoch 2) with no formal error; preserve the unchanged exact three-epoch recipe.
- 2026-07-31 Stage211A medium step-390000 continued aggregate best with a small difficult-band rotation: total fixed loss improves `0.146549 -> 0.146047` from 380k (`-0.34%`) and is `46.10%` below initialization; mean 70-layer loss/cosine improve `0.143403/0.948916 -> 0.142897/0.949106`. Loss and cosine improve in `55/70` layers. Six depth bands reduce loss by `0.16-0.69%`, while the difficult `10-19` band changes slightly upward by `0.13%`; layer 0 has the largest relative increase (`+12.45%`) only because its absolute loss moves from a very low `0.022516` to `0.025318`, and layer 55 increases `1.82%`. Mean energy MSE establishes another best at `0.108212`, but log-RMS loss and absolute RMS-ratio error rotate slightly above the more scale-balanced 380k values (`0.014096/0.030964` versus `0.014024/0.030504`). Every `70/70` layer remains better than initialization in loss and cosine, provenance is unchanged, all metrics are finite, online matching/frame lengths remain exact, and the step-390000 export plus complete four-rank optimizer state is recoverable. Rank 390k first by the declared total objective but retain 380k as the scale-balanced fallback for the eventual phase gate. Training continued through step 391480 (`39.00%` of the medium contract and `17.01%` of epoch 2) without formal errors; continue unchanged.
- 2026-07-31 Stage211A medium step-400000 broad fixed-best breakthrough and difficult-band recovery: total fixed loss improves `0.146047 -> 0.144283` from 390k (`-1.21%`), is `1.55%` below 380k, and is `46.75%` below initialization. Mean 70-layer loss/cosine improve `0.142897/0.949106 -> 0.141197/0.949732`. Relative to 390k, loss improves in `68/70` layers, cosine in `67/70`, and all seven depth bands reduce loss by `0.70-1.57%`; the previously rotating difficult `10-19` band recovers by `1.57%`. Relative to the scale-balanced 380k checkpoint, loss and cosine improve in `69/70` layers. Mean energy MSE reaches a new best `0.107007`, while log-RMS loss and absolute RMS-ratio error remain slightly worse than 380k (`0.014182/0.032322` versus `0.014024/0.030504`), so retain 380k only as the amplitude-balanced fallback and rank 400k first by the declared complete objective. Every layer remains better than initialization, evaluation provenance is unchanged, all values are finite, teacher matching and frame lengths are exact, and the step-400000 export plus all four optimizer shards is complete. Training continued through step 402210 (`40.07%` of the medium contract and `20.22%` of epoch 2) without formal errors; preserve the unchanged three-epoch exact-coverage recipe.
- 2026-07-31 Stage211A medium step-410000 confirms the post-400k improvement: total fixed loss improves `0.144283 -> 0.143656` (`-0.43%`) and is `46.98%` below initialization; mean 70-layer loss/cosine improve `0.141197/0.949732 -> 0.140576/0.949956`. Loss/cosine improve in `52/70` and `54/70` layers, and all seven depth bands reduce loss by `0.20-0.93%`; the difficult `10-19` band improves another `0.31%`. Mean energy MSE reaches a new best `0.106549` and log-RMS loss improves slightly from 400k, while absolute RMS-ratio error is nearly flat/slightly higher (`0.032515` versus `0.032322`) and remains above the amplitude-balanced 380k fallback. Every layer remains better than initialization in loss/cosine, provenance is unchanged, all metrics are finite, teacher matching/frame lengths remain exact, and the step-410000 model plus complete four-rank optimizer state is recoverable. Rank 410k first by the declared objective, retain 380k only as a scale diagnostic fallback, and continue unchanged through exact three-epoch coverage. Training continued through step 412950 (`41.14%` of the medium contract and `23.43%` of epoch 2) without formal errors.
- 2026-08-01 Stage211A medium step-420000 continued fixed-best gain with a shallow-band rotation: total fixed loss improves `0.143656 -> 0.142591` from 410k (`-0.74%`) and is `47.38%` below initialization; mean 70-layer loss/cosine improve `0.140576/0.949956 -> 0.139513/0.950345`. Loss/cosine improve in `61/70` and `60/70` layers. Bands `10-69` reduce loss by `0.52-1.69%`, including `0.79%/0.97%` improvements in the difficult `10-19/20-29` bands; only band `0-9` rotates upward by a negligible `0.08%`, driven by layer 0 moving from the very low loss `0.021858` to `0.025981`. Mean energy MSE reaches a new best `0.105786`; log-RMS and absolute RMS-ratio errors change slightly upward to `0.014180/0.033105`, but the latter remains less than half the initialization error `0.070660` and does not override the broad hidden/cosine gain. Every layer remains better than initialization, provenance is unchanged, all values are finite, teacher matching/frame lengths remain exact, and step 420000 has a complete model export plus all four optimizer shards. Rank 420k first by the declared objective, keep 380k only as the amplitude-balanced diagnostic fallback, and continue unchanged. Training resumed through step 420120 (`41.86%` of the medium contract and `25.57%` of epoch 2) without formal errors.
- 2026-08-01 Stage211A medium step-430000 small fixed-set reversal after the 420k best: total fixed loss changes `0.142591 -> 0.142746` (`+0.109%`), mean 70-layer loss changes `0.139513 -> 0.139695`, and cosine changes `0.950345 -> 0.950287`. Only `25/70` layers improve loss and cosine. Band `0-9` improves by `0.73%`, while bands `10-69` rotate upward by `0.07-1.23%`, led by `40-49`; the difficult `10-19/20-29` bands regress only `0.15%/0.07%`. This is a shallow depth rotation rather than a material failure: 430k remains `47.32%` below initialization and still beats 410k, while absolute RMS-ratio error improves slightly from 420k (`0.033105 -> 0.032610`). Every layer remains better than initialization, provenance is identical, metrics are finite, online matching/frame lengths remain exact, and the complete 430k export plus all four optimizer shards is recoverable. Retain 420k as the fixed-best candidate, continue the mandatory exact-coverage run unchanged, and use 440k to distinguish a one-gate oscillation from a persistent reversal. Training continued through step 430890 (`42.93%` of the medium contract and `28.79%` of epoch 2) without formal errors.
- 2026-08-01 Stage211A medium step-440000 recovers the 430k oscillation and establishes a new fixed best: total fixed loss improves to `0.141960`, which is `0.44%` below 420k, `0.55%` below 430k, and `47.61%` below initialization; mean 70-layer loss/cosine reach `0.138942/0.950576`. Relative to 430k, loss/cosine improve in `57/70` and `58/70` layers; bands `0-49` and `60-69` improve by `0.33-0.97%`, while `50-59` is effectively flat at `+0.07%`. Relative to the prior 420k best, the difficult `10-19/20-29` bands improve by `0.83%/0.31%`, although bands `40-49/50-59` remain `0.71%/0.43%` higher. Mean energy MSE reaches a new best `0.105461`, while log-RMS and absolute RMS-ratio errors continue a slow amplitude rotation to `0.014467/0.034183`; both remain materially below initialization, but retain 380k as the amplitude-balanced fallback for the eventual phase gate. Every layer remains better than initialization in loss/cosine, provenance is unchanged, all metrics are finite, online matching/frame lengths are exact, and the complete 440k export plus four optimizer shards is recoverable. Rank 440k first by the declared objective and continue unchanged. Training resumed through step 441650 (`44.00%` of the medium contract and `32.00%` of epoch 2) without formal errors.
- 2026-08-01 Stage211A medium step-450000 broad new fixed best with amplitude recovery: total fixed loss improves `0.141960 -> 0.140679` from 440k (`-0.90%`) and is `48.08%` below initialization; mean 70-layer loss/cosine improve `0.138942/0.950576 -> 0.137712/0.950989`. Loss/cosine improve in `58/70` and `59/70` layers. Bands `0-59` reduce loss by `0.67-1.50%`, including `0.84%/0.83%` in the difficult `10-19/20-29` bands; band `60-69` is statistically flat at `+0.027%`. The prior amplitude drift also recovers: mean log-RMS loss improves to `0.014013`, marginally below the scale-balanced 380k value `0.014024`, and absolute RMS-ratio error improves `0.034183 -> 0.032622`; mean energy MSE establishes a new best `0.104429`. Every layer remains better than initialization in loss/cosine, provenance is identical, all metrics are finite, teacher matching/frame lengths remain exact, and the complete 450k model plus all four optimizer shards is recoverable. Rank 450k first by the declared objective; keep 380k only as an absolute-RMS diagnostic fallback rather than evidence of continuing scale degradation. Training continued through step 452370 (`45.07%` of the medium contract and `35.21%` of epoch 2) without formal errors.
- 2026-08-01 Stage211A medium step-460000 strongest broad fixed-best gain in the current interval: total fixed loss improves `0.140679 -> 0.138960` from 450k (`-1.22%`) and is `48.72%` below initialization; mean 70-layer loss/cosine improve `0.137712/0.950989 -> 0.136019/0.951601`. Loss and cosine improve in `65/70` layers, and every ten-layer depth band improves by `0.81-1.58%`, including `0.81%/1.58%` in the difficult `10-19/20-29` bands. Mean energy MSE and log-RMS loss establish new bests `0.103185/0.013962`; absolute RMS-ratio error is essentially flat to 450k (`0.032754` versus `0.032622`) and remains well below initialization. Every layer remains better than initialization in loss/cosine, provenance is unchanged, all metrics are finite, online matching/frame lengths are exact, and the complete 460k export plus all four optimizer shards is recoverable. Rank 460k first by the declared objective; the broad gain shows that this recipe has not saturated and must continue through exact three-epoch coverage. Training resumed through step 463080 (`46.14%` of the medium contract and `38.41%` of epoch 2) without formal errors.
- 2026-08-01 Stage211A medium step-470000 one-gate reversal after the strong 460k gain: total fixed loss changes `0.138960 -> 0.139491` (`+0.382%`), mean 70-layer loss changes `0.136019 -> 0.136517`, and cosine changes `0.951601 -> 0.951462`. Only `27/70` layers improve loss and cosine. Bands `0-39` and `50-59` regress by `0.32-0.70%`, `60-69` is flat at `+0.013%`, and only `40-49` improves by `0.62%`; log-RMS and absolute RMS-ratio errors also rotate upward to `0.014503/0.034402`. This is not yet a sustained reversal: 470k remains `0.84%` better than 450k and `48.52%` below initialization, every layer remains better than initialization in loss/cosine, provenance is unchanged, all metrics are finite, and online matching/frame lengths remain exact. The complete 470k model plus four optimizer shards is recoverable. Retain 460k as the fixed-best candidate, continue the exact three-epoch run unchanged, and use 480k to distinguish oscillation from persistent degradation. Training resumed through step 470210 (`46.85%` of the medium contract and `40.54%` of epoch 2) without formal errors.
- 2026-08-01 Stage211A medium step-480000 confirms a shallow two-gate fixed-set reversal while preserving the exact-coverage contract: total fixed loss changes `0.139491 -> 0.139822` from 470k (`+0.238%`) and is `0.620%` above the 460k best; mean 70-layer loss/cosine change `0.136517/0.951462 -> 0.136871/0.951338`. Relative to 470k only `26/70` layers improve and bands `10-69` regress by `0.10-0.76%`, while `0-9` recovers by `0.22%`; relative to 460k only `19/70` layers improve and all seven bands regress by `0.11-0.91%`. Energy MSE, log-RMS loss, and absolute RMS-ratio error also rotate upward to `0.103980/0.014565/0.034843`. This is persistent local degradation, but not collapse or proof that the required second and third traversals should be skipped: 480k remains `48.40%` below initialization, every `70/70` layer remains better than initialization, provenance is identical, all metrics are finite, and online matching has zero missing rows and zero frame delta. The complete export, model state, and four optimizer shards are recoverable; the ranked top-k index correctly retains 460k first. Continue the mandatory three-epoch run unchanged, retain 460k as the phase-selection candidate, and use 490k to determine whether the local reversal extends. Training resumed through step 480960 (`47.92%` of the medium contract and `43.76%` of epoch 2) without formal errors.
- 2026-08-01 Stage211A medium step-490000 interrupts the two-gate reversal with a shallow partial recovery but does not retake the 460k best: total fixed loss improves `0.139822 -> 0.139748` from 480k (`-0.053%`), mean 70-layer loss improves `0.136871 -> 0.136789`, and cosine improves `0.951338 -> 0.951384`; loss/cosine improve in `41/70` and `43/70` layers. Bands `20-59` recover by `0.15-0.44%`, while `0-9` and `60-69` regress by `0.40%/0.63%` and `10-19` is effectively flat at `+0.017%`. Relative to 460k, total loss remains `0.567%` higher, only `24/70` layers improve, and only band `40-49` is better; the other bands are `0.08-1.41%` higher. Energy MSE is essentially flat to 480k at `0.103984`, while log-RMS and absolute RMS-ratio errors continue upward to `0.014782/0.035497`, so this is a plateau-scale hidden/cosine recovery with residual amplitude drift rather than a new optimum. The result remains `48.43%` below initialization, every layer remains better than initialization, provenance is identical, all metrics are finite, online matching/frame lengths remain exact, and the complete model plus all four optimizer shards is recoverable. The ranked index correctly keeps 460k first and places 490k third behind 470k. Continue the mandatory exact three-epoch coverage unchanged and retain 460k as the current phase-selection candidate. Training resumed through step 491650 (`48.98%` of the medium contract and `46.95%` of epoch 2) without formal errors.
- 2026-08-02 Stage211A medium step-500000 establishes a broad recovery from the 470k-490k plateau and nearly retakes the fixed best: total fixed loss improves `0.139748 -> 0.139300` from 490k (`-0.321%`) and is only `0.244%` above 460k; mean 70-layer loss/cosine improve `0.136789/0.951384 -> 0.136339/0.951526`. Loss and cosine improve in `49/70` layers. Six of seven depth bands improve by `0.36-0.89%`; only the difficult `10-19` band rotates upward by `0.20%`. Relative to 460k, bands `20-59` are already better by `0.12-0.78%`, while `0-9`, `10-19`, and `60-69` remain higher by `0.40%`, `1.13%`, and `0.58%`, respectively. Scale diagnostics recover with the hidden objective: mean energy MSE, log-RMS loss, and absolute RMS-ratio error improve from `0.103984/0.014782/0.035497` to `0.103568/0.014492/0.034803`. Every layer remains better than initialization, the result is `48.59%` below the initial fixed loss, provenance is identical, all metrics are finite, online matching has zero missing rows and zero frame delta, and the complete export plus four optimizer shards is recoverable. The ranked index keeps 460k first and promotes 500k to second. Continue the unchanged exact three-epoch recipe and use subsequent gates to determine whether the recovery can resolve the remaining shallow/difficult-band gap. Training resumed through step 502250 (`50.04%` of the medium contract and `50.12%` of epoch 2) without formal errors.
- 2026-08-02 Stage211A medium step-510000 completes the plateau recovery and establishes a marginal aggregate fixed best: total fixed loss improves `0.139300 -> 0.138952` from 500k (`-0.249%`) and edges below the prior 460k best by `0.0058%`; mean 70-layer loss/cosine improve `0.136339/0.951526 -> 0.136008/0.951671`, with `52/70` losses and `51/70` cosines improving relative to 500k. The difficult `10-19` band recovers by `0.589%`; bands `0-9`, `30-49`, and `60-69` also improve by `0.07-0.73%`, `20-29` is flat at `+0.018%`, and only `50-59` rotates upward by `0.425%`. Relative to 460k, `39/70` layers improve and bands `20-49` are better by `0.31-1.14%`, while the remaining four bands are `0.27-0.53%` higher. This is an aggregate/cosine best rather than a scale-balanced breakthrough: energy MSE is only slightly higher than 460k (`0.103420` versus `0.103185`), but log-RMS and absolute RMS-ratio errors remain higher (`0.014810/0.035274` versus `0.013962/0.032754`). Every layer remains better than initialization, total fixed loss is `48.72%` below initialization, provenance is identical, all metrics are finite, teacher matching/frame lengths are exact, and the complete model plus all four optimizer shards is recoverable. Rank 510k first by the declared aggregate objective and retain 460k as the amplitude-balanced fallback. Continue the mandatory three-epoch recipe unchanged. Training resumed through step 512840 (`51.09%` of the medium contract and `53.28%` of epoch 2) without formal errors.
- 2026-08-02 Stage211A medium step-520000 delivers a broad fixed-set breakthrough after the plateau recovery: total fixed loss improves `0.138952 -> 0.135969` from 510k (`-2.147%`) and is `2.152%` below the previous robust 460k best; mean 70-layer loss/cosine improve `0.136008/0.951671 -> 0.133048/0.952704`. Every `70/70` layer improves both loss and cosine relative to 510k, and all seven depth bands improve by `1.63-3.79%`, led by bands `50-59` and `60-69`. Relative to 460k, `67/70` losses and `66/70` cosines improve, and every depth band is better by `1.26-3.52%`, including the difficult `10-19` band. Mean energy MSE establishes a clear new best at `0.101102`; log-RMS and absolute RMS-ratio errors recover from 510k to `0.014243/0.033114` and are now close to, though still slightly above, the scale-balanced 460k values `0.013962/0.032754`. Total fixed loss is `49.82%` below initialization, every layer remains better than initialization, provenance is identical, all metrics are finite, teacher matching has zero missing rows and zero frame delta, and the complete export, model state, and four optimizer shards are recoverable. Rank 520k first without qualification by the declared aggregate objective; this all-layer gain proves that additional full-data traversal is still productive and the mandatory three-epoch coverage must continue unchanged. Training resumed through step 523360 (`52.14%` of the medium contract and `56.43%` of epoch 2) without formal errors.
- 2026-08-02 Stage211A medium step-530000 confirms and extends the 520k breakthrough: total fixed loss improves `0.135969 -> 0.134461` (`-1.109%`) and is `3.238%` below the former 460k best; mean 70-layer loss/cosine improve `0.133048/0.952704 -> 0.131579/0.953227`. Loss and cosine improve in `68/70` layers relative to 520k, while all seven depth bands improve by `0.74-1.87%`; relative to 460k every band is now better by `2.12-4.61%`. Mean energy MSE crosses below `0.100` to a new best `0.099987`. Absolute RMS-ratio error improves to `0.032218`, now better than the scale-balanced 460k value `0.032754`; log-RMS also recovers to `0.014092` and is only slightly above 460k's `0.013962`. Total fixed loss is `50.38%` below initialization, every layer remains better than initialization, provenance is identical, all metrics are finite, teacher matching/frame lengths remain exact, and the complete export, model state, and four optimizer shards are recoverable. Rank 530k first and treat the two consecutive broad gains as evidence that the second traversal remains productively convergent rather than oscillating around saturation. Continue the mandatory three-epoch recipe unchanged. Training resumed through step 530320 (`52.84%` of the medium contract and `58.57%` of epoch 2) without formal errors.
- 2026-08-02 Stage211A medium step-540000 extends the broad descent for a third consecutive fixed best: total fixed loss improves `0.134461 -> 0.133773` (`-0.512%`) and is `3.733%` below the former 460k best; mean 70-layer loss/cosine improve `0.131579/0.953227 -> 0.130909/0.953462`. Loss and cosine improve in `54/70` layers relative to 530k. Six depth bands improve by `0.43-0.81%`; band `40-49` is effectively flat at `+0.025%`. Mean energy MSE and log-RMS establish/recover to `0.099466/0.013975`, while absolute RMS-ratio error rotates slightly from `0.032218` to `0.032414` but remains better than 460k. Relative to 460k, `68/70` losses and cosines improve and every band is `2.62-5.22%` better. Total fixed loss is `50.63%` below initialization, every layer remains better than initialization, provenance is identical, all metrics are finite, teacher matching/frame lengths remain exact, and the complete export, model state, and four optimizer shards are recoverable. Rank 540k first; the improvement rate is slowing after the 520k breakthrough but the objective is still moving materially and is not saturated. Continue the mandatory three-epoch recipe unchanged. Training resumed through step 540780 (`53.88%` of the medium contract and `61.64%` of epoch 2) without formal errors.
- 2026-08-02 Stage211A medium step-550000 restores a stronger broad gain and is the first checkpoint in this interval to establish new bests across every primary component diagnostic: total fixed loss improves `0.133773 -> 0.132613` (`-0.867%`) and is `4.567%` below the former 460k best; mean 70-layer loss/cosine improve `0.130909/0.953462 -> 0.129753/0.953875`. Loss improves in `65/70` layers and cosine in `66/70`. All seven depth bands improve by `0.13-1.43%`; the previously flat `40-49` band recovers by `1.16%`. Mean energy MSE, log-RMS, and absolute RMS-ratio error all establish new bests at `0.098594/0.013877/0.031652`, so the aggregate gain is accompanied by both directional and amplitude convergence. Relative to 460k, every band is `3.40-6.06%` better and `68/70` losses/cosines improve. Total fixed loss is `51.06%` below initialization, every layer remains better than initialization, provenance is identical, all metrics are finite, teacher matching/frame lengths remain exact, and the complete export, model state, and four optimizer shards are recoverable. Rank 550k first and continue the mandatory exact three-epoch recipe unchanged. Training resumed through step 551140 (`54.91%` of the medium contract and `64.73%` of epoch 2) without formal errors.
- 2026-08-02 Stage211A medium step-560000 produces another strong all-depth breakthrough: total fixed loss improves `0.132613 -> 0.129968` (`-1.995%`) and is `6.471%` below the former 460k best; mean 70-layer loss/cosine improve `0.129753/0.953875 -> 0.127163/0.954800`. Loss and cosine improve in `69/70` layers relative to 550k, and all seven depth bands improve by `1.59-2.40%`. Mean energy MSE and log-RMS reach new bests `0.096650/0.013657`; absolute RMS-ratio error rotates only slightly from the 550k best `0.031652` to `0.031798` and remains substantially below 460k. Relative to 460k, every band is `4.94-8.01%` better and `68/70` losses/cosines improve. Total fixed loss is `52.04%` below initialization, every layer remains better than initialization, provenance is identical, all metrics are finite, teacher matching/frame lengths remain exact, and the complete export, model state, and four optimizer shards are recoverable. Rank 560k first; the renewed broad gain confirms that the second full traversal remains far from exhausted. Continue the mandatory exact three-epoch recipe unchanged. Training resumed through step 561710 (`55.96%` of the medium contract and `67.89%` of epoch 2) without formal errors.
- 2026-08-02 Stage211A medium step-570000 establishes only a marginal aggregate best with a clear depth rotation after the strong 560k gain: total fixed loss improves `0.129968 -> 0.129869` (`-0.077%`), mean 70-layer loss improves `0.127163 -> 0.127096`, and cosine edges up `0.954800 -> 0.954822`, but only `34/70` layers improve loss and cosine. Bands `0-29` improve by `0.06-0.58%`, while bands `30-69` rotate upward by `0.03-0.78%`, led by `50-59`. Mean energy MSE and log-RMS make small new bests `0.096591/0.013619`; absolute RMS-ratio error rotates from `0.031798` to `0.032144`. Relative to 460k, every band remains `5.25-7.36%` better, total fixed loss is `6.543%` lower and `52.07%` below initialization, and `68/70` losses plus `69/70` cosines improve. Provenance is identical, all metrics are finite, matching/frame lengths remain exact, and the complete export, model state, and four optimizer shards are recoverable. Rank 570k first by the declared aggregate objective but retain 560k as the more depth-balanced candidate. Treat this as a shallow post-breakthrough plateau/rotation, not divergence, and use 580k to determine whether broad convergence resumes. Continue the mandatory three-epoch recipe unchanged. Training resumed through step 572250 (`57.01%` of the medium contract and `71.04%` of epoch 2) without formal errors.
- 2026-08-03 Stage211A medium step-580000 fully resolves the 570k depth rotation with another all-depth breakthrough: total fixed loss improves `0.129869 -> 0.127506` (`-1.819%`) and is `8.243%` below the former 460k best; mean 70-layer loss/cosine improve `0.127096/0.954822 -> 0.124787/0.955617`. Every `70/70` layer improves loss and `69/70` layers improve cosine relative to 570k; all seven depth bands improve by `1.45-2.32%`, including complete recovery of the prior `30-69` rotation. Mean energy MSE, log-RMS, and absolute RMS-ratio error all establish new bests `0.094737/0.013036/0.030666`. Relative to 560k, every layer improves loss, `69/70` improve cosine, and every band is `1.42-2.42%` better. Relative to 460k, all `70/70` layers improve both loss and cosine. Total fixed loss is `52.94%` below initialization, provenance is identical, all metrics are finite, matching/frame lengths remain exact, and the complete export, model state, and four optimizer shards are recoverable. Rank 580k first without qualification; this gate proves that 570k was a transient rotation rather than sustained degradation and that the second traversal remains productively convergent. Continue the mandatory exact three-epoch recipe unchanged. Training resumed through step 583080 (`58.09%` of the medium contract and `74.28%` of epoch 2) without formal errors.
- 2026-08-03 Stage211A medium step-590000 extends the recovered all-depth convergence: total fixed loss improves `0.127506 -> 0.125538` (`-1.543%`) and is `9.659%` below the former 460k best; mean 70-layer loss/cosine improve `0.124787/0.955617 -> 0.122856/0.956304`. Loss and cosine improve in `66/70` layers relative to 580k, and all seven depth bands improve by `1.10-2.41%`, led by `60-69`. Mean energy MSE, log-RMS, and absolute RMS-ratio error all establish new bests `0.093277/0.012842/0.030177`. Relative to 460k, every layer improves loss and cosine and every depth band is `8.54-11.58%` better. Total fixed loss is `53.67%` below initialization, provenance is identical, all metrics are finite, matching/frame lengths remain exact, and the complete export, model state, and four optimizer shards are recoverable. Rank 590k first; the continued broad gain shows no saturation in the latter part of the second traversal. Continue the mandatory exact three-epoch recipe unchanged. Training resumed through step 590360 (`58.82%` of the medium contract and `76.45%` of epoch 2) without formal errors.
- 2026-08-03 Stage211A medium step-600000 establishes a further fixed best with slower but still broad convergence: total fixed loss improves `0.125538 -> 0.124707` from 590k (`-0.662%`), mean 70-layer loss/cosine improve `0.122856/0.956304 -> 0.122065/0.956558`, and loss/cosine improve in `57/70` and `59/70` layers. Six depth bands improve by `0.05-1.57%`; band `40-49` is effectively flat at `+0.017%`. Mean energy MSE, log-RMS, and absolute RMS-ratio error all establish new bests `0.092571/0.012402/0.028287`. Total fixed loss is `53.98%` below initialization, every layer remains better than initialization in both loss and cosine, evaluation provenance is identical, all values are finite, and online matching has zero missing rows and zero frame delta. The exported model, DeepSpeed model state, and all four optimizer shards are complete, and the ranked index promotes 600k to first. Training resumed through step 601320 (`59.91%` of the medium contract and `79.73%` of epoch 2) without formal errors. The interval gain has slowed after the larger 580k-590k improvements but remains broad and scale-consistent, so continue the mandatory exact three-epoch recipe unchanged.
- 2026-08-03 Stage211A medium step-610000 establishes another broad fixed best: total fixed loss improves `0.124707 -> 0.124092` from 600k (`-0.493%`), mean 70-layer loss/cosine improve `0.122065/0.956558 -> 0.121452/0.956793`, and loss/cosine improve in `60/70` and `63/70` layers. All seven depth bands improve by `0.05-0.90%`, led by `50-59`, so the gain is not a depth rotation. Mean energy MSE also improves `0.092571 -> 0.092172`; log-RMS and absolute RMS-ratio error rotate slightly upward `0.012402/0.028287 -> 0.012556/0.029077`, which records a small amplitude-diagnostic regression without overturning the broad hidden/cosine gain. Total fixed loss is `54.20%` below initialization and every layer remains better than initialization in both loss and cosine. Evaluation provenance is identical, all values are finite, matching/frame lengths remain exact, and the exported model, DeepSpeed model state, and four optimizer shards are complete. The ranked index promotes 610k to first. Training resumed through step 612110 (`60.98%` of the medium contract and `82.95%` of epoch 2) without formal errors. Continue the mandatory exact three-epoch recipe unchanged and use the next gates to verify that amplitude diagnostics recover while the aggregate descent continues.
- 2026-08-03 Stage211A medium step-620000 records a clear all-layer fixed-set reversal from the 610k best: total fixed loss changes `0.124092 -> 0.127377` (`+2.647%`), mean 70-layer loss/cosine change `0.121452/0.956793 -> 0.124685/0.955680`, and all `70/70` layers regress in loss, cosine, and energy MSE. Every depth band increases by `1.79-3.90%`, with the largest increase in `60-69`; mean energy MSE, log-RMS, and absolute RMS-ratio error also change adversely `0.092172/0.012556/0.029077 -> 0.094762/0.013369/0.031940`. This is a genuine broad gate reversal rather than layer rotation, but it is only the first reversal after five consecutive fixed bests and remains `52.99%` below initialization with every layer still better than initialization. Provenance is unchanged, metrics are finite, matching/frame lengths remain exact, no formal error is present, and the complete export plus DeepSpeed model state and all four optimizer shards are recoverable. The ranked index correctly retains 610k first and places 620k fourth, narrowly ahead of 580k. Training resumed through step 623020 (`62.07%` of the medium contract and `86.21%` of epoch 2). Do not restart or shorten the mandatory three-epoch contract on one reversal; retain 610k as the phase-selection candidate and use 630k to distinguish transient oscillation from sustained degradation.
- 2026-08-03 Stage211A medium step-630000 substantially recovers the 620k all-layer reversal without yet retaking the 610k best: total fixed loss improves `0.127377 -> 0.125022` from 620k (`-1.849%`), mean 70-layer loss/cosine improve `0.124685/0.955680 -> 0.122398/0.956455`, and loss/cosine/energy improve in `67/70`, `66/70`, and `67/70` layers. All seven depth bands recover by `1.27-2.49%`. Mean energy MSE, log-RMS, and absolute RMS-ratio error recover `0.094762/0.013369/0.031940 -> 0.092879/0.012625/0.029503`. Relative to 610k, however, total loss remains `0.749%` higher, only `7/70` losses and `6/70` cosines improve, and every depth band remains `0.18-1.86%` higher; the three scale diagnostics are also slightly worse than 610k. Total fixed loss remains `53.86%` below initialization with every layer better than initialization. Provenance is identical, metrics are finite, matching/frame lengths are exact, and the export plus DeepSpeed model state and all four optimizer shards are complete. The ranked index places 630k third behind 610k and 600k. This establishes 620k as a largely transient oscillation rather than a second consecutive degradation, but recovery is incomplete; retain 610k as the phase-selection candidate, continue the mandatory contract unchanged, and use 640k to test whether the best is fully recovered. Training resumed through step 630330 (`62.80%` of the medium contract and `88.40%` of epoch 2) without formal errors.
- 2026-08-03 Stage211A medium step-640000 fully resolves the 620k reversal and establishes a new fixed best: total fixed loss improves `0.125022 -> 0.123255` from 630k (`-1.413%`) and is `0.674%` below the former 610k best; mean 70-layer loss/cosine improve `0.122398/0.956455 -> 0.120694/0.957063`. Relative to 630k, loss and cosine improve in `69/70` layers and energy in `67/70`, while all seven depth bands improve by `1.00-1.99%`. Relative to 610k, `47/70` losses and `46/70` cosines improve; bands `0-29` are better by `1.00-1.81%`, while bands `30-69` retain only small `0.10-0.84%` rotations. Crucially, mean energy MSE, log-RMS, and absolute RMS-ratio error all establish new bests `0.091599/0.012472/0.028870`, so the aggregate recovery is directionally and amplitude consistent. Total fixed loss is `54.51%` below initialization with every layer better than initialization in loss and cosine. Provenance is identical, all metrics are finite, matching/frame lengths remain exact, and the export plus DeepSpeed model state and all four optimizer shards are complete. The ranked index promotes 640k to first. This confirms that 620k was a transient oscillation rather than sustained degradation; continue the mandatory three-epoch contract unchanged with 640k as the new phase-selection candidate. Training resumed through step 641400 (`63.90%` of the medium contract and `91.71%` of epoch 2) without formal errors.
- 2026-08-03 Stage211A medium step-650000 is an effectively flat depth/amplitude rotation immediately after the 640k best: total fixed loss changes `0.123255 -> 0.123363` (`+0.0876%`), mean 70-layer loss changes `0.120694 -> 0.120791`, and mean cosine is effectively unchanged at `0.957063`. Loss/cosine improve in `32/70` and `34/70` layers. Bands `10-39` improve slightly by `0.05-0.26%`, while `0-9` regresses `0.79%` and bands `40-69` rotate upward by only `0.01-0.22%`. Mean energy MSE, log-RMS, and absolute RMS-ratio error change `0.091599/0.012472/0.028870 -> 0.091804/0.012921/0.030723`, so 640k remains the more amplitude-balanced checkpoint. Total fixed loss is still `54.47%` below initialization and every layer remains better than initialization in loss and cosine. Provenance is identical, metrics are finite, matching/frame lengths remain exact, and the export plus DeepSpeed model state and all four optimizer shards are complete. The ranked index retains 640k first and places 650k second. Treat this as a one-gate plateau/rotation rather than sustained degradation; continue the exact three-epoch contract unchanged and use 660k to determine whether broad descent resumes. Training resumed through step 652450 (`65.00%` of the medium contract and `95.01%` of epoch 2) without formal errors.
- 2026-08-03 Stage211A medium step-660000 extends the 650k plateau into a small depth-biased drift without constituting major divergence: total fixed loss changes `0.123363 -> 0.123607` from 650k (`+0.198%`) and is `0.285%` above the 640k best; mean 70-layer loss/cosine change `0.120791/0.957063 -> 0.121068/0.956942`. Relative to 650k, loss/cosine/energy improve in only `29/70`, `24/70`, and `27/70` layers. Band `0-9` improves `0.99%`, while bands `10-69` regress by `0.14-1.21%`, led by `50-59`; the same shallow-versus-deep pattern holds relative to 640k. Mean energy MSE rotates slightly upward `0.091804 -> 0.091924`, while log-RMS and absolute RMS-ratio error recover from 650k `0.012921/0.030723 -> 0.012657/0.030120` but remain behind 640k. Total fixed loss remains `54.38%` below initialization and every layer is still better than initialization in loss and cosine. Provenance is identical, all metrics are finite, matching/frame lengths remain exact, and the complete export plus DeepSpeed model state and four optimizer shards are recoverable. The ranked index retains 640k first and places 660k third behind 650k. Two small non-best gates now indicate an emerging late-epoch depth plateau rather than a single rotation, but the cumulative regression is only `0.285%`; retain 640k, do not interrupt the mandatory three-epoch contract, and use 670k immediately across the exact epoch-3 boundary at step 669142 to test whether reshuffling restores broad descent. Training resumed through step 663450 (`66.10%` of the medium contract and `98.30%` of epoch 2) without formal errors.
- 2026-08-09 Stage211A recovery audit after unattended continuation: Medium completed its exact `1,003,713`-step, three-epoch contract with all `42,247,508` rows consumed per epoch, a complete SHA-bound coverage receipt, zero forbidden parameter changes, and final fixed loss `0.105265`. The controller then entered Hard (`18,649,326` rows, `322,105` steps/epoch, `966,315` total steps) and stopped after committed step 14000 when a later rank-0 batch reached audio bytes that libsndfile reported as `Format not recognised`; the other ranks were terminated by the distributed launcher. The failure is a data-format decode failure, not a teacher, frame-alignment, CUDA-memory, or optimization failure. Strict coverage continues to require `webdataset_skip_decode_errors=false`: no row may be silently discarded. Add a deterministic fallback that invokes the already-required system ffmpeg on the in-memory WebDataset audio bytes only when libsndfile fails, decoding to mono float32 at 16 kHz like the existing path-based fallback. If ffmpeg also fails or returns no samples, fail with the sample key and decoder diagnostics so an actually corrupt row can be audited. Resume Hard from the complete DeepSpeed step-14000 model/optimizer state and its exact epoch batch offset; the uncommitted post-checkpoint work is intentionally replayed and is not counted twice in the final checkpoint lineage or coverage receipt. Independently restore the missing `/dev/nvidia*` device nodes before relaunch; the four PCI devices and NVIDIA kernel modules remain present.
- 2026-08-09 Stage211 restart receipt policy: a controller restart must reuse an existing immutable curriculum receipt for a segment whose exact completion checkpoint is already present, after validating its phase/difficulty, full-data contract, bound paths, SHA-256 field syntax, runtime epoch coverage, and zero-forbidden-delta audit. It must not regenerate that receipt or re-read multi-gigabyte Nano/initial/completion checkpoints merely to re-admit a previously completed segment; the final full-phase gate still independently hashes every bound artifact before promotion. This keeps restart admission strict while avoiding redundant large-file I/O after a storage interruption.
- 2026-08-09 Stage211A Hard recovery verification: the post-power-cycle XFS mount was stale (`/dev/sda2`, major/minor `8:2`) while the re-enumerated disk was `/dev/sdb2` (`8:18`); directory metadata remained visible but a direct tar read returned `EIO`. `scripts/remount_usbhd_readonly.sh` now resolves the disk by UUID, repairs the stale mount as `ro,norecovery`, and validates an 8 MiB real-file read. After remount, the strict controller reused the immutable Easy/Medium receipts, loaded all four DeepSpeed ranks from Hard `step=14000, epoch=1, epoch_batch_offset=14000`, completed the exact metadata fast-forward, and crossed the former failure point through step 15110 with zero decode/I/O/matching/frame errors. No ffmpeg fallback was needed on the replay, so the former libsndfile format error was likely caused by the failed storage path rather than a persistent unsupported sample encoding; retain the fallback as a no-skip recovery guard. The hourly monitor and W&B run `xffk5tb7` remain active.
- 2026-08-09 Stage211A Hard zero-byte source repair: the replay ultimately failed after step 15120 on `gigaspeech_POD0000005964_S0000139`. Both the curriculum entry and source Parquet `train-00077-of-00258.parquet` bind this segment to a zero-byte audio payload, while preserving `audio_id=POD0000005964`, source interval `1324.18-1333.64s`, title, transcript, and the original programme URL. A complete-dataset scan finds exactly one `audio_size=0` row in Hard and none in Easy/Medium/Long. Do not skip or synthesize this row: recover the corresponding 9.46-second waveform from the publisher's available complete episode, verify duration/format and Nano-CTC transcript agreement, place it in an immutable persistent repair shard, and redirect only this curriculum row while preserving its key, order, frame count, row count, and three-epoch exposure contract. Emit a repair receipt binding source metadata, downloaded programme SHA-256, crop parameters, repaired waveform/shard SHA-256, original/repaired row SHA-256, and Nano verification result. Future curriculum builders and validators must reject non-positive audio sizes before training.
- 2026-08-09 Stage211A Hard audited source repair completed: Parquet row group 24/index 632 was re-read and confirms `segment_id=POD0000005964_S0000139`, interval `1324.1800537-1333.6400146s`, and zero audio bytes. The currently published complete episode (`SHA-256=473d744bb653e4eafbdae3562008eec6762eb12823ae9af8b1ca3215d3a0a6f1`) has a changed advertisement timeline, so direct reuse of the old timestamp was invalid; a Nano-guided search located the exact utterance at the current decoded interval `1091.72-1101.1799375s`. The recovered 16 kHz mono PCM waveform has 151359 samples (`SHA-256=226db6a62506927ce3638f3c729aadb3b4b201480450b0dcec113cddbf227ca9`) and direct Nano CTC produces all 29 normalized reference words with English WER/CER `0.0` and 158 logit frames. `scripts/repair_stage211_zero_audio.py` packages the waveform plus unchanged source JSON into an immutable repair tar (`SHA-256=02fed448b96ac6a4427cb2cfc7022a30ff03938ae18ba920cf291618da61458e`), replaces only Hard bucket `0014` part `000000` line 24327, and emits a hash-bound receipt under `~/rwkvasr_data/stage211_audio_repairs/POD0000005964/`. The row retains key, split, `num_frames=946`, `_stage179_sample_index=155275`, order, total rows, steps, and three-epoch exposure. Length-index parsing and future Stage179 construction now reject explicit non-positive audio sizes.
- 2026-08-09 Stage211 hidden-eval representativeness correction: an audit of the immutable shared 256-row periodic hidden gate found `256/256` rows are short Chinese Easy samples (`117` AISHELL-3 and `139` Common Voice CN, `119-187` frames, mean `167.19`). Preserve this exact gate and its hashes during the active Stage211A run so historical 10k curves and checkpoint ranking remain reproducible, but do not interpret a Hard/Long increase on it as global hidden-alignment divergence or use it alone for phase promotion. Add a separate immutable sidecar hidden-eval manifest sampled deterministically across Easy/Medium/Hard/Long and English/Chinese wherever each cell exists (Long is Chinese-only), bind every source manifest/part and selected row by SHA-256, and report per-cell plus macro loss/layer metrics on phase-start and phase-end checkpoints. The sidecar must not mutate the active train manifest, optimizer, checkpoint ranking, or supervisor. Full normalized five-dataset English WER/Chinese CER remains the authoritative phase-level behavioral gate after each complete A/B/C/D stage.
- 2026-08-09 Stage211A Hard stratified step-20k diagnosis: materialized seven immutable 256-row cells (`easy_en/easy_zh/medium_en/medium_zh/hard_en/hard_zh/long_zh`, `1,792` rows total) with receipt SHA-256 `12147615e293ab7abdec17578a9f8c4e26b3733612697034060cd6555358bab5`. A paired evaluation of the exact Medium-final initialization and Hard step 20k shows equal-cell macro hidden loss `0.137867 -> 0.124187` (`-9.923%`). Hard English/Chinese improve `20.064%/17.038%` with loss and cosine better in all `70/70` layers, Long Chinese improves `26.630%` with `69/70` layers better, Medium English improves `3.623%`, while Medium Chinese regresses `4.340%` and Easy English/Chinese regress `5.037%/9.470%`. The original short-Chinese Easy gate therefore overstates global degradation: it rises `0.105260 -> 0.125781` by step 20k and continues monotonically to `0.139036` at step 60k, while the representative step-20k macro already improves strongly. Continue the exact Hard three-epoch contract without changing architecture or optimizer. Preserve a later full-phase sidecar comparison to determine whether Easy/Medium regressions recover; if not, add a small language-balanced old-difficulty replay at a future phase boundary rather than interrupting or rewriting the active Hard segment. Public English WER/Chinese CER remains required before Stage211A promotion.
- 2026-08-09 Stage211 representative hidden-promotion gate: final Mixer and Block phase evaluation must run the same checkpoint pair on all seven immutable stratified cells in addition to retaining the original 256-row report for historical diagnostics. The hidden gate must bind the stratified receipt, every cell manifest/report, both checkpoint hashes, and exact `1,792`-sample coverage. Promotion requires lower equal-cell macro loss; macro loss and cosine improvement in at least `68/70` layers; lower Hard-English, Hard-Chinese, and Long-Chinese loss; no cell regression above `10%`; and improving macro weak bands `10-19` and `20-29`. The Hard English/Chinese cells must each improve loss and cosine in all `70/70` layers. Block additionally requires lower stratified macro CTC-decoder hidden loss. Preserve the legacy single-cell result in the gate artifact but use the representative criteria for final pass/fail when the SHA-bound stratified summary is present. This changes evaluation and phase admission only; it must not alter or interrupt the active Mixer optimizer, data order, exact three-epoch coverage, or BiRWKV architecture.
- 2026-08-09 Stage211 representative gate implementation verification: `finalize_stage211_phase.py` now runs the historical paired report plus all seven immutable sidecar cells for Mixer/Block, summarizes macro/per-layer/weak-band and optional decoder-hidden metrics, and supplies the SHA-bound summary to the hidden gate. Replaying the Medium-final versus Hard-step-20k pair produces `legacy_gate_passed=false`, `stratified_gate_passed=true`, and final `gate_passed=true`; macro loss/cosine improve in `69/70` layers, while weak bands `10-19` and `20-29` reduce loss by `8.683%` and `9.452%`. The focused and complete Stage211 regression suites pass (`58` and `77` tests respectively). This is evaluation-only and leaves the active four-GPU Hard optimizer and supervisor untouched.
- 2026-08-09 Stage211 representative gate rebuild invariant: the outer phase gate must reproduce a Mixer/Block hidden gate with the exact SHA-bound stratified summary recorded by that hidden gate. Rebuilding without `stratified_summary_path` silently changes the decision back to the legacy short-Chinese-only rule and must be rejected. Validate the recorded summary path/hash before rebuilding, pass it to `build_hidden_alignment_gate`, and require byte-equivalent structured output before phase promotion. Legacy hidden reports without a stratified summary remain readable only for historical validation; the current A/B finalizer always emits the representative binding.
- 2026-08-09 Stage211C representative logits gate: the independent final-logit gate must not rely only on the historical 256-row short-Chinese Easy split. Reuse the immutable seven-cell `easy_en/easy_zh/medium_en/medium_zh/hard_en/hard_zh/long_zh` sidecar (`256` rows per cell, `1,792` total) and evaluate the exact Stage211C initialization/final checkpoint pair on every cell with fixed features. Bind every report, manifest, checkpoint, and receipt by SHA-256. The representative gate must pass the existing macro full-KL, conditional-nonblank-KL, blank calibration, all/active top-1, token-error/deletion, sequence-exact, nonblank-rate, and collapsed-length checks; require Hard EN, Hard ZH, and Long ZH to improve full KL, conditional KL, and CTC token ER; and reject any cell whose token ER regresses by more than `3` absolute percentage points. Preserve the legacy report for historical diagnostics, but when the representative summary is present it governs the Stage211C logits gate. The complete normalized five-dataset EN WER/ZH CER versus Nano threshold remains an additional mandatory behavioral gate.
- 2026-08-09 Stage211A Hard 70k fixed diagnostic: the uninterrupted run exported the exact `step-70000.pt` plus DeepSpeed recovery state and evaluated all 256 historical fixed rows. Legacy short-Chinese Easy loss is `0.141348`, up from the Hard initialization `0.105260` and step-60k `0.139036`; all `70/70` mixer layers have worse loss and cosine on this cell. In contrast, Hard training-batch means continue down from `0.111148` over steps 50k-60k to `0.110168` over 60k-70k, and the representative Hard-step-20k macro had already improved `9.923%`. Treat 70k as stronger evidence of old-difficulty forgetting, not Hard divergence. Do not interrupt or alter the exact Hard three-epoch contract; final A promotion still requires the seven-cell representative gate and complete public EN WER/ZH CER. If the final representative gate exceeds its Easy/Medium regression bound, add a small language-balanced replay only after the full A curriculum completes rather than selecting an early Hard checkpoint or rewriting the active optimizer trajectory.
- 2026-08-09 Stage211 hourly-monitor attempt isolation: a training log is append-only across restart attempts, so an hourly health check must not rescan historical Traceback, OOM, NCCL, non-finite, teacher-missing, frame-delta, dropped-tail, or skipped-sample records from a completed failed attempt. Scope both health errors and attempt-sensitive diagnostics to the suffix beginning at the last `[rwkvasr] Distributed init complete.` marker; use the whole file only when no marker exists. Keep latest-step reporting, checkpoint/public-eval coverage, GPU telemetry, mount state, and disk space in each snapshot. Default polling remains `3600s`, with an explicit one-shot mode for deterministic verification. This monitor change is observational only and must not signal, restart, reconfigure, or otherwise modify the active Stage211 training process.
- 2026-08-10 Stage211A Hard step-110k domain-retention diagnostic: the recovered four-GPU run continued through step `112030` with zero current-attempt decode/I/O/CUDA/NCCL, teacher-missing, or frame-delta errors. Hard online ten-thousand-step mean mixer loss decreases from `0.116258` over `10k-20k` to `0.107060` over `100k-110k`, while the historical 256-row short-Chinese Easy gate changes monotonically from the Hard initialization `0.105260` to `0.119994/0.141348/0.150130` at `10k/70k/110k`; at 110k all `70/70` layers have higher loss, mean layer loss changes `0.103155 -> 0.147134`, and mean cosine changes `0.963314 -> 0.949484`. This is strong evidence of old-Easy-domain forgetting but does not contradict the representative Hard-step-20k seven-cell macro gain `0.137867 -> 0.124187` (`-9.923%`). Continue the exact Hard three-epoch contract without changing the optimizer or BiRWKV architecture. The final Mixer gate must reevaluate all seven cells and the complete five-dataset normalized English-WER/Chinese-CER suite; failure of either gate must prevent Block promotion and trigger a separately receipted, language-balanced replay correction only after full Hard/Long coverage is complete.
- 2026-08-10 Stage211 A/B/C/D evaluation-chain audit: the strict supervisor runs `mixer -> block -> logits -> labeled SFT` under `set -euo pipefail`, and every transition requires the previous final checkpoint plus its passing SHA-bound promotion receipt. Mixer and Block finalizers require all four Easy/Medium/Hard/Long three-epoch coverage receipts, the seven-cell representative hidden gate, and complete normalized greedy-CTC AISHELL-1, LibriSpeech clean/other, Common Voice EN, and WenetSpeech TEST_NET evaluation against the same bound Nano predictions; each must improve public macro error and deletion while limiting every dataset regression. Logits additionally requires the representative logits gate and every public dataset within `1.20x` Nano and `3.0` absolute points. Labeled SFT requires its exact one-epoch coverage receipt, no public-dataset regression relative to Logits, macro improvement, and the same every-dataset Nano threshold. The immutable final report validates the ordered calibration/A/B/C/D checkpoint and Nano-teacher SHA chains and renders every stage's English WER and Chinese CER. These are admission requirements, not best-effort reports: a missing, incomplete, mismatched, or failing artifact stops the chain before the next stage.
- 2026-08-10 Stage211A Hard step-114k immutable seven-cell diagnostic: preserve the complete periodic checkpoint outside the active top-k rotation as `~/rwkvasr_eval/stage211_hard_step114000_stratified/checkpoint/step-114000.pt` (`SHA-256=56f772330a717aead6706d65ac96d6b11c7514c820bb9d87fda5ed64a62be2a4`) and evaluate the exact Medium-final/Hard-114k pair on all seven receipt-bound cells. The validated `1,792`-row summary (`SHA-256=7a10bee94bcc0381da9fbd55275a2675e23137ea4681c3682bc25089e54ff3ce`) improves equal-cell macro loss `0.137867 -> 0.121673` (`-11.746%`), with loss and cosine better in every `70/70` macro layer and weak bands `10-19/20-29` improving `12.359%/13.290%`. Current-domain gains strengthen materially from step 20k: Hard EN/ZH improve `26.210%/22.567%`, Long ZH improves `33.759%`, and all three cells improve loss and cosine in every layer; Medium EN improves `4.527%`. Retention moves in the opposite direction: Medium ZH regresses `6.708%`, Easy EN regresses `9.355%`, and Easy ZH regresses `17.394%` with `0/70` layers improved. Re-evaluating the exact production criterion returns `stratified_gate_passed=false` solely because Easy ZH exceeds the `10%` per-cell regression bound. This proves stronger Hard/Long learning and simultaneous Easy-ZH forgetting, not optimization collapse. Continue all prescribed Hard and Long exposures unchanged; do not promote to Block if the final gate still fails. After complete coverage, use a separately receipted language-balanced retention replay and rerun both the seven-cell hidden gate and full five-dataset normalized WER/CER gate before promotion.
- 2026-08-10 Stage211A post-coverage retention-replay contract: prepare a deterministic, source-aware replay manifest now, but admit and run it only after Easy/Medium/Hard/Long have completed their original exact three-epoch coverage and the final Mixer gate proves a retention failure. One replay round contains `130,000` unique rows from each of `easy_en` and `easy_zh`, `270,000` each from `medium_en` and `medium_zh`, `100,000` each from `hard_en` and `hard_zh`, plus every eligible `long_zh` row not used by a bound evaluation set, for approximately one million unique audio rows with English and Chinese exposure balanced near `1:1`. Exclude every key in the immutable seven-cell sidecar and historical fixed hidden gate so correction training cannot directly optimize promotion samples. Select without replacement by deterministic per-cell/per-source reservoir sampling, retain source and duration diversity, rebuild efficient 80-frame training buckets, and bind source manifests, exclusions, output parts, row counts, and hashes in an immutable receipt. Initialize from the completed Stage211A checkpoint; preserve the same Nano teacher, Mixer-only trainable boundary, frozen copied MLP/Norm/CTC decoder/head, online hidden objective, and BiRWKV architecture. Use a lower `1e-6` learning rate and complete the entire one-million-row round before evaluation; do not early-stop within a round. After each round, rerun the SHA-bound seven-cell hidden gate and complete normalized five-dataset WER/CER comparison. Promote only when both pass; otherwise run at most two additional complete rounds from the latest correction checkpoint. Record each round as an explicit post-coverage correction linked after the original four curriculum receipts rather than rewriting or pretending to extend any Easy/Medium/Hard/Long receipt.
- 2026-08-10 Stage211A correction admission and failure-evidence contract: phase finalization must always finish the bound seven-cell hidden evaluation and five-dataset normalized WER/CER evaluation and then persist an immutable `phase_gate` decision, whether it passes or fails. A failed gate must return a failing process status and must never create a promotion receipt, but the complete failed gate remains the required admission artifact for retention correction. Preserve `full_data_coverage.segments` and every original row/hour/exposure total exactly; append corrections only under an ordered `post_coverage_corrections` list. Correction round 1 must bind the failed original Mixer gate and initialize from the original Long completion checkpoint; rounds 2 and 3 must bind the immediately preceding failed corrected gate and initialize from that round's completion checkpoint. Every correction receipt must bind its round number, replay manifest and replay-selection receipt, exact one-round step/runtime coverage, `lr=1e-6`, unchanged Mixer-only/Nano-hidden objective, parameter-delta audit, Nano teacher SHA, initial/completion checkpoints, and previous gate SHA. The coverage validator must verify the original four-receipt chain first and the correction chain second, while reporting original coverage and correction exposure separately. Finalization evaluates the latest chained checkpoint against the original phase initialization; only a passing corrected `phase_gate` may create the Mixer-to-Block promotion receipt.
- 2026-08-10 Stage211A retention-replay production preflight: the first exact two-pass build scanned all `62,072,225` source rows and correctly refused the provisional `easy_zh=200,000` quota because only `131,084` Easy-ZH rows remain after immutable evaluation exclusions. No replacement, duplicated row, partial manifest, or training intervention is allowed to hide this capacity constraint. Use `easy_en/easy_zh=130,000`, `medium_en/medium_zh=270,000`, and `hard_en/hard_zh=100,000`; with the expected `148` eligible Long-ZH rows this yields `1,000,148` unique rows and `500,000 EN : 500,148 ZH`. Persist the complete source/cell/bucket capacity preflight before reservoir sampling so a later output-stage failure can be resumed without rescanning or weakening the immutable quota audit.
- 2026-08-10 Stage211A correction-chain representation: keep the original Easy/Medium/Hard/Long `segments` list and all original `total_*` coverage fields byte-equivalent to their four immutable receipts. A corrected Mixer gate appends enriched correction receipts only under ordered `post_coverage_corrections` and reports their aggregate rows, hours, steps, tail padding, and executed exposures in a separate `post_coverage_correction_exposure` object. The validator must first prove the unchanged four-segment chain, then recursively prove each round's failed admission gate, same replay receipt, same Nano teacher, exact previous-checkpoint initialization, one complete runtime epoch, `lr=1e-6` Mixer-only config, parameter-delta boundary, and completion checkpoint. The phase `final_checkpoint` becomes the latest correction checkpoint only after that chain validates; the phase baseline remains the original Easy initialization for hidden and public WER/CER comparison. Finalization accepts explicit correction receipts, evaluates the latest checkpoint, passes those exact receipts into phase-gate construction, and cannot create a promotion receipt from a failed or incomplete correction chain.
- 2026-08-10 Stage211A production retention replay completed: the optimized reservoir pass materialized and the independent row-level validator accepted exactly `1,000,148` unique rows (`2,218.4501778` hours, `798,642,064` feature frames), split `500,000` English and `500,148` Chinese. Cell counts are `130,000/130,000` Easy EN/ZH, `270,000/270,000` Medium EN/ZH, `100,000/100,000` Hard EN/ZH, and all `148` eligible Long ZH rows. The immutable receipt is `~/rwkvasr_data/stage211_full_curriculum/retention_replay_v1/receipt.json` with SHA-256 `0b1e389560b441d1a78f4cc5c26c5aa4693f8e476e575babc850fb7b42257749`; the bucket manifest SHA-256 is `305dcaa35e1a03681ba20519b88994c6397135112b2149ca6b0cb1769307b53e`. This artifact is preparation only and must not be admitted before original Mixer Easy/Medium/Hard/Long coverage and a complete failed Mixer phase gate.
- 2026-08-10 Stage211A retention-loop continuity: use a restart-safe controller to consume an already persisted original Mixer gate, run at most three ordered correction rounds, and after every round invoke the complete seven-cell hidden plus five-dataset normalized WER/CER finalizer. A failed finalizer is an expected state transition only when its complete `phase_gate.json` independently validates with `gate_passed=false`; any missing/invalid gate is fatal. A passing round must validate its promotion receipt and write immutable selected-gate, selected-promotion, and selected-checkpoint pointers. Do not rewrite `stage211a_mixer_full_data_3ep/final_checkpoint.txt`, because it remains evidence of the original Long completion. Stage211B must initialize from the checkpoint bound by the selected Mixer promotion receipt and use that selected gate directory's public comparison as its baseline.
- 2026-08-10 Stage211 final-report selected-Mixer binding: when retention correction is admitted, the labeled-SFT finalizer and strict stepwise report must consume the immutable `mixer_selected.json` pointer rather than the original failed `mixer/phase_gate.json`. Validate the selected gate, checkpoint, promotion receipt, Nano teacher, and every recorded SHA before rendering the A/B/C/D checkpoint chain; bind the selection and selected gate hashes into `stage211_complete.json`. A legacy run without a selection may use the canonical original Mixer gate, but an explicitly requested missing or changed selection is fatal. This reporting fix does not signal or reconfigure the active Mixer process.
- 2026-08-10 Stage211 old-supervisor continuation: because a long-lived Bash supervisor retains function definitions loaded before later control-plane fixes, run one separate hourly watcher while that legacy supervisor remains active. The watcher must only query tmux and sleep; it must never signal the active job. After the legacy session exits naturally, choose the narrowest restart stage from immutable artifacts (`sft`, `logits`, `block`, `post_mixer`, otherwise `full`), recreate the canonical supervisor session, and let each current validator fail closed on stale or invalid artifacts. If the immutable final SFT report already passes, do not restart. Restore the hourly observational monitor if needed. This changes orchestration continuity only, not model parameters, optimizer state, dataset order, or phase coverage.
- 2026-08-10 Stage211 independent final-proof hardening: the immutable stepwise result is the completion audit, not merely a renderer. It must rerun the full labeled-SFT completion validator (exact `283,868` train rows, `809.12755` hours, one complete runtime epoch, no drop/skip, config, teacher, log, and checkpoint hashes), independently validate the Logits-to-SFT promotion receipt, and require the SFT report's recorded Mixer and Logits phase gates to be byte-identical to the exact gates used in the ordered report. The report may be emitted only after these checks plus the existing A/B/C three-epoch coverage, same-Nano, checkpoint-chain, five-dataset normalized WER/CER, and every-dataset final threshold checks all pass.
- 2026-08-10 Stage211 final coverage presentation: the final machine-readable result and Markdown must expose one normalized coverage row for each requested alignment step. Layer/Mixer, Block, and Logits report the validated unique audio rows, hours per epoch, exact three epochs, row/hour/executed exposures, and any separately receipted Mixer retention rounds. Labeled SFT reports train/eval rows, labeled hours, exact epoch count, and executed exposures. Keep this alongside the per-stage English WER/Chinese CER table so the final deliverable visibly proves both data sufficiency and ordered metric outcomes rather than burying coverage only inside nested gate JSON.
- 2026-08-10 Stage211 standalone final-report reproducibility: a no-override invocation of `create_stage211_stepwise_report.py` after SFT completion must resolve the exact Mixer gate recorded and hash-bound by `stage211_complete.json`. This is necessary when retention replay selected a corrected gate outside the canonical original `mixer/` directory. An explicit `--mixer-phase-gate` remains authoritative, legacy final reports without a Mixer binding fall back to the canonical gate, and the normal report validator still rejects any path/hash mismatch.
- 2026-08-10 Stage211 Mixer-correction preflight parity: every admitted retention-correction round must run its own restart-safe two-step four-GPU smoke on the exact replay manifest before any formal replay exposure. Bind the round, failed admission gate, replay receipt/manifest, initialization checkpoint, Nano teacher, smoke checkpoint/log, and peak-memory limit in an immutable marker. Apply the same current-attempt non-finite, OOM, CUDA/NCCL, teacher-missing, frame-delta, skipped-sample, two-optimizer-step, and peak-reserved checks as A/B/C full-profile smoke. A resumed formal correction requires its existing marker; it may not retroactively manufacture one after optimizer progress. This is a future correction admission check and does not alter or interrupt the active original Mixer curriculum.
- 2026-08-10 Stage211 two-artifact final completion: `stage211_complete.json` proves the labeled-SFT coverage and final public gate, while `stage211_stepwise_results.json` proves the requested ordered Calibration -> Mixer/Layer -> Block -> Logits -> SFT checkpoint, Nano-teacher, full-data coverage, and per-stage WER/CER chains. A continuation watcher may declare the objective complete only after rerunning the current stepwise builder as a deep validator and accepting both immutable artifacts; a shallow `complete=true` check on the SFT report alone is insufficient because the finalizer necessarily writes that input before constructing the dependent stepwise report. If the stepwise file is absent but all bound evidence is valid, the watcher may reproducibly materialize it; any mismatch must fail closed and continue from the narrowest validated stage.
- 2026-08-10 Stage211D labeled-SFT preflight proof: the exact pronunciation-only labeled audit currently accepts all `285,302` unique utterances (`283,868` train, `1,434` eval), `809.12755 h`, and `8,792,460` legal CTC tokens with zero unknown, empty, duplicate, or frame-infeasible target rows. Before any of the exact `12,045` formal optimizer steps, require an immutable two-step four-GPU SFT smoke marker bound to the promoted Logits checkpoint/receipt and exact labeled manifest/index. A missing marker may be created only while the formal output has no checkpoint, DeepSpeed step, latest-step record, or positive-step training log; it cannot be reconstructed after formal progress. Revalidate schema, artifact hashes, finite peak-reserved telemetry, and its bound inputs on reuse, include the marker path/hash in `sft_complete.json`, and make both the SFT finalizer and stepwise final proof reject a missing, changed, or unsafe marker.
- 2026-08-10 Stage211 public-metric source replay: a phase WER/CER gate must not trust finite values copied from `nano_comparison.json` alone. Before Mixer, Block, Logits, or labeled-SFT admission, replay the comparison directly from the hash-bound Nano and student prediction JSONL files with `greedy_ctc` decoding semantics and `ctc` normalization. Require exact utterance/reference coverage and compare every reported WER, CER, selected-language error rate, absolute/relative gap, insertion/deletion/substitution rate, prediction/reference ratio, changed/improved/worsened/unchanged count, and per-dataset gate decision against the recomputed value. Reject any mismatch before writing an immutable phase gate. This replay is CPU-only and does not alter an active training process.
- 2026-08-10 Stage211 requested-stage naming: keep Calibration as a separately reported initialization baseline, but expose the user's required machine-readable alignment order exactly as `rwkv_layer -> block -> logits -> sft`. Bind `rwkv_layer` to the internal `mixer` phase explicitly; the alias does not create, omit, merge, or reorder a training stage.
- 2026-08-10 Stage211 A/B/C preflight proof parity: the Mixer/Layer, Block, and Logits full-data controllers must apply the same immutable two-step four-GPU smoke guarantees as labeled SFT. Rebuild a reused marker from its exact step-2 checkpoint and current-attempt log; bind schema, phase, initialization checkpoint, Easy manifest, artifact hashes, finite peak-reserved telemetry, and the configured memory limit. A missing marker may be created only before any positive formal checkpoint, DeepSpeed directory, latest-step record, training-log step, or curriculum receipt exists. Every A/B/C phase gate and final stepwise result must bind and revalidate that marker. The already-running Stage211A marker predates formal training and is valid under this stronger contract, so applying the validator must not interrupt or restart its active Hard segment.
- 2026-08-10 Stage211 global-dedup provenance: per-segment row totals do not alone prove that Easy, Medium, Hard, and Long are disjoint. Persist and bind the Stage179 global curriculum manifest with SHA-256 `9228d24a8befe24d0e35debd63070f24a6a6313bf318308452e95656dd6708ba`. Revalidate its `blake2b16(root_or_tar_path + audio_member + audio_offset + audio_size)` dedupe key, `62,072,225` total unique audio rows, `118,465.16068055555 h`, exact four-way row/hour partition, and zero accepted duplicates in every source input. Every Layer/Mixer, Block, and Logits phase gate and final stepwise report must bind the same local persistent copy so all-data coverage remains auditable if the USB enclosure is unavailable later.
- 2026-08-10 Stage211 actual-loader manifest-chain proof: the global Stage179 summary and matching per-difficulty row totals are not sufficient by themselves to prove that the four manifests consumed by the training loader are the exact globally deduplicated partitions. Materialize one immutable receipt that binds the global-dedup manifest, the four original Stage179 bucket manifests, the four `manifest_stage211_fixed_eval.json` runtime manifests, their byte-identical train splits, the shared fixed hidden-eval sidecar, and every referenced train-part file by resolved path, byte size, row count, and SHA-256. Require exact `1,174,987 / 42,247,508 / 18,649,326 / 404` rows, `70 / 436 / 218 / 21` train parts, and the existing step/tail-padding contracts; the only permitted runtime-manifest difference from its original manifest is the appended immutable eval split. Mixer/Layer, Block, and Logits phase gates must bind and revalidate the same receipt, and the final stepwise proof must reject a missing, changed, phase-specific, or manifest-mismatched binding. Full part hashing is a one-time CPU/storage audit and must run at low I/O priority without signaling or reconfiguring the active GPU training process.
- 2026-08-10 Stage211 actual-loader production audit: the low-priority full scan accepted all `745/745` referenced train parts and exactly `62,072,225` JSONL rows. The Easy source-grouped runtime repartition and original Stage179 Easy partition both produce the same sorted dedupe-key-set SHA-256 `06af458d1ddadf72dcea917f9a410b6f6e27447f32bf8be94a7107d69ce83e36`; Medium, Hard, and Long retain byte-identical original bucket manifests. The immutable receipt is `~/rwkvasr_data/stage211_full_curriculum/stage211_loaded_manifest_chain_receipt.json` with SHA-256 `af7c38a72fd714148390de5dea7d4d32a062a78ed1e25143928e0c3db88561c1`. This proves the actual A/B/C loader inputs, rather than only their declared totals, cover the complete globally deduplicated `118,465.16068055555 h` pool.
- 2026-08-10 Stage211 calibration metric-source receipt upgrade: replay the complete `53,905` calibration Nano/student prediction pairs through the current normalized `greedy_ctc` metric implementation before final A/B/C/D reporting. The recomputation exactly preserves every WER/CER, edit decomposition, coverage count, gate field, and source hash; the only receipt change is `metric_source_recomputed=true` on all five public datasets. Preserve the pre-replay receipt as `reuse_receipt.pre_metric_source_replay.json` (`SHA-256=33c76474988c3403f262ea8a6af1e3757ce25af3ffd9dfdae99624cba9449b67`) and use the independently rebuilt canonical `reuse_receipt.json` (`SHA-256=e72ccde4aaa4c4716f302bbba996c99af2b1b287ae16c826466e58fd99b348f6`). No training or promotion artifact had bound the superseded receipt, so this proof upgrade changes no checkpoint, metric, stage admission, or active process.
- 2026-08-10 Stage211 public/train overlap audit and CV22 correction plan: do not infer evaluation cleanliness solely from Stage179's synthetic `split=train` field. The original CV22 converter indexed `validated/other/train/dev/test` together and omitted the source split from emitted metadata. Direct comparison of all `63,625` Stage178/179 CV22-English rows against the official Common Voice 22 English `test.tsv` finds `1,474` matching `client_id + sentence_id` pairs; verify these candidates by exact MP3 byte SHA-256 before classifying them as leaked test recordings. The other clean-public source records an explicit `dev/eval/test/valid/validation` exclusion, AISHELL-1 is absent from the training sources, and the exact four runtime manifests contain no WenetSpeech `TEST_NET/TEST_MEETING` identifiers. Preserve the running Mixer curriculum unchanged and complete its contracted coverage. For every A/B/C/D WER/CER gate, derive an immutable official Common Voice test manifest excluding every byte-identical training recording, filter the already-bound Nano and calibration predictions by the same utterance IDs, and rebuild all metric/provenance receipts without GPU inference. Keep the full official Common Voice result only as a contamination-marked diagnostic; it must not contribute to promotion or the final clean metric table. Bind the overlap audit, exclusion list, clean manifest, and parent/derived prediction hashes into the final proof.
- 2026-08-10 Stage211 CV22 byte-level overlap result: the low-priority encoded-audio audit verifies that all `1,474/1,474` pair candidates are byte-identical MP3 recordings in the actual Stage211 training source, so the strict Common Voice English test subset contains `14,922` clean rows. Preserve the original `16,396`-row manifest as `~/rwkvasr_data/stage211_full_curriculum/public_train_overlap_v1/commonvoice_en_test.full_contaminated.jsonl` (`SHA-256=0a77c71be0a020992f1f4eb0d8fc3caf2ef54a3919461b846ed2b8d0becb7a81`) so canonical clean-manifest migration cannot invalidate the source proof. The immutable audit is `~/rwkvasr_data/stage211_full_curriculum/public_train_overlap_v1/receipt.json` (`SHA-256=af6a0034efe6d231649966337d29aadf43b33d658c0e479e48f0ce1fae8da6b4`); its exclusion list has SHA-256 `8eb55d9cea5831d697334715fc8f0276919d3c144ec39713e09350944bb0090e`, and its derived clean manifest has SHA-256 `4cf4f3e171f888660b140e187d4848c9f0d26d77b9ed166d7f3e0e25c699e21b`. The audit binds the merged-split converter source, official `test.tsv`, archived original public manifest, Stage178 index, global-dedup manifest, and exact runtime-loader receipt. Use the `14,922`-row subset as the canonical promotion metric in Calibration and every A/B/C/D phase; preserve the full result only as a labeled contaminated diagnostic.
- 2026-08-10 Stage211 clean-public canonical installation: install the audited `14,922`-row manifest and same-ID Nano/calibration prediction subsets atomically, archive every superseded canonical artifact under `~/rwkvasr_eval/stage211_public_contaminated_archive_v1`, and rebuild normalized metrics, Nano comparison, Nano provenance, and calibration reuse receipts from source. The rollback-capable install receipt is `~/rwkvasr_eval/stage211_public_clean_v1/canonical_install_receipt.json` (`SHA-256=574314795de83ada77d5c9d14ededfcdad60474f3240a915f600deff9783ca91`); the contaminated archive receipt has SHA-256 `a414be78231f272feaa4ccdcdaf36de1d6982ab851ec9e4e02eb7ab36db3cebf`. On the clean subset, Calibration records Nano WER `21.865832%`, BiRWKV WER `57.558865%`, gap `+35.693032` points, and ratio `2.632366x`; therefore the large initialization gap is not explained by the removed leakage. The canonical Nano baseline receipt is SHA-256 `58f2ee823ae617c5484747d0c0c8791dc8413e847cb6fb3d7125e9d39a624b16`, and the independently replayed calibration reuse receipt is SHA-256 `d4ffa3c53b2388be3936d5ead12188317b73f1fe7e4e80e6c49edac96091e552`.
- 2026-08-10 Stage211 Block/Logits post-coverage liveness contract: a fail-closed phase gate is necessary but is not a training strategy. After Block or Logits has completed the original immutable Easy/Medium/Hard/Long three-epoch coverage, a failed hidden/logits or public WER/CER gate must not cause the continuation watcher to reevaluate the same unchanged checkpoint indefinitely. Generalize the separately receipted post-coverage correction chain to `mixer`, `block`, and `logits`, while preserving the original four segment receipts and totals. Admit a correction only from that phase's complete failed gate, initialize each round from the immediately preceding failed-gate checkpoint, keep the phase's exact online objective and trainable boundary, preserve the Nano teacher and BiRWKV architecture, and complete the full `1,000,148`-row (`2,218.4501778 h`) balanced replay epoch without early stopping. Re-run the phase-appropriate seven-cell hidden/logits gate and all five normalized public English-WER/Chinese-CER datasets after every complete round; create a promotion receipt only from a passing gate. Keep phase-specific immutable selection pointers so Block initializes from selected Mixer, Logits from selected Block, and SFT from selected Logits. Bound automatic rounds remain a recovery policy rather than permission to weaken any gate; exhausting them must stop with explicit failed evidence instead of looping on an unchanged checkpoint. This control-plane work must not signal, restart, or alter the active Stage211A Mixer process.
- 2026-08-10 Stage211 phase-general correction implementation verification: the restart-safe correction controller now applies the preceding contract to Mixer/Layer, Block, and Logits with phase-preserving objectives, phase-specific learning rates (`1e-6 / 1e-6 / 1.5e-7`), immutable failed-gate admission chains, complete balanced replay receipts, selected gate/promotion/checkpoint pointers, and selected A/B/C bindings in labeled-SFT plus the final stepwise proof. `START_STAGE` reconstruction revalidates or reuses each prerequisite selection before downstream initialization, including recovery from the legacy in-memory supervisor after it exits naturally. All `15` Stage211 test modules pass `127/127`; Ruff, Python compilation, Bash syntax, and `git diff --check` pass. These checks modify no checkpoint, optimizer state, dataset order, or active training process.
- 2026-08-10 Stage211 fixed-step-eval interpretation: the immutable `256`-row sidecar used by the active every-`10,000`-step evaluation is not a global validation sample. Direct metadata audit proves it is entirely Easy Chinese: `117` AISHELL-3 plus `139` Common Voice Chinese rows, with `119..187` feature frames (`167.1875` mean). From the Hard-entry baseline through step `140,000`, all `70/70` layer losses worsen on this Easy-ZH sidecar (`0.105260 -> 0.153170`, `+45.52%`), while the independent seven-cell step-`114,000` audit improves macro hidden loss by `11.75%`, including Hard EN/ZH and Long ZH gains of `26.21% / 22.57% / 33.76%`, but confirms Easy ZH regression of `17.39%`. Treat the step curve only as an Easy-ZH retention sentinel; never use it alone for checkpoint promotion or as evidence of global failure/success. Promotion must continue to require the immutable seven-cell stratified hidden/logits gate plus all five normalized public English-WER/Chinese-CER datasets. The balanced post-coverage replay is therefore a targeted retention correction, and this interpretation does not alter or interrupt the active full-data run.
- 2026-08-10 Stage211 USB-wide coverage correction: the hash-bound `62,072,225`-row, `118,465.16068055555 h` receipt proves complete loading of the four Stage179 inputs, but a direct top-level USB audit proves that it is not yet a complete inventory of usable natural ASR audio. `/media/usbhd/MLCommons/peoples_speech` contains complete clean (`804` train, `9` test, `5` validation Parquet files) and dirty (`3,140` train, `11` test, `6` validation Parquet files) source trees with embedded 16 kHz audio and explicit `duration_ms`; only `137` partially converted tar shards exist and they must not stand in for the Parquet source. `/media/usbhd/LLaSO-Align` additionally contains reusable natural-audio ZIP members. Source-path inspection identifies LJSpeech in parts `1-3`, VCTK in parts `41-51`, GigaSpeech/LibriSpeech in other early parts, and MLS English in parts `317+`. Exclude the repackaged LibriSpeech and GigaSpeech members because those corpora already occur in Stage179; admit only train audio from LJSpeech, VCTK, MLS English, and People's Speech after integrity, duration, split, duplicate, and public-overlap audits. The currently valid ZIP subset contains `12,000` LJSpeech utterances (`~28.1696 h`), `44,000` VCTK utterances (`~43.5889 h`), and `853,970` MLS-train utterances (`~3,526.18 h`); MLS ZIP parts `528/532/533/534` are truncated without an end-of-central-directory record, parts `535-550` are absent, and neither invalid nor absent archives may be counted. Treat local MiniMax/blog speech and `testnew/tts_dataset_combined.parquet` (`556,667` clips, `1,024.71 h`) as synthetic-TTS augmentation rather than core natural-ASR coverage; raw video/vocal stems remain inadmissible until speech segmentation, duration, decode, and provenance receipts exist.
- 2026-08-10 Stage211 supplemental-coverage execution rule: do not restart, signal, reorder, or mutate the active original Stage211A Easy/Medium/Hard/Long run. Before any Mixer/Layer promotion, append a separately receipted `supplemental_natural` segment initialized from the original Long completion checkpoint and consume every admitted, globally deduplicated supplemental train utterance for exactly three epochs with the unchanged online Nano hidden-state objective, trainable boundary, and no early stopping/drop/skip. The phase gate must fail closed if the supplemental inventory/manifest/loader-chain receipt or exact three-epoch runtime receipt is absent. Block and Logits must each consume the same original Stage179 pool plus the same supplemental-natural pool for three exact epochs before their own English-WER/Chinese-CER gates. Preserve original Stage179 receipts and totals verbatim and report supplemental rows/hours/exposures separately and in the combined total. Apply balanced retention correction only after both original and supplemental coverage are complete. Existing Labeled SFT remains a later, label-bound phase and does not convert unlabeled supplemental audio into labels.
- 2026-08-10 Stage211 supplemental archive-loader contract: adding a path to a manifest is not coverage proof. Extend the length-index dataset with an explicit immutable storage kind (`tar`, `zip`, or `parquet`) while retaining tar as the default for all existing manifests. ZIP rows must bind archive path, member name, uncompressed size, CRC, compression method, source/split, duration-derived frame count, and archive SHA-256/integrity result; workers must cache `ZipFile` handles and read one compressed member without archive extraction. Parquet rows must bind file path/SHA-256, row-group index, row index, source ID, split, duration, and audio format; workers must cache one projected audio row group and never materialize the entire embedded-audio table. Metadata for online unlabeled distillation may be generated from the immutable index (`text=""`, language/source/utt-id) rather than trusting unrelated instruction labels. Add runnable tests for tar backward compatibility, ZIP and Parquet decode, worker pickling/cache reset, bad/truncated archive refusal, split exclusion, exact sample/frame coverage, and no decode skipping. Build all inventory and duplicate receipts at low I/O priority without competing with or controlling the active GPU training process.
- 2026-08-10 Stage211 supplemental admission and fixed-eval contract: the supplemental runtime manifest must append the same immutable `256`-row fixed hidden-eval split used by the original Easy/Medium/Hard/Long manifests; a train-only supplemental manifest is not runnable evidence. Bind the original Stage179 global-dedup manifest, its exact source set, the supplemental source set, and the explicit exclusion of repackaged LibriSpeech/GigaSpeech into the supplemental inventory. The existing Stage179 dedupe identity is storage-object based rather than a decoded-audio fingerprint, so report the supplemental proof precisely as exact source-identity dedupe plus known-corpus exclusion and do not claim content-level cross-corpus dedupe. Preserve the original four `segments` and their totals byte-for-byte; store the dynamic `supplemental_natural` runtime receipt separately, require its inventory and manifest hashes, calculate rows/hours/steps/tail padding from that immutable inventory, and expose both original and combined coverage totals. Every production full-phase finalizer and phase-gate validator must fail closed when this supplemental field or its exact three-epoch runtime coverage is missing. The continuation controller may append the segment only after Long completes naturally and must never signal or mutate the active Stage211A Hard process.
- 2026-08-10 Stage211 supplemental continuation and migration proof: the already-running legacy full-phase controller may finish the original four segments and write an original-only `curriculum_complete.json`, but that artifact cannot admit evaluation or Block. A newly loaded controller must archive it immutably as `curriculum_complete.original_only.json`, bind the archive with a migration receipt, append `supplemental_natural` from the exact Long completion checkpoint, and write a new combined summary. This migration is idempotent across a crash before or after the rename: an identical pre-existing archive is reusable, a different archive fails closed, and a current combined summary is reused only when its complete rendered content is identical. The continuation watcher must require a complete three-epoch supplemental segment before choosing `post_mixer`; restarting that watcher is permitted only after the source and tests are committed and must not signal the active supervisor or training workers.
- 2026-08-10 Stage211 final segment-report proof: the final machine-readable and Markdown result must prove the requested order `rwkv_layer -> block -> logits -> sft`, with Calibration reported separately, and must bind one Nano teacher plus one supplemental inventory SHA-256 across Layer/Mixer, Block, and Logits. For each online phase, report all five training segments (`easy`, `medium`, `hard`, `long`, `supplemental_natural`) with rows, hours, steps, exact three epochs, row/hour exposure, tail padding, and executed-sample exposure; the per-segment sums must equal combined coverage. The final completion watcher must reject a report lacking any online phase, any five-segment proof, any exact three-epoch segment, the supplemental inventory chain, all five normalized public datasets, or the English-WER/Chinese-CER checkpoint chain.
- 2026-08-10 Stage211 supplemental proof hardening: phase validation must require the original segment sequence exactly once and in order (`easy -> medium -> hard -> long`), followed by one `supplemental_natural` receipt initialized from the exact Long completion checkpoint. Every declared supplemental source must contribute positive rows and hours. The final report must independently recompute all five-segment row, hour, row-exposure, hour-exposure, tail-padding, and executed-sample sums against the validated combined coverage; a reordered/duplicated segment, changed inventory, broken Long-to-supplemental checkpoint chain, or changed combined total must fail closed.
- 2026-08-10 Stage211 final bilingual-metric proof: the final machine report and completion watcher must prove exactly three English `WER` datasets and two Chinese `CER` datasets. Every dataset row must contain results for the complete ordered checkpoint sequence `calibration -> mixer/rwkv_layer -> block -> logits -> sft`; a five-row placeholder, missing stage, wrong language/metric pair, or incomplete stage-value map cannot satisfy final completion even when the report generator previously ran.
- 2026-08-10 Stage211 supplemental post-build receipt: after the multi-terabyte builder completes naturally, run an independent immutable validation pass before any phase consumes the inventory. Revalidate the production inventory and runtime manifest, hash every generated train-index part, recompute exact three-epoch dynamic-batch steps/tail padding/exposures, and persist per-source row/hour plus archive-status counts in `supplemental_profile_receipt.json`. Reusing an identical receipt is allowed; replacing it with different content must fail closed. Every A/B/C full-phase controller must rebuild the expected receipt from the bound inventory and require byte-identical receipt content before launching any formal segment.
- 2026-08-10 Stage211A Hard online-convergence diagnostic: deduplicate repeated resume records by optimizer step in the active `stage211_mixer_hard_full_966315steps.yaml` log and compare the first `1..10,000` Hard steps with the latest complete `160,001..165,130` window. Mean online TimeMixer hidden loss improves `0.122594 -> 0.104432` (`-14.81%`), mean cosine improves `0.956160 -> 0.962626`, mean RMS ratio remains close to one at `0.997300 -> 0.997622`, and all seven ten-layer depth bands improve both loss and cosine; per-band loss reductions span `8.94%..22.22%`. The current mount-recovery attempt resumed at step `14,000` and has zero online-teacher misses, zero layer-frame deltas, and no current-attempt OOM, CUDA, NCCL, or non-finite failures. Preserve the earlier decode-failure tracebacks in the append-only log as historical evidence and scope health checks from the final resume boundary rather than suppressing them. This proves optimization progress on the Hard training distribution only: the immutable Easy-ZH retention sentinel is still worsening, and no Layer/Mixer promotion is allowed until all original plus supplemental three-epoch coverage, the seven-cell hidden gate, and the complete normalized three-English-WER/two-Chinese-CER suite finish.
- 2026-08-10 Stage211 Nano-initialization proof: the final stepwise result must bind an independently rebuilt immutable receipt for the actual Layer/Mixer starting lineage, not merely trust configuration intent. Starting from the calibration reuse receipt, prove the selected calibration checkpoint and its provenance back to the Stage210 Nano-initialized checkpoint; bind the exact Nano `model.pt`, Stage210 train config, completed initialization log, and loader implementation hashes. Require the runtime load report to show Nano Q/K/V mapped into both BiRWKV directions with `rwkv_norm` scaling, the first-layer SVD projection errors finite and below `0.25`, all Nano non-attention MLP/Norm tensors copied, the five-layer Nano CTC decoder and projected CTC head loaded, and the non-attention encoder, decoder, and head frozen. Independently compare every frozen MLP/Norm tensor, all decoder tensors, and all projected/ignored CTC-head rows in the Stage210 checkpoint against Nano after dtype projection. The final `calibration -> rwkv_layer -> block -> logits -> sft` report and continuation watcher must fail closed if this receipt is missing, changed, bound to another Nano/calibration checkpoint, reports any tensor mismatch, or does not prove the required initialization/freeze contract. Building this receipt is a low-priority CPU audit and must not signal or reconfigure active training.
- 2026-08-10 Stage211 Nano-initialization production receipt: the independent audit binds Nano SHA-256 `81fec8616083c69377f3ceef36aba3655660ee0ca69a5d4a1e9810cd340ca499` and calibration checkpoint SHA-256 `3bbef41989961b9f1ff0e172dafd313b4976b98f5549e935ca31543303b9944e`. The actual runtime report records `1,125` encoder tensors loaded from Nano, comprising the expected `564` MLP/Norm tensors and `561` bidirectional Q/K/V/output plus first-layer input-projection tensors; first-layer Q/K/V reconstruction errors are `0.0495377 / 0.0453578 / 0.0284988`. Direct checkpoint comparison finds zero mismatches across all `564` MLP/Norm tensors, `84` five-layer CTC-decoder tensors, `60,515` teacher head rows projected into the `60,516`-row student head, and ignored row `60,514`. The freeze report proves `650` frozen tensors and `189,378,560` trainable TimeMixer/input-projection parameters. Persist this as `~/rwkvasr_eval/stage211_initialization/nano_initialization_receipt.json` with SHA-256 `d72e9ea2fbce4a70b7bb33b26abfd329cdbc297044d8add8976d8a111a09c13d`; the final report now requires `nano_initialization_chain_passed=true`.
- 2026-08-10 Stage211 labeled-SFT target proof: Stage D completion and the final stepwise result must explicitly prove pronunciation-only CTC labels rather than infer them from the SFT phase name. Bind the read-only preprocessing summary and log, require `sensevoice_tiktoken`, `text_normalization=ctc`, `sensevoice_lfr6`, unknown-token dropping, zero dropped/unknown targets, exact English/Chinese source counts, and the complete `283,868` train plus `1,434` eval rows. The full length-index audit must still require positive normalized text/token counts, zero unknown tokens, feasible CTC frame counts, unique utterance IDs, and exact bucket-manifest coverage for every row. Stage D train-config validation must prove `ctc_suppress_non_pronunciation_tokens=true`; completion must embed the exact labeled-data audit and match it to immutable provenance. The final machine/Markdown report and continuation watcher must require `ctc_label_normalization_chain_passed=true` and expose the normalization, suppression, token, language, and source counts.
- 2026-08-10 Stage211 labeled-SFT production audit: a low-priority full scan of the bound read-only length index validates all `285,302/285,302` rows (`283,868` train, `1,434` eval, `809.12755 h`) as unique-ID, non-empty pronunciation targets with feasible CTC frame counts. It records `8,792,460` CTC tokens, zero unknown tokens, `222,040` English/LibriSpeech rows, and `63,262` Chinese/AISHELL-3 rows. The preprocessing proof binds summary SHA-256 `38a2bc09e2afb26c7094ff6f120a564015f8fe7d79e6d71c57dc2d76b0071f52`, log SHA-256 `6c196476c62f007e890a6d18c9d0ce6cb716b97dac377812ec4e0f8e101f948e`, and SenseVoice tokenizer SHA-256 `747979631e813193436aabcff7c1c235d37de8097b71c563ec8b63b7a515c718`. The exact one-epoch runtime contract is `12,045` optimizer steps with `368` deterministic tail-padding exposures and `284,236` executed sample exposures; this is the Stage D labeled correction after the three online-distillation phases, not a substitute for their five-segment three-epoch coverage.
- 2026-08-10 Stage211 downstream gradient-chain audit: the generated Mixer/Block/Logits/SFT configs match the immutable phase contracts, and the actual DeepSpeed freeze implementation leaves only `.time_mixer.` plus the first-layer `.input_proj.` trainable while keeping copied Nano MLP/Norm, five-layer CTC decoder, and CTC head parameters frozen. A CPU production-path regression test runs labeled CTC loss through the frozen Nano decoder/head and proves finite nonzero gradients reach BiRWKV while every frozen CTC parameter retains `grad=None`. Upstream zero initialization of each TimeMixer output projection intentionally makes the first-layer input projection gradient zero on the first optimization step; after that output projection updates, the second identical step produces nonzero finite gradients in all `80/80` tiny-test input-projection elements. Preserve this two-step behavior in verification so a true frozen-path gradient break is detected without misclassifying upstream initialization intent. This audit does not alter the active Stage211A process, checkpoint, optimizer, or data order.
- 2026-08-10 Stage211 production optimizer-group audit: direct read-only inspection of the active Hard `step-168000` DeepSpeed optimizer shards confirms that the runtime optimizer, not only the source/config contract, preserves upstream RWKV-7 scaling. The base optimizer contains `rwkv_1x` at LR `3e-6` and `rwkv_2x` at LR `6e-6`, both with zero weight decay and Adam betas/epsilon `0.9/0.99/1e-8`. Across four ZeRO partitions, the `rwkv_1x` flat group contains `189,306,880` parameters and the `w0`-only `rwkv_2x` group contains exactly `71,680`, summing to the initialization receipt's canonical `189,378,560` trainable TimeMixer/input-projection parameters. This rules out a production optimizer-group collapse or accidental frozen Nano parameter admission and does not modify the active process or checkpoint.
- 2026-08-10 Stage211A Hard step-168000 parameter-boundary audit: compare the exported Hard checkpoint directly against the exact Medium completion checkpoint that initialized Hard. Both contain the same `12,697` tensor keys with identical shapes and dtypes; `10,905` allowed TimeMixer/input-projection aliases changed (`567,936,000` aliased numel), `18` allowed aliases remain byte-identical, all `1,774` frozen-path aliases remain byte-identical, and forbidden changed tensors are exactly zero. Together with the initialization and optimizer-group receipts, this proves the active Hard gain is still confined to the intended BiRWKV operator path and has not drifted the copied Nano MLP/Norm, five-layer CTC decoder, or CTC head.
- 2026-08-10 Stage211 production loaded-manifest-chain validation: run `validate_stage211_loaded_manifest_receipt(..., verify_part_sha256=False)` against the immutable production receipt after remount recovery. The validator accepts the fixed receipt/global-manifest SHA chain, exact runtime/source train-split identity, fixed `256`-row eval binding, all `745` local train-part path/row/size/mtime records, and all four loaded/global audio-key-set identities. The admitted original pool is exactly `62,072,225` unique rows / `118,465.16068055555 h`: Easy `1,174,987` / `1,490.1186027777776 h` / `70` parts, Medium `42,247,508` / `47,771.24217222222 h` / `436`, Hard `18,649,326` / `69,200.3677611111 h` / `218`, and Long `404` / `3.4321444444444444 h` / `21`; each segment remains bound to exactly three epochs and `29,946 / 1,003,713 / 966,315 / 105` optimizer steps. The receipt records `part_hash_audit_complete=true` and each part's SHA-256; this live validation intentionally rechecks metadata rather than rereading the full multi-terabyte part set while GPU training is active. This proves complete loading of the original Stage179 pool only; Mixer/Block/Logits promotion still additionally requires the independently validated supplemental-natural segment and its three exact epochs.
- 2026-08-10 Stage211 Unicode WER-token correction contract: the public scorer's ASCII-only `[A-Za-z0-9]+` word matcher incorrectly splits one normalized word at each non-ASCII Latin letter. The audited clean Common Voice set contains four affected references (`Popayán`, `Hückeswagen`, `Wipperfürth`, `Saarbrücken`, and `Fortià` across four utterances); LibriSpeech clean/other contain none. Replace WER tokenization with a Unicode-aware scanner after the existing NFKC/casefold, CTC normalization, punctuation/symbol removal, and apostrophe policy: emit each basic CJK ideograph as one unit, retain every other Unicode alphanumeric run as one word, and retain combining marks only within an existing word. Add direct tests for accented Latin words, decomposed combining marks, mixed English/CJK, punctuation, and unchanged ASCII behavior. Prediction JSONL, manifests, sample coverage, decode outputs, and training remain byte-identical. Before any new phase gate, archive the superseded derived Nano/calibration metric files, rebuild both metrics tables plus the Nano comparison and calibration reuse receipt from source predictions, rebuild the Nano-initialization receipt against the corrected calibration receipt, and persist an immutable correction receipt binding the prior clean canonical-install receipt and every before/after path/SHA-256. This is an evaluation-definition correction only and must not signal, restart, or mutate the active Stage211A optimizer/checkpoint/data order.
- 2026-08-10 Stage211 Unicode WER-token production result: the atomic installer audited exactly four affected Common Voice references among the fixed `52,431` public utterances, archived all eight superseded derived artifacts, and left prediction JSONL, manifests, coverage, decode outputs, and training state unchanged. Common Voice Nano WER changes from `0.21865832328736579` to `0.2186922921472025`; calibration WER changes from `0.5755886472436884` to `0.575562880218532`. The other authoritative baselines remain LibriSpeech test-clean Nano/calibration WER `0.07902845404747413 / 0.2803370359099209`, test-other `0.11963623858469298 / 0.5083680409613694`, AISHELL-1 Nano/calibration CER `0.041826946022049345 / 0.5725576289791438`, and WenetSpeech TEST_NET `0.08630970277596711 / 0.9839445624382136`. Independent source replay reproduces calibration reuse SHA-256 `b972ea27af25c597c7aaf5b3f2ee512c6d9991a3c94d8792f7c477a65a2f9862`; direct Nano provenance plus initialization validation accepts Nano checkpoint `81fec8616083c69377f3ceef36aba3655660ee0ca69a5d4a1e9810cd340ca499` and rebuilt initialization receipt SHA-256 `b98ed6834925bd90e3329543b8a656f74137cb383fb3763003343c2c6182e093`. The idempotent installer replay returns the same hashes without rewriting artifacts; immutable correction receipt SHA-256 is `0956b3ce2553b6ed335328d1ecaa29ded3aaa159aec3a26087ba4ee094c1e8a5`.
- 2026-08-10 Stage211 final metric-definition proof: the immutable calibration receipt binds corrected derived metrics but does not by itself bind the scorer implementation that all later Layer/Block/Logits/SFT subprocesses import. The final stepwise result must therefore require and validate the Unicode metric-correction receipt, including its tokenizer source path/SHA-256, every installed artifact path/SHA-256, and exact calibration-reuse plus Nano-initialization receipt bindings. It must expose `public_metric_definition_chain_passed=true`, the correction receipt path/SHA-256, tokenizer contract, and tokenizer source SHA-256 in JSON and Markdown. The continuation watcher must reject a nominally complete A/B/C/D result unless that explicit proof is present. This is a final-proof hardening only; no active training process, optimizer, checkpoint, data order, phase objective, or metric value may change.
- 2026-08-10 Stage211 multi-handoff continuation contract: the continuation watcher must remain alive across every naturally completed or failed supervisor lifetime, not launch one replacement and then exit. Its outer loop must wait at the configured hourly cadence while a supervisor exists, deep-validate the final proof whenever the supervisor disappears, select the first incomplete strict stage, launch at most one replacement supervisor, apply a bounded restart backoff, and resume watching that replacement. It must also restore the hourly monitor from inside the active-supervisor loop so a monitor/session handoff race cannot leave later phases unobserved. `WATCH_ONCE=1` remains a deterministic single-audit mode for tests and operator diagnostics. Installing this watcher revision may replace only the watcher tmux session; it must not signal or restart the active training supervisor, distributed workers, optimizer, or data builder.
- 2026-08-10 Stage211 final supplemental-dedupe disclosure: binding only the supplemental inventory path/SHA-256 is insufficient for a reader to distinguish storage/source-identity dedupe from decoded-waveform content dedupe. The final stepwise JSON and Markdown must load the common A/B/C supplemental inventory and expose its exact cross-pool mode, `source_sets_disjoint`, `content_fingerprint_complete`, known-overlap exclusions, and the bound Stage179 rows/hours/manifest SHA-256. The final continuation proof must fail closed unless the admitted production contract remains source-disjoint, uses `source_identity_plus_known_corpus_exclusion`, records content fingerprinting as incomplete, and excludes both repackaged LibriSpeech and GigaSpeech. Combined rows/hours remain valid source-identity coverage and must never be described as content-fingerprint-global coverage.
- 2026-08-10 Stage211 hourly live-progress contract: the hourly monitor must distinguish historical controller `latest_step` messages from the active distributed training position. Resolve the unique active `--config-yaml` from the live Stage211 workers, bind its `output_dir` and `max_steps`, read the newest DeepSpeed training record from that run directory, and report explicit `live_step`, target, percentage, and highest persisted top-level checkpoint. Preserve append-only supervisor/log history and current-attempt error scoping, but never present a completed prior segment or resume-start step as the live optimizer step. This monitoring-only change must not signal, restart, reprioritize, or reconfigure the supervisor, workers, optimizer, data order, or supplemental builder.
- 2026-08-10 Stage211A Hard step-180000 retention diagnostic: at the `22:24:51 CST` hourly audit the active Hard run reached live step `183,970/966,315` (`19.0383%`) with persisted `step-182000.pt`, approximately `5,080` optimizer steps since the preceding hourly snapshot. After deduplicating append-only resume records by step, the latest `170001..183970` online window has mean TimeMixer hidden loss `0.103316`, RMS ratio `0.997292`, and cosine `0.963029`, versus `0.122594 / 0.997300 / 0.956160` over the first `1..10000` Hard steps; this remains broad training-distribution convergence. The immutable `256`-row Easy-ZH retention sentinel at step `180000` is `0.15818100981414318`, however, which regresses `1.0864%` from step `170000` (`0.156481`) and `50.2765%` from initialization (`0.105260`). Treat the two results separately: do not stop exact original-plus-supplemental coverage, do not promote Mixer from the Hard online metric, and require the already prepared balanced retention replay plus the seven-cell hidden and complete normalized public WER/CER gates after full coverage.
- 2026-08-10 Stage211 USB social-media pre-admission audit: `/media/usbhd/videos`, `videos_bilibili5/6/7`, and `clean_vocals` contain `8,583` files / `92,610,944,135` bytes: `7,773` MP3, `283` WAV, and `527` MP4, with no subtitle, transcript, timestamp, or other sidecar files. The `videos` tree contributes `4,475` MP3, `263` extracted `_audio.wav`, and `526` duplicate source/derived MP4 objects; the other Bilibili trees contribute `3,298` MP3 and one otherwise unsupported MP4. Every one of the `20` HTDemucs `clean_vocals/.../vocals.wav` titles maps uniquely to an existing `videos/深蓝PMS662/*.mp3`, so the stem may replace that MP3 but the pair must never both contribute exposure. This leaves `8,036` canonical audio-file candidates / `91,714,921,022` bytes before decode/VAD validation: all `7,773` MP3 plus `263` extracted WAV, with the `20` mapped MP3 replaced one-for-one by their vocal stems; all `527` MP4 are excluded. A local CPU-only smoke test of FunASR `speech_fsmn_vad_zh-cn-16k-common-pytorch` succeeds and honors `max_single_segment_time=30000`; bind model/config/MVN SHA-256 `b3be75be477f0780277f3bae0fe489f48718f585f3a6e45d7dd1fbb1a4255fc5 / 486861ca26ddb79081663b6179cb204c6bfae71c52f04aafc48a9e9d8dde1e93 / 6820fef9687708c4fc3fab2530179c8fcea6262daa25514380056cd8f6eb1754`. The layout-valid immutable source inventory is `~/rwkvasr_data/stage211_social_vad_audit_v1/source_inventory.json`, SHA-256 `2e65d2cc778f506ad427a3bb4cae97043dbabb0ee5b355cc387f6ba0b23415f0`; it deliberately reports `training_ready=false`, `media_content_hash_complete=false`, and no duration total. Source inventory alone is not training readiness: admission additionally requires every canonical file to pass duration/decode probing, deterministic receipt-bound VAD boundaries, `0.5-30 s` materialized mono 16 kHz segments, exact source-to-segment duration accounting, segment decode verification, source/content duplicate handling, public-overlap audit, bucket-manifest hashes, and the same fixed hidden-eval split. The system `ffmpeg` executable is currently unusable because `libblas.so.3` and `liblapack.so.3` are missing; the audited MP3 sample is readable through libsndfile, but any segmentation implementation must fail closed unless its selected decoder/resampler passes a production preflight. Do not mutate the active original or current supplemental builder, and do not count these raw files/hours in Layer/Block/Logits coverage until the complete segmented receipt exists and the identical pool can still be consumed by all three online phases.
- 2026-08-10 Stage211 social VAD boundary contract and production pilot: consume only the completed source inventory with every canonical file SHA-256 and libsndfile duration/decode probe present, bind and rehash all three FSMN-VAD assets, and choose one deterministic winner per exact compressed-file duplicate group (`clean_vocals`, then extracted WAV, then lexical path priority). Partition sources deterministically by `source_index % num_workers`; persist one immutable receipt per source so a restart validates and reuses completed work without loading the model. Merge raw VAD intervals separated by at most `200 ms`, clamp them to the probed source duration, and partition each merged span without loss into `0.5-30 s` intervals; record exact no-speech rejection separately rather than failing or manufacturing exposure. A CPU-only real-MP3 pilot on `1,187.77034 s` produces `40` admitted segments / `1,182.78 s`, with source-inventory, source-boundary, and final-boundary SHA-256 `6198604cf4b2676237e00fa82d0323d9bbb80808a8e81fd17faac07e63a7bf40 / f3887fd8b0f29d1ab85683d6795d25e183ea84f881083ff9e6f459078dde1eef / eae691ef19f40ae199d164ba331b9f5622f1185a9ad3f5f59008d55f73594084`. An identical second worker pass reuses the existing source receipt without model initialization. Boundary completion still reports `training_ready=false`; materialized mono 16 kHz shards, full decode/duration validation, segment-level duplicate/public-overlap proof, a fixed-eval-bound bucket manifest, and an atomic base-plus-social supplemental inventory migration remain mandatory before any exposure is counted.
- 2026-08-10 Stage211 social segment-materialization contract: materialize only an immutable completed boundary inventory. Open each source once, seek each receipt-bound millisecond interval, average channels in float32, resample with the installed Torch audio sinc resampler, force the exact `duration_ms * 16` sample count, and encode deterministic mono 16 kHz PCM16 FLAC without relying on the broken system ffmpeg. Write one deterministic uncompressed tar per admitted source with paired `<key>.flac` and `<key>.json` members and fixed tar ownership/time metadata; persist every member offset/size and source/boundary/tar SHA-256 in an immutable per-source receipt. A crash may leave a generated tar without its receipt: rebuild into a temporary tar, accept an identical existing tar by hash, and reject a difference rather than deleting or silently replacing evidence. Worker assignment and receipt reuse follow the boundary `source_index % num_workers` contract. Finalization must independently hash every tar, direct-read every indexed member, decode every FLAC, verify mono/16 kHz/exact sample count and metadata identity, sum rows/hours by source, and build 80-frame bucket parts plus the shared immutable 256-row hidden-eval split. The real 20-minute pilot materializes all `40` receipt-bound segments / `0.32855 h`; source receipt, tar, and final materialized-inventory SHA-256 are `2e523dc4609f7141022937f5c9da291aa4dff4f17268b5d060042e846f74df1a / 0a7af6684bff0b7973a56d44048f1d35378a8cc61dde016b59627d8ea677da7f / efcc2a1182fb8e0d8b1e86832db34a4024387f834f97ab6ae8ee4b9c56b9e25c`. The production bucket loader decodes the first tar member at direct offset `512` into feature shape `[1,2955,80]` with zero target length, and its online-teacher row independently extracts a valid mono 16 kHz FLAC. The resulting social materialization inventory remains `training_ready=false` until segment-level public-overlap/dedup validation and an atomic base-supplemental-plus-social inventory migration pass; no currently running training process or existing immutable supplemental artifact may be modified by this layer.
- 2026-08-10 Stage211 social segment exact-content admission contract: fingerprint every materialized social segment and every utterance in the five canonical public evaluation manifests after the same deterministic conversion to mono 16 kHz PCM16, and hash an explicit sample-rate/channel/sample-count header plus little-endian PCM bytes. Persist immutable per-source fingerprint parts so interrupted full scans resume without rereading completed tar members. For each equal social fingerprint, admit exactly one deterministic winner ordered by clean-vocal replacement, extracted WAV, then source path/index and segment key; reject every other copy. Reject every social segment whose canonical PCM fingerprint equals any AISHELL-1, LibriSpeech test-clean/test-other, Common Voice 22 English clean-test, or WenetSpeech TEST_NET utterance, and persist all matching dataset/utterance identities. Build a new fixed-eval-bound bucket manifest only from admitted rows and bind materialized inventory, source receipts/tars, all five public manifests/audio hashes, fingerprint parts, exclusions, row/hour totals, and output-part hashes in a replayable inventory. This proof is deliberately `normalized_pcm_exact`, not an acoustic near-duplicate claim: lossy re-encoding, partial embedding, time shift, or changed VAD boundaries are outside this exact hash and must not be described as excluded. The filtered social inventory remains separate and immutable until a versioned, atomic combined supplemental inventory binds the completed English natural pool plus filtered social pool; only that combined inventory may become the common three-epoch Layer/Block/Logits input.
- 2026-08-10 Stage211 combined supplemental-v2 contract: preserve the in-progress English-only `stage211_supplemental_natural_v1` directory and its post-build receipt unchanged. After the filtered social inventory completes, create `stage211_supplemental_combined_v2` atomically in a new directory; never rename over, append into, or rewrite either component inventory. The combined manifest may reference immutable component bucket parts by absolute path, but must independently rehash every referenced part, require byte-identical 256-row fixed-eval splits, keep explicit source labels, and sum component/source rows and hours exactly. Schema v2 binds the base and social inventory/manifest SHA-256 values, records mixed English/Chinese and Parquet/tar/ZIP storage, preserves the overall cross-pool disclosure `content_fingerprint_complete=false`, and separately proves that social intra-pool dedupe and five-public-set filtering are complete only under `normalized_pcm_exact` while acoustic near-duplicate coverage remains false. Newly loaded Stage211 controllers default to combined v2; the already-running legacy supervisor is not signaled or mutated and may exit naturally before the hourly continuation watcher reloads the new default. Layer/Block/Logits completion and the final machine proof must bind the same combined-v2 SHA and still consume it for exactly three epochs per phase.
- 2026-08-11 Stage211 USB top-level coverage and archived-social correction: a claim that all usable USB audio was considered must bind an immutable registry for every immediate child of `/media/usbhd`, fail on an unknown or unclassified entry, and distinguish `admitted`, `queued_for_admission`, `same_corpus_repack`, `synthetic_tts_augmentation`, `text_only`, and `non_dataset` without treating path names as decoded-content proof. The Stage179 manifest already binds `14,506,415` WenetSpeech rows / `9,926.039 h`; `/media/usbhd/wenetdata` is the same WenetSpeech corpus in raw and alternate WebDataset form, and its local converter iterates every segment without enforcing `subsets`, so it is neither new coverage nor safe to append because it can duplicate train utterances and admit TEST_NET/TEST_MEETING. Bind that conclusion as corpus-identity evidence, not an exact byte/content-dedup claim. Bind `testnew/README.md`'s explicit `text-to-speech` declaration and keep the `556,667`-clip / `1,024.71 h` Parquet plus local MiniMax/blog speech as synthetic augmentation rather than natural-ASR coverage; their use or exclusion must remain explicit in the final report. `/media/usbhd/new_video.tar` is a separate `7,230,351,360`-byte social-audio archive and must not be ignored, but its first regular member maps to the existing `videos/鹏城杰森` path with identical size and SHA-256 `613499aab18dc837060e6e32b041b73656cb34e5617075881a71ff72e7660b05`, so current evidence points to a packaging duplicate rather than unique coverage. After the completed five-root social source inventory is available, index every regular audio member without extraction, bind tar/member offsets, sizes and hashes, reject unsafe/unsupported members, and compare every member against the corresponding existing source path/hash. If every member is byte-identical, emit an immutable all-member exclusion receipt and keep combined-v2 while binding that receipt into the USB/final proof. If any valid member is unique, only those unique members proceed through decode/duration probing, the same FSMN VAD, `0.5-30 s` mono-16 kHz materialization, normalized-PCM exact intra-pool and five-public-set filtering; then build combined-v3 from base natural, filtered directory-social, and filtered archived-social, and require all three Layer/Block/Logits phases to bind the identical v3 SHA for exactly three epochs. Do not signal or mutate the active legacy Stage A workers: an original-only completion must fail the current supplemental gate, be archived by the migration path, and resume from its completed Long checkpoint into the proven combined inventory before Stage B.
- 2026-08-11 Stage211 base-supplemental public-overlap audit contract: the English base supplemental pool (`People's Speech` train plus LJSpeech, VCTK, and MLS train members) currently proves source/split exclusion but not decoded-content separation from the five authoritative public evaluation sets. Before a combined supplemental inventory is training-ready, decode every row referenced by the immutable base bucket manifest, convert it with the same canonical mono-16 kHz PCM16 routine used for social admission, and compare the explicit sample-rate/channel/sample-count-prefixed SHA-256 against all AISHELL-1, LibriSpeech test-clean/test-other, Common Voice 22 English, and WenetSpeech TEST_NET utterances. Make the scan resumable at immutable manifest-part boundaries: every receipt must bind the base inventory and input part path/size/SHA-256, row/source/duration totals, one complete per-row fingerprint JSONL and its size/SHA-256, and zero decode failures. A final audit must require exact row/source/hour coverage, bind all five public manifest/fingerprint receipts and every part receipt, persist all dataset/utterance matches, and declare `training_ready=true` only when exact public overlap is zero. This remains an exact normalized-PCM guarantee, not near-duplicate/resegmentation detection. If any match exists, fail combined admission and derive a versioned filtered base manifest from the stored fingerprints before continuing; never silently train the matched rows or rewrite the original base inventory. Run the production scan only after the current base-manifest builder releases the USB and at low CPU/I/O priority; it must not signal or reconfigure the active Stage211A workers.
- 2026-08-11 Stage211 base-supplemental public-overlap audit verification: add a streaming, manifest-part-resumable scanner for Parquet/ZIP-backed base audio, immutable per-row encoded/normalized-PCM fingerprints, five-public-set comparison replay, zero-overlap admission, and changed-evidence rejection. Combined-v2 construction and runtime validation now fail closed without the exact base inventory-bound audit; the final stepwise JSON/Markdown and continuation watcher require its receipt SHA-256, positive scanned-row count, and zero public overlap alongside the existing social and archived-social proofs. The queued post-materialization pipeline runs the audit under inherited `nice 19` and idle I/O priority before combined admission, without changing the active training process. Synthetic overlap/no-overlap and tamper tests pass, and the complete Stage211 suite passes `168/168`; Ruff, Python bytecode compilation, shell syntax, and diff checks pass.
- 2026-08-11 Stage211 base-supplemental PCM scan-order correction: the first resumability design grouped work by duration-bucket manifest part, but production inspection shows at least `20` active People's Speech duration buckets while a representative `473,252,221`-byte Parquet has `19` row groups. Because one Parquet row group's utterances span many duration buckets, part-order scanning can repeatedly read and decompress the same embedded audio row group and multiply USB I/O by roughly the number of occupied buckets. Replace the production path with an immutable disk-backed manifest-location index built by streaming every bound part once, requiring unique keys and exact base row/source/hour totals. Derive a stable sorted archive table from that index, then resume at archive boundaries: query each Parquet archive by ascending row-group/row index so its reader cache loads every referenced row group at most once, and keep each ZIP open while reading every selected member once. Bind the index database/receipt and each archive fingerprint/receipt to the original base inventory/manifest and source archive metadata; finalization must require one receipt for every indexed archive and exact indexed row coverage before public-overlap comparison. Retain single-worker idle-I/O production defaults for the USB HDD; worker partitioning may exist for tests or faster storage but must never imply more data coverage. The earlier part-order implementation is test evidence only and must not be the production `all` path.
- 2026-08-11 Stage211 archive-order PCM scanner implementation: production `all` now executes public fingerprints, immutable SQLite location-index construction, single-worker archive-order PCM fingerprinting, and final overlap replay in that order. The database enforces unique utterance keys plus unique Parquet row-group/row and ZIP-member locations, records a stable archive table, and is bound by size/SHA-256 receipt; every archive receipt binds source archive size/mtime/inventory SHA, exact row/duration totals, encoded-audio and canonical-PCM fingerprints, zero decode failures, and scan order `manifest_location_index_archive_order_v1`. Combined-v2 validation, the final stepwise JSON/Markdown, and the continuation watcher reject any legacy part-order audit or missing scan-order proof. A fixture with six rows in five source archives places two same-row-group People's Speech rows in separate duration-bucket parts and proves one archive open plus one row-group cache miss, immutable replay, overlap/no-overlap behavior, changed-fingerprint rejection, and that the production `all` CLI never creates legacy part fingerprints. The complete Stage211 suite passes `169/169`; focused archive tests pass `4/4`; Ruff, bytecode compilation, shell syntax, CLI load, and diff checks pass.
- 2026-08-11 Stage211 legacy-supervisor supplemental handoff contract: the active supervisor was loaded before `supplemental_natural` became mandatory, so its original Easy/Medium/Hard/Long completion may exit only after the current finalizer rejects promotion without the combined supplemental proof. The hourly continuation watcher may then launch exactly one current bootstrap, but that bootstrap must remain alive and poll at an hourly default until both the combined-v2 inventory and immutable profile receipt exist and `create_stage211_supplemental_profile_receipt.py` reproduces the receipt byte-for-byte from the current inventory. Missing, partial, changed, or invalid evidence must not start calibration reuse, initialization validation, a GPU controller, Block, or Logits, and must not cause a 60-second supervisor relaunch loop. Once validation succeeds, the current full-phase controller archives the original-only summary and resumes at `supplemental_natural` from the exact Long checkpoint for three epochs before the Mixer retention/public gate. This readiness wait is control-plane-only and must not signal, restart, or reconfigure the currently active supervisor or its four training ranks.
- 2026-08-11 Stage211 base-manifest interrupted-build preservation: the long-running base supplemental builder writes bucketed JSONL evidence under `<output>.partial`; an abnormal exit may leave many hours of valid source indexing there even though no final immutable inventory exists. A subsequent invocation must fail closed when that staging directory exists and must never recursively delete or overwrite it. Recovery or restart requires an explicit audited decision after inspecting the partial rows and source progress. This preservation change affects only a future process invocation; it must not signal or reload the currently active builder or training ranks.
- 2026-08-11 Stage211 failed-supervisor retry cadence: continuation remains automatic, but a supervisor that exits without a valid next-stage selection must not be relaunched every minute with the same immutable failed gate and unchanged recipe. Use the same one-hour default for active-supervisor checks and failed-launch backoff. This preserves the failed Layer/Block/Logits WER/CER and alignment evidence for an evidence-based correction strategy while preventing repeated CPU/hash/evaluation setup churn. Explicit test fixtures may override the delay; changing the watcher cadence must not signal or restart any training rank.
- 2026-08-11 Stage211 labeled-SFT production preflight: replay the exact `run_stage211_strict_chained_alignment._audit_labeled_data` contract over the mounted production length index and bucket manifest before Logits completion. The audit accepts `285,302` unique pronunciation-target utterances (`283,868` train and `1,434` eval), `809.12755 h`, `8,792,460` CTC tokens, zero unknown tokens, zero infeasible CTC targets, `12,045` one-epoch optimizer steps, and `368` deterministic tail-padding exposures. Its preparation proof binds `sensevoice_tiktoken`, Nano's `multilingual.tiktoken` SHA-256 `747979631e813193436aabcff7c1c235d37de8097b71c563ec8b63b7a515c718`, CTC text normalization, LFR6 frame accounting, explicit special-token logit suppression, and summary/log SHA-256 values `38a2bc09e2afb26c7094ff6f120a564015f8fe7d79e6d71c57dc2d76b0071f52 / 6c196476c62f007e890a6d18c9d0ce6cb716b97dac377812ec4e0f8e101f948e`. All `65` referenced tar shards (`69,105,448,960` bytes) exist and are non-empty. This is an availability/index/label-contract preflight, not an audio-decode substitute: the eventual SFT launch must rerun the full audit, execute its two-step GPU smoke, consume the complete one-epoch runtime coverage without skip/drop, and then pass all five normalized public CTC datasets.
- 2026-08-11 Stage211 frozen-MLP hidden-objective correction: the strict phase implementation must distinguish freezing Nano-copied MLP parameters from supervising the MLP output hidden states. Phase A remains the isolated teacher-forced TimeMixer target with FFN/MLP weight `0`. Phase B must add the explicit stacked FFN/MLP hidden objective at weight `0.25` alongside mixer `0.25` and complete block `1.0`; Phase C and labeled SFT retain lower FFN/MLP anchors `0.10` and `0.05`, respectively. Nano-copied FFN/MLP, Norm, five-layer CTC decoder, and CTC head parameters remain frozen in every phase, so the FFN loss backpropagates through the fixed Nano MLP only into the preceding BiRWKV TimeMixer/input projection and does not change the architecture or trainable-parameter boundary. This corrects the earlier all-phase `zero FFN loss` implementation, which could align residual block outputs without directly enforcing the requested MLP hidden-state target. The already-loaded Phase-A process and its generated configuration remain unchanged and must not be signaled, restarted, or reconfigured; the correction applies only when future B/C/D configurations are generated. Phase-contract tests, the complete Stage211 suite (`172/172`), Ruff, bytecode compilation, and diff checks pass.
- 2026-08-11 Stage211 Block component-gate correction: enabling the frozen-MLP hidden objective is insufficient if Phase-B promotion can inspect only the aggregate block output. Every fixed and seven-cell stratified Block pair evaluation must contain finite, exact 70-layer `mixer`, `ffn`, and `block` component metrics from the same cached audio/features and checkpoint pair. Preserve the existing block/decoder/path gate, and additionally require mixer-retention and FFN component macro loss/cosine improvement, at least `68/70` improved layers, improving weak `10-19`/`20-29` bands, improving Hard EN/ZH component losses, and no component cell regression above the existing `10%` tolerance. A stratified block result may not bypass these component gates. This changes only future Block evaluation/promotion evidence; it does not alter or interrupt the active Phase-A process. Missing-FFN, regressed-FFN, stratified-bypass, and component-summary tests pass; the complete Stage211 suite passes `173/173`, and Ruff, bytecode compilation, and diff checks pass.
- 2026-08-11 Stage211 Logits hidden-retention gate contract: Phase C carries positive mixer, frozen-MLP/FFN, block, final-encoder, and five-layer CTC-decoder hidden anchors, so promotion evidence must prove those Stage-B representations were not discarded while final Nano CTC logits improved. Every fixed paired Logits evaluation must contain finite, exact 70-layer `mixer`, `ffn`, and `block` component metrics plus a finite decoder-hidden loss from the same cached audio/features and checkpoint pair. For each component, require candidate all-layer mean loss and weak `10-19`/`20-29` band loss to remain within `10%` of the Phase-C initialization and require mean/weak-band cosine to fall by no more than `0.01`; require decoder-hidden loss to remain within `10%`. The selected fixed or seven-cell Logits result must pass its existing distribution/path gate and this fixed hidden-retention gate; a passing stratified Logits summary may never bypass hidden retention. These are bounded retention checks rather than Phase-B improvement checks because Phase C optimizes final logits. This changes only future Logits evaluation/promotion evidence and does not signal, restart, or alter the active Phase-A process.
- 2026-08-11 Stage211 Logits hidden-retention gate verification: the deterministic paired evaluator now rejects Logits reports missing any of the exact 70-layer mixer/FFN/block components or finite decoder-hidden loss. The Logits gate emits per-component all-layer and weak-band retention checks, a decoder retention record, and requires hidden retention in addition to the selected fixed/seven-cell logits result. Regression tests prove both FFN and decoder regression are rejected and that a passing stratified logits summary cannot bypass the hidden gate. Targeted tests pass `11/11`; the complete Stage211 suite passes `174/174`; Ruff, bytecode compilation, and diff checks pass. No active training process, checkpoint, optimizer, data order, or generated Phase-A config was changed.
- 2026-08-11 Stage211 Logits seven-cell hidden-retention contract: the fixed Logits pair is the shared Easy-ZH sentinel and therefore cannot by itself prove that Phase C preserved Stage-B representations on English, Hard, or Long audio. Every one of the seven `easy_en/easy_zh/medium_en/medium_zh/hard_en/hard_zh/long_zh` paired Logits reports must contain finite, exact 70-layer mixer/FFN/block metrics and finite decoder-hidden loss. The immutable stratified Logits summary must aggregate per-layer macro, weak `10-19`/`20-29` bands, and per-cell loss/cosine for all three components plus macro/per-cell decoder loss. Require each component's macro/weak-band and every cell to remain within the fixed `10%` loss and `0.01` cosine regression bounds, and require decoder macro plus every cell to remain within `10%`. These checks are part of the stratified Logits gate itself, in addition to the existing KL/path checks and the independent fixed hidden-retention gate. Missing or regressed cross-domain hidden evidence must fail Phase-C promotion even when final logits improve. This evidence-only correction applies to future Phase-C evaluation and does not alter or interrupt active Phase-A training.
- 2026-08-11 Stage211 Logits seven-cell hidden-retention verification: the stratified Logits summarizer now validates every report's exact mixer/FFN/block 70-layer coverage and finite decoder loss, emits all-layer/per-layer/weak-band/per-cell component summaries plus macro/per-cell decoder summaries, and the Logits gate validates and enforces those records. A regression fixture leaves all seven cells' final-logit improvements intact while worsening only Hard-EN FFN hidden loss by `20%`; the fixed hidden gate passes, but the stratified `ffn_hidden_retained` check and Phase-C gate correctly fail. Targeted tests pass `11/11`; the complete Stage211 suite passes `174/174`; Ruff, bytecode compilation, and diff checks pass. The active Phase-A ranks and their configuration remain untouched.
- 2026-08-11 Stage211 final alignment-gate replay contract: hashing an alignment-gate JSON and copying its top-level `gate_passed` value into a phase gate does not prove that the requested Layer/Block/Logits sub-gates were present or internally consistent. Every phase-gate read, including promotion, SFT admission, and final stepwise reporting, must validate the bound baseline/candidate source reports' phase-appropriate exact 70-layer component coverage, finite decoder hidden where required, and complete finite Logits metrics/teacher coverage. Recombine the alignment decision from its bound booleans: Layer/Block must use the selected fixed/seven-cell hidden result and Block's three-component gate; Logits must use the selected fixed/seven-cell logits result AND fixed hidden retention, while a stratified Logits pass must itself include mixer/FFN/block/decoder cross-cell retention checks. Require the stratified summary path/hash, embedded payload, checkpoint pair, receipt, and all per-cell report hashes to remain valid. Reject an old, missing, altered, or internally inconsistent alignment-gate schema even if the phase gate and public WER/CER booleans claim success. This final-evidence correction changes no model, objective, checkpoint, or active process.
- 2026-08-11 Stage211 final alignment-gate replay verification: the shared phase-gate validator now recomputes fixed 70-layer component summaries and decisions from the bound pair reports, validates mandatory seven-cell summary/receipt/checkpoint/manifest/report bindings, and independently recomputes the hidden or Logits stratified decision. Mixer requires mixer evidence; Block requires mixer/FFN/block plus decoder hidden; Logits requires complete CTC metrics, fixed hidden retention, and seven-cell mixer/FFN/block/decoder retention. Regression tests reject a missing Block FFN component, a missing Logits hidden-retention field, and an altered seven-cell FFN-retention decision. Phase-gate tests pass `31/31`; the complete Stage211 suite passes `177/177`; Ruff, bytecode compilation, and diff checks pass. Production finalization always materializes the seven-cell summary, while promotion, SFT admission, and the final stepwise report all call the hardened validator. The active Phase-A training ranks, optimizer, checkpoints, and data order were not changed or interrupted.
- 2026-08-11 Stage211 public WER/CER replay contract: a prediction-file hash plus a copied `metric_source_recomputed=true` flag does not prove that a phase gate's English WER, Chinese CER, deletion rates, Nano gap, or pass decision equals those files. Every phase-gate read must replay CTC normalization and edit-distance accounting from the bound public manifest, Nano prediction JSONL, and student prediction JSONL for all three English WER and both Chinese CER datasets, require exact utterance/reference coverage, and compare every reported metric and per-dataset gate decision with the replay. Mixer/Block must additionally replay their baseline public comparison binding and the macro/deletion/no-large-regression progress gate; Logits must replay the every-dataset Nano closeness gate. SFT admission and the final stepwise report inherit the same validator so no stage can be represented by unattested WER/CER fields. This evidence-only change must not alter or interrupt active Phase-A training.
- 2026-08-11 Stage211 public WER/CER replay verification: one shared single-pass scorer now reconstructs normalized references, hypotheses, edit decomposition, English WER, Chinese CER, Nano gaps, and per-dataset decisions from the exact bound JSONL files. Phase gates replay the candidate checkpoint and Mixer/Block initialization baselines before rebuilding their public-progress decision; labeled SFT and the final stepwise report replay the Logits baseline, SFT candidate, and zero-regression improvement decision. Regression fixtures prove parity with the canonical public comparison implementation and reject changed raw/embedded metrics or progress decisions. A read-only production replay over all `52,431` AISHELL-1, LibriSpeech clean/other, Common Voice EN, and WenetSpeech TEST_NET predictions exactly reproduces every existing metric and decision. Focused replay tests pass `5/5`, phase-gate integration tests pass `36/36`, the complete Stage211 suite passes `182/182`, and Ruff, bytecode compilation, and diff checks pass. The active Phase-A workers, optimizer, checkpoints, and data order were not changed or interrupted.
- 2026-08-11 Stage211 production retention-replay readiness: the immutable post-coverage replay contains `1,000,148` unique rows and `2,218.4501778 h`, balanced as `500,000` English and `500,148` Chinese rows across `easy_en/easy_zh/medium_en/medium_zh/hard_en/hard_zh/long_zh = 130,000/130,000/270,000/270,000/100,000/100,000/148`. A low-priority production validation rereads all `255` train parts, verifies every unique key, source/language/frame bucket, deterministic selection score, original-row hash, fixed/stratified evaluation exclusion, quota/capacity summary, manifest binding, and receipt hash; it passes with receipt SHA-256 `0b1e389560b441d1a78f4cc5c26c5aa4693f8e476e575babc850fb7b42257749`. This is sufficient concrete input for any bounded one-epoch Layer/Block/Logits post-coverage correction round after a complete failed phase gate; it does not replace or shorten the mandatory original five-segment three-epoch coverage.
- 2026-08-11 Stage211 combined-supplemental dependency wait contract: the post-materialization pipeline must be restart-safe when invoked directly, not only when an outer tmux wrapper happens to wait for social materialization. Before normalized-PCM filtering, require both the immutable base-natural `supplemental_inventory.json` and social `materialized_inventory.json` to exist and be non-empty; poll at the configured hour-scale interval without starting the filter, combined inventory, profile receipt, or any GPU phase early. Preserve the existing archived-social and USB coverage checks. This is a control-plane liveness correction only and must not signal, restart, or mutate active training/data builders.
- 2026-08-11 Stage211 combined-supplemental dependency wait verification: the post-materialization script now owns a named non-empty-artifact wait and applies it independently to the base-natural and social-materialized inventories. A subprocess regression fixture executes the real shell script with controlled `tmux`, `sleep`, and `uv` shims, proves that a missing social materialized inventory causes a wait, and proves that normalized-PCM filtering starts only after the artifact becomes non-empty. The focused combined-supplemental suite passes `3/3`, the complete Stage211 suite passes `183/183`, and Ruff, Python bytecode compilation, shell syntax, and diff checks pass. No active training/data-builder process was signaled, restarted, or reconfigured.
- 2026-08-11 Stage211 narrow restart-dispatch contract: the continuation watcher already selects the first incomplete strict stage from immutable Mixer/Block/Logits selection artifacts, so the bootstrap must honor that boundary. A `logits` restart must deep-validate and reuse the selected Mixer and Block gates without re-entering either completed full-data controller; an `sft` restart must deep-validate and reuse all three selected A/B/C gates without re-entering any completed A/B/C controller. `full`, `post_mixer`, and `block` retain their ordered training behavior. Reuse must still execute the existing phase-selection validator, including alignment, public WER/CER replay, promotion, checkpoint, and SHA bindings, before the next stage starts. This recovery-only change must not signal, restart, or reconfigure the active legacy supervisor, training ranks, optimizer, data builders, or data order.
- 2026-08-11 Stage211 narrow restart-dispatch verification: Block and Logits correction/selection validation are now reusable functions separate from their full-data controllers. Bootstrap dispatch is explicit per restart boundary: `logits` validates selected Mixer/Block evidence then runs only the Logits full controller and SFT, while `sft` validates selected Mixer/Block/Logits evidence then runs only SFT. Controlled subprocess fixtures execute both branches with command-recording `uv`/`jq` shims and prove that no completed full-phase controller is invoked. Focused supervisor tests pass `5/5`, the complete Stage211 suite passes `185/185`, and Ruff, shell syntax, and diff checks pass. The active legacy supervisor and four training ranks were not reloaded or signaled.
- 2026-08-11 Stage211A Hard 04:24 hourly status: the uninterrupted four-rank run reached live step `213,370/966,315` (`22.0808%`) with persisted step `212,000`, advancing `4,960` optimizer steps since the preceding hourly snapshot. The sampled step-213370 batch has mixer loss `0.0761`, cosine `0.9726`, RMS ratio `1.0076`, complete `11/11` online-layer matches, and zero frame delta; this is batch-local health evidence, not a promotion metric. All four ranks remain active at about `8.8 GiB` per GPU, the current attempt has no OOM/CUDA/NCCL/non-finite/decode/teacher-missing/frame-delta/skip error, `/media/usbhd` remains the intended `/dev/sdb2` read-only XFS mount, and root has `781 GiB` free. The latest completed fixed sentinel remains step 210,000 at loss `0.1610885905`, `53.03%` above the Hard initialization `0.1052601582`; therefore the old-Easy retention concern remains active despite healthy Hard-domain optimization. Continue exact Hard/Long/supplemental coverage unchanged, then require balanced retention correction if the seven-cell hidden and full public WER/CER gates fail.
- 2026-08-11 Stage211A Hard 0-210k fixed-retention trend: all `22` immutable fixed reports from initialization through step 210,000 are strictly increasing in loss; none of the `70` layers improves either loss or cosine at 210k. Per-depth-band loss regressions are `0-9 +63.77%`, `10-19 +46.68%`, `20-29 +44.07%`, `30-39 +43.56%`, `40-49 +35.46%`, `50-59 +70.05%`, and `60-69 +93.98%`. The recent five-gate mean increase has slowed to `0.0009572` per 10k steps, but there is no reversal or stable retention recovery. This is still the intentionally preserved short Easy-ZH sentinel, so it cannot override the seven-cell representative result; it does prove that any post-coverage repair must supervise all TimeMixer depths on balanced old/new English/Chinese audio rather than applying a last-layer-only patch. The prepared one-million-row replay and at-most-three complete low-LR rounds retain that scope.
- 2026-08-11 Stage211 post-coverage replay depth-coverage proof: loading the immutable production replay manifest through the current Stage211 constants (`batch_size=36`, `world_size=4`, `frame_budget=24,000`) yields `10,135` optimizer steps and `1,420` deterministic tail-padding sample exposures for one complete round over `1,000,148` unique rows (`2,218.4501778 h`). Replaying the actual `_select_layer_hidden_ids` schedule for all formal steps shows that Mixer and Block each produce `81,080` layer-selection events and cover every layer exactly `1,158` or `1,159` times. Logits produces `121,620` events and also covers `70/70` layers: its eight configured difficult boundary layers are supervised on all `10,135` steps, while every other layer is selected `653` or `654` times. The replay is therefore balanced across its seven language/difficulty cells and has complete phase-appropriate TimeMixer depth coverage; no last-layer-only or partial-depth correction path exists. These figures supersede the immediately preceding manual preflight that accidentally used the obsolete `12/8,000` batch/frame values; no training config, replay row, or runtime artifact was changed. This verifies future correction capacity only and does not admit a correction before complete original-plus-supplemental coverage and a failed full hidden/public gate.
- 2026-08-11 Stage211D CTC target/logit-mask invariant: the pronunciation-only index audit is necessary but does not by itself prove that the labels decoded from the current tar metadata cannot contain a token row suppressed by the runtime CTC logit mask. Before calling `torch.nn.functional.ctc_loss`, validate the exact packed targets against the model's current CTC vocabulary, project blank, and resolved suppressed-token buffer. Reject an out-of-range token, any blank token in a target, any suppressed target token, a negative target length, or a packed-target length mismatch with an actionable error. Keep the check inside the positive-CTC-loss path so online unlabeled A/B/C distillation remains unchanged; Stage211D's two-step smoke and every formal SFT batch then fail before an invalid target can be optimized. The production tokenizer binding is Nano's `60,515`-entry SenseVoice tiktoken vocabulary, while the project blank is the additional row `60,515`; all `1,679` tokenizer special rows, including Nano's original blank row `60,514`, must be suppressed, while the project blank must remain available only to CTC. This safety change must not signal, restart, or reconfigure the active Stage211A process.
- 2026-08-11 Stage211D CTC target/logit-mask verification: the model now validates the exact packed target tensor immediately before positive-weight CTC loss and fails on suppressed non-pronunciation rows, project blank, out-of-vocabulary IDs, negative lengths, non-packed shapes, or declared/actual packed-length mismatch. A real Nano tokenizer check resolves `60,515` text rows and `3,629` suppressed rows under project blank `60,515`; every one of the `1,679` special rows is suppressed, including Nano blank `60,514`, while ordinary English and Chinese text IDs remain available. Replaying the immutable production index accepts all `285,302` rows, `8,792,460` tokens, zero unknown/infeasible rows, `12,045` formal steps, and `368` tail repetitions with the bound tokenizer/summary/log hashes. Targeted tests pass `6/6`; the combined model/tokenizer/Stage211 suite passes `259/259`, and Ruff plus diff checks pass. The active Stage211A ranks remained untouched.
- 2026-08-11 Stage211A Hard 05:24 hourly status: the uninterrupted four-rank run reached live step `218,280/966,315` (`22.5889%`) with persisted DeepSpeed tag `step-218000`, advancing `4,910` optimizer steps in the hour. The first Hard epoch is approximately `67.76%` complete, and original Easy/Medium/Hard/Long Stage-A coverage is approximately `62.59%` complete by optimizer-step contract. The sampled step-218280 batch has mixer loss `0.1228`, cosine `0.9556`, RMS ratio `0.9919`, complete `14/14` hidden matches, and zero frame delta; the immediately following step-218330 depth-62--69 batch has loss `0.0481`, cosine `0.9828`, RMS ratio `1.0025`, and `15/15` matches, illustrating expected batch/depth variation rather than a phase-level gate. All four ranks remain active at about `8.8 GiB` per GPU, with no current-attempt OOM/CUDA/NCCL/non-finite/decode/teacher-missing/frame-delta/skip error. The supplemental natural builder remains active at idle I/O priority after reading `476.4 GB`, with its partial output growing to `1.2 GB`; downstream social/combined builders remain dependency-waiting rather than bypassing the mandatory fifth segment. `/media/usbhd` is still the intended `/dev/sdb2` read-only XFS mount. Keep the exact run unchanged and retain the next check at the one-hour cadence.
- 2026-08-11 Stage211A Hard through-218k current-domain trend: parse only the current distributed-attempt suffix and cap it at the `05:24` snapshot so later writes cannot change this diagnosis. Across `20,428` ten-step records, every online hidden comparison has complete matches, zero teacher missing, and zero frame delta. The first complete `20k-30k` window has mean mixer loss/cosine `0.114160/0.959144`; the `200k-210k` window reaches `0.103350/0.963014`, a `9.47%` loss reduction and `+0.003871` cosine gain. The partial `210k-218.28k` window remains stable at `0.103108/0.963103`. Recent complete-window losses fluctuate narrowly around `0.1028-0.1055`, indicating a healthy Hard-domain plateau rather than divergence. Combined with the monotonic short Easy-ZH sentinel regression, the evidence is specifically domain retention failure: do not shorten Hard coverage or declare Layer alignment complete from training loss, and retain the balanced post-coverage replay plus seven-cell/public gates as mandatory promotion evidence.
- 2026-08-11 Stage211A Hard 06:24 hourly status and step-220k sentinel: the uninterrupted four-rank run reached live step `223,250/966,315` (`23.1032%`) with persisted DeepSpeed tag `step-222000`, advancing `4,970` optimizer steps in the hour. The first Hard epoch is approximately `69.31%` complete, and original Easy/Medium/Hard/Long Stage-A coverage is approximately `62.84%` complete by the exact `2,000,079`-step contract. All four ranks remain active at about `8.8 GiB` per GPU; current online comparisons retain zero hidden missing count and zero frame delta. Hard-domain ten-step-window means continue to improve from loss/cosine `0.114100/0.959165` at `20k-30k`, through `0.103369/0.963007` at `200k-210k` and `0.102917/0.963172` at `210k-220k`, to `0.101746/0.963593` over the partial `220k-223.4k` window. In contrast, the immutable short Easy-ZH sentinel worsens from step-210k `0.1610885905` to step-220k `0.1617147204` (`+0.389%`), with `0/70` layers improving relative to step 10k; this confirms continuing cross-domain retention loss rather than Hard-domain optimization failure. The base supplemental builder remains active at idle I/O priority, has read about `531.8 GB`, and is processing People's Speech dirty shards beyond the logged 300-file boundary. Do not interrupt or retune the active Hard segment: finish exact Hard/Long/supplemental coverage, then apply the already validated balanced low-LR all-depth replay only if the seven-cell hidden and complete public WER/CER gates fail.
- 2026-08-11 Stage211A Hard 07:24 hourly status: the uninterrupted four-rank run reached live step `228,230/966,315` (`23.6186%`) with persisted DeepSpeed tag `step-228000`, advancing `4,980` optimizer steps since the preceding monitor snapshot. The first Hard epoch is approximately `70.86%` complete, and original Easy/Medium/Hard/Long Stage-A coverage is approximately `63.09%` complete by the exact-step contract. The partial `220k-228.23k` current-domain window contains `823` ten-step records at mean mixer loss/cosine `0.101810/0.963570`, versus `0.102917/0.963172` over the complete `210k-220k` window; every record has zero hidden missing count and zero frame delta. All four ranks remain active at about `8.8 GiB` per GPU, W&B artifacts are still growing, and the current-attempt error audit remains empty for OOM/CUDA/NCCL/non-finite/decode/teacher-missing/frame-delta/skip failures. The next immutable short Easy-ZH sentinel is due at step 230,000 and no pre-230k batch-local value may substitute for it. The supplemental natural builder has read about `578.6 GB`, logged People's Speech dirty completion through 400 files, and is actively reading files 416--417 of 3,140; `/media/usbhd` remains the intended `/dev/sdb2` read-only XFS mount. Continue unchanged at the hour-scale monitor cadence.
- 2026-08-11 Stage211A Hard 08:24 hourly status and step-230k sentinel: the uninterrupted four-rank run reached live step `233,210/966,315` (`24.1340%`) with persisted DeepSpeed tag `step-232000`, again advancing `4,980` optimizer steps in the hour. The first Hard epoch is approximately `72.40%` complete, and original Easy/Medium/Hard/Long Stage-A coverage is approximately `63.34%` complete. Hard-domain optimization remains healthy: the complete `220k-230k` window improves mean mixer loss/cosine to `0.101887/0.963542` from `0.102917/0.963172` at `210k-220k`, and the partial `230k-233.19k` window reaches `0.100721/0.963961`; all rows retain zero hidden missing count and zero frame delta. The immutable Easy-ZH sentinel nevertheless worsens from step-220k `0.1617147204` to step-230k `0.1629997568` (`+0.7946%`), with mean 70-layer loss/cosine/RMS ratio `0.1597978191/0.9458181787/0.8883465380`; only `13/70` layers improve loss and `12/70` improve cosine relative to step 220k. This strengthens the all-depth cross-domain retention diagnosis without changing the mandatory complete-coverage decision. All four ranks remain active, W&B continues writing, and the current-attempt formal error audit remains empty. The supplemental builder has read about `625.9 GB`, accepted `2,000,000` natural utterances totaling `7,775.759 h`, logged completion through 500 People's Speech dirty files, and is actively reading files 519--520 of 3,140. Keep the current process unchanged; no Stage-B admission or public WER/CER claim is valid before all five Stage-A segments and the independent final gates finish.
- 2026-08-11 Stage211A Hard 09:24 hourly status: the uninterrupted four-rank run reached live step `238,210/966,315` (`24.6514%`) with persisted DeepSpeed tag `step-238000`, advancing `5,000` optimizer steps since the preceding direct snapshot. The first Hard epoch is approximately `73.95%` complete, and original Easy/Medium/Hard/Long Stage-A coverage is approximately `63.59%` complete. The partial `230k-238.21k` current-domain window contains `822` ten-step records at mean mixer loss/cosine `0.100744/0.963950`, improving on the complete `220k-230k` window's `0.101887/0.963542`; all online hidden comparisons retain zero missing count and zero frame delta. The 240k immutable Easy-ZH sentinel is not yet present, so this snapshot makes no retention interpolation and does not substitute the sampled step-238210 batch for a fixed report. All four ranks remain active at about `8.8 GiB` per GPU, W&B continues writing, and the current-attempt formal error audit remains empty. The supplemental builder has read about `674.2 GB`, logged People's Speech dirty completion through 600 files, and is actively reading files 625--626 of 3,140. Continue the exact run unchanged and inspect the completed 240k sentinel at the next hour-scale check.
- 2026-08-11 Stage211A Hard 10:24 hourly status and step-240k sentinel: the uninterrupted four-rank run reached live step `243,230/966,315` (`25.1709%`) with persisted DeepSpeed tag `step-242000`, advancing `5,020` optimizer steps in the hour. The first Hard epoch is approximately `75.51%` complete, and original Easy/Medium/Hard/Long Stage-A coverage is approximately `63.84%` complete. The complete current-domain `230k-240k` window improves mean mixer loss/cosine to `0.101005/0.963855` from `0.101887/0.963542` at `220k-230k`; the partial `240k-243.19k` window remains within recent variation at `0.101769/0.963581`, with zero hidden missing count and zero frame delta. In contrast, the immutable Easy-ZH sentinel worsens from step-230k `0.1629997568` to step-240k `0.1646727686` (`+1.0264%`), with mean 70-layer loss/cosine/RMS ratio `0.1614181467/0.9453443671/0.8860395119`; only `11/70` layers improve loss and `8/70` improve cosine relative to step 230k. This remains all-depth cross-domain forgetting alongside healthy Hard-domain optimization. All four ranks remain active, W&B continues writing, and the current-attempt formal error audit is empty. The supplemental builder has read about `724.2 GB`, logged People's Speech dirty completion through 700 files, and is actively reading files 722--723 of 3,140. Continue all exact coverage unchanged and defer any balanced replay until the complete five-segment Stage-A gate.
- 2026-08-11 Stage211A Hard 11:24 hourly status: the uninterrupted four-rank run reached live step `248,180/966,315` (`25.6831%`) with persisted DeepSpeed tag `step-248000`, advancing `4,950` optimizer steps since the preceding monitor snapshot. The first Hard epoch is approximately `77.05%` complete, and original Easy/Medium/Hard/Long Stage-A coverage is approximately `64.09%` complete. The partial `240k-248.18k` current-domain window contains `820` ten-step records at mean mixer loss/cosine `0.101414/0.963710`, a small plateau fluctuation relative to the complete `230k-240k` window's `0.101005/0.963855`; all comparisons retain zero hidden missing count and zero frame delta. The 250k immutable Easy-ZH sentinel is not yet present, so no fixed-retention result is inferred from the sampled step-248180 batch. All four ranks remain active at about `8.8 GiB` per GPU, W&B continues writing, and the current-attempt error audit remains empty. The supplemental builder has read about `773.2 GB`, logged People's Speech dirty completion through 800 files, and is actively reading files 827--828 of 3,140. Continue unchanged and read the completed 250k sentinel at the next hour-scale check.
- 2026-08-11 Stage211A Hard 12:24 hourly status and step-250k sentinel: the uninterrupted four-rank run reached live step `253,150/966,315` (`26.1975%`) with persisted DeepSpeed tag `step-252000`, advancing `4,970` optimizer steps in the hour. The first Hard epoch is approximately `78.59%` complete, and original Easy/Medium/Hard/Long Stage-A coverage is approximately `64.34%` complete. The complete current-domain `240k-250k` window remains on the same healthy plateau at mean mixer loss/cosine `0.101106/0.963819`, and the partial `250k-253.15k` window is `0.100651/0.963983`; every online comparison retains zero hidden missing count and zero frame delta. The immutable Easy-ZH sentinel worsens from step-240k `0.1646727686` to step-250k `0.1654291450` (`+0.4593%`), with mean 70-layer loss/cosine/RMS ratio `0.1621580191/0.9451189952/0.8854081034`; `21/70` layers improve loss and `23/70` improve cosine relative to step 240k, but the aggregate retention trend has not reversed. All four ranks remain active, W&B continues writing, and the current-attempt formal error audit remains empty. The supplemental builder has read about `823.2 GB`, logged People's Speech dirty completion through 900 files, and is actively reading files 930--931 of 3,140. Preserve exact coverage and the planned post-coverage balanced gate correction; do not promote Stage B from the current-domain plateau alone.
- 2026-08-11 Stage211A Hard 13:24 hourly status: the uninterrupted four-rank run reached live step `258,130/966,315` (`26.7128%`) with persisted DeepSpeed tag `step-258000`, advancing `4,980` optimizer steps in the hour. The first Hard epoch is approximately `80.14%` complete, and original Easy/Medium/Hard/Long Stage-A coverage is approximately `64.59%` complete. The partial `250k-258.13k` current-domain window contains `814` ten-step records at mean mixer loss/cosine `0.100681/0.963973`, modestly better than the complete `240k-250k` window's `0.101106/0.963819`; all comparisons retain zero hidden missing count and zero frame delta. The 260k immutable Easy-ZH sentinel is not yet present, so this snapshot makes no fixed-retention inference from its sampled batch. All four ranks remain active, W&B continues writing, and the current-attempt formal error audit remains empty. The supplemental builder has read about `873.5 GB`, logged People's Speech dirty completion through 1,000 files, and is actively reading files 1,039--1,040 of 3,140. Continue the exact run unchanged at the established hour-scale cadence.
- 2026-08-11 Stage211A Hard 14:24 hourly status and step-260k sentinel: the uninterrupted four-rank run reached live step `263,060/966,315` (`27.2230%`) with persisted DeepSpeed tag `step-262000`, advancing `4,930` optimizer steps in the hour. The first Hard epoch is approximately `81.67%` complete, and original Easy/Medium/Hard/Long Stage-A coverage is approximately `64.83%` complete. The complete current-domain `250k-260k` window improves mean mixer loss/cosine to `0.100633/0.963990`, and the partial `260k-263.06k` window reaches `0.100166/0.964157`; all comparisons retain zero hidden missing count and zero frame delta. The immutable Easy-ZH sentinel records its first small reversal, improving from step-250k `0.1654291450` to step-260k `0.1652758815` (`-0.0926%`), with mean 70-layer loss/cosine/RMS ratio `0.1620084473/0.9451578101/0.8861304499`; `36/70` layers improve loss and `34/70` improve cosine relative to step 250k. One 10k reversal is insufficient to establish retention recovery and remains far above the old-domain initialization, so preserve the balanced replay plan until consecutive sentinels plus the seven-cell/public gates prove otherwise. A transient one-card zero-utilization sample was directly rechecked: the log advanced from step 263080 to 263100 in 20 seconds, all four ranks retained their expected memory, and utilization resumed normal asynchronous variation, excluding a rank stall. W&B continues writing and the current-attempt formal error audit remains empty. The supplemental builder has read about `924.2 GB`, logged People's Speech dirty completion through 1,100 files, and is actively reading files 1,149--1,150 of 3,140. Continue exact coverage unchanged.
- 2026-08-11 Stage211A Hard 15:24 hourly status: the uninterrupted four-rank run reached live step `268,000/966,315` (`27.7342%`) with persisted DeepSpeed tag `step-268000`, advancing `4,940` optimizer steps since the preceding snapshot. The first Hard epoch is approximately `83.20%` complete, and original Easy/Medium/Hard/Long Stage-A coverage is approximately `65.08%` complete. The partial `260k-268k` current-domain window contains `801` ten-step records and improves mean mixer loss/cosine to `0.099424/0.964421` from the complete `250k-260k` window's `0.100633/0.963990`; every comparison retains zero hidden missing count and zero frame delta. The 270k immutable Easy-ZH sentinel is not yet present, so the single step-260k reversal remains unconfirmed. A low-utilization sample coincided with the exact step-268000 checkpoint boundary; a direct 20-second recheck observed step `268020 -> 268040` and normal four-card utilization variation, excluding a checkpoint-related stall. W&B continues writing and the current-attempt formal error audit remains empty. The supplemental builder has read about `977.8 GB`, logged People's Speech dirty completion through 1,200 files, is actively reading files 1,254--1,255 of 3,140, and has now accepted `3,000,000` natural utterances totaling `11,773.635 h`. Continue scanning and exact training unchanged; this milestone is data-volume evidence, not a substitute for the final immutable supplemental inventory and three-epoch receipt.
- 2026-08-11 Stage211A Hard 16:24 hourly status and step-270k sentinel: the uninterrupted four-rank run reached live step `272,930/966,315` (`28.2444%`) with persisted DeepSpeed tag `step-272000`, advancing `4,930` optimizer steps in the hour. The first Hard epoch is approximately `84.73%` complete, and original Easy/Medium/Hard/Long Stage-A coverage is approximately `65.33%` complete. The complete current-domain `260k-270k` window improves mean mixer loss/cosine to `0.099542/0.964380` from `0.100633/0.963990`, and the partial `270k-272.93k` window reaches `0.098733/0.964668`; every comparison retains zero hidden missing count and zero frame delta. The immutable Easy-ZH sentinel disproves sustained recovery from the prior single-point reversal: loss worsens from step-260k `0.1652758815` to step-270k `0.1672270163` (`+1.1805%`), with mean 70-layer loss/cosine/RMS ratio `0.1639100401/0.9446226356/0.8832398901` and only `11/70` layers improving either loss or cosine. Treat step 260k as local noise and retain the balanced all-depth replay requirement unless the complete seven-cell/public gate later proves otherwise. All four ranks remain active, W&B continues writing, and the current-attempt formal error audit remains empty. The supplemental builder has read about `1.032 TB`, logged People's Speech dirty completion through 1,300 files, and is actively reading files 1,374--1,375 of 3,140. Continue exact coverage unchanged.
- 2026-08-11 Stage211A Hard 17:24 hourly status: the uninterrupted four-rank run reached live step `277,860/966,315` (`28.7546%`) with persisted DeepSpeed tag `step-276000`, advancing `4,930` optimizer steps in the hour. The first Hard epoch is approximately `86.26%` complete, and original Easy/Medium/Hard/Long Stage-A coverage is approximately `65.57%` complete. The partial `270k-277.86k` current-domain window contains `787` ten-step records at mean mixer loss/cosine `0.098839/0.964629`, improving on the complete `260k-270k` window's `0.099542/0.964380`; every comparison retains zero hidden missing count and zero frame delta. The 280k immutable Easy-ZH sentinel is not yet present, so no fixed-retention inference is made from the sampled batch. All four ranks remain active, W&B continues writing, and the current-attempt formal error audit remains empty. The supplemental builder has read about `1.084 TB`, logged People's Speech dirty completion through 1,400 files, and is actively reading files 1,475--1,476 of 3,140, approximately `47%` of the dirty archive-file set. Continue exact training and natural-data enumeration unchanged.
- 2026-08-11 Stage211A Hard 18:24 hourly status and step-280k sentinel: the uninterrupted four-rank run reached live step `282,830/966,315` (`29.2689%`) with persisted DeepSpeed tag `step-282000`, advancing `4,970` optimizer steps in the hour. The first Hard epoch is approximately `87.81%` complete, and original Easy/Medium/Hard/Long Stage-A coverage is approximately `65.82%` complete. The complete current-domain `270k-280k` window improves mean mixer loss/cosine to `0.098990/0.964576`; the partial `280k-282.83k` window remains within local variation at `0.099932/0.964230`, and all comparisons retain zero hidden missing count and zero frame delta. The immutable Easy-ZH sentinel worsens from step-270k `0.1672270163` to step-280k `0.1695592104` (`+1.3946%`), with mean 70-layer loss/cosine/RMS ratio `0.1661684054/0.9439493028/0.8804379620`; only `8/70` layers improve loss and `10/70` improve cosine relative to step 270k. The old-domain retention failure is therefore continuing across nearly all depths despite healthy Hard convergence. All four ranks remain active, W&B continues writing, and the current-attempt formal error audit remains empty. The supplemental builder has read about `1.138 TB`, logged People's Speech dirty completion through 1,500 files, and is actively reading files 1,578--1,579 of 3,140, crossing half of the dirty archive-file set. Continue exact coverage unchanged and retain the post-coverage balanced all-depth correction gate.
- 2026-08-11 Stage211A Hard 19:24 hourly status: the uninterrupted four-rank run reached live step `287,800/966,315` (`29.7832%`) with persisted DeepSpeed tag `step-286000`, advancing `4,970` optimizer steps in the hour. The first Hard epoch is approximately `89.35%` complete, and original Easy/Medium/Hard/Long Stage-A coverage is approximately `66.07%` complete. The partial `280k-287.8k` current-domain window contains `781` ten-step records at mean mixer loss/cosine `0.100455/0.964051`, above the best complete `270k-280k` window's `0.098990/0.964576` but still within the recent plateau; do not diagnose reversal before the full 280k-290k window. Every comparison retains zero hidden missing count and zero frame delta, and the 290k immutable Easy-ZH sentinel is not yet present. All four ranks remain active, W&B continues writing, and the current-attempt formal error audit remains empty. The supplemental builder has read about `1.190 TB`, logged People's Speech dirty completion through 1,600 files, and is actively reading files 1,680--1,681 of 3,140, approximately `53.5%` of the dirty archive-file set. Continue exact training and natural-data enumeration unchanged.
- 2026-08-11 Stage211A Hard 20:24 hourly status and step-290k sentinel: the uninterrupted four-rank run reached live step `292,780/966,315` (`30.2986%`) with persisted DeepSpeed tag `step-292000`, advancing `4,980` optimizer steps in the hour. The first Hard epoch is approximately `90.90%` complete, and original Easy/Medium/Hard/Long Stage-A coverage is approximately `66.32%` complete. The complete current-domain `280k-290k` window settles at mean mixer loss/cosine `0.100533/0.964025`, above the best `270k-280k` window but consistent with the partial `290k-292.78k` window's `0.100513/0.964050`; every comparison retains zero hidden missing count and zero frame delta, so this is a plateau fluctuation rather than an implementation failure. The immutable Easy-ZH sentinel improves from step-280k `0.1695592104` to step-290k `0.1675769101` (`-1.1691%`), with mean 70-layer loss/cosine/RMS ratio `0.1642533260/0.9445017808/0.8832613993` and `57/70` loss plus `56/70` cosine improvements. It nevertheless remains slightly worse than step 270k `0.1672270163` and far above initialization, so interpret this as recovery from the 280k spike, not broad old-domain recovery. All four ranks remain active, W&B continues writing, and the current-attempt formal error audit remains empty. The supplemental builder has read about `1.242 TB`, logged People's Speech dirty completion through 1,700 files, and is actively reading files 1,787--1,788 of 3,140. Continue exact coverage unchanged.
- 2026-08-11 Stage211A Hard 21:24 hourly status: the uninterrupted four-rank run reached live step `297,770/966,315` (`30.8150%`) with persisted DeepSpeed tag `step-296000`, advancing `4,990` optimizer steps in the hour. The first Hard epoch is approximately `92.45%` complete, and original Easy/Medium/Hard/Long Stage-A coverage is approximately `66.57%` complete. The partial `290k-297.77k` current-domain window contains `778` ten-step records at mean mixer loss/cosine `0.101064/0.963849`, a small plateau increase from the complete `280k-290k` window's `0.100533/0.964025`; every comparison retains zero hidden missing count and zero frame delta. The 300k immutable Easy-ZH sentinel is not yet present, so no retention inference is made from the sampled batch. All four ranks remain active, W&B continues writing, and the current-attempt formal error audit remains empty. The supplemental builder has read about `1.295 TB`, logged People's Speech dirty completion through 1,800 files, and is actively reading files 1,897--1,898 of 3,140, approximately `60.4%` of the dirty archive-file set. Continue exact training and natural-data enumeration unchanged.
- 2026-08-11 Stage211A Hard 22:24 hourly status and step-300k sentinel: the uninterrupted four-rank run reached live step `302,980/966,315` (`31.3542%`) with persisted DeepSpeed tag `step-302000`. The first Hard epoch is approximately `94.06%` complete with `19,125` steps remaining to its exact natural boundary, and original Easy/Medium/Hard/Long Stage-A coverage is approximately `66.83%` complete. The complete current-domain `290k-300k` window remains on the plateau at mean mixer loss/cosine `0.100969/0.963883`; the partial `300k-302.98k` window is `0.101657/0.963627`, with zero hidden missing count and zero frame delta. The immutable Easy-ZH sentinel worsens slightly from step-290k `0.1675769101` to step-300k `0.1679257373` (`+0.2082%`), with mean 70-layer loss/cosine/RMS ratio `0.1646020487/0.9444425594/0.8825547997`; `35/70` layers improve loss and `38/70` improve cosine, so no broad retention recovery exists. A transient asymmetric utilization snapshot was directly rechecked: step advanced `303000 -> 303030` in 20 seconds and all four cards resumed normal variation, excluding a rank stall. W&B continues writing and the current-attempt formal error audit remains empty. The supplemental builder has read about `1.350 TB`, logged People's Speech dirty completion through 2,000 files, is actively reading files 2,015--2,016 of 3,140, and has accepted `4,000,000` natural utterances totaling `15,766.377 h`. Continue exact training and enumeration unchanged; the mutable count still requires final immutable inventory validation before fifth-segment admission.
- 2026-08-11 Stage211A Hard 23:24 hourly status: the uninterrupted four-rank run reached live step `308,020/966,315` (`31.8757%`) with persisted DeepSpeed tag `step-308000`, advancing `5,040` optimizer steps in the hour. The first Hard epoch is approximately `95.63%` complete with `14,085` steps remaining to its exact natural boundary, and original Easy/Medium/Hard/Long Stage-A coverage is approximately `67.08%` complete. The partial `300k-308.02k` current-domain window contains `804` ten-step records at mean mixer loss/cosine `0.101865/0.963553`, a plateau increase from the complete `290k-300k` window's `0.100969/0.963883`; every comparison retains zero hidden missing count and zero frame delta. The 310k immutable Easy-ZH sentinel is not yet present. All four ranks remain active, W&B continues writing, and the current-attempt formal error audit remains empty. The supplemental builder has read about `1.402 TB`, logged People's Speech dirty completion through 2,100 files, and is actively reading files 2,125--2,126 of 3,140, approximately `67.7%` of the dirty archive-file set. Continue exact training and natural-data enumeration unchanged.
- 2026-08-12 Stage211A Hard 00:24 hourly status and step-310k sentinel: the uninterrupted four-rank run reached live step `313,100/966,315` (`32.4014%`) with persisted DeepSpeed tag `step-312000`, advancing `5,080` optimizer steps in the hour. The first Hard epoch is approximately `97.20%` complete with `9,005` steps remaining to its exact natural boundary, and original Easy/Medium/Hard/Long Stage-A coverage is approximately `67.34%` complete. The complete current-domain `300k-310k` window remains on the plateau at mean mixer loss/cosine `0.101827/0.963566`, while the partial `310k-313.1k` window improves to `0.100295/0.964114`; every comparison retains zero hidden missing count and zero frame delta. The immutable Easy-ZH sentinel worsens from step-300k `0.1679257373` to step-310k `0.1686186568` (`+0.4126%`), with mean 70-layer loss/cosine/RMS ratio `0.1652196779/0.9442854115/0.8814717947` and only `31/70` layers improving either loss or cosine. Old-domain retention therefore remains unresolved. All four ranks remain active, W&B continues writing, and the current-attempt formal error audit remains empty. The supplemental builder has read about `1.453 TB`, logged People's Speech dirty completion through 2,200 files, and is actively reading files 2,230--2,231 of 3,140, approximately `71%` of the dirty archive-file set. Continue exact training and enumeration unchanged.
- 2026-08-12 Stage211A Hard 01:24 hourly status: the uninterrupted four-rank run reached live step `318,120/966,315` (`32.9209%`) with persisted DeepSpeed tag `step-318000`, advancing `5,020` optimizer steps in the hour. The first Hard epoch is `98.76%` complete with only `3,985` steps remaining to its exact `322,105`-step natural boundary, and original Easy/Medium/Hard/Long Stage-A coverage is approximately `67.59%` complete. The partial `310k-318.12k` current-domain window contains `813` ten-step records and improves mean mixer loss/cosine to `0.100145/0.964173` from the complete `300k-310k` window's `0.101827/0.963566`; every comparison retains zero hidden missing count and zero frame delta. The 320k immutable Easy-ZH sentinel is not yet present. All four ranks remain active, W&B continues writing, and the current-attempt formal error audit remains empty. The supplemental builder has read about `1.505 TB`, logged People's Speech dirty completion through 2,300 files, and is actively reading files 2,340--2,341 of 3,140, approximately `74.5%` of the dirty archive-file set. Continue unchanged; the next hour-scale check must verify the immutable `epoch-1.pt` natural-boundary evidence rather than infer epoch completion from step arithmetic alone.
- 2026-08-12 Stage211A Hard 02:24 first-epoch completion and step-320k sentinel: the uninterrupted four-rank run reached live step `322,830/966,315` (`33.4084%`) and entered epoch 2 by `725` steps (`0.2251%`). Hard epoch 1 is proven complete at the exact natural boundary rather than inferred: top-level `epoch-1.pt` and its DeepSpeed `epoch-1` directory were written at step `322,105`; the directory contains one model shard and all four non-empty optimizer shards; `latest_checkpoint.yaml` records `step=322105`, `epoch=1`, and `epoch_batch_offset=0`; a memory-mapped weights-only read of the exported checkpoint independently records `step=322105`, `extra.epoch=1`, `extra.epoch_batch_offset=0`, and `extra.completed_epoch_batch_count=322105`; the log reports epoch-1 train loss `0.1010`. The complete current-domain `310k-320k` window improves mean mixer loss/cosine to `0.100296/0.964117` from `0.101827/0.963566`, while the partial second-epoch `320k-322.83k` window is `0.101749/0.963604`; every comparison retains zero hidden missing count and zero frame delta. The immutable Easy-ZH sentinel worsens slightly from step-310k `0.1686186568` to step-320k `0.1688130504` (`+0.1153%`), with mean 70-layer loss/cosine/RMS ratio `0.1654437573/0.9442252681/0.8810577252` and `35/70` loss plus `35/70` cosine improvements, so old-domain retention remains unresolved. Original four-segment Stage-A progress is approximately `67.82%`. All four ranks remain active, W&B continues writing, and the current-attempt formal error audit remains empty. The supplemental builder has read about `1.548 TB`, logged People's Speech dirty completion through 2,400 files, and is actively reading files 2,427--2,428 of 3,140, approximately `77.3%` of the dirty archive-file set. Continue exact epoch 2 without interruption.
- 2026-08-12 Stage211A Hard 03:24 hourly status and I/O diagnosis: the uninterrupted four-rank run reached live step `326,110/966,315` (`33.7478%`), putting epoch 2 at `4,005/322,105` steps (`1.2434%`) and original Easy/Medium/Hard/Long Stage-A coverage at approximately `67.99%`. The partial `320k-326.11k` current-domain window contains `612` ten-step records at mean mixer loss/cosine `0.102311/0.963402`, above the complete `310k-320k` window's `0.100296/0.964117` but with zero hidden missing count and zero frame delta; the 330k immutable sentinel is not yet present. Hourly throughput fell to approximately `3,280` steps while the low-priority supplemental builder advanced only from files 2,428 to 2,440 and read about `5.4 GB`. A direct 30-second health recheck observed step `326140 -> 326170`, normal four-card utilization variation, active/synchronizing training ranks, and the builder waiting in `blk_mq_get_tag`; this proves ongoing training with storage/data-segment pressure rather than a rank stall. Do not restart or reprioritize either process: the builder's idle I/O policy is correctly yielding to formal training. W&B continues writing and the current-attempt formal error audit remains empty. The builder remains active on files 2,439--2,440 of 3,140 (`77.7%`) with about `1.553 TB` read. Continue exact epoch 2 and monitor throughput at the next hour-scale check.
- 2026-08-12 Stage211A Hard 04:24 hourly status: the uninterrupted four-rank run reached live step `329,530/966,315` (`34.1017%`) with persisted DeepSpeed tag `step-328000`, advancing `3,420` optimizer steps in the hour. Hard epoch 2 is at `7,425/322,105` steps (`2.3051%`), and original Easy/Medium/Hard/Long Stage-A coverage is `68.1568%` complete. The partial `320k-329.53k` current-domain window contains `954` ten-step records at mean mixer loss/cosine `0.102022/0.963503`, above the complete `310k-320k` window's `0.100296/0.964117` but with zero hidden missing count and zero frame delta. The next immutable Easy-ZH sentinel is due at step 330,000 and is not yet present, so no new fixed-retention or public WER/CER claim is made. All four ranks remain active, W&B continues writing, and expanded inspection confirms that the four broad-regex audit hits are normal configuration/resume messages rather than runtime errors; the current-attempt formal error audit remains empty. The low-priority supplemental builder is waiting in `blk_mq_get_tag`, has read about `1.559 TB`, and is actively reading People's Speech dirty files 2,450--2,451 of 3,140 (approximately `78.1%`). The second consecutive I/O-constrained hour is slow but healthy; do not restart or reprioritize training or enumeration, and continue exact epoch 2 unchanged.
- 2026-08-12 Stage211A Hard step-330k fixed sentinel: the immutable 256-sample Easy-ZH report was naturally written against the same manifest/provenance binding at step `330,000`, together with a complete DeepSpeed checkpoint. Fixed loss improves from step-320k `0.1688130504` to `0.1676924613` (`-0.6638%`); mean 70-layer loss/cosine/RMS ratio improves from `0.1654437573/0.9442252681/0.8810577252` to `0.1643913677/0.9445045985/0.8826330364`, with `35/70` layers improving loss and `35/70` improving cosine. This is only a small local reversal: relative to the bound Hard initialization, fixed loss is still `59.3124%` worse and `0/70` layers improve either loss or cosine. The complete `320k-330k` Hard-domain window contains `1,000` ten-step records at mean mixer loss/cosine `0.102009/0.963507`, with zero hidden missing count and zero frame delta; the current-attempt formal error audit is empty. Continue exact Hard epoch 2 unchanged. This sentinel is hidden-alignment retention evidence only and must not be reported as public WER/CER or used to enter Block early.
- 2026-08-12 Stage211A Hard 05:24 hourly status: the uninterrupted four-rank run reached live step `333,010/966,315` (`34.4618%`) with persisted DeepSpeed tag `step-332000`, advancing `3,480` optimizer steps in the hour. Hard epoch 2 is at `10,905/322,105` steps (`3.3855%`), and original Easy/Medium/Hard/Long Stage-A coverage is `68.3308%` complete. The partial `330k-333.01k` current-domain window contains `301` ten-step records and improves mean mixer loss/cosine to `0.100919/0.963899` from the complete `320k-330k` window's `0.102009/0.963507`, with zero hidden missing count and zero frame delta. The latest immutable sentinel remains step 330,000; step 340,000 is not yet present, so there is no newer fixed-retention or public WER/CER evidence. All four ranks remain active, W&B continues writing, the current-attempt formal error audit is empty, `/media/usbhd` remains the intended `/dev/sdb2` read-only XFS mount, and root retains `771 GiB` free. The low-priority supplemental builder remains I/O-waiting in `blk_mq_get_tag`, has read about `1.565 TB`, and is actively reading People's Speech dirty files 2,461--2,462 of 3,140 (approximately `78.4%`). Continue exact Hard epoch 2 and natural-data enumeration unchanged.
- 2026-08-12 Stage211A Hard 06:24 hourly status: the uninterrupted four-rank run reached live step `336,410/966,315` (`34.8137%`) with persisted DeepSpeed tag `step-336000`, advancing `3,400` optimizer steps in the hour. Hard epoch 2 is at `14,305/322,105` steps (`4.4411%`), and original Easy/Medium/Hard/Long Stage-A coverage is `68.5007%` complete. The partial `330k-336.41k` current-domain window contains `641` ten-step records at mean mixer loss/cosine `0.100791/0.963939`, with zero hidden missing count and zero frame delta. The latest immutable sentinel remains step 330,000; step 340,000 is not yet present, so no new fixed-retention or public WER/CER conclusion is available. All four ranks remain active or synchronizing, W&B continues writing, the current-attempt formal error audit is empty, `/media/usbhd` remains the intended `/dev/sdb2` read-only XFS mount, and root retains `771 GiB` free. The low-priority supplemental builder remains I/O-waiting in `blk_mq_get_tag`, has read about `1.571 TB`, and is actively reading People's Speech dirty files 2,471--2,472 of 3,140 (approximately `78.7%`). Continue exact Hard epoch 2 and natural-data enumeration unchanged.
- 2026-08-12 Stage211A Hard 07:31 status and step-340k fixed sentinel: the uninterrupted four-rank run reached live step `340,300/966,315` (`35.2163%`) with a complete DeepSpeed checkpoint at step 340,000, advancing `3,890` optimizer steps since the 06:24 snapshot. Hard epoch 2 is at `18,195/322,105` steps (`5.6488%`), and original Easy/Medium/Hard/Long Stage-A coverage is `68.6952%` complete. The complete `330k-340k` current-domain window improves mean mixer loss/cosine to `0.100474/0.964053`, with zero hidden missing count and zero frame delta. On the immutable 256-sample Easy-ZH sentinel with unchanged provenance, fixed loss improves from step-330k `0.1676924613` to `0.1662237565` (`-0.8758%`); mean 70-layer loss/cosine/RMS ratio improves from `0.1643913677/0.9445045985/0.8826330364` to `0.1629767587/0.9448985724/0.8851470698`. Loss improves in `53/70` layers, cosine in `50/70`, and all seven ten-layer depth bands improve loss, which is broader local recovery than at 330k. It still fails retention decisively: fixed loss remains `57.9171%` above the Hard initialization and `0/70` layers improve either loss or cosine versus initialization. All four ranks remain active, W&B continues writing, and the current-attempt formal error audit is empty. The supplemental builder has read about `1.577 TB`, is I/O-waiting in `blk_mq_get_tag`, and is actively reading People's Speech dirty files 2,483--2,484 of 3,140 (approximately `79.1%`). Continue exact Hard epoch 2; this sentinel is not public WER/CER and cannot admit Block early.
- 2026-08-12 Stage211A Hard 08:30 hourly status: the uninterrupted four-rank run reached live step `343,790/966,315` (`35.5774%`) with persisted DeepSpeed tag `step-342000`, advancing `3,490` optimizer steps since the 07:31 snapshot. Hard epoch 2 is at `21,685/322,105` steps (`6.7323%`), and original Easy/Medium/Hard/Long Stage-A coverage is `68.8697%` complete. The partial `340k-343.79k` current-domain window contains `379` ten-step records at mean mixer loss/cosine `0.100566/0.964011`, with zero hidden missing count and zero frame delta. The latest immutable sentinel remains step 340,000; step 350,000 is not yet present, so there is no newer fixed-retention or public WER/CER evidence. An initially low four-GPU utilization snapshot was transient: a direct follow-up observed live progress to step 343,880 and resumed utilization variation, excluding a rank stall. All four ranks remain active or synchronizing, W&B continues writing, the current-attempt formal error audit is empty, `/media/usbhd` remains the intended `/dev/sdb2` read-only XFS mount, and root retains `771 GiB` free. The supplemental builder remains I/O-waiting in `blk_mq_get_tag`, has read about `1.583 TB`, and is actively reading People's Speech dirty files 2,493--2,494 of 3,140 (approximately `79.4%`). Continue exact Hard epoch 2 and natural-data enumeration unchanged.
- 2026-08-12 Stage211A Hard 09:30 hourly status: the uninterrupted four-rank run reached live step `347,370/966,315` (`35.9479%`) with persisted DeepSpeed tag `step-346000`, advancing `3,580` optimizer steps in the hour. Hard epoch 2 is at `25,265/322,105` steps (`7.8437%`), and original Easy/Medium/Hard/Long Stage-A coverage is `69.0487%` complete. The partial `340k-347.37k` current-domain window contains `737` ten-step records at mean mixer loss/cosine `0.100793/0.963929`, with zero hidden missing count and zero frame delta. The latest immutable sentinel remains step 340,000; step 350,000 is not yet present, so no new fixed-retention or public WER/CER conclusion is available. All four ranks remain active or synchronizing, W&B continues writing, the current-attempt formal error audit is empty, `/media/usbhd` remains the intended `/dev/sdb2` read-only XFS mount, and root retains `771 GiB` free. The supplemental builder remains I/O-waiting in `blk_mq_get_tag`, has read about `1.589 TB`, and is actively reading People's Speech dirty files 2,504--2,505 of 3,140 (approximately `79.8%`). Continue exact Hard epoch 2 and natural-data enumeration unchanged.
- 2026-08-12 Stage211A Hard 10:30 status and step-350k fixed sentinel: the uninterrupted four-rank run reached live step `350,890/966,315` (`36.3122%`) with a complete DeepSpeed checkpoint at step 350,000, advancing `3,520` optimizer steps in the hour. Hard epoch 2 is at `28,785/322,105` steps (`8.9365%`), and original Easy/Medium/Hard/Long Stage-A coverage is `69.2247%` complete. The complete `340k-350k` current-domain window remains stable at mean mixer loss/cosine `0.100725/0.963955`, with zero hidden missing count and zero frame delta. On the unchanged-provenance 256-sample Easy-ZH sentinel, fixed loss regresses from step-340k `0.1662237565` to `0.1689167286` (`+1.6201%`); mean 70-layer loss/cosine/RMS ratio regresses from `0.1629767587/0.9448985724/0.8851470698` to `0.1656031862/0.9441292612/0.8819543161`. Only `3/70` layers improve loss and `3/70` improve cosine, while all seven ten-layer depth bands regress. Relative to the Hard initialization, fixed loss is now `60.4755%` worse and `0/70` layers improve either loss or cosine. This confirms that the 340k recovery did not persist and that old-domain forgetting remains, while stable Hard-domain optimization and an empty runtime error audit exclude a training failure. All four ranks remain active, W&B continues writing, and the supplemental builder has read about `1.596 TB`, remains I/O-waiting in `blk_mq_get_tag`, and is actively reading People's Speech dirty files 2,516--2,517 of 3,140 (approximately `80.1%`). Continue exact full coverage; after all five segments, require balanced all-depth Layer replay before Block if the representative hidden/public gates fail.
- 2026-08-12 Stage211A Hard 11:29 hourly status: the uninterrupted four-rank run reached live step `354,540/966,315` (`36.6899%`) with persisted DeepSpeed tag `step-354000`, advancing `3,650` optimizer steps in the hour. Hard epoch 2 is at `32,435/322,105` steps (`10.0697%`), and original Easy/Medium/Hard/Long Stage-A coverage is `69.4072%` complete. The partial `350k-354.54k` current-domain window contains `454` ten-step records at mean mixer loss/cosine `0.101433/0.963711`, with zero hidden missing count and zero frame delta. The latest immutable sentinel remains step 350,000; step 360,000 is not yet present, so no newer fixed-retention or public WER/CER conclusion is available. All four ranks remain active or synchronizing with consistent cumulative CPU use, W&B continues writing, the current-attempt formal error audit is empty, `/media/usbhd` remains the intended `/dev/sdb2` read-only XFS mount, and root retains `771 GiB` free. The low-utilization GPU sample coincides with the known storage wait and is not stall evidence because live logging and checkpointing continue. The supplemental builder remains I/O-waiting in `blk_mq_get_tag`, has read about `1.602 TB`, and is actively reading People's Speech dirty files 2,527--2,528 of 3,140 (approximately `80.5%`). Continue exact Hard epoch 2 and natural-data enumeration unchanged.
- 2026-08-12 Stage211A Hard 12:28 hourly status: the uninterrupted four-rank run reached live step `358,270/966,315` (`37.0759%`) with persisted DeepSpeed tag `step-358000`, advancing `3,730` optimizer steps in the hour. Hard epoch 2 is at `36,165/322,105` steps (`11.2277%`), and original Easy/Medium/Hard/Long Stage-A coverage is `69.5937%` complete. The partial `350k-358.27k` current-domain window contains `827` ten-step records at mean mixer loss/cosine `0.101024/0.963857`, with zero hidden missing count and zero frame delta. The latest immutable sentinel remains step 350,000; step 360,000 is not yet present, so no newer fixed-retention or public WER/CER conclusion is available. All four ranks remain active or synchronizing, W&B continues writing, the current-attempt formal error audit is empty, `/media/usbhd` remains the intended `/dev/sdb2` read-only XFS mount, and root retains `771 GiB` free. The supplemental builder remains I/O-waiting in `blk_mq_get_tag`, has read about `1.608 TB`, and is actively reading People's Speech dirty files 2,538--2,539 of 3,140 (approximately `80.9%`). Continue exact Hard epoch 2 and natural-data enumeration unchanged.
- 2026-08-12 Stage211A Hard 13:27 status and step-360k fixed sentinel: the uninterrupted four-rank run reached live step `362,050/966,315` (`37.4671%`) with persisted DeepSpeed tag `step-362000`, advancing `3,780` optimizer steps in the hour. Hard epoch 2 is at `39,945/322,105` steps (`12.4012%`), and original Easy/Medium/Hard/Long Stage-A coverage is `69.7827%` complete. The complete `350k-360k` current-domain window remains stable at mean mixer loss/cosine `0.100892/0.963904`, with zero hidden missing count and zero frame delta. On the unchanged-provenance 256-sample Easy-ZH sentinel, fixed loss regresses from step-350k `0.1689167286` to `0.1695937448` (`+0.4008%`); mean 70-layer loss/cosine/RMS ratio regresses from `0.1656031862/0.9441292612/0.8819543161` to `0.1662402939/0.9439560675/0.8810978171`. Only `28/70` layers improve loss and `27/70` improve cosine; depth bands 40--59 improve locally while the other five bands regress. Relative to the Hard initialization, fixed loss is `61.1186%` worse and `0/70` layers improve either loss or cosine. Stable Hard-domain optimization, complete online matching, and an empty runtime error audit continue to identify old-domain forgetting rather than a training fault. All four ranks remain active, W&B continues writing, `/media/usbhd` remains the intended read-only mount, and the supplemental builder has read about `1.616 TB`, remains I/O-waiting in `blk_mq_get_tag`, and is actively reading People's Speech dirty files 2,551--2,552 of 3,140 (approximately `81.3%`). Continue exact full coverage; retain the mandatory balanced all-depth Layer replay before Block if the representative hidden/public gates fail.
- 2026-08-12 Stage211A Hard 14:26 hourly status: the uninterrupted four-rank run reached live step `365,860/966,315` (`37.8614%`) with persisted DeepSpeed tag `step-364000`, advancing `3,810` optimizer steps in the hour. Hard epoch 2 is at `43,755/322,105` steps (`13.5841%`), and original Easy/Medium/Hard/Long Stage-A coverage is `69.9732%` complete. The partial `360k-365.86k` current-domain window contains `586` ten-step records and improves mean mixer loss/cosine to `0.099908/0.964254`, with zero hidden missing count and zero frame delta. The latest immutable sentinel remains step 360,000; step 370,000 is not yet present, so no newer fixed-retention or public WER/CER conclusion is available. All four ranks remain active, W&B continues writing, the current-attempt formal error audit is empty, `/media/usbhd` remains the intended `/dev/sdb2` read-only XFS mount, and root retains `771 GiB` free. The supplemental builder remains I/O-waiting in `blk_mq_get_tag`, has read about `1.622 TB`, and is actively reading People's Speech dirty files 2,562--2,563 of 3,140 (approximately `81.6%`). Continue exact Hard epoch 2 and natural-data enumeration unchanged.
- 2026-08-12 Stage211A Hard 15:32 status and step-370k fixed sentinel: the uninterrupted four-rank run reached live step `370,190/966,315` (`38.3095%`) with a complete DeepSpeed checkpoint at step 370,000, advancing `4,330` optimizer steps since the 14:26 snapshot. Hard epoch 2 is at `48,085/322,105` steps (`14.9284%`), and original Easy/Medium/Hard/Long Stage-A coverage is `70.1897%` complete. The complete `360k-370k` current-domain window improves mean mixer loss/cosine to `0.100058/0.964198`, with zero hidden missing count and zero frame delta. On the unchanged-provenance 256-sample Easy-ZH sentinel, fixed loss is effectively flat from step-360k `0.1695937448` to `0.1695795477` (`-0.0084%`); mean 70-layer loss/cosine/RMS ratio changes from `0.1662402939/0.9439560675/0.8810978171` to `0.1661629545/0.9439898186/0.8817435948`. Loss improves in `37/70` layers and cosine in `36/70`; bands 20--59 improve, while shallow bands 0--19 and deepest band 60--69 regress. Relative to the Hard initialization, fixed loss remains `61.1052%` worse and `0/70` layers improve either loss or cosine. This is an old-domain forgetting plateau, not recovery. All four ranks remain active, W&B continues writing, and the current-attempt formal error audit is empty. The supplemental builder has read about `1.631 TB`, remains I/O-waiting in `blk_mq_get_tag`, and is actively reading People's Speech dirty files 2,577--2,578 of 3,140 (approximately `82.1%`). Continue exact full coverage and preserve the mandatory balanced all-depth Layer replay before Block if the representative hidden/public gates fail.
- 2026-08-12 Stage211A Hard 16:32 hourly status: the uninterrupted four-rank run reached live step `374,210/966,315` (`38.7255%`) with persisted DeepSpeed tag `step-374000`, advancing `3,930` optimizer steps in approximately one hour. Hard epoch 2 is at `52,105/322,105` steps (`16.1764%`), and original Easy/Medium/Hard/Long Stage-A coverage is `70.3907%` complete. The partial `370k-374.21k` current-domain window contains `421` ten-step records at mean mixer loss/cosine `0.101110/0.963819`, with zero hidden missing count and zero frame delta. The latest immutable sentinel remains step 370,000; step 380,000 is not yet present, so no newer fixed-retention or public WER/CER conclusion is available. All four ranks remain active or synchronizing, W&B continues writing, the current-attempt formal error audit is empty, `/media/usbhd` remains the intended `/dev/sdb2` read-only XFS mount, and root retains `771 GiB` free. The supplemental builder remains I/O-waiting in `blk_mq_get_tag`, has read about `1.639 TB`, and is actively reading People's Speech dirty files 2,592--2,593 of 3,140 (approximately `82.6%`). Continue exact Hard epoch 2 and natural-data enumeration unchanged.
- 2026-08-12 Stage211A Hard 17:31 hourly status: the uninterrupted four-rank run reached live step `378,360/966,315` (`39.1549%`) with persisted DeepSpeed tag `step-378000`, advancing `4,150` optimizer steps in the hour. Hard epoch 2 is at `56,255/322,105` steps (`17.4648%`), and original Easy/Medium/Hard/Long Stage-A coverage is `70.5982%` complete. The partial `370k-378.36k` current-domain window contains `836` ten-step records at mean mixer loss/cosine `0.100556/0.964017`, with zero hidden missing count and zero frame delta. The latest immutable sentinel remains step 370,000; step 380,000 is not yet present, so no newer fixed-retention or public WER/CER conclusion is available. All four ranks remain active, W&B continues writing, the current-attempt formal error audit is empty, `/media/usbhd` remains the intended `/dev/sdb2` read-only XFS mount, and root retains `771 GiB` free. The supplemental builder remains I/O-waiting in `blk_mq_get_tag`, has read about `1.649 TB`, and is actively reading People's Speech dirty files 2,609--2,610 of 3,140 (approximately `83.1%`). Continue exact Hard epoch 2 and natural-data enumeration unchanged.
- 2026-08-12 Stage211A Hard 18:30 status and step-380k fixed sentinel: the uninterrupted four-rank run reached live step `382,810/966,315` (`39.6154%`) with persisted DeepSpeed tag `step-382000`, advancing `4,450` optimizer steps in the hour. Hard epoch 2 is at `60,705/322,105` steps (`18.8463%`), and original Easy/Medium/Hard/Long Stage-A coverage is `70.8207%` complete. The complete `370k-380k` current-domain window remains stable at mean mixer loss/cosine `0.100425/0.964064`, with zero hidden missing count and zero frame delta. On the unchanged-provenance 256-sample Easy-ZH sentinel, fixed loss is effectively flat from step-370k `0.1695795477` to `0.1695750394` (`-0.0027%`); mean 70-layer loss/cosine/RMS ratio changes from `0.1661629545/0.9439898186/0.8817435948` to `0.1662355918/0.9439595513/0.8814292950`. Only `30/70` layers improve loss and `29/70` improve cosine; depth bands 0--19 and 60--69 improve while bands 20--59 regress. Relative to the Hard initialization, fixed loss remains `61.1009%` worse and `0/70` layers improve either loss or cosine, confirming the old-domain forgetting plateau. All four ranks remain active, W&B continues writing, and the current-attempt formal error audit is empty. The supplemental builder has read about `1.663 TB`, remains I/O-waiting in `blk_mq_get_tag`, and is actively reading People's Speech dirty files 2,640--2,641 of 3,140 (approximately `84.1%`). Continue exact full coverage and preserve the mandatory balanced all-depth Layer replay before Block if the representative hidden/public gates fail.
- 2026-08-12 Stage211A Hard 19:29 hourly status: the uninterrupted four-rank run reached live step `387,330/966,315` (`40.0832%`) with persisted DeepSpeed tag `step-386000`, advancing `4,520` optimizer steps in the hour. Hard epoch 2 is at `65,225/322,105` steps (`20.2496%`), and original Easy/Medium/Hard/Long Stage-A coverage is `71.0466%` complete. The partial `380k-387.33k` current-domain window contains `733` ten-step records and improves mean mixer loss/cosine to `0.098766/0.964664`, with zero hidden missing count and zero frame delta. The latest immutable sentinel remains step 380,000; step 390,000 is not yet present, so no newer fixed-retention or public WER/CER conclusion is available. All four ranks remain active or synchronizing, W&B continues writing, the current-attempt formal error audit is empty, `/media/usbhd` remains the intended `/dev/sdb2` read-only XFS mount, and root retains `771 GiB` free. The supplemental builder remains I/O-waiting in `blk_mq_get_tag`, has read about `1.678 TB`, and is actively reading People's Speech dirty files 2,670--2,671 of 3,140 (approximately `85.1%`). Continue exact Hard epoch 2 and natural-data enumeration unchanged.
- 2026-08-12 Stage211A Hard 20:28 status and step-390k fixed sentinel: the uninterrupted four-rank run reached live step `391,940/966,315` (`40.5603%`) with a complete DeepSpeed checkpoint at step 390,000, advancing `4,610` optimizer steps in the hour. Hard epoch 2 is at `69,835/322,105` steps (`21.6808%`), and original Easy/Medium/Hard/Long Stage-A coverage is `71.2771%` complete. The complete `380k-390k` current-domain window improves mean mixer loss/cosine to `0.098628/0.964712`, with zero hidden missing count and zero frame delta. On the unchanged-provenance 256-sample Easy-ZH sentinel, fixed loss regresses from step-380k `0.1695750394` to `0.1707038155` (`+0.6656%`); mean 70-layer loss/cosine/RMS ratio regresses from `0.1662355918/0.9439595513/0.8814292950` to `0.1673205684/0.9436481632/0.8803569196`. Only `24/70` layers improve loss and `25/70` improve cosine; the deepest 60--69 band regresses `2.1329%`. Relative to the Hard initialization, fixed loss is `62.1732%` worse and `0/70` layers improve either loss or cosine. The simultaneous Hard-domain improvement and empty runtime error audit further separate optimization health from worsening old-domain retention. All four ranks remain active, W&B continues writing, and the supplemental builder has read about `1.696 TB`, remains I/O-waiting in `blk_mq_get_tag`, and is actively reading People's Speech dirty files 2,705--2,706 of 3,140 (approximately `86.2%`). Continue exact full coverage and preserve mandatory balanced all-depth Layer replay before Block if the representative hidden/public gates fail.
- 2026-08-12 Stage211A Hard 21:27 hourly status: the uninterrupted four-rank run reached live step `396,560/966,315` (`41.0384%`) with persisted DeepSpeed tag `step-396000`, advancing `4,620` optimizer steps in the hour. Hard epoch 2 is at `74,455/322,105` steps (`23.1151%`), and original Easy/Medium/Hard/Long Stage-A coverage is `71.5081%` complete. The partial `390k-396.56k` current-domain window contains `656` ten-step records at mean mixer loss/cosine `0.098657/0.964700`, with zero hidden missing count and zero frame delta. The latest immutable sentinel remains step 390,000; step 400,000 is not yet present, so no newer fixed-retention or public WER/CER conclusion is available. All four ranks remain active or synchronizing with consistent cumulative CPU use, W&B continues writing, the current-attempt formal error audit is empty, `/media/usbhd` remains the intended `/dev/sdb2` read-only XFS mount, and root retains `771 GiB` free. A low-utilization GPU sample is another storage/synchronization point rather than stall evidence because live logging and checkpointing continue. The supplemental builder remains I/O-waiting in `blk_mq_get_tag`, has read about `1.715 TB`, and is actively reading People's Speech dirty files 2,743--2,744 of 3,140 (approximately `87.4%`). Continue exact Hard epoch 2 and natural-data enumeration unchanged.
- 2026-08-12 Stage211A Hard 22:27 status and step-400k fixed sentinel: the uninterrupted four-rank run reached live step `401,100/966,315` (`41.5082%`) with a complete DeepSpeed checkpoint at step 400,000, advancing `4,540` optimizer steps in the hour. Hard epoch 2 is at `78,995/322,105` steps (`24.5246%`), and original Easy/Medium/Hard/Long Stage-A coverage is `71.7351%` complete. The complete `390k-400k` current-domain window remains stable at mean mixer loss/cosine `0.098711/0.964682`, with zero hidden missing count and zero frame delta. On the unchanged-provenance 256-sample Easy-ZH sentinel, fixed loss improves from step-390k `0.1707038155` to `0.1698858256` (`-0.4792%`); mean 70-layer loss/cosine/RMS ratio improves from `0.1673205684/0.9436481632/0.8803569196` to `0.1665546997/0.9438836346/0.8803363314`. Loss and cosine each improve in `48/70` layers, though bands 40--49 and 60--69 regress. This is a broad local reversal but not retention recovery: relative to the Hard initialization, fixed loss remains `61.3961%` worse and `0/70` layers improve either loss or cosine. All four ranks remain active, W&B continues writing, and the current-attempt formal error audit is empty. The supplemental builder has read about `1.732 TB`, remains I/O-waiting in `blk_mq_get_tag`, and is actively reading People's Speech dirty files 2,777--2,778 of 3,140 (approximately `88.5%`). Continue exact full coverage and preserve mandatory balanced all-depth Layer replay before Block if the representative hidden/public gates fail.
- 2026-08-12 Stage211A Hard 23:26 hourly status: the uninterrupted four-rank run reached live step `405,760/966,315` (`41.9904%`) with persisted DeepSpeed tag `step-404000`, advancing `4,660` optimizer steps in the hour. Hard epoch 2 is at `83,655/322,105` steps (`25.9713%`), and original Easy/Medium/Hard/Long Stage-A coverage is `71.9681%` complete. The partial `400k-405.76k` current-domain window contains `576` ten-step records at mean mixer loss/cosine `0.098534/0.964741`, with zero hidden missing count and zero frame delta. The latest immutable sentinel remains step 400,000; step 410,000 is not yet present, so no newer fixed-retention or public WER/CER conclusion is available. All four ranks remain active, W&B continues writing, the current-attempt formal error audit is empty, `/media/usbhd` remains the intended `/dev/sdb2` read-only XFS mount, and root retains `771 GiB` free. The supplemental builder remains I/O-waiting in `blk_mq_get_tag`, has read about `1.755 TB`, and is actively reading People's Speech dirty files 2,823--2,824 of 3,140 (approximately `89.9%`). Continue exact Hard epoch 2 and natural-data enumeration unchanged.
- 2026-08-13 Stage211A Hard 00:25 status and step-410k fixed sentinel: the uninterrupted four-rank run reached live step `410,510/966,315` (`42.4820%`) with a complete DeepSpeed checkpoint at step 410,000, advancing `4,750` optimizer steps in approximately one hour. Hard epoch 2 is at `88,405/322,105` steps (`27.4460%`), and original Easy/Medium/Hard/Long Stage-A coverage is `72.2056%` complete. The complete `400k-410k` current-domain window improves mean mixer loss/cosine to `0.098448/0.964769`, with zero hidden missing count and zero frame delta. On the unchanged-provenance 256-sample Easy-ZH sentinel, fixed loss regresses from step-400k `0.1698858256` to `0.1722706686` (`+1.4038%`); mean 70-layer loss/cosine/RMS ratio regresses from `0.1665546997/0.9438836346/0.8803363314` to `0.1688228850/0.9432210241/0.8784945036`. Only `11/70` layers improve loss and `11/70` improve cosine; six of seven ten-layer depth bands regress, with only band 60--69 effectively flat at `-0.0225%`. Relative to the Hard initialization, fixed loss is `63.6618%` worse and `0/70` layers improve either loss or cosine. The simultaneous current-Hard improvement, complete online matching, and empty runtime error audit identify increasingly current-domain-biased optimization rather than a training failure. All four ranks remain active, W&B continues writing, `/media/usbhd` remains the intended read-only mount, and the supplemental builder has read about `1.781 TB`, remains I/O-waiting in `blk_mq_get_tag`, and is actively reading People's Speech dirty files 2,876--2,877 of 3,140 (approximately `91.6%`). Continue exact full coverage and preserve mandatory balanced all-depth Layer replay before Block if the representative hidden/public gates fail.
- 2026-08-13 Stage211A Hard 01:24 hourly status: the uninterrupted four-rank run reached live step `415,290/966,315` (`42.9767%`) with persisted DeepSpeed tag `step-414000`, advancing `4,780` optimizer steps in the hour. Hard epoch 2 is at `93,185/322,105` steps (`28.9300%`), and original Easy/Medium/Hard/Long Stage-A coverage is `72.4446%` complete. The partial `410k-415.29k` current-domain window contains `529` ten-step records and improves mean mixer loss/cosine to `0.097978/0.964936`, with zero hidden missing count and zero frame delta. The latest immutable sentinel remains step 410,000; step 420,000 is not yet present, so no newer fixed-retention or public WER/CER conclusion is available. All four ranks remain active or synchronizing, W&B continues writing, the current-attempt formal error audit is empty, `/media/usbhd` remains the intended `/dev/sdb2` read-only XFS mount, and root retains `771 GiB` free. The supplemental builder remains I/O-waiting in `blk_mq_get_tag`, has read about `1.808 TB`, and is actively reading People's Speech dirty files 2,958--2,959 of 3,140 (approximately `94.2%`). Its mutable progress log has accepted at least `5,000,000` rows totaling `19,769.476 h`, but those are not final training-scale evidence until the immutable inventory and coverage validation complete. Continue exact Hard epoch 2 and natural-data enumeration unchanged.
- 2026-08-13 Stage211A Hard 02:23 status and step-420k fixed sentinel: the uninterrupted four-rank run reached live step `420,100/966,315` (`43.4744%`) with a complete DeepSpeed checkpoint at step 420,000, advancing `4,810` optimizer steps in the hour. Hard epoch 2 is at `97,995/322,105` steps (`30.4233%`), and original Easy/Medium/Hard/Long Stage-A coverage is `72.6851%` complete. The complete `410k-420k` current-domain window improves mean mixer loss/cosine to `0.097981/0.964938`, with zero hidden missing count and zero frame delta. On the unchanged-provenance 256-sample Easy-ZH sentinel, fixed loss regresses from step-410k `0.1722706686` to `0.1728708488` (`+0.3484%`); mean 70-layer loss/cosine/RMS ratio regresses from `0.1688228850/0.9432210241/0.8784945036` to `0.1694567426/0.9430264498/0.8776886449`. Only `18/70` layers improve loss and `21/70` improve cosine; six of seven ten-layer depth bands regress. Relative to the Hard initialization, fixed loss is `64.2320%` worse and `0/70` layers improve either loss or cosine. Stable current-Hard optimization, complete matching, and an empty runtime error audit continue to identify old-domain forgetting rather than training failure. All four ranks remain active, W&B continues writing, and `/media/usbhd` remains the intended read-only mount. The supplemental builder has read about `1.839 TB`, remains I/O-waiting in `blk_mq_get_tag`, and is actively reading People's Speech dirty files 3,033--3,034 of 3,140 (approximately `96.6%`). No final-root artifact exists yet, so fifth-segment inventory completion remains unproven despite mutable progress. Continue exact full coverage and preserve mandatory balanced all-depth Layer replay before Block if the representative hidden/public gates fail.
- 2026-08-13 Stage211A Hard 03:22 hourly status: the uninterrupted four-rank run reached live step `424,920/966,315` (`43.9732%`) with persisted DeepSpeed tag `step-424000`, advancing `4,820` optimizer steps in the hour. Hard epoch 2 is at `102,815/322,105` steps (`31.9197%`), and original Easy/Medium/Hard/Long Stage-A coverage is `72.9261%` complete. The partial `420k-424.92k` current-domain window contains `492` ten-step records and improves mean mixer loss/cosine to `0.097457/0.965128`, with zero hidden missing count and zero frame delta. The latest immutable sentinel remains step 420,000; step 430,000 is not yet present, so no newer fixed-retention or public WER/CER conclusion is available. All four ranks remain active or synchronizing, W&B continues writing, the current-attempt formal error audit is empty, `/media/usbhd` remains the intended `/dev/sdb2` read-only XFS mount, and root retains `771 GiB` free. The supplemental builder remains alive, has read about `1.869 TB`, and is actively reading People's Speech dirty files 3,105--3,106 of 3,140 (approximately `98.9%`). The final output root is still empty, so immutable fifth-segment inventory completion remains unproven until finalization and validation finish. Continue exact Hard epoch 2 and natural-data enumeration unchanged.
- 2026-08-13 Stage211A Hard 04:28 status and step-430k fixed sentinel: the uninterrupted four-rank run reached live step `430,200/966,315` (`44.5196%`) with a complete DeepSpeed checkpoint at step 430,000, advancing `5,280` optimizer steps since the 03:22 snapshot. Hard epoch 2 is at `108,095/322,105` steps (`33.5589%`), and original Easy/Medium/Hard/Long Stage-A coverage is `73.1901%` complete. The complete `420k-430k` current-domain window improves mean mixer loss/cosine to `0.097589/0.965080`, with zero hidden missing count and zero frame delta. On the unchanged-provenance 256-sample Easy-ZH sentinel, fixed loss regresses from step-420k `0.1728708488` to `0.1740847933` (`+0.7022%`); mean 70-layer loss/cosine/RMS ratio regresses from `0.1694567426/0.9430264498/0.8776886449` to `0.1706715961/0.9426656306/0.8761389580`. Only `18/70` layers improve loss and `17/70` improve cosine, while all seven ten-layer depth bands regress. Relative to the Hard initialization, fixed loss is `65.3853%` worse and `0/70` layers improve either loss or cosine. Stable current-Hard improvement, complete matching, and an empty runtime error audit continue to prove old-domain forgetting rather than training failure. All four ranks remain active, W&B continues writing, and `/media/usbhd` remains the intended read-only mount. The supplemental builder has completed People's Speech dirty enumeration at `3,140/3,140` and entered ZIP-source processing, but remains alive after reading about `1.900 TB`; the final output root is still empty, so immutable fifth-segment completion remains unproven. Continue exact full coverage and preserve mandatory balanced all-depth Layer replay before Block if the representative hidden/public gates fail.
- 2026-08-13 Stage211A Hard 05:27 hourly status: the uninterrupted four-rank run reached live step `435,030/966,315` (`45.0195%`) with persisted DeepSpeed tag `step-434000`, advancing `4,830` optimizer steps in approximately one hour. Hard epoch 2 is at `112,925/322,105` steps (`35.0584%`), and original Easy/Medium/Hard/Long Stage-A coverage is `73.4315%` complete. The partial `430k-435.03k` current-domain window contains `503` ten-step records and improves mean mixer loss/cosine to `0.096790/0.965366`, with zero hidden missing count and zero frame delta. The latest immutable sentinel remains step 430,000; step 440,000 is not yet present, so no newer fixed-retention or public WER/CER conclusion is available. All four ranks remain active or synchronizing, W&B continues writing, the current-attempt formal error audit is empty, `/media/usbhd` remains the intended `/dev/sdb2` read-only XFS mount, and root retains `771 GiB` free. The supplemental builder remains alive after reading about `1.929 TB`; it has completed People's Speech dirty enumeration plus LJSpeech `3/3` and VCTK `11/11` archive acceptance, but the final output root remains empty, so immutable fifth-segment completion remains unproven until all remaining sources, finalization, and validation finish. Continue exact Hard epoch 2 and natural-data enumeration unchanged.
- 2026-08-13 Stage211A Hard 06:32 status and step-440k fixed sentinel: the uninterrupted four-rank run reached live step `440,260/966,315` (`45.5607%`) with a complete DeepSpeed checkpoint at step 440,000, advancing `5,230` optimizer steps since the 05:27 snapshot. Hard epoch 2 is at `118,155/322,105` steps (`36.6821%`), and original Easy/Medium/Hard/Long Stage-A coverage is `73.6930%` complete. The complete `430k-440k` current-domain window improves mean mixer loss/cosine to `0.097207/0.965218`, with zero hidden missing count and zero frame delta. On the unchanged-provenance 256-sample Easy-ZH sentinel, fixed loss improves from step-430k `0.1740847933` to `0.1736181718` (`-0.2680%`); mean 70-layer loss/cosine/RMS ratio improves from `0.1706715961/0.9426656306/0.8761389580` to `0.1702469429/0.9427966974/0.8768917806`. Loss and cosine each improve in `45/70` layers, but bands 30--39 and 50--69 regress. This remains local fluctuation rather than retention recovery: relative to the Hard initialization, fixed loss is `64.9420%` worse and `0/70` layers improve either loss or cosine. All four ranks remain active, W&B continues writing, and the current-attempt formal error audit is empty. The supplemental builder remains alive after reading about `1.960 TB`; People's Speech dirty, LJSpeech, and VCTK processing are complete, but remaining archive reads continue and the final output root is still empty, so immutable fifth-segment completion remains unproven. Continue exact full coverage and preserve mandatory balanced all-depth Layer replay before Block if the representative hidden/public gates fail.
- 2026-08-13 Stage211A Hard 07:31 hourly status: the uninterrupted four-rank run reached live step `445,130/966,315` (`46.0647%`) with persisted DeepSpeed tag `step-444000`, advancing `4,870` optimizer steps in approximately one hour. Hard epoch 2 is at `123,025/322,105` steps (`38.1941%`), and original Easy/Medium/Hard/Long Stage-A coverage is `73.9365%` complete. The partial `440k-445.13k` current-domain window contains `513` ten-step records at mean mixer loss/cosine `0.097070/0.965271`, with zero hidden missing count and zero frame delta. The latest immutable sentinel remains step 440,000; step 450,000 is not yet present, so no newer fixed-retention or public WER/CER conclusion is available. All four ranks remain active or synchronizing, W&B continues writing, the current-attempt formal error audit is empty, `/media/usbhd` remains the intended `/dev/sdb2` read-only XFS mount, and root retains `770 GiB` free. The supplemental builder remains alive after reading about `1.990 TB`; its final output root is still empty, so immutable fifth-segment completion remains unproven while remaining source processing continues under idle-I/O priority. Continue exact Hard epoch 2 and natural-data enumeration unchanged.
- 2026-08-13 Stage211A Hard 08:34 status and step-450k fixed sentinel: the uninterrupted four-rank run reached live step `450,230/966,315` (`46.5925%`) with a complete DeepSpeed checkpoint at step 450,000, advancing `5,100` optimizer steps since the 07:31 snapshot. Hard epoch 2 is at `128,125/322,105` steps (`39.7774%`), and original Easy/Medium/Hard/Long Stage-A coverage is `74.1915%` complete. The complete `440k-450k` current-domain window remains stable at mean mixer loss/cosine `0.097225/0.965214`, with zero hidden missing count and zero frame delta. On the unchanged-provenance 256-sample Easy-ZH sentinel, fixed loss improves from step-440k `0.1736181718` to `0.1725401760` (`-0.6209%`); mean 70-layer loss/cosine/RMS ratio improves from `0.1702469429/0.9427966974/0.8768917806` to `0.1691184011/0.9431719146/0.8772397392`. Loss improves in `49/70` layers and cosine in `50/70`, though band 10--19 regresses. This remains local fluctuation rather than retention recovery: relative to the Hard initialization, fixed loss is `63.9178%` worse and `0/70` layers improve either loss or cosine. All four ranks remain active, W&B continues writing, and the current-attempt formal error audit is empty. The supplemental builder remains alive after reading about `2.018 TB`; remaining source processing continues under idle-I/O priority and the final output root is still empty, so immutable fifth-segment completion remains unproven. Continue exact full coverage and preserve mandatory balanced all-depth Layer replay before Block if the representative hidden/public gates fail.
- 2026-08-13 Stage211A Hard 09:33 hourly status: the uninterrupted four-rank run reached live step `455,060/966,315` (`47.0923%`) with persisted DeepSpeed tag `step-454000`, advancing `4,830` optimizer steps in approximately one hour. Hard epoch 2 is at `132,955/322,105` steps (`41.2769%`), and original Easy/Medium/Hard/Long Stage-A coverage is `74.4330%` complete. The partial `450k-455.06k` current-domain window contains `506` ten-step records at mean mixer loss/cosine `0.096751/0.965381`, with zero hidden missing count and zero frame delta. The latest immutable sentinel remains step 450,000; step 460,000 is not yet present, so no newer fixed-retention or public WER/CER conclusion is available. All four ranks remain active or synchronizing, W&B continues writing, the current-attempt formal error audit is empty, `/media/usbhd` remains the intended `/dev/sdb2` read-only XFS mount, and root retains `770 GiB` free. The supplemental builder remains alive after reading about `2.045 TB`; its final output root is still empty, so immutable fifth-segment completion remains unproven while remaining source processing continues under idle-I/O priority. Continue exact Hard epoch 2 and natural-data enumeration unchanged.
- 2026-08-13 Stage211A Hard 10:36 status and step-460k fixed sentinel: the uninterrupted four-rank run reached live step `460,210/966,315` (`47.6253%`) with a complete DeepSpeed checkpoint at step 460,000, advancing `5,150` optimizer steps since the 09:33 snapshot. Hard epoch 2 is at `138,105/322,105` steps (`42.8758%`), and original Easy/Medium/Hard/Long Stage-A coverage is `74.6905%` complete. The complete `450k-460k` current-domain window improves mean mixer loss/cosine to `0.096350/0.965523`, with zero hidden missing count and zero frame delta. On the unchanged-provenance 256-sample Easy-ZH sentinel, fixed loss regresses from step-450k `0.1725401760` to `0.1745248912` (`+1.1503%`); mean 70-layer loss/cosine/RMS ratio regresses from `0.1691184011/0.9431719146/0.8772397392` to `0.1710991007/0.9425522568/0.8755803839`. Only `13/70` layers improve loss and `10/70` improve cosine, while all seven ten-layer depth bands regress. Relative to the Hard initialization, fixed loss is `65.8034%` worse and `0/70` layers improve either loss or cosine. Stable current-Hard improvement, complete matching, and an empty runtime error audit continue to prove current-domain specialization with old-domain forgetting rather than a training failure. All four ranks remain active and W&B continues writing. The supplemental builder remains alive after reading about `2.075 TB`; its mutable progress reached `6,000,000` accepted rows and `23,623.236 h`, but the final output root is still empty, so this is not immutable fifth-segment scale evidence. Continue exact full coverage and preserve mandatory balanced all-depth Layer replay before Block if the representative hidden/public gates fail.
- 2026-08-13 Stage211A Hard 11:35 hourly status: the uninterrupted four-rank run reached live step `465,100/966,315` (`48.1313%`) with persisted DeepSpeed tag `step-464000`, advancing `4,890` optimizer steps in approximately one hour. Hard epoch 2 is at `142,995/322,105` steps (`44.3939%`), and original Easy/Medium/Hard/Long Stage-A coverage is `74.9350%` complete. The partial `460k-465.1k` current-domain window contains `510` ten-step records at mean mixer loss/cosine `0.096602/0.965436`, with zero hidden missing count and zero frame delta. The latest immutable sentinel remains step 460,000; step 470,000 is not yet present, so no newer fixed-retention or public WER/CER conclusion is available. All four ranks remain active, W&B continues writing, the current-attempt formal error audit is empty, `/media/usbhd` remains the intended `/dev/sdb2` read-only XFS mount, and root retains `770 GiB` free. The supplemental builder remains alive after reading about `2.105 TB`; its final output root is still empty, so immutable fifth-segment completion remains unproven while remaining source processing continues under idle-I/O priority. Continue exact Hard epoch 2 and natural-data enumeration unchanged.
- 2026-08-13 Stage211A Hard 12:38 status and step-470k fixed sentinel: the uninterrupted four-rank run reached live step `470,210/966,315` (`48.6601%`) with a complete DeepSpeed checkpoint at step 470,000, advancing `5,110` optimizer steps since the 11:35 snapshot. Hard epoch 2 is at `148,105/322,105` steps (`45.9803%`), and original Easy/Medium/Hard/Long Stage-A coverage is `75.1905%` complete. The complete `460k-470k` current-domain window improves mean mixer loss/cosine to `0.095954/0.965670`, with zero hidden missing count and zero frame delta. On the unchanged-provenance 256-sample Easy-ZH sentinel, fixed loss regresses from step-460k `0.1745248912` to `0.1759219533` (`+0.8005%`); mean 70-layer loss/cosine/RMS ratio regresses from `0.1710991007/0.9425522568/0.8755803839` to `0.1725128847/0.9421608999/0.8738590368`. Only `14/70` layers improve loss and `13/70` improve cosine, while all seven ten-layer depth bands regress. Relative to the Hard initialization, fixed loss is `67.1306%` worse and `0/70` layers improve either loss or cosine. Stable current-Hard improvement, complete matching, and an empty runtime error audit continue to prove current-domain specialization with old-domain forgetting rather than a training failure. All four ranks remain active and W&B continues writing. The supplemental builder remains alive after reading about `2.137 TB`; its final output root is still empty, so immutable fifth-segment completion remains unproven while remaining source processing continues under idle-I/O priority. Continue exact full coverage and preserve mandatory balanced all-depth Layer replay before Block if the representative hidden/public gates fail.
- 2026-08-13 Stage211A Hard 13:37 hourly status: the uninterrupted four-rank run reached live step `475,030/966,315` (`49.1589%`) with persisted DeepSpeed tag `step-474000`, advancing `4,820` optimizer steps in approximately one hour. Hard epoch 2 is at `152,925/322,105` steps (`47.4768%`), and original Easy/Medium/Hard/Long Stage-A coverage is `75.4315%` complete. The partial `470k-475.03k` current-domain window contains `503` ten-step records at mean mixer loss/cosine `0.095794/0.965725`, with zero hidden missing count and zero frame delta. The latest immutable sentinel remains step 470,000; step 480,000 is not yet present, so no newer fixed-retention or public WER/CER conclusion is available. All four ranks remain active, W&B continues writing, the current-attempt formal error audit is empty, `/media/usbhd` remains the intended `/dev/sdb2` read-only XFS mount, and root retains `770 GiB` free. The supplemental builder remains alive after reading about `2.164 TB`; its final output root is still empty, so immutable fifth-segment completion remains unproven while remaining source processing continues under idle-I/O priority. Continue exact Hard epoch 2 and natural-data enumeration unchanged.
- 2026-08-13 Stage211A Hard 14:40 status and step-480k fixed sentinel: the uninterrupted four-rank run reached live step `480,200/966,315` (`49.6939%`) with a complete DeepSpeed checkpoint at step 480,000, advancing `5,170` optimizer steps since the 13:37 snapshot. Hard epoch 2 is at `158,095/322,105` steps (`49.0818%`), and original Easy/Medium/Hard/Long Stage-A coverage is `75.6900%` complete. The complete `470k-480k` current-domain window remains stable at mean mixer loss/cosine `0.095917/0.965680`, with zero hidden missing count and zero frame delta. On the unchanged-provenance 256-sample Easy-ZH sentinel, fixed loss improves only from step-470k `0.1759219533` to `0.1757501017` (`-0.0977%`); mean 70-layer loss/cosine/RMS ratio changes from `0.1725128847/0.9421608999/0.8738590368` to `0.1723152081/0.9422218666/0.8740619486`. Loss improves in `38/70` layers and cosine in `37/70`, while bands 0--19 and 30--39 regress. This is effectively a forgetting plateau: relative to the Hard initialization, fixed loss remains `66.9674%` worse and `0/70` layers improve either loss or cosine. All four ranks remain active, W&B continues writing, and the current-attempt formal error audit is empty. The supplemental builder remains alive after reading about `2.196 TB`; its final output root is still empty, so immutable fifth-segment completion remains unproven while remaining source processing continues under idle-I/O priority. Continue exact full coverage and preserve mandatory balanced all-depth Layer replay before Block if the representative hidden/public gates fail.
- 2026-08-13 Stage211 base-natural supplemental immutable completion: the low-priority builder exited normally and atomically published a `complete=true`, `training_ready=true` component inventory with SHA-256 `255896bdf98981a6e08f92fc0ffba3f41e7c36b01563cd71cf2fe336eee79513`. It contains `6,434,286` source-identity-deduplicated English rows and `25,419.4279425 h` across LJSpeech `9,937/18.206697 h`, MLS English `853,970/3,525.953318 h`, People's Speech clean `1,501,271/5,894.746822 h`, People's Speech dirty `4,026,820/15,938.586370 h`, and VCTK `42,288/41.934735 h`; `1,450,078` duplicate dirty rows were rejected. Its `4,176` archive records bind hashes for every accepted storage archive, the supplemental source set is disjoint from the immutable Stage179 source set, known LibriSpeech/GigaSpeech repackages are excluded, and the fixed 256-row hidden evaluation binding remains SHA-256 `754d73f4b0bc4d1da9a8d10423a86960bc46f9bb796a25a8d9d910d0dc950977`. The production profile validator reread and SHA-verified all `137` train parts and emitted receipt SHA-256 `a1f9d2d8fcc264c83ba0bfb8a95cf9d7af98718ab5a0f07600f65db22067b816`: exactly `103,129` steps per epoch, `309,387` steps for three epochs, `1,582` deterministic tail-padding samples per epoch, `19,302,858` natural row exposures, and `19,307,604` executed sample exposures. This proves the base-natural component only, not the final fifth segment: strict Stage211 binds `stage211_supplemental_combined_v2`, which still requires normalized-PCM public-overlap audit, social-VAD materialization/filtering, archived-social exclusion, USB coverage binding, combined inventory, and its own profile receipt. The already-running hourly dependency chain will construct those artifacts without signaling or changing active Hard training.
- 2026-08-13 Stage211A Hard 16:40 status, step-490k sentinel, and supplemental handoff: the uninterrupted four-rank run reached live step `490,100/966,315` (`50.7185%`) with a complete DeepSpeed checkpoint at step 490,000, advancing `9,900` optimizer steps since the 14:40 snapshot. Hard epoch 2 is at `167,995/322,105` steps (`52.1554%`), and original Easy/Medium/Hard/Long Stage-A coverage is `76.1849%` complete. The complete `480k-490k` current-domain window remains stable at mean mixer loss/cosine `0.096072/0.965630`, with zero hidden missing count and zero frame delta. On the unchanged-provenance Easy-ZH sentinel, fixed loss improves only from step-480k `0.1757501017` to `0.1755987568` (`-0.0861%`); mean 70-layer loss/cosine/RMS ratio changes from `0.1723152081/0.9422218666/0.8740619486` to `0.1721682526/0.9422689018/0.8743391429`. Loss improves in `39/70` layers and cosine in `40/70`, but bands 0--9 and 50--69 regress; relative to Hard initialization, fixed loss remains `66.8236%` worse and `0/70` layers improve either loss or cosine. All four ranks remain active, W&B continues writing, and the current-attempt formal error audit is empty. The base-natural receipt was independently replayed unchanged, and the automatic combined-supplemental chain has now handed off to the low-priority full social-audio audit: `audit_stage211_social_vad_sources.py --probe-audio --hash-media` is actively running at `nice 19`, while boundary, materialization, public-overlap, archived-social, and combined-v2 jobs remain fail-closed on their dependencies. Continue exact Hard epoch 2 and do not duplicate or reprioritize the automatic chain.
- 2026-08-13 Stage211A Hard 18:42 status, step-500k sentinel, and social-source audit completion: the uninterrupted four-rank run reached live step `500,260/966,315` (`51.7699%`) with complete DeepSpeed and exported checkpoints at step 500,000. Hard epoch 2 is at `178,155/322,105` steps (`55.3096%`), and original Easy/Medium/Hard/Long Stage-A coverage is `76.6929%` complete. The complete `490k-500k` current-Hard window improves mean mixer loss/cosine to `0.094852/0.966063`, with zero hidden missing count and zero frame delta. On the unchanged-provenance 256-sample Easy-ZH sentinel, fixed loss regresses from step-490k `0.1755987568` to `0.1773305752` (`+0.9862%`); mean 70-layer loss/cosine/RMS ratio regresses from `0.1721682526/0.9422689018/0.8743391429` to `0.1738798411/0.9417763162/0.8721079763`. Only `18/70` layers improve loss and `16/70` improve cosine; bands 10--69 regress and only band 0--9 improves by `0.0729%`. Relative to the bound Hard initialization, fixed loss is `68.4688%` worse and `0/70` layers improve either loss or cosine. This strengthens the current-domain-specialization/old-domain-forgetting diagnosis while the empty current-attempt error audit, complete online matching, and stable Hard loss exclude a runtime alignment failure. The full social-source audit also exited normally and published SHA-256 `8ad53ebb747cb1ea63373960747dc3b3e66e9680c7e3c839c39b9e28a6c0f3b9`: `8,036` canonical hashed and decode-probed audio files, `91,714,921,022` bytes, `1,931.604748 h`, zero probe errors, and `119` exact-content duplicate groups. It remains explicitly `training_ready=false`/`source_inventory_only`; deterministic VAD boundaries, segment materialization, public-overlap filtering, combined-v2 inventory, and final profile receipt remain mandatory. Their existing hourly dependency chain remains fail-closed and will wake naturally; do not count source hours as admitted exposure or enter Block before the complete Layer data, balanced retention correction when required, and public WER/CER gate.
- 2026-08-13 Stage211A Hard 19:43 hourly status and social-boundary handoff: the uninterrupted four-rank run reached live step `505,130/966,315` (`52.2738%`) with persisted DeepSpeed tag `step-504000`, advancing `4,870` optimizer steps since the 18:42 snapshot. Hard epoch 2 is at `183,025/322,105` steps (`56.8215%`), and original Easy/Medium/Hard/Long Stage-A coverage is `76.9364%` complete. The partial `500k-505.13k` current-Hard window contains `513` ten-step records at mean mixer loss/cosine/RMS ratio `0.095581/0.965807/0.997501`, with zero hidden missing count and zero frame delta. Step 510,000 has not been reached, so step 500,000 remains the latest immutable retention sentinel and there is no new public WER/CER evidence. All four ranks and W&B remain active, the current-attempt error audit is empty, and `/media/usbhd` remains the intended read-only `/dev/sdb2` mount. The fail-closed supplemental chain has naturally started CPU FSMN-VAD boundary generation and atomically published `473/8,036` per-source receipts so far; no aggregate boundary inventory is yet admissible. The archived-social audit separately published receipt SHA-256 `02b89fe2d46acdfef5ba0167ff88ba37aaf1c22828b5e57ef0d680e4945effb0`: all `795/795` archive audio members are exact byte duplicates of already inventoried social files, leaving zero unique members, so the archive is excluded rather than counted twice. Continue Hard unchanged and allow boundary/materialization/filtering/combined-v2 jobs to proceed in dependency order.
- 2026-08-13 Stage211A Hard 20:46 status and step-510k fixed sentinel: the uninterrupted four-rank run reached live step `510,180/966,315` (`52.7964%`) with complete DeepSpeed and exported checkpoints at step 510,000. Hard epoch 2 is at `188,075/322,105` steps (`58.3893%`), and original Easy/Medium/Hard/Long Stage-A coverage is `77.1889%` complete. The complete `500k-510k` current-Hard window remains stable at mean mixer loss/cosine `0.095705/0.965762`, with zero hidden missing count and zero frame delta. On the unchanged-provenance Easy-ZH sentinel, fixed loss locally improves from step-500k `0.1773305752` to `0.1766688755` (`-0.3731%`); mean 70-layer loss/cosine/RMS ratio improves from `0.1738798411/0.9417763162/0.8721079763` to `0.1732071602/0.9419596059/0.8735566264`. Loss improves in `40/70` layers and cosine in `42/70`; bands 10--49 and 60--69 improve, while 0--9 and 50--59 regress. This local recovery does not satisfy retention: relative to the bound Hard initialization, fixed loss remains `67.8402%` worse and `0/70` layers improve either loss or cosine. Four ranks, online matching, W&B, and checkpoints remain healthy with an empty current-attempt error audit. The CPU VAD boundary pipeline reached at least `850/8,036` immutable per-source receipts and continues under idle-I/O priority; aggregate boundary admission remains pending. Continue exact Hard coverage without entering Block or reporting hidden metrics as WER/CER.
- 2026-08-13 Stage211A Hard 21:45 hourly status: the uninterrupted four-rank run reached live step `514,970/966,315` (`53.2921%`) with persisted DeepSpeed tag `step-514000`, advancing `4,790` optimizer steps since the step-510k snapshot. Hard epoch 2 is at `192,865/322,105` steps (`59.8764%`), and original Easy/Medium/Hard/Long Stage-A coverage is `77.4284%` complete. The partial `510k-514.97k` current-Hard window contains `497` ten-step records at mean mixer loss/cosine/RMS ratio `0.094321/0.966252/0.997339`, with zero hidden missing count and zero frame delta. The latest immutable retention sentinel remains step 510,000 and no new public WER/CER evidence exists. All four ranks, W&B, checkpoints, and the intended `/dev/sdb2` read-only mount remain healthy; the current-attempt error audit is empty. The CPU VAD boundary pipeline accelerated to `1,436/8,036` immutable per-source receipts while preserving dependency ordering; aggregate boundary admission and all downstream materialization/overlap/combined-v2 receipts remain pending. Continue exact Hard epoch 2 unchanged.
- 2026-08-13 Stage211A Hard 22:49 status and step-520k fixed sentinel: the uninterrupted four-rank run reached live step `520,070/966,315` (`53.8199%`) with complete DeepSpeed and exported checkpoints at step 520,000. Hard epoch 2 is at `197,965/322,105` steps (`61.4598%`), and original Easy/Medium/Hard/Long Stage-A coverage is `77.6834%` complete. The complete `510k-520k` current-Hard window remains stable at mean mixer loss/cosine `0.094898/0.966049`, with zero hidden missing count and zero frame delta. On the unchanged-provenance Easy-ZH sentinel, fixed loss regresses from step-510k `0.1766688755` to `0.1774347650` (`+0.4335%`); mean 70-layer loss/cosine/RMS ratio regresses from `0.1732071602/0.9419596059/0.8735566264` to `0.1739413919/0.9417482816/0.8725142363`. Only `21/70` layers improve loss and `20/70` improve cosine; bands 0--59 regress and only 60--69 improves. Relative to the bound Hard initialization, fixed loss is `68.5678%` worse and `0/70` layers improve either loss or cosine. Stable current-domain optimization, complete matching, complete checkpoints, and an empty current-attempt error audit continue to identify domain forgetting rather than a runtime failure. The social VAD pipeline has produced at least `1,882/8,036` immutable source-boundary receipts but no aggregate training-ready inventory. Continue exact Hard coverage and retain the mandatory balanced all-depth Layer replay/public gate before Block.
- 2026-08-13 Stage211A Hard 23:48 hourly status: the uninterrupted four-rank run reached live step `524,870/966,315` (`54.3167%`) with persisted DeepSpeed tag `step-524000`, advancing `4,800` optimizer steps since the step-520k snapshot. Hard epoch 2 is at `202,765/322,105` steps (`62.9500%`), and original Easy/Medium/Hard/Long Stage-A coverage is `77.9234%` complete. The partial `520k-524.87k` current-Hard window contains `487` ten-step records at mean mixer loss/cosine/RMS ratio `0.094797/0.966089/0.997346`, with zero hidden missing count and zero frame delta. Step 520,000 remains the latest immutable retention sentinel and no new public WER/CER evidence exists. All four ranks, W&B, checkpoints, disk headroom, and the intended read-only USB mount remain healthy with an empty current-attempt error audit. CPU VAD boundary production reached `2,464/8,036` immutable per-source receipts; the aggregate training-ready boundary inventory and downstream artifacts remain pending. Continue exact Hard epoch 2 unchanged.
- 2026-08-14 Stage211A Hard 00:52 status and step-530k fixed sentinel: the uninterrupted four-rank run reached live step `530,080/966,315` (`54.8558%`) with complete DeepSpeed and exported checkpoints at step 530,000. Hard epoch 2 is at `207,975/322,105` steps (`64.5675%`), and original Easy/Medium/Hard/Long Stage-A coverage is `78.1839%` complete. The complete `520k-530k` current-Hard window improves mean mixer loss/cosine to `0.094633/0.966145`, with zero hidden missing count and zero frame delta. On the unchanged-provenance Easy-ZH sentinel, fixed loss regresses from step-520k `0.1774347650` to `0.1778528069` (`+0.2356%`); mean 70-layer loss/cosine/RMS ratio regresses from `0.1739413919/0.9417482816/0.8725142363` to `0.1743396497/0.9416781536/0.8710602832`. Loss improves in `30/70` layers and cosine in `35/70`; bands 40--59 improve while the other five bands regress. Relative to the bound Hard initialization, fixed loss is `68.9650%` worse and `0/70` layers improve either loss or cosine. This extends the repeated old-domain-forgetting evidence despite stable current-domain optimization, complete matching, complete checkpoints, and an empty current-attempt error audit. The VAD pipeline had reached at least `3,164/8,036` immutable source-boundary receipts before this checkpoint. Continue exact Hard coverage and require balanced all-depth Layer replay plus public EN-WER/ZH-CER gate before Block.
- 2026-08-14 Stage211A Hard 01:51 hourly status: the uninterrupted four-rank run reached live step `534,900/966,315` (`55.3546%`) with persisted DeepSpeed tag `step-534000`, advancing `4,820` optimizer steps since the step-530k snapshot. Hard epoch 2 is at `212,795/322,105` steps (`66.0639%`), and original Easy/Medium/Hard/Long Stage-A coverage is `78.4249%` complete. The partial `530k-534.9k` current-Hard window contains `490` ten-step records at mean mixer loss/cosine/RMS ratio `0.094842/0.966067/0.997318`, with zero hidden missing count and zero frame delta. Step 530,000 remains the latest immutable retention sentinel and no new public WER/CER evidence exists. All four ranks, W&B, checkpoints, storage, and mount health remain normal with an empty current-attempt error audit. CPU VAD boundary generation reached `3,814/8,036` immutable source receipts (`47.46%`); no aggregate training-ready boundary inventory exists yet. Continue exact Hard epoch 2 unchanged.
- 2026-08-14 Stage211A Hard 02:55 status and step-540k fixed sentinel: the uninterrupted four-rank run reached live step `540,080/966,315` (`55.8907%`) with complete DeepSpeed and exported checkpoints at step 540,000. Hard epoch 2 is at `217,975/322,105` steps (`67.6720%`), and original Easy/Medium/Hard/Long Stage-A coverage is `78.6838%` complete. The complete `530k-540k` current-Hard window remains stable at mean mixer loss/cosine `0.094709/0.966115`, with zero hidden missing count and zero frame delta. On the unchanged-provenance Easy-ZH sentinel, fixed loss regresses from step-530k `0.1778528069` to `0.1788909619` (`+0.5837%`); mean 70-layer loss/cosine/RMS ratio regresses from `0.1743396497/0.9416781536/0.8710602832` to `0.1753985842/0.9413792491/0.8701339627`. Only `20/70` layers improve loss and `22/70` improve cosine; only band 0--9 improves while bands 10--69 regress. Relative to the bound Hard initialization, fixed loss is `69.9513%` worse and `0/70` layers improve either loss or cosine. Four healthy ranks, complete matching/checkpoints, stable current-domain loss, and an empty current-attempt error audit continue to prove specialization/forgetting rather than runtime failure. VAD boundary production had reached at least `4,500/8,036` immutable source receipts (`56.00%`) before this checkpoint, but aggregate admission remains pending. Continue exact Hard and preserve the mandatory balanced Layer replay/public gate before Block.
- 2026-08-14 Stage211A Hard 03:54 hourly status: the uninterrupted four-rank run reached live step `544,910/966,315` (`56.3905%`) with persisted DeepSpeed tag `step-544000`, advancing `4,830` optimizer steps since the step-540k snapshot. Hard epoch 2 is at `222,805/322,105` steps (`69.1715%`), and original Easy/Medium/Hard/Long Stage-A coverage is `78.9253%` complete. The partial `540k-544.91k` current-Hard window contains `491` ten-step records at mean mixer loss/cosine/RMS ratio `0.094493/0.966196/0.997091`, with zero hidden missing count and zero frame delta. Step 540,000 remains the latest immutable retention sentinel and no new public WER/CER evidence exists. All four ranks, W&B, checkpoints, root/USB headroom, and the intended read-only mount remain healthy with an empty current-attempt error audit. CPU VAD boundary production reached `4,902/8,036` immutable source receipts (`61.00%`); aggregate boundary admission and downstream materialization remain pending. Continue exact Hard epoch 2 unchanged.
- 2026-08-14 Stage211A Hard 04:58 status and step-550k fixed sentinel: the uninterrupted four-rank run reached live step `550,090/966,315` (`56.9266%`) with complete DeepSpeed and exported checkpoints at step 550,000. Hard epoch 2 is at `227,985/322,105` steps (`70.7797%`), and original Easy/Medium/Hard/Long Stage-A coverage is `79.1843%` complete. The complete `540k-550k` current-Hard window improves mean mixer loss/cosine to `0.094069/0.966347`, with zero hidden missing count and zero frame delta. On the unchanged-provenance Easy-ZH sentinel, fixed loss regresses from step-540k `0.1788909619` to `0.1791610704` (`+0.1510%`); mean 70-layer loss/cosine/RMS ratio changes from `0.1753985842/0.9413792491/0.8701339627` to `0.1756049022/0.9412925883/0.8703616714`. Loss improves in `37/70` layers and cosine in `36/70`; bands 30--69 improve but 0--29 regress enough to worsen the aggregate. Relative to the bound Hard initialization, fixed loss is `70.2079%` worse and `0/70` layers improve either loss or cosine. Improving Hard-domain alignment with complete matching/checkpoints and no runtime error, alongside monotonic old-domain degradation, is direct evidence of sequential-curriculum catastrophic forgetting rather than failed optimization. VAD production had reached at least `5,454/8,036` source-boundary receipts (`67.87%`) before this checkpoint. Continue exact Hard and require balanced all-depth Layer correction before Block.
- 2026-08-14 Stage211A Hard 05:56 hourly status: the uninterrupted four-rank run reached live step `554,970/966,315` (`57.4316%`) with persisted DeepSpeed tag `step-554000`, advancing `4,880` optimizer steps since the step-550k snapshot. Hard epoch 2 is at `232,865/322,105` steps (`72.2947%`), and original Easy/Medium/Hard/Long Stage-A coverage is `79.4283%` complete. The partial `550k-554.97k` current-Hard window contains `497` ten-step records at mean mixer loss/cosine/RMS ratio `0.094269/0.966278/0.997356`, with zero hidden missing count and zero frame delta. Step 550,000 remains the latest immutable retention sentinel and no new public WER/CER evidence exists. All four ranks, W&B, checkpoints, storage, and mount health remain normal with an empty current-attempt error audit. CPU VAD boundary generation reached `6,268/8,036` immutable source receipts (`77.999%`); aggregate boundary admission and downstream materialization remain pending. Continue exact Hard epoch 2 unchanged.
- 2026-08-14 Stage211A Hard 06:59 status and step-560k fixed sentinel: the uninterrupted four-rank run reached live step `560,130/966,315` (`57.9656%`) with complete DeepSpeed and exported checkpoints at step 560,000. Hard epoch 2 is at `238,025/322,105` steps (`73.8967%`), and original Easy/Medium/Hard/Long Stage-A coverage is `79.6863%` complete. The complete `550k-560k` current-Hard window remains stable at mean mixer loss/cosine `0.094103/0.966336`, with zero hidden missing count and zero frame delta. On the unchanged-provenance Easy-ZH sentinel, fixed loss regresses from step-550k `0.1791610704` to `0.1803963942` (`+0.6895%`); mean 70-layer loss/cosine/RMS ratio regresses from `0.1756049022/0.9412925883/0.8703616714` to `0.1768609952/0.9409276958/0.8687984795`. Only `17/70` layers improve either loss or cosine; only band 0--9 improves while bands 10--69 regress. Relative to the bound Hard initialization, fixed loss is `71.3815%` worse and `0/70` layers improve either loss or cosine. The retention gate therefore fails decisively even though four ranks, current-domain loss, matching, checkpoints, W&B, and the current-attempt error audit all remain healthy. The exact three-epoch Hard run must continue without early stopping, followed by balanced all-depth Layer correction before Block. VAD source-boundary production had reached at least `6,967/8,036` receipts (`86.70%`) before this checkpoint; aggregate training readiness remains unproven.
- 2026-08-14 Stage211A Hard 07:58 hourly status: the uninterrupted four-rank run reached live step `564,970/966,315` (`58.4664%`) with persisted DeepSpeed tag `step-564000`, advancing `4,840` optimizer steps since the step-560k snapshot. Hard epoch 2 is at `242,865/322,105` steps (`75.3993%`), and original Easy/Medium/Hard/Long Stage-A coverage is `79.9283%` complete. The partial `560k-564.97k` current-Hard window contains `497` ten-step records at mean mixer loss/cosine/RMS ratio `0.095274/0.965917/0.996975`, with zero hidden missing count and zero frame delta. Step 560,000 remains the latest immutable retention sentinel and no new public WER/CER evidence exists. All four ranks, W&B, checkpoints, storage, and mount health remain normal with an empty current-attempt error audit. CPU VAD boundary generation reached `7,339/8,036` immutable source receipts (`91.3265%`), but no aggregate boundary inventory has been atomically published; the materializer therefore correctly remains fail-closed. Continue exact Hard epoch 2 and the existing dependency chain unchanged.
- 2026-08-14 Stage211A Hard 09:03 status and step-570k fixed sentinel: the uninterrupted four-rank run reached live step `570,290/966,315` (`59.0170%`) with complete DeepSpeed and exported checkpoints at step 570,000. Hard epoch 2 is at `248,185/322,105` steps (`77.0510%`), and original Easy/Medium/Hard/Long Stage-A coverage is `80.1943%` complete. The complete `560k-570k` current-Hard window remains stable at mean mixer loss/cosine `0.094800/0.966085`, with zero hidden missing count and zero frame delta. On the unchanged-provenance Easy-ZH sentinel, fixed loss regresses from step-560k `0.1803963942` to `0.1806341843` (`+0.1318%`); mean 70-layer loss/cosine/RMS ratio changes from `0.1768609952/0.9409276958/0.8687984795` to `0.1771078583/0.9408360818/0.8688717392`. Loss improves in `34/70` layers and cosine in `33/70`; bands 10--39 improve while the other four regress. Relative to the bound Hard initialization, fixed loss is `71.6074%` worse and `0/70` layers improve either loss or cosine. The retention failure remains unambiguous despite healthy current-domain training and a clean runtime audit. VAD boundary processing reached `7,983/8,036` immutable per-source receipts (`99.3405%`), but the final long sources remain in progress and no aggregate inventory exists; materialization correctly remains fail-closed. Continue exact Hard and do not count the boundary stage as complete until atomic finalization.
- 2026-08-14 Stage211A Hard 10:02 hourly status and social-boundary completion: the uninterrupted four-rank run reached live step `575,200/966,315` (`59.5251%`) with persisted DeepSpeed tag `step-574000`, advancing `4,910` optimizer steps since the step-570k snapshot. Hard epoch 2 is at `253,095/322,105` steps (`78.5753%`), and original Easy/Medium/Hard/Long Stage-A coverage is `80.4398%` complete. The partial `570k-575.2k` current-Hard window contains `520` ten-step records at mean mixer loss/cosine/RMS ratio `0.093745/0.966464/0.997395`, with zero hidden missing count and zero frame delta. Step 570,000 remains the latest immutable retention sentinel and no new public WER/CER evidence exists. All four ranks, W&B, checkpoints, and the intended mount remain healthy with an empty current-attempt error audit; root free space is `762 GiB`. The social boundary builder exited normally and atomically published SHA-256 `23a32ff43936edb6b6dc14b8b9e5ca4d0178e2c2d12497f48771eadf205e85b7`: `8,036` sources accounted, `7,903` admitted sources, `14` no-speech sources, `119` exact duplicates, `395,125` segments, and `1,790.823792 h`, bound to source-inventory SHA-256 `8ad53ebb747cb1ea63373960747dc3b3e66e9680c7e3c839c39b9e28a6c0f3b9`. It remains explicitly `training_ready=false`/`vad_boundaries_only`. The materializer naturally started and reached `644/8,036` source receipts with about `8.38 GB` written; materialized validation, segment/public overlap filtering, combined-v2 inventory, and final profile receipt remain mandatory before any social exposure is counted. Continue both active jobs unchanged.
- 2026-08-14 Stage211A Hard 11:02 status and step-580k fixed sentinel: the uninterrupted four-rank run reached live step `580,120/966,315` (`60.0343%`) with complete DeepSpeed and exported checkpoints at step 580,000. Hard epoch 2 is at `258,015/322,105` steps (`80.1028%`), and original Easy/Medium/Hard/Long Stage-A coverage is `80.6858%` complete. The complete `570k-580k` current-Hard window improves mean mixer loss/cosine to `0.093581/0.966524`, with zero hidden missing count and zero frame delta. On the unchanged-provenance Easy-ZH sentinel, fixed loss locally improves from step-570k `0.1806341843` to `0.1802640844` (`-0.2049%`); mean 70-layer loss/cosine/RMS ratio improves from `0.1771078583/0.9408360818/0.8688717392` to `0.1767334150/0.9409256927/0.8700840381`. Loss improves in `42/70` layers and cosine in `39/70`; bands 0--9 and 50--69 improve while 10--49 regress slightly. This is only local fluctuation: relative to the bound Hard initialization, fixed loss remains `71.2558%` worse and `0/70` layers improve either loss or cosine. All runtime health evidence remains clean. Social materialization reached `1,395/8,036` source receipts (`17.3594%`) and about `21.17 GB`; root retains `750 GiB`, so current growth remains within headroom. The inventory is not training-ready until complete materialization validation and downstream overlap/combined receipts finish. Continue exact Hard and materialization unchanged.
- 2026-08-14 Stage211A Hard 12:01 hourly status: the uninterrupted four-rank run reached live step `585,070/966,315` (`60.5465%`) with persisted DeepSpeed tag `step-584000`, advancing `4,950` optimizer steps since the step-580k snapshot. Hard epoch 2 is at `262,965/322,105` steps (`81.6395%`), and original Easy/Medium/Hard/Long Stage-A coverage is `80.9333%` complete. The partial `580k-585.07k` current-Hard window contains `507` ten-step records and improves mean mixer loss/cosine/RMS ratio to `0.092866/0.966777/0.997014`, with zero hidden missing count and zero frame delta. Step 580,000 remains the latest immutable retention sentinel and no new public WER/CER evidence exists. All four ranks, W&B, checkpoints, and the read-only USB mount remain healthy with an empty current-attempt error audit. Social materialization reached `1,996/8,036` source receipts (`24.8382%`) and about `34.30 GB`; root retains `738 GiB`, so current growth remains within adequate headroom. The downstream chain remains correctly fail-closed until atomic materialization completion. Continue both active jobs unchanged.
- 2026-08-14 Stage211A Hard 13:03 status and step-590k fixed sentinel: the uninterrupted four-rank run reached live step `590,170/966,315` (`61.0743%`) with complete DeepSpeed and exported checkpoints at step 590,000. Hard epoch 2 is at `268,065/322,105` steps (`83.2229%`), and original Easy/Medium/Hard/Long Stage-A coverage is `81.1882%` complete. The complete `580k-590k` current-Hard window remains strong at mean mixer loss/cosine `0.092860/0.966781`, with zero hidden missing count and zero frame delta. On the unchanged-provenance Easy-ZH sentinel, fixed loss regresses from step-580k `0.1802640844` to `0.1815134446` (`+0.6931%`); mean 70-layer loss/cosine/RMS ratio regresses from `0.1767334150/0.9409256927/0.8700840381` to `0.1779174370/0.9406248806/0.8674750189`. Only `15/70` layers improve loss and `16/70` improve cosine; only band 50--59 improves marginally while the other six regress. Relative to the bound Hard initialization, fixed loss is `72.4427%` worse and `0/70` layers improve either loss or cosine. This is further direct evidence of sequential-domain forgetting despite healthy Hard optimization and a clean runtime path. Social materialization reached `2,940/8,036` source receipts (`36.5854%`) and about `48.08 GB`; root retains `725 GiB`, so growth remains within headroom. Continue exact Hard and materialization unchanged, followed by mandatory balanced all-depth Layer replay before Block.
- 2026-08-14 Stage211A Hard 14:02 hourly status: the uninterrupted four-rank run reached live step `595,050/966,315` (`61.5793%`) with persisted DeepSpeed tag `step-594000`, advancing `4,880` optimizer steps since the step-590k snapshot. Hard epoch 2 is at `272,945/322,105` steps (`84.7379%`), and original Easy/Medium/Hard/Long Stage-A coverage is `81.4322%` complete. The partial `590k-595.05k` current-Hard window contains `505` ten-step records and improves mean mixer loss/cosine/RMS ratio to `0.092734/0.966827/0.996437`, with zero hidden missing count and zero frame delta. Step 590,000 remains the latest immutable retention sentinel and no new public WER/CER evidence exists. All four ranks, W&B, checkpoints, and the read-only USB mount remain healthy with an empty current-attempt error audit. Social materialization reached `3,858/8,036` source receipts (`48.0090%`) and about `62.28 GB`; root retains `712 GiB`, so growth remains within adequate headroom. The downstream chain remains correctly fail-closed. Continue both active jobs unchanged.
- 2026-08-14 Stage211A Hard 15:06 status and step-600k fixed sentinel: the uninterrupted four-rank run reached live step `600,300/966,315` (`62.1226%`) with complete exported and DeepSpeed checkpoints at step 600,000. Hard epoch 2 is at `278,195/322,105` steps (`86.3678%`), and original Easy/Medium/Hard/Long Stage-A coverage is `81.6947%` complete. The complete `590k-600k` current-Hard window contains `1,000` ten-step records at mean mixer loss/cosine/RMS ratio `0.092889/0.966770/0.996855`, with zero hidden missing count and zero frame delta. On the unchanged-provenance Easy-ZH sentinel, fixed loss regresses from step-590k `0.1815134446` to `0.1837963950` (`+1.2577%`); mean 70-layer loss/cosine/RMS ratio changes from `0.1779174370/0.9406248806/0.8674750189` to `0.1801245252/0.9399496725/0.8660087274`. Only `14/70` layers improve loss and `13/70` improve cosine; only band 40--49 improves while the other six regress. Relative to the bound Hard initialization, fixed loss is `74.6116%` worse and `0/70` layers improve either loss or cosine. All four ranks, W&B, complete checkpoints, teacher matching, and the current-attempt runtime audit remain healthy, so this remains domain forgetting rather than a training failure. Social materialization reached `4,616/8,036` source receipts (`57.4415%`) and about `70 GB`; root retains about `700 GiB`. Continue exact Hard and materialization unchanged, then require the balanced all-depth Layer replay and full normalized public EN-WER/ZH-CER gate before Block.
- 2026-08-14 Stage211A Hard 16:03 hourly status: the uninterrupted four-rank run reached live step `605,030/966,315` (`62.6121%`) with persisted DeepSpeed tag `step-604000`, advancing `4,730` optimizer steps since the step-600k snapshot. Hard epoch 2 is at `282,925/322,105` steps (`87.8363%`), and original Easy/Medium/Hard/Long Stage-A coverage is `81.9312%` complete. The partial `600k-605.03k` current-Hard window contains `503` ten-step records at mean mixer loss/cosine/RMS ratio `0.092881/0.966772/0.997401`, with zero hidden missing count and zero frame delta. Step 600,000 remains the latest immutable retention sentinel and no new public WER/CER evidence exists. All four ranks, W&B, checkpoints, and the intended read-only USB mount remain healthy with an empty current-attempt error audit. Social materialization reached `5,374/8,036` source receipts (`66.8741%`) and `89.21 GB`; root retains `687 GiB`, so current growth remains within adequate headroom. Downstream materialized, overlap-filtered, combined-v2, and profile inventories remain correctly fail-closed. Continue both active jobs unchanged.
- 2026-08-14 Stage211A Hard 17:06 status and step-610k fixed sentinel: the uninterrupted four-rank run reached live step `610,210/966,315` (`63.1481%`) with complete exported and DeepSpeed checkpoints at step 610,000. Hard epoch 2 is at `288,105/322,105` steps (`89.4444%`), and original Easy/Medium/Hard/Long Stage-A coverage is `82.1902%` complete. The complete `600k-610k` current-Hard window contains `1,000` ten-step records at mean mixer loss/cosine/RMS ratio `0.092708/0.966835/0.996958`, with zero hidden missing count, zero frame delta, and an empty current-attempt error audit. On the unchanged-provenance Easy-ZH sentinel, fixed loss locally improves from step-600k `0.1837963950` to `0.1818323757` (`-1.0686%`); mean 70-layer loss/cosine/RMS ratio improves from `0.1801245252/0.9399496725/0.8660087274` to `0.1781697424/0.9405404210/0.8676180931`. Loss and cosine improve in `58/70` layers; all depth bands except 40--49 improve. This is only a local recovery: relative to the bound Hard initialization, fixed loss remains `72.7457%` worse and `0/70` layers improve either loss or cosine. Four-rank training, W&B, teacher matching, checkpoints, and storage remain healthy. Social materialization reached at least `6,372/8,036` source receipts (`79.2932%`) and `102.17 GB`; downstream admission remains pending. Continue exact Hard and materialization unchanged, followed by mandatory balanced all-depth Layer replay and full normalized public EN-WER/ZH-CER evaluation before Block.
- 2026-08-14 Stage211A Hard 18:04 hourly status: the uninterrupted four-rank run reached live step `615,000/966,315` (`63.6438%`) with persisted DeepSpeed tag `step-614000`, advancing `4,790` optimizer steps since the step-610k snapshot. Hard epoch 2 is at `292,895/322,105` steps (`90.9315%`), and original Easy/Medium/Hard/Long Stage-A coverage is `82.4297%` complete. The partial `610k-615k` current-Hard window contains `500` ten-step records at mean mixer loss/cosine/RMS ratio `0.093840/0.966435/0.997047`, with zero hidden missing count and zero frame delta. Step 610,000 remains the latest immutable retention sentinel and no new public WER/CER evidence exists. Four ranks, W&B, checkpoints, the read-only USB mount, and the current-attempt runtime audit remain healthy. Social materialization reached `7,095/8,036` source receipts (`88.2902%`) and `115.35 GB`; root retains `662 GiB`. Materialized validation and downstream overlap-filtered/combined-v2/profile artifacts remain correctly fail-closed until atomic completion. Continue both active jobs unchanged.
- 2026-08-14 Stage211A Hard 19:19 status, step-620k fixed sentinel, and social materialization completion: the uninterrupted four-rank run reached live step `620,370/966,315` (`64.1996%`) with complete exported and DeepSpeed checkpoints at step 620,000. Hard epoch 2 is at `298,265/322,105` steps (`92.5987%`), and original Easy/Medium/Hard/Long Stage-A coverage is `82.6982%` complete. The complete `610k-620k` current-Hard window contains `1,000` ten-step records at mean mixer loss/cosine/RMS ratio `0.093754/0.966470/0.997435`, with zero hidden missing count, zero frame delta, and an empty current-attempt error audit. On the unchanged-provenance Easy-ZH sentinel, fixed loss locally improves from step-610k `0.1818323757` to `0.1809200514` (`-0.5017%`); mean 70-layer loss/cosine/RMS ratio improves from `0.1781697424/0.9405404210/0.8676180931` to `0.1773190322/0.9408191009/0.8686894685`. Loss improves in `40/70` layers and cosine in `41/70`; bands 0--19 and 30--59 improve, while 20--29 and 60--69 regress slightly. Relative to the bound Hard initialization, fixed loss remains `71.8789%` worse and `0/70` layers improve either loss or cosine, so post-coverage retention correction remains mandatory. Social VAD materialization completed atomically with inventory SHA-256 `eb32645ab2075b1e751016742a50e4f1ac8b2176059499dce826d912f09aae5a`: all `8,036` source receipts are bound, with `7,903` materialized sources, `119` exact duplicates skipped, `14` no-speech rejects, `395,125` segments, and `1,790.823792 h`. Its bucket-manifest SHA-256 matches the recorded `4996274f5bd82c00def3fc6fbb737cc0c51485c240745e6a3b11609451bf7c56`. The inventory correctly remains `training_ready=false`/`materialized_pending_overlap_and_merge`; the existing hour-scale dependency waiter will start PCM/public-overlap filtering and combined-v2/profile construction after observing the completed materializer session. Continue exact Hard unchanged and do not count social exposure until all downstream receipts pass.
- 2026-08-14 Stage211A Hard 20:03 hourly status and social-overlap audit start: the uninterrupted four-rank run reached live step `624,960/966,315` (`64.6746%`) with persisted DeepSpeed tag `step-624000`, advancing `4,590` optimizer steps since the step-620k snapshot. Hard epoch 2 is at `302,855/322,105` steps (`94.0237%`), and original Easy/Medium/Hard/Long Stage-A coverage is `82.9277%` complete. The partial `620k-624.96k` current-Hard window contains `496` ten-step records at mean mixer loss/cosine/RMS ratio `0.094943/0.966046/0.997379`, with zero hidden missing count and zero frame delta. Step 620,000 remains the latest immutable retention sentinel and no new public WER/CER evidence exists. Four ranks, W&B, checkpoints, storage, mount state, and the current-attempt runtime audit remain healthy. The hour-scale dependency waiter observed completed social materialization at `19:47` and started the fail-closed post-materialization chain. It created normalized PCM indexes for all five public sets and is actively auditing the `6,434,286`-row base-natural inventory against public audio, reporting at least `4,000,000` indexed rows by this snapshot. Social filtering, combined-v2 construction, and profile admission have not started yet and their missing artifacts remain expected. Continue both active jobs unchanged.
- 2026-08-14 Stage211A Hard 21:05 status and step-630k fixed sentinel: the uninterrupted four-rank run reached live step `630,260/966,315` (`65.2230%`) with complete exported and DeepSpeed checkpoints at step 630,000. Hard epoch 2 is at `308,155/322,105` steps (`95.6691%`), and original Easy/Medium/Hard/Long Stage-A coverage is `83.1927%` complete. The complete `620k-630k` current-Hard window contains `1,000` ten-step records at mean mixer loss/cosine/RMS ratio `0.095216/0.965943/0.997724`, with zero hidden missing count, zero frame delta, and an empty current-attempt error audit. On the unchanged-provenance Easy-ZH sentinel, fixed loss locally improves from step-620k `0.1809200514` to `0.1800755787` (`-0.4668%`); mean 70-layer loss/cosine/RMS ratio improves from `0.1773190322/0.9408191009/0.8686894685` to `0.1764087824/0.9411307560/0.8695509435`. Loss improves in `51/70` layers and cosine in `52/70`; bands 10--69 improve while 0--9 regresses. Relative to the bound Hard initialization, fixed loss remains `71.0767%` worse and `0/70` layers improve either loss or cosine, so this local recovery does not remove the mandatory retention correction. All training health signals remain normal. The base-natural/public PCM audit completed its `6,434,286`-row location index and began content processing across `4,172` archives, reaching at least `57/4,172`; social filtering and combined admission remain fail-closed pending this exact-content audit. Continue both jobs unchanged.
- 2026-08-14 Stage211A Hard 22:01 hourly status: the uninterrupted four-rank run reached live step `634,950/966,315` (`65.7084%`) with persisted DeepSpeed tag `step-634000`, advancing `4,690` optimizer steps since the step-630k snapshot. Hard epoch 2 is at `312,845/322,105` steps (`97.1252%`) with `9,260` steps remaining, and original Easy/Medium/Hard/Long Stage-A coverage is `83.4272%` complete. The partial `630k-634.95k` current-Hard window contains `495` ten-step records at mean mixer loss/cosine/RMS ratio `0.094256/0.966274/0.997517`, with zero hidden missing count and zero frame delta. Step 630,000 remains the latest immutable retention sentinel and no new public WER/CER evidence exists. Four-rank training, W&B, checkpoints, storage, mount state, and the current-attempt error audit remain healthy. The exact-content base-natural/public PCM audit reached `113/4,172` archives (`2.7085%`) and continues under intentional low-priority CPU/I/O scheduling; social filtering and combined-v2 admission remain fail-closed. Continue both active jobs unchanged.
- 2026-08-14 Stage211A Hard 23:03 status and step-640k fixed sentinel: the uninterrupted four-rank run reached live step `640,220/966,315` (`66.2538%`) with complete exported and DeepSpeed checkpoints at step 640,000. Hard epoch 2 is at `318,115/322,105` steps (`98.7613%`) with `3,990` steps remaining, and original Easy/Medium/Hard/Long Stage-A coverage is `83.6906%` complete. The complete `630k-640k` current-Hard window contains `1,000` ten-step records at mean mixer loss/cosine/RMS ratio `0.093705/0.966479/0.997649`, with zero hidden missing count, zero frame delta, and an empty current-attempt error audit. On the unchanged-provenance Easy-ZH sentinel, fixed loss regresses from step-630k `0.1800755787` to `0.1820873714` (`+1.1172%`); mean 70-layer loss/cosine/RMS ratio regresses from `0.1764087824/0.9411307560/0.8695509435` to `0.1784076404/0.9405755761/0.8661038516`. Only `15/70` layers improve loss and `16/70` improve cosine, and every depth band regresses. Relative to the bound Hard initialization, fixed loss is `72.9879%` worse and `0/70` layers improve either loss or cosine. Stable Hard-domain optimization versus oscillating, severely degraded Easy-ZH retention remains direct evidence for the mandatory balanced replay after complete Layer coverage. All runtime health signals remain normal. The exact-content base/public audit reached at least `178/4,172` archives; downstream social filtering and combined-v2 admission remain fail-closed. Continue both jobs unchanged.
- 2026-08-14 Stage211A Hard epoch-2 completion at 23:54: the uninterrupted run completed Hard epoch 2 at the exact required cumulative step `644,210=322,105*2`, reported epoch-2 mean train loss `0.0927`, and atomically wrote exported `epoch-2.pt` plus the complete DeepSpeed `epoch-2` recovery tag. It immediately entered epoch 3 and reached live step `644,410/966,315` (`66.6874%`), exactly `200/322,105` steps (`0.0621%`) into the final Hard epoch; original Easy/Medium/Hard/Long Stage-A coverage is `83.9001%` complete. The epoch-2 tail `640,010-644,210` contains `421` ten-step records at mean mixer loss/cosine/RMS ratio `0.094005/0.966387/0.996902`, with zero hidden missing count and zero frame delta. The first `20` epoch-3 records are too small for trend inference but also have complete layer/frame matching. Four ranks, W&B, boundary checkpoints, mount/storage, and the current-attempt runtime audit remain healthy. The exact-content base/public audit reached at least `226/4,172` archives (`5.4171%`); downstream social filtering and combined-v2 admission remain fail-closed. Continue the full third Hard epoch without early stopping or configuration changes.
- 2026-08-15 Stage211A Hard 00:54 hourly status: the uninterrupted four-rank run reached live step `647,610/966,315` (`67.0185%`) with persisted DeepSpeed tag `step-646000`. Hard epoch 3 is at `3,400/322,105` steps (`1.0556%`), and original Easy/Medium/Hard/Long Stage-A coverage is `84.0601%` complete. The epoch-3 head `644,220-647,610` contains `340` ten-step records at mean mixer loss/cosine/RMS ratio `0.097278/0.965208/0.995990`, with zero hidden missing count and zero frame delta. This small newly shuffled window is slightly harder than the epoch-2 tail but is not evidence of divergence; all four ranks, W&B, checkpoints, mount/storage, and the current-attempt runtime audit remain healthy. The exact-content base/public audit reached `234/4,172` archives (`5.6088%`). Its archive-count rate slowed on larger inputs, but a direct liveness check while reading a `538 MB` People’s Speech parquet observed about `16.5 MB` of physical reads in 20 seconds and an actively growing temporary fingerprint file, excluding a hung or corrupt-archive condition. Preserve its low CPU/I/O priority and continue both jobs unchanged.
- 2026-08-15 Stage211A Hard 01:52 status and step-650k fixed sentinel: the uninterrupted four-rank run reached live step `650,860/966,315` (`67.3548%`) with complete exported and DeepSpeed checkpoints at step 650,000. Hard epoch 3 is at `6,650/322,105` steps (`2.0645%`), and original Easy/Medium/Hard/Long Stage-A coverage is `84.2226%` complete. The complete `640k-650k` current-Hard window contains `1,000` ten-step records spanning the epoch boundary at mean mixer loss/cosine/RMS ratio `0.095596/0.965811/0.996412`, with zero hidden missing count, zero frame delta, and an empty current-attempt error audit. On the unchanged-provenance Easy-ZH sentinel, fixed loss locally improves from step-640k `0.1820873714` to `0.1794012181` (`-1.4752%`); mean 70-layer loss/cosine/RMS ratio improves from `0.1784076404/0.9405755761/0.8661038516` to `0.1758683203/0.9412746758/0.8697025046`. Loss improves in `55/70` layers and cosine in `52/70`, with every depth band improving. This is still only local recovery: relative to the bound Hard initialization, fixed loss remains `70.4360%` worse and `0/70` layers improve either loss or cosine. All training health evidence remains normal. The exact-content base/public audit reached `242/4,172` archives (`5.8006%`) and is processing consecutive approximately `530 MB` People’s Speech parquet archives with active I/O; downstream admission remains fail-closed. Continue both jobs unchanged.
- 2026-08-15 Stage211A Hard 02:51 hourly status: the uninterrupted four-rank run reached live step `654,210/966,315` (`67.7015%`) with persisted DeepSpeed tag `step-654000`. Hard epoch 3 reached the exact offset `10,000/322,105` (`3.1046%`), and original Easy/Medium/Hard/Long Stage-A coverage is `84.3901%` complete. The partial `650k-654.21k` current-Hard window contains `421` ten-step records at mean mixer loss/cosine/RMS ratio `0.094290/0.966273/0.997265`, with zero hidden missing count and zero frame delta; this recovers from the harder small epoch-3 head and remains consistent with stable optimization. Four ranks, W&B, checkpoints, mount/storage, and the current-attempt runtime audit remain healthy. The exact-content base/public audit reached `252/4,172` archives (`6.0403%`) while reading consecutive approximately `490-530 MB` People’s Speech parquet files. Its low-priority I/O reduces aggregate training throughput to about `3.3k` steps/hour but does not stop or corrupt training; preserve both complete-data jobs unchanged.
- 2026-08-15 Stage211A Hard 03:49 hourly status: the uninterrupted four-rank run reached live step `657,540/966,315` (`68.0461%`) with persisted DeepSpeed tag `step-656000`, advancing `3,330` optimizer steps since the previous snapshot. Hard epoch 3 is at `13,330/322,105` steps (`4.1384%`), and original Easy/Medium/Hard/Long Stage-A coverage is `84.5566%` complete. The partial `654.21k-657.54k` current-Hard window contains `333` ten-step records at mean mixer loss/cosine/RMS ratio `0.093912/0.966406/0.997115`, with zero hidden missing count and zero frame delta. Four ranks, W&B, checkpoints, mount/storage, and the current-attempt runtime audit remain healthy. The exact-content base/public audit reached `261/4,172` archives (`6.2560%`) and was actively reading a `551 MB` People’s Speech parquet at this snapshot; no filtered or combined artifact is admitted early. Continue both complete-data jobs unchanged.
- 2026-08-15 Stage211A Hard 04:47 status and step-660k fixed sentinel: the uninterrupted four-rank run reached live step `660,890/966,315` (`68.3928%`) with complete exported and DeepSpeed checkpoints at step 660,000. Hard epoch 3 is at `16,680/322,105` steps (`5.1784%`), and original Easy/Medium/Hard/Long Stage-A coverage is `84.7241%` complete. The complete `650k-660k` current-Hard window contains `1,000` ten-step records at mean mixer loss/cosine/RMS ratio `0.094588/0.966172/0.996906`, with zero hidden missing count, zero frame delta, and an empty current-attempt error audit. On the unchanged-provenance Easy-ZH sentinel, fixed loss improves from step-650k `0.1794012181` to `0.1767594208` (`-1.4726%`); mean 70-layer loss/cosine/RMS ratio improves from `0.1758683203/0.9412746758/0.8697025046` to `0.1732908125/0.9420610223/0.8714489523`. Loss and cosine each improve in `56/70` layers; all depth bands except 50--59 improve. The final-epoch local recovery reduces the gap versus the bound Hard initialization to `+67.9262%`, but `0/70` layers still improve either loss or cosine over that initialization, so retention correction remains mandatory. All training health signals remain normal. The exact-content base/public audit reached `271/4,172` archives (`6.4957%`) while actively reading another approximately `482 MB` People’s Speech parquet; downstream admission remains fail-closed. Continue both jobs unchanged.
- 2026-08-15 Stage211A Hard 05:46 hourly status: the uninterrupted four-rank run reached live step `664,320/966,315` (`68.7478%`) with persisted DeepSpeed tag `step-664000`, advancing `3,430` optimizer steps since the step-660k snapshot. Hard epoch 3 is at `20,110/322,105` steps (`6.2433%`), and original Easy/Medium/Hard/Long Stage-A coverage is `84.8956%` complete. The partial `660k-664.32k` current-Hard window contains `432` ten-step records at mean mixer loss/cosine/RMS ratio `0.094528/0.966185/0.996945`, with zero hidden missing count and zero frame delta, consistent with the preceding complete window. Four ranks, W&B, checkpoints, mount/storage, and the current-attempt runtime audit remain healthy; step 660,000 remains the latest fixed retention sentinel and no new public WER/CER evidence exists. The exact-content base/public audit reached `280/4,172` archives (`6.7114%`) and was actively reading an approximately `522 MB` People’s Speech parquet; downstream filtering and combined admission remain fail-closed. Continue both jobs unchanged.
- 2026-08-15 Stage211A Hard 06:44 hourly status: the uninterrupted four-rank run reached live step `667,790/966,315` (`69.1069%`) with persisted DeepSpeed tag `step-666000`, advancing `3,470` optimizer steps since the previous snapshot. Hard epoch 3 is at `23,580/322,105` steps (`7.3206%`), and original Easy/Medium/Hard/Long Stage-A coverage is `85.0691%` complete. The partial `664.33k-667.79k` current-Hard window contains `347` ten-step records at mean mixer loss/cosine/RMS ratio `0.093703/0.966481/0.998421`, with zero hidden missing count and zero frame delta. Four ranks, W&B, checkpoints, `639 GiB` root headroom, the intended read-only USB mount, and the current-attempt runtime audit remain healthy; step 660,000 remains the latest fixed retention sentinel and no new public WER/CER evidence exists. The exact-content base/public audit reached `290/4,172` archives (`6.9511%`) while actively reading an approximately `475 MB` People’s Speech parquet; downstream filtering and combined admission remain fail-closed. Continue both jobs unchanged.
- 2026-08-15 Stage211A Hard 07:42 status and step-670k fixed sentinel: the uninterrupted four-rank run reached live step `671,200/966,315` (`69.4598%`) with complete exported and DeepSpeed checkpoints at step 670,000. Hard epoch 3 is at `26,990/322,105` steps (`8.3793%`), and original Easy/Medium/Hard/Long Stage-A coverage is `85.2396%` complete. The complete `660k-670k` current-Hard window contains `1,000` ten-step records at mean mixer loss/cosine/RMS ratio `0.094047/0.966358/0.997582`, with zero hidden missing count, zero frame delta, and an empty current-attempt error audit. On the unchanged-provenance Easy-ZH sentinel, fixed loss regresses from step-660k `0.1767594208` to `0.1798982428` (`+1.7758%`); mean 70-layer loss/cosine/RMS ratio regresses from `0.1732908125/0.9420610223/0.8714489523` to `0.1762978614/0.9411413386/0.8696480320`. Only `10/70` layers improve either loss or cosine, and every depth band regresses. Relative to the bound Hard initialization, fixed loss is `70.9082%` worse and `0/70` layers improve either loss or cosine. This reversal confirms that the apparent final-epoch retention recovery is not monotonic and does not change the mandatory balanced-replay decision. All training health signals remain normal. The exact-content base/public audit reached `300/4,172` archives (`7.1908%`) while actively reading an approximately `472 MB` People’s Speech parquet; downstream admission remains fail-closed. Continue both jobs unchanged.
- 2026-08-15 Stage211A Hard 08:40 hourly status: the uninterrupted four-rank run reached live step `674,720/966,315` (`69.8240%`) with persisted DeepSpeed tag `step-674000`, advancing `3,520` optimizer steps since the step-670k snapshot. Hard epoch 3 is at `30,510/322,105` steps (`9.4721%`), and original Easy/Medium/Hard/Long Stage-A coverage is `85.4156%` complete. The partial `670k-674.72k` current-Hard window contains `472` ten-step records at mean mixer loss/cosine/RMS ratio `0.093404/0.966593/0.996650`, with zero hidden missing count and zero frame delta. Four ranks, W&B, checkpoints, mount/storage, and the current-attempt runtime audit remain healthy; step 670,000 remains the latest fixed retention sentinel and no new public WER/CER evidence exists. The exact-content base/public audit reached `311/4,172` archives (`7.4545%`) while actively reading an approximately `469 MB` People’s Speech parquet; downstream filtering and combined-v2 admission remain fail-closed. Continue both jobs unchanged.
- 2026-08-15 Stage211A Hard 09:38 hourly status: the uninterrupted four-rank run reached live step `678,350/966,315` (`70.1997%`) with persisted DeepSpeed tag `step-678000`, advancing `3,630` optimizer steps since the previous snapshot. Hard epoch 3 is at `34,140/322,105` steps (`10.5990%`), and original Easy/Medium/Hard/Long Stage-A coverage is `85.5971%` complete. The partial `674.73k-678.35k` current-Hard window contains `363` ten-step records at mean mixer loss/cosine/RMS ratio `0.092779/0.966807/0.998104`, with zero hidden missing count and zero frame delta. Four ranks, W&B, checkpoints, mount/storage, and the current-attempt runtime audit remain healthy; step 670,000 remains the latest fixed retention sentinel and no new public WER/CER evidence exists. The exact-content base/public audit reached `322/4,172` archives (`7.7181%`) while actively reading an approximately `467 MB` People’s Speech parquet; downstream filtering and combined-v2 admission remain fail-closed. Continue both jobs unchanged.
- 2026-08-15 Stage211A Hard 10:36 status and step-680k fixed sentinel: the uninterrupted four-rank run reached live step `682,050/966,315` (`70.5826%`) with complete exported and DeepSpeed checkpoints through step 682,000. Hard epoch 3 is at `37,840/322,105` steps (`11.7477%`), and original Easy/Medium/Hard/Long Stage-A coverage is `85.7821%` complete. The complete `670k-680k` current-Hard window contains `1,000` ten-step records at mean mixer loss/cosine/RMS ratio `0.093273/0.966635/0.997168`, with zero hidden missing count, zero frame delta, and an empty current-attempt error audit. On the unchanged-provenance Easy-ZH sentinel, fixed loss is nearly flat but locally regresses from step-670k `0.1798982428` to `0.1801790387` (`+0.1561%`); mean 70-layer loss/cosine/RMS ratio changes from `0.1762978614/0.9411413386/0.8696480320` to `0.1765519433/0.9410744203/0.8696962947`. Only `30/70` layers improve loss and cosine; bands 0--39 regress while 40--69 improve. Relative to the bound Hard initialization, fixed loss remains `71.1750%` worse and `0/70` layers improve either loss or cosine. Improving current-Hard alignment alongside flat, severely degraded Easy-ZH retention continues to require balanced replay. All runtime health signals remain normal. The exact-content base/public audit reached `332/4,172` archives (`7.9578%`) while actively reading an approximately `585 MB` People’s Speech parquet; downstream admission remains fail-closed. Continue both jobs unchanged.
- 2026-08-15 Stage211A Hard 11:35 hourly status: the uninterrupted four-rank run reached live step `685,760/966,315` (`70.9665%`) with persisted DeepSpeed tag `step-684000`, advancing `3,710` optimizer steps since the previous snapshot. Hard epoch 3 is at `41,550/322,105` steps (`12.8995%`), and original Easy/Medium/Hard/Long Stage-A coverage is `85.9676%` complete. The partial `680k-685.76k` current-Hard window contains `576` ten-step records at mean mixer loss/cosine/RMS ratio `0.093920/0.966410/0.997151`, with zero hidden missing count and zero frame delta. Four ranks, W&B, checkpoints, mount/storage, and the current-attempt runtime audit remain healthy; step 680,000 remains the latest fixed retention sentinel and no new public WER/CER evidence exists. The exact-content base/public audit reached `341/4,172` archives (`8.1735%`) while actively reading an approximately `585 MB` People’s Speech parquet; downstream filtering and combined-v2 admission remain fail-closed. Continue both jobs unchanged.
- 2026-08-15 Stage211A Hard 12:45 status and step-690k fixed sentinel: the uninterrupted four-rank run reached live step `690,230/966,315` (`71.4291%`) with complete exported and DeepSpeed checkpoints at step 690,000. Hard epoch 3 is at `46,020/322,105` steps (`14.2873%`), and original Easy/Medium/Hard/Long Stage-A coverage is `86.1910%` complete. The complete `680k-690k` current-Hard window contains `1,000` ten-step records at mean mixer loss/cosine/RMS ratio `0.093702/0.966484/0.997152`, with zero hidden missing count, zero frame delta, and an empty current-attempt error audit. On the unchanged-provenance Easy-ZH sentinel, fixed loss locally improves from step-680k `0.1801790387` to `0.1790318578` (`-0.6367%`); mean 70-layer loss/cosine/RMS ratio improves from `0.1765519433/0.9410744203/0.8696962947` to `0.1755049090/0.9413703296/0.8704364292`. Loss improves in `40/70` layers and cosine in `42/70`; bands 0--19 and 30--39 improve while the other four are flat-to-worse. Relative to the bound Hard initialization, fixed loss remains `70.0851%` worse and `0/70` layers improve either loss or cosine. This local fluctuation does not change the mandatory balanced-replay decision. All training health signals remain normal. The exact-content base/public audit reached `354/4,172` archives (`8.4851%`); downstream filtering and combined-v2 admission remain fail-closed. Continue both jobs unchanged.
- 2026-08-15 Stage211A Hard 13:44 hourly status: the uninterrupted four-rank run reached live step `694,030/966,315` (`71.8223%`) with persisted DeepSpeed tag `step-694000`, advancing `3,800` optimizer steps since the step-690k snapshot. Hard epoch 3 is at `49,820/322,105` steps (`15.4670%`), and original Easy/Medium/Hard/Long Stage-A coverage is `86.3810%` complete. The partial `690k-694.03k` current-Hard window contains `403` ten-step records at mean mixer loss/cosine/RMS ratio `0.093730/0.966463/0.997436`, with zero hidden missing count and zero frame delta. Four ranks, W&B, checkpoints, mount/storage, and the current-attempt runtime audit remain healthy; step 690,000 remains the latest fixed retention sentinel and no new public WER/CER evidence exists. The exact-content base/public audit reached `364/4,172` archives (`8.7248%`) while actively reading an approximately `578 MB` People’s Speech parquet; downstream filtering and combined-v2 admission remain fail-closed. Continue both jobs unchanged.
- 2026-08-15 Stage211A Hard 14:42 hourly status: the uninterrupted four-rank run reached live step `698,050/966,315` (`72.2383%`) with persisted DeepSpeed tag `step-698000`, advancing `3,820` optimizer steps since the previous snapshot. Hard epoch 3 is at `53,840/322,105` steps (`16.7150%`), and original Easy/Medium/Hard/Long Stage-A coverage is `86.5820%` complete. The partial `690k-698.05k` current-Hard window contains `805` ten-step records at mean mixer loss/cosine/RMS ratio `0.093477/0.966558/0.997335`, with zero hidden missing count and zero frame delta. Four ranks, W&B, checkpoints, mount/storage, and the current-attempt runtime audit remain healthy; step 690,000 remains the latest fixed retention sentinel and no new public WER/CER evidence exists. The exact-content base/public audit reached `376/4,172` archives (`9.0125%`) while actively reading an approximately `523 MB` People’s Speech parquet; downstream filtering and combined-v2 admission remain fail-closed. Continue both jobs unchanged.
- 2026-08-15 Stage211A Hard 15:40 status and step-700k fixed sentinel: the uninterrupted four-rank run reached live step `702,180/966,315` (`72.6657%`) with complete exported and DeepSpeed checkpoints through step 702,000. Hard epoch 3 is at `57,970/322,105` steps (`17.9972%`), and original Easy/Medium/Hard/Long Stage-A coverage is `86.7885%` complete. The complete `690k-700k` current-Hard window contains `1,000` ten-step records at mean mixer loss/cosine/RMS ratio `0.093350/0.966603/0.997328`, with zero hidden missing count, zero frame delta, and an empty current-attempt error audit. On the unchanged-provenance Easy-ZH sentinel, fixed loss is effectively flat, changing from step-690k `0.1790318578` to `0.1789938963` (`-0.0212%`); mean 70-layer loss/cosine/RMS ratio changes from `0.1755049090/0.9413703296/0.8704364292` to `0.1754252722/0.9414115204/0.8710374051`. Loss improves in `43/70` layers and cosine in `42/70`, with mixed band behavior. Relative to the bound Hard initialization, fixed loss remains `70.0490%` worse and `0/70` layers improve either loss or cosine. The plateau confirms that continued Hard-only training is not restoring Easy-ZH retention, so the post-coverage balanced replay remains mandatory. All runtime health signals remain normal. The exact-content base/public audit reached `391/4,172` archives (`9.3720%`) while actively reading an approximately `405 MB` People’s Speech parquet; downstream admission remains fail-closed. Continue both jobs unchanged.
- 2026-08-15 Stage211A Hard 16:38 hourly status: the uninterrupted four-rank run reached live step `706,510/966,315` (`73.1138%`) with persisted DeepSpeed tag `step-706000`, advancing `4,330` optimizer steps since the previous snapshot. Hard epoch 3 is at `62,300/322,105` steps (`19.3415%`), and original Easy/Medium/Hard/Long Stage-A coverage is `87.0050%` complete. The partial `700k-706.51k` current-Hard window contains `651` ten-step records at mean mixer loss/cosine/RMS ratio `0.093904/0.966409/0.996557`, with zero hidden missing count and zero frame delta. Four ranks, W&B, checkpoints, `638 GiB` root headroom, the intended read-only USB mount, and the current-attempt runtime audit remain healthy; step 700,000 remains the latest fixed retention sentinel and no new public WER/CER evidence exists. The exact-content base/public audit reached `409/4,172` archives (`9.8035%`) while actively reading an approximately `537 MB` People’s Speech parquet; downstream filtering and combined-v2 admission remain fail-closed. Continue both jobs unchanged.
- 2026-08-15 Stage211A Hard 17:36 status and step-710k fixed sentinel: the uninterrupted four-rank run reached live step `710,920/966,315` (`73.5702%`) with complete exported and DeepSpeed checkpoints at step 710,000. Hard epoch 3 is at `66,710/322,105` steps (`20.7106%`), and original Easy/Medium/Hard/Long Stage-A coverage is `87.2255%` complete. The complete `700k-710k` current-Hard window contains `1,000` ten-step records at mean mixer loss/cosine/RMS ratio `0.093561/0.966532/0.997101`, with zero hidden missing count, zero frame delta, and an empty current-attempt error audit. On the unchanged-provenance Easy-ZH sentinel, fixed loss regresses from step-700k `0.1789938963` to `0.1806356884` (`+0.9172%`); mean 70-layer loss/cosine/RMS ratio regresses from `0.1754252722/0.9414115204/0.8710374051` to `0.1770025635/0.9409353288/0.8686765064`. Only `16/70` layers improve loss and `17/70` improve cosine; all bands except 40--49 regress. Relative to the bound Hard initialization, fixed loss is `71.6088%` worse and `0/70` layers improve either loss or cosine. Healthy Hard alignment with worsening retention continues to require balanced replay. All runtime health signals remain normal. The exact-content base/public audit reached `429/4,172` archives (`10.2828%`) while actively reading an approximately `539 MB` People’s Speech parquet; downstream admission remains fail-closed. Continue both jobs unchanged.
- 2026-08-15 Stage211A Hard 18:35 hourly status: the uninterrupted four-rank run reached live step `715,370/966,315` (`74.0307%`) with persisted DeepSpeed tag `step-714000`, advancing `4,450` optimizer steps since the step-710k snapshot. Hard epoch 3 is at `71,160/322,105` steps (`22.0922%`), and original Easy/Medium/Hard/Long Stage-A coverage is `87.4480%` complete. The partial `710k-715.37k` current-Hard window contains `537` ten-step records at mean mixer loss/cosine/RMS ratio `0.092712/0.966831/0.996943`, with zero hidden missing count and zero frame delta. Four ranks, W&B, checkpoints, `638 GiB` root headroom, the intended read-only USB mount, and the current-attempt runtime audit remain healthy; step 710,000 remains the latest fixed retention sentinel and no new public WER/CER evidence exists. The exact-content base/public audit reached `451/4,172` archives (`10.8102%`) while actively reading an approximately `477 MB` People’s Speech parquet; downstream filtering and combined-v2 admission remain fail-closed. Continue both jobs unchanged.
- 2026-08-15 Stage211A Hard 19:37 status and step-720k fixed sentinel: the uninterrupted four-rank run reached live step `720,200/966,315` (`74.5306%`) with complete exported and DeepSpeed checkpoints at step 720,000. Hard epoch 3 is at `75,990/322,105` steps (`23.5917%`), and original Easy/Medium/Hard/Long Stage-A coverage is `87.6895%` complete. The complete `710k-720k` current-Hard window contains `1,000` ten-step records at mean mixer loss/cosine/RMS ratio `0.092600/0.966873/0.997388`, with zero hidden missing count, zero frame delta, and an empty current-attempt error audit. On the unchanged-provenance Easy-ZH sentinel, fixed loss locally improves from step-710k `0.1806356884` to `0.1792670005` (`-0.7577%`); mean 70-layer loss/cosine/RMS ratio improves from `0.1770025635/0.9409353288/0.8686765064` to `0.1756711257/0.9413703268/0.8696344380`. Loss and cosine each improve in `50/70` layers; bands 0--59 improve while 60--69 regresses. Relative to the bound Hard initialization, fixed loss remains `70.3085%` worse and `0/70` layers improve either loss or cosine. Current-Hard alignment continues improving but retention remains decisively failed, preserving the balanced-replay requirement. All runtime health signals remain normal. The exact-content base/public audit reached `477/4,172` archives (`11.4334%`); downstream filtering and combined-v2 admission remain fail-closed. Continue both jobs unchanged.
- 2026-08-15 Stage211A Hard 20:36 hourly status: the uninterrupted four-rank run reached live step `724,760/966,315` (`75.0025%`) with persisted DeepSpeed tag `step-724000`, advancing `4,560` optimizer steps since the step-720k snapshot. Hard epoch 3 is at `80,550/322,105` steps (`25.0074%`), and original Easy/Medium/Hard/Long Stage-A coverage is `87.9175%` complete. The partial `720k-724.76k` current-Hard window contains `476` ten-step records at mean mixer loss/cosine/RMS ratio `0.093025/0.966726/0.997255`, with zero hidden missing count and zero frame delta. Four ranks, W&B, checkpoints, `638 GiB` root headroom, the intended read-only USB mount, and the current-attempt runtime audit remain healthy; step 720,000 remains the latest fixed retention sentinel and no new public WER/CER evidence exists. The exact-content base/public audit reached `502/4,172` archives (`12.0326%`) while actively reading an approximately `527 MB` People’s Speech parquet; downstream filtering and combined-v2 admission remain fail-closed. Continue both jobs unchanged.
- 2026-08-15 Stage211A Hard 21:44 status and step-730k fixed sentinel: the uninterrupted four-rank run reached live step `730,190/966,315` (`75.5644%`) with complete exported and DeepSpeed checkpoints at step 730,000. Hard epoch 3 is at `85,980/322,105` steps (`26.6932%`), and original Easy/Medium/Hard/Long Stage-A coverage is `88.1890%` complete. The complete `720k-730k` current-Hard window contains `1,000` ten-step records at mean mixer loss/cosine/RMS ratio `0.092409/0.966941/0.997720`, with zero hidden missing count, zero frame delta, and an empty current-attempt error audit. On the unchanged-provenance Easy-ZH sentinel, fixed loss regresses from step-720k `0.1792670005` to `0.1804779447` (`+0.6755%`); mean 70-layer loss/cosine/RMS ratio regresses from `0.1756711257/0.9413703268/0.8696344380` to `0.1768988719/0.9409937596/0.8686004420`. Only `16/70` layers improve either loss or cosine; only band 0--9 improves while 10--69 regress. Relative to the bound Hard initialization, fixed loss is `71.4589%` worse and `0/70` layers improve either loss or cosine. Current-domain alignment continues improving while retention worsens, preserving the mandatory balanced-replay requirement. All runtime health signals remain normal. The exact-content base/public audit reached `536/4,172` archives (`12.8476%`); downstream filtering and combined-v2 admission remain fail-closed. Continue both jobs unchanged.
- 2026-08-15 Stage211A Hard 22:43 hourly status: the uninterrupted four-rank run reached live step `734,840/966,315` (`76.0456%`) with persisted DeepSpeed tag `step-734000`, advancing `4,650` optimizer steps since the step-730k snapshot. Hard epoch 3 is at `90,630/322,105` steps (`28.1368%`), and original Easy/Medium/Hard/Long Stage-A coverage is `88.4215%` complete. The partial `730k-734.84k` current-Hard window contains `484` ten-step records at mean mixer loss/cosine/RMS ratio `0.092210/0.967012/0.997098`, with zero hidden missing count and zero frame delta. Four ranks, W&B, checkpoints, `638 GiB` root headroom, the intended read-only USB mount, and the current-attempt runtime audit remain healthy; step 730,000 remains the latest fixed retention sentinel and no new public WER/CER evidence exists. The exact-content base/public audit reached `567/4,172` archives (`13.5906%`) while actively reading an approximately `484 MB` People’s Speech parquet; downstream filtering and combined-v2 admission remain fail-closed. Continue both jobs unchanged.
- 2026-08-15 Stage211A Hard 23:52 status and step-740k fixed sentinel: the uninterrupted four-rank run reached live step `740,360/966,315` (`76.6168%`) with complete exported and DeepSpeed checkpoints at step 740,000. Hard epoch 3 is at `96,150/322,105` steps (`29.8505%`), and original Easy/Medium/Hard/Long Stage-A coverage is `88.6974%` complete. The complete `730k-740k` current-Hard window contains `1,000` ten-step records: mean mixer loss/cosine improves from the preceding window's `0.092409/0.966941` to `0.091968/0.967096`, while RMS ratio remains close to one but changes slightly from `0.997720` to `0.997150`; hidden missing count and frame delta remain zero, and the current-attempt error audit is empty. On the unchanged-provenance 256-sample Easy-ZH sentinel, fixed loss regresses from step-730k `0.1804779447` to `0.1822242072` (`+0.9676%`); mean 70-layer loss/cosine/RMS ratio regresses from `0.1768988719/0.9409937596/0.8686004420` to `0.1785989519/0.9404561560/0.8677291559`. Only `21/70` layers improve loss and `20/70` improve cosine; bands 30--49 improve slightly while the other five bands regress. Relative to the bound Hard initialization, fixed loss is `73.1179%` worse and `0/70` layers improve either loss or cosine. Healthy current-domain alignment alongside worsening Easy-ZH retention continues to identify catastrophic forgetting and preserves the mandatory balanced all-depth Layer replay before Block; this is still a hidden-alignment sentinel, and no new public WER/CER evidence exists. Four ranks, W&B, checkpoints, `638 GiB` root headroom, the intended `/dev/sdb2` read-only XFS mount, and runtime health remain normal. The exact-content base/public audit completed `604/4,172` archives (`14.4775%`) and is actively reading archive index 605; downstream filtering and combined-v2 admission remain fail-closed. Continue both jobs unchanged.
- 2026-08-15 Stage211 formal supplemental admission hardening: every non-dry-run Mixer/Block/Logits full-phase controller and every formal `supplemental_natural` segment must reject a validated but obsolete schema-v1 base-natural inventory. Formal admission additionally requires schema version `2` and artifact `stage211_supplemental_combined_inventory`, after the existing exhaustive training-ready/component/public-overlap/USB-coverage validation. This protects the active long-lived Mixer controller, which was loaded before the default changed to combined-v2 and may retain the old base-v1 default in memory: its current Hard child remains untouched, Long may complete normally, but a subsequently spawned obsolete Supplemental child must fail before provenance creation or optimizer startup. The hourly continuation watcher will then restart through the current bootstrap, which waits for the immutable combined-v2 inventory and profile receipt before reusing Easy/Medium/Hard/Long receipts and starting the exact three-epoch fifth segment. No existing training process may be signalled or reconfigured to apply this guard.
- 2026-08-16 Stage211 per-phase public-gate readiness revalidation: the canonical normalized greedy-CTC public suite and direct Nano baseline were rehashed and deep-validated without changing any artifact. The immutable Nano provenance receipt binds checkpoint SHA-256 `81fec8616083c69377f3ceef36aba3655660ee0ca69a5d4a1e9810cd340ca499`, the audited public/train overlap receipt, all five manifests and predictions, identical utterance coverage, zero normalized-reference mismatch, and exactly `52,431` utterances: AISHELL-1 test `7,176` Chinese CER, LibriSpeech test-clean `2,620` English WER, LibriSpeech test-other `2,939` English WER, clean Common Voice 22 English test `14,922` WER, and WenetSpeech TEST_NET `24,774` Chinese CER. The finalizer executes checkpoint-paired hidden/logits evaluation, all seven stratified hidden cells, all five public decodes, Nano comparison, phase gate, and promotion receipt in that order. Mixer, Block, and Logits may therefore advance only after their complete normalized public WER/CER and phase-specific alignment gates pass; SFT has its own complete five-dataset final gate. Current Layer training has not yet reached this gate, so calibration numbers remain the only public student baseline and no intermediate hidden sentinel may be reported as a phase WER/CER result.
- 2026-08-16 Stage211D labeled-SFT data readiness revalidation: a low-priority full scan of the immutable `183,865,973`-byte length index, its bucket manifest, preparation summary, and preparation log passes the production Stage211D audit without decoding audio or modifying artifacts. The dataset contains exactly `283,868` train plus `1,434` eval rows (`285,302` unique utterances, `809.12755 h`), split as `222,040` English LibriSpeech and `63,262` Chinese AISHELL-3 samples. All `285,302` rows have non-empty pronunciation-only normalized CTC targets, `8,792,460` total CTC tokens, zero unknown tokens, and feasible required/logit frame lengths. One exact labeled epoch is `12,045` optimizer steps with `368` deterministic tail-padding exposures and `284,236` executed train-sample exposures; the configuration suppresses tokenizer special/non-pronunciation logits and rejects any such target token. This proves Stage D input readiness only: SFT remains strictly gated behind passing Mixer, Block, and Logits promotion receipts and must still run its complete five-dataset normalized WER/CER final evaluation.
- 2026-08-16 Stage211 initialization-source evolution proof: deep validation of the immutable Nano-initialization receipt correctly found that `rwkv_asr_ctc.py` no longer has its recorded SHA-256. Git history proves the receipt-bound bytes are commit `c4fee65421617b29ab70d415e5f3721f0ca64cbe` with SHA-256 `ed555d921292bfb4fc4efcf4dbaf24c8ca6fe8c0dc31cde968bba78156e4bc56`; the only later change is commit `eb38f47ac1c6a4c838b5d8cecafbd054ce9557e3`, which adds fail-closed CTC target validation after initialization and does not alter Nano loading. Historical receipts must remain immutable. Their validator may accept a changed loader source only when it locates ancestor Git content matching the recorded full-file SHA, the current file matches the working-tree SHA, and the complete normalized module AST is identical after removing exactly the approved `RWKVCTCModel._validate_ctc_targets` method and the exact `self._validate_ctc_targets(targets, target_lengths)` call from `ctc_loss`. The unchanged SenseVoice loader must still match its full-file SHA exactly. The validator must return per-source exact/evolved evidence, and the final stepwise proof must expose this evidence; any other source, call shape, or AST difference remains fatal. Exact checkpoint audits of all Nano MLP/Norm, five-layer CTC decoder, projected CTC-head rows, runtime QKV mapping, and freeze state remain mandatory and are not weakened by this source-evolution rule. Production revalidation passes with normalized AST SHA-256 `8be87102853f56f162ee32c37248ce76a3ba712590777da6c2dc025d645a0f9b`, exactly one removed method/call, exact SenseVoice-loader SHA-256 `e327fd335d264052a1a160ba71610c77bfea27e2cffd53e2c9c2a4cbac72cd93`, all `564 + 84` frozen tensors and `60,515/60,516` CTC rows matched, and zero tensor/head-row mismatches. The immutable Unicode metric-correction receipt still validates against initialization-receipt SHA-256 `b98ed6834925bd90e3329543b8a656f74137cb383fb3763003343c2c6182e093` with no decode, prediction, or manifest change.
- 2026-08-16 Stage211 teacher-forced Layer numerical-path audit: compare the production helper against the installed FunASR `EncoderLayerSANM` and the project `SenseVoiceRWKVEncoderLayer` before attributing the worsening fixed Easy-ZH sentinel to an implementation fault. Both paths use pre-norm mixer output, omit the first-layer residual for the `560 -> 512` dimension change, then apply pre-norm frozen Nano MLP and the second residual; formal configs use dropout `0.0`. Nano hooks capture raw layer input, `self_attn` output before dropout, `feed_forward` output before dropout, and final block output. The student helper must preserve the same component definitions and derive both directional deeper-layer `v_first` tensors from the student first-layer TimeMixer on the same teacher layer-zero input. Add a deterministic regression that compares every helper `mixer`, `ffn`, and `block` tensor with direct `SenseVoiceRWKVEncoderLayer.forward()` execution for layer zero and deeper layers, including unequal padded lengths. This audit and test do not alter the active Stage211A process, objective, checkpoint, or data order; the current evidence therefore continues to identify curriculum retention failure unless the new numerical equivalence gate fails.
- 2026-08-16 Stage211 stacked Block capture audit: Stage B/C/D use hooks from one complete student-propagated encoder forward, unlike Stage A teacher forcing. Add a deterministic regression that pre-captures each selected layer input and proves the production student hooks expose the raw BiRWKV TimeMixer output, the frozen Nano MLP output, and the exact residual block output from that same invocation. The first dimension-changing layer must reconstruct as `block = mixer + ffn`; every later layer must reconstruct as `block = layer_input + mixer + ffn`, including a selected TP layer after `after_norm` and unequal sequence lengths. Keep the existing frozen-decoder/head gradient test as the downstream proof that Block/Logits/SFT losses still update TimeMixer and the first input projection while frozen Nano parameters retain `grad=None`. This is verification-only and must not change or signal the active Stage211A run.
- 2026-08-16 terminal top-k checkpoint cleanup correction: a complete CPU DeepSpeed regression found that the training-time safety rule intentionally retains the current step even when it falls outside `top_k_step_checkpoints`, but the terminal path did not revisit that protection after writing the durable `epoch-*` resume checkpoint. With rolling periodic retention disabled, this leaves one unranked final `step-*.pt` and sharded directory on disk despite a top-k-only contract. Preserve the current-step protection during training; only after the epoch checkpoint and latest pointer are durable, prune all non-top-k step artifacts when `periodic_checkpoint_keep_last is None`. Assert that the retained step files exactly match the metrics-bound best records and that `latest_checkpoint.yaml` points to the existing epoch checkpoint. Formal Stage211 uses `periodic_checkpoint_keep_last=2`, so its intentional best-plus-two-rolling retention and the active process remain unchanged.
- 2026-08-16 Stage211D pronunciation-support consistency correction: labeled CTC SFT cannot combine tokenizer-derived non-pronunciation suppression on the student with a Nano conditional KL that still assigns probability to those same token IDs. The canonical SenseVoice tokenizer asset, student blank `60515`, and current pronunciation classifier resolve exactly `3,629` suppressed IDs, including the remapped-away Nano blank slot `60514` and excluding student blank `60515`; the ordered comma-separated ID fingerprint is SHA-256 `745c61a54cc6118c7a7988407d8f4fb8a794b0a6102f0ecf2aaf577945fc50f0`. Stage D must serialize this exact ordered set as both `ctc_suppressed_token_ids` and `ctc_teacher_online_project_ignored_token_ids`, while Mixer/Block/Logits retain only the ordinary `60514` projection ignore. The phase contract must rederive the set from the bound tokenizer asset and fail closed on count, fingerprint, student/teacher support mismatch, or blank inclusion errors. Before producing top-k, blank, argmax, or full-log-prob records, the online Nano teacher must remove every ignored nonblank ID and renormalize over the projected student support; otherwise its raw blank posterior and pronunciation-only conditional distribution describe incompatible probability spaces. The labeled-data audit and final stepwise CTC proof must expose the exact support count/fingerprint and affirm teacher/student support equality. Add deterministic tests in which the teacher's raw top-1 is deliberately suppressed and prove the projected record excludes it, maps blank, sums to one, and reports the corresponding renormalized blank probability. This future Stage D correction must not signal, reconfigure, or otherwise alter the active Stage211A process.
- 2026-08-16 Stage211D pronunciation-support verification: the production config constructor plus phase validator independently resolve and accept exactly `3,629/3,629` equal student/teacher IDs with fingerprint `745c61a54cc6118c7a7988407d8f4fb8a794b0a6102f0ecf2aaf577945fc50f0`, Nano blank slot `60514` ignored, and student blank `60515` retained. A controlled teacher record whose raw top-1 is suppressed proves that projected top-k and argmax exclude it, the old blank slot is removed, projected full probabilities sum to one, and stored blank log probability equals the projected distribution. Full Stage211 runner regression passes `95/95`; combined SFT, online-teacher, and DeepSpeed regressions pass `58` with `2` existing skips. This closes the future Stage D objective-conflict defect without changing the active Stage211A process or its loaded code.
- 2026-08-16 Stage211 base-public PCM audit local-cache contract: exhaustive normalized-PCM public-overlap proof remains mandatory before the combined supplemental fifth segment, but the source USB disk is rotational, 99% occupied, and the archive scanner is I/O-wait bound at roughly `4 MB/s` while the immutable inventory spans `4,172` archives and about `2.02 TiB`. Permit an optional one-archive-at-a-time cache on the local NVMe filesystem. Each cache population must stream the original archive once, compute and match the inventory-bound archive SHA-256 and byte size before decoding, use an atomic cache publication, retain the original archive path/size/mtime/SHA in every evidence receipt, and remove the cached archive after success or failure. Existing archive receipts must remain byte-identical and reusable regardless of cache configuration. Do not add parallel reads against the rotational USB disk; this optimization changes only transient I/O placement and must not signal or reconfigure the active four-GPU Stage211A training process.
- 2026-08-16 Stage211 base-public PCM audit local-cache production verification: focused PCM/supplemental regressions pass `15/15`, including original-source receipt binding, byte-stable receipt reuse, same-size/same-mtime source mutation rejection by SHA-256, changed-cache rejection, and success/failure cache cleanup; Ruff, shell syntax, formatting, and diff checks pass. The low-priority production audit resumed the existing immutable prefix through archive index `678`, recreated interrupted archive `679`, and completed new cached archives `679--682` without changing prior receipts. Steady completion intervals for archives `680--682` are approximately `58.9/44.8/65.3 s` (`56.3 s` mean), versus approximately `70.8/129.8/77.6 s` (`92.7 s` mean) for the immediately preceding uncached archives `676--678`, about `39%` lower wall time in this short matched operating window. Only the currently copied archive is present under the NVMe cache and it is removed at each archive boundary. Keep one sequential worker, preserve the resumable evidence chain, and let the exact audit finish before supplemental admission.
- 2026-08-16 Stage211 base-public PCM bounded decode parallelism: after local archive staging, the production scanner still averages below one CPU core while canonical soundfile decode/resample/PCM hashing dominates each archive. Permit a configurable bounded thread pool only inside the currently staged archive, with one sequential USB archive copy and one archive in flight. Read indexed entries in canonical archive order, decode bounded batches concurrently, and write fingerprint rows in the original order so serial and parallel fingerprint JSONL bytes, row counts, durations, PCM counts, receipts, and final overlap decisions are identical. Keep the default library behavior serial for compatibility; the supplemental builder may request four decode workers after exact serial/parallel fixture equality, controlled-failure cleanup, formatting, lint, and focused supplemental tests pass. This optimization must remain CPU-only, low priority, and independent of the active Stage211A GPU training process.
- 2026-08-16 Stage211A Hard step-750k fixed sentinel: the uninterrupted four-rank Layer/Mixer run completed Hard epochs 1 and 2 at mean training losses `0.1010092525` and `0.0927421553` and reached step `751,370/966,315` (`77.7562%`, Hard epoch 3 `33.2687%`) with complete online hidden matching, zero missing events, and zero frame delta. The exact step-750k current batch has mixer loss/cosine `0.1235/0.9554`, while the latest step-751,370 batch is `0.0940/0.9665`; batch variation remains expected across sampled depth bands. On the unchanged 256-row historical Easy-ZH sentinel, step-750k loss is `0.1831261819`, `73.975%` above the bound Hard initialization `0.1052601582`, and recent sentinels remain flat-to-worse (`0.176759` at 660k, `0.178994` at 700k, `0.179267` at 720k, `0.180478` at 730k, `0.182224` at 740k). This continues the already proven current-Hard gain plus old-Easy retention failure rather than indicating a numerical-path or runtime fault. Do not interrupt the exact three-epoch Hard trajectory or substitute an early checkpoint. Complete Hard, Long, and schema-v2 Supplemental coverage; then require the seven-cell representative hidden gate and complete normalized five-dataset WER/CER gate, followed by the prepared language-balanced all-depth replay correction when either gate fails, before any Block promotion.
- 2026-08-16 Stage211 base-public PCM four-thread production observation: the production audit resumed through archive index `693` with byte-stable serial/parallel fixture proof, no decode failure, and only one sequential USB archive copy in flight. Four-thread steady archive intervals for indices `690--693` are approximately `42.9/81.4/52.5/115.8 s` over `299/351/315/316 MB`; aggregate throughput is approximately `4.38 MB/s`, effectively no better than the immediately preceding single-decode-worker archives `684--688` at approximately `4.60 MB/s`. The process spends substantial time in uninterruptible USB I/O wait, so the active bottleneck is the nearly full rotational USB source while formal training reads the same device, not canonical PCM decode concurrency. Leave the already running audit unchanged to preserve its resumable archive boundary and avoid operational churn, do not add another USB reader, and do not infer a 71-hour remaining estimate as stable after Hard releases storage pressure. Reassess only from later immutable receipt intervals; Supplemental admission remains fail-closed until all `4,172` archives and combined-v2 receipts validate.
- 2026-08-16 Stage211 supplemental weighted-progress monitor: archive-count percentage alone is misleading because the immutable base-natural index spans widely varying archive sizes, row counts, and durations. Add a read-only reporter that proves completed archive receipts form the exact contiguous prefix `0..N`, rejects malformed/duplicate/out-of-range receipts, reads only the local SQLite `archives` table, and reports covered/total bytes, rows, duration, archive count, next source, and remaining source breakdown. It must not open any USB audio archive, estimate completion from unstable short-window throughput, mutate evidence, or signal the training/audit processes. Invoke the reporter from the existing hourly Stage211 monitor when the local index exists; reporter failure is visible monitor evidence but must remain observational. At archive index `697`, the immutable prefix covers `698/4,172` archives, `344,389,849,610/2,220,394,575,325` bytes (`15.5103%`), `1,303,369/6,434,286` rows (`20.2566%`), and `18,433,423,087/91,509,940,593 ms` (`5,120.40/25,419.43 h`, `20.1436%`); most remaining bytes are the `1,490,325,020,535`-byte People's Speech dirty component.
- 2026-08-16 Stage211 hourly formal-error log classification: the weighted-monitor production one-shot exposed that the formal-training error section also scanned the separately restarted Supplemental audit log. Because that log has no distributed-training attempt marker, historical controlled-interruption Tracebacks were incorrectly rendered as current formal training failures even while all four ranks had a clean latest attempt. Restrict `current-attempt formal-training errors` to recent logs containing the exact `[rwkvasr] Distributed init complete.` marker, then apply the existing last-marker suffix rule. Keep Supplemental health independent through its active tmux/process state and contiguous weighted receipt report. This is an observational classification correction only and must not suppress any marked formal log, mutate evidence, or signal either running job.
- 2026-08-16 Stage211 continuation-watcher runtime refresh: the continuation watcher process had been resident since `2026-08-11 01:49`, so its in-memory Bash functions predated the `2026-08-16` initialization-source evolution proof and pronunciation-support final-proof requirements even though the repository script and its regression tests were current. Restart only the watcher tmux session at `02:30:23` with the unchanged `3600s` poll and restart backoff so future supervisor recovery and terminal completion use the current deep validator. The existing Stage211 supervisor, four Hard training ranks, Supplemental PCM audit, and weighted hourly monitor retained their original PIDs and state. The refreshed watcher immediately observed the active supervisor and returned to sleep; it did not choose a restart stage, run a finalizer, or signal any model/data process.
- 2026-08-16 Stage211 social exact-PCM prefilter scheduling: the completed materialized social inventory contains `395,125` Chinese audio-only segments (`1,790.8237917 h`) across `8,036` source-local tar archives occupying `123 GB` on root NVMe. Social source fingerprinting is independent of the still-running base/public archive audit and may run concurrently at CPU nice `19`, idle I/O priority, and with CUDA hidden, because it opens only the materialized root-NVMe source receipts/tars and writes only the dedicated filtered-social root. Reuse the already complete five-dataset, `52,431`-row public canonical-PCM evidence from the base audit by first validating every source receipt/part against the same bound public manifests, then installing byte-identical receipt files under the social output root; each copied receipt continues to bind the original immutable part path, size, and SHA-256 and must validate as `reused` before source work starts. Run one production source as a restart-safe preflight before launching all sources. Do not run `--finalize-only` until all `8,036` source receipts exist, and do not start a second worker against the same output root. The original postmaterialization builder may later replay the source/public steps as immutable reuse before finalization. This scheduling change must not read USB audio, use a GPU, alter the active base audit, or signal formal training. The production worker started in tmux session `rwkvasr_stage211_social_pcm_prefilter` at `02:35:54` as PID `1045388`; after 90 seconds it had created and validated `77` source receipt/part pairs, remained at CPU nice `19` with idle I/O scheduling and an empty `CUDA_VISIBLE_DEVICES`, and had not changed any formal-training PID or GPU allocation.
- 2026-08-16 Stage211 social exact-PCM weighted monitoring: extend the existing observational hourly monitor with a dedicated read-only social-prefilter reporter. Bind progress to the immutable materialized inventory and require completed source-fingerprint receipts to be the exact contiguous prefix `0..N`; every counted receipt must match its source index, source-receipt path/SHA, materialized tar identity (or inventory-bound absence), row count, duration, and expected output part identity/size. The inventory contains `133` legitimate zero-segment sources rejected by VAD or exact-source deduplication; require those entries to have zero duration, no tar identity, and a zero-byte fingerprint part rather than treating them as missing work or audio coverage. Report source-count, materialized-tar bytes, rows, and duration coverage independently because source archives vary in size. Reporter failure must remain visible but observational. It must not hash or decode source audio, open `/media/usbhd`, mutate evidence, start/finalize workers, use CUDA, or signal the training, base-audit, or social-prefilter processes. Synthetic regression tests cover weighted progress, the zero-source contract, noncontiguous completion, changed identity, and monitor invocation. A production one-shot validated contiguous receipts through source `727`; refresh only the hourly monitor at `02:44:30` as PID `1047028` to load the reporter. Its first snapshot bound source receipts through `751` (`752/8,036` sources, `41,178/395,125` rows, `153.459186/1,790.823792 h`) while the four formal rank PIDs and both data-worker PIDs remained unchanged.
- 2026-08-16 Stage211 initialization-proof restart compatibility: the immutable production initialization receipt predates the approved loader-source evolution that adds only CTC target validation. Its deep validator correctly proves both current loader sources by exact hash or historical non-initialization AST equivalence, but the receipt-creation CLI currently rebuilds a current-source payload first and rejects the byte difference before invoking that validator. This would fail a future supervisor restart at `ensure_initialization_receipt` even though the bound calibration/Nano checkpoints, QKV mapping, frozen Nano MLP/norm/decoder/head tensors, and loader initialization semantics remain valid. When the output receipt already exists, validate and reuse it before attempting any rebuild: bind the caller-supplied calibration reuse receipt path/SHA, calibration checkpoint path/SHA, and Nano checkpoint path/SHA, then require the existing deep loader-source validation to pass. Create a new immutable receipt only when output is absent. Never rewrite or silently migrate an existing receipt, and continue to reject any checkpoint/input mismatch or loader initialization-semantic change. The fixed production command returned `status=reused` against Nano SHA-256 `81fec8616083c69377f3ceef36aba3655660ee0ca69a5d4a1e9810cd340ca499`, revalidated both loader sources, `648` frozen MLP/norm/decoder tensors, and all `60,516` projected head rows, and left the receipt bytes unchanged.
- 2026-08-16 Stage211 strict-chain regression and supplemental readiness audit: run the complete CPU-only Stage211 test selection at low CPU and idle-I/O priority while leaving the four-GPU Layer process untouched. All `209/209` tests pass in `186.35 s`; the only output is `14` upstream PyTorch JIT deprecation warnings. The suite covers immutable full-data receipts, Layer/Block/Logits objectives and gates, bounded correction replay, promotion/checkpoint bindings, normalized public WER/CER replay, labeled SFT admission, continuation dispatch, and final stepwise evidence. Ruff passes for every Stage211 Python source/test/script, Python bytecode compilation passes, and `bash -n` passes for the bootstrap, continuation watcher, hourly monitor, and post-materialization builder. A contemporaneous read-only weighted snapshot validates contiguous base-public PCM receipts through archive `746`: `747/4,172` archives, `366,746,786,458/2,220,394,575,325` bytes (`16.5172%`), `1,394,852/6,434,286` rows (`21.6784%`), and `5,485.059668/25,419.427942 h` (`21.5782%`). Social normalized-PCM filtering validates contiguous receipts through source `4,531`: `4,532/8,036` sources (`56.3962%`), `72,066,273,280/130,934,937,600` bytes (`55.0398%`), `195,385/395,125` rows (`49.4489%`), and `981.942472/1,790.823792 h` (`54.8319%`). The transient base archive cache is only `105 MB`, root retains about `638 GB`, and combined-v2 Supplemental admission remains fail-closed until both exact scans finish and their immutable receipts validate.
- 2026-08-16 Stage211 completed-segment storage compaction contract: the active Stage-A tree already occupies `127 GB`, while completed Easy/Medium/Hard segments retain roughly `29/49/49 GB`; extrapolating the same DeepSpeed retention across five segments in each of Layer, Block, and Logits plus correction/SFT runs can exceed the remaining `638 GB` root capacity. Storage compaction may run only after an immutable `curriculum_coverage` receipt has validated exact three-epoch completion, its exported completion checkpoint, all three receipt-bound `epoch-*.pt` checkpoints, and the parameter-delta/data/teacher chain. It may remove only child directories under that completed run's `ds_checkpoints` directory which match a known `step-N` or `epoch-N` tag and are not referenced by `latest_checkpoint.yaml` or `best_checkpoint.yaml`; preserve both referenced DeepSpeed recovery tags, `ds_checkpoints/latest`, `zero_to_fp32.py`, every exported `.pt`, every fixed/step evaluation, W&B data, config, provenance, and immutable curriculum receipt. Write an immutable deterministic plan before deletion and an immutable completion receipt after all planned tags are absent, so interruption can resume from the same plan without widening deletion scope. Unknown directories, changed receipt/run bindings, missing preserved tags, a nonterminal latest checkpoint, or any path escape must fail closed. Invoke this only from the full-phase controller after receipt validation; an active or incomplete segment has no admissible receipt and therefore cannot be compacted. This changes no model, optimizer trajectory, data order, WER/CER gate, or current Hard process, while preserving one latest and one best optimizer recovery state per completed segment.
- 2026-08-16 Stage211 completed-segment storage compaction production verification: add a reusable module/CLI with a non-destructive `--plan-only` pass, immutable plan and completion receipts, exact receipt/exported-epoch validation before the first deletion, path/tag containment, latest/best recovery preservation, restart after a controlled mid-delete interruption, idempotent replay, and rejection when an unknown, nonterminal, changed, or post-completion reappearing tag is observed. Integrate it after each validated original or supplemental full-phase receipt; the currently loaded Hard controller is unaffected, while future restarted/current controllers compact only completed segments. Production Easy plan SHA-256 `6a914d1e213c8526d18872444fd90fdc58cf85c58375b20ed2a8b634d9017a9c` removes six non-preserved tags totaling `18,171,679,714` bytes; completion receipt SHA-256 is `c9ffdcd90a310b22cfdfb79bfc96153a88560d0138b6282c399a2d2aee82a2c9`. Medium plan SHA-256 `cf5cdef9555f10eee9ea34a6da47a7c506fd58e403b2fac8588e41949d1d0cc2` removes eleven tags totaling `33,315,191,977` bytes; completion receipt SHA-256 is `b0e32bebf607385bd93a41f1482dc8e7e8a65750ca682768d04ff21bb70ea669`. Both preserve only `best` and terminal `epoch-3` DeepSpeed tags while all exported checkpoints/evaluation evidence remain present; idempotent production replay returns the same receipt hashes. The Stage211 tree falls from `127 GB` to `79 GB`, and root free space rises from about `638 GB` to `686 GB`. The contemporaneous hourly snapshot shows all original Hard rank PIDs unchanged at step `758,670/966,315` (`78.5117%`) with no current-attempt formal error. New storage tests pass `3/3`, existing runner tests pass `97/97`, and the complete Stage211 suite passes `212/212`; focused Ruff, formatting, bytecode compilation, shell syntax, and diff checks pass.
- 2026-08-16 Stage211 social normalized-PCM finalization: the independent low-priority prefilter naturally completed the exact contiguous source prefix `0..8,035`, covering `8,036/8,036` sources, `130,934,937,600/130,934,937,600` materialized bytes, `395,125/395,125` rows, and `1,790.8237917/1,790.8237917 h`. A single CPU-only `--finalize-only` pass revalidated all `8,036` source receipts, five public receipts, fixed-eval exclusion, and output identities, then produced training-ready `filtered_inventory.json` SHA-256 `1aaa98754d63e6ad7af61a1c150ac45b9bef267c19a23ec6f0efd58a192a35f1`. Normalized-PCM exact deduplication rejects `230` rows in `125` duplicate groups and retains `394,895` rows (`1,790.3335056 h`) across `228` data parts and `231` output-tree records. Exact public overlap against all `52,431` AISHELL-1, LibriSpeech clean/other, Common Voice EN, and WenetSpeech TEST_NET rows is zero. Near-duplicate detection remains explicitly incomplete rather than being overstated. This closes the social component of combined-v2 Supplemental readiness; combined admission must still wait for the independent base-natural/public exact-PCM audit to finish and validate, and no second social worker or GPU process is started.
- 2026-08-16 Stage211A Hard 04:44 status and strict-boundary audit: the uninterrupted four-rank Layer/TimeMixer run reached live step `763,400/966,315` (`79.0012%`) with persisted step `762,000`; Hard epochs 1 and 2 remain exactly complete at mean training losses `0.1010092525` and `0.0927421553`, and epoch 3 is `119,190/322,105` steps (`37.0035%`). The latest online sample has mixer loss/cosine/RMS ratio `0.0890/0.9682/0.9975`, complete `9/9` teacher matching, zero missing events, and zero frame delta. The unchanged 256-row Easy-ZH sentinel at step 760,000 is `0.1827659941`, `73.6326%` above the bound Hard initialization `0.1052601582`; the recent sequence `0.176759/0.178994/0.179267/0.180478/0.182224/0.183126/0.182766` at steps 660k/700k/720k/730k/740k/750k/760k proves persistent old-domain forgetting despite current-Hard optimization. This is not Stage-A public WER/CER evidence: the current checkpoint must not be reported as improved or promoted before Hard, Long, schema-v2 Supplemental, seven-cell retention evaluation/correction, and the five full normalized public datasets complete. A source/config audit confirms the production path remains a 70-layer bidirectional RWKV-7 TimeMixer with `head_size=64` and `cuda_clampw`; the non-attention FFN is the Nano-shaped two-linear ReLU MLP, no RWKV channel mixer is present, `att.w0` remains in the 2x-LR no-decay group, and only large matrix weights may receive decay when enabled. Focused CPU-only regression tests pass `19/19`, covering Nano QKV initialization into both RWKV directions, frozen Nano MLP/norm/decoder/head behavior, gradient isolation to BiRWKV, optimizer grouping, bounded balanced retention correction, full five-dataset WER/CER replay, Block/Logits evidence, exact three-epoch/Supplemental admission, and final stepwise proof. The weighted Supplemental audit is independently at `807/4,172` archives and `5,903.578261/25,419.427942 h`, while social exact filtering is complete; leave the one sequential USB reader and active GPU trajectory unchanged.
- 2026-08-16 Stage211 hourly fixed-sentinel reporting contract: every active Layer, Block, Logits, correction, or SFT run that emits `step_eval_baseline.yaml` and immutable `step_eval_layers_step-N.yaml` reports must expose the numerically greatest complete report in the existing hourly monitor. Report the baseline loss, latest report step/loss/sample count, signed absolute and relative change, and a lower-is-better `improved/flat/regressed` classification. Bind the report step to its filename, ignore temporary or malformed names, and render invalid/missing report fields explicitly rather than silently substituting a training batch. Label this telemetry `fixed_hidden_not_public_wer_cer`; it is retention evidence only and must never be presented as normalized public WER/CER or used to bypass the five-dataset phase gate. The change is read-only and observational: it must not load checkpoints, evaluate audio, signal or restart training, alter the one-hour cadence, or change any phase decision. Focused monitor/continuation tests pass `12/12`, including temporary-file exclusion and report-step/filename mismatch rejection; a production-directory one-shot renders `baseline_loss=0.1052601582`, `latest_step=760000`, `latest_loss=0.1827659941`, `delta_pct=+73.6326`, and `trend=regressed`. Do not restart the resident monitor between its established hourly snapshots merely to load this observational change; deploy it at a controlled monitor-only refresh boundary without touching any training or data process.
- 2026-08-16 Stage211 post-monitor full regression: after adding explicit fixed-sentinel telemetry and downstream correction-provenance coverage, the complete CPU-only `tests/test_stage211*.py` selection passes `216/216` in `177.44 s`; the only output is `14` upstream PyTorch JIT deprecation warnings. The suite revalidates strict Layer/Block/Logits/SFT order, exact three-epoch five-segment coverage, online Nano objective/frozen-path contracts, balanced bounded correction rounds, normalized English WER/Chinese CER replay, SFT pronunciation labels, immutable checkpoint promotion, continuation recovery, and the final stepwise report. A dedicated cross-phase correction test proves Block and Logits corrections bind their own phase and reduced learning rate while retaining the intentional `mixer_only` trainable boundary and `early_stopping=false`; Block/Logits are progressively wider loss/evaluation objectives, not Nano-MLP/decoder/head unfreeze stages. This verification changes no production process or artifact.
- 2026-08-16 Stage211 resident legacy-controller handoff contract: the production full-phase Python and Bash controller processes were loaded on 2026-08-09 before the schema-v2 Supplemental fifth segment existed. They may finish Hard and Long and emit the historical four-segment `curriculum_complete.json` plus `final_checkpoint.txt`, but the current finalizer must reject that summary because `supplemental_natural` is absent. The current continuation watcher must then classify the artifact as `full`, never `post_mixer`; the newly loaded bootstrap waits for the validated combined-v2 Supplemental inventory, reuses the immutable Easy/Medium/Hard/Long receipts, archives the legacy summary through the existing restart-safe migration, trains Supplemental for exactly three epochs, and writes `final_checkpoint_with_supplemental.txt`. Execution-level Bash tests prove a four-segment summary selects `full` and only a complete three-epoch Supplemental summary selects `post_mixer`; together with migration and multi-handoff coverage, the focused handoff suite passes `7/7`. Keep the watcher sourceable only for tests while preserving normal direct-execution behavior. Do not restart or signal the resident supervisor, four training ranks, hourly monitor, or data audit to deploy this testability change; the already refreshed continuation watcher has the required runtime gate in memory.
- 2026-08-16 Stage211 post-handoff full regression: after adding execution-level legacy/fifth-segment routing coverage, the complete low-priority `tests/test_stage211*.py` selection passes `218/218` in `178.23 s`; only the same `14` upstream PyTorch JIT deprecation warnings remain. `bash -n`, Ruff semantic checks, and diff checks also pass. This validation reads no training audio, uses no CUDA, and leaves every production process unchanged.
- 2026-08-16 Stage211 continuation selection-validation contract: a supervisor restart must not choose `block`, `logits`, or `sft` from a shallow JSON artifact/phase match. Before selecting a downstream restart point, reuse the correction loop's authoritative existing-selection validator to replay the bound phase gate, complete normalized public English WER/Chinese CER evidence, passing decision, checkpoint and SHA, promotion receipt and Nano-teacher chain, then rebuild and byte-compare the selection payload. Add a side-effect-free `--validate-selection-only` mode which fails when the selection is absent or invalid and can never finalize a gate, run a correction, or start training. The continuation watcher falls back to the nearest earlier proven stage when deep validation fails. This hardens future supervisor recovery only; do not restart or signal the resident supervisor, training ranks, hourly monitor, or data workers to load it while the current Hard process is healthy.
- 2026-08-16 Stage211 continuation deep-selection verification: the correction-loop selection tests pass `6/6`, the focused continuation/migration tests pass `12/12`, and the complete low-priority Stage211 suite passes `222/222` in `178.19 s` with only the existing `14` upstream PyTorch JIT deprecation warnings. Execution-level Bash fixtures prove that the same superficially present Logits selection routes to `sft` only when the deep validator exits successfully and otherwise falls back to `full`; when Logits validation fails but Block passes, the watcher selects `logits` without falling through to Mixer. The Python fixture proves validate-only invokes the authoritative validator and cannot enter `run_retention_loop`. Ruff, `bash -n`, and diff checks pass. No production process is restarted or signaled.
- 2026-08-16 Stage211 canonical public-baseline source replay: rerun the production calibration reuse validator at low CPU and idle-I/O priority over all five canonical manifests plus Nano and calibration prediction JSONL files. The newly rendered receipt is byte-identical to the installed immutable receipt with SHA-256 `b972ea27af25c597c7aaf5b3f2ee512c6d9991a3c94d8792f7c477a65a2f9862`. It independently source-recomputes exactly `52,431` utterances with identical ID coverage and zero normalized-reference mismatch: AISHELL-1 `7,176` CER `4.182695/57.255763%` Nano/calibration, LibriSpeech clean `2,620` WER `7.902845/28.033704%`, LibriSpeech other `2,939` WER `11.963624/50.836804%`, clean Common Voice EN `14,922` WER `21.869229/57.556288%`, and WenetSpeech TEST_NET `24,774` CER `8.630970/98.394456%`. This proves the reusable baseline and metric routes are current, but `all_datasets_pass=false`; it is calibration evidence only and cannot substitute for the pending Layer, Block, Logits, or SFT public evaluations.
- 2026-08-16 Stage211 public-evaluation input preflight contract: before A/B/C phase finalization or D/SFT finalization launches any GPU alignment or public decode, deep-validate the bound Nano public-baseline receipt and require the caller's five canonical manifest paths and five Nano prediction paths to exactly match the receipt. This must prove the clean `14,922`-row Common Voice subset, the other four fixed datasets, all file SHA-256 values, exact language/metric/sample metadata, public/train overlap receipt, Nano checkpoint, and inference-report provenance. Fail immediately on a different manifest or prediction directory rather than spending a full four-GPU evaluation and rejecting it afterward. Keep the existing post-decode source replay and candidate binding as an independent second check. Dry-run remains side-effect-free and may print commands without production artifacts.
- 2026-08-16 Stage211 public-evaluation input preflight verification: focused tests pass canonical directories and reject independently changed manifest and Nano-prediction directories before decode; both A/B/C and SFT import the same helper, and Ruff plus Python compilation pass. A real low-priority production invocation deep-validates the current Nano provenance receipt and reports `datasets=5 samples=52431` without using CUDA. The complete Stage211 suite passes `223/223` in `179.58 s` with only the existing `14` upstream PyTorch JIT deprecation warnings. Training, monitor, continuation, and data-worker processes are not restarted or signaled.
- 2026-08-16 Stage211 public-decode normalization contract: every formal calibration, Layer, Block, Logits, and SFT public decode must pass `CTC_TEXT_NORMALIZATION=ctc` to the labeled prediction loader in addition to `METRIC_NORMALIZATION=ctc` at aggregation and `--normalization ctc` in the Nano comparison. Prediction text still comes directly from greedy CTC and is unaffected; this setting ensures manifest reference text and reference token targets remove non-pronounced/control units at ingestion rather than relying only on a later idempotent metric pass. Keep source-recomputed metric replay against the immutable manifests as the authoritative check.
- 2026-08-16 Stage211 public-decode normalization verification: focused environment/bootstrap tests pass `3/3`, explicitly proving both `CTC_TEXT_NORMALIZATION=ctc` and `METRIC_NORMALIZATION=ctc`; Ruff, shell syntax, and diff checks pass. The complete low-priority Stage211 suite remains `223/223` passing in `177.98 s`, with only the existing `14` upstream PyTorch JIT deprecation warnings. No running process is restarted or signaled.
- 2026-08-16 Stage211 hourly-monitor controlled deployment: after the established `05:44:40+08:00` automatic snapshot completed, refresh only the observational monitor tmux session at `05:45:20+08:00` so its resident process loads the fixed-sentinel reporting contract. Preserve the strict supervisor, continuation watcher, Supplemental builder, and all four Layer/Hard training ranks unchanged; rank PIDs remain `250599--250602` with uninterrupted elapsed time. The immediate deployment snapshot reports live step `768,270/966,315` (`79.5051%`), persisted step `768,000`, no current-attempt formal-training error, and fixed-sentinel loss `0.1827659941` at step `760,000` versus the bound `0.1052601582` baseline (`+73.6326%`, `trend=regressed`, `256` samples). This is explicitly `fixed_hidden_not_public_wer_cer`: it proves continued retention failure and the need for the already receipted replay, but cannot substitute for or pre-judge the pending normalized five-dataset Layer WER/CER gate. The same snapshot validates the USB source as `/dev/sdb2` read-only, social exact-PCM preparation complete, and base-public exact-PCM preparation at `6,144.151474/25,419.427942 h` (`24.1711%`); retain one sequential USB reader and the one-hour monitor cadence.
- 2026-08-16 Stage211 strict public-gate handoff audit: re-read the production bootstrap, A/B/C finalizer, phase-gate builder, correction selector, SFT finalizer, and immutable stepwise-report builder as one chain. Layer/Mixer evaluates all `52,431` canonical utterances against Calibration, Block evaluates the same set against the selected Layer gate, Logits evaluates the same set against Nano's every-dataset `1.20x` plus `3.0`-point bound, and labeled SFT evaluates the same set against the selected Logits gate while retaining that Nano bound. Every decode uses `CTC_TEXT_NORMALIZATION=ctc`, every comparison uses `normalization=ctc`, every gate source-replays exact manifest/Nano/student prediction coverage, and each selected transition deep-validates its checkpoint SHA, Nano teacher, public evidence, passing decision, and promotion receipt. A failed phase gate is preserved without promotion and routes only to the phase-local bounded full replay correction; the final report independently requires ordered `rwkv_layer -> block -> logits -> sft` checkpoint, five-segment three-epoch coverage for A/B/C, labeled one-epoch SFT coverage, and three English-WER plus two Chinese-CER rows at every stage. The CPU-only runner/correction/SFT control-chain regression passes `126/126` in `155.08 s` with only `14` upstream PyTorch JIT deprecation warnings. No corrective code change is required by this audit, and no training, GPU, supervisor, monitor, or data process is altered.
- 2026-08-16 Stage211 runtime split-label clarification: the inherited Stage210 segment template carries an `easy` curriculum object solely as a legacy display field, so the Stage211 controller historically prints `split=easy` even for a Hard run. This field does not select training data. The authoritative production Hard YAML has `webdataset_split=train`, `webdataset_root=/`, the Hard `stage179c_hard_dedup_audio_only_online_ctc/webdataset_lengths.jsonl`, and the locally bound Hard `manifest_stage211_fixed_eval.json`; its loader audit reports `18,649,326` train rows plus the independent `256`-row fixed eval sidecar. Replace the ambiguous future console label with the effective `loader_split=train` (or `labeled_train` for SFT) and keep `difficulty=hard` as the curriculum identity. Add a runtime-config assertion/test so a generated Stage211 config cannot silently retain a Stage210 source path or non-train loader split. This observability and fail-closed check applies only to newly invoked controllers and must not restart the resident Hard process.
- 2026-08-16 Stage211 runtime data-binding verification: every newly generated A/B/C/D config now requires `webdataset_split=train`, the exact caller-admitted bucket manifest, and, when an audio or labeled audit is available, byte-equivalent resolved WebDataset root and length-index paths. Audio-only configs explicitly overwrite the inherited split before this check, and future segment logs emit `loader_split=train` instead of the unrelated Stage210 curriculum enum. Synthetic tests reject both `webdataset_split=easy` and an old Easy manifest, while the complete Stage211 runner suite passes `105/105` in `154.25 s`; post-format focused data-binding/Medium/SFT tests pass `3/3`, Ruff semantic checks, Python compilation, and diff checks pass. The already running production Hard YAML was independently read and satisfies the same binding, so no runtime process is changed.
- 2026-08-16 Stage211 pre-training difficulty-manifest admission: config-to-caller binding alone cannot exclude a controller passing the wrong but internally consistent manifest, while the existing loaded-manifest provenance check occurs only in the post-training phase gate. Before any newly invoked non-dry-run A/B/C full-phase smoke or optimizer step, deep-validate the immutable loaded-manifest chain receipt and require each caller-resolved Easy/Medium/Hard/Long runtime manifest path and SHA-256 to equal its receipt-bound difficulty record. Continue to validate global dedup provenance, fixed-eval identity, train-split identity, all `70/436/218/21` part metadata records, exact `1,174,987/42,247,508/18,649,326/404` row partition, and total `62,072,225` unique rows. A mismatch must fail before GPU allocation rather than after three epochs. Dry-run fixtures remain side-effect-free, while production uses the canonical receipt by default. This affects only future controller invocations and must not restart the resident legacy Hard process.
- 2026-08-16 Stage211 pre-training difficulty-manifest verification: the new full-phase admission gate accepts the four canonical production manifests against loaded-manifest receipt SHA-256 `af7c38a72fd714148390de5dea7d4d32a062a78ed1e25143928e0c3db88561c1` in `7.99 s` under CPU-only low-priority execution. Synthetic coverage accepts the exact four-way map, rejects Hard bound to the Easy path, and rejects a changed Hard manifest digest before training. The complete runner suite passes `106/106` in `152.65 s`; Ruff semantic checks, Python compilation, and diff checks pass. Existing dry-run behavior remains free of production-artifact requirements. No active rank, supervisor, monitor, or data worker is signaled or restarted.
- 2026-08-16 Stage211 pre-optimizer phase-objective admission: the authoritative phase objective contract was previously enforced while creating or validating the post-training coverage receipt, which could detect a generated-config drift only after an expensive segment completed. Invoke the same `validate_stage211_phase_train_config` immediately after every A/B/C/D config is constructed and after its runtime data binding is established, before writing the YAML or launching DeepSpeed. This must bind the common Nano teacher/frontend/tokenizer/blank mapping, Mixer-only trainable boundary, frozen copied Nano MLP/norm/decoder/head path, normalized hidden-loss formulation, and every phase-specific LR/loss weight. Layer remains teacher-forced Mixer-only hidden alignment; Block remains stacked Mixer/FFN/block/encoder/decoder-hidden alignment; Logits intentionally uses factorized blank plus legal-nonblank conditional/hard and sequence/window objectives with uniform full-vocabulary KL at zero, following the prior full-init stability controls; SFT adds pronunciation-only label CTC while retaining low Nano anchors. Any drift must fail before an optimizer step. This applies only to future controller invocations and does not alter the resident Hard configuration or process.
- 2026-08-16 Stage211 pre-optimizer phase-objective verification: parameterized generator tests prove Layer, Block, Logits, and SFT each invoke the authoritative validator during config construction, while the existing cross-phase mutation test proves a wrong objective is rejected. A direct CPU-only replay over the active production Hard YAML validates all `63` Mixer contract fields, confirming the resident process already uses the intended teacher-forced hidden objective. The complete runner suite passes `110/110` in `157.78 s`; Ruff semantic checks, Python compilation, and diff checks pass. No active process or generated production config is modified.
- 2026-08-16 Stage211 post-admission full regression and error precedence: the first complete `tests/test_stage211*.py` run correctly exposed one control-plane regression rather than a training fault: a fixture containing both a legacy base-only Supplemental inventory and noncanonical four-way manifests encountered the new manifest admission first, obscuring the more fundamental schema-v2 inventory rejection. Preserve fail-closed behavior but validate the Supplemental profile before loaded-manifest provenance; both checks still complete before Nano loading, smoke, GPU allocation, or an optimizer step. Focused invalid-inventory, difficulty-binding, and dry-run tests pass `3/3`, and the repeated complete Stage211 suite passes `228/228` in `179.16 s` with only `14` upstream PyTorch JIT deprecation warnings. This ordering fix does not weaken either gate and changes no active training or data process.
- 2026-08-16 Stage211 final language-macro presentation: the immutable final result already contains all five source-replayed dataset metrics at Calibration, Layer, Block, Logits, and SFT, but the requested English-WER and Chinese-CER progress should not require manual aggregation. Add two derived summaries: English WER over the three canonical English datasets and Chinese CER over the two canonical Chinese datasets, each explicitly labeled `unweighted_dataset_macro` because the bound comparison reports expose exact per-dataset error rates but not pooled reference-unit counts. For Nano and every ordered stage, compute the arithmetic mean only from the already validated per-dataset rows; bind dataset names/count, sample-count sum, metric, language, and all five stage values in JSON, and render a compact macro table before the detailed dataset table. Do not call this a micro WER/CER or use it to replace any per-dataset gate. The final completion proof must continue to require all five underlying datasets and exact stage order.
- 2026-08-16 Stage211 final language-macro verification: the stepwise builder now emits exactly `english_wer` and `chinese_cer` summaries with `3/2` datasets, `20,481/31,950` utterances, Nano plus Calibration/Layer/Block/Logits/SFT values, and the explicit `unweighted_dataset_macro` aggregation label; tests independently recompute every value from the five bound dataset rows. Markdown renders the two-row macro table before the unchanged per-dataset table. The continuation watcher requires both fully populated summaries and rejects an otherwise complete old report that omits them. Focused builder/watcher tests pass `4/4`; the complete Stage211 suite passes `229/229` in `179.66 s`, and Ruff, Python compilation, Bash syntax, and diff checks pass. This presentation/proof change does not affect training or any phase gate.
- 2026-08-16 Stage211 production retention-replay readiness replay: rerun the independent validator at CPU nice `19` and idle I/O priority over the immutable prepared correction data. It rehashes and parses the full selected output, validates exactly `1,000,148` unique utterance keys, all seven Easy/Medium/Hard/Long language cells, per-source/per-bucket quotas and capacities, deterministic row scores, output-part identities, fixed and stratified evaluation exclusions, and the `500,000 EN : 500,148 ZH` balance. Validation completes successfully with receipt SHA-256 `0b1e389560b441d1a78f4cc5c26c5aa4693f8e476e575babc850fb7b42257749`. The artifact remains preparation only: admit it after complete Layer Hard/Long/Supplemental coverage and a complete failed hidden/public gate, then train one full round before reevaluation. No replay training, GPU process, USB audio read, or active-process change occurs during this audit.
- 2026-08-16 Stage211 long-lived phase manifest revalidation: an A/B/C full phase can span weeks, so a single loaded-manifest check before its Easy smoke is not sufficient evidence that a later Medium/Hard/Long launch still consumes the admitted file. Retain the entry-time deep receipt/global-dedup/part-metadata validation, keep its validated payload in memory, and immediately before each of the four original segment launches plus the Supplemental launch rehash all four small runtime manifest files against that payload. The per-segment runner continues to audit the selected manifest's root, length index, part paths/counts, rows, steps, and tail padding. This closes post-preflight manifest drift without repeatedly hashing audio or large bucket-part contents. A changed manifest must fail before the next segment's DeepSpeed command; completed immutable receipts are not rewritten. The newly loaded behavior applies to future controllers only and must not signal the resident legacy Hard process.
- 2026-08-16 Stage211 long-lived phase manifest revalidation verification: AST-level control-flow coverage proves both runner call sites are preceded by a receipt-bound manifest revalidation: one inside every Easy/Medium/Hard/Long iteration and one before Supplemental. Existing synthetic identity tests still accept the exact four-way map and reject swapped or changed Hard manifests; dry-run behavior remains unaffected. Focused tests pass `3/3`, the complete runner suite passes `112/112` in `154.96 s`, and Ruff, Python compilation, and diff checks pass. This change performs only small manifest hashes between future segments and does not touch active training.
- 2026-08-16 Stage211 fixed-sentinel scope diagnosis: the active Hard run's fixed `256`-utterance sidecar is not a Hard-distribution or public-WER/CER sample. A full structured replay of its immutable JSONL proves `256/256` unique `easy_zh` rows, split between AISHELL-3 (`117`) and Common Voice Chinese (`139`). At Hard step `770,000`, all `70/70` taps regress against the Hard-entry checkpoint on this Easy-Chinese retention sentinel; mean tap loss/cosine/RMS ratio moves from `0.103155/0.963314/0.984476` to `0.179042/0.940363/0.866591`, with the `60-69` band loss increasing `143.7%`. This does not imply Hard alignment itself failed: the existing seven-cell stratified step-114k evaluation improved Hard EN/ZH and Long ZH while regressing Easy EN/ZH and Medium ZH, which identifies cross-curriculum forgetting and amplitude shrinkage as the primary observed failure mode. The prepared replay covers `1,000,148` rows and `2,218.450178 h`, balances EN/ZH at `500,000/500,148`, includes all seven cells, globally shuffles length-bucket steps each epoch, and interleaves `cell:source` streams within buckets; retain it as the first post-coverage correction rather than changing the resident Hard objective mid-run.
- 2026-08-16 Stage211 fixed-sentinel telemetry provenance contract: preserve `fixed_eval_scope=fixed_hidden_not_public_wer_cer`, but future hourly monitor output must distinguish the canonical Easy-Chinese retention sentinel from an unknown fixed sidecar. Read the first eval-part path and SHA-256 from `step_eval_baseline.yaml`, require the canonical fixed-sidecar path and receipt-bound SHA-256, then independently hash the small part file before emitting `sentinel_cell=easy_zh`, `sentinel_language=zh`, and `sentinel_sources=aishell3,commonvoice_cn`. Emit `sentinel_cell=unknown` when any binding is absent or different. This is scope metadata only: it must not infer WER/CER, alter the loss trend, read audio, invoke model evaluation, restart the resident monitor, or affect a phase gate.
- 2026-08-16 Stage211 fixed-sentinel telemetry verification: focused tests accept a path/SHA/content-bound canonical fixture, reject the same YAML binding after the part content changes, preserve unknown-sidecar and malformed-step behavior, and pass `3/3`. The complete runner suite passes `113/113` in `159.19 s`; the complete Stage211 suite passes `231/231` in `184.95 s`, with only `14` upstream PyTorch JIT deprecation warnings. Bash syntax, Ruff, and diff checks pass. A production function-only replay reads no audio and renders `sentinel_cell=easy_zh sentinel_language=zh sentinel_sources=aishell3,commonvoice_cn`, step `770,000` loss `0.1827020436` versus baseline `0.1052601582` (`+73.5719%`, regressed). Do not restart the resident hourly monitor solely to deploy this metadata; the existing loss telemetry remains valid until its next controlled lifecycle handoff.
- 2026-08-16 Stage211 continuation curriculum admission: the continuation watcher must not route a merely field-shaped Mixer summary to `post_mixer`. Before that transition, invoke a CPU-only curriculum validation path which deep-validates the complete Mixer `curriculum_complete.json`, all five Easy/Medium/Hard/Long/Supplemental three-epoch coverage receipts, checkpoint chain, Nano teacher binding, runtime epoch evidence, parameter-delta policy, and Supplemental inventory provenance without creating an evaluation directory or launching hidden/public GPU evaluation. Route to `post_mixer` only when that validation passes; route legacy four-segment, forged, incomplete, changed, or unavailable summaries to `full`, where the restart-safe controller can reuse valid immutable receipts and finish missing coverage. Preserve the existing precedence of deeply validated Mixer/Block/Logits selections. This change is future control-plane behavior only and must not restart or signal the resident Hard training, supervisor, monitor, watcher, or Supplemental audit.
- 2026-08-16 Stage211 continuation curriculum-admission verification: add `finalize_stage211_phase.py --validate-curriculum-only` as the no-evaluation validator and require its success in the watcher before `post_mixer`. Focused tests prove that the CLI calls only curriculum resolution, a valid deep result routes to `post_mixer`, a validator failure routes even a field-complete Supplemental summary to `full`, and the legacy four-segment summary remains `full`; they pass `5/5`. The complete runner suite passes `115/115` in `157.62 s`, and the complete Stage211 suite passes `233/233` in `183.01 s`, with only the existing `14` upstream PyTorch JIT deprecation warnings. Ruff, Bash syntax, and diff checks pass. A production function-only replay with the currently incomplete Mixer artifacts returns `full`, which is the required recovery route after the resident legacy four-segment controller exits. No training, supervisor, watcher, monitor, or data-audit process is restarted or signaled.
- 2026-08-16 Stage211A Hard epoch-benefit diagnosis at live step `775,260/966,315`: parse the uninterrupted production log for the exact `mixer/full_hard/stage211_mixer_hard_full_966315steps.yaml` run, de-duplicate repeated attempt telemetry by global optimizer step, and compare the same completed `131,150`-step prefix length across epochs. Mean online Mixer loss/cosine/RMS ratio moves `0.111308/0.960162/0.996994` in epoch 1 to `0.099166/0.964517/0.997093` in epoch 2 and `0.092990/0.966736/0.997236` in epoch 3. Thus epoch 2 reduces prefix loss by about `10.91%` versus epoch 1, and the partial epoch 3 reduces it by another `6.23%` versus epoch 2. All seven layer bands observable at ten-step telemetry (`2-9`, `12-19`, `22-29`, `32-39`, `42-49`, `52-59`, `62-69`) improve both loss and cosine from epoch 2 to epoch 3; no hidden match is missing and RMS amplitude remains near one on current Hard batches. Keep the exact three-epoch Hard run unchanged. This current-domain convergence does not overturn the independently proven Easy-ZH retention regression; evaluate all seven cells after complete Hard/Long/Supplemental coverage and use the receipted balanced replay if that gate fails.
- 2026-08-16 Stage211 Supplemental-aware retention and hidden-gate contract: once `supplemental_natural` is a mandatory fifth three-epoch segment, the historical seven-cell sidecar and original-only replay cannot by themselves prove that a post-coverage correction retained the new natural English and social Chinese domains. Preserve the immutable validated `retention_replay_v1` and seven-cell sidecar byte-for-byte; after the combined-v2 Supplemental inventory and profile receipt validate, build a separately receipted schema-v2 extension. Deterministically withhold `256` rows each for `supplemental_en` and `supplemental_zh`, balanced over source and 80-frame bucket strata, and exclude those keys plus every historical fixed/stratified evaluation key from correction training. Add `130,000` unique replay rows from each Supplemental language, again source/bucket balanced and without replacement, then compose them with the existing `1,000,148`-row replay into exactly `1,260,148` rows with `630,000` English and `630,148` Chinese samples. The resulting hidden gate has exactly nine cells and `2,304` rows. Its receipt must bind the validated v1 receipts, combined-v2 inventory/profile/manifest, all selected source and output part identities, exclusion/key/row digests, quotas, capacities, language/source/bucket counts, and hours. Future Layer/Block/Logits finalizers and bounded correction loops must default to these v2 artifacts and fail closed on seven-cell evidence; after any Supplemental-trained checkpoint, a correction manifest that contains only original curriculum data is inadmissible because it can erase Supplemental alignment. Keep all nine cells as retention evidence and continue to require the independent five-dataset normalized English-WER/Chinese-CER public gate before promotion. This preparation is CPU-only and must not restart, signal, or reconfigure the resident Hard training or the sequential Supplemental audit.
- 2026-08-16 Stage211 Supplemental-aware retention verification and deployment: implement a separate schema-v2 builder and independent validator while retaining the v1 builder and artifact contract unchanged. Synthetic end-to-end coverage proves deterministic nine-cell `2,304`-row evaluation selection, v1 plus Supplemental replay composition, language/source/80-frame-bucket quotas, all evaluation and replay exclusions, public validator dispatch, and output-part tamper rejection; the complete Stage211 suite passes `234/234` in `183.38 s` with only `14` upstream PyTorch JIT deprecation warnings. A production revalidation of immutable replay v1 still accepts exactly `1,000,148` unique rows with receipt SHA-256 `0b1e389560b441d1a78f4cc5c26c5aa4693f8e476e575babc850fb7b42257749`, and its builder SHA-256 remains `41d717d81caf919500d9ad736fd56d63ac7c72138612e010b34b59267a829f1a`. Launch tmux session `rwkvasr_stage211_supplemental_retention_v2` at `2026-08-16 07:57 CST`; it checks hourly for the fail-closed combined-v2 inventory/profile, then builds and independently validates both v2 receipts under low CPU/I/O priority and an exclusive file lock. At launch the prerequisite exact-PCM audit is still incomplete at `984/4,172` archives, so no v2 output is admitted early. The resident four-rank Hard training, supervisor, continuation watcher, hourly monitor, and sequential audit are not signaled or restarted.
- 2026-08-16 Stage211 normalized public baseline: freeze the exact five-dataset `ctc`-normalized comparison already bound by the Nano provenance and Calibration reuse receipts. FunASR-Nano CTC scores `7.9028%/11.9636%/21.8692%` WER on LibriSpeech test-clean/test-other/Common Voice 22 English and `4.1827%/8.6310%` CER on AISHELL-1/WenetSpeech TEST_NET; the unweighted dataset macros are `13.912%` English WER and `6.407%` Chinese CER. The Stage211 Calibration checkpoint scores `28.0337%/50.8368%/57.5563%` WER and `57.2558%/98.3945%` CER on the same manifests, with unweighted macros `45.476%` and `77.825%`. Prediction coverage is exactly `2,620/2,939/14,922/7,176/24,774` utterances with zero normalized-reference mismatch. These are the immutable start-of-chain values, not a claim about the still-running Mixer checkpoint; rerun the same full comparison after each completed Mixer, Block, Logits, and SFT gate.
- 2026-08-16 Stage211 Supplemental retention capacity preflight: use the immutable component inventories and bucket manifests, without reading audio, to prove that the pending combined-v2 artifact can satisfy the nine-cell/replay contract after final exact-PCM admission. Natural English contains `6,434,286` rows and `25,419.427943 h` across five sources and 26 nonempty 80-frame buckets; exact-filtered social Chinese contains `394,895` rows and `1,790.333506 h` across six sources and 38 nonempty buckets. Their expected union is `6,829,181` rows and `27,209.761448 h`, before any final base/public exclusions. Running the production capacity allocator at `130,256` rows per language (the `130,000` replay target plus `256` held-out evaluation rows) succeeds across every nonempty bucket: English quotas consume all `9,937` LJSpeech rows and distribute approximately `30.1k` to each remaining source, while Chinese quotas consume the two small `369/648`-row sources and distribute approximately `32.3k` to each remaining source. This proves ample aggregate and stratum capacity; the main Supplemental phase must still train all admitted combined rows for three epochs, while `130,000` per language is only the bounded post-coverage correction subset. Final artifact creation remains blocked on the complete base/public PCM audit and must recompute capacities after its exclusions.
- 2026-08-16 Stage211 bilingual public-progress gate: evaluating all five datasets is insufficient if Mixer or Block can trade a regression in one language for a larger gain in the other. For each of these two intermediate promotion gates, independently aggregate the three English WER datasets and the two Chinese CER datasets. Require both language macros to strictly reduce error rate and deletion rate relative to that phase's bound initialization, while retaining the existing per-dataset absolute regression ceiling of `0.03`; consequently each language must contain at least one improved dataset. Preserve the overall five-dataset macro values for reporting, but do not use a cross-language average as the promotion decision. Logits and SFT continue to require the stronger every-dataset Nano proximity and no-regression contracts respectively. Deep gate validation must replay these bilingual summaries from the bound manifests and prediction JSONL rather than trusting recorded values.
- 2026-08-16 Stage211 bilingual public-progress verification: the shared public-progress builder now emits independently bound `en/wer` and `zh/cer` summaries with dataset identities/counts, error/deletion macros, improved-dataset counts, and per-language decisions. A focused regression proves that five-dataset aggregate improvement no longer passes when only Chinese improves and English is flat; simultaneous bilingual improvement passes, and an individual dataset beyond the `0.03` regression ceiling still fails. Because phase-gate validation recomputes both the baseline and candidate benchmarks from their exact manifest/Nano/student JSONL before rebuilding the progress object, changing or omitting a language summary is rejected. Focused tests pass `2/2`; the complete Stage211 suite passes `234/234` in `184.22 s` with the existing `14` upstream PyTorch JIT deprecation warnings. This future promotion change does not alter the resident Hard training or any current runtime process.
- 2026-08-16 Stage211D full-labeled expansion contract: the historical `285,302`-utterance LibriSpeech/AISHELL-3 pronunciation stage remains an immutable readiness baseline, but it is no longer sufficient as the formal interpretation of "all available labeled data." Prepare a schema-v2 Stage211D corpus from every one of the `1,430,801` rows in the already filtered `clean_ctc_voxbox_webdataset` root: LibriSpeech `222,040`, AISHELL-3 `63,262`, Common Voice English `1,112,745`, and Common Voice Chinese `32,754`. The source filter already excludes source-declared `dev/eval/test/valid/validation` splits; the v2 proof must bind that filter receipt and independently reject any admitted row carrying one of those split labels. Retokenize every row with the exact SenseVoice tiktoken asset, `ctc` text normalization, and `sensevoice_lfr6` frame accounting; reject empty, unknown, project-blank, tokenizer-special/non-pronunciation, duplicate, or frame-infeasible targets. Do not assume all rows survive: freeze the exact accepted and rejected counts, source/language/split distribution, hours, CTC-token total, optimizer-step count, tail padding, source filter, tokenizer hash, output index, bucket manifest, and preparation log in an immutable data-profile receipt after the full scan. The formal SFT controller must consume only a deeply validated schema-v2 receipt at `${HOME}/rwkvasr_data/stage211_sft_full_labeled_v2/stage211_labeled_profile_receipt.json` and must not retain hard-coded legacy `285,302/12,045` completion constants. Use one exact traversal of every accepted unique training row. Assign accepted rows deterministically to `45` source lanes (`aishell3:2`, `commonvoice_cn:1`, `commonvoice_en:35`, `librispeech:7`) and bind `stage211_sft_interleave_lane` as the bucket source field, so round-robin lane reads preserve the approximately `14:1` raw English/Chinese exposure while avoiding long monolingual stretches; this changes ordering, not the all-row exposure count. If the complete five-dataset post-SFT gate shows bilingual regression, use a separately receipted source-balanced labeled correction round and reevaluate, but never replace or claim the mandatory all-row traversal with a sampled subset. This data preparation is CPU/I/O only, must run at low priority, and must not signal or reconfigure the active Stage211A training or its teacher audit.
- 2026-08-16 Stage211D full-labeled implementation verification: preparation publishes the length index atomically, fails closed on metadata/tokenization errors, records every source/language/drop reason, and at that revision verified the then-current `3,629`-token non-pronunciation fingerprint; this support definition is explicitly superseded by the byte-fallback correction and canonical `2,114`-ID fingerprint recorded below. The schema-v2 audit independently rescans every accepted row, rejects duplicate IDs and invalid/infeasible targets, and requires all 45 lane totals to agree across the preparation summary, length index, and bucket manifest. The formal controller and completion/final-report validators derive coverage and terminal step from the immutable profile instead of the legacy constants; the resident bootstrap's future SFT invocation defaults to the v2 root and explicitly supplies the profile. Focused tests pass `24/24`; the complete Stage211 suite passes `234/234` in `176.19 s`, with only the existing `14` upstream PyTorch JIT deprecation warnings. Bash syntax, Ruff, Python compilation, and diff checks pass. Launch the low-priority production scan in tmux session `rwkvasr_stage211_sft_full_labeled_v2_prepare` at `2026-08-16 08:39 CST`; its actual command binds all strict options and writes only under `${HOME}/rwkvasr_data`, while the active four-rank Hard Mixer training retains its original PIDs and configuration.
- 2026-08-16 Stage211D tiktoken byte-fallback correction: the first full-labeled scan exposed that treating every mergeable token whose isolated decode contains U+FFFD as non-pronunciation is invalid for byte-level BPE. Rare but valid Chinese characters such as `溦` and `捘` span multiple mergeable byte tokens; suppressing any member corrupts otherwise exact CTC labels and caused three valid AISHELL-3 rows to be dropped in the first `25,000` scanned rows. Preserve every mergeable byte token that may compose into valid UTF-8, including tokens whose isolated decode is U+FFFD. Continue to suppress tokenizer special/timestamp/control IDs and mergeable tokens whose isolated complete text normalizes to empty pronunciation content, such as whitespace or punctuation. Decode the complete filtered token sequence first and remove only residual U+FFFD from the final string so unmatched predicted byte fragments cannot enter normalized transcripts. Recompute and freeze the Stage211D suppression count/fingerprint from the canonical tokenizer after this correction, update all student/teacher support gates and the continuation watcher, and restart the schema-v2 full scan from zero because artifacts produced under the old support are inadmissible. Add synthetic isolated-byte and canonical rare-Chinese round-trip regressions. This correction is limited to future labeled-data/SFT preparation and must not signal, restart, or reconfigure the active four-rank Stage211A Hard Mixer process.
- 2026-08-16 Stage211D tiktoken byte-fallback verification: the canonical tokenizer now resolves exactly `2,114` suppressed IDs with ordered SHA-256 `76a68d03bb2dc486c214fd44891f3e3e2c36286d79fea1e69c8e74d7767d5a09`; Nano blank `60514` remains suppressed and student blank `60515` remains available. Canonical encodings for `溦` (`15723,99`) and `捘` (`162,7959`) round-trip exactly and have no intersection with the corrected suppression set, while an isolated invalid byte decodes to an empty final transcript rather than U+FFFD. Focused tokenizer, length-builder, runner, and report tests pass `27/27`; the complete Stage211 suite passes `234/234` in `170.21 s`, with only the existing `14` upstream PyTorch JIT deprecation warnings. The first schema-v2 scan is discarded and the replacement must start from zero under this exact support contract.
- 2026-08-16 Stage211D corrected full-scan launch: start the replacement low-priority scan from zero at `09:17 CST` in tmux session `rwkvasr_stage211_sft_full_labeled_v2_prepare`, followed by fail-closed profile finalization in `rwkvasr_stage211_sft_full_labeled_v2_finalize`. The command binds all `1,430,801` source rows, the canonical tokenizer, corrected pronunciation support, CTC normalization, SenseVoice LFR6 feasibility, all five rejected source-split labels, the deterministic 45-lane interleave, and `OVERWRITE=1`; no old temporary length rows or profile receipt may be reused. The active Stage211A Hard Mixer rank PIDs and training configuration remain unchanged.
- 2026-08-16 Stage211 continuation watcher runtime refresh: the resident `2026-08-09` supervisor launched its in-memory Mixer controller without Supplemental inventory/profile arguments and therefore can finish only the historical Easy/Medium/Hard/Long four-segment summary. Preserve that uninterrupted Hard/Long work, but require the current deep five-segment curriculum validator to reject the incomplete summary and route the replacement controller through `full`, where immutable completed receipts are reused and the missing Supplemental three-epoch segment is executed before Mixer evaluation. Transactionally replace only the sleeping continuation watcher at `09:21:53 CST` so it loads script SHA-256 `91281d4caef7d7f5ae88f78f259b29ad433eb383c21511d06c52760b66414a5a`, the nine-cell/five-segment contract, and the corrected `2,114`-ID SFT support. The supervisor session is not signaled, and the four active Hard rank PIDs remain exactly `250599,250600,250601,250602`; process-count checks before and after replacement agree.
- 2026-08-16 Stage211D labeled-correction contract: the mandatory Stage211D result remains one exact traversal of every accepted unique row in the schema-v2 `1,430,801`-input profile; no correction subset may replace, shorten, or be reported as that all-row coverage. If and only if the complete five-dataset post-SFT gate fails, derive a separately receipted correction profile from accepted `train` rows only. Select all accepted Chinese rows, then select exactly the same number of English rows without replacement by deterministic SHA-256 ranking, allocating English capacity as evenly as possible across Common Voice English and LibriSpeech and across 80-frame buckets. Preserve the original CTC-normalized labels, source audio bindings, tokenizer/suppression support, source-filter exclusion proof, and unique IDs; exclude every schema-v2 `eval` row from correction selection and training exposure, and reject unknown sources/languages, duplicate IDs, malformed targets, or a correction profile that is not exactly language balanced. The correction bucket manifest may bind and reuse the unchanged schema-v2 eval parts only as a read-only step-evaluation monitor; its receipt must prove exact part/hash/count identity with the full profile, train/eval key disjointness, and exclusion from correction sample and optimizer-step accounting. Each correction round is one complete traversal at a lower `1e-7` learning rate with the same CTC plus Nano alignment objectives and TimeMixer-only trainable boundary, no early stopping, at most three rounds. After every round, recompute all three English WER and both Chinese CER datasets against the unchanged Logits checkpoint and Nano baselines. Promotion requires every-dataset Nano proximity, no dataset regression versus Logits, and bilingual macro improvement. The final report must bind the original all-row completion plus every correction data/run receipt in order; a correction checkpoint without the original full SFT proof is inadmissible.
- 2026-08-16 Stage211D labeled-correction implementation verification: the correction-profile builder now performs a fail-closed two-pass deterministic selection, balances all accepted Chinese rows against English quotas across both English sources and 80-frame acoustic buckets, invokes the standard Rust bucket-index builder, preserves the full profile's eval parts only as an exact-hash read-only monitor, and publishes an immutable receipt binding selected keys, row hashes, source/language/bucket counts, tokenizer support, source filtering, manifest parts, and optimizer-step accounting. The round runner deeply validates the full-SFT failure and preceding round chain, executes an exact two-step smoke followed by one complete `1e-7` TimeMixer-only epoch with the unchanged CTC plus Nano objectives, and proves terminal checkpoint step, coverage, telemetry, and allowed parameter deltas. The evaluator recomputes all five public datasets after every round and requires no per-dataset regression against the unchanged Logits baseline plus strict improvement of both the three-dataset English WER macro and two-dataset Chinese CER macro; the bounded three-round loop can advance only from the preceding failed correction checkpoint. Final-report validation rejects any post-SFT checkpoint without the ordered, hash-bound correction profile/run/evaluation evidence. The shared post-coverage correction configuration now explicitly accepts only its phase-specific lowered learning rate, fixing the prior latent failure in Mixer, Block, and Logits correction runners without relaxing ordinary training configs. Profile and runner tests pass `5/5`; the complete Stage211 suite passes `239/239` in `177.18 s`, with only the existing `14` upstream PyTorch JIT deprecation warnings. Ruff, Python compilation, shell syntax, and diff checks pass. These future artifacts are gated behind the still-running Layer, Block, Logits, and complete full-labeled SFT stages and do not signal or reconfigure the active Hard Mixer ranks.
- 2026-08-16 Stage211 final-report automatic closure contract: a successful SFT path is not complete until the immutable stepwise report has been rebuilt and deeply validated over the exact `rwkv_layer -> block -> logits -> sft` checkpoint, coverage, Nano-initialization, label-normalization, metric-definition, Supplemental-dedupe, and five-public-dataset WER/CER chains. The bootstrap must invoke the stepwise builder immediately after either direct full-SFT admission or a passing balanced correction round, selecting the exact successful final report and writing JSON/Markdown beside it. Preserve the failed direct-SFT report when correction is required; the corrected report remains under `sft_corrected/` and must not overwrite that failure evidence. The continuation watcher must independently consider both locations, prefer a deeply valid direct report when one exists, otherwise accept only a deeply valid corrected report, and write/revalidate the matching stepwise artifacts. It must never restart SFT merely because a passing corrected report is outside the direct report directory. Add executable regressions for direct and corrected success selection plus static bootstrap ordering. This closure change is future orchestration only and must not signal, restart, or reconfigure active training ranks.
- 2026-08-16 Stage211 final-report automatic closure verification: the bootstrap now runs the immutable stepwise builder immediately after either direct SFT admission or corrected SFT admission, explicitly binding the initialization, calibration, Unicode metric-correction, selected final report, and colocated JSON/Markdown outputs; it fails if the successful final report or either output is absent. The continuation watcher now deep-validates the direct candidate first and the corrected candidate second, rebuilding and applying the complete strict-stage/coverage/dedupe/label/metric/bilingual schema to the matching output directory before declaring completion. An executable shell regression simulates direct SFT failure, preserves its failed report, produces a passing corrected report, and proves `finalizer -> correction -> corrected stepwise` ordering; a second regression proves the watcher accepts that corrected proof without launching another supervisor. Direct-path and narrow-restart regressions remain passing. Focused orchestration tests pass `17/17`; the complete Stage211 suite passes `241/241` in `174.44 s`, with only the existing `14` upstream PyTorch JIT deprecation warnings. Bash syntax, Ruff, and diff checks pass. Refresh only the sleeping continuation-watcher process after this revision; preserve the active supervisor, training ranks, hourly monitor, and data preparation sessions.
- 2026-08-16 Stage211 final-report watcher runtime refresh: after pushing commit `844c9f1`, replace only tmux session `rwkvasr_stage211_abcd_continuation_watch` at `10:28:45 CST` with the same `POLL_SECONDS=3600` and `RESTART_BACKOFF_SECONDS=3600` settings. The new watcher PID is `1155779` and its loaded script SHA-256 is `7f7174f111ccf877604b17356c9e13e519dbfb0c363595476478fcdc5a0081ac`; its first log record confirms the existing supervisor remains active and schedules the next hourly check. The four Hard Mixer rank PIDs before and after replacement are exactly `250599,250600,250601,250602`. No supervisor, optimizer, checkpoint, data order, hourly monitor, SFT scan, or Supplemental audit process was signaled or reconfigured.
- 2026-08-16 Stage211 final alignment-metric disclosure contract: deep phase-gate validation proves that the bound hidden/logits reports are internally valid, but a final result containing only `gate=pass` and public WER/CER is insufficient to report how each requested alignment stage performed. Extend the immutable stepwise JSON and Markdown with concise, hash-bound alignment results for Mixer, Block, and Logits after their existing phase-gate replay succeeds. Mixer must disclose fixed and nine-cell mixer loss/cosine, improved-layer counts, and weak-band evidence. Block must disclose the same evidence for Mixer, FFN, and whole Block plus decoder-hidden loss. Logits must disclose fixed and nine-cell full KL, conditional-nonblank KL, CTC token error, top-1 agreement, KL reductions, and retained Mixer/FFN/Block/decoder hidden summaries. Include all nine cell names, sample counts, baseline/candidate values, selected gate decisions, source report path/hash, and checkpoint pair; do not copy unbounded per-frame logits or claim SFT has a hidden-alignment objective. SFT remains represented by exact labeled coverage plus its five-dataset WER/CER result. The continuation watcher must require all three disclosed alignment stages in addition to the existing strict checkpoint/coverage/public-metric proof.
- 2026-08-16 Stage211 final alignment-metric disclosure verification: the immutable stepwise JSON now publishes ordered Mixer, Block, and Logits alignment records with each bound source report/hash and checkpoint pair, fixed `256`-sample evidence, exact nine-cell `2,304`-sample evidence, selected gate decisions, component weak bands, per-cell summaries, and concise objective metrics. Markdown renders fixed and nine-cell hidden tables, fixed and nine-cell Logits tables, retained Logits hidden components, decoder-hidden comparisons, and alignment evidence hashes before the existing coverage/checkpoint sections. The builder rereads and hashes each disclosure source after the complete phase-gate replay; source mutation is rejected. Three production-schema regressions construct and deep-validate real Mixer/Block/Logits phase gates before extracting disclosures, while the final-report regression checks metric values, all Markdown sections, and tamper rejection. The continuation watcher now requires exactly the ordered three disclosures, passing fixed/nine-cell gates, `256/2,304` sample counts, nine cell names, and 64-character source hashes; a missing disclosure regression launches recovery instead of accepting completion. Focused report/watcher tests pass `19/19`; the complete Stage211 suite passes `245/245` in `220.26 s`, with only the existing `14` upstream PyTorch JIT deprecation warnings. Ruff, Bash syntax, Python compilation, and diff checks pass. Refresh only the sleeping watcher after this revision so the runtime completion audit loads the stronger schema.
- 2026-08-16 Stage211 alignment-disclosure watcher runtime refresh: after pushing commit `2a56f0d`, replace only `rwkvasr_stage211_abcd_continuation_watch` at `10:48:44 CST` with unchanged hourly poll/backoff settings. The new PID is `1160608` and loaded script SHA-256 is `5e8ae1adaf6595e926fb795c9f9835ab05e211cd25cf4fbe3d9f8ce1febb585d`. Its first record confirms the existing supervisor remains active. Training rank PIDs remain exactly `250599,250600,250601,250602`, and supervisor PID `249742` remains the same with increasing elapsed time before and after refresh. No training or data process was signaled.
- 2026-08-16 Stage211 Supplemental retention idempotent handoff contract: the social post-materialization pipeline and the independent retention watcher are intentional redundant producers so either can close the combined-v2 handoff after a restart. Keep the existing exclusive `${METADATA_ROOT}/.stage211_supplemental_retention_v2.lock`, but once a producer acquires it, first attempt to reuse a complete v2 result instead of rescanning all Supplemental manifests. Reuse is admissible only when both the nine-cell hidden-eval receipt and the composed replay receipt pass their full independent validators and their bound builder, v1 replay, v1 stratified, combined-v2 inventory, and combined-v2 profile paths and SHA-256 values exactly match the current invocation. Any missing, stale, tampered, or differently bound artifact must fall back to the full deterministic build and validation path. This optimization changes neither the required `2,304`-row nine-cell evaluation, the `1,260,148`-row bilingual replay target, nor the five-segment three-epoch training and public WER/CER gates; it only prevents the second redundant producer from repeating a complete manifest scan after the first has committed valid outputs.
- 2026-08-16 Stage211 Supplemental retention idempotent handoff verification: the v2 builder now performs the reuse decision only after acquiring its existing exclusive lock. It deep-validates both receipts, verifies current base-replay/base-stratified and combined-inventory/profile paths and hashes, verifies the exact nine-cell receipt binding, and checks the requested per-language replay and per-cell evaluation counts before returning an existing result. A mismatch emits a visible rejection and enters the unchanged full deterministic builder. Synthetic end-to-end coverage proves successful reuse, count-mismatch rejection, output tamper rejection, and that the locked CLI path does not invoke the full builder after valid reuse. Focused Supplemental/controller tests pass `127/127`; the complete Stage211 suite passes `246/246` in `221.06 s`, with only the existing `14` upstream PyTorch JIT deprecation warnings. Ruff, Python compilation, and diff checks pass. The already running post-materialization shell and retention watcher need no refresh because each starts the Python builder as a future subprocess after combined-v2 admission, so both will load this revision while all training and data-audit PIDs remain uninterrupted.
- 2026-08-16 Stage211A Hard step-790k fixed sentinel: while the CPU-only handoff regressions ran, the uninterrupted four-rank Layer/TimeMixer job naturally reached live step `790,810/966,315` (`81.8377%`), with Hard epoch 3 at `45.5131%`, and committed a complete DeepSpeed/export checkpoint at step `790,000`. The current sample has mixer loss/cosine `0.1115/0.9597`, complete teacher matching, zero hidden missing events, and zero frame delta. The immutable 256-row historical Easy-ZH sentinel is `0.1835764558`, `74.4026%` above the bound Hard initialization `0.1052601582` and slightly worse than step-780k `0.1832833386`; this confirms the prior current-Hard convergence plus old-domain retention failure remains unresolved. Do not promote or substitute this checkpoint. Continue the exact Hard, Long, and Supplemental coverage, then require nine-cell retention correction and the complete normalized five-dataset WER/CER gate before entering Block. Rank PIDs remain `250599--250602`, supervisor PID remains `249742`, and no runtime process was signaled by the code/test work.
- 2026-08-16 Stage211A Mixer throughput diagnosis: at live Hard step approximately `792k/966,315`, bound W&B telemetry shows mean step time `0.6975 s`, Nano online-teacher time `0.0869 s`, effective local batch `14.57`, post-LFR padded frame budget `3,432`, peak reserved memory `7.99 GiB`, and an observed `23 GiB` memory estimate of approximately `9,623` post-LFR frames. Teacher-forced training already executes only the selected eight BiRWKV TimeMixers, and the deterministic `step * 8 mod 70` selector covers every layer; do not attribute the runtime to an accidental full 70-layer student forward or missing-layer bug. The active Hard segment is already about `82%` complete and its step-based epoch cursor is bound to the immutable `batch=36/frame_budget=24,000` coverage contract, so do not restart or change its batching in place. A separate same-objective throughput smoke must precede any larger-batch contract for future Mixer data, and receipts must continue to prove exact rows, three epochs, no dropped tails, and the actual batch profile. Independently, the concurrent base/public normalized-PCM audit was launched at default I/O priority despite sharing the rotational USB source with formal training, producing sustained `34%--54%` host I/O wait. A matched five-minute live comparison gives training/audit rates of `1.299 step/s` and `0.600 archive/min` at idle I/O versus `1.332 step/s` and `0.799 archive/min` at best-effort priority `7`; retain CPU nice `10` plus best-effort I/O priority `7` because it advances the Supplemental admission critical path without reducing measured training throughput. Changing process scheduling must not alter audit ordering, receipts, data, model state, or optimizer state.
- 2026-08-16 Stage211 batch-throughput preflight contract: do not promote a larger batch solely from free-memory estimates or instantaneous GPU utilization. Provide a standalone four-GPU preflight which starts every profile from the same immutable checkpoint, manifest, model objective, learning rate, and data order; changes only local batch size and frame budgets; disables W&B and formal step evaluation; and writes all temporary configs, logs, checkpoints, and a non-promotional JSON report under a dedicated probe root. Run a baseline plus one or more candidates for a caller-selected warmup and measured step window. The report must bind input file paths/hashes, git commit, exact commands/config hashes, expected full-epoch steps and tail padding, timestamped per-step loss/cosine/match/missing/frame-delta telemetry, process return status, and sampled per-GPU peak memory. A candidate is admissible for further design work only when all requested train steps are observed, losses are finite, every enabled teacher match is complete, all missing/frame-delta counters are zero, peak memory stays below the caller's limit, and projected complete-coverage wall time improves over baseline by the caller's minimum ratio. The probe itself must never modify Stage211 coverage constants, receipts, selection pointers, supervisor state, or a running formal process; execution must fail when any GPU already has a compute process. Mixer, stacked Block, and Logits require separate probes because their activation and vocabulary-memory profiles differ. Any formal batch-profile change still requires a subsequent Spec update, exact step/tail recomputation, backward-compatible receipt validation, and a fresh segment boundary rather than an in-epoch resume.
- 2026-08-16 Stage211 batch-throughput preflight implementation verification: commit `a461ba3` adds the standalone runner, clean-worktree and idle-GPU execution gates, per-profile immutable config/log/checkpoint layout, `nvidia-smi` peak-memory sampling, complete alignment-telemetry safety checks, full-manifest step/tail recomputation, projected-wall-time comparison, and an explicitly non-promotional selection result. Focused preflight and Supplemental scheduling tests pass `10/10`; Ruff, Python compilation, and diff checks pass. A production dry-run against the active Hard config and immutable step-794000 export completes without allocating a GPU and writes report SHA-256 `82d7c64faa8e48fa3eff9057bad2d6504fcf665d906796696fd943f3e19cf8da`. It binds clean commit `a461ba33167263f8b497e8f43767fe104879851a`, script SHA-256 `16f140f4191ce8a7064cd3230feba44d77cadef81a86635cbcb0607343c810d2`, checkpoint SHA-256 `8a5b58208c7d81ae836042b52b9ddab75f88166f191abc43038dcac8495b957a`, and Hard manifest SHA-256 `754d73f4b0bc4d1da9a8d10423a86960bc46f9bb796a25a8d9d910d0dc950977`. The baseline recomputes exactly `322,105` steps and `986` tail-padding rows per epoch (`966,315` steps over three epochs); `batch=48/frame_budget=42,000` recomputes `182,150` steps and `1,974` tail-padding rows per epoch (`546,450` steps over three epochs). The result remains `dry_run_only/formal_admission=false`; do not alter formal batching until a clean four-GPU measured report passes after the active ranks exit naturally.
- 2026-08-16 Stage211 post-preflight complete regression: rerun the full CPU-only `tests/test_stage211_*.py` selection at CPU nice `19` and idle I/O priority while preserving the active four-GPU Hard Mixer ranks. All `252/252` tests pass in `224.73 s`; the only diagnostics are the existing `14` upstream PyTorch JIT deprecation warnings. The persisted JUnit report records `tests=252`, `failures=0`, `errors=0`, `skipped=0`, and has SHA-256 `506f5f001c984fcf86d3bbe533ffc3a582a3102933baa4672aaacccd3b49df6a` at `${HOME}/rwkvasr_runs/stage211_test_reports/stage211_19a1b78.xml`. This closes the previously missing full-suite exit evidence for commit `19a1b78`; it does not admit the dry-run-only larger-batch profile or alter any training, checkpoint, data, or supervisor process.
- 2026-08-16 Stage211 expanded batch-profile dry run: bind clean commit `ede2d67bb1dc0fb3707c74449f123ba7c59b1593`, the unchanged Hard manifest SHA-256 `754d73f4b0bc4d1da9a8d10423a86960bc46f9bb796a25a8d9d910d0dc950977`, and immutable step-794000 export SHA-256 `8a5b58208c7d81ae836042b52b9ddab75f88166f191abc43038dcac8495b957a` in a three-profile coverage-only preflight. The existing `36/24,000` baseline remains `322,105` steps and `986` tail-padding rows per epoch; `48/42,000` remains `182,150` steps and `1,974` tail rows; the additional `64/56,000` candidate is exactly `135,825` steps and `2,542` tail rows. Across three epochs this is `966,315`, `546,450`, and `407,475` steps respectively, so the larger candidates reduce optimizer-step count by `43.45%` and `57.83%` without changing row coverage. The JSON report SHA-256 is `239656e4f40ccdde97189a50b858148515a2e39709d11f801e6bcb19cda54473`; it remains `dry_run=true/formal_admission=false`. Make these exact three profiles the benchmark runner defaults so an operator cannot accidentally omit the higher-throughput candidate; focused runner tests pass `6/6`, with Ruff, Python compilation, and diff checks passing. Commit `2aa9b2f212afc9f2e267c4a56a7876e138fa2cc2` then passes the complete Stage211 suite `253/253` in `222.54 s` with the existing `14` upstream warnings; its zero-failure/error/skip JUnit report has SHA-256 `1e4d69a42941fa0f83aa4e5a255cfb780cabe3cea5b498644128e69eea9591be`. When the current ranks exit naturally, measure all three profiles from one immutable checkpoint and admit only a numerically complete profile below the `22 GiB` peak-memory ceiling with at least `10%` projected full-coverage wall-time improvement.
- 2026-08-16 Stage211 strict public-metric execution audit: the production bootstrap cannot promote Mixer, Block, or Logits from a hidden/logits report alone. For each phase, the five-segment curriculum must first deep-validate; the finalizer then evaluates the bound baseline/candidate checkpoint on the fixed `256` rows and all nine `256`-row difficulty/language cells, runs direct CTC predictions on all five immutable public manifests, and source-recomputes `ctc`-normalized LibriSpeech clean/other/Common Voice English WER plus AISHELL-1/WenetSpeech TEST_NET CER against the unchanged Nano predictions. The four smaller public jobs occupy the four devices concurrently and WenetSpeech is sharded across all four devices. The correction loop creates a selection/promotion receipt only from a deeply valid passing phase gate. SFT invokes the same public-evaluation implementation and the final stepwise builder rereads every stage benchmark, requires exact manifest/Nano hashes and Nano error-rate identity, and emits both three-dataset English WER and two-dataset Chinese CER macros plus all five per-dataset values in strict `calibration,mixer,block,logits,sft` order. No formal Mixer/Block/Logits gate artifact exists yet, which correctly reflects the still-running Mixer coverage rather than missing evaluation wiring; do not report a stage metric until its immutable gate is present and passes replay validation.
- 2026-08-16 Stage211 objective-aware throughput safety contract: the batch preflight must derive its required online-teacher match fields from the immutable candidate config instead of treating `online_layer_match` as sufficient for every phase. Mixer requires its enabled Layer objective; Block additionally requires every enabled encoder and decoder-hidden objective; Logits must validate every enabled blank, conditional-nonblank, hard conditional, sequence, sequence-presence/window, nonblank/window/top-k, encoder, decoder-hidden, Layer, full-logit, or other online objective represented by its positive loss weights. Parse every named `online_*_match=count/total` field, retain the field identities in telemetry/report evidence, require every configured field to be present at every measured step with positive totals and complete counts, and reject unexpected missing enabled fields even when aggregate `*_missing` counters remain zero. Disabled objectives must not be required merely because their zero-valued counters are printed. Use one deterministic primary match field only for sample-throughput accounting; it must not weaken the all-enabled-field safety decision. Add Mixer, Block, and Logits config-mapping and partial-match regressions before using this preflight for formal profile selection.
- 2026-08-16 Stage211 objective-aware throughput safety verification: the preflight now parses every named online match field into each timestamped telemetry record, derives the exact required set from positive config loss weights, records that set per candidate, and reports missing and incomplete fields by measured step. Its safety decision requires every enabled field to be present with `count=total>0`; disabled printed counters are ignored, while sample throughput uses a deterministic primary match field. Regression coverage proves the production Mixer mapping, stacked Block encoder/decoder-hidden/Layer mapping, multi-objective Logits mapping, and simultaneous missing plus partial-match rejection. The production Hard dry run still resolves only `online_layer_match` for all three batch candidates and preserves their exact `966,315/546,450/407,475` three-epoch coverage counts. Focused tests pass `8/8`; the complete Stage211 suite passes `255/255` in `221.34 s` with only the existing `14` upstream warnings. The persisted JUnit report records zero failures/errors/skips and has SHA-256 `fe3b1df5e0c33fd08729e5d47aa96625066924a0f77bb68414285628420cd161`.
- 2026-08-16 Stage211 batch-throughput quality-equivalence contract: reducing complete-coverage optimizer steps is useful only when the larger batch preserves the short-run alignment signal from the same immutable checkpoint and data order. Extend profile selection to compare each otherwise safe candidate against the measured baseline, not merely require finite values. Require candidate mean alignment loss to stay within a caller-bound relative regression ceiling and mean Layer cosine to stay within a caller-bound absolute regression ceiling; default to at most `5%` loss regression and `0.005` cosine regression. Record both deltas, thresholds, and the quality decision for every candidate. A faster candidate that violates either threshold must not be recommended even if memory, match completeness, and projected wall-time gates pass. Keep the report non-promotional: this short probe detects gross learning degradation but does not replace fixed/nine-cell hidden/logits gates or the post-phase five-dataset WER/CER evaluation.
- 2026-08-16 Stage211 batch-throughput quality-equivalence verification: profile summaries now aggregate loss and Layer cosine by the deterministic primary-match sample count rather than giving tiny and full batches equal weight. Selection binds the baseline means, candidate loss/cosine deltas, default/caller thresholds, and a separate quality decision into every comparison; recommendation requires quality, complete objective matches, memory, process, and projected-wall-time gates simultaneously. A regression constructs a candidate with `50%` projected wall-time improvement but `20%` loss inflation and `0.02` cosine regression and proves it is rejected. Focused tests pass `9/9`; the production Hard dry run binds the `0.05/0.005` thresholds and unchanged three-profile coverage. The complete Stage211 suite passes `256/256` in `227.05 s` with only the existing `14` upstream warnings; its zero-failure/error/skip JUnit report has SHA-256 `7b78ee6f461c1c876c5916020b657a22714fb2cb4df3c1f62ee941f4c4d560bf`.
- 2026-08-16 Stage211 measured batch-profile admission design: the legacy `batch=36/world=4/frame_budget=24,000` contract remains the only admitted profile until a phase-specific four-GPU preflight passes all process, memory, complete-objective-match, quality-equivalence, and projected-wall-time gates. A future admitted profile must be represented by an immutable schema-versioned receipt binding the exact phase, selected profile values, preflight report path/hash, preflight git/script/base-config/init-checkpoint/manifest identities, complete candidate summary/comparison, thresholds, and an explicit review/admission decision; Mixer evidence cannot admit Block or Logits because their activation/objective memory differs. Admission applies only to a fresh segment with no existing optimizer/checkpoint state. Preserve every completed schema-v1 segment receipt byte-for-byte and continue validating it against the legacy profile. New schema-v2 segment receipts may bind an admitted profile receipt and must recompute steps per epoch and tail padding directly from their immutable runtime manifest; they must still prove every unique row exactly three times, no dropped tail, no skipped/decode-error rows, exact epoch-boundary checkpoints, and the same checkpoint/parameter/Nano chains. Full-phase validation must accept an ordered mixture of legacy schema-v1 and admitted schema-v2 segments without weakening either contract. Separate immutable data identity (rows, hours, parts, manifest hashes, dedupe) from profile-derived runtime accounting so the loaded-manifest and Supplemental data receipts do not need to be regenerated for every phase profile. The controller accepts no implicit environment override: an optional profile receipt must be supplied explicitly, deep-validated, match the current phase, and be recorded in generated config/provenance/segment/combined coverage. Absent that receipt, behavior and all current constants remain exactly legacy. Do not activate this path or change the resident Hard process until an actual idle-GPU report passes and the backward-compatible builder/runner/validator tests are complete.
- 2026-08-16 Stage211 measured batch-profile admission implementation: add a phase-specific deep validator and explicit immutable admission CLI for actual four-GPU preflight reports. The strict segment runner, receipt builder, full-phase controller, and full-data gate now carry an explicitly supplied per-difficulty admission through config, provenance, runtime step accounting, segment receipt, and combined coverage. Schema-v1 remains byte- and behavior-compatible with `36/24,000`; schema-v2 recomputes exact steps and tail padding from the bound manifest and validates the admitted report/checkpoint/manifest chain. The controller accepts repeatable `difficulty=/absolute/admission.json` arguments and does not infer a profile from environment variables or free memory. Dry-run and resume accounting use the selected complete-coverage step count, and old plus new segment exposure totals are aggregated dynamically. The complete Stage211 suite passes `262/262` in `224.02 s` with only the existing `14` upstream warnings; JUnit SHA-256 is `982e8951b9313b2e678ac81cdedd0b0fa398598f4ba2369000c98fe58620718e`. Ruff, Python compilation, and diff checks pass. No larger profile is admitted yet because all four GPUs remain occupied by the unchanged Hard job.
- 2026-08-16 Stage211 live USB-contention control: pause only the two background Python readers for approximately ten minutes while leaving rank PIDs `250599--250602` unchanged. The formal rate is approximately `790/589 s = 1.34 step/s`, effectively unchanged from the preceding approximately `1.33 step/s`; therefore the background tasks are restored at their prior low CPU/I/O priority instead of delaying Supplemental and labeled-SFT readiness. At `13:30 CST`, the uninterrupted job is at step `802,440/966,315` (`83.0412%`), Hard epoch 3 offset `158,230/322,105` (`49.1237%`), with current mixer loss/cosine `0.1003/0.9639`, complete `9/9` hidden matching, and `0.08 s` teacher time. The durable speed opportunity remains a larger measured frame budget at a fresh segment boundary; do not restart or remap the current Hard epoch cursor.
- 2026-08-16 Stage211 profiled Supplemental boundary-handoff design: the resident pre-admission full-phase controller would otherwise launch the legacy-profile Supplemental segment immediately after the `105`-step Long segment, leaving no idle four-GPU window for the required measured preflight. Arm a separate observational process-tree guard while Hard continues unchanged. Once the controller launches Long, stop only the waiting full-phase controller process; never signal the Long child, Hard/Long ranks, torchrun, optimizer, or checkpoint writers. After Long exits naturally, require its exact terminal checkpoint and create/reuse its schema-v1 coverage receipt, then wait for the combined-v2 Supplemental inventory/profile to validate. Generate a dry-run baseline config from that exact Long checkpoint, predecessor receipt, and exact Supplemental manifest; benchmark the legacy `36/24,000` profile and both `48/42,000` and `64/56,000` candidates on all four idle GPUs. Admit only a recommendation that passes the existing phase/objective/memory/match/loss/cosine/projected-wall-time contract. Launch an updated full-phase controller with an explicit `supplemental_natural=/absolute/admission.json` argument so completed schema-v1 segments are reused and only the fresh Supplemental segment uses schema-v2 dynamic accounting. Keep the stopped legacy supervisor session as an exclusivity lock until the profiled Mixer curriculum deeply validates; then replace it atomically with `START_STAGE=post_mixer`. Any missing/tampered receipt, non-idle GPU, dirty worktree, failed profile, failed controller, or unexpected legacy-controller state must fail closed and leave the stopped supervisor available for manual recovery rather than silently falling back to `36/24,000`.
- 2026-08-16 Stage211 profiled Supplemental boundary-handoff implementation: add a standalone handoff runner that validates the stopped legacy controller identity/state, exact Hard-to-Long checkpoint chain, immutable Long receipt, combined-v2 profile, generated baseline config, idle-GPU preflight, explicit admission, profiled full-phase replay, and final curriculum before replacing the supervisor at `post_mixer`. Existing complete preflight/admission files are deeply validated and reused; incomplete probe directories, ambiguous configs, failed recommendations, changed inputs, or missing tmux ownership fail closed. Command-construction regressions prove the exact predecessor checkpoint, all-three-profile default benchmark, fixed memory/quality thresholds, explicit Supplemental-only admission, and post-Mixer restart. The complete Stage211 suite passes `267/267` in `225.72 s` with only the existing `14` upstream warnings; JUnit SHA-256 is `2f399110cf85492c86af5752f9cc8dc0ddcf200d91d6ddfd1221016f702bdd67`. Ruff, Python compilation, and diff checks pass. The observational boundary watcher is armed while Hard ranks remain unchanged and will invoke this handoff only after Long exits naturally.
- 2026-08-16 Stage211 future-segment automatic profile contract: Block and Logits must not inherit a Mixer throughput admission because each phase has different enabled objectives, activation memory, and vocabulary traffic. Before every fresh Block or Logits `easy/medium/hard/long/supplemental_natural` segment, use the exact preceding checkpoint and exact segment manifest to generate the legacy baseline config, then measure the fixed `36/24,000`, `48/42,000`, and `64/56,000` profiles while all four GPUs are naturally idle at that boundary. A larger profile is usable only through the existing phase/checkpoint/manifest-bound immutable admission and must preserve exact three-epoch row coverage with dynamically recomputed steps and tail padding. A complete, replay-valid measurement that recommends retaining the safe legacy baseline is not an error and must continue the formal segment at `36/24,000`; it must never fabricate an admission. A failed, incomplete, changed, busy-GPU, or non-replayable measurement fails closed before formal optimizer state exists. Explicit caller-supplied admissions take precedence and disable automatic measurement for that segment. Completed or already-started segments are never reprofiled, remapped, or restarted. Enable this only for future Block and Logits controllers; do not signal, reload, or alter the resident Mixer controller or ranks.
- 2026-08-16 Stage211 future-segment automatic profile verification: the full-phase controller now recovers an admitted profile from immutable segment provenance on restart, otherwise generates and measures the exact baseline config only for a fresh Block or Logits segment. It revalidates the loaded original-data manifests before either the template probe or formal runner, reuses only a complete report bound to the same phase/checkpoint/manifest, admits the fastest replayed candidate only when every existing safety and quality gate passes, and continues at the legacy profile when a deeply replayed selection proves that no candidate is admissible. The measurement validator independently recomputes candidate improvement, loss/cosine regression, quality, admissibility, and fastest-candidate selection, including rejection of a forged `keep_baseline` decision that hides an admissible candidate. The bootstrap enables this path for Block and Logits only; Mixer remains unchanged. The complete Stage211 suite passes `271/271` in `222.95 s` with only the existing `14` upstream warnings; its zero-failure/error/skip JUnit report has SHA-256 `f354074d90fa6e8c76720b95f0b4cd9efa749db7fcb909cf2aba370d3be723bb`. Ruff, Python compilation, Bash syntax, and diff checks pass. During verification the resident Mixer advanced to step `805,350`, and rank PIDs `250599--250602` remained unchanged.
- 2026-08-16 Stage211 profiled-handoff retention barrier: combined-v2 creation immediately starts the Supplemental nine-cell and retention-replay builders, which read the same storage that the four-GPU batch benchmark needs for audio loading. The Mixer Supplemental boundary handoff must therefore not treat inventory/profile publication alone as the GPU preflight boundary. After validating the combined profile, wait for both `stratified_hidden_eval_v2/receipt.json` and `retention_replay_v2/receipt.json`, then execute their existing deep validators before generating the baseline config or launching any GPU probe. This serializes finite metadata preparation ahead of throughput measurement, removes avoidable I/O bias, and guarantees the later `post_mixer` supervisor will not wait on missing retention evidence. Missing, partial, stale, or invalid retention receipts remain a fail-closed wait/error and may not fall back to a smaller data contract. The builders already share an exclusive file lock, so the social pipeline and retention watcher may safely converge on one immutable result. This changes only the future Long-to-Supplemental boundary and does not signal or modify active Hard/Long ranks.
- 2026-08-16 Stage211 profiled-handoff retention barrier verification: the boundary runner resolves both receipt paths relative to the selected metadata root, waits for nonempty immutable files, invokes the stratified and replay deep-validation modes in order, and only then generates the Supplemental baseline config and starts the four-GPU profile benchmark. Command and source-order regressions cover both validators and the barrier-before-template/preflight invariant. Focused retention/handoff tests pass `8/8`; the complete Stage211 suite passes `272/272` in `225.04 s` with only the existing `14` upstream warnings. Its zero-failure/error/skip JUnit report has SHA-256 `256c8eb8fb484dc62aaead18b9bfc149e780a8b3232202ebba3a1e55ccaa0323`. Ruff, Python compilation, and diff checks pass. The resident Hard Mixer advanced to step `806,000` during verification with unchanged rank PIDs `250599--250602`.
- 2026-08-16 Stage211 requested-stage result contract: retain `calibration,mixer,block,logits,sft` as the internal compatibility order for existing runners, receipts, checkpoint links, and public-metric replay, but expose Calibration separately as the initial baseline and make the primary user-facing alignment result exactly `rwkv_layer -> block -> logits -> sft`. The `rwkv_layer` result must bind internal phase `mixer`; every requested-stage result must bind its exact checkpoint/report hashes and include finite normalized three-dataset English WER and two-dataset Chinese CER macro values reconstructed from the already validated five public datasets. Publish a second requested-stage per-dataset view whose stage keys are exactly the requested four names, and require the continuation watcher to reject any final proof with a missing, reordered, non-finite, or internally inconsistent requested-stage metric. This is an additive final-evidence change only; it must not rename runtime phases, rewrite old receipts, alter training objectives, or signal the active Stage211 process.
- 2026-08-16 Stage211 requested-stage result verification: the immutable JSON now separates `initial_calibration_result` from ordered `requested_alignment_results`, maps only `rwkv_layer` to internal `mixer`, binds each requested stage to its checkpoint and source-report SHA-256, and exposes its normalized English WER and Chinese CER macros. Parallel requested-name language-macro and five-dataset views retain the exact validated internal metrics without changing legacy receipt keys. Markdown leads with the requested four-stage order and a concise per-stage bilingual table. The continuation watcher independently requires exact stage/internal order, finite numeric bilingual values, four-stage dataset keys, 64-character hashes, and equality between each stage row and the bilingual macro view; regressions reject missing, reordered, and inconsistent results. Focused tests pass `7/7`; the complete Stage211 suite passes `275/275` in `219.35 s` with only the existing `14` upstream warnings. The JUnit report SHA-256 is `2b1f792a20b9148ac89a179b2e01df02f365aa60c4243a57003c2dfc5e307cdd`. Ruff, Python compilation, Bash syntax, and diff checks pass. During verification the uninterrupted Hard Mixer reached step `807,300/966,315` (`83.5441%`), with unchanged rank PIDs `250599--250602`; no training, supervisor, monitor, or data process was signaled.
- 2026-08-16 Stage211 requested-stage watcher runtime refresh: after pushing commit `3221fd2`, replace only the sleeping `rwkvasr_stage211_abcd_continuation_watch` session at `14:33:29 CST`, preserving its `POLL_SECONDS=3600` and `RESTART_BACKOFF_SECONDS=3600` settings. The replacement pane PID is `1212941`; its loaded watcher SHA-256 is `14b920df3849521416b76ab168b56082e5cf4a0dcc0a092c47c68b28fd21f89b`, and its first log record confirms the existing supervisor remains active. Controller PID `250320`, Hard child PID `250519`, and rank PIDs `250599--250602` remain unchanged; the live job subsequently reached step `807,420`. No training, optimizer, checkpoint, data-order, hourly-monitor, or profiled-boundary process was signaled or reconfigured.
- 2026-08-16 Stage211 base/public PCM one-archive prefetch contract: the exact Supplemental/public-overlap audit is on the Layer-to-Block critical path and currently serializes USB archive copy with cached archive decode. Add an explicit one-archive-ahead mode that uses one background thread only to copy and SHA-256-verify the next assigned archive into the existing transient local cache while the current cached archive is decoded. Preserve the canonical archive assignment and output order, normalized-PCM fingerprint bytes, per-archive immutable receipts, no-overlap final gate, low CPU/I/O priority, and bounded cache occupancy. A prefetched file must be revalidated through the existing cache path before decode and removed after use; cancellation, copy failure, decode failure, or process restart must leave no accepted partial receipt and must clean transient cache files. Keep the mode opt-in until deterministic serial/prefetch byte-equivalence, overlap ordering, failure cleanup, and a matched production throughput measurement prove benefit. This data-preparation optimization must not signal, restart, reconfigure, or add I/O priority above the active four-rank training job.
- 2026-08-16 Stage211 base/public PCM prefetch verification and admission decision: the audit worker now supports an explicit `--prefetch-next-archive` with exactly one background copy, skips already receipted archives, and tracks only cache entries scheduled by its own process for cleanup. The post-materialization wrapper exposes it through `BASE_PUBLIC_PREFETCH_NEXT_ARCHIVE`, default `0`; production behavior therefore remains unchanged. Synthetic archive tests prove serial, parallel-decode, and prefetch fingerprints are byte-identical, prove the next copy starts before current decode ends, prove decode failure removes both current and prefetched cache, and prove a serial worker does not delete an unowned future cache. A low-priority matched production attempt paused only the resumable audit reader and kept all training ranks active, but one approximately `420 MB` People’s Speech archive required about four minutes and the next copy remained in `blk_mq_get_tag`; the probe was stopped at a complete first-archive boundary, produced no promotional report, and the original reader was immediately resumed. This is evidence of saturated USB I/O, not evidence that prefetch improves throughput, so production prefetch remains disabled. Focused tests pass `18/18`; the complete Stage211 suite passes `277/277` in `227.85 s` with the existing `14` upstream warnings. JUnit SHA-256 is `ce6e000748a9d1b0d545cf6a4397e9845da2bcaaed7f5191e1b2d0055668ee3e`; Ruff, Python compilation, Bash syntax, and diff checks pass. The uninterrupted Hard Layer run reached step `809,170/966,315` (`83.7377%`) with complete `12/12` teacher matching and unchanged rank PIDs `250599--250602`. The restored exact audit has contiguous receipts through archive `1,178` (`1,179/4,172`) and remains the only production base/public reader.
- 2026-08-16 Stage211D formal full-profile gate contract: historical schema-v1 `285,302`-row SFT completions remain readable only for explicit compatibility checks, but they are inadmissible to the requested `rwkv_layer -> block -> logits -> sft` result. Every formal SFT finalizer, post-SFT correction admission, and stepwise final report must call completion validation in fail-closed full-profile mode. That mode requires a present, hash-bound, deeply recomputed schema-v2 labeled profile with `all_accepted_unique_rows_required=true` and `source_language_interleave_required=true`; completion metrics, runtime steps, full data audit, provenance, and checkpoint step must all derive from that receipt rather than the legacy constants. The formal runner must use the same mode when it writes a profile-bound completion. Missing, changed, legacy, or internally inconsistent profile evidence must fail before public WER/CER evaluation or correction can promote the SFT checkpoint. This evidence-only tightening must not restart or signal the active Layer training or its data-preparation processes.
- 2026-08-16 Stage211D formal full-profile gate verification: the formal runner now refuses to start without a schema-v2 full-labeled receipt, while the completion validator exposes an explicit compatibility default only for historical artifact inspection. Runner completion, SFT finalization, balanced SFT-correction admission, and final stepwise replay all enable strict full-profile mode. Regressions prove missing-profile rejection, dynamic receipt-derived metrics rather than legacy constants, formal finalizer/correction/stepwise call-site enforcement, and preservation of historical completion validation only outside the formal gate. Focused SFT tests pass `18/18`; the complete Stage211 suite passes `287/287` in `224.18 s` with the existing `14` upstream warnings. The zero-failure/error/skip JUnit report is `/home/yueyulin/rwkvasr_runs/stage211_test_reports/stage211_full_profile_fail_closed.xml` with SHA-256 `3a4c8dd4ff49d050a83cd97168efdae1a853c3f4bcd750de890807c90b8bcbfb`; Ruff, Python compilation, Bash syntax, and diff checks pass. During verification the uninterrupted Layer run advanced to step `810,820/966,315`, with latest mixer loss/cosine `0.0709/0.9745`, complete `16/16` hidden matching, zero missing/frame delta, and unchanged rank PIDs `250599--250602`.
- 2026-08-16 Stage211 hourly readiness-observability contract: the existing one-hour monitor must expose the two production readiness chains that gate future Block/Logits/SFT execution without performing another data scan. For full labeled SFT, report preparation state, latest processed/kept counts against the immutable `1,430,801` input rows, percentage, profile-receipt presence/hash, and the last finalizer record. For Supplemental handoff, report presence/hash independently for the combined-v2 inventory/profile and nine-cell/replay-v2 receipts. Derive progress only from the append-only preparation log and use constant-time file metadata/hash checks on the small receipts; do not validate or reread the 1.4M-row index or Supplemental audio from the hourly monitor. Missing artifacts are an explicit `pending` state rather than a false success. Updating this observer may replace only its sleeping tmux session and must leave the supervisor, Layer controller/ranks, data workers, optimizer, and data order unchanged.
- 2026-08-16 Stage211 hourly readiness-observability verification: the monitor now emits separate Supplemental handoff and full-labeled SFT sections. An isolated shell regression proves completed preparation is reported as `100.0000%`, all five ready receipts carry their actual SHA-256 values, and absent preparation/artifacts remain explicit `incomplete/pending`; a production one-shot snapshot reports `1,350,000/1,430,801` processed (`94.3527%`), `1,345,374` kept, and all four still-pending Supplemental handoff receipts without an AWK/runtime error. Focused tests pass `2/2`; the complete Stage211 suite passes `289/289` in `221.99 s` with the existing `14` upstream warnings. JUnit SHA-256 is `342e607bb16e4c5d19040cf9fb458b06f910bf839f6742d78a5400086462aab4`. Bash syntax, Ruff, and diff checks pass. During verification the uninterrupted Layer run reached step `811,500/966,315`, with complete `6/6` hidden matching, zero missing/frame delta, unchanged rank PIDs `250599--250602`, SFT preparation advanced further to `1,375,000` processed, and the exact Supplemental audit reached `1,193/4,172` archives.
- 2026-08-16 Stage211 hourly readiness-monitor runtime refresh: after pushing observer commit `86c6fe2`, replace only tmux session `rwkvasr_stage211_abcd_hourly_monitor`, preserving `POLL_SECONDS=3600`; its pane PID changes from `1088587` to `1227101`. The first production snapshot from the replacement reports SFT preparation `1,375,000/1,430,801` (`96.1000%`) with `1,370,164` kept, its full-labeled profile still pending, and all four Supplemental handoff receipts pending. Layer controller/rank PIDs `250320/250599--250602`, exact-audit PID `1038346`, and SFT-preparation PID `1141802` retain their pre-refresh identities and running states. The Layer job subsequently reaches step `811,580`, with mixer loss/cosine `0.0439/0.9843`, complete `10/10` hidden matching, and zero missing/frame delta; no training, optimizer, supervisor, data worker, or data cursor was signaled or changed.
- 2026-08-16 Stage211 final SFT preparation-count observability correction: when preprocessing completes between the fixed `25,000`-row progress intervals, use the terminal `ctc-align lengths complete processed=... kept=...` record instead of retaining the prior periodic `kept` value. The outer success marker still remains the authority for complete versus active state. The isolated monitor regressions pass `2/2`, including exact terminal `100/92` count selection over an earlier `75/70` progress record; the complete Stage211 suite remains `289/289` in `222.84 s` with `14` upstream warnings. JUnit SHA-256 is `30ba5c2d1a42649f6cb135f2baee3ad43c762d99ae504f884300f01204f24ab1`; Bash syntax, Ruff, and diff checks pass. The unchanged SFT preparation PID `1141802` remains active, while the Layer run reaches step `812,080/966,315` with loss/cosine `0.0707/0.9746`, complete `25/25` hidden matching, and zero missing/frame delta.
- 2026-08-16 Stage211D full-labeled metadata-language correction: the completed fail-closed preprocessing proof covers exactly `1,430,801` input rows and `1,425,343` accepted rows. Its source counts match the immutable filter receipt, while direct metadata accounting is `en=1,334,783` and `zh=96,018`, not the source-derived `en=1,334,785` and `zh=96,016`: two `commonvoice_en` rows carry `language=zh`. Both exceptional rows are among the `5,458` non-pronunciation-token rejections, so accepted counts remain source/language coherent (`en=1,329,369`, `zh=95,974`) and no relabeling or data rewrite is warranted. Bind the formal profile gate to the observed input metadata-language counts, retain the exact source-count and accepted/dropped accounting gates, and add a regression that prevents reintroducing source-derived language assumptions. Regenerate and independently deep-validate the schema-v2 labeled profile without rerunning preprocessing or touching active Layer training.
- 2026-08-16 Stage211D full-labeled profile verification: the corrected gate generates and independently revalidates immutable schema-v2 receipt `/home/yueyulin/rwkvasr_data/stage211_sft_full_labeled_v2/stage211_labeled_profile_receipt.json` with SHA-256 `f16b09e0aa6f76536bc42836bd0ec2ea7e07bb98af0177e5801d2e4f4e827858`. It binds `1,418,201` train and `7,142` eval rows, `2,421.999431` hours, `21,711,995` CTC tokens, `37,506` one-epoch SFT steps, `567` tail exposures, `1,425,343` unique/feasible pronunciation targets, zero unknown and forbidden target tokens, the exact four-source counts, exact `45` source-language lanes, and both all-accepted uniqueness and source-language interleave requirements. Focused tests pass `8/8`; the complete Stage211 suite passes `290/290` in `221.93 s` with the existing `14` upstream warnings. JUnit SHA-256 is `a2be419abf84db0952627e8189d00fdb82c388d49e7c16df3df9b4491637a5f0`; Ruff and diff checks pass. The active Layer ranks remain PID `250599--250602` and advance through Hard step `814,770/966,315` while validation runs; no training or data process is signaled.
- 2026-08-16 Stage211 Layer retention diagnosis: exact fixed Easy-ZH hidden evaluation improves monotonically through Easy (`0.325849 -> 0.270618`) and Medium (`0.270969 -> 0.105265`), then regresses under Hard from `0.105260` to `0.184322` by step `810,000` while current-domain online matching remains complete. The direction and continuity identify cross-difficulty/language forgetting rather than a general TimeMixer fitting failure, so do not interrupt or shorten the required Hard coverage. The immutable base replay receipt contains exactly `1,000,148` unique rows: `easy_en/easy_zh=130,000` each, `medium_en/medium_zh=270,000` each, `hard_en/hard_zh=100,000` each, and the `148` remaining `long_zh` rows after excluding its independent `256`-row evaluation cell. Supplemental v2 adds `supplemental_en/supplemental_zh=130,000` each, producing an exact planned replay of `1,260,148` rows with near-exact language balance (`en=630,000`, `zh=630,148`) and a disjoint nine-cell hidden evaluation of `256` rows per cell (`2,304` total). Run up to three complete one-epoch correction rounds at the existing post-coverage contract; Block remains fail-closed until both the replayed hidden-alignment decision and source-recomputed three-dataset English WER/two-dataset Chinese CER progress decision pass.
- 2026-08-16 Stage211 downstream phase-readiness audit: the strict controller starts Block only from the selected Layer checkpoint and hash-bound passing promotion receipt, and starts Logits only from the equivalent selected Block evidence. Layer, Block, Logits, and labeled SFT retain the Nano-copied non-attention encoder path, five-layer CTC decoder, and CTC head as frozen parameters; only the bidirectional RWKV TimeMixer and its input projections are trainable. Block uses stacked mixer/FFN/block plus final-encoder and decoder-hidden objectives without a logits objective. Logits retains those hidden anchors while enabling factorized blank/nonblank, conditional hard-token, sequence, and spike-window objectives. Focused phase-config, cross-phase rejection, promotion-chain, Block hidden-gate, and Logits distribution/hidden-retention regressions pass `16/16` in `52.66 s` with only the existing `14` upstream Torch deprecation warnings. No active training, data-audit, optimizer, or data-order state was changed by this CPU-only verification.
- 2026-08-16 Stage211 all-data coverage accounting: the immutable globally deduplicated Easy/Medium/Hard/Long curriculum contains exactly `62,072,225` unique rows and `118,465.160681` hours. Every Layer, Block, and Logits stage must execute three complete epochs over those four segments, yielding `186,216,675` base row exposures and `355,395.482042` base hour exposures per stage before Supplemental is counted. The existing Supplemental base pool adds `6,434,286` rows and `25,419.427943` hours, while materialized social candidates contribute up to `395,125` rows and `1,790.823792` hours before their exact duplicate and public-overlap exclusions. Do not record their arithmetic sum as final coverage: only the pending normalized-PCM-audited combined-v2 inventory/profile may define the fifth segment's exact rows, hours, three-epoch steps, and final total. The labeled SFT profile remains separate and exact at `1,425,343` accepted rows and `2,421.999431` hours.
- 2026-08-16 Stage211 per-stage public-metric readiness audit: every candidate and Nano baseline metric is recomputed from hash-bound prediction files with the same `unicode_alnum_words_basic_cjk_chars_v1` tokenizer after Unicode normalization, and exact utterance/reference identity is required. The public suite remains AISHELL-1 `7,176` plus WenetSpeech test-net `24,774` for Chinese CER, and LibriSpeech test-clean `2,620`, test-other `2,939`, plus Common Voice English `14,922` for English WER, for `52,431` utterances total. Layer and Block require source-recomputed bilingual macro-error and deletion progress without an individual-dataset regression beyond the configured tolerance; Logits and SFT additionally require every dataset to pass their Nano/preceding-stage gates. The final report rejects placeholders, unreplayed calibration numbers, a missing language macro, a missing requested stage, or any dataset without all `rwkv_layer/block/logits/sft` values. Focused full-set, normalization, replay, progress, and final-report tests pass `15/15` in `13.63 s` with only `14` upstream Torch deprecation warnings; this CPU-only audit does not change active training state.
- 2026-08-16 Stage211 production architecture-identity audit: the active Layer/Hard config and persisted step metadata bind a `sensevoice_rwkv` encoder with `70` layers, RWKV head size `64`, and the copied five-layer Nano CTC decoder. `direction_variant=none` means no branch is dropped during this offline Nano-alignment phase; each encoder layer still instantiates independent forward and backward RWKV-7 TimeMixers, not self-attention or the historical Conformer control. The training boundary freezes the Nano-copied MLP/norm path, CTC decoder, and CTC head and permits gradients only for TimeMixer/input-projection parameters. Focused recurrent-state, chunk-consistency, bidirectional merge/padding/mask, Nano QKV-to-both-directions initialization, frozen-path backpropagation, exact frozen-tensor, and mutation-rejection tests pass `12/12` in `8.18 s` with only upstream deprecation warnings. The latest persisted production metadata observed by this read-only audit is Layer/Hard `step-816000`; no checkpoint payload is loaded and no training process is signaled.
- 2026-08-16 Stage211 intra-phase trajectory-retention gate contract: the terminal fixed Easy-ZH loss improved to `0.1052653219` after Medium but regressed to `0.1843219729` by Hard step `810,000`, proving that comparison only against the phase initialization can admit a final checkpoint after substantial within-phase forgetting. Every future Layer, Block, and Logits phase gate must therefore replay the terminal fixed-evaluation report for ordered `easy -> medium -> hard -> long -> supplemental_natural` coverage and every completed post-coverage correction round. Each entry must bind the receipt, run directory, terminal step/checkpoint, train config, report path/hash, the same immutable 256-row eval-part fingerprint, feature seed `0` (with the pre-contract Layer/Easy report's absent seed accepted only as the one legacy exception), complete phase-appropriate hidden/logit metrics, and finite loss. The promoted candidate must be the last ordered entry and its fixed loss may be at most `10%` above the best prior terminal loss; otherwise the phase gate fails and the existing balanced one-epoch correction loop runs before any Block/Logits promotion. This additional gate applies independently of the phase-initial paired hidden/logits gate and bilingual public WER/CER gate, does not shorten any three-epoch coverage, and must not signal or reconfigure the active Layer training process.
- 2026-08-16 Stage211 trajectory-retention implementation verification: phase-gate schema `2` now rebuilds the immutable terminal-report trajectory, combines its decision with the hidden/logits and public-metric sub-gates, and discloses the best prior segment, final segment, losses, and regression percentage in the final stepwise JSON/Markdown. Mutation, best-prior regression, builder, legacy-seed, correction-chain, final-report, and all existing Stage211 tests pass `286/286` in `257.81 s`; JUnit SHA-256 is `72775b3e60a718ebe1544676634d655ede23370411f8961fabdee958d932c95d`. Ruff, Python bytecode compilation, and `git diff --check` also pass. These CPU-side checks did not load checkpoints or signal the active four-GPU Layer/Hard process.
- 2026-08-16 Stage211 readiness-monitor evidence precedence: the hourly monitor must not present an older SFT finalizer failure as current after a newer immutable labeled-profile receipt exists. A successful receipt that is newer than a failure-bearing finalizer log supersedes that log for status reporting, while the original log remains on disk for audit. The monitor must preserve a current success line, preserve failures that have no newer receipt, and report pending when neither artifact exists. This display rule does not weaken the SFT validator: readiness is still established only by the deeply validated profile receipt and the final SFT controller revalidates all `1,425,343` accepted rows before training. A production revalidation completed over all accepted rows with `2,421.999431` hours, zero forbidden/UNK targets, and `37,506` estimated one-epoch steps; the dedicated monitor suite passes `3/3`, all hourly-monitor integration cases pass `8/8`, Bash syntax and Ruff pass, and the live artifact now reports `profile validated stale_failure_superseded=true`.
- 2026-08-16 Stage211 final trajectory-proof watcher contract: final completion validation must explicitly require a passing intra-phase trajectory-retention disclosure for each ordered `mixer`, `block`, and `logits` alignment result, in addition to the phase report's deep source replay. Each disclosure must retain the immutable `256`-sample fixed evaluation, at least the five ordered terminal entries beginning `easy -> medium -> hard -> long -> supplemental_natural`, finite best-prior/candidate loss and relative-regression values, and the exact `10%` maximum relative-regression threshold. Missing, reordered, non-finite, or weakened trajectory evidence must make the continuation watcher reject final completion and resume from the deepest independently validated promotion. This proof-only hardening must not change or restart the active Layer training, its optimizer, data order, batch profile, or supervisor.
- 2026-08-16 Stage211 final trajectory-proof watcher verification: the continuation watcher now independently rejects missing per-stage trajectory evidence, requires the exact five-segment prefix, at least five terminal entries, finite endpoint/regression metrics, the immutable `256`-sample fixed split, and the exact `10%` threshold for all three alignment phases. The watcher-focused suite passes `18/18`; the complete Stage211 suite passes `288/288` in `256.49 s` with only the existing `14` upstream Torch deprecation warnings. The zero-failure/error/skip JUnit report is `/home/yueyulin/rwkvasr_runs/stage211_test_reports/stage211_final_trajectory_watcher.xml` with SHA-256 `b058361fe5412a889637838ca3363024324199a40f33dba15e5a2a8ea9311365`; Ruff, Bash syntax, and diff checks pass. The stale-evidence monitor regression now uses explicit one-second mtime ordering to avoid filesystem timestamp-resolution flakiness without changing production precedence. During verification the uninterrupted Layer/Hard run reached step `821,650/966,315`, and rank PIDs `250599--250602` remained unchanged.
- 2026-08-16 Stage211 progress-qualified correction extension: the historical three-round post-coverage cap is not sufficient evidence for the observed Layer retention gap (`0.1857375818` at Hard step `820,000` versus the `0.1052653219` Medium terminal best and approximately `0.1158` trajectory admission ceiling). Preserve three complete one-epoch rounds as the unconditional correction budget, but allow at most eight ordered rounds for Layer, Block, and Logits. After round three, admit another round only when the just-completed deterministic evidence strictly improves over the preceding failed gate in at least one comparable dimension: immutable fixed-trajectory candidate loss, macro student error over the same five public datasets, or the mean of all finite candidate-loss values in the deeply bound phase alignment report. Record an immutable extension decision binding both gate paths/hashes, all prior/current metric values, improved metric names, the next round, and the hard cap. A plateau must stop with explicit failed evidence; a hard-cap failure must stop; a passing phase gate must still promote immediately. This extension cannot weaken hidden/logits, trajectory, or English-WER/Chinese-CER gates, cannot alter the one-epoch replay/LR/trainable-boundary contract, and does not apply to the separately bounded labeled-SFT correction policy.
- 2026-08-16 Stage211 progress-qualified correction verification: the shared Layer/Block/Logits correction controller now guarantees three complete rounds, admits rounds four through eight only through an immutable deeply replayed progress decision, binds that decision into the next run's provenance and completion receipt, rejects stale decisions and plateaus, and preserves immediate promotion on a passing phase gate. The focused correction suite passes `23/23`; the complete Stage211 suite passes `293/293` in `256.75 s` with only the existing `14` upstream Torch deprecation warnings. The zero-failure/error/skip JUnit report is `/home/yueyulin/rwkvasr_runs/stage211_test_reports/stage211_progress_qualified_corrections.xml` with SHA-256 `6fe3c1fb76edad14fe8c97fa2cdb58ba79319c9fa15c8de7b99ca08574790e3a`; Ruff, Python compilation, and diff checks pass. Verification did not restart production: Layer/Hard advanced to step `823,480/966,315`, and rank PIDs `250599--250602` remained unchanged.
- 2026-08-16 Stage211 future-phase batch-search expansion: the phase-bound throughput preflight currently searches only baseline `36/24k`, `48/42k`, and `64/56k` per-rank batch/frame-budget profiles despite the active baseline using approximately `8.8 GiB` per RTX 4090. Expand the search for the not-yet-started profiled Mixer Supplemental, Block, and Logits segments with `80/70k` and `96/84k` candidates. This is a search-space change only: every candidate must still run the exact phase objective for `20` warmup plus `100` measured steps, complete every enabled online-teacher match with zero missing/frame delta, remain at or below `22 GiB`, stay within the existing `5%` mean-loss and `0.005` cosine-regression limits, and improve projected complete three-epoch wall time by at least `10%`. A process failure or OOM makes only that candidate inadmissible; selection must retain the fastest admissible profile or the baseline, and formal training must recompute exact full-coverage steps and tail padding from the immutable manifest. Do not reconfigure, signal, or restart the active Layer/Hard run.
- 2026-08-16 Stage211 expanded batch-search verification: the default preflight now evaluates all five ordered profiles through independent processes and preserves safe lower-profile selection when a larger candidate exits nonzero. A dry run against the real Hard manifest and step-822,000 checkpoint recomputed exact three-epoch coverage as `966,315`, `546,450`, `407,475`, `324,564`, and `270,144` steps for `36/24k`, `48/42k`, `64/56k`, `80/70k`, and `96/84k`, respectively; no profile is formally admitted without the later four-GPU measurement. The focused throughput/admission/handoff suite passes `26/26`; the complete Stage211 suite passes `294/294` in `255.40 s` with only the existing `14` upstream Torch deprecation warnings. The JUnit report is `/home/yueyulin/rwkvasr_runs/stage211_test_reports/stage211_expanded_batch_search.xml` with SHA-256 `a0ead39bbd3d6e59362c0b288beea6c1761bf71709a5749ebd0aee9c03a59de6`; Ruff, Python compilation, and diff checks pass. Verification did not restart production: Layer/Hard advanced to step `824,270/966,315`, and rank PIDs `250599--250602` remained unchanged.
- 2026-08-16 Stage211 high-throughput profile ceiling contract: the active `36/24k` Layer profile reserves only about `8.8 GiB` per RTX 4090, while every future candidate is already isolated in its own process and rejected independently on OOM, nonzero exit, incomplete teacher matching, missing/frame delta, quality regression, or peak reservation above `22 GiB`. Extend the future-only search after `96/84k` with `128/112k`, `160/140k`, and `192/168k`. Preserve the same `20` warmup plus `100` measured steps, exact phase objective, immutable checkpoint/manifest binding, `5%` loss and `0.005` cosine limits, and at least `10%` projected three-epoch wall-time improvement. Candidate order is ascending capacity only; selection remains the fastest admissible measured profile and may retain any lower profile or the baseline. The additional probes may run only after all four GPUs become naturally idle at a fresh Mixer-Supplemental, Block, or Logits segment boundary and must not signal, restart, or reconfigure the current Layer/Hard ranks.
- 2026-08-16 Stage211 high-throughput profile ceiling verification: the eight-profile default is now exact and ordered from `36/24k` through `192/168k`. Coverage-only recomputation on the immutable real Hard manifest yields three-epoch step counts `966,315`, `546,450`, `407,475`, `324,564`, `270,144`, `202,143`, `161,448`, and `134,316`; the existing base Supplemental projection yields `309,387`, `173,340`, `130,047`, `102,288`, `85,389`, `64,260`, `51,072`, and `42,681`. Tail padding remains explicitly recomputed per manifest/profile and no sample is dropped. Focused throughput/admission/handoff tests pass `26/26`; the complete Stage211 suite passes `294/294` in `258.42 s` with only the existing `14` upstream Torch deprecation warnings. The zero-failure/error/skip JUnit report is `/home/yueyulin/rwkvasr_runs/stage211_test_reports/stage211_high_throughput_profiles.xml` with SHA-256 `43f274e7127ae2641f25e4f17b5894d118357eaf4f2cff4b56e12f6cb976f2fb`; Ruff, Python compilation, and diff checks pass. No profile is admitted by this CPU-only verification, and the uninterrupted Layer/Hard run advances to step `825,810/966,315` with unchanged rank PIDs `250599--250602`.
- 2026-08-16 Stage211 probe-artifact retention contract: a measured 120-step probe still emits rank-zero `step`, `best`, and `epoch` exported models even with DeepSpeed shard saving disabled, consuming about `2.2 GiB` per candidate. Eight candidates across fresh Mixer-Supplemental, Block, and Logits boundaries would retain roughly `200 GiB` of models that are never used for initialization or admission. Upgrade the measured-preflight proof so every candidate, including a nonzero/OOM candidate, removes only its dedicated generated `run` directory after the subprocess has fully exited while preserving the separately stored immutable config, complete raw log, in-report telemetry, memory samples, and coverage result. Record whether the run directory existed plus removed file/byte counts, require it to be absent during deep replay, and fail the whole preflight if cleanup cannot complete. Dry-run configs may record a zero-artifact cleanup. Do not delete the source initialization checkpoint, manifest, candidate config/log, final preflight report, or admission receipt, and do not change any formal training checkpoint policy.
- 2026-08-16 Stage211 probe-artifact retention verification: measured-preflight schema `3` now records a schema-v1 cleanup result for every profile. Cleanup is path-confined to the profile's generated `run` child, rejects symlinked or unexpected paths, counts files/directories/bytes before deletion, runs after the torchrun subprocess exits even on an exception, and fails closed if the directory remains. Deep replay independently binds the config output path to that directory, validates all cleanup fields, and rejects a recreated run directory; candidate config and raw log remain hash-bound outside the deleted tree. A real eight-profile Hard dry run emits eight complete zero-artifact cleanup records and leaves no run directories. Focused throughput/admission/handoff tests pass `29/29`; the complete Stage211 suite passes `297/297` in `256.39 s` with only the existing `14` upstream Torch deprecation warnings. The zero-failure/error/skip JUnit report is `/home/yueyulin/rwkvasr_runs/stage211_test_reports/stage211_probe_cleanup_schema3.xml` with SHA-256 `a435bf1bf9453b0f8519a40575494dfa8fecfcd675135174b8c224562ffbb691`; Ruff, Python compilation, and diff checks pass. The active Layer/Hard run reaches step `826,750/966,315` with unchanged rank PIDs `250599--250602`; no production checkpoint or process is removed or signaled.
- 2026-08-16 Stage211 same-distribution batch-profile quality contract: the schema-3 preflight quality comparison is not methodologically comparable because its `mean_loss` and `mean_cosine` are aggregated from each profile's own training batches; larger profiles consume more and different utterances during the same 120 optimizer steps. Upgrade future measured preflights so every profile still uses its 20-step warmup plus 100-step training window only for throughput, memory, objective-match, missing-count, and frame-parity evidence, but also evaluates its terminal probe checkpoint on the exact same immutable `eval` split of 256 utterances with shuffle disabled and feature seed `0`. Preserve the small terminal fixed-eval sidecar and SHA-256 outside the disposable `run` directory before probe cleanup. Quality admission must compare terminal fixed-eval total objective loss plus arithmetic mean 70-layer cosine from the phase-primary hidden component (`mixer` for Layer; `block` for Block and Logits), while independently requiring all phase components, Block/Logits decoder-hidden loss, and Logits distribution metrics to be finite and exactly covered. Every profile must bind identical manifest/eval-part provenance; a missing, changed, partial, non-finite, or cross-profile fixed evaluation makes that profile unsafe regardless of training-batch telemetry. Keep the existing `5%` loss, `0.005` cosine, `22 GiB`, complete-match, and `10%` projected-wall-time limits. This fixed evaluation is a profile-equivalence guard only and does not replace the phase terminal/nine-cell hidden or Logits gate, five-dataset normalized English WER/Chinese CER evaluation, or trajectory-retention correction.
- 2026-08-16 Stage211 same-distribution batch-profile quality verification: measured-preflight schema `4` now schedules one terminal `eval/256/feature_seed=0` evaluation for every isolated profile, copies its sidecar and SHA-256 outside the disposable run tree, and keeps throughput timing restricted to the 100 measured training steps. Deep replay validates the exact terminal step, manifest and eval-part hashes, all required 70-layer phase components, Block/Logits decoder-hidden loss, Logits distribution coverage/frame parity, and identical fixed-eval provenance across baseline/candidates. Selection uses only fixed-eval total loss and the phase-primary 70-layer mean cosine for the existing `5%/0.005` quality limits; training-batch means remain diagnostic. Regressions prove that a candidate with artificially superior training-batch loss/cosine but worse fixed-eval quality is rejected, as are changed eval parts and incomplete Block/Logits metrics. A real eight-profile Hard dry run preserves exact three-epoch coverage counts `966,315/546,450/407,475/324,564/270,144/202,143/161,448/134,316`, emits the fixed-eval contract in every config, and leaves no run directory. Focused throughput/admission/handoff tests pass `35/35`; the complete Stage211 suite passes `303/303` in `258.02 s` with the existing `14` upstream warnings. The zero-failure/error/skip JUnit report is `/home/yueyulin/rwkvasr_runs/stage211_test_reports/stage211_same_distribution_batch_profile_schema4.xml` with SHA-256 `1bc32de9990947223bad6e2b5e0da410ef1ec4e310f93fe61b00ec341515bd14`; Ruff, Python compilation, and diff checks pass. During verification the unchanged production Layer/Hard run advanced from step `827,030` to `828,520`, retained rank PIDs `250599--250602`, complete online hidden matches, and zero missing/frame delta.
- 2026-08-16 Stage211A Hard step-830k fixed sentinel: the uninterrupted Layer/TimeMixer job commits the complete step-830,000 checkpoint and a deeply replayed fixed `eval/256/feature_seed=0` report bound to Hard manifest SHA-256 `754d73f4b0bc4d1da9a8d10423a86960bc46f9bb796a25a8d9d910d0dc950977` and eval-part SHA-256 `9f4bf09cdbbf5bc963d6282a640e45fcd8da75f2e23a41e4515b6766288c84f1`. Its objective loss is `0.1838928063` and the unweighted 70-layer Mixer cosine is `0.9400243391`; versus step 820,000, loss improves by approximately `0.99%`, cosine improves by `0.000567`, `58/70` layer losses improve, and `60/70` layer cosines improve. Eleven layers remain below cosine `0.90`, with layer 11 weakest at `0.8673415613`, while the aggregate remains essentially level with step 780,000 (`0.1832833386/0.9401775167`). Treat this as broad but plateaued Easy-ZH retention, not a missing-layer sampling failure: finish exact Hard/Long/Supplemental coverage, then require the planned `1,260,148`-row balanced replay, nine-cell hidden gate, and five-dataset bilingual public gate before Block promotion. No training process or configuration is changed by this read-only validation.
- 2026-08-16 Stage211 strict-order bilingual-gate audit: an independent current-tree target suite proves cross-phase objective rejection, passing promotion-receipt requirements, source-recomputed public WER/CER and progress decisions, Block Mixer/FFN/whole-Block evidence, Logits stratified hidden retention, SFT zero-dataset-regression behavior, deep continuation-watcher admission, and final ordered `rwkv_layer -> block -> logits -> sft` metric/checkpoint reporting. All `21/21` tests pass in `40.20 s` with only the existing `14` upstream Torch deprecation warnings. The JUnit report is `/home/yueyulin/rwkvasr_runs/stage211_test_reports/stage211_strict_order_bilingual_gate_audit.xml` with SHA-256 `4ccabc015597c54a9c5f2da534d7dccbcfd7200848d2b35f53fb904f10ae09d9`; this CPU-only audit leaves the active Layer ranks unchanged while they advance beyond step `830,400`.
- 2026-08-16 Stage211A step-830k production parameter-boundary audit: directly mmap and compare the Hard initialization checkpoint (`medium/step-1003713.pt`) with `hard/step-830000.pt` through the same receipt builder used at formal segment completion. Both states contain exactly `12,697` tensor keys with identical shape/dtype metadata. Exactly `10,905` allowed TimeMixer/input-projection tensors (`567,936,000` elements) change, `18` allowed tensors remain equal, all `1,774` frozen tensors remain bitwise equal, and `forbidden_changed_tensors=0`. Together with the immutable Nano initialization proof (`564` copied MLP/norm tensors, `84` copied five-layer CTC-decoder tensors, zero mismatched head rows), this proves the running Layer phase has not updated the frozen Nano MLP, norm, convolution, decoder, or CTC head and has not leaked Block/Logits/SFT trainability into Layer. The audit is read-only and does not load a GPU or signal production.
- 2026-08-16 Stage211A runtime full-coverage replay: independently deep-reload the immutable Easy and Medium curriculum receipts and their runtime epoch records. Easy proves `1,174,987` unique rows, `3,524,961` three-epoch row exposures, `1,490.119` hours, and exactly `29,946` steps; Medium proves `42,247,508` rows, `126,742,524` exposures, `47,771.242` hours, and exactly `1,003,713` steps. Recomputing the active Hard manifest from its 53 train buckets yields `18,649,326` rows, `322,105` steps per epoch, and only `986` deterministic tail-padding exposures per epoch at the bound `batch=36/world=4/frame_budget=24,000` profile. At live step `831,110`, the checkpoint/log cursor is epoch 3 offset `186,900/322,105` (`58.0246%` of epoch 3, `86.0082%` overall), with both prior epoch checkpoints and metrics present. This proves the large step count is advancing through the exact all-row three-epoch contract rather than recycling a reduced shard subset; the read-only replay does not alter the iterator or training state.
- 2026-08-16 Stage211 Supplemental audit I/O-priority restoration: the uninterrupted base/public normalized-PCM worker tree was found at inherited `nice=19` plus idle I/O class even though the later matched production experiment admitted best-effort priority 7 as the faster no-training-regression setting. Restore only PIDs `1038302/1038309/1038346` in place to `best-effort:7`; do not restart the worker, change its four decode threads, enable archive prefetch, alter cache/input/output paths, or signal training. The transient root-NVMe cache holds only the current approximately `177 MB` archive and root retains `683 GB`, so no migration to the nearly full read-only USB volume is needed. A five-minute post-change window advances four immutable receipts (`0.800 archive/min`) while Layer advances 410 steps (`1.367 step/s`), reproducing the previously admitted throughput without slowing training. Keep this scheduling class for the remaining exact archive-order scan; it changes neither receipt bytes nor deterministic scan order.
- 2026-08-16 Stage211A Hard 10k-evaluation completeness audit: independently deep-validate every required fixed report from step `10,000` through `830,000` inclusive. All `83/83` expected reports are present with no interval gap, exact `256`-utterance coverage, feature seed `0`, complete 70-layer Mixer metrics, and one unchanged manifest/eval-part provenance; every report has a finite objective loss and mean cosine. On this fixed Easy-ZH retention set, step 10,000 remains best at loss/cosine `0.1199936161/0.9585534030`, step 820,000 is worst at `0.1857375818/0.9394574123`, and step 830,000 only locally recovers to `0.1838928063/0.9400243391`. This proves the requested 10k loss evaluation cadence is complete while also proving persistent cross-domain forgetting; do not promote directly from Hard. Finish the exact remaining coverage and require balanced replay, nine-cell hidden evaluation, and full English-WER/Chinese-CER progress before Block.
- 2026-08-16 Stage211 Supplemental weighted-prefix and early-overlap audit: the official read-only progress validator proves a contiguous base/public normalized-PCM prefix through archive 1,447 with `2,249,729/6,434,286` rows (`34.9647%`), `8,764.573896/25,419.427943` hours (`34.4798%`), and `684,757,228,601/2,220,394,575,325` source bytes (`30.8394%`). A subsequent immutable-receipt snapshot captures one newly completed archive and uses the production receipt/location-index/public-fingerprint validators plus the production overlap renderer to replay exactly `1,449` archives and `2,251,330` training rows against all `52,431` canonical public fingerprints. The validated prefix has `overlap_rows=0`, an empty per-dataset overlap map, and zero rendered overlap bytes. This is early evidence only and cannot substitute for the final all-`4,172`-archive receipt; combined-v2 remains fail-closed until complete coverage and final zero-overlap replay.
- 2026-08-16 Stage211 profiled-boundary handoff re-audit: current-tree control flow validates and receipts Long before waiting for combined-v2, then waits for the Supplemental profile plus both nine-cell hidden/replay receipts, deeply validates those barriers, runs the phase-specific same-distribution four-GPU profile only after the legacy ranks are naturally idle, trains Supplemental for its admitted exact three-epoch step count, validates the complete five-segment Mixer curriculum, and only then replaces the stopped legacy supervisor with `START_STAGE=post_mixer`. Focused execution tests prove Long checkpoint chaining, fresh Supplemental-only batch admission, complete-summary routing, shallow-summary rejection, and multi-supervisor handoff survival; all `9/9` pass in `7.03 s` with the existing `14` upstream Torch warnings. JUnit SHA-256 is `19d6c52b71dfa1827f47e5871c7978b3c9bffcf896c96596d3570ef8b099c3ff`. This CPU-only audit does not trigger the boundary or alter the active Layer run.
- 2026-08-16 Stage211 future-phase readiness re-audit: the current strict controller independently profiles every fresh `easy/medium/hard/long/supplemental_natural` segment for Block and Logits, recomputes exact three-epoch coverage and tail padding from the immutable segment manifest, and rejects cross-phase, incomplete-match, changed-fixed-eval, retained-probe-artifact, or quality-regressing admissions. The shared post-coverage loop preserves one complete `1,260,148`-row balanced replay epoch per round, phase-specific objectives and lower learning rates (`1e-6` for Layer/Block, `1.5e-7` for Logits), three progress-independent failed-gate rounds, and progress-qualified rounds four through eight. Focused profile, correction, checkpoint-chain, Block-component, Logits-retention, five-dataset normalized WER/CER replay, Supplemental-coverage, and final-proof tests pass `67/67` in `42.29 s` with the existing `14` upstream warnings. The zero-failure/error/skip JUnit report is `/home/yueyulin/rwkvasr_runs/stage211_test_reports/stage211_future_phase_readiness_reaudit.xml` with SHA-256 `b0579be506c42097ea690415d4575d1f70198129419c36ec569f802fe46e2705`. This CPU-only verification leaves production rank PIDs `250599--250602` unchanged while Layer/Hard advances to step `833,100/966,315` with complete online hidden matches and zero missing/frame delta.
- 2026-08-16 Stage211D full-SFT readiness re-audit: one formal SFT epoch means one exact, no-replacement traversal of every accepted schema-v2 training row rather than a sampled or shortened stage. Production receipt replay validates `1,425,343` unique CTC-feasible pronunciation targets over `2,421.999431` hours, with `1,418,201` train and `7,142` eval rows, exactly `37,506` optimizer steps, `567` deterministic tail exposures, `21,711,995` CTC tokens, and zero unknown or forbidden/non-pronunciation target tokens; receipt SHA-256 remains `f16b09e0aa6f76536bc42836bd0ec2ea7e07bb98af0177e5801d2e4f4e827858`. Preserve this single all-row SFT traversal after the passing Logits checkpoint, then require the same three-English/two-Chinese normalized public gate; only a failed gate may launch up to three separately receipted, one-epoch, English/Chinese-balanced correction rounds at `1e-7`, and promotion still requires every-dataset Nano proximity, zero regression versus Logits, and both language macros to improve. Focused full-profile, label-support, checkpoint-chain, correction-profile, bilingual-gate, and final-stepwise-proof tests pass `32/32` in `52.70 s` with the existing `14` upstream warnings. The zero-failure/error/skip JUnit report is `/home/yueyulin/rwkvasr_runs/stage211_test_reports/stage211_full_sft_readiness_reaudit.xml` with SHA-256 `a1f7940cf1b53e522e1e0457a304e0ce104f89cdabe30c8f4f0e0ceb6d51e49a`; verification is CPU-only and does not change active training.
- 2026-08-16 Stage211D balanced-correction production profile: precompute the deterministic correction data because it depends only on the immutable full-labeled profile, while retaining the fail-closed admission rule that no correction training may begin before a complete all-row SFT run and failed five-dataset bilingual gate exist. The deeply replayed receipt at `/home/yueyulin/rwkvasr_data/stage211_sft_source_balanced_correction_v1/stage211_sft_correction_profile.json` has SHA-256 `87d813d3e9b335ba1bfa4b05cbd81cf27e182ca16a03ec2e0d9755e667f204a6` and binds exactly `191,024` unique train rows over `309.737461` hours: all `95,512` Chinese train rows (`62,952` AISHELL-3 plus `32,560` Common Voice Chinese) and `95,512` English rows selected without replacement and evenly split between Common Voice English and LibriSpeech (`47,756` each) across 80-frame acoustic buckets. One correction round is exactly `5,147` optimizer steps with `432` tail exposures and `3,138,269` CTC tokens. The full profile's `7,142` eval keys are hash-bound, reused read-only, and excluded from training accounting; the selected key and row fingerprints are `fc9d8cba05b952efe43228672b973bbbc0af82a00ded54982a148298462776c4` and `b7efa4bd09b3fefed053079042bf6a8ea577860b1a3aad26ce4ce54672e5b471`. A separate `--validate-only` replay succeeds after construction, proving restart-safe reuse without weakening the failed-gate admission or replacing mandatory full SFT.
- 2026-08-16 Stage211 layer-sampling coverage contract: do not infer missing online supervision from the every-10-step console projection. Layer and Block use the deterministic eight-of-70 selector every optimizer step; its exact period is 35 steps and every layer occurs four times per period. Therefore the immutable Easy/Medium/Hard/Long targets produce per-layer selection counts `3,422--3,423`, `114,710--114,711`, exactly `110,436`, and exactly `12`, respectively, with no missing layer and at most one exposure of imbalance. Logits must pin the eight declared weak/boundary layers `(0,11,12,17,20,49,50,69)` while rotating four additional layers; over its 31-step period each anchor occurs 31 times and every other layer occurs twice. Labeled SFT intentionally uses those same eight weak layers only as low-weight hidden anchors while its primary full-label CTC objective trains every accepted row. Add regression coverage for these exact Stage211 schedules so a future selector, sample-count, boundary, or step-total change cannot silently create an untrained hidden layer.
- 2026-08-16 Stage211 layer-sampling coverage verification: focused selector and phase-config regressions pass `7/7` in `8.07 s`, proving exact 70-layer Layer/Block period coverage, real Easy/Medium/Hard/Long selection counts, distributed fixed-eval coverage, Logits weak-anchor plus rolling-layer coverage, and the intentional SFT weak-anchor set. The zero-failure/error/skip JUnit report is `/home/yueyulin/rwkvasr_runs/stage211_test_reports/stage211_layer_sampling_coverage.xml` with SHA-256 `e63f065ca2598bb356c9da63d44e26d85ad56de6dba9027a2edf9381feadd5ec`; Ruff and diff checks pass. This CPU-only audit leaves rank PIDs `250599--250602` unchanged while production advances to Layer/Hard step `834,660/966,315` with complete matches and zero missing/frame delta.
- 2026-08-16 Stage211 Nano-initialization current-source re-audit: replay the production creation command against calibration checkpoint SHA-256 `3bbef41989961b9f1ff0e172dafd313b4976b98f5549e935ca31543303b9944e` and Nano SHA-256 `81fec8616083c69377f3ceef36aba3655660ee0ca69a5d4a1e9810cd340ca499`; it deeply validates and reuses the existing immutable receipt without rewriting it. The runtime proof still binds all 70 Nano Q/K/V matrices to both BiRWKV directions with admitted `rwkv_norm` scaling, the first `560 -> 512` joint-SVD input projection with Q/K/V relative reconstruction errors `0.0495377/0.0453578/0.0284988`, `561` bidirectional Q/K/V/output plus input-projection tensors, `564` exact Nano MLP/Norm tensors, `84` five-layer CTC-decoder tensors, and all `60,516` projected/ignored head rows. The direct frozen audit remains zero-mismatch across `648` tensors and every head row. Current-source QKV mapping, scaling, immutable-reuse, loader-evolution, frozen-tensor, decoder/head, and attention-skip regressions pass `12/12` in `11.88 s`; JUnit SHA-256 is `a7ba8ec941db7e69df9e3a1ba0e81e2747a703bf63a3fd32be11f7abaeb8bd9a`. This excludes random or one-direction-only production initialization and does not signal the active Layer run.
- 2026-08-16 Stage211 native-Nano capture-point regression requirement: supplement the prior real-audio numerical audit with a deterministic current-source test built from FunASR's installed `sense_voice.model.EncoderLayerSANM`, not only project fake modules. Exercise both the first dimension-changing layer and a same-width deeper layer, then independently reconstruct raw SANM output, post-mixer residual, copied-MLP output, and final block output from each hook's captured layer input. Require exact equality with the production `input/mixer/ffn/block` capture contract, including unequal padded lengths. This makes any upstream hook-location, residual, pre-norm, or first-layer behavior drift fail before a future Stage211 phase is launched; it is verification-only and must not signal or reconfigure the active Layer run.
- 2026-08-16 Stage211 native-Nano capture-point verification: the installed FunASR `EncoderLayerSANM` regression proves bit-exact production capture semantics for a dimension-changing first layer and a same-width deeper layer: `mixer` is the raw SANM output before dropout/residual, `ffn` is the raw pre-norm MLP output after the correct post-mixer residual, and `block` is the exact final residual result. The combined native-teacher, teacher-forced-student, and stacked-student capture selection passes `9/9` in `10.89 s`; JUnit SHA-256 is `d088fe6b4411ea48398cfe2df098f34f3b009949513c872b389af793cce8f231`. This rules out capture-point/residual semantic mismatch in current source while the unchanged production Layer/Hard run advances through step `835,680/966,315` with complete teacher matches and zero missing/frame delta.
- 2026-08-16 Stage211 phase-chain and SFT-support current-source re-audit: the generated Mixer config remains teacher-forced and mixer-only; Block is stacked and enables mixer/FFN/block plus encoder/decoder-hidden alignment while every blank, conditional, sequence, and window logits objective remains exactly zero; Logits alone enables output-distribution objectives; labeled CTC SFT remains last. Block cannot start without a checkpoint and passing Mixer promotion receipt, and each promotion receipt binds the source gate and checkpoint hashes. The canonical current SFT support is the byte-fallback-safe `2,114`-ID set with SHA-256 `76a68d03bb2dc486c214fd44891f3e3e2c36286d79fea1e69c8e74d7767d5a09`, not the explicitly superseded `3,629`-ID set; the full labeled-data receipt and continuation watcher bind the corrected count and fingerprint, and composable rare-Chinese byte tokens remain available. The combined phase-objective, promotion, tokenizer, and final ordered-report regressions pass `10/10` in `42.27 s`; JUnit SHA-256 is `52f6fba3ff497c154617ff0bc663a631945f6fb9baa82be2404510db54b76361`. Verification leaves production at Layer/Hard step `836,010/966,315` and Supplemental base/public archive prefix `0..1500` without signaling either process.
- 2026-08-16 Stage211 student public-prediction provenance requirement: every future Layer, Block, Logits, and SFT five-dataset evaluation must emit a post-decode immutable receipt that binds the evaluated checkpoint path/SHA-256, `greedy_ctc`/bidirectional/CTC-normalization contract, all five canonical manifest paths/SHA-256 values, all five student prediction paths/SHA-256 values, exact per-dataset row counts, and the `52,431`-sample total. The phase comparison must bind this receipt by path and SHA-256, and phase-gate construction plus deep replay must reject a missing, changed, wrong-checkpoint, incomplete, or prediction-mismatched receipt. The pre-existing calibration comparison remains admissible as a legacy baseline only because its `52,431` raw predictions, manifests, normalized references, metrics, and checkpoint-bound reuse receipt have already been independently replayed; it must not relax provenance for newly decoded phase candidates. This closes the gap where a comparison report could name the current checkpoint while pointing at stale student predictions.
- 2026-08-16 Stage211 student public-prediction provenance verification: the shared phase finalizer now creates and immediately deep-validates one immutable receipt after all five student decodes finish, the Nano comparison binds its path/SHA-256, and Layer/Block/Logits phase construction, phase replay, full SFT, and SFT correction all require that candidate receipt. Regressions prove valid receipt reuse and reject a changed checkpoint, changed prediction file, missing candidate receipt, and a corrected checkpoint that retains the previous receipt; the complete Stage211 suite passes `305/305` in `331.15 s` with only the existing `14` upstream Torch deprecation warnings. The zero-failure/error/skip JUnit report is `/home/yueyulin/rwkvasr_runs/stage211_test_reports/stage211_public_prediction_provenance_full.xml` with SHA-256 `b76388f03ed2de297365fb2eaa204900d963fd69f1f5cd4065e40437aff53e01`; Ruff, Python compilation, and diff checks pass. A current-tree source replay of all `52,431` legacy calibration predictions still succeeds and reproduces English WER `0.4547559857` versus Nano `0.1391189949` and Chinese CER `0.7782510957` versus Nano `0.0640683244`. This CPU-only work leaves production ranks `250599--250602` unchanged while Layer/Hard advances to step `838,900/966,315` with complete online hidden matches and zero missing/frame delta.
- 2026-08-16 Stage211 progress-qualified correction budget revision: supersede the earlier eight-round operational cap for Layer, Block, and Logits because a still-improving round eight is not evidence that the current phase is good enough to promote or that optimization has plateaued. Preserve three unconditional complete one-epoch rounds over the exact `1,260,148`-row bilingual/difficulty-balanced replay, then require the existing immutable consecutive-gate progress decision before every additional round. Raise the restart-safe operational ceiling to `32` complete rounds, or `40,324,736` balanced row exposures if all are used. A passing phase gate still promotes immediately; a round with no strict improvement in trajectory candidate loss, five-dataset macro student error, or mean deeply bound alignment candidate loss still stops immediately with failed evidence. Reaching round 32 without passing remains an explicit failure rather than permission to advance. This revision changes neither per-round data, learning rate, trainable boundary, evaluation cadence, nor the separately bounded labeled-SFT correction policy; its purpose is only to prevent an improving Layer/Block/Logits phase from being terminated at the old arbitrary cap.
- 2026-08-16 Stage211 32-round progress-qualified correction verification: the shared Layer/Block/Logits constant, controller default, correction runner, receipt builder, correction-chain validator, and parser admission now consistently allow rounds `1..32`; an explicit regression proves an improving failed round eight emits a deeply bound decision for round nine. Plateau rejection after the three guaranteed rounds, immediate passing-gate promotion, complete one-epoch replay, and the independent three-round SFT limit remain unchanged. Focused correction tests pass `17/17`; the complete Stage211 suite passes `306/306` in `334.20 s` with only the existing `14` upstream Torch deprecation warnings. The zero-failure/error/skip JUnit report is `/home/yueyulin/rwkvasr_runs/stage211_test_reports/stage211_progress_qualified_corrections_32_full.xml` with SHA-256 `1bf4fa0fc904a7c2194457ed01601db953291e181d923aca1be7b3945e239445`; Ruff and diff checks pass. This changes no active process: Layer/Hard reaches step `839,720/966,315` with original rank PIDs `250599--250602`, complete hidden matches, zero missing/frame delta, while the Supplemental exact audit reaches archive prefix `0..1551`.
- 2026-08-16 Stage211A Hard step-840k fixed sentinel: the uninterrupted job emits the 84th required `10k` report with the same `256` samples, 70-layer coverage, feature seed `0`, manifest SHA-256 `754d73f4b0bc4d1da9a8d10423a86960bc46f9bb796a25a8d9d910d0dc950977`, and eval-part SHA-256 `9f4bf09cdbbf5bc963d6282a640e45fcd8da75f2e23a41e4515b6766288c84f1`; a direct replay verifies all `84/84` reports from step 10,000 through 840,000 with one provenance variant and no gap. Step 840,000 has fixed loss `0.1858882044`, mean 70-layer loss/cosine/RMS ratio `0.1821911030/0.9394279731/0.8636955959`. Versus step 830,000, fixed loss regresses `1.0851%`, mean cosine regresses `0.0005964`, only `15/70` layers improve either loss or cosine, and all seven ten-layer depth bands regress. Versus the Medium terminal checkpoint, fixed loss is `76.5902%` worse and mean cosine is lower by `0.0238889`; eleven layers remain below cosine `0.90`, led by layers `11/13/15/55/20`. The recent `800k..840k` fixed losses remain in the narrow `0.1839..0.1859` plateau, so this is persistent cross-domain forgetting rather than spontaneous retention recovery. Finish exact Hard/Long/Supplemental coverage unchanged, then require complete balanced replay and the hidden/public gates before Block.
- 2026-08-16 Stage211 downstream-correction objective re-audit: the shared post-coverage runner keeps the same frozen Nano MLP/norm, five-layer CTC decoder, and CTC-head boundary for Layer, Block, and Logits, but it constructs and fail-closed validates a phase-specific training contract rather than reverting downstream correction to the Layer objective. Block correction is stacked and uses mixer/FFN/block weights `0.25/0.25/1.0` plus encoder/decoder-hidden weights `0.5/0.5`, with every output-distribution weight zero. Logits correction is stacked and retains hidden anchors while enabling blank `0.25`, conditional nonblank `1.0`, hard conditional `0.125`, sequence `0.20`, and spike-window `0.25`; its correction LR remains `1.5e-7` versus `1e-6` for Layer/Block. The literal `trainable_boundary=mixer_only` in correction provenance therefore describes the intentionally shared parameter boundary, not a shared loss objective. Current-source correction, hidden/logits gate, promotion, and strict-runner regressions pass `163/163` in `304.92 s` with the existing `14` upstream warnings. This CPU-only audit leaves production rank PIDs `250599--250602` unchanged while Layer/Hard advances to step `841,130/966,315`; the exact Supplemental base/public scan advances through archive `1,571`.
- 2026-08-16 Stage211 CTC initialization/label-space handoff preflight: replay the production initialization command with the current source and reuse immutable receipt SHA-256 `b98ed6834925bd90e3329543b8a656f74137cb383fb3763003343c2c6182e093`. It still proves 70-layer Nano Q/K/V initialization into both BiRWKV directions, exact frozen equality for `564` Nano MLP/Norm tensors and `84` five-layer CTC-decoder tensors, zero mismatch across `648` audited frozen tensors, and the complete `60,516`-row project head with Nano blank row `60514` mapped to project blank `60515`. Independently deep-validate full-labeled receipt SHA-256 `f16b09e0aa6f76536bc42836bd0ec2ea7e07bb98af0177e5801d2e4f4e827858`: exactly `1,425,343` unique CTC-feasible pronunciation targets over `2,421.999431 h`, `21,711,995` CTC tokens, `37,506` optimizer steps, and zero unknown or forbidden/non-pronunciation target tokens under the canonical `2,114`-ID suppression set. Initialization, tokenizer, byte-fallback, target feasibility, immutable-receipt, and deep-label regressions pass `34/34` in `13.66 s` with only the existing `14` upstream Torch warnings. This closes the deferred vocabulary/blank/label-space risk before Block and Logits without changing the active production run, whose original ranks remain alive while Layer/Hard advances through step `842,440/966,315` with complete hidden matches and zero frame delta.
- 2026-08-16 Stage211 real-audio Nano CTC projection audit: run the current production online-teacher helper on LibriSpeech test-clean utterance `1089-134686-0000`, whose FLAC SHA-256 is `30885601173f96b0d8ddd020dc959b055c6c1582b85a33e3fcab8c4b08ed94c2`, through the actual FunASR-Nano encoder, five-layer CTC decoder, and `60,515`-row teacher head on CPU. The resulting `174` frames project into exactly `60,516` student rows: teacher blank `60514` is negative infinity on every projected frame, project blank `60515` is finite on every frame, and every one of the canonical `2,114` suppressed non-pronunciation IDs is negative infinity. Neither top-16 nor collapsed argmax contains a suppressed ID. After the production FP16 full-log-prob storage conversion, maximum absolute frame log-sum-exp is `0.0001623631`; the maximum FP16-versus-FP32 blank log-prob delta is `0.0038146973`, below the dtype-derived `0.0109352199` log tolerance. This real-model evidence complements the controlled projection/gradient tests and rules out a deferred blank/support mismatch before Logits or SFT; it uses no training GPU and leaves the original Layer ranks unchanged while Hard advances through step `843,300/966,315` with complete hidden matches and zero frame delta.
- 2026-08-16 Stage211 rotational-storage locality contract: the active Layer/Hard process is healthy but data-starved rather than TimeMixer-compute-bound. A live ten-second sample shows the four ranks reading about `9.3 MiB/s` while the required low-priority Supplemental PCM audit reads about `10.5 MiB/s` from the same read-only USB HDD; `vmstat` observes up to about `30%` I/O wait and GPU compute arrives in short bursts. The online Nano path is not rereading archive audio because formal configs use `ctc_teacher_online_use_batch_features=true`, and its measured forward time is only about `0.07--0.09 s/step`. The current fully shuffled Hard schedule changes length buckets on `309,045/322,104` adjacent batch boundaries (`95.9457%`), while source interleaving rotates after every batch; this defeats the per-worker eight-shard reader cache on rotational storage. Preserve exact per-bucket counts, rank partitioning, tail padding, three-epoch coverage, deterministic resume, and source coverage, but permit future-only configurable locality blocks: shuffle fixed-size runs of one length bucket, retain one source for a bounded number of those batches before rotating, and serialize indexed archive payload reads in schedule order while retaining parallel waveform decode/fbank work. Defaults must reproduce the historical one-batch shuffle/interleave and threaded-I/O behavior. Stage211 full-data Layer Supplemental, Block, Logits, and post-coverage corrections may request `64`-batch bucket/source locality and serialized payload reads; every fresh formal segment remains subject to the existing four-GPU objective/quality/memory throughput preflight. Do not signal, restart, or change the already running Layer/Hard process or the exact Supplemental audit.
- 2026-08-16 Stage211 rotational-storage locality verification: the default block sizes remain `1/1` with threaded payload reads and reproduce the historical seeded schedule exactly. Stage211 unlabeled full-data configs request `64/64` plus serialized reads, while labeled SFT remains unchanged. On the immutable Hard manifest, the new schedule still contains exactly `322,105` steps per epoch, but adjacent bucket changes fall from `95.9457%` to `1.5138%`; over the first `10,000` rank-zero batches, boundary shard changes fall from `99.7900%` to `37.2337%`. Indexed payload reads execute on one ordered producer per rank, while the existing worker pool continues parallel waveform decode and fbank extraction. Resume advances the same deterministic streams while skipping, and step/tail estimators remain independent of physical ordering. Data/entrypoint/Stage211 runner regressions pass `186/186`; throughput-profile, admission, correction, and DeepSpeed regressions pass `74/74` in `20.04 s`; Ruff and diff checks pass. The active Layer/Hard process and Supplemental audit were not signalled, and the new settings can enter production only through a fresh phase-bound four-GPU profile.
- 2026-08-16 Stage211 phase-specific bilingual gate contract: every Layer, Block, Logits, and SFT candidate must still decode and source-recompute normalized English WER and Chinese CER on all five canonical public datasets. Layer and Block promotion must combine their phase-primary hidden gate, intra-phase retention, and bilingual public progress versus that phase's exact initialization; they must not require final Nano proximity before the downstream Block/Logits objectives have run. Logits promotion must instead require its fixed/nine-cell logit-alignment gate, intra-phase retention, and the every-dataset `<=1.20x` plus `<=3`-point Nano CTC WER/CER gate. Centralize this phase-specific decision so report construction and deep replay cannot drift into different semantics, and add regressions proving a hidden-stage candidate may pass on real bilingual progress while still failing final Nano proximity, whereas the same final-proximity failure rejects Logits.
- 2026-08-16 Stage211A step-846k production parameter-boundary re-audit: compare the immutable Medium terminal initialization (`medium/step-1003713.pt`) with the current Hard export `hard/step-846000.pt` using the same completion-receipt tensor audit. Both checkpoints contain `12,697` tensors with identical shape/dtype metadata; `10,905` allowed TimeMixer/input-projection tensors (`567,936,000` elements) changed, `18` allowed tensors remained equal, all `1,774` frozen tensors remained bit-identical, and `forbidden_changed_tensors=0`. This independently confirms that the long-running Layer/Hard phase still has no trainability leakage into Nano-copied MLP/norm, convolution, five-layer CTC decoder, or CTC head parameters. The read-only audit does not load a GPU or signal training.
- 2026-08-16 Stage211 shared public-decision verification: phase-gate construction and deep receipt replay now call one shared phase decision. Focused objective/public/Block/Logits/frozen-boundary checks pass `9/9`, and the broader phase-gate plus promotion-receipt suite passes `39/39` in `186.25 s` with only the existing `14` upstream Torch warnings. The zero-failure/error JUnit report is `/home/yueyulin/rwkvasr_runs/stage211_test_reports/stage211_phase_public_decision_shared.xml` with SHA-256 `79fdbc6b07106f37c9b9ca5b4f81aaf9f7f7a69140884f86ec2e920ae05c1c84`; Ruff, Python compilation, and diff checks pass. This control-path-only change does not alter the resident Layer optimizer/data order or signal any production process.
- 2026-08-16 Stage211 direct final-logit alignment correction: the current Logits contract requires at least `5%` improvement in fixed and nine-cell full-vocabulary KL, but its training contract leaves `ctc_teacher_online_full_loss_weight=0` and optimizes only binary blank, teacher-active conditional nonblank, hard-token, sequence, and spike-window surrogates. That is an objective/gate mismatch. Stage186 proved that all-frame full-vocabulary KL at weight `1.0` preserves a student already on the Nano representation manifold, whereas Stage184/185 and the complete Stage210n easy epoch showed that sequence/window-heavy mixtures can move an initialized model away from Nano timing or plateau without closing the public CTC gap. The strict Layer and Block phases now provide the representation-first prerequisite that the earlier off-manifold Stage187/189 full-KL attempts lacked. Therefore make all-frame, temperature-`1.0`, full-vocabulary Nano CTC KL the primary Logits objective at weight `1.0` for both formal five-segment training and every post-coverage Logits correction. Retain binary blank `0.25`, conditional nonblank soft KL `1.0`, conditional hard CE `0.125`, and the existing low mixer/FFN/block/encoder/decoder-hidden anchors as anti-collapse and retention terms; set sequence CTC and spike-window losses to `0` because they intentionally permit timing displacement and conflict with exact final-distribution alignment. Mixer and Block must keep every output loss at zero, labeled SFT remains a later ground-truth phase, and no active Layer process may be signalled or reconfigured by this future-only correction.
- 2026-08-16 Stage211 projected-head inert-row audit: direct mmap inspection of the immutable Layer initialization and current Hard step `848,000` proves project row `60514` remains zero in every CTC-head weight element with BF16 bias `-9984`, while project blank row `60515` remains the copied finite Nano blank row (`weight L2 0.671526`, bias `0.020264`). The model also applies any configured CTC suppression set before returning logits; the pre-SFT `60514` row is already inert independently of that optional mask. Combined with the initialization receipt's zero mismatches over all `60,516` projected head rows and the real-audio teacher projection audit, this rules out the student-only ignored row as the cause of the current gap; the required Logits correction is direct full-distribution supervision, not another vocabulary remap or architecture change.
- 2026-08-16 Stage211 direct final-logit objective implementation verification: the shared phase contract and strict runner now set Logits all-frame full-vocabulary KL to `1.0`, sequence/window to `0`, and preserve blank/conditional-soft/conditional-hard weights `0.25/1.0/0.125` plus the existing hidden anchors. Mixer, Block, and SFT keep full KL at `0`, and the post-coverage Logits runner inherits the same corrected phase object with only its prescribed lower LR. Configuration regressions explicitly cover all four phase boundaries and Logits correction; full-logit projection, device-target, nonblank-weighting, conditional-soft, and conditional-hard loss/gradient tests pass `10/10`. The complete Stage211 suite passes `310/310` in `336.09 s` with only the existing `14` upstream Torch warnings; its zero-failure JUnit report is `/home/yueyulin/rwkvasr_runs/stage211_test_reports/stage211_direct_full_kl_full.xml` with SHA-256 `63b405291324658463db472bf9bf49fb09a85afb23bba1c1a85c498e164303e7`. Ruff, Python compilation, and diff checks pass. This is future-only: during verification the original Layer/Hard ranks `250599--250602` advanced naturally to step `849,420/966,315` with complete matches and zero missing/frame delta.
- 2026-08-16 Stage211 Logits memory-safe profile baseline contract: the automatic profile selector requires its named baseline to complete safely before it can admit any candidate. Keep the established `36/24k` baseline for Mixer and Block, but do not assume it fits the denser Logits graph after direct full-vocabulary KL is enabled. The historical Stage210n `12/8k` output objective already approached `19--20 GiB` per RTX 4090, so a failed `36/24k` Logits baseline could otherwise abort the entire third stage before a smaller profile is considered. Use a Logits-only `12/8k` named baseline, then test `24/16k`, `36/24k`, and the existing `48/42k` through `192/168k` candidates in ascending capacity. The baseline is a safety reference, not a forced production choice: the immutable four-GPU preflight must still select the fastest candidate with complete objective matches, zero missing/frame delta, peak reservation at or below `22 GiB`, same-distribution fixed-eval quality within the existing limits, and at least `10%` projected complete-coverage speedup. If every candidate fails, exact three-epoch Logits training may retain `12/8k`; if even that baseline fails, stop before formal optimizer state exists. Deep replay may continue accepting older explicitly requested `36/24k` Logits baselines for backward compatibility, but new automatic Logits profiles must use the safer search. This future-only admission change must not alter Mixer/Block batching or signal the active Layer run.
- 2026-08-16 Stage211 Logits memory-safe profile implementation verification: automatic profile defaults are now phase-specific. Mixer and Block retain the exact eight-profile `36/24k -> 192/168k` search, while Logits uses ten ordered profiles `12/8k`, `24/16k`, `36/24k`, then `48/42k -> 192/168k`. Deep replay accepts the new Logits baseline and the historical `36/24k` form but rejects `12/8k` for Mixer/Block, so the safety exception cannot leak into earlier phases. The four direct baseline/replay tests pass, the profile/admission/handoff/controller selection passes `29/29`, and the complete Stage211 suite passes `313/313` in `328.08 s` with only the existing `14` upstream Torch warnings. The zero-failure JUnit report is `/home/yueyulin/rwkvasr_runs/stage211_test_reports/stage211_logits_safe_profile_full.xml` with SHA-256 `1deebd018ee4ac46ec8bd1fdd33087c1031c98344968bc75bcd5a291cc5f3278`; Ruff, Python compilation, and diff checks pass. This is an admission-path proof rather than a fabricated GPU result: the real Logits profile will run only at its future idle-GPU segment boundary. During verification the original Layer/Hard ranks `250599--250602` advanced naturally to step `850,270/966,315` with complete teacher matches and zero missing/frame delta.
- 2026-08-16 Stage211 Logits full-objective runtime-evidence contract: a positive `ctc_teacher_online_full_loss_weight` in a generated YAML is necessary but not sufficient proof that the dense Nano target branch executed. The phase-bound batch preflight already derives required match fields from positive weights; update its regression to derive the complete current Logits contract and require `online_full_match` while no longer expecting disabled sequence/window matches. In addition, every fresh formal or post-coverage Logits two-step smoke must contain a terminal step-2 `online_full_match=N/N` event with `N>0`; a missing, zero, or partial field fails before formal optimizer state exists even when process exit, checkpoint, memory, and generic missing counters look healthy. Reused smoke markers must rebuild this evidence from their hash-bound source log. This requirement changes no loss value, data exposure, trainable boundary, or active Layer process; it closes only the possibility of silently admitting a Logits run whose primary full-vocabulary objective never executed.
- 2026-08-16 Stage211 Logits full-objective runtime-evidence verification: the batch-profile validator now derives the complete current Logits objective and requires blank, full-distribution, conditional-soft, conditional-hard, encoder, decoder-hidden, and layer match evidence while excluding disabled sequence/window objectives. Fresh and correction Logits smoke validation rejects absent, partial, or zero-total `online_full_match` evidence at step 2; complete positive evidence passes, and hash-bound reused markers are re-audited through the same path. Focused runtime checks pass `3/3`; the complete Stage211 suite passes `314/314` in `334.09 s` with only the existing `14` upstream Torch warnings. The zero-failure JUnit report is `/home/yueyulin/rwkvasr_runs/stage211_test_reports/stage211_logits_full_runtime_full.xml` with SHA-256 `c512ce5f71a9fbc7ead4e5a1ca3eac3841d00263c990e7dcfee42d784080d70d`; Ruff, Python compilation, and diff checks pass. This verification did not signal or restart the active Layer/Hard ranks.
- 2026-08-16 Stage211 selected-baseline formal-binding correction: `keep_baseline` means retain the phase-specific measured baseline, not necessarily the historical `36/24k` formal default. Mixer and Block still use `36/24k`, so their automatic controller may omit an admission when that exact profile is selected. Logits instead measures from the memory-safe `12/8k` baseline; if no larger profile is admissible, returning no admission would silently regenerate and launch `36/24k`, defeating the memory preflight and potentially OOMing before formal progress. Any selected Logits profile that differs from the historical default, including its safe baseline, must therefore receive an immutable phase/checkpoint/manifest-bound admission and be threaded into formal config generation. Admission serialization must preserve `selected_comparison=null` for a retained baseline, and deep replay must allow a preflight-proven Logits selection at or below `36/24k` while retaining the larger-than-legacy restriction for Mixer/Block candidate admissions. A failed `12/8k` Logits baseline still fails closed; this correction does not weaken memory, objective-match, fixed-eval-quality, coverage, or four-GPU evidence gates and must not affect the active Layer process.
- 2026-08-16 Stage211 selected-baseline formal-binding verification: admission creation and deep replay now validate a measured `keep_baseline` report without requiring a larger candidate, preserve its nullable comparison, and permit the phase-specific Logits `12/8k` profile. The automatic controller has an explicit routing predicate: only an exact retained historical `36/24k` profile may omit an admission; a retained Logits-safe baseline and every admitted candidate must produce and consume one. Focused routing tests pass `3/3`, related profile/admission/handoff tests pass `42/42`, and the complete Stage211 suite passes `316/316` in `327.97 s` with only the existing `14` upstream Torch warnings. The zero-failure JUnit report is `/home/yueyulin/rwkvasr_runs/stage211_test_reports/stage211_logits_safe_baseline_binding_full.xml` with SHA-256 `201a013a040d1fb83a9c5f47b1b27c7b292fee4319dd5f827cca1bca67600c4c`; Ruff, Python compilation, and diff checks pass. The active Layer/Hard ranks were neither signalled nor restarted and advanced naturally from step `852,070` to `852,750` during verification with complete online layer matches and zero missing/frame delta.
- 2026-08-17 Stage211 Supplemental retained-baseline handoff contract: the Long-boundary Supplemental profiler must distinguish a failed baseline from a safe baseline that remains faster than every admissible candidate. A failed `36/24k` Mixer baseline still fails closed before formal optimizer state, and any selected larger candidate still requires the immutable phase/checkpoint/manifest-bound admission. If the deeply replayed four-GPU preflight instead returns `keep_baseline` for the exact historical Mixer `36/24k` profile, that is a valid measured production decision: omit the optional admission, launch the formal Supplemental segment with the identical default profile, and continue through exact three-epoch coverage and all retention/public gates. Requiring a larger candidate in this case would deadlock Layer completion for lack of a speedup rather than for a memory, match, quality, or coverage failure. This correction is future-only and must not signal or reconfigure the active Layer/Hard ranks or weaken any downstream Layer, Block, Logits, or SFT gate.
- 2026-08-17 Stage211 Supplemental retained-baseline handoff verification: both fresh and reused Supplemental preflights are deeply replayed with candidate selection optional while baseline safety remains mandatory. A selected candidate still creates, validates, and threads its immutable admission; an exact retained Mixer `36/24k` baseline omits the optional argument so the formal controller uses that identical default, and a retained non-default profile fails closed. Focused routing checks pass `10/10`, the complete profile/admission pair passes `27/27`, and the complete Stage211 suite passes `318/318` in `330.59 s` with only the existing `14` upstream Torch warnings. The zero-failure JUnit report is `/home/yueyulin/rwkvasr_runs/stage211_test_reports/stage211_supplemental_retained_baseline_full.xml` with SHA-256 `01bbf531865d59b30b902bcebffb9ee9c4d7269d9934a603a7f98dbbd7b95aa0`; Ruff, Python compilation, and diff checks pass. Production ranks `250599--250602` and PCM audit PID `1038346` were neither signalled nor restarted; they advanced naturally to Layer/Hard step `853,760/966,315` with complete matches and zero missing/frame delta and through Supplemental archive `1,732/4,171`, respectively.
- 2026-08-17 Stage211 gate-directed correction-layer contract: a complete post-coverage replay must continue consuming every admitted bilingual/difficulty-balanced row for one epoch, but uniform eight-of-70 supervision is unnecessarily slow when the deeply validated failed gate identifies a small set of TimeMixer layers that did not improve. Build an immutable correction-focus receipt only from the exact failed admission gate and its SHA-bound fixed plus nine-cell alignment reports. Rank a layer by the number and severity of non-improving loss/cosine comparisons across every phase-required hidden component and evaluation scope; ties prefer the deeper layer because it is closer to the frozen decoder/head output path. Layer and Block corrections may pin at most five failed layers inside the existing eight-layer objective, leaving at least three deterministic rotating slots and therefore complete 70-layer coverage. Logits corrections must retain the eight declared weak/boundary anchors, may add at most three newly failed hidden layers inside the existing 12-layer objective, and must leave at least one rotating slot; if no hidden layer fails, preserve the phase's ordinary sampling schedule. Bind the selected and complete failed-layer sets, ranking evidence, rotating-slot count, admission gate, alignment report, and stratified summary into the focus receipt, smoke marker, train config, run provenance, correction receipt, and full-data replay validator. Rebuild the focus from the admission evidence on every validation so a forged or stale layer list cannot be admitted. This changes only future post-coverage correction sampling; it does not alter the active Layer run, any mandatory three-epoch full-data segment, Nano-copied frozen MLP/norm/convolution/decoder/head parameters, correction data exposure, or the independent normalized English-WER/Chinese-CER promotion gate.
- 2026-08-17 Stage211 gate-directed correction-layer implementation verification: Mixer and Block correction configs can pin at most five failed layers while preserving at least three rotating slots; Logits retains all eight hard anchors, can pin at most three additional failed layers, and preserves at least one rotating slot. Synthetic fixed plus nine-cell evidence proves deterministic severity/depth ranking, complete 70-layer selector coverage, unchanged uniform sampling when no hidden layer fails, and fail-closed replay when the focus artifact is modified. The focus path and SHA are now bound through smoke, config, run provenance, correction receipt, and deep phase-gate replay. The first full regression exposed and corrected an over-broad check that attempted to require hidden-layer focus for labeled SFT correction; focus is now explicitly limited to Mixer/Block/Logits, while SFT retains its existing lower-LR pronunciation-label objective. Focused correction/SFT checks pass `20/20`; the complete Stage211 suite passes `324/324` in `327.34 s` with only the existing `14` upstream Torch warnings. The zero-failure JUnit report is `/home/yueyulin/rwkvasr_runs/stage211_test_reports/stage211_gate_directed_layer_focus_full.xml` with SHA-256 `ffc2ffa1d9444d83c4dd06943783cc40a0ebbec1e9979cd95ace0f45b3f54406`; Ruff, Python compilation, and diff checks pass. The implementation is future-only and does not signal, restart, or reconfigure the active Layer/Hard training process.
- 2026-08-17 Stage211 trajectory-aware correction-focus extension: post-coverage correction is also the recovery path for intra-phase forgetting, so ranking only phase-initial fixed/nine-cell alignment failures can miss layers that still beat the phase initialization but regress sharply from the best prior curriculum terminal. Every correction focus must therefore inspect and bind the deeply validated trajectory-retention gate's exact best-prior and candidate terminal fixed-eval reports. When trajectory retention fails, compare every phase-required hidden component layer between those two reports and add non-improving loss/cosine signals under an explicit trajectory scope before deterministic ranking; when trajectory retention passes, bind and validate the same reports without adding failure signals. Preserve the existing pin limits, rotating slots, complete replay epoch, full 70-layer coverage, and independent bilingual public gate. This extension is future-only and must not alter or signal the active Layer/Hard process.
- 2026-08-17 Stage211 trajectory-aware correction-focus verification: focus construction now validates the trajectory phase/decision, entry coverage, best-prior and candidate indices, replayed embedded entries, terminal steps, report paths, and SHA-256 bindings. A regression in which every layer improves against phase initialization but layers `55/68/69` regress from the best prior proves that those three layers become the exact ranked correction targets under the `trajectory_retention` scope; a passing trajectory binds the same evidence without adding failure signals. Focused correction and phase-gate replay checks pass `22/22`; the complete Stage211 suite passes `325/325` in `330.11 s` with only the existing `14` upstream Torch warnings. The zero-failure JUnit report is `/home/yueyulin/rwkvasr_runs/stage211_test_reports/stage211_trajectory_aware_layer_focus_full.xml` with SHA-256 `3273975a4ea7502de68ef43d883864da555a5f716e8136a514d281886adfc5fe`; Ruff, Python compilation, and diff checks pass. The active Layer optimizer, process tree, data order, and checkpoint chain were not modified.
- 2026-08-17 Stage211 periodic fixed-eval cadence contract: promotion from Layer, Block, or Logits must prove the requested fixed-eval loss cadence from runtime artifacts, not only from `step_eval_every: 10000` in a generated config. For every ordered Easy, Medium, Hard, Long, and Supplemental full-data run, and for every admitted post-coverage correction run, derive the exact expected local-step sequence as every positive multiple of `10,000` through the run plus the terminal step when it is not itself a multiple. Require the cumulative `step_checkpoint_metrics.yaml` records and the `step_eval_layers_step-<step>.yaml` files to cover exactly that sequence with no duplicate, missing, or stale extra step; require finite non-negative loss, exactly `256` fixed-eval samples, the bound segment manifest, feature seed zero (retaining only the historical Mixer/Easy missing-field compatibility already admitted by trajectory validation), and SHA-bound fixed-eval parts totaling `256` rows. Bind the train config, cumulative metrics file, every per-step report, and the complete replayed source order into a top-level phase-gate audit. Deep gate validation must rebuild this audit from current files so deleting, changing, inserting, or renumbering any periodic evaluation blocks promotion. This audit is evidence for the existing fixed `10,000`-step loss schedule; it does not add evaluations, alter optimizer state, change five-segment three-epoch coverage, weaken terminal hidden/logit or bilingual WER/CER gates, rewrite completed immutable curriculum receipts, or signal the active Layer process.
- 2026-08-17 Stage211 periodic fixed-eval cadence implementation verification: phase-gate schema v3 now embeds and deeply rebuilds the exact cadence for all five curriculum segments plus every contiguous correction round. Validation binds each immutable source receipt, train config, manifest, cumulative metrics file, fixed-eval report path/hash, loss, epoch index, and fixed 256-row part fingerprint; it rejects a missing metrics record, missing report, stale extra report, report/metrics loss mismatch, wrong epoch, wrong manifest, non-finite/negative loss, or nonzero/missing feature seed outside the one historical Mixer/Easy exception. The final JSON and Markdown stepwise reports disclose interval, terminal step, report count, and sample count for every source in Layer, Block, and Logits. Direct replay of production artifacts proves exact Easy `3/3` and Medium `101/101` report coverage through steps `29,946` and `1,003,713`; live Hard naturally reached step `861,070` and had all `86` expected reports through `860,000`. Focused compatibility checks pass `45/45`; the complete Stage211 suite passes `326/326` in `341.87 s` with only the existing `14` upstream Torch warnings. The zero-failure JUnit report is `/home/yueyulin/rwkvasr_runs/stage211_test_reports/stage211_periodic_eval_cadence_full.xml` with SHA-256 `4236af83ae7e8bfbf1c68d7172b21b5f18170792fa3d75ac1ece606faa903e92`; Ruff, Python compilation, and diff checks pass. Rank PIDs `250599--250602` remained alive and were neither signalled nor restarted.
- 2026-08-17 Stage211 labeled-SFT periodic fixed-eval cadence contract: final four-stage completion must prove the labeled CTC SFT optimizer's existing fixed evaluation schedule from runtime artifacts, not only from the generated config or terminal checkpoint. The current schema-v2 full-labeled profile contains `1,418,201` train rows, `7,142` eval rows, `2,421.999431 h`, and one complete `37,506`-step epoch; it must evaluate the same deterministic `256`-row eval subset at every positive multiple of `2,000` through step `36,000` plus terminal step `37,506`, for exactly `19` reports. Derive these steps from the profile-bound terminal step rather than retaining the superseded `12,045`-step subset profile. Bind the SFT run directory, full-labeled manifest, train config, cumulative `step_checkpoint_metrics.yaml`, and every exact `step_eval_layers_step-<step>.yaml` report in the immutable SFT completion receipt. Require exact step order with no missing, duplicate, or stale extra report; finite non-negative report/metrics-equal loss; correct epoch index; manifest path/SHA; feature seed zero; and an unchanged SHA-bound eval-part fingerprint totaling `256` rows. Deep SFT completion validation must rebuild this evidence from current files, and the final JSON/Markdown report must disclose SFT beside Layer, Block, and Logits in the periodic-evaluation table. Missing or changed cadence evidence must fail SFT finalization even when one-epoch runtime coverage and bilingual WER/CER otherwise pass. This evidence-only extension does not add evaluations, alter the SFT objective, data exposure, checkpoint, or active Layer process.
- 2026-08-17 Stage211 labeled-SFT periodic fixed-eval cadence verification: the shared cadence auditor now accepts a phase-specific interval while retaining the exact manifest, config, cumulative-metrics, report-set, loss, epoch, deterministic feature-seed, and fixed-part checks used by Layer/Block/Logits. SFT completion schema v2 binds its run directory and embeds a deeply replayed single-source `labeled_sft` cadence derived from the schema-v2 profile's terminal step; current production inputs therefore require exactly `19` reports through step `37,506`, while the legacy test profile dynamically requires `7` through step `12,045`. Final JSON coverage and Markdown disclose the SFT cadence beside all three distillation phases, and the continuation watcher independently rejects a final proof with missing SFT cadence. Regressions reject a missing cumulative record and a report/metrics loss mismatch; focused SFT and watcher suites pass `18/18` and `19/19`. The complete Stage211 suite passes `327/327` in `335.98 s` with only the existing `14` upstream Torch warnings. The zero-failure JUnit report is `/home/yueyulin/rwkvasr_runs/stage211_test_reports/stage211_sft_periodic_eval_full.xml` with SHA-256 `b475369ae607a7634382950fa2be23ae9833aba5124221621405683dbba0feca`; Ruff, Python compilation, shell syntax, and diff checks pass. Production Layer training was not signalled, restarted, or reconfigured.
- 2026-08-17 Stage211 direct bilingual Nano-gap disclosure contract: the final requested-order result must make the normalized FunASR-Nano comparison explicit for each of `rwkv_layer`, `block`, `logits`, and `sft`, rather than requiring consumers to join a separate language-macro table. Derive one unweighted three-dataset English WER Nano reference and one unweighted two-dataset Chinese CER Nano reference from the same deeply replayed five public manifests/predictions used by every stage. Add those exact Nano rates and each stage's signed absolute error-rate gaps to the initial calibration record and every requested-stage record. Render stage, Nano, and signed point gaps together in the requested alignment Markdown table. The continuation watcher must reject a missing, non-finite, or arithmetically inconsistent gap before accepting the final proof. This is a disclosure and final-proof strengthening only; it must not replace per-dataset evidence, alter phase-specific promotion semantics, or signal the active Layer process.
- 2026-08-17 Stage211 direct bilingual Nano-gap disclosure verification: final report construction now derives `nano_english_wer`, `english_wer_gap_to_nano`, `nano_chinese_cer`, and `chinese_cer_gap_to_nano` for calibration plus every requested stage directly from the deeply replayed language-macro records. The requested-order Markdown table presents student rate, Nano rate, and signed point gap for both languages at each Layer/Block/Logits/SFT checkpoint. The continuation watcher independently requires every value to be finite, requires every stage to share the corresponding replayed Nano macro, and recomputes both signed gaps; deleting one gap is rejected. Focused report/watcher checks pass `6/6`; the complete Stage211 suite passes `328/328` in `341.03 s` with only the existing `14` upstream Torch warnings. The zero-failure JUnit report is `/home/yueyulin/rwkvasr_runs/stage211_test_reports/stage211_direct_nano_gap_full.xml` with SHA-256 `cb8d3ffb21699344d7d813dd2f331c6abd3e4b604096ead0833a8543cfff2190`; Ruff, Python compilation, shell syntax, and diff checks pass. The change is final-report-only and did not signal or alter production training.
- 2026-08-17 Stage211 final full-labeled-profile outer-gate contract: final Python reconstruction already deep-validates the schema-v2 profile receipt, but the independent continuation watcher must also reject a fabricated stepwise JSON that substitutes the superseded `285,302`-row profile after report generation. Require label proof schema v2, `all_accepted_unique_rows_required=true`, `source_language_interleave_required=true`, exact `1,418,201/7,142/1,425,343` train/eval/total rows, `1,425,343` unique/pronunciation/CTC-feasible rows, `21,711,995` CTC tokens, zero unknown/forbidden tokens, exact source counts `aishell3/commonvoice_cn/commonvoice_en/librispeech = 63,262/32,712/1,109,477/219,892`, and exact language counts `en/zh = 1,329,369/95,974`. Require SFT coverage to expose one epoch, `2,421.999431 h`, `1,418,768` executed exposures including `567` deterministic tail exposures, and the exact `37,506`-step/`19`-report cadence. Keep the receipt/path/hash and Python deep replay authoritative; these outer checks are a second fail-closed assertion of the currently admitted all-data contract, not a replacement for it. Any future profile expansion requires a versioned contract update rather than silently weakening these counts.
- 2026-08-17 Stage211 final full-labeled-profile outer-gate verification: SFT coverage disclosure now includes its formal optimizer-step count and deterministic tail-padding exposures. The continuation watcher independently requires the exact schema-v2 profile identity flags, receipt hash, total/train/eval/unique/pronunciation/feasible/token counts, four-source and two-language counts, one-epoch hours/exposures, `37,506` optimizer steps, `567` tail exposures, and `19` fixed-eval reports; replacing only the total with the legacy `285,302` count is rejected. Focused final-report/watcher checks pass `4/4`; the complete Stage211 suite passes `329/329` in `338.10 s` with only the existing `14` upstream Torch warnings. The zero-failure JUnit report is `/home/yueyulin/rwkvasr_runs/stage211_test_reports/stage211_full_labeled_outer_gate_full.xml` with SHA-256 `cc22a5bdaade5781b11bcb4efc67f9c08f45e7b5f75ea807d5db9c49d796174f`; Ruff, Python compilation, shell syntax, and diff checks pass. No production process was signalled or reconfigured.
- 2026-08-17 Stage211 corrected-SFT exposure disclosure contract: the final SFT checkpoint may be either the mandatory one-epoch full-labeled checkpoint or the checkpoint from one of at most three admitted balanced-label correction rounds. The final proof must distinguish these cases from the checkpoint chain itself. An uncorrected result must contain no correction artifact bindings and must report exactly zero correction rounds, rows, hours, steps, tail padding, and executed exposures. A corrected result must bind the passed correction evaluation, immutable correction profile, and exactly the contiguous round-`1..N` completion receipts; deep validation must replay every receipt, require a shared full-SFT completion/profile/Nano teacher, require each round to initialize from the preceding checkpoint, and require the final round checkpoint to equal the reported final checkpoint. Sum and disclose correction row/hour exposures, optimizer steps, deterministic tail-padding exposures, executed sample exposures, and per-language/per-source row exposures separately from the mandatory full-SFT traversal, then disclose effective totals without rewriting the base full-labeled profile. The final JSON, Markdown, and independent continuation watcher must reject hidden, missing, reordered, stale, or arithmetically inconsistent correction evidence. This is an evidence correction only: it does not permit correction before full SFT, increase the three-round limit, weaken the normalized three-English/two-Chinese Nano gate, or alter the active Layer process.
- 2026-08-17 Stage211 corrected-SFT exposure disclosure verification: correction-evaluation schema v2 now embeds a coverage object rebuilt from every deeply validated completion receipt. Replay requires contiguous full-SFT-to-round checkpoint initialization, one shared correction profile and Nano teacher, exact per-round profile rows/hours/steps/tail/executed counts, and a final correction checkpoint equal to the reported SFT checkpoint. Corrected final reports copy that object; stepwise validation distinguishes corrected from uncorrected checkpoints, rejects dangling evidence in the uncorrected case, and exposes base, correction, and effective totals plus per-round receipt hashes in JSON and Markdown. The independent watcher recomputes all aggregate equations and rejects a missing object or a forged extra row. Focused corrected/uncorrected/watcher checks pass `23/23`; the complete Stage211 suite passes `333/333` in `337.96 s` with only the existing `14` upstream Torch warnings. The zero-failure/error/skip JUnit report is `/home/yueyulin/rwkvasr_runs/stage211_test_reports/stage211_sft_correction_coverage_full.xml` with SHA-256 `2c62e85962cbd0240aa1844d4c28838145a800ddc44d3ecfe98ea44cd8730cb4`; Ruff, Python compilation, shell syntax, and diff checks pass. Production training was not signalled, restarted, or reconfigured.
- 2026-08-17 02:50 CST Stage211 Layer/Hard status and downstream-start audit: the uninterrupted four-rank TimeMixer run reached live step `866,730/966,315` (`89.6947%`) with persisted checkpoint `step-866000`, an observed recent rate of approximately `4,929` optimizer steps/hour, complete online layer matching, zero missing hidden states, and zero frame delta. The latest immutable Easy-ZH fixed sentinel remains step `860,000` at loss `0.1856174940`, versus the Hard initialization baseline `0.1052601582` (`+76.3416%`), so stable current-Hard losses around `0.09--0.11` do not constitute a Layer promotion result; the balanced all-depth retention replay and normalized public English-WER/Chinese-CER gate remain mandatory. The exact Supplemental/public normalized-PCM audit has contiguous receipts through archive index `1,918`, or `1,919/4,172` archives (`46.0%`), while the combined-v2 inventory/profile and its nine-cell/replay receipts remain pending. At the current measured Hard rate, Hard is projected to finish near `2026-08-17 23:00 CST`; the final Layer Supplemental traversal still requires its independently profiled full three epochs after combined-v2 publication. A source audit confirms every Layer, Block, and Logits controller derives exact profile-specific three-epoch steps for all five segments, and every phase finalizer decodes and compares the same hash-bound three English WER plus two Chinese CER public datasets against Nano. The armed Long-boundary handoff stops only the legacy Mixer controller after its Long child has started, creates the immutable Long receipt, waits for and validates combined-v2 plus both retention receipts, profiles and completes Supplemental, validates the full Mixer curriculum, then replaces the old August 9 supervisor with the current bootstrap at `START_STAGE=post_mixer`. Therefore stale in-memory shell functions cannot launch Block or Logits, and Block remains fail-closed until a selected Layer checkpoint and promotion receipt pass all hidden-retention and bilingual public gates.
- 2026-08-17 Stage211 public-set count chronology re-audit: the historical `53,905`-utterance records are correct for the original five-set suite containing all `16,396` Common Voice English test rows. The August 10 byte-level train/public overlap audit then excluded exactly `1,474` byte-identical Common Voice training recordings and installed the current clean `14,922`-row manifest, reducing the formal total to `52,431`; old status entries must retain their contemporaneous count. A current-source replay over all clean manifests plus Nano and calibration predictions validates the current Nano provenance receipt SHA-256 `58f2ee823ae617c5484747d0c0c8791dc8413e847cb6fb3d7125e9d39a624b16`, exact per-dataset coverage, zero normalized-reference mismatches, Nano/initial English-WER macros `0.139118994926/0.454755985697`, and Nano/initial Chinese-CER macros `0.064068324399/0.778251095709`. Every future Layer, Block, Logits, and SFT result remains bound to the clean `52,431`-utterance suite.
- 2026-08-17 Stage211 formal W&B observability contract: the active Layer/Hard rank-zero run is confirmed online rather than merely locally buffered; its W&B filestream continuously returns HTTP `200 OK` while recording train loss, per-component Nano alignment, fixed hidden evaluation, throughput/ETA, and system telemetry. Make this behavior explicit for every future formal `rwkv_layer`, `block`, `logits`, `sft`, and post-coverage correction run instead of relying on the inherited Stage210 configuration chain: formal configs must enable project `rwkvasr_longform_asr_gigaspeech_wenetspeech` and use their segment-specific run name. Keep two-step smoke runs and batch-throughput probes disabled in W&B so preflight data cannot contaminate formal curves. This is metrics/config/system telemetry only; raw training audio is not uploaded. The change applies only to newly generated configs and must not signal, restart, or mutate the uninterrupted Layer/Hard process.
- 2026-08-17 Stage211 formal W&B observability verification: the Stage211 config builder now explicitly sets `wandb_enabled=true` for every formal Mixer/Block/Logits/SFT segment, including formal post-coverage corrections through the shared builder, fixes the project to `rwkvasr_longform_asr_gigaspeech_wenetspeech`, and preserves the output-directory plus segment-specific run name. Every smoke config explicitly sets `wandb_enabled=false`; the existing throughput probe remains independently disabled. Parametrized tests cover all four phases in both formal and smoke modes. Focused configuration plus SFT/correction checks pass `42/42`; Ruff, Python compilation, and diff checks pass. The complete Stage211 suite passes `341/341` in `339.29 s` with only the existing `14` upstream Torch warnings; its zero-failure/error/skip JUnit report is `/home/yueyulin/rwkvasr_runs/stage211_test_reports/stage211_wandb_contract_full.xml` with SHA-256 `db422a11d6da44267994f913aa80c83abd1faa5d46c90fd84f8425ea0acc4806`. Controller PID `250320`, rank PIDs `250599--250602`, boundary handoff, hourly monitor, and continuation watcher remain unchanged; no production process was signalled, restarted, or reconfigured.
- 2026-08-17 Stage211 Layer/Hard step-860000 all-depth retention diagnosis: deeply compare the same seed-0, hash-bound 256-utterance Easy-ZH fixed features in the Hard initialization report (SHA-256 `71d5332ae30a83eb3d572f1a21da8c8ae979fc96bafdb12e1cb6a7fa3328461e`) and step-860000 report (SHA-256 `9cce69d9f2354666119798f6c83d1681a95bf853b36fb2362f520ebf553549d5`) under formal config SHA-256 `6cc1ab64064eb75399a5e235c354d5c6d265c5b08831d17a1b6ce9f32e67f26e`. Loss and cosine regress in all `70/70` layers, so the aggregate fixed loss change `0.1052601582 -> 0.1856174940` (`+76.3416%`) is broad cross-domain forgetting rather than one defective depth. Ten-layer-band mean loss changes are `+84.31/+71.20/+69.96/+60.71/+39.31/+93.20/+149.79%` from layers `0-9` through `60-69`; the largest relative losses are layers `68/64/65/69/53` at `+618.15/+351.53/+300.15/+217.75/+156.10%`, with strong RMS contraction in several of those layers. The fixed trajectory is already approximately flat at this degraded level from 700k through 860k (`0.178994 -> 0.185617`) and therefore cannot justify hoping that the remaining Hard tail alone will restore Easy-ZH. Preserve exact Hard/Long/Supplemental three-epoch coverage, then require the failed-gate correction path. Mixer correction fixes at most the gate-ranked five worst layers and reserves three rotating slots; with 70 layers and five anchors the selector covers every remaining layer within 22 optimizer steps, while the correction loop guarantees at least three complete one-epoch balanced-replay rounds and evaluates fixed, trajectory, nine-cell hidden, English WER, and Chinese CER evidence after each round. This diagnosis does not preselect the final five anchors because the complete nine-cell gate remains authoritative, and it does not modify or stop the active run.
- 2026-08-17 Stage211 Layer retention-correction exposure audit: the deeply validated immutable v1 replay receipt SHA-256 `0b1e389560b441d1a78f4cc5c26c5aa4693f8e476e575babc850fb7b42257749` binds `1,000,148` unique rows over `2,218.450178 h`, split almost exactly evenly as `500,000` English and `500,148` Chinese. It includes `130,000/130,000` Easy EN/ZH, `270,000/270,000` Medium EN/ZH, `100,000/100,000` Hard EN/ZH, and all `148` Long ZH rows; Easy-ZH alone contains `61,557` AISHELL-3, `26,804` Common Voice CN, and `41,639` CV22-ZH rows, so the degraded sentinel sources receive direct replay rather than only proxy-domain data. Under the immutable `batch=36/world=4/frame_budget=24,000` correction profile, the v1 manifest SHA-256 `305dcaa35e1a03681ba20519b88994c6397135112b2149ca6b0cb1769307b53e` yields exactly `10,135` optimizer steps and `1,420` deterministic tail exposures per one-epoch round. The guaranteed three rounds therefore provide `30,405` updates to each fixed failed-layer anchor and approximately `1,403` updates to every non-anchor through the three rotating slots. The pending v2 replay must retain all v1 rows and add exactly `130,000` Supplemental EN plus `130,000` Supplemental ZH rows selected by source and 80-frame bucket after nine-cell exclusion, raising the final replay to `1,260,148` rows; its exact hours, steps, tail padding, and hashes remain intentionally unclaimed until the combined-v2 inventory is materialized and deeply validated. This capacity proves meaningful correction exposure but not efficacy: promotion still requires measured recovery in fixed and nine-cell hidden alignment plus normalized three-dataset English WER and two-dataset Chinese CER, and rounds beyond three are allowed only while those replayed metrics make qualifying progress.
- 2026-08-17 Stage211 student-public CTC execution-provenance contract: the Layer, Block, Logits, and SFT public gates must prove that every normalized English-WER/Chinese-CER student row came from the exact candidate checkpoint through the project CTC path, rather than merely hashing an already-populated prediction file beside that checkpoint. Future formal public prediction invocations must embed one invariant schema-versioned execution record in every JSONL row: exact checkpoint path/SHA-256, model/tokenizer configuration and tokenizer-model hashes, project blank `60515`, CTC output size `60516`, tokenizer vocabulary `60515`, bidirectional mode, beam size `1`, zero decoder rescoring, zero blank bias, no hotwords, disabled LLM decoder, and the actual per-row `ctc_greedy` strategy. The Stage211 student receipt must require the same execution record across all `52,431` rows and five datasets, bind every prediction/manifest hash, verify token IDs exclude blank and remain in tokenizer range, and expose the execution record for deep phase/SFT replay. Missing or inconsistent provenance, Nano-format rows, calibration rows bound to another checkpoint, AR/rescored rows, or wrong blank/tokenizer contracts must fail before WER/CER promotion. Existing historical calibration evidence remains immutable; the stronger requirement applies to newly generated Layer/Block/Logits/SFT and correction evaluations and must not signal or modify the active Layer training process.
- 2026-08-17 Stage211 student-public CTC provenance implementation and verification: `predict_ctc_labeled` now optionally embeds the strict execution record, both sharded and non-sharded formal public runners enable it, and the schema-v2 student prediction receipt deeply validates one checkpoint/config/tokenizer binding before enforcing identical provenance, CTC-only greedy fields, tokenizer round trips, token ranges, debug lengths/probabilities, and alignment token IDs for every row. The verifier performs the expensive checkpoint/config validation once and then uses exact row equality, avoiding repeated checkpoint hashing across tens of thousands of rows. Regression tests reject missing/Nano provenance, another checkpoint, blank `60514`, and decoder-rescored rows; phase correction fixtures regenerate predictions for the corrected checkpoint instead of relabeling old files. Shell syntax, Ruff, Python compilation, diff checks, focused public/phase/SFT checks, and all `19` prediction tests pass. The complete Stage211 suite passes `346/346` in `437.14 s` with only the existing `14` upstream Torch warnings; its zero-failure/error/skip JUnit report is `/home/yueyulin/rwkvasr_runs/stage211_test_reports/stage211_student_ctc_provenance_full.xml` with SHA-256 `30a946550d7d63cf26b288cda2cb74a191bf23cfc80275635e5c184de3cb8bf9`. No production training process was signalled, restarted, or reconfigured.
- 2026-08-17 Stage211 Common Voice TSV-integrity correction contract: a full replay of the canonical Common Voice 22 English `test.tsv` proves it contains `16,401` physical utterance rows, while Python CSV quote interpretation used by the legacy public-manifest and overlap readers returned only `16,396`. One unmatched leading quote at `common_voice_en_41772182` caused six physical TSV rows to be parsed as one malformed reference, dropping `common_voice_en_154952`, `common_voice_en_26240905`, `common_voice_en_117312`, `common_voice_en_23428991`, and `common_voice_en_33006819`; the malformed reference also makes one future student row differ between one-pass manifest normalization and two-pass target-text metric normalization. Common Voice TSV is tab-delimited source data whose quote characters belong to transcript text, so all project readers must use literal `csv.QUOTE_NONE` semantics. Rebuild the full public manifest with all `16,401` unique audio IDs, rerun the exact encoded-byte train/public overlap audit, retain the independently established `1,474` overlap exclusions if replay confirms them, and install the resulting `14,927` clean Common Voice rows. Regenerate or reference-correct Nano and historical calibration predictions only through a versioned, hash-bound correction receipt, infer the five restored audios with the actual Nano/student CTC checkpoints, and rebuild reports, normalized metrics, public-baseline provenance, calibration reuse, and initialization bindings before any Layer promotion evaluation. The corrected five-set formal total is `52,436`; every future Layer, Block, Logits, and SFT student receipt must bind this corrected suite and preserve exact CTC target-token/text round trips. Legacy `52,431` results remain historical evidence only and cannot be used as a future promotion gate. This evaluation-data correction does not change, signal, restart, or weaken the active Layer training run.
- 2026-08-17 Stage211 corrected-public PCM migration: do not repeat the completed `25,419.427943 h` base or `1,790.823792 h` social PCM decode merely because five public utterances were restored. After the immutable v1 base audit finishes, create `stage211_base_public_pcm_overlap_v2` by hard-linking its validated location database and all `4,172` archive fingerprint JSONLs, rebinding every destination receipt, decoding the corrected `52,436`-row public suite, and replaying the original final overlap algorithm over all `6,434,286` base rows. Likewise create `stage211_social_vad_filtered_v2` by hard-linking and rebinding all `8,036` validated social source fingerprints, then rerun social duplicate/public filtering against the corrected suite. Rebase receipts must prove same-file inodes, source/destination receipt-set hashes, `audio_redecoded=false`, complete row/hour coverage, and zero public overlap before admission. Keep existing v1/v2 evidence immutable; bind the corrected outputs through `stage211_supplemental_combined_v3`, `stratified_hidden_eval_v3`, and `retention_replay_v3`. Install the canonical clean-v2 public artifacts and regenerate Nano provenance, calibration reuse, initialization, and Unicode metric correction only after the legacy PCM pipeline exits, so no in-flight old-public finalizer observes changed canonical paths. The future Layer boundary and every Block/Logits/SFT controller must wait for and deeply validate this v3 chain.
- 2026-08-17 Stage211 exact-coverage runtime audit: the formal Layer/Block/Logits segment contract has no metric-based or patience-based early-stop path. Every segment terminates only at its manifest- and profile-derived `max_steps = 3 * steps_per_epoch`; `length_bucket_drop_last=false` deterministically pads each bucket tail, while `skip_oversized_samples=false` and `webdataset_skip_decode_errors=false` convert otherwise skipped training rows into fail-fast errors. Completion receipts require the exact terminal checkpoint plus all three `epoch-{1,2,3}.pt` files and independently verify each epoch's step, zero terminal offset, and `completed_epoch_batch_count == steps_per_epoch`. Production Layer/Hard evidence at this audit records epoch 1 at step `322,105` and epoch 2 at step `644,210`, each with exactly `322,105` completed batches; `step-878,000` is correctly inside epoch 3 at offset `233,790`. Existing Easy and Medium receipts deep-validate complete three-epoch runtime coverage at `29,946` and `1,003,713` steps. Focused exact-coverage, resume, tail, decode-failure, receipt, and phase-gate checks pass `20/20` in `56.30 s`; the active four-rank Layer training was not signalled, restarted, or reconfigured.
- 2026-08-17 Stage211D production-profile revalidation: independently deep-validate the immutable schema-v2 receipt SHA-256 `f16b09e0aa6f76536bc42836bd0ec2ea7e07bb98af0177e5801d2e4f4e827858` against its length index and bucket manifest. The source filter supplies `1,430,801` rows; exact CTC normalization and canonical pronunciation support reject `5,458` rows containing non-pronunciation targets and retain `1,425,343` unique feasible rows with zero unknown or forbidden tokens. The formal split contains `1,418,201` train rows and `7,142` fixed-eval rows over `2,421.999431 h`; its one mandatory complete traversal is exactly `37,506` optimizer steps plus `567` deterministic tail-padding exposures. The receipt binds all `45` source/language interleave lanes, the canonical `2,114`-ID suppression fingerprint, and exact source counts. Full-profile, completion-chain, CTC-label-normalization, finalizer, and correction-coverage checks pass `7/7`; this validates future SFT readiness without starting it before the Logits promotion gate or modifying the active Layer process.
- 2026-08-17 Stage211 post-quote complete regression: run every `tests/test_stage211*.py` test after the literal Common Voice TSV repair, corrected `52,436`-utterance public suite, v3 Supplemental/retention defaults, exact-coverage audit, and full-labeled SFT profile revalidation. All `352/352` tests pass in `450.79 s` with zero failures, errors, or skips and only the existing `14` upstream Torch JIT deprecation warnings. The JUnit evidence is `/home/yueyulin/rwkvasr_runs/stage211_test_reports/stage211_post_quote_full.xml` with SHA-256 `c573dd4aeae5a0bb0403a932cc682274c4947f463ba2ddbcd85d7ce374175b50`. This proves the current orchestration and verifier code is internally consistent; it does not substitute for the still-pending production Layer, Block, Logits, and SFT checkpoints or their measured English-WER/Chinese-CER gates. Production training was not signalled, restarted, or reconfigured.
- 2026-08-17 Stage211 broad-failure correction contract: the Layer/Hard fixed Easy-ZH trajectory at step `880,000` measures hidden loss `0.1877234150` versus the Hard initialization `0.1052601582` (`+78.3423%`), with loss and cosine regression in all `70/70` layers; the last ten fixed evaluations also retain a small positive loss slope. This is broad curriculum forgetting, not a five-layer defect. Mixer and Block promotion require at least `68/70` layers to improve, so assigning five of eight correction sample slots permanently to the worst layers while only three slots rotate over the other 65 layers under-serves the gate when failures are broad. Make correction focus adaptive for Mixer and Block: derive the rotating-slot target as `max(3, ceil(8 * failed_layer_count / 70))`, cap selected fixed boundaries to the remaining slots and the existing five-layer limit, and use all eight slots for uniform full rotation when all 70 layers fail. As failures concentrate over successive gate evaluations, restore ranked fixed anchors automatically. Keep Logits hard anchors and its existing one rotating slot unchanged because those anchors protect the spike-sensitive CTC path. Bump the correction-focus evidence schema, record the failure breadth and computed slot allocation, and deep-rebuild it during validation. This changes only future post-coverage correction configs; it must not modify or restart the active full-data Layer traversal.
- 2026-08-17 Stage211 adaptive broad-failure correction implementation and verification: correction-focus schema v2 now records `failed_layer_count`, the configured and adaptive boundary limits, the minimum and adaptive rotating-slot targets, the actual rotating slots, and the resulting strategy. Mixer/Block retain the prior ranked five-anchor plus three-rotation behavior for six localized failures, use four anchors plus four rotating slots for 35 failed layers, and use zero anchors plus all eight rotating slots for 70 failed layers; the latter covers every layer within nine optimizer steps instead of leaving 65 layers with only three slots. Logits behavior remains unchanged and explicitly reports `static_hard_anchors` when no additional hidden failure is present. Every correction completion receipt directly summarizes the strategy and allocation, while deep validation compares each summary field with the hash-bound focus artifact and reconstructs that artifact from immutable failed-gate evidence; changing `failed_layer_count` and recomputing the outer receipt hash still fails closed. Focused receipt and correction tests pass, and the exact final code passes the complete Stage211 suite `358/358` in `462.04 s` with zero failures, errors, or skips and only the existing `14` upstream Torch warnings. JUnit evidence is `/home/yueyulin/rwkvasr_runs/stage211_test_reports/stage211_adaptive_correction_receipt_final.xml`, SHA-256 `4f9f523ea653690caf11b3ba0064041919ad1b104ea12c09d6df51f31a85f3f7`. The active Layer process was not signalled, restarted, or reconfigured.
- 2026-08-17 Stage211 guaranteed correction-round promotion contract: once a Layer, Block, or Logits full-coverage gate fails and enters post-coverage correction, execute at least `3` complete one-epoch balanced-replay rounds before promotion even if round 1 or 2 produces a provisional passing gate. Keep the passing metric evidence unchanged, but do not create a promotion receipt or selected-stage pointer before the guaranteed round count. A pre-guarantee provisional pass may admit only the immediately following correction round, must be hash-bound as such in correction provenance/completion evidence, and must never authorize downstream phase promotion. The round-3-or-later gate remains authoritative: promote only if it passes; if it fails, use the existing measured-progress extension rule up to the configured maximum. This rule does not force correction when the original full-coverage round-0 gate passes, and it must not signal, restart, or reconfigure the active Layer/Hard traversal.
- 2026-08-17 Stage211 guaranteed correction-round promotion implementation: phase-gate schema v4 separates the replayed objective result as `metric_gate_passed` from a hash-verifiable `correction_round_promotion` gate. With no correction, the original full-coverage gate remains eligible; after correction starts, completed rounds `1` and `2` are explicitly ineligible even when all hidden/logits and public WER/CER metrics pass, while round `3` and later are eligible. The final decision is the conjunction of those two gates, and the deep validator reconstructs both from bound correction receipts. The correction supervisor independently rejects any unexpectedly passing pre-guarantee report, so neither a malformed report nor a stale controller can emit an early promotion receipt. Focused policy, phase-gate, correction-chain, tamper, and loop tests pass `25/25`; the exact final code passes the complete Stage211 suite `367/367` in `464.47 s` with zero failures, errors, or skips and only the existing `14` upstream Torch warnings. JUnit evidence is `/home/yueyulin/rwkvasr_runs/stage211_test_reports/stage211_guaranteed_correction_rounds_full.xml`, SHA-256 `ecd4f5cef0931ae886a8767e443da8e40b2320a80eb7c2d4c4e02b2a2d7ebe72`. No formal phase gate or promotion receipt existed before the schema upgrade, and the active Layer/Hard process was not signalled, restarted, or reconfigured.
- 2026-08-17 Stage211 final correction-promotion disclosure contract: the strict final stepwise JSON and Markdown must expose the deeply validated correction-round promotion evidence for each ordered Layer, Block, and Logits result, not only aggregate correction exposure. Rebuild the policy from the phase coverage correction count during final-report generation, require equality with the source phase-gate evidence and a passing eligibility result, and render whether correction was not entered or how many complete rounds satisfied the three-round minimum. A final report that omits, changes, or contradicts this evidence must fail generation even when its WER/CER numbers are otherwise present.
- 2026-08-17 Stage211 final correction-promotion disclosure implementation: final stepwise coverage reconstruction now derives each Layer, Block, and Logits correction count from the deeply validated full-data coverage, rebuilds the required promotion policy, requires exact equality with the source phase-gate evidence, and rejects any ineligible policy. The JSON preserves that evidence per ordered stage, while Markdown renders either `not entered; eligible` or the completed/required correction rounds. A targeted tamper test proves that changing the reported completed-round count fails closed. The exact final code passes the complete Stage211 suite `368/368` in `477.87 s` with zero failures, errors, or skips and only the existing `14` upstream Torch warnings. JUnit evidence is `/home/yueyulin/rwkvasr_runs/stage211_test_reports/stage211_final_correction_promotion_disclosure_final.xml`, SHA-256 `2c1c8f187094d10a061590982d13599a9e09148a6ef2073a40599b62965779f6`. The active Layer/Hard process was not signalled, restarted, or reconfigured.
- 2026-08-17 Stage211 stratified gate-evaluation parallelism contract: the nine immutable `256`-sample hidden/logits cells are independent model evaluations and must no longer run serially on one GPU at every Layer, Block, Logits, and post-coverage-correction gate. Resolve the formal evaluation device set from the existing four-device public-evaluation assignment unless an explicit stratified override is supplied, collocate each cell's Nano teacher and student on its assigned device by default, isolate its teacher-audio cache, and execute deterministic device-width waves (`4 + 4 + 1` on the target host) with at most one cell process per GPU. Preserve cell order, manifests, seeds, sample counts, checkpoint bindings, immutable report paths, and summary/gate semantics exactly. Do not launch the summary until every cell succeeds; on any non-zero child exit, terminate the remaining children in that wave and fail closed without public evaluation or promotion. Dry-run output must expose every exact command and device assignment. This evaluation-only optimization must not signal, restart, or reconfigure active training, and any wall-time claim remains provisional until measured on a production phase gate.
- 2026-08-17 Stage211 stratified gate-evaluation parallelism implementation: the phase finalizer now derives stratified devices from the existing public `--devices` value by default, normalizes `0,1,2,3` to `cuda:0,cuda:1,cuda:2,cuda:3`, supports validated singular or plural overrides, and rejects empty, duplicate, conflicting, or teacher/student-count-mismatched assignments. It binds each cell's Nano teacher to the same assigned GPU unless an exact parallel teacher list is supplied, gives every cell an isolated cache directory, and launches the nine commands in deterministic device-width waves before running the unchanged summary. A child failure or parent interruption terminates all still-running peers and prevents downstream summary/public/promotion work. Tests prove the target `4 + 4 + 1` wave shape, exact round-robin device commands, teacher collocation, isolated caches, override rejection, and peer termination. Ruff, formatting, Python compilation, six focused finalizer checks, a `186`-test related regression, and the exact full Stage211 suite pass; the latter is `371/371` in `478.45 s` with only the existing `14` upstream Torch warnings. JUnit evidence is `/home/yueyulin/rwkvasr_runs/stage211_test_reports/stage211_parallel_stratified_full.xml`, SHA-256 `49fa4d660c301517b7591e769a70e914c5df9eb1f4b2e3d9b401afd677a51244`. The optimization has not yet been wall-time measured on a production phase gate, and the active Layer/Hard process was not signalled, restarted, or reconfigured.
- 2026-08-17 Stage211 direct-CTC metric-content contract: formal Nano/BiRWKV public WER/CER must not apply the generic AR-only language-confirmation-prefix removal. Direct greedy CTC has no LLM response wrapper, so a decoded leading `this is English text` or `这是中文文字` is recognition output and must be scored rather than silently discarded. Every Stage211 public comparison and deeply replayed benchmark must explicitly bind `strip_language_confirmation=false`; missing or true values fail before phase promotion. Keep the generic text-metric default available for AR evaluation, but pass the direct-CTC setting explicitly through Nano report generation, public comparison, source recomputation, summary metrics, Common Voice derivation, phase-reference replay, calibration/SFT evidence, and the final stepwise report. A complete scan of the corrected `52,436` references, `52,436` Nano predictions, and `52,436` calibration predictions found zero rows changed by the legacy prefix stripper, so this correction is expected to preserve all current Nano/calibration WER/CER values while preventing a future BiRWKV CTC prefix from receiving unearned metric credit. The corrected Nano targets remain per-dataset corpus metrics: LibriSpeech test-clean WER `7.9028%`, test-other WER `11.9636%`, Common Voice English WER `21.8127%`, AISHELL-1 CER `4.1827%`, and WenetSpeech TEST_NET CER `8.6310%`; the report may expose unweighted language macros (`13.8931%` English WER and `6.4068%` Chinese CER) only in addition to, never instead of, all five primary dataset results.
- 2026-08-17 Stage211 direct-CTC metric-content implementation: the CTC target normalizer now exposes a backward-compatible `strip_language_confirmation` control so label preparation keeps its default non-pronunciation cleanup while direct-CTC metric normalization can preserve decoded content. Nano inference, student comparison, canonical/decomposition replay, prediction receipts, calibration/SFT evidence, Unicode correction, derived clean reports, phase gates, final stepwise output, and the restart-safe final watcher all bind `false`; missing or true comparison/benchmark contracts fail closed. A regression proves that `this is English text wrong` against `wrong` is scored as four WER insertions instead of zero error. Re-scanning the corrected public bundle with the fixed lower-level normalizer found zero affected rows independently in every manifest text, Nano reference, Nano prediction, calibration reference, and calibration prediction field across all five datasets, and recomputation preserved Nano corpus metrics exactly at AISHELL-1 CER `0.0418269460`, LibriSpeech test-clean WER `0.0790284540`, LibriSpeech test-other WER `0.1196362386`, Common Voice English WER `0.2181271915`, and WenetSpeech TEST_NET CER `0.0863097028`. The complete Stage211 plus normalization/evaluation regression passes `417/417` in `471.11 s` with only the existing `14` upstream Torch warnings. JUnit evidence is `/home/yueyulin/rwkvasr_runs/stage211_test_reports/stage211_direct_ctc_metric_full.xml`, SHA-256 `123ffd1a69e12102c56e969b45b4b2274d1e8ebd31a22bd921e8d2bc4b743831`. The active Layer/Hard training process was not signalled, restarted, or reconfigured.
- 2026-08-17 Stage211 correction-stall patience contract: supersede the single-round post-guarantee stall rule for Layer, Block, and Logits. A deterministic evaluation removes sampling noise from metric computation, but one complete balanced-replay optimizer epoch can still move non-monotonically while the gate-directed layer focus changes across rounds. After the mandatory three correction rounds, continue until either the unchanged phase gate passes, the existing `32`-round hard cap is reached, or `3` consecutive complete correction rounds fail to improve every progress signal relative to the immediately preceding gate. Keep the progress signals unchanged: trajectory candidate loss, five-dataset macro student error, and mean deeply bound alignment candidate loss; any strict improvement resets the consecutive-stall count to zero. A first or second consecutive non-improving round authorizes only the next correction round and never authorizes phase promotion. Encode every round-3-or-later extension as a schema-v2 immutable decision that binds the preceding decision by path and SHA-256, recursively replays its gate/metric chain, and records the stall patience, prior streak, current streak, and next-round result. The third consecutive non-improving decision fails closed and remains evidence; reaching the hard cap without a passing phase gate also fails. This changes neither the minimum/maximum correction rounds, one-epoch replay exposure, LR, trainable boundary, WER/CER or hidden/logit gate thresholds, nor the active Layer/Hard process. Its purpose is only to prevent a temporary one- or two-epoch plateau from terminating a still-unfinished alignment stage.
- 2026-08-17 Stage211 correction-stall patience implementation and verification: the shared extension decision is now schema v2 and records the fixed patience `3`, prior/current consecutive non-improvement counts, unchanged three-metric values, and the previous extension path/SHA. Round three starts the chain; every later decision recursively revalidates all earlier decision and failed-gate bindings before it can admit another complete replay epoch. Tests prove that one and two consecutive plateaus continue, the third stops, any qualifying improvement resets the streak, progress can still extend beyond round eight toward the hard cap, a forged prior decision fails its SHA binding, and the supervisor honors the bounded stop without creating promotion evidence. Focused correction tests pass `27/27`; Ruff, formatting, Python compilation, and diff checks pass. The exact final code passes the complete Stage211 suite `374/374` in `459.34 s` with zero failures, errors, or skips and only the existing `14` upstream Torch warnings. JUnit evidence is `/home/yueyulin/rwkvasr_runs/stage211_test_reports/stage211_correction_stall_patience_full.xml`, SHA-256 `bc996edc0a1e5dfc9e14a4ff597b1b5d6652b90f96bb2c54094abd5771946fcc`. During verification the original Layer/Hard ranks `250599--250602` remained unchanged and advanced naturally to step `894,400/966,315`; no training process was signalled, restarted, or reconfigured.
- 2026-08-17 Stage211 short-Long boundary sentinel contract: replace the ad-hoc `10 s` process poll used to intercept the legacy Mixer controller at the Long-to-Supplemental boundary. Long has only `105` optimizer steps, so observing only a live Long child leaves a finite race in which the child can finish between polls and the stale August-9 controller can enter its obsolete Supplemental defaults. Use a checked-in, restart-safe sentinel with a sub-second default poll that first validates the exact live legacy Mixer controller identity, detects its direct `--difficulty long` child, and also recognizes an exact loadable Long `step-105.pt` terminal checkpoint as a fallback after the child exits. On either condition send `SIGSTOP` only to the legacy parent controller, verify its Linux state is stopped, allow an already-running Long child and all of its ranks to finish naturally, then `exec` the current-tree profiled boundary handoff. Never signal the active Hard/Long training child or ranks. Reject multiple matching children, a changed controller command, malformed/stale Long checkpoint, or a controller that exits before a valid Long boundary. Log which trigger was used and preserve the existing handoff behavior: create/deep-validate Long evidence, wait for combined-v3 and retention-v3, profile and train Supplemental for three epochs, validate complete Layer coverage, and replace the stale supervisor at `post_mixer`. This is an orchestration race fix only; it changes no model, optimizer, data exposure, or phase gate.
- 2026-08-17 Stage211 short-Long boundary sentinel implementation and verification: add `scripts/watch_stage211_profiled_boundary.py` with exact `/proc` controller/direct-child validation, a `0.25 s` default poll, dual live-child and exact-terminal-checkpoint triggers, parent-only `SIGSTOP` plus stopped-state verification, natural Long-child completion waiting, and `os.execv` into the current virtual-environment Python handoff. Unit tests synthesize Linux process trees to prove Hard is ignored, exactly one Long child is selected, ambiguous Long children fail, only the controller receives `SIGSTOP`, step `105` is mandatory, both triggers route through the same handoff, and current interpreter/readiness arguments are preserved. Sentinel plus existing handoff tests pass `15/15`; Ruff, formatting, Python compilation, and diff checks pass. The exact final code passes the complete Stage211 suite `381/381` in `458.84 s` with zero failures, errors, or skips and only the existing `14` upstream Torch warnings. JUnit evidence is `/home/yueyulin/rwkvasr_runs/stage211_test_reports/stage211_short_long_boundary_sentinel_full.xml`, SHA-256 `c91e9a0f8eaf5d5a9a8c3d12893cf1bea7783e84fd388023b15a3ba090b31b22`. Production activation replaces only the old observational boundary tmux session; it must leave controller PID `250320`, Hard child PID `250519`, torchrun/ranks `250590/250593/250599--250602`, and every optimizer state untouched.
- 2026-08-17 08:45 CST Stage211 short-Long sentinel production activation: replace only tmux session `rwkvasr_stage211_batch_profile_boundary`; the checked-in sentinel is live as PID `1500052` with validated legacy controller PID `250320` and a `0.25 s` boundary poll. Immediately after replacement, the unchanged Hard child/torchrun/rank chain `250519/250590/250593/250599--250602` remained live and advanced naturally to step `895,580/966,315` with no restart or optimizer interruption. The sentinel log records the exact validated Mixer command and has no boundary trigger yet. This sub-second `/proc` identity check is not a training/evaluation monitor and performs no audio, checkpoint, GPU, or USB reads until Long begins or its exact terminal checkpoint appears; hourly training-status reporting remains unchanged.
- 2026-08-17 Stage211 Block runtime-objective evidence contract: the future Block phase must prove that its configured stacked alignment graph actually executed before any formal optimizer state exists. A generated YAML with positive Mixer `0.25`, FFN `0.25`, full Block `1.0`, encoder `0.5`, and five-layer CTC-decoder hidden `0.5` weights is insufficient if a hook or routing regression silently returns no effective target. In the exact terminal step-2 smoke line, require complete positive-total `online_layer_match`, `online_encoder_match`, and `online_decoder_hidden_match` counts with matched equals total; require finite non-negative `online_layer_mixer`, `online_layer_ffn`, `online_layer_block`, `online_ctc_encoder`, and `online_ctc_decoder_hidden` telemetry, with the primary full-Block loss strictly positive. Preserve the same principle for Logits by requiring every currently positive Nano objective's match count and finite non-negative loss telemetry, including a strictly positive all-frame full KL; do not require disabled sequence/window terms. Complete match counts prove that an auxiliary branch executed even when a correctly aligned finite loss rounds to `0.0000`. Embed the parsed step, exact required match counts, and active loss values in the hash-bound smoke marker and rebuild them from the source log on every reuse and phase-gate validation. Mixer historical smoke compatibility may remain unchanged because the current formal Layer run predates this future-stage admission rule; Block and Logits have no formal progress or smoke marker yet and must not receive a legacy exception. This strengthens runtime evidence only and changes no loss weight, model parameter, batch profile, data exposure, promotion threshold, or active Layer process.
- 2026-08-17 Stage211 Block/Logits runtime-objective evidence implementation and verification: the full-phase smoke audit now reconstructs the exact terminal step-2 training line, requires complete match coverage for every active hidden/logit branch, validates finite non-negative telemetry for every configured objective, requires a strictly positive primary Block or full-logit loss, and embeds the parsed evidence in the immutable marker so reuse and phase-gate replay remain source-bound. Tests reject a missing Block FFN branch, a zero primary Block loss, and incomplete Logits full-vocabulary matching. Ruff, formatting, Python compilation, and diff checks pass; the exact complete Stage211 suite passes `382/382` in `463.79 s` with zero failures, errors, or skips and only the existing `14` upstream Torch warnings. JUnit evidence is `/home/yueyulin/rwkvasr_runs/stage211_test_reports/stage211_block_runtime_objective_full.xml`, SHA-256 `5f31ac5b1067f603df6584f0d3d646d84ea292c20a9736dd8487dd34072afca0`. The active Layer/Hard controller, torchrun, and rank processes were not signalled, restarted, or reconfigured.
- 2026-08-17 Stage211 Logits memory-frontier profile contract: retain the measured-profile admission mechanism and the `12/8k` memory-safe Logits baseline, but close the unmeasured capacity gap before the existing `24/16k` candidate with ordered `16/10k` and `20/13k` candidates. Historical full-vocabulary Logits memory near `19--20 GiB` makes it plausible that `24/16k` exceeds the `22 GiB` admission ceiling while a smaller candidate remains safe and materially faster. On the real Hard manifest, exact three-epoch coverage is `3,136,296` steps at `12/8k`, `2,500,626` at `16/10k`, `1,851,843` at `20/13k`, and `1,475,727` at `24/16k`; on the existing base Supplemental manifest it is `942,849`, `779,037`, `578,349`, and `463,668`. These are coverage projections only, not admissions. Every candidate must still run the exact Logits graph for `20` warmup plus `100` measured steps, complete the same immutable `256`-sample fixed evaluation, prove every active objective match with zero missing/frame delta, stay at or below `22 GiB`, preserve the existing loss/cosine limits, and improve projected complete three-epoch wall time by at least `10%`. Selection remains the fastest admissible measured profile, all rows remain covered exactly three times with tail padding recorded, and no profile may affect the active Layer process.
- 2026-08-17 Stage211 Logits memory-frontier profile implementation and verification: the phase-specific default now searches `12/8k -> 16/10k -> 20/13k -> 24/16k -> 36/24k` before the unchanged larger candidates, while Mixer and Block retain their existing profile tuple. The two new candidates pass exact coverage recomputation and remain subject to the existing immutable four-GPU admission rather than being enabled by configuration alone. Focused profile/admission/handoff/smoke tests pass `52/52`; Ruff, formatting, Python compilation, and diff checks pass. The exact complete Stage211 suite passes `382/382` in `477.42 s` with zero failures, errors, or skips and only the existing `14` upstream Torch warnings. JUnit evidence is `/home/yueyulin/rwkvasr_runs/stage211_test_reports/stage211_logits_memory_frontier_full.xml`, SHA-256 `653943f0ddc737965550705b1b6c1b9978f2b7075d20253b7de0dd87ba09c1e4`. The active Layer/Hard ranks retained PIDs `250599--250602` and advanced naturally from step `896,880` to `898,220`; no training or data process was signalled, restarted, or reconfigured.
- 2026-08-17 Stage211 combined-v3 exact-reuse binding contract: an existing, deeply valid combined Supplemental inventory must not be reused merely because its internally recorded component files remain valid. Before reuse, require its base-natural inventory, normalized-PCM base/public audit, normalized-PCM social inventory, USB top-level coverage receipt, and archived-social exact-duplicate receipt to match the current build request by both resolved absolute path and SHA-256. Reject a stale or alternate complete output before returning it, including the case where an alternate requested path currently has byte-identical content. Preserve the initial atomic-directory publication, the existing production handoff serialization, all source admission and dedupe policies, and the active Layer optimizer state. This closes a restart-only provenance gap before combined-v3 is produced; it does not add, remove, or rescan audio.
- 2026-08-17 Stage211 combined-v3 exact-reuse binding implementation and verification: the combined inventory builder now resolves all five requested provenance inputs before either creation or reuse, deeply validates an existing output, and then requires every internally recorded path and SHA-256 to equal that exact request. Five parameterized regressions prove that byte-identical alternate paths for the base inventory, base/public audit, social inventory, USB coverage receipt, and archived-social exclusion are all rejected, while an exact repeat remains idempotent. Ruff, Python compilation, diff checks, focused tests, and the exact final complete Stage211 suite pass; the latter is `387/387` in `461.11 s` with zero failures, errors, or skips and only the existing `14` upstream Torch warnings. JUnit evidence is `/home/yueyulin/rwkvasr_runs/stage211_test_reports/stage211_combined_exact_reuse_final.xml`, SHA-256 `1015a36a02eae51eb15a6e2539af4d68891a8b146e973d076582d2a59a13b627`. The unchanged Layer/Hard rank PIDs `250599--250602` advanced naturally through step `900,710/966,315`; no training or data process was signalled, restarted, or reconfigured.
- 2026-08-17 Stage211 corrected-public PCM rebase exact-reuse contract: the base and social normalized-PCM rebase outputs for the corrected `52,436`-utterance public suite are restartable only when they bind the current request exactly. Base reuse must match the requested base-natural inventory, completed source audit, corrected five-manifest path/SHA/row map, and destination audit; social reuse must match the requested source filtered inventory, corrected five-manifest path/SHA/row map, and destination filtered inventory. Deep self-validation of an older rebase is necessary but not sufficient because an internally consistent `52,431`-row artifact must never satisfy a later `52,436`-row request. Reject byte-identical alternate source or manifest paths as different provenance. Keep source PCM/archive fingerprint files hardlinked and do not decode source training audio again; only corrected public fingerprints and overlap joins may be rebuilt on first creation. This must land before either production v2 rebase directory exists and must not affect active Layer training.
- 2026-08-17 Stage211 corrected-public PCM rebase exact-reuse implementation and verification: both rebase entrypoints now resolve the requested source and public manifests before checking for prior output. Base reuse requires the exact base inventory through the source-audit binding, the exact source-audit path/SHA, and every corrected public manifest path/SHA/row count; social reuse requires the exact source filtered-inventory path/SHA and the same complete corrected public map. Both also retain their prior deep destination and hardlink validation. Focused normalized-PCM tests pass `15/15` and prove exact idempotent reuse plus rejection of byte-identical alternate base inventory, source audit, social inventory, public manifest, and changed row maps. Ruff, Python compilation, diff checks, and the exact complete Stage211 suite pass; the latter is `387/387` in `463.22 s` with zero failures, errors, or skips and only the existing `14` upstream Torch warnings. JUnit evidence is `/home/yueyulin/rwkvasr_runs/stage211_test_reports/stage211_rebase_exact_reuse_full.xml`, SHA-256 `d8c1cfaf7a404c1909b85b4f8943f88a1d2bec464c5b92b86884fbb3f78dbd40`. Neither production v2 output existed when the code landed, and the unchanged Layer/Hard ranks `250599--250602` advanced naturally through step `901,820/966,315` without any signal, restart, or reconfiguration.
- 2026-08-17 Stage211 exact five-dataset public-decision contract: every reusable `public_benchmark` validator, not only the deeper source-comparison replay, must require exactly one result for each corrected public dataset: LibriSpeech test-clean/test-other and Common Voice English WER plus AISHELL-1 and WenetSpeech TEST_NET CER. Reject extra, duplicate, non-object, missing, or unexpected result records before dictionary indexing can hide them. Each result must carry boolean `absolute_gate_pass`, `relative_gate_pass`, and `gate_pass` values; the combined result must equal the conjunction of its two threshold decisions, and top-level `all_datasets_pass` must be a boolean equal to the conjunction across all five results. Keep source prediction replay authoritative for recomputing the numeric rates and decisions, but make the standalone benchmark contract equally fail-closed because final stepwise, SFT, SFT-correction, calibration-correction, and reuse validators consume it independently. This strengthens report validation only and changes no WER/CER threshold, normalization, training objective, data exposure, active Layer process, or promotion order.
- 2026-08-17 Stage211 exact five-dataset public-decision implementation and verification: the shared public-benchmark validator now rejects any result list that is not exactly the five unique corrected datasets, requires boolean absolute/relative/combined decisions, replays the `3.0`-point and `1.20x` thresholds from the numeric gaps, and requires the top-level decision to equal their every-dataset conjunction. Seven focused regressions cover duplicate, extra, non-object, each inconsistent per-dataset decision, and an inconsistent aggregate decision. Ruff, Python compilation, diff checks, the targeted final-report chain test, and the exact complete Stage211 suite pass; the latter is `394/394` in `474.52 s` with zero failures, errors, or skips and only the existing `14` upstream Torch warnings. JUnit evidence is `/home/yueyulin/rwkvasr_runs/stage211_test_reports/stage211_exact_public_decision_full.xml`, SHA-256 `459117c2d1e868a1f13061e01149b98002dc8594302b70346a47222ed9f3e0b8`. The unchanged Layer/Hard rank PIDs `250599--250602` advanced naturally through step `904,310/966,315`; no training or data process was signalled, restarted, or reconfigured.
- 2026-08-17 Stage211 corrected-public canonical-install exact-reuse contract: before the first corrected `52,436`-utterance canonical installation, make reuse fail closed and bind it to the exact current request. An existing `canonical_install_receipt.json` must have the complete Stage211 schema/artifact contract; bind the exact requested overlap receipt path/SHA, the exact clean-root derivation receipt path/SHA and its five-dataset derivation replay, the exact twelve canonical destination paths and current SHA-256 values derived from the requested manifest/Nano/calibration roots, and the exact requested archive receipt path/SHA plus matching embedded archive payload. Require the corrected Common Voice sample count of `14,927`, reject duplicate or non-object installed-file entries, and reject byte-identical alternate overlap, clean, canonical, or archive paths as different provenance. If any existing receipt differs, raise instead of treating the state as a fresh install. This is installation/reuse hardening only; it does not alter the corrected predictions or metrics, training inputs, model, optimizer, active Layer process, or phase order.
- 2026-08-17 Stage211 corrected-public canonical-install exact-reuse implementation and verification: canonical install reuse now first replays the requested overlap and clean derivation, requires the exact receipt location and provenance paths/SHA-256 values, reconstructs the exact twelve requested canonical destinations, rejects duplicate/non-object/changed records, verifies the requested archive receipt and every rollback file, and requires its embedded archive payload to match byte for byte. An existing invalid receipt now raises instead of falling through to a fresh installation. The Common Voice quote-repair test is independently runnable and its `9/9` focused cases pass, including six new exact-reuse regressions for duplicate destinations, byte-identical alternate overlap/clean/canonical/archive paths, and changed embedded archive evidence. Ruff, Python compilation, diff checks, and the exact complete Stage211 suite pass; the latter is `400/400` in `474.92 s` with zero failures, errors, or skips and only the existing `14` upstream Torch warnings. JUnit evidence is `/home/yueyulin/rwkvasr_runs/stage211_test_reports/stage211_canonical_install_exact_reuse_full.xml`, SHA-256 `50aa2de9b10a4c30464ab0f1c863bb60954f56f89dbe8d92c38a2a201da65ef5`. The unchanged Layer/Hard rank PIDs `250599--250602` advanced naturally through step `905,650/966,315`; no training or data process was signalled, restarted, reprioritized, or reconfigured.
- 2026-08-17 Stage211 correction batch-profile contract: post-coverage correction must not bypass the phase-specific measured batch admission used by formal Block and Logits coverage. The current correction runner hard-codes `batch_size=36` and `frame_budget=24000` for every phase, even though full-vocabulary Logits has a measured-safe baseline of `12/8000`; this can make the first Logits correction smoke or formal batch OOM before its one-million-row replay begins. Before any formal optimizer step in every Mixer, Block, or Logits correction round, benchmark the exact phase objective, failed-gate checkpoint, retention-v3 manifest, and four-GPU runtime with the existing ordered phase-specific candidates and unchanged `22 GiB`, match-completeness, fixed-eval, loss/cosine, and projected-wall-time gates. Bind the immutable preflight and any selected admission into the exact correction config, two-step smoke marker, provenance, completion receipt, and downstream deep validator. Recompute the one-epoch correction step count and tail padding from the selected batch/frame profile; the benchmark's three-epoch projection remains only the common profile-comparison horizon and must not increase correction exposure beyond exactly one replay epoch. A resumed correction may reuse only an exact checkpoint/manifest-bound complete profile; it must fail closed if formal progress exists without that evidence. This changes no BiRWKV architecture, Nano-frozen parameter boundary, objective weight, correction LR, guaranteed correction-round count, replay rows, WER/CER gate, active Layer process, or phase order.
- 2026-08-17 Stage211 correction batch-profile implementation and verification: the shared full-phase helper now measures an exact phase/scope/base-config/checkpoint/manifest profile and returns both replayed preflight evidence and any formal admission. Every Mixer, Block, and Logits correction command enables this four-GPU preflight on a separate port before smoke or optimizer state, binds the selected profile through smoke/config/provenance/schema-2 receipt/deep phase-gate validation, and derives the exact one-epoch steps and tail padding from that profile. Mixer/Block may omit admission only for a measured `keep_baseline` result at the legacy `36/24000`; Logits always requires an admission, including its measured-safe `12/8000` retained baseline. Focused correction tests are `28/28`; controller/profile/deep-gate tests are `212/212`; Ruff, Python compilation, diff checks, and the complete Stage211 suite pass. The complete suite is `401/401` in `471.19 s`, with only the existing `14` upstream Torch warnings; JUnit evidence is `/home/yueyulin/rwkvasr_runs/stage211_test_reports/stage211_correction_batch_profile_full.xml`, SHA-256 `7175024b7eef766631d524cc25dbed79dddd0b200d8163743be7877d12a0c107`. The unchanged Layer/Hard rank PIDs `250599--250602` advanced naturally through step `909440/966315`; no training or data process was signalled, restarted, reprioritized, or reconfigured.
- 2026-08-17 Stage211 Long-boundary process-reaping contract: the profile-boundary sentinel deliberately stops only the legacy Mixer parent while allowing its already-running Long child to finish. Because a stopped parent cannot reap that completed direct child, Linux may retain the Long PID in zombie state and therefore retain `/proc/<pid>`; waiting only for path disappearance can deadlock before Supplemental v3 readiness/profile handoff. Treat `Z` or `X` as terminal for this specific validated Long child, then require the exact `step-105.pt` payload before executing the handoff. All other live states continue waiting, an unreadable state while the process still exists fails closed, and no Hard/controller/training signal is allowed during watcher replacement; only the independent pre-boundary sentinel may be reloaded before Long begins.
- 2026-08-17 Stage211 Long-boundary zombie-reaping implementation and verification: `_wait_for_exit` now distinguishes a live child from an unreaped terminal child, returns for `Z/X` or a fully removed `/proc` entry, continues polling all other states, and propagates an unreadable status while the PID directory still exists. Four regressions cover immediate `Z`, immediate `X`, live-to-zombie transition, and fail-closed unreadable state; the focused sentinel/handoff suite is `19/19`. Ruff, Python compilation, diff checks, and the complete Stage211 suite pass; the latter is `405/405` in `456.38 s` with only the existing `14` upstream Torch warnings. JUnit evidence is `/home/yueyulin/rwkvasr_runs/stage211_test_reports/stage211_long_zombie_handoff_full.xml`, SHA-256 `5130e17004edde1bc4f67e290f093f80d95d83fc402c2f07b59049f78dbee72f`. Before Long existed, only the independent sentinel session was reloaded from PID `1500052` to PID `1547804`; controller `250320`, Hard child `250519`, and rank PIDs `250599--250602` remained live and unsignalled. Layer/Hard advanced naturally through `910500/966315`; its 910k fixed-eval loss is `0.1891940379`, versus `0.1882855903` at 900k, so no Block promotion claim is made and post-coverage Mixer correction remains required unless the complete final gate proves otherwise.
- 2026-08-17 Stage211 corrected-public phase-entry readiness contract: publishing combined-v3 after the corrected public installation serializes production creation, but a restarted `post_mixer`, `block`, `logits`, or `sft` supervisor must not trust that ordering indirectly or defer a damaged public-evaluation chain until the final stepwise report. Before any phase branch runs, perform a read-only deep replay of the corrected `52,436`-utterance public chain: require the exact clean canonical-install receipt and `14,927`-row Common Voice manifest, the Unicode direct-CTC metric correction and every installed-file hash, a byte-identical reconstruction of the calibration reuse receipt from the five canonical manifests/metrics/comparison plus normalized-PCM overlap proof, the exact five-dataset Nano baseline provenance against that rebuilt benchmark, and the Nano QKV/MLP/five-layer-decoder/head initialization receipt bound to the same calibration reuse and Nano checkpoint. Validation must not install, rewrite, infer, train, or otherwise mutate public/model artifacts. Run this barrier after combined-v3/retention-v3 readiness and before dispatching any `START_STAGE` branch, so Layer correction and all later English WER/Chinese CER gates can only use the corrected benchmark chain.
- 2026-08-17 Stage211 corrected-public phase-entry readiness implementation and verification: the Unicode correction entrypoint now has a write-free `--validate-only` mode that deeply validates the exact canonical installation, exact installed artifact set, direct-CTC correction, source-rebuilt calibration reuse, corrected five-dataset/`52,436`-utterance benchmark, Nano baseline checkpoint provenance, and initialization-to-reuse binding. The Stage211 bootstrap executes this barrier after combined-v3/retention-v3 validation and before every `START_STAGE` dispatch, including `post_mixer`, so a stale `52,431`-row chain cannot reach Layer correction or Block. A focused test proves complete replay and byte-identical inputs before/after validation; the supervisor source regression proves barrier ordering and exact correction/Nano receipt forwarding. Focused checks pass `11/11`; Ruff, shell syntax, and diff checks pass; the exact complete Stage211 suite passes `406/406` in `470.52 s` with only the existing `14` upstream Torch warnings. JUnit evidence is `/home/yueyulin/rwkvasr_runs/stage211_test_reports/stage211_corrected_public_readiness_full.xml`, SHA-256 `879762ff9ed1224aefa8f29cb634e50d4b270428f4a8d5612f4794ce3babfacb`. The unchanged Layer/Hard rank PIDs `250599--250602` advanced naturally through step `913,290/966,315`; no training process was signalled, restarted, or reconfigured.
- 2026-08-17 Stage211 future loader-worker calibration contract: the measured batch/frame preflight must not silently inherit an unmeasured `num_workers=8` per rank on a host with only eight physical CPU cores and four training ranks. For every not-yet-started profiled Mixer-Supplemental, Block, Logits, and post-coverage correction scope, first compare the base profile at its configured worker count with exactly one CPU-balanced profile at `max(1, physical_cores // world_size)` workers per rank while keeping batch size, frame budget, checkpoint, manifest, optimizer objective, 20-step warmup, 100-step timing window, and immutable 256-row fixed evaluation identical. Select the balanced worker count for the remaining larger batch/frame probes only when that worker-only candidate passes the existing complete-match, zero-missing/frame-delta, `22 GiB`, fixed-eval loss/cosine, and at least `10%` projected three-epoch wall-time gates; otherwise retain the configured worker count. This adds one probe rather than a worker/batch Cartesian product. Every resolved profile must include a positive integer `num_workers`, and the selected value must be hash-bound through the probe config/report, admission, formal config, smoke marker, provenance, curriculum/correction receipt, and deep replay. Worker calibration may change decode concurrency only; it must not change row order, batch/frame coverage accounting, three-epoch exposure, phase objective, trainable boundary, or the strict `rwkv_layer -> block -> logits -> sft` order. It is future-only and must not signal, restart, or reconfigure the active Layer/Hard ranks.
- 2026-08-17 Stage211 future loader-worker calibration implementation and verification: measured-preflight schema `5` now derives affinity-constrained logical/physical CPU topology, adds exactly one same-batch/same-frame CPU-balanced worker candidate, applies the existing objective-match/memory/fixed-eval/`10%` wall-time gates, and propagates the selected worker count to all later candidates. Admission schema `2`, formal segment and correction configs, smoke markers, provenance, schema-2 curriculum/correction receipts, boundary handoff, and deep phase-gate replay all bind the positive integer `num_workers`; explicit custom profiles remain explicit and the dry run does not infer an unmeasured selection. A real CPU-only Hard dry run against the active config found `16` logical CPUs, `8` physical cores, and the expected `2` workers per rank candidate while preserving every original coverage target and creating no retained probe run directory. Focused correction/profile/handoff checks pass, Ruff, Python compilation, and diff checks pass, and the exact complete Stage211 suite passes `408/408` in `461.72 s` with only the existing `14` upstream Torch warnings. JUnit evidence is `/home/yueyulin/rwkvasr_runs/stage211_test_reports/stage211_loader_worker_full.xml`, SHA-256 `05e860346424e0cbf164a7ab554ac1bcf9e3eda030a9eb1bc5e6e58dd68ee7bd`. The active Layer/Hard ranks were not signalled, restarted, or reconfigured.
