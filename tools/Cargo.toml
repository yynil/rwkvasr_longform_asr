[package]
name = "rwkvasr-tools"
version = "0.1.0"
edition = "2021"

[dependencies]
candle-core = "0.9.2"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[profile.release]
lto = "thin"
codegen-units = 1
strip = false
